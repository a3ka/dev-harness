# Контракт 003 — скилы Мэтта: четыре адаптации и встройка в воркфлоу

Номер выдан механизмом: `next_id.sh CONTRACT` → 003, тег `id/CONTRACT/003`. План —
`plans/008-skills-metta-adaptacija.md`; решения гриллинга Q1–Q16 — в его коммитах и сессии
(три раунда, живой прогон).

## Предмет

Взять из `mattpocock/skills` снимок `9c9f36ccd3995266cd675468af71639c8dde1ec5` (2026-08-17)
ЧЕТЫРЕ скила и встроить их в воркфлоу так, чтобы роли знали, КОГДА что открывать:

1. `grilling` — интервью владельца до общего понимания: дерево решений, раунды по фронтиру,
   вопрос с рекомендованным ответом, факты — саб-агентом, решения — владельца. Обёртка
   `grill-me` НЕ отдельный каталог: у Мэтта это роутер «Call the Skill tool with grilling»
   (7 строк); вызов `grill-me` документируется в шапке-адаптации `grilling` как псевдоним.
2. `writing-for-agents` — грамматика документов для агентов: указатели, две нагрузки,
   иерархия информации, leading words, запрет отрицаний, выбраковка no-op'ов.
3. `tdd` — red → green цикл: швы, вертикальные срезы, антипаттерны (tautological,
   horizontal slicing).
4. `diagnosing-bugs` — дисциплина «тугого красного контура»: 10 способов построения, затяжка,
   недетерминизм через частоту.

Отказы остальных 29 — с причинами — в плане 008.

## Критерий готовности

1. **Состав и подлинность.** `skills/` содержит РОВНО каталоги `grilling`,
   `writing-for-agents`, `tdd`, `diagnosing-bugs` — множество проверяется барьером дословно.
   Тела — английские оригиналы снимка `9c9f36c`; шапка-адаптация на русском несет ИМЕННО этот
   hash. `bash scripts/check_skills.sh` → 0 — НОВЫЙ барьер, ВОСЕМЬ ветвей отказа:
   (а) скил не обнаружен omp — ЖИВОЙ пробой, отдельной командой `check:skills-live`
      (`check_skills.sh --live`): из корня репозитория omp (БЕЗ --no-tools) читает
      skill://<имя> инструментом read, ответ цитирует ДОСЛОВНО строку шапки «# Адаптация: …»,
      которой в запросе НЕТ; код вызова обязан быть 0. Ответу можно верить только от бинаря,
      чей sha256 совпадает с пином `config/harness_pin.json` репозитория: расхождение —
      отказ, отсутствие omp — код 2, НЕ зелёный пропуск. Базовый `check:skills` ветвь (а)
      не заявляет вовсе. Остаточный риск `cognitive-only`: в клоне можно подменить и бинарь,
      и пин; ловцы — анти-плацебо (читает объявленное окружение фикстур) и подлинность
      бинаря в честном прогоне (`check:overlay`);
   (б) имя каталога ≠ имени фронтматтера;
   (в) hash шапки ≠ значению из блоба высшей заморозки ЭТОГО контракта (не зашит в барьер);
   (г) каталог вне объявленного множества четырёх;
   (д) зеркало разложения: `.omp/skills/<имя>` — полное дерево `skills/<имя>`, `diff -r` в обе
      стороны, состав каталога точен. omp 17.2.10 читает ПРОЕКТНОЕ `./.omp/skills` (замер
      2026-08-18: из него модель процитировала дословно «# Источник: 9c9f36c…» через
      `skill://tdd`); прежняя цель `$HOME/.omp/agent/skills` не читается вовсе — пути нет в
      бинаре (Н-31 в NABLIUDENIA);
   (е) тело не совпадает со снимком upstream: `cmp` репозиторного `skills/<имя>/SKILL.md`
   с raw.githubusercontent.com/mattpocock/skills/<hash>/skills/<категория>/<имя>/SKILL.md,
   где <категория> — productivity для grilling и writing-for-agents, engineering для tdd и
   diagnosing-bugs (пути upstream с категориями — замер арбитража 5: путь без категории даёт
   404); <hash> — из блоба высшей заморозки контракта, как в ветви (в). Сверяется ТЕЛО ПОСЛЕ
   шапки-адаптации: граница — первая строка фронтматтера upstream `---`, всё от неё —
   байт-в-байт; шапка-адаптация и её русский текст во владении адаптации. Сеть есть локально
   и в CI; недоступность — код 2 с причиной, не зелёное;
   (ж) псевдоним `grill-me` не документирован в шапке `grilling` (grep по «grill-me»);
   (з) полнота красных фикстур: на РОДИТЕЛЕ первого коммита по `skills/` и
   `scripts/check_skills.sh` каталог `fixtures/check_skills/` уже содержит фикстуры ВСЕХ
   объявленных ветвей — мера полноты, а не только первенства (одна ранняя фикстура и шесть
   поздних не проходят).
2. **Указатели в ролях — гибрид, мерой поимённо.** Для КАЖДОЙ пары из таблицы ниже
   соответствующий файл роли обязан содержать строку, где `skills/<имя>/SKILL.md` и ведущее
   английское слово из скобок стоят ВМЕСТЕ; посторонних `skills/`-строк в ролях нет. Проверка
   командой на пару: `grep -cE "skills/<имя>.*<слово>|<слово>.*skills/<имя>" <файл роли>` →
   ровно 1, иначе код 1. Русское условие строки — `cognitive-only`, ловец ревьюер.

   | роль | скил (leading word) |
   |---|---|
   | architect | grilling (frontier), writing-for-agents (context pointer), tdd (tautological), diagnosing-bugs (tight loop) |
   | implementer | tdd (red → green), diagnosing-bugs (tight loop) |

   Качество русских условий — `cognitive-only`, ловец ревьюер.
3. **Красные фикстуры ВСЕХ ВОСЬМИ ветвей (а)-(з) — архитектор ДО раздачи, порядком коммитов.**
   Фикстуры ВСЕХ восьми ветвей (а)-(з) лежат в коммитах архитектора, и ПЕРВЫЙ коммит по
   `fixtures/check_skills/` старше ПЕРВОГО коммита по `scripts/check_skills.sh` и `skills/`.
   Мера: `git log --reverse --format=%an -- fixtures/check_skills/ | sed -n 1p` → `architect`
   и `git log --reverse --format=%h -- fixtures/check_skills/ skills/ scripts/check_skills.sh |
   sed -n 1p` указывает на фикстурный коммит — раньше ЛЮБОГО коммита по скилам и барьеру →
   иначе код 1. Ветви, найденные при реализации,
   исполнитель ПОДАЁТ заявкой; красную фикстуру к ней пишет архитектор в своей зоне —
   исполнитель в `fixtures/` не пишет вовсе (zone-согласовано, см. ниже).
4. **Зеркало коммитится, omp обнаруживает.** Полное дерево `skills/` лежит зеркалом в
   `.omp/skills/` репозитория и КОММИТИТСЯ — прецедент `.omp/agents` из `roles/`: порождённое
   лежит рядом с источником, дрейф ловит барьер, чистый чекаут получает скилы без прогонов.
   Раскладка при изменении `skills/` — `cp -r skills/. .omp/skills/`; дрейф держит ветвь (д).
   Обнаружение доказывает `check:skills-live` на машине с запиненным omp (ветвь (а));
   в CI бинаря нет — объявленное исключение паритета по прецеденту `check:overlay`.
   `check:skills` в приёмке и CI; `check:skills-live` в приёмке.

## Исполнители и зоны

ЗОНА implementer: skills/ .omp/skills/ package.json .github/workflows/ci.yml scripts/overlay.sh config/
ЗОНА architect: contracts/003-skills-metta-adaptacija.md plans/008-skills-metta-adaptacija.md NABLIUDENIA.md HANDOFF.md fixtures/check_skills/ fixtures/check_zones/ decisions/ roles/architect.md roles/implementer.md scripts/check_skills.sh scripts/check_zones.sh
СПАСЕНО architect: b5b9ebccd77103c674dfbe87bff2792883ed5507 a5e0e221794b73093995c7642ca18e1cef4dc837 — спасение работы исполнителя из таймаута (Н-29), слово владельца 2026-08-18

Связность правок (шесть прецедентов Н-22 учтены): каталог скилов, зеркало, проводка и
overlay — исполнителю (один механизм доставки целиком); роли, план, контракт, фикстуры
барьеров, записи решений и грамматика СПАСЕНО в `check_zones.sh` — архитектору (красные
ДО раздачи по Q8-C). Пересечений зон нет: спасённые коммиты выведены из судьбы зон НЕ
расширением зоны, а поимённой строкой СПАСЕНО — ровно эти два хеша и никакие другие;
будущие правки четырёх путей архитектором снова красные. `.omp/agents/` не задействован.

## Правка v3 (2026-08-18)

Три правки, все — следствия измерений; слово владельца записано в сессии 2026-08-18:

1. **Критерий 4 и ветви (а)/(д): цель разложения перенацелена** с `$HOME/.omp/agent/skills`
   на проектное `.omp/skills`. Причина: три круга адверсария по ветви (а) кончились находкой
   «эхо argv проходит», а расследование измерило сам инструмент — эфемерный `omp -p` не
   регистрирует скилов ни в одной из семи форм запуска; бинарь знает `.omp/skills` и не знает
   `.omp/agent/skills`; из проектного `.omp/skills` модель процитировала дословно неугадываемый
   hash «# Источник: 9c9f36c…» (Н-31). Прежняя зелёность ветви (а) была эхом промпта даже на
   честном omp.
2. **Зоны: пересечение легализовано** — коммиты b5b9ebc/a5e0e22 (спасение из таймаута, Н-29).
3. **overlay больше не раскладывает скилы** — раскладка в непрочитываемое место удалена;
   зеркало `.omp/skills/` коммитится по прецеденту `.omp/agents`.

## Правка v4 (2026-08-18, ночь)

Ответы на три блокирующие находки критика по v3 (`verdicts/critic/contracts-003-v3.md`):

1. Оракул ветви (а) переведён в живую пробу `check:skills-live` с пином: ответу можно
   верить только от бинаря с sha256 из `config/harness_pin.json` — подставной omp с честным
   пином даёт отказ, а не зелёное. Пол объявлен `cognitive-only`, ловцы названы.
2. Зелёный пропуск без omp удалён: базовый `check:skills` ветвь (а) не заявляет;
   `check:skills-live` без omp отвечает 2; в CI — объявленное исключение паритета.

3. Легализация зон переделана с расширения зоны на грамматику
   `СПАСЕНО <автор>: <полные хеши через пробел> — <причина>`: поимённые коммиты
   исключаются из судьи зон, будущие правки остаются красными. Механизм — в
   `scripts/check_zones.sh` (зона архитектора в этом контракте), красные фикстуры — в
   `fixtures/check_zones/`.

## Заимствование и версионирование (Q12/Q13)

Снимок `9c9f36c…` фиксируется hash'ем в шапке каждой адаптации и в `decisions/008`.
Обновление снимка: правка в зоне + новый hash в шапке + правка этого контракта vN (круг
критика) — сменить источник без смены контракта невозможно, потому барьер сверяет hash с
КОНТРАКТОМ: значение читается из блоба высшей заморозки `frozen/contracts/003/<v>`, а не
зашивается в барьер.

## Приёмка

1. `bash scripts/check_skills.sh` → 0.
2. Таблица указателей: поимённые `grep -c` → 1 для каждой пары; посторонних `skills/`-строк
   в `roles/` нет (`grep -rn 'skills/' roles/ | wc -l` → ровно 6).
3. Порядок Q8-C: две команды из критерия 3 → `architect` и фикстурный хеш.
4. Зеркало и обнаружение: `diff -r skills/<имя> .omp/skills/<имя>` пуст для всех четырёх;
   состав `.omp/skills/` точен; `bash scripts/check_skills.sh --live` → 0 на машине с
   запиненным omp — дословная строка «# Адаптация: …» от каждого скила. `npm run
   check:antiplacebo` → 0; `check:ci-parity` с объявленным исключением `check:skills-live`
   (бинаря omp в CI нет), `check:gen`, `check:zones`, `check:charter`, `check:contract-frozen`
   → 0; `check:skills` в CI.

## Статус

Статуса задач нет: контракт заморожен побайтово, ход работ — в `HANDOFF.md`.
