# Контракт 033 — union-семантика зон check_zones (Н-100): формализация исполненного бутстрапа

Статус: черновик (слово владельца 2026-09-19 ~19:05: «Н-100 (union-зоны, ПРЕРЕКВИЗИТ)»; кодовая
часть исполнена ДО контракта owner-каналом П1а — слово владельца 2026-09-17 «П1а — да»; настоящий
контракт = пост-фактум суд из очереди нормализации, кодифицирует действующее соглашение по
грамматике шапки check_zones:20-24). Фикс-цикл 2: закрытие Б1/Б2 вердикта к1 (5a4f1cf) — итоговая
приёмка формализации разведена с регрессом механики, стаб-проза изъята из замораживаемого текста.

## Предмет

Боль Н-100 (замер 2026-09-17, ci_diag на ed738a8; дословная запись боли — Н-100 в
NABLIUDENIA.md): суд путей в check_zones шёл per-contract (фильтр по номеру контракта, тогда
:335) вопреки заявленной шапкой объединительности — 8 FAIL по
легальным коммитам параллельных окон 026∥027, а judge_gate = check_ci_gate, поэтому красный CI =
отказ любого судьи: конвейер ВСЕХ контрактов заблокирован замкнутым кругом (фикс требует
заморозки, заморозка — критика, критик — зелёный CI).

Исполнение (до контракта; развилка владельца 2026-09-17 «owner-channel-фикс против двойного
waiver мини-контракта» закрыта исполнением, второго развилки воли в предмете нет): `cbbf6b1` —
фильтр суда путей стал ОБЪЕДИНЕНИЕМ зон автора по всем заморозкам (как шапка check_zones:31-32 и
check_staged), добавлена фикстура union-vs-per-contract, case_done_diapazony_neizmenny переписан
версионно-независимым; остаток 75f5ffe закрыт СПАСЕНО-заморозками frozen/contracts/026/4 + 027/3
(РАЗРЕШИЛ-ВЛАДЕЛЕЦ 2026-09-17). Демо П1а: ПРЕД rc=1 8 FAIL / ПОСТ зелёный.

033 НЕ несёт кодовой дельты: предмет уже в дереве. Предмет 033 — НОРМА: зафиксировать
union-семантику заморозкой, предъявить приёмку прогонами и закрыть Н-100 судейски (владелец
держит его открытым до этого; потолок параллельности N≥3 связан с пререквизитом по его слову).

## Frontier: границы и отвергнутые варианты

- **Граница-1: семантика суда путей, НЕ грамматика ЗОНА-строк.** А-176 (фантомные зоны
  многострочных ЗОНА-строк 026/027) и Н-99 (кириллические пути) — соседние боли других адресов;
  здесь не трогаются.
- **Граница-2: З1-остаток закрывается ЗАЯВЛЕНИЕМ, не механизмом.** Ревьюер 029 к1 (ab233af, З1):
  «пораздельная раздача внутри контракта необязывающая — "кто несёт проводку нормы" перестаёт
  быть механической мерой, как только исполнителю тот же путь однажды выдан другим контрактом».
  Вариант «per-contract карательный фильтр» отвергнут: это и есть дефект Н-100 — шапка называет
  пересечение бессмыслицей. Вариант «мягкий warn-канал per-contract» отвергнут: вторая семантика
  рядом с единственным читателем lib_zones — ровно то, от чего ушёл 016, и боли за ним не
  измерено. Кто несёт работу окна — предмет оркестратора и ревьюера (остаток назван в разделе
  риска). Вето владельца — через круг критика.
- **Граница-3: ретро-расширение union будущими заморозками — заявленная семантика, не дефект.**
  Суд идёт по заморозкам, живым на момент суда («во ВСЕХ замороженных», шапка): иначе легальные
  коммиты окна B, сделанные до заморозки B, снова красны — исходная боль. Сужение зон высшей
  заморозкой v2 компенсируется СПАСЕНО по грамматике 003-v3 (прецедент 026/4+027/3).
- **Граница-4: вне предмета.** Н-108 (две живые сессии на одном дереве), Н-85/А-122 (write-гард),
  мера параллельности окон (021, measure_parallel_windows — своя семья фикстур).

## Инварианты

1. Суд путей коммита идёт по ОБЪЕДИНЕНИЮ зон автора из блобов ВЫСШИХ заморозок ВСЕХ контрактов;
   источник формулировки — шапка check_zones:31-32, читатель один — lib_zones.
2. Per-contract фильтр зон в суде путей запрещён. Ловец — антиплацебо-фикстура
   fixtures/check_zones/case_union_zon_vseh_zamorozok.sh (зелёный контроль + красный повторный
   прогон под управлением verify_antiplacebo). Какие порчи предъявлены и где они различимы —
   дело фикстуры и отчёта прогона; замораживаемый текст пару «стаб ↔ ветвь» не несёт (Н-39).
3. Потребители зон не расходятся: check_zones и check_staged судят одной семантикой через
   lib_zones (цель 016; расхождение двух потребителей = дефект).
4. ЗОНА-строка окна не карает руку, получившую тот же путь из другого окна (следствие union,
   З1): раздача внутри окна — распределение работ, не барьер.
5. Диапазоны — от ПЕРВОЙ заморозки до done/HEAD; необъявленные авторы не судятся; merge-коммиты
   пропускаются — действующие нормы, 033 их не меняет.

## Зоны

ЗОНА architect: contracts/033-union-semantika-zon-check-zones.md NABLIUDENIA_ARCHITECT.md
РАБОТА НЕ РАЗДАЁТСЯ: scripts/check_zones.sh scripts/lib_zones.sh scripts/check_staged.sh fixtures/check_zones/ (предмет исполнен бутстрапом cbbf6b1 до контракта; правка этих путей — отдельный контракт со своей болью) scripts/verify_antiplacebo.sh (раннер не правится) .githooks/ (зона 022) registry/contracts.tsv (исключительная dual-control зона, 023/031) .github/workflows/ci.yml package.json (семья check_zones в CI — новых точек приёмки нет) verdicts/ (судейский канал) HANDOFF.md NABLIUDENIA.md (оркестратор)

## Приёмочный критерий (код возврата; числа — из вывода команды)

Регресс механики — зелёный УЖЕ на черновике: код в дереве с cbbf6b1, оба прогона rc 0 (измерено
к1 в одноразовом клоне); это суд уже исполненного, НЕ приёмка предмета 033:

1. `bash scripts/verify_antiplacebo.sh --scope check_zones` → rc 0: вся семья предъявлена, каждая
   фикстура — зелёный контроль + красный повторный прогон с именованной причиной; в том числе
   case_union_zon_vseh_zamorozok — ловец инварианта 2.
2. `bash scripts/check_zones.sh .` → rc 0 на живом дереве; счёт замороженных контрактов и
   проверенных коммитов — из вывода (пустая выборка красна по построению барьера).

Итог формализации — сам предмет 033; на черновике ДО заморозки КРАСНЫЙ (обход к1: дерево зелёно
обоими регрессами при отсутствующих тегах frozen/contracts/033/* и done/contracts/033/* и
открытом Н-100), зелёный — условие done. Общий check_contract_frozen недостаточен: незамороженное
он принимает как черновик:

- Заморозка-033-проверка: `git show-ref --verify refs/tags/frozen/contracts/033/1 && test "$(git for-each-ref --format='%(objecttype)' refs/tags/frozen/contracts/033/1)" = tag && test "$(git rev-parse frozen/contracts/033/1:contracts/033-union-semantika-zon-check-zones.md)" = "$(git hash-object contracts/033-union-semantika-zon-check-zones.md)"` → rc 0: тег заморозки именно 033 существует, аннотирован (Н-35) и несёт блоб контракта, побайтово равный файлу в дереве.
- Закрытие-Н-100-проверка: ``test "$(grep '^### Н-100\. ' NABLIUDENIA.md | grep -o '`[^`]*`' | tail -n 1)" = '`ЗАКРЫТО (033: код — бутстрап П1а cbbf6b1, только барьер + две фикстуры; остаток 75f5ffe закрыт СПАСЕНО frozen/contracts/026/4 + frozen/contracts/027/3 — отдельные заморозки; формализация — frozen/contracts/033/1; приёмка — прогонами)`'`` → rc 0: ПОСЛЕДНЯЯ бэктик-группа заголовка записи Н-100 — статус-поле по грамматике check_nabludenia, извлечение тем же правилом, что check_nabludenia.sh:122-127 (shell-эквивалент `grep -o | tail -n 1`), — побайтово равна закрывающему токену :102. Это точная rc-транскрипция условия done «заменяется … на ДОСЛОВНО следующую строку» (:99) и строго сильнее четырёх якорей: токен в непоследней группе при живом ОТКРЫТО, парафраз внутри последней группы, порча любого якоря — красный; заголовок Н-100 отсутствует — пустая выборка ⇒ неравенство ⇒ rc 1 («ноль строк — красный»). Команда содержит бэктики и записана двойным бэктик-ограждением, чтобы остаться copy-paste-исполнимой (вёрстка по вердикту a74ba04).

## Остаточный риск (назван прямо)

- Раздача внутри окна необязывающа (З1) — cognitive: держится оркестратором и ревьюером; смена
  руки видна в `git log --format='%an %s'` и судится областью ревью (шапка check_zones:39-41).
- `user.name` — не удостоверение: действующий cognitive-only шапки check_zones:37-42, 033 не
  ужесточает.
- Ретро-расширение зон будущими заморозками — заявлено границей-3, механизм не строится.

## Закрытие Н-100 (условие done)

Запись Н-100 в NABLIUDENIA.md: статус-токен в заголовке записи (последняя бэктик-группа строки,
грамматика check_nabludenia) заменяется с ОТКРЫТО на ДОСЛОВНО следующую строку — приземляет
оркестратор, его зона (прецедент статуса в заголовке записи: Н-98):

`ЗАКРЫТО (033: код — бутстрап П1а cbbf6b1, только барьер + две фикстуры; остаток 75f5ffe закрыт СПАСЕНО frozen/contracts/026/4 + frozen/contracts/027/3 — отдельные заморозки; формализация — frozen/contracts/033/1; приёмка — прогонами)`

Закрытие-Н-100-проверка побайтово сравнивает ПОСЛЕДНЮЮ бэктик-группу заголовка записи с этим
токеном (:102) — статус-поле файла, не строка целиком (к2; решение арбитража a74ba04). Открытие потолка
N≥3 — после done 032+033 по слову владельца 2026-09-19 ~19:05.
