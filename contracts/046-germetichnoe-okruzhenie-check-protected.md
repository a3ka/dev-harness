# Контракт 046 — герметичное окружение `check_protected.sh` (Н-130): re-exec через `env -i`

Статус: черновик. Минт: тег `id/CONTRACT/046` (tag-object `90419deeca0849430da448c640ce30a0f80ed78b`)
+ строка `registry/contracts.tsv` (`d47da26`, строка 48) — dual-control Н-77а исполнен ДО этой
пачки. Предмет вынесен в отдельный контракт записью самой Н-130 («для check_protected.sh (039) —
НЕ ЗАКРЫТО… адрес: отдельный будущий контракт»), приоритет КРИТИЧЕСКИЙ (владелец 2026-09-24,
security-кластер). Заморозка — после круга критика.

## Предмет — измеренная боль, а не пересказ

`scripts/check_protected.sh:81` ставит доверенный PATH (`export PATH=/usr/bin:/bin`, закрытие
PATH-spy адверсария круга 1 контракта 039), но PATH — НЕ единственный способ разрешения ИМЕНИ
команды: bash разрешает ФУНКЦИИ раньше `$PATH`, а экспортированная функция переживает старт
любого дочернего bash сериализацией `BASH_FUNC_<имя>%%=() {…}` (механизм Shellshock). Барьер
считает решающую разность ОДНОЙ строкой — `comm -23 "$TMP/existed" "$TMP/head"` — и его же шапка
называет `comm` прямо: «та самая утилита, чей подмен даёт зелёное без ALLOW».

Живой репродюсер этой пачки (`/tmp/dev-harness-verify/scratch/arch046/repro-n130-check-protected.sh`,
ожидания в ПАМЯТИ проверяющего, сверка по rc И по подстроке отказа), toy-дерево формы
`fixtures/check_protected/_repo.sh`, `plans/001-p.md` удалён коммитом БЕЗ строки
`ALLOW-ARTIFACT-DELETE`, барьер СЕГОДНЯШНИЙ:

```
A. чистое дерево, чистое окружение          rc=0  причина_напечатана=0
B. удаление, чистое окружение               rc=1  причина_напечатана=1
C. удаление + тень comm                     rc=0  причина_напечатана=0   ← ОБХОД
D. удаление + тени unset и comm             rc=0  причина_напечатана=0   ← ОБХОД
расхождений: 2
```

Цена в терминах предмета барьера: защищённый артефакт исчезает из истории БЕЗ единой строки
разрешения, а гейт отвечает `rc=0` и не печатает ни одной строки `FAIL` — без записи в
`/usr/bin:/bin`, без root, без подмены `/usr/bin/git`. Это ровно та категория обходов, которую
арбитраж 040-II оставил открытой для будущих кругов (категория (i)).

ВТОРОЙ, НЕОЧЕВИДНЫЙ УЩЕРБ, найден замером этой же пачки: лечение (re-exec через `env -i`) стирает
и ИЗМЕРИТЕЛЬНЫЕ переменные вызывающего, а постоянный CI-страж call-budget
`fixtures/check_protected/red_predel_git_vyzovov_ours.sh` (контракт 040, §Приёмка Р2) считал
git-вызовы через `SHELLOPTS=xtrace BASH_XTRACEFD=9`. Замер на живой паре (тот же toy, тот же
файл стража):

```
сегодняшний барьер:   git-подпроцессов LOW=29, HIGH=29 → "ПРЕДЕЛ ДЕРЖИТСЯ: LOW=29<=30", rc=0
пофикшенный барьер:   git-подпроцессов LOW=0,  HIGH=0  → "ПРЕДЕЛ ДЕРЖИТСЯ: LOW=0<=30",  rc=0
```

То есть без второй части предмета фикс превратил бы живого стража в молчаливо-зелёного: страж
перестал бы отличать предмет контракта 040 от его полного отсутствия. Поэтому предмет 046 — ДВЕ
неразделимые части: герметичное окружение барьера И перевод его измерительного канала на
внешнюю трассировку, переживающую `env -i`.

## Frontier: варианты и оправдание болью

- **В-1. `unset -f <перечень доверенных имён>` сразу после PATH-пина.** ОТВЕРГНУТ — не вкусом, а
  измерением на близнеце: эта редакция жила в `check_zones.sh` один круг и пала
  (`verdicts/adversary/contracts-040-v5-circle.md`, `d46a615`) от экспортированной функции
  `unset`, no-op'ящей ИМЕННО `-f`-форму. Сам `unset` в перечень войти не может: перечень есть
  то, ЧЕМ снимают. Перечисление структурно не сходится — следующее отравленное имя (`printf`,
  `[`) открывает ту же дыру заново. Предъявление живёт в дереве отдельной фикстурой
  (`case_ten_funkcii_unset_comm.sh`), чтобы В-1 нельзя было внести «как проще».
- **В-2. Абсолютные пути (`/usr/bin/git`, `/usr/bin/comm`, …) во всех вызовах.** ОТВЕРГНУТ:
  дороже (правка каждого call-сайта, вечный долг на каждую новую строку), хуже читается И НЕ
  закрывает класс — bash допускает функцию с ЛИТЕРАЛЬНЫМ слэшем в имени, объявляемую через
  сорсинг `BASH_ENV` (арбитраж 040-III, векторы D/E).
- **В-3. `env -i` re-exec ДО первой строки кода, со сверкой по `/proc/$$/environ`.** ПРИНЯТ —
  лечение по прецеденту, не изобретается заново: `scripts/check_zones.sh:119-207` (контракт 040,
  редакции v6/v7) и `scripts/check_provodka.sh` (контракт 038, круг 9, арбитраж
  `038-vrazhdebnaja-sreda-zapuska.md`) несут ровно эту форму, прожитую кругами адверсария.
  Закрывает ВЕСЬ класс «унаследованная экспортированная bash-функция с ЛЮБЫМ именем» без
  перечисления: дочернему bash нечего импортировать.
- **В-4. Ничего не менять в барьере, вызывать его доверенно (`env -i … bash check_protected.sh`).**
  ОТВЕРГНУТ как ЕДИНСТВЕННАЯ мера: перекладывает защиту на каждого вызывающего (CI-шаг, дрилл,
  `check_spec_ready`, человек), то есть держится памятью, а не механизмом. Как ДОПОЛНЕНИЕ к В-3
  остаётся честным усилением и названо в §Риски.
- **НЕ ВХОДИТ В ПРЕДМЕТ** (класс назван прямо, чтобы круги его не переоткрывали): «враждебное
  ОКРУЖЕНИЕ ВЫЗОВА» — `BASH_ENV`/`ENV`, slash-именованные функции, `SHELLOPTS`, сорсинг —
  структурно незакрываем изнутри скрипта (арбитраж 040-III
  `contracts-040-vrazhdebnoe-okruzhenie-predel.md`: `BASH_ENV` с `exit 0` даёт rc=0 на ВСЕХ
  формах гарда); подлинность вывода `git` при живом PATH (арбитраж 040-II, три замера);
  `user.name` как удостоверение; правка `scripts/check_zones.sh` (там класс уже закрыт).

## Инварианты (механизм: блок «ГЕРМЕТИЧНОЕ ОКРУЖЕНИЕ» в `scripts/check_protected.sh`)

Грамматика и числа — ЕДИНЫЙ источник этот контракт; реализация несёт названные литералы
ПОБАЙТОВО.

1. **Блок стоит ДО ЛЮБОЙ ДРУГОЙ СТРОКИ КОДА** — раньше `set -euo pipefail`, раньше
   `unset GIT_DIR …`, раньше `export PATH=…`. Любая строка кода выше блока исполняется в
   отравленном процессе.
2. **Решение о re-exec принимается по ФАКТИЧЕСКОМУ окружению процесса** (`/proc/$$/environ`,
   `tr '\0' '\n'`), а НЕ по флагу-часовому вида `[ -z "$REEXECED" ]`: часовой выставляется
   вызывающим заранее (класс закрыт на соседнем барьере — `check_no_leak.sh`, адверсарий к5,
   `4dfc0ff`).
3. **Allowlist — РОВНО две переменные, `PATH` и `LC_ALL`, в этом порядке**, и строка сравнения
   несёт ровно их: `$'PATH=/usr/bin:/bin\nLC_ALL=C.UTF-8'`. Порядок обязан совпадать с порядком
   аргументов `env -i` (иначе вечный re-exec). `HOME`/`TMPDIR` НЕ протаскиваются: ни одна строка
   барьера не читает `$HOME`, скратч — явный путь с каталогом (`$ROOT/tmp/protected.XXXXXX`).
4. **`POSIXLY_CORRECT=1` — отдельной строкой ВЫШЕ `exec`** (присваивание функцией не
   перехватывается и включает POSIX-режим, где `exec` — special builtin раньше пользовательской
   функции; арбитраж 040-III, вектор H). `env` и `bash` зовутся АБСОЛЮТНЫМИ путями.
5. **`export PATH=/usr/bin:/bin` остаётся на месте** (строка 81 сегодня) — вторичная защита и
   побайтовое diff-сравнение с прецедентом; `unset -f`-перечисления в файле не появляется НИ
   ОДНОГО (В-1 отвергнут).
6. **Решающая логика барьера не меняется ни байтом**: область из `roles/*.md`, `existed`/`head`,
   `comm -23`, `excuse_for`, оракул `merge-tree`, тексты `bad()`/`ok()` — байт-в-байт. Предмет
   046 — окружение процесса, а не семантика защиты.
7. **Измерительный канал стража call-budget переживает re-exec.** `run_trace` в
   `fixtures/check_protected/red_predel_git_vyzovov_ours.sh` — внешняя `strace -f -e trace=execve`
   (форма и счётная строка ПОБАЙТОВО с близнеца `fixtures/check_zones/red_predel_git_vyzovov.sh`,
   ревью-круг 040 `b38e78a`), плюс fail-closed проверка живости меры: `CALLS_LOW>0 ∧ CALLS_HIGH>0`,
   иначе именованный отказ. Ноль вызовов — КРАСНОЕ, а не зелёное. (Приземлено architect'ом ДО
   заморозки, см. §Зоны.)
8. **Ни одна из 25 существующих `case_*.sh` семьи не правится, не удаляется и не
   переименовывается** — добавление новых легально (§Приёмка Р6).

## Модель угроз

ЗАЩИЩАЕТ:
- унаследованная экспортированная bash-функция с ИМЕНЕМ ЛЮБОЙ утилиты, которую барьер зовёт
  голым именем (`comm`, `git`, `sort`, `awk`, `grep`, `mktemp`, `mkdir`, `rm`, `tr`, `sed`, `wc`,
  `dirname`) — структурно, без перечисления имён;
- рекурсивная тень самого средства нейтрализации (`unset`, `command`, `builtin`) — их таблица в
  дочернем процессе пуста по построению, снимать нечего;
- подделка часового re-exec заранее выставленной переменной окружения вызывающего;
- унаследованные `GIT_*` и конфиг вызывающего как канал подмены предмета (сохранено из 039 плюс
  стёрто целиком границей `env -i`);
- молчаливая смерть измерительного канала стража call-budget: ноль вызовов даёт именованный
  отказ, а не тривиально проходящую границу.

НЕ ЗАЩИЩАЕТ:
- вызывающего, управляющего интерпретатором ДО первой строки барьера (`BASH_ENV`/`ENV`,
  slash-именованная функция на `/usr/bin/env`, сорсинг барьера в отравленную оболочку) —
  структурно незакрываемый класс, решение арбитража 040-III, закрытие существует только в
  доверенном ВЫЗОВЕ барьера;
- подлинность вывода `git` и прочих утилит при живом доверенном PATH (подмена `/usr/bin/git`
  через root или скомпрометированный пакет) — граница доверия, а не арифметика вывода;
- поле `user.name` как удостоверение автора коммита (остаток, наследуемый из 039/040);
- отсутствие `procfs` (не-Linux): сверка окружения опирается на `/proc/$$/environ` — тот же
  принятый риск, что уже несут `check_zones.sh` и `check_provodka.sh`;
- старые `case_*.sh` семьи `check_protected` — их полярность этим контрактом не пересматривается.

## Зоны

ЗОНА architect: contracts/046-germetichnoe-okruzhenie-check-protected.md scripts/check_protected.sh fixtures/check_protected/

ПЕРЕСЕЧЕНИЕ architect: fixtures/check_protected/ — 040 семья фикстур этого барьера объявлена контрактом 040 ДВАЖДЫ (architect: пространства имён `red_`/`probe_`/`stab_`/`_*`; implementer: добавление `case_*`), union-семантика 033; 046 добавляет два `case_*`-предъявления и правит измерительный канал `red_`-стража — оба пространства уже architect-ские по 040, новых путей контракт не занимает

ПЕРЕСЕЧЕНИЕ architect: scripts/check_protected.sh — 039 тот же барьер: 039 завёл его перенос-семантику и отдал файл implementer'у; 046 не трогает семантику 039 (§Инварианты п.6 — решающая логика байт-в-байт), предмет только окружение процесса, а сам файл уже объявлен `ЗОНА architect` живой заморозкой 042

ПЕРЕСЕЧЕНИЕ architect: scripts/check_protected.sh — 040 тот же барьер: 040 отдал файл implementer'у под батчинг git-вызовов (diff-tree --stdin); 046 конвейеры 040 не трогает, а его измерительного стража чинит в том же движении (§Инварианты п.7), потому что герметичное окружение стирает прежний канал счёта

РАЗДАЧА IMPLEMENTER'У НЕ ОТКРЫВАЕТСЯ — это решение, а не умолчание. Предмет реализации —
один блок комментария и ЧЕТЫРЕ строки кода, побайтово повторяющие уже прожитый прецедент
(`scripts/check_zones.sh:119-207`, редакция v7); красная сторона (три фикстуры семьи и две
мутационные пробы) и измерительный канал (§Инварианты п.7) — работа того же автора и того же
движения, разрезать их между двумя ролями значит развести фикс и его меру по разным кругам.
Механически барьер уже принадлежит архитектору: `scripts/check_protected.sh` объявлен
`ЗОНА architect` ЖИВОЙ заморозкой 042 (`contracts/042-excuse-for-otstavshij-roditel.md:214`) —
не новая власть, а существующая; прецедент «architect чинит барьер ЭТОГО класса сам» —
`ed8d093` (контракт 040, та же Н-130 на близнеце `check_zones.sh`).

ПОРЯДОК ПРЕДЪЯВЛЕНИЯ СОХРАНЁН РЕГЛАМЕНТНЫМ, и это проверяемо: КРАСНОЕ зафиксировано
ОТДЕЛЬНЫМ коммитом `fe9936e` (контракт + две фикстуры, барьер ещё не тронут —
`bash scripts/verify_antiplacebo.sh --scope check_protected` rc=1, «фикстур: 27 · предъявлено
красным повторным прогоном: 25», обе новые «барьер остался зелёным на обманном дереве»), и
только СЛЕДУЮЩИЙ коммит вносит фикс. Судья воспроизводит красноту прогоном на `fe9936e`, а не
верит на слово; ни одна фикстура после фикса не правится (§Приёмка Р6-пин).

КРУГ 2 (находки Б2/Б3 критика) добавил третью фикстуру семьи
(`case_ten_funkcii_exec_comm.sh`) и две пробы (`probe_mutant_bez_posix_rezhima.sh`,
`probe_mera_mertva_predela.sh`). На `fe9936e` их нет, и переключением коммита их «ДО» не
снимается — поэтому оно снимается МУТАЦИЕЙ и снимается ИСПОЛНИМО: каждая проба сама строит
мутанта названного инварианта, краснеет на нём и зеленеет на честном дереве (§Приёмка
Р14-Р16). Оракул тот же, подан там, где истории ещё нет; обещания в прозе он не заменяет.

Что легло в дерево ДО заморозки (все три цели уже в union архитектора — `fixtures/check_protected/`
по 040, `scripts/check_protected.sh` по 042, черновик контракта признаётся дверью draft-пуска):
`fixtures/check_protected/case_ten_funkcii_comm.sh`,
`fixtures/check_protected/case_ten_funkcii_unset_comm.sh` (красные на `fe9936e`),
`fixtures/check_protected/case_ten_funkcii_exec_comm.sh`,
`fixtures/check_protected/probe_mutant_bez_posix_rezhima.sh`,
`fixtures/check_protected/probe_mera_mertva_predela.sh` (круг 2), правка
измерительного канала `fixtures/check_protected/red_predel_git_vyzovov_ours.sh` (§Инварианты п.7 —
безопасна и ДО фикса: внешняя трассировка считает одинаково у сегодняшнего и у пофикшенного
барьера, fail-closed на нулевой мере краснеет только там, где канал мёртв) и сам блок
«ГЕРМЕТИЧНОЕ ОКРУЖЕНИЕ» в барьере.

РАБОТА НЕ РАЗДАЁТСЯ: scripts/check_zones.sh scripts/check_provodka.sh scripts/check_no_leak.sh
(класс там уже закрыт своими контрактами, этим контрактом не правятся) .github/workflows/ci.yml
package.json (CI-проводка стража call-budget существует с 040, новых ключей контракт не вводит)
registry/contracts.tsv (dual-control 023/031) NABLIUDENIA.md HANDOFF.md (каналы оркестратора)

## Приёмочный критерий

Канон 008: каждая строка — исполняемая команда с известным кодом возврата, не проза.

### Красное сейчас (до фикса — воспроизводится прогоном на коммите `fe9936e`)

Колонка «ДО» снята живьём на `fe9936e` (контракт + две фикстуры, барьер ещё не тронут);
колонка «ПОСЛЕ» — живьём на HEAD этой ветки. Судья воспроизводит обе стороны переключением
коммита, а не верит таблице. Счёт семьи в «ПОСЛЕ» — 28, а не 27: круг 2 добавил третью
фикстуру (§Зоны, абзац «КРУГ 2»), на `fe9936e` её нет.

КЛЮЧ `--scope` — ЭТО КЛЮЧ БАРЬЕРА И ИМЯ case БЕЗ РАСШИРЕНИЯ. `scope_select` расширения `.sh`
не знает и на ключе с ним отвечает rc=1 «FAIL scope_select отказал» НА ЛЮБОМ дереве,
независимо от состояния барьера, — то есть такая запись команды не предъявляет теста вовсе
(находка Б1 критика круга 1: прежняя редакция этой таблицы несла `.sh` в Р2 и Р3 и была
неисполнима на всех трёх коммитах — `fe9936e`, `5baf803`, HEAD).

| # | Проба | Команда | ДО (`fe9936e`) | ПОСЛЕ (HEAD) |
|---|---|---|---|---|
| Р1 | Антиплацебо семьи целиком: новые фикстуры предъявляют барьер красным на дереве с тенью-функцией | `bash scripts/verify_antiplacebo.sh --scope check_protected` | **rc=1**, «фикстур: 27 · предъявлено красным повторным прогоном: 25», обе новые — «барьер остался зелёным на обманном дереве» | **rc=0**, «фикстур: 28 · предъявлено красным повторным прогоном: 28» |
| Р2 | Тень `comm` одна (ловит отсутствие защиты ВООБЩЕ) | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_ten_funkcii_comm` | **rc=1** | **rc=0**, причина «существовал и на HEAD его нет: plans/001-p.md» |
| Р3 | Тени `unset`+`comm` (ловит В-1 — «фикс» перечислением) | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_ten_funkcii_unset_comm` | **rc=1** | **rc=0**, причина «существовал и на HEAD его нет: verdicts/adversary/v-a.md» |
| Р4 | Прямой репродюсер Н-130 (четыре входа, ожидания в памяти проверяющего) | `bash /tmp/dev-harness-verify/scratch/arch046/repro-n130-check-protected.sh scripts/check_protected.sh` | **rc=1**: A rc=0 · B rc=1 · C rc=0 · D rc=0 · расхождений: 2 | **rc=0**: A rc=0 · B rc=1 · C rc=1 · D rc=1 · расхождений: 0 |

Р4 — вне дерева намеренно (скратч проверяющего, не артефакт репозитория): постоянную
регрессию несут Р1-Р3 через анти-плацебо-протокол, где вызовы барьера исполняет ПРОВЕРЯЮЩИЙ, а
не фикстура. Дублировать тот же предмет третьим файлом в дереве — рост без механизма.

### Зелёное и обязано остаться зелёным (не-ослабление, живые прогоны на HEAD этой ветки)

Счётчики ОБХОДА (сколько коммитов пройдено, сколько путей существовало) растут с историей и
различаются между коммитами по построению — приёмку несут код возврата и НАЗВАННЫЕ строки
решения, а не эти числа. Где число решает (Р1, Р14-Р16) — оно названо прямо.

| # | Проба | Команда | Результат |
|---|---|---|---|
| Р5 | Барьер на реальном дереве | `bash scripts/check_protected.sh .` | rc=0 и строка решения «исчезло: 0 · с разрешения: 30 · перенесено: 10». Счётчики ОБХОДА за приёмку не отвечают и в ней не пиннуются: они растут с КАЖДЫМ коммитом ветки (на `5baf803` — «коммитов пройдено: 2322 · существовало: 477 · на HEAD: 437»). 1.8с |
| Р6 | Старые 25 `case_*.sh` семьи не правлены, не удалены, не переименованы | `git diff --exit-code --diff-filter=MD --no-renames 90419deeca0849430da448c640ce30a0f80ed78b HEAD -- ':(glob)fixtures/check_protected/case_*'` | rc=0 (добавление новых легально) |
| Р7 | Зелёная ветвь «исключение принято» (дрилл; анти-плацебо её доказать не умеет) | `bash scripts/drill_protected_exception.sh` | rc=0 |
| Р8 | Зелёная ветвь «перенос признан» | `bash scripts/drill_protected_rename.sh` | rc=0 |
| Р9 | Н-127: удаление за отставшим родителем мержа (оракул `merge-tree`) | `bash fixtures/check_protected/repro_n127_otstavshij_roditel.sh` | rc=0 |
| Р10 | Суд зон на пачке | `bash scripts/check_zones.sh .` | rc=0. Счёт зависит от живых заморозок и от числа коммитов ветки, за приёмку не отвечает и в ней не пиннуется (на `5baf803` — «коммитов в диапазонах: 2123 · проверено по зонам: 1374») |
| Р11 | Пре-фриз precision-гейт 043 на этом черновике | `bash scripts/check_precision_gate.sh . contracts/046-germetichnoe-okruzhenie-check-protected.md` | rc=0, «OK» (задача (б) по семье protected пропущена: барьер правится этим же черновиком) |
| Р12 | Форма модели угроз (041) и проводки (038) | `bash scripts/check_threat_model.sh . contracts/046-germetichnoe-okruzhenie-check-protected.md` И `bash scripts/check_provodka.sh . contracts/046-germetichnoe-okruzhenie-check-protected.md` И `bash scripts/check_consumers.sh . contracts/046-germetichnoe-okruzhenie-check-protected.md` | rc=0 · rc=0 · rc=0 |
| Р13 | Страж call-budget контракта 040 после смены канала (§Инварианты п.7) | `bash fixtures/check_protected/red_predel_git_vyzovov_ours.sh` | rc=2 `NOT_IMPLEMENTED: нет strace` на рабочей станции (strace здесь нет — измерено, тот же статус, что у близнеца `check_zones`); измерение живёт в CI (ubuntu-24.04, strace 6.8) |

### Мутационные предъявления инвариантов 4 и 7 (круг 2, находки Б2/Б3 критика)

Обе пробы строят мутанта САМИ, в собственном скратче, и дерева не трогают: субъект и страж
исполняются по-настоящему, ожидания живут в памяти проверяющего. Обе исполнимы на рабочей
станции — внешнего `strace` они не требуют (Р16 подаёт канал трассировки шимом, честно
исполняющим трассируемую команду). Колонка «на мутанте» — не обещание, а поведение, которое
даёт та же команда на дереве с названной единственной правкой.

| # | Что доказывает | Команда | На HEAD | На мутанте |
|---|---|---|---|---|
| Р14 | Третья фикстура семьи: тени `exec`+`comm` — отравлено САМО СРЕДСТВО УХОДА (инвариант 4) | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_ten_funkcii_exec_comm` | **rc=0**, причина «существовал и на HEAD его нет: plans/001-p.md» | барьер без строки `  POSIXLY_CORRECT=1`: **rc=1**, «барьер остался зелёным на обманном дереве» |
| Р15 | Что решает исход на этом входе — РОВНО строка `POSIXLY_CORRECT=1` (мутация + контроль исправности мутанта) | `bash fixtures/check_protected/probe_mutant_bez_posix_rezhima.sh` | **rc=0**: честный+тени rc=1 с причиной · мутант+тени rc=0 без причины · мутант без теней rc=1 | барьер без строки `  POSIXLY_CORRECT=1`: **rc=1**, «мутация не применилась … инвариант 4 требует её ПОБАЙТОВО» |
| Р16 | Fail-closed живости меры (инвариант 7): мёртвый канал КРАСНЫЙ, живой — зелёный | `bash fixtures/check_protected/probe_mera_mertva_predela.sh` | **rc=0**: пустая трасса → страж rc=1 «мера мертва» · синтетическая (12 вызовов) → страж rc=0 «LOW=12» | страж без проверки `CALLS_LOW>0 ∧ CALLS_HIGH>0`: **rc=1**, «страж на мёртвой мере вернул rc=0 вместо 1 — fail-closed живости отсутствует» |

Почему п.7 вообще стал частью предмета — исторический замер architect'а на скратч-копиях, ДО
внесения блока в дерево (исполнимая форма этого замера теперь Р16, а не этот блок):

```
прежний канал (SHELLOPTS=xtrace), сегодняшний барьер   LOW=29 HIGH=29 → «ПРЕДЕЛ ДЕРЖИТСЯ», rc=0
прежний канал (SHELLOPTS=xtrace), пофикшенный барьер   LOW=0  HIGH=0  → «ПРЕДЕЛ ДЕРЖИТСЯ», rc=0  ← ложно-зелёное
```

Без п.7 фикс оставил бы постоянного CI-стража зелёным на любом входе: страж перестал бы
отличать предмет контракта 040 от его полного отсутствия.

## ПРОВОДКА

ПРОВОДКА:
- guard=scripts/check_protected.sh

ПРОВОДКА-ЭНФОРСМЕНТ: guard-only обоснован полным текстом в подразделе ниже (вне поля).

### Обоснование guard-only ПРОВОДКА (вне поля г0 — полный текст)

Норма контракта — свойство АРТЕФАКТА `scripts/check_protected.sh` («барьер судит то дерево, что
ему назвали, из окружения, которое он построил сам»), а не поведение РОЛИ: агенту нечего делать
иначе, судится прогоном фикстур §Приёмка Р1-Р3, которые уже постоянные CI-стражи через
`verify_antiplacebo` (ключ `check:antiplacebo`) и `npm run check:protected`. role-строка здесь
была бы пином общей нормы под видом предметной (обход В1 грамматики 038). `AGENTS.md` секции о
герметичности окружения барьеров не несёт — charter= неприменим. Новых ключей CI контракт не
вводит: обе команды §Приёмка уже в проводке с 039/040.

## Риски (named residual)

1. **Класс «враждебное окружение ВЫЗОВА» остаётся открытым** (§Модель угроз, арбитраж 040-III):
   `BASH_ENV` с `exit 0` даёт rc=0 на ЛЮБОЙ форме внутрискриптового гарда. Закрытие существует
   ровно в одной точке — вызове барьера доверенной стороной как
   `env -i PATH=/usr/bin:/bin LC_ALL=C.UTF-8 bash scripts/check_protected.sh` — это цена и воля
   ВЛАДЕЛЬЦА, а не предмет кругов 046.
2. **`/proc/$$/environ` — Linux/procfs.** На не-Linux сверка окружения не работает; тот же
   принятый риск, что уже живёт в двух соседних барьерах. Ловец — не барьер, а среда проекта.
3. **Страж call-budget измеряется только в CI** (локально `rc=2`, strace нет). Локальный прогон
   больше НЕ доказывает бюджет вызовов — доказывает его CI-шаг. Тот же статус, что у близнеца
   `check_zones` с ревью-круга 040; названо здесь, чтобы не прочиталось как регресс.
4. **Стоимость re-exec** — один лишний `execve` на прогон (замер: 1.8с против 1.8с на реальном
   дереве, разницы в пределах шума). Не оптимизируется.
5. **Остальные барьеры семейства `scripts/check_*.sh` этим контрактом не аудируются.** Н-130
   назвала два файла; оба закрыты (`check_zones.sh` — 040, этот — 046). Есть ли третий носитель
   того же класса — предмет отдельного прохода, не этого контракта (честная граница объёма, а не
   умолчание).

## Незаполненные требования:
нет
