# Контракт 035 — жнец ./tmp: закрытие Н-97ост по отсутствию остаточной боли (В1)

Минт: тег `id/CONTRACT/035` = `2a5a2f460dde3b0d4addfa8fb89a5a2c29c36b98` (tag-object на
origin), строка реестра `1e232c7`. Предмет минта: «жнец ./tmp (Н-97ост): расширение
gc_agent_branches на ./tmp по возрасту/done-тегу при close-out».

## Frontier: предмет минта УЖЕ ИСПОЛНЕН — слой 1 закрыт контрактом 026 (done)

Сверка дерева с минт-строкой (назначенная работа фронтира):

- `scripts/gc_agent_branches.sh` несёт ветвь TMP-РЕАП (контракт 026, done): реап
  in-tree `./tmp` при close-out по done-тегу/возрасту, dry-run по умолчанию,
  `--tmp-reap-apply`, tracked-контроль, NUL-транспорт, fail-closed отказы
  (шапка скрипта, строки 27–82).
- Норма close-out ЖИВА: `roles/orchestrator.md:164` и
  `.omp/agents/orchestrator.md:167` — «close-out — gc_agent_branches (016 И-6) +
  gc:agent-branches --tmp-reap-apply (026 Б5(i))».
- Живое предъявление Б5(ii) исполнено: close-out 2026-09-17 снёс 224 кандидата
  (HANDOFF: «026 close-out: реап ./tmp исполнен (224 кандидата, "слито и снесено
  1", зависших 0; живой wip/020/implementer проверен после — цел)»).

## Замер остатка (живой основной чекаут, 2026-09-20 00:40)

`du -sh tmp` = 72M; lstat-сумма верхнеуровневых = 67 377 267 байт (64.3 MiB);
`git ls-files --others -- tmp/` = 11 файлов → 5 git-видимых записей; lstat видит
12 верхнеуровневых (5 непустых + 7 пустых каталогов; пустых всего 15 — 8 вложенных
лежат внутри critic/ и умирают вместе с ним):

| запись | возраст | размер | судьба в механизме 026 |
|---|---|---|---|
| critic/ | −3.1д | 67 245 752 байт / 4860 файлов | выживает <7д; снос возрастом при 7д простоя (клон роли: roles/critic.md:155) |
| charter.RERj63/ | −5.9д | 7751 байт | выживает <7д; снос возрастом |
| ci_job_103282533944.log | −8.1д | 111 789 байт | «032» из имени CI-джоба при живом 032 → актив; снос done-правилом при done/032 |
| draft020-task.md | −16.3д | 5462 байт | кандидат done 020 — снос ближайшим apply-close-out |
| fix5-draft020-task.md | −16.3д | 6513 байт | кандидат done 020 — тот же |
| 7 пустых: barrier005 −21.1д, check_approval −16.0д, ci_parity −1.4д, coldstart005 −21.1д, critic-tmp −19.4д, fix005limits −21.1д, scratch −43.5д | 1–44д | 0 байт | НЕДОСЯГАЕМЫ: источник `git ls-files --others` пустых не видит; счёт верхнеуровневых стабилен 7→7 с замера 026 |

Симуляция живого остатка (игрушка с дословными именами/возрастом, done-ландшафт
020-done/032-активен/026-жив; прогон 2026-09-20, стенограмма
`/tmp/dev-harness-verify/scratch/035/`): dry-run назначает РОВНО 2 кандидата
(`tmp/draft020-task.md done 020`, `tmp/fix5-draft020-task.md done 020`); apply
сносит ровно их, 5 прочих целы; пред-026 стаб (блоб `358b12a~1`, эпоха 016,
0 строк TMP-РЕАП) на том же входе оставляет ВСЁ.

## Варианты

**В1 (РЕКОМЕНДАЦИЯ) — закрытие Н-97ост за отсутствием боли, с доказательством прогоном.**
Боль-остаток вне механизма = 7 пустых каталогов = 0 байт, счёт стабилен; масса
64.3 MiB bounded возрастным правилом и активностью роли критика; 2 done-записи
снесены ближайшим close-out СУЩЕСТВУЮЩЕЙ нормой Б5(i). Цена: ноль кода предмета;
постоянная фикстура-страж держит квалификацию остатка от регрессии. Зона:
fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh. Приёмка ниже; красное
предъявление и стаб-привязка — в КОДЕ фикстуры (Н-39), не в прозе.

**В2 (развилка владельца; в 035 НЕ реализуется) — жнец пустых каталогов.**
Единственная объявленная-открытая ветвь 026 («Не-цели: пустые untracked-каталоги —
отдельная ветвь по боли, замер 7 штук»). Боль: 0 байт, косметика, счёт стабилен.
Цена: малая — lstat-источник верхнеуровневых пустых записей в python3-ветке,
критерии те же возраст/done. Красные при выборе (Н-39, свап-церемония 024,
стаб = текущий скрипт):
- `red_pustoj_vozrast.sh`: вход tmp/pustoj10d/ (ПУСТОЙ, −10д) — стаб apply
  оставляет каталог (дефект наблюдаем ИМЕННО здесь: источник `git ls-files`
  пустых не видит; прогонное доказательство слепоты — фикстура В1, часть
  «пустые целы»); после В2 apply сносит.
- `red_pustoj_svezhij.sh`: пустой −1ч ВЫЖИВАЕТ — красное СВОЁ на нарушении
  инварианта «возраст защищает» (стаб здесь честен: свежий обязан выжить и до).
- `red_pustoj_batareja_026_celes.sh`: вся семья 026 зелёная после В2 (источник
  расширен, прежние правила не тронуты).
REGRESS-024 не ослабляется: пустой каталог несёт 0 файлов — нога-2 не слепнет.
Выбор владельцем → респект этого файла ДО заморозки.

**В3 (отвергнуто) — слой 2 durable $HARNESS_SCRATCH.** Carve-out задания 035
(«скратч-producer'ы вне дерева — их не трогаем»); поверхность 19 писателей +
отмена правила-16 = строка РАЗРЕШИЛ-ВЛАДЕЛЕЦ (прецедент 1737953; заморожено вне
зон 026). Отдельный контракт по слову владельца.

## Предмет (В1, инварианты)

1. `scripts/gc_agent_branches.sh` НЕ меняется — заморожен для 035: квалификация
   остатка — свойство 026-механизма, доказывается стаб-предъявлением, не правкой.
2. Фикстура-страж `fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh`:
   зелёный контроль (dry-run назначает ровно 2 done-записи, сводка
   «TMP-РЕАП кандидатов 2», НЕ назначает возрастных и ложный актив; apply сносит
   ровно 2; выжившие и пустые каталоги целы) + стаб-предъявление (блоб
   `358b12a~1` слеп на done-записях) + красное (chmod 555 → rc 1
   «не удалось удалить», персистентно для повтора проверяющего).
3. Закрытие Н-97ост оформляется при close-out 035: сверка статуса Н-97 в
   NABLIUDENIA.md (ОТКРЫТО → ЗАКРЫТО, ссылка на прогон) и census-строка в
   HANDOFF — оркестратор.

## Зоны

ЗОНА architect: contracts/035-zhnets-tmp.md fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh
ЗОНА orchestrator: NABLIUDENIA.md HANDOFF.md (п.3: сверка статуса Н-97 + census при close-out 035)
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/

Заморожено вне зон: `scripts/gc_agent_branches.sh` (026), `scripts/verify_antiplacebo.sh`
(011/012/014), `AGENTS.md` правило 16.

## Приёмочный критерий

    bash fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh        # rc 0
    bash scripts/verify_antiplacebo.sh --scope gc_agent_branches/case_zhnets_ostatok_035   # rc 0
    bash scripts/verify_antiplacebo.sh --scope gc_agent_branches      # rc 0 (семья: 8 case)
    bash scripts/gc_agent_branches.sh --root <живой корень>           # rc 0; TMP-РЕАП список: РОВНО draft020 ×2
    # close-out 035 (оркестратор, действующая норма Б5(i)):
    bash scripts/gc_agent_branches.sh --root <живой корень> --tmp-reap-apply
    # rc 0; после: draft020 ×2 отсутствуют; critic/, charter.RERj63/,
    # ci_job_103282533944.log, 7 пустых — целы; census-строка в HANDOFF.

## Не-цели

- Код жнеца; пустые каталоги (В2 — развилка владельца); слой 2 durable (В3).
- Ручная чистка ./tmp (Н-97: грязное дерево — реальный мир).
- Ложный актив ci_job_*.log — остаточный риск 026 инв.4, само-разрешается
  done/032; не предмет.
- Правки ролей (клон критика в ./tmp/critic — норма роли, bounded механизмом).
