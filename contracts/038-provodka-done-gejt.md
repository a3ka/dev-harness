# Контракт 038 — ПРОВОДКА done-gate (Н-116)

Статус: черновик 038 в дереве; круг 1 — FAIL к1 «набор артефактов неполон»
(verdicts/critic/contracts-038-v1.md, 8700ea7), закрыт в v2 (0aa0803); круг 2 —
FAIL к2, пять блокеров Б1–Б5 (verdicts/critic/contracts-038-v2.md, 5cc1398;
репро критика: /tmp/dev-harness-verify/scratch/{audit,compat}-038-k2.py). Круг 3 —
FAIL к3, два повторных предмета (verdicts/critic/contracts-038-v3.md, e9d52b5) →
арбитраж b0e4beb (verdicts/arbitration/038-dostizhimost-i-zony.md): РЕШЕНИЕ по
обоим исполнено в v4.
**v6 — исполнение РЕШЕНИЯ арбитра `78785ec` (verdicts/arbitration/
contracts-038-head-granica-dokumentacija.md; созван вторым подряд FAIL
критика по HEAD-границе §Инварианты 3 — contracts-038-v6.md `f78dcf1`,
contracts-038-v7.md `66a47d1`; предмет — дельта `8182723..536e627`, ветка
architect, в main НЕ ВЛИТА)**: §Инварианты 3 — честная двухаргументная
сигнатура `check_provodka.sh <корень> <отн-путь-контракта>` заменяет
однопараметрический показ и ложную пометку «корень = HEAD-дерево» (барьер
её НИКОГДА не проверял — З1 арбитра: 17 ревизий истории барьера перебраны,
ни одного чтения git-объектов); новый текст называет механизм честно:
чтение С ДИСКА по канонизированному `<корень>` (`cd <корень> && pwd -P`),
БЕЗ сверки с HEAD (П1 — коррекция, НЕ ослабление). Фраза «эту гарантию
несёт ВЫЗЫВАЮЩИЙ» НЕ вводится (измеренно ложна для role/charter-целей — З2
арбитра: ни один вызывающий, включая `done_contract.sh`, никогда не сверял
HEAD для этих целей); вместо неё §Инварианты 3 честно называет пробел:
гарантия «проводка-цели закоммичены на HEAD» СЕЙЧАС НЕ ЗАКРЫТА НИКЕМ (П2).
§Инварианты 4 получает новый именованный implementer-остаток «ПРОВОДКА-цели
на HEAD»: расширение байтовой сверки шага 4 (`done_contract.sh:63-67`,
`git hash-object` против `HEAD:<путь>`) на role/charter-цели поля ПРОВОДКА;
НЕ реализовано в этой правке (П3 — дешёвая форма замены тем же паттерном,
переписывание check_provodka.sh на git-объекты НЕ требуется). Единственный
тронутый файл этой правки — сам контракт; scripts/, fixtures/ не тронуты,
тег заморозки не ставится.

**v5 — закрытие к4 (verdicts/critic/contracts-038-v4.md, fe11b3b; Б1 — различимость
C6) + совет (устаревший md5-якорь)**: вход red_C6 заменяет обязательного писателя
ПРОИЗВОЛЬНЫМ ТРЕТЬИМ — файл пары done_contract изъят, добавлена пара
scripts/other.sh→fixtures/reader.sh: писателей ТРИ (счёт честен, замер несётся),
обязательного состава нет; слабый п1 «любые ≥3 разных писателя» даёт на этом
входе rc0 → проба КРАСНА отсутствием ожидаемого отказа, поимённый предикат
отказывает именем scripts/done_contract.sh. Дискриминационная проба
/tmp/dev-harness-verify/scratch/n116-red/_v5_c6_discrim.sh: слабый rc1 / сильный
rc0 (стабы изолируют только п1, слабый — по коду-репро критика
critic038k4-c6.py); вход v4 «пара изъята, писателей два» изъят как неотличимый
(слабый п1 при <3 печатает те же имена — живое репро критика fixture_rc=0).
Пачка перегнана (31 файл, расхождений 0). Совет к4: устаревшая запись md5
e1108d2f…/585 байт заменена живой мерой — 584 байта без LF,
c3f7d03cce8f0d99ddf408c901c1bc69 (_v5_anchor_md5.py, §Меры).
**v4 — исполнение РЕШЕНИЯ арбитра b0e4beb (правка круга 4, два предмета к3)**:
Б3-достижимость — каркасы _toy.sh обеих семей с маппингом и freeze-проба сеют
НАСТОЯЩИЕ пути писателей-минимум стабами по коду реальных
(scripts/freeze_contract.sh — под тестом, scripts/lib_registry.sh,
scripts/done_contract.sh); предикат п1 НЕ ослаблен («любые три разных» —
названный обход, отвергнут арбитром); добавлен красный вход состава red_C6
«писатели-минимум не зарегистрированы». Б5-зоны — три каталога семей объявлены
ДОПОЛНИТЕЛЬНО в ЗОНА implementer (двойное объявление, union-семантика 033;
прецеденты 027:224+235, 020:111-112), раннер пачки — точный файл
fixtures/_krasnye_038.sh в ЗОНА architect. Пачка перегнана (31 файл), обе
пробы арбитра воспроизведены на исправленном дереве (§Меры).
**v3 — закрытие к2**: Б1 верификация потребителя = ТОЛЬКО проба
исполнения (ЗОНА-альтернатива удалена); Б2 обязательная точка повторной
верификации — шаг 6а писателя done (окно frozen..HEAD на момент тега);
Б3 маппинг файл-на-пару `scripts/consumers.d/`, census-совместимая замер-форма
(живой прогон против неизменённого спек-гейта: rc 0); Б4 честный зелёный G1 —
якорь живой опечатки «Воркфлоу» байт-в-байт (проба исправлена, не барьер);
Б5 две фазы предъявления названы явно (до реализации — прямые red/green прогоны,
scoped-ключи и case_-презентации активируются С реализацией). Советы к2-1..3
учтены: D4-ветвь rc2 названа residual в §Риски, состояние реестра игрушек
уточнено (п4), порядок диагностики г0→г0-доп→г1..г6 задан. Красная пачка
перегнана (30 файлов), freeze-семья обновлена тем же коммитом. Формальный цикл
(критик → freeze → implementer → CI → адверсарий → ревьюер → done) — после
зелёного CI базового дерева и слова владельца.
Основа: `scratch/n116/frontier-v2.md` + решения грилинга владельца 2026-09-21
(В1/В2/В3, ниже ДОСЛОВНО) + обязательное требование владельца «freeze-изменение
верифицирует ВСЕХ потребителей» (§Freeze-верификация потребителей).
Карта различий с frontier-v2 — `diff-map-frontier-v2.md` рядом.

Решения грилинга (слово владельца 2026-09-21, применены ДОСЛОВНО):

- **В1**: канал по природе правила — role ОБЯЗАН для поведенческих (029-класс), guard
  достаточен для чисто-энфорсмент (Н-100-класс) с обоснованием в контракте;
  role=roles/*.md (НЕ charter — charter = owner-налог на каждый контракт).
- **В2**: fence-aware матч НЕ в пакете — named residual (§Риски-1).
- **В3**: дубль шаг8↔правило12 НЕ входит (отдельный кандидат-фикс; §Устав-дельта
  сохраняет последнюю фразу дословно).

## Предмет

Норма-проводка контракта: у каждого контракта есть поле ПРОВОДКА, каждый канал нормы
СУЩЕСТВУЕТ и ПОДКЛЮЧЁН (барьер судит), done-тег ставится только через писателя, гейт
который это обеспечивает, — и правка писателя реестра верифицирует всех его поимённых
потребителей. Четыре носителя, один предмет:

1. **Поле ПРОВОДКА** в contracts/NNN-*.md (грамматика — §Инварианты 1–2; В1 — природа
   канала). Обязательность — с заморозки ЭТОГО контракта; для контрактов, замороженных
   без поля, — клапан владельца (писатель, шаг 7), НЕ список (Б2: список = дешёвый
   обход из зоны implementer; живой случай ровно один — 001).
2. **`scripts/check_provodka.sh`** — барьер-читатель поля: г0–г6 + г0-доп (В1),
   rc 0/1/2 (канон freeze). Граница правила 8: судит «существует и подключён»;
   «то ли правило» — ревьюер по целям поля (строка роли, §Само-применение).
3. **`scripts/done_contract.sh`** — писатель тега done/contracts/NNN/v: env-гигиена,
   basename-грамматика, закоммиченность, вердикт ревьюера (равенство после trim+fold),
   ПРОВОДКА через барьер (rc2 → отказ fail-closed), клапан РАЗРЕШИЛ-ВЛАДЕЛЕЦ ровно на
   два административных состояния, v=max+1, причина → аннотация тега, реестр не трогаем.
4. **`scripts/check_consumers.sh` + `scripts/consumers.d/` (файл-на-пару)** —
   верификация потребителей (НОВОЕ требование владельца, §ниже) в ДВУХ
   обязательных точках: заморозка (проводка в freeze) и done (шаг 6а писателя);
   правка зарегистрированного писателя без ИСПОЛНЕНИЯ пробы его потребителя =
   именованный отказ в обеих точках.

Замеренная боль: Н-116 (029 near-miss — проводка отложена, поймана ПОЗДНО ревьюером;
031×4 frozen-проводки; Н-100 устаревшая запись), Н-117 (несанкционированный дубль-тег),
027 / 036-copy-list (54ff90f) / А-198 (spawn 3в) — три ПРОВОДКА-неполноты писателя.

## Frontier: выбор варианта (кратко; полный разбор — frontier-v2 §11)

- Ядро (поле + г0–г6 + зеркало-писатель) — рекомендация frontier-v2, СОревью подтвердил.
- **Потребители (новое требование)**: (а) церемония «изменение писателя несёт красную
  пробу на каждого читателя» — правило без механизма, измерено Н-38 (судья пропускает);
  (б) CI-пост-чек — ловит ПОСЛЕ спада (класс Н-118: регресс спал ~16 ч вне scoped-набора);
  (в) **пре-фриз гейт потребителей (РЕКОМЕНДОВАН)** — маппинг писатель→потребители в
  одном машиночитаемом файле + живые пробы, отказ ДО тега, атомарно как заморозка.
  Граница с Н-115: Н-115 сверяет ЧЕРНОВИК с frozen-зависимостями (зоны/грамматики/
  достижимость приёмки); здесь — ПРАВКУ ПИСАТЕЛЯ с его потребителями. Пересечения нет:
  разные входы (текст контракта vs diff-окно писателя); Н-115 остаётся отдельным словом.
- В1 — решён владельцем (не пересматривается): структурный минимум (≥1 предметного
  канала) НЕ вводится как безусловный; природа правила выбирает канал, обоснование
  guard-only — строкой в контракте, семантику обоснования судит ревьюер.

## Инварианты

Грамматики — ЕДИНЫЙ источник этот контракт; реализация несет фразы ПОБАЙТОВО (фикстуры
сверяют дословно). Маркеры ищутся по ВСЕМУ файлу контракта (канон 008). Парсинг —
anchored `sed -nE` (прецедент ЗОНА-строк check_spec_ready.sh).

1. **Поле ПРОВОДКА.** 1..N строк, ≥1 обязательна (г0). Формат поля: строка `ПРОВОДКА:`,
   за ней канальные строки `- <канал>=<цель> …`:
   - `- guard=scripts/<имя>.sh` — цель: файл;
   - `- role=roles/<роль>.md «<норма-строка>»` — цель: файл; подключение: строка
     ДОСЛОВНО, самостоятельной строкой (`grep -Fxq`);
   - `- charter=AGENTS.md §<полный заголовок секции> «<норма-строка>»` — цель: секция
     устава; строка ДОСЛОВНО в теле секции (`grep -Fxq`).
   Кавычки: норма-строка обрамлена «…», вложенные «» внутри норма-строки запрещены
   (парсер берёт от первой « до последней » строки поля). Charter-pinning (Б3):
   `§<полный заголовок>` = дословный текст заголовка БЕЗ решёток и без пробела уровня,
   целиком (живой: `## Воркфлоу майлстоуна` → `§Воркфлоу майлстоуна`); матч заголовка
   ТОЧНЫЙ (`^#+ <текст>$`, побайтовое равенство); тело секции = от заголовка до
   следующего заголовка ТОГО ЖЕ или более высокого уровня; `§Воркфлоу` (короткий
   префикс живого заголовка) и опечатки — вне грамматики, г5 красен «секция не
   найдена». Б4-правило (v3): живой якорь — опечатка последней буквой «у»
   (AGENTS.md:115, байты d0 83); примеры грамматики, toy-уставы и честные входы
   фикстур несут якорь БАЙТ-В-БАЙТ; написание той же лексемы с «ю» (байты
   d0 8e) запрещено везде в предмете 038 — мера по байтам, §Меры.
2. **В1 — природа канала.** Поведенческая норма (029-класс) обязана идти role-каналом
   (roles/*.md). Guard достаточен для чистого энфорсмента (Н-100-класс) ТОЛЬКО со
   строкой `ПРОВОДКА-ЭНФОРСМЕНТ: <непустое обоснование>` в контракте (по ВСЕМУ файлу;
   барьер проверяет наличие и непустоту — г0-доп; честность обоснования судит ревьюер,
   строка роли §Само-применение). Charter — добровольный канал для норм самого устава
   (owner-налог по решению В1 с контрактов НЕ взимается); удовлетворяет предметность
   наравне с role. Псевдо-грамматика `ПРОВОДКА-ЭНФОРСМЕНТ:` с пустым обоснованием —
   красное «guard-only без обоснования».
3. **check_provodka.sh `<корень> <отн-путь-контракта>`** (двухаргументная
   форма; барьер читает поле и целевые файлы С ДИСКА по канонизированному
   `<корень>` — `cd <корень> && pwd -P`, БЕЗ сверки с HEAD и без чтения
   git-объектов; арбитраж `78785ec` — коррекция ложной пометки «корень =
   HEAD-дерево», не ослабление, П1), rc 0/1/2:
   - **г0**: поле непусто; каждая строка matches ровно один канал, нераспознанная —
     «вне грамматики»; шапка `ПРОВОДКА:` без канальных строк — «поле пусто»;
     **г0-доп (В1)**: ни одной строки `- role=` ∧ ни одной `- charter=` ∧ нет
     непустой `ПРОВОДКА-ЭНФОРСМЕНТ:`-строки — «guard-only без обоснования».
     Порядок диагностики (v3, совет к2-3): строки поля судятся слева направо,
     г0 → г0-доп → г1..г6; побеждает ПЕРВАЯ красная строка своего канала —
     вход «guard-only ∧ guard-призрак» краснеет г0-доп (предметного канала нет
     вовсе), а не г1; порядок не оставляется исполнителю.
   - **г1** guard: файл существует на HEAD в scripts/.
   - **г2** НЕ-ОРФАН: имя скрипта матчится как СЛОВО в `.githooks/*` или
     `.github/workflows/*.yml` в строке, чей первый не-пробельный символ ≠ `#`
     (комментарий sh и закомментированный yml-шаг строкой вызова НЕ считаются —
     синтаксис, не семантика; остаток «упоминание ≠ исполнение» — named, §Риски-2).
   - **г3** role-файл существует; **г4** норма-строка `grep -Fxq` (подстрока-в-чужой-
     строке НЕ засчитывается).
   - **г5/г6** charter по п.1: заголовок — точный матч, норма-строка — `grep -Fxq`
     в теле секции (строка из другой секции НЕ засчитывается).
   - Именованные причины (дословно, единый источник для реализатора):
     «проводка: поле ПРОВОДКА отсутствует»; «проводка: поле пусто»; «проводка: строка
     вне грамматики: <строка>»; «проводка: guard-only поле без предметного канала и
     строки ПРОВОДКА-ЭНФОРСМЕНТ»; «проводка: guard-файл не существует: <путь>»;
     «проводка: guard не подключён: <путь> не вызывается в .githooks/ или
     .github/workflows/»; «проводка: role-файл не существует: <путь>»; «проводка:
     норма-строка не найдена в role-файле: <путь>»; «проводка: секция устава не
     найдена: §<заголовок>»; «проводка: норма-строка не найдена в теле секции
     §<заголовок>».
   - **Граница механизма (арбитраж `78785ec`,
     verdicts/arbitration/contracts-038-head-granica-dokumentacija.md,
     П1–П2)**: барьер читает ТОЛЬКО с диска (см. выше) — ни один канал
     (guard/role/charter) с HEAD не сверяется. Гарантия «проводка-цели
     (role/charter) закоммичены на HEAD и байтово совпадают с рабочим
     деревом» СЕЙЧАС НЕ ЗАКРЫТА НИКЕМ: не барьером (не его предмет —
     §Предмет п.2, правило 8) и не вызывающим (`done_contract.sh` байтово
     сверяет с HEAD ТОЛЬКО сам файл контракта, шаг 4 — role/charter-цели
     поля ПРОВОДКА вне этой сверки). Именованный implementer-остаток
     закрытия — §Инварианты 4, буллет «ПРОВОДКА-цели на HEAD».
4. **done_contract.sh `<отн-путь-контракта> "<причина>"`**, rc 0/1:
   1. env-гигиена: unset GIT_DIR/GIT_INDEX_FILE/GIT_WORK_TREE и пр. (как
      freeze_contract.sh:37-42) — писателю вдвойне;
   2. basename-грамматика NNN — parse_artifact_basename (единственная реализация,
      next_id.sh:150);
   3. причина непуста — ДО всех остальных проверок;
   4. закоммиченность контракта на HEAD байтово (как freeze §4);
   5. вердикт ревьюера: файл(ы) `verdicts/review/contracts-NNN-*.md` на HEAD; несколько
      — свежий по коммит-истории, имя свежего в отказе при расхождении; первая строка →
      trim + case-fold → побайтовое РАВЕНСТВО `accept` (НЕ префикс: «Acceptance …»,
      «accepted» → «вне грамматики»); ЕДИНАЯ реализация разбора, вид сравнения назван;
      одновременно — дельта roles/reviewer.md (А-198): первая строка вердикта обязана
      быть `ACCEPT` | `FAIL` | `ESCALATE`; новые вердикты чистые, старые файлы не
      трогаем;
   6. ПРОВОДКА: check_provodka.sh → rc0 дальше; rc1 → отказ; **rc2 → отказ rc1
      fail-closed** («нечем проверить» ≠ «проверено», прецедент doc-preflight 027);
   6а. **ПОТРЕБИТЕЛИ (v3, Б2 — обязательная точка повторной верификации)**:
      `check_consumers.sh <корень> <контракт>` — ПОСЛЕ ПРОВОДКИ, ДО клапана.
      Окно п2 = последний живой `frozen/contracts/<NNN>/<v>` .. HEAD НА МОМЕНТ
      ТЕГА DONE — пост-фриз реализация писателя ВХОДИТ в окно (у точки freeze
      окно проходит пустым над собственной пост-фриз правкой пакета — обход Б2:
      mint → freeze с vacuous-окном → реализация меняет писателя → done без
      повторной верификации); rc 0 → дальше; rc 1 → отказ «done: потребители 116
      красны: <первая причина>»; **rc 2 → отказ rc1 fail-closed** «done:
      потребители не проверены (rc 2)». Проводка в freeze (§ниже) остаётся —
      две точки, окно каждый раз на момент вызова;
   7. **клапан владельца**: ровно ДВА красных состояния отпираются строкой
      `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` (литерал, ASCII-дефис U+002D, `grep -F`) в ПРИЧИНЕ:
      (а) ПРОВОДКА красна (контракт заморожен без поля; живой класс — legacy 001);
      (б) v>1 — повторный done (боль Н-117). НИКОГДА не отпираются: вердикт,
      незакоммиченность, пустая причина. Процедура (С4): при v>1 команду запускает
      владелец, либо сессия по прямому слову владельца, зафиксированному ДО запуска;
      слово переносится в причину дословно, дефис литеральный ASCII;
   8. тег `done/<dir>/<NNN>/<v>`, v = max+1; причина → аннотация тега (санкция
      клапана остаётся в истории — greppable след); в реестр НЕ пишем (замер
      frontier §0: done-писатель реестр не трогает; минт-строки пишет дверь 031).
   - Именованные причины (дословно): «done: причина пуста»; «done: имя контракта не по
     грамматике NNN»; «done: контракт не закоммичен на HEAD»; «done: вердикт ревьюера
     не найден: verdicts/review/contracts-NNN-*.md»; «done: вердикт вне грамматики:
     <первая строка>»; «done: вердикт ревьюера FAIL»; «done: несколько вердиктов,
    свежий <имя> не accept»; «done: ПРОВОДКА красна: <первая причина>»; «done:
    ПРОВОДКА не проверена (rc 2)»; «done: потребители 116 красны: <первая
    причина>»; «done: потребители не проверены (rc 2)»; «done: v>1 требует
    строку РАЗРЕШИЛ-ВЛАДЕЛЕЦ: в причине».
   - Повтор spec-preflight — не нужен (флаги дисквалификации — предмет 036, на дереве);
     doc-preflight по `--type` для doc-контрактов 027 — включить (как freeze 6б).
   - **ПРОВОДКА-цели на HEAD (named implementer-остаток, арбитраж
     `78785ec`, verdicts/arbitration/contracts-038-head-granica-dokumentacija.md,
     П3; ЕЩЁ НЕ РЕАЛИЗОВАНО)**: расширение байтовой сверки шага 4
     (`git hash-object` против `HEAD:<путь>`, `done_contract.sh:63-67`) на
     role/charter-цели поля ПРОВОДКА проверяемого контракта — каждый
     `roles/<роль>.md` и (при charter-канале) `AGENTS.md`, на который
     ссылается поле, обязаны быть закоммичены на HEAD и байтово совпадать с
     рабочим деревом; расхождение → именованный отказ, тега нет (форма —
     прецедент шага 4). Приёмка будущей реализации: red-фикстура, где
     норма-строка ПРОВОДКА-цели существует ТОЛЬКО в рабочем дереве (не
     закоммичена на HEAD) → done-гейт обязан дать rc1. До реализации
     гарантия «проводка-цели закоммичены на HEAD и байтово совпадают с
     рабочим деревом» НЕ действует (§Инварианты 3, граница механизма) —
     разрыв живёт здесь как названный красный остаток, не как норма.
5. **Заморозка не несёт провода на пре-фриз ПОЛЯ** (обязательность — с этой заморозки,
   пре-фриз проверка поля у НОВЫХ контрактов — Н-115-класс, вне пакета).

## Freeze-верификация потребителей (НОВОЕ обязательное требование владельца)

Требование владельца (2026-09-21): **freeze-изменение верифицирует ВСЕХ потребителей**.
Три замеренные ПРОВОДКА-неполноты одного класса «писатель изменился — читатель молча
сломался»: (1) 036-проводка b4e82d1 не обновила copy-list дрилла drill_contract_change —
зелёная ветвь дрилла красна, регресс спал ~16 ч, фикс owner-identity 54ff90f (Н-118);
(2) 027 — реестр против фикстуры (писатель реестра, читатель-фикстура разошлись);
(3) А-198 — freeze сменил семантику строки реестра (frozen tag-object-sha), страж 3в
spawn_agent остался на минт-sha — spawn по замороженному невозможен, поймано владельцем
живьём, не батареей.

Механизм (пре-фриз гейт, вариант (в) Frontier + точка done, v3):

- **`scripts/consumers.d/`** — машиночитаемый маппинг, ФАЙЛ-НА-ПАРУ (v3, Б3: живой
  канон census — файловый glob, считаемая сущность обязана быть ФАЙЛОМ; v2-форма
  «один tsv, wc-строки» давала живой красный «заявлено 3, дерево даёт 1» на
  честных трёх писателях — репро критика compat-038-k2.py). Каждый файл
  `scripts/consumers.d/<writer-базовое-имя>__<consumer-базовое-имя>.tsv` несёт
  РОВНО одну строку `<writer-путь><TAB><consumer-путь>` с завершающим переводом
  строки; обе стороны — существующие относительные пути. Дубль пары физически
  невозможен (одна пара — одно детерминированное имя файла); разные пары с
  совпадающей парой базовых имён — вне грамматики, именованный отказ. Писатели-
  минимум: `scripts/freeze_contract.sh`, `scripts/lib_registry.sh`,
  `scripts/done_contract.sh` (п1 судит наличие всех трёх). Потребители-якоря
  ТРЁХ болей ОБЯЗАТЕЛЬНЫ: `fixtures/drill_contract_change/` (Н-118),
  `scripts/spawn_agent.sh` (А-198), `fixtures/check_check_contract_ready/`
  (027: семья фикстур-читателей реестра; точная пара — по дереву, _doc027.py
  читает registry/contracts.tsv). Начальный состав из ШЕСТИ пар ПЕРЕЧИСЛЕН в
  §Само-применение и материализуется реализацией дословно; расширение маппинга —
  только пачкой, несущей пробы своих тронутых писателей (п3): v2-обещание
  «остальной состав — census при реализации» УДАЛЕНО — census-добавление пары
  потребителя тронутого писателя краснило бы done самой пачки отсутствием
  пробы в её же замороженном тексте. Неполнота маппинга (незарегистрированный
  потребитель невидим гейту) — named residual (§Риски-6).
- **`scripts/check_consumers.sh <корень> <отн-путь-контракта>`**, rc 0/1/2:
  - **п1 маппинг**: читается; грамматика файл-на-пару (имя файла ↔ пара, пути
    существуют); все три писателя-минимума присутствуют; контракт, правящий
    зарегистрированного писателя, обязан нести замер-строку census-совместимой
    формы (индентная запись грамматики — с отступом внутри пункта, НЕ с начала
    строки: «замер: `cat scripts/consumers.d/*.tsv | wc -l` = N census
    scripts/consumers.d/*.tsv»; census считает файлы = пары, команда — строки =
    пары, единство по построению «файл = одна строка»; исполнение и пересчёт —
    грамматика 036-В2 на заморозке контракта-носителя, ЗДЕСЬ проверяется
    НАЛИЧИЕ строки). Исключение — контракт-регистратор: на дереве ЕГО frozen-тега
    `scripts/consumers.d/` ещё нет (живой случай один — ЭТОТ контракт: 036-В2
    исполняет замер при заморозке, а маппинг появляется реализацией ПОСЛЕ неё;
    замер регистратора в его собственном тексте неисполним по построению —
    и потому НЕ несётся). Признак объективный, кодом: `git ls-tree <frozen-тег>
    scripts/consumers.d/` пуст → обязанность замер-строки vacuous. Отсутствие
    маппинга при живом регистре — отказ п4, не «vacuous п1»;
  - **п2 окно**: последний живой `frozen/contracts/<NNN>/<v>` (при первой версии —
    минт-резерв 023) .. HEAD НА МОМЕНТ ВЫЗОВА; писатели-в-правке =
    зарегистрированные писатели, тронутые коммитами окна (diff-tree, любая
    роль). Пусто → vacuous rc 0. ДВЕ обязательные точки вызова, окно каждый
    раз на момент вызова: (а) freeze — проводка ниже, ловит пре-фриз правки;
    (б) done — шаг 6а писателя (§Инварианты 4): окно frozen..HEAD на момент
    тега ЗАДЕЙСТВУЕТ пост-фриз реализацию писателя (Б2);
  - **п3 покрытие = ТОЛЬКО проба исполнения (v3, Б1)**: для каждой пары
    (писатель W тронут окном, потребитель C из маппинга): контракт несёт строку
    `ПОТРЕБИТЕЛЬ <C>: <cmd>` — живую пробу; гейт ИСПОЛНЯЕТ её (cwd = корень,
    таймаут 60 с), требуется rc 0 (правило 8 — оракул сам потребитель, не
    парсинг прозы). Альтернатива v2 «C есть в ЗОНА-путях черновика» УДАЛЕНА:
    перечисление ≠ исполнение, назначить исполнителя не значит проверить его
    (обход Б1: писатель меняет формат, потребитель в ЗОНА, гейт даёт 0, потребитель
    ни разу не исполнен). Нет пробы → именованный отказ; проба красна/таймаут →
    именованный отказ. Честность пробы (проба обязана исполнять потребителя,
    а не `exit 0`) — семантика ревьюера, механика — гейта (тот же расклад, что
    у ПРОВОДКА-ЭНФОРСМЕНТ: гейт — наличие/код, судья — смысл);
  - **п4 fail-closed**: маппинг не читается / состояние реестра (lib_registry,
    канон 011) ≠ full → именованный отказ. Уточнение (совет к2-2, без ослабления):
    игрушечные деревья БЕЗ origin законно дают `full` (нет пульта сравнения —
    нет расхождения); строку реестра пишет только писатель freeze (036-г5б, тем
    же актом, что тег), поэтому «frozen-тег есть, строки реестра нет» — состояние
    вне живого дерева и вне честных игрушек; каркасы создают тег руками и этого
    достаточно читателю;
  - Именованные причины (дословно): «потребители 116: маппинг не по грамматике:
    <файл>»; «потребители 116: путь маппинга не существует: <путь>»; «потребители
    116: имя файла маппинга не соответствует паре: <файл>»; «потребители 116:
    писатели-минимум не зарегистрированы: <W>»; «потребители 116: замер маппинга
    отсутствует (контракт правит писателя <W>)»; «потребители 116: писатель <W>
    изменён, потребитель <C> не верифицирован: нет ПОТРЕБИТЕЛЬ-пробы»;
    «потребители 116: ПОТРЕБИТЕЛЬ-проба красна: <C>: rc <K>»; «потребители 116:
    ПОТРЕБИТЕЛЬ-проба превысила таймаут: <C>»; «потребители 116: маппинг не
    читается»; «потребители 116: реестр заморозок не читается: <state>».
- **Проводка в freeze**: порядок реестр → грамматика → коммитность → причина →
  вердикт → spec-preflight (036) → **check_consumers (ЭТОТ)** → doc-preflight (027) →
  кап → тег. Красный = rc 1 «ОТКАЗ: потребители 116 красны: <первая причина>»;
  тег/реестр не тронуты, отказ атомарен. Вторая обязательная точка — шаг 6а
  done_contract.sh (§Инварианты 4); freeze-точка одна не замыкает цикл: у
  собственной пост-фриз реализации пакета её окно проходит пустым (Б2).
- **Само-применение — первый судимый случай**: эта пачка правит freeze (проводка
  гейта) и СОЗДАЁТ done-писателя → на её СОБСТВЕННОМ done шаг 6а обязан найти её
  же пост-фриз правки писателей (freeze тронут проводкой, done создан) и покрыть
  их ПОТРЕБИТЕЛЬ-пробами из §Само-применение (пробы трёх якорей болей + пара
  done→check_spec_ready). Замкнутость честная: гейт, находящий сам свою пачку,
  — живой контроль.

## Зоны

ЗОНА architect: contracts/038-provodka-done-gejt.md NABLIUDENIA_ARCHITECT.md fixtures/check_provodka/ fixtures/done_contract/ fixtures/check_consumers/ fixtures/_krasnye_038.sh
ЗОНА implementer: scripts/check_provodka.sh scripts/done_contract.sh scripts/check_consumers.sh scripts/consumers.d/ scripts/freeze_contract.sh scripts/verify_ci_parity.sh fixtures/drill_contract_change/ .github/workflows/ci.yml package.json
ЗОНА orchestrator: roles/architect.md roles/orchestrator.md roles/reviewer.md HANDOFF.md NABLIUDENIA.md
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/

РАБОТА НЕ РАЗДАЁТСЯ: registry/contracts.tsv (dual-control 023/031) frozen-теги (читаются;
пишет только freeze_contract) scripts/lib_registry.sh scripts/lib_zones.sh
scripts/next_id.sh scripts/check_spec_ready.sh scripts/check_contract_ready.sh (пин 008)
scripts/spawn_agent.sh (потребитель-читатель: верифицируется пробой, не правится — его
фикс А-198 уже в дереве, 3e334e5) .githooks/ (зона 022 — вызовы guard-каналов идут через
CI-ключи, не через новые хуки).

- `AGENTS.md` — канал владельца (правило 11): дельта ниже, РАЗРЕШИЛ на заморозке;
  ЗОНА-строкой не раздаётся.
- roles/reviewer.md (дельта первой строки вердикта, А-198) и role-строки
  architect/orchestrator/reviewer — правило 14: не автором судимого предмета в открытом
  кругу; редактирует orchestrator/владелец МЕЖДУ кругами.
- fixtures/check_provodka/, fixtures/done_contract/, fixtures/check_consumers/ —
  каталоги семей с ДВОЙНЫМ объявлением (арбитраж 038-Б5 / b0e4beb; union-семантика
  033; прецеденты 027:224+235 и 020:111-112 — архитекторская строка сохранена,
  implementer добавлен ниже):

ЗОНА implementer: fixtures/check_provodka/ fixtures/done_contract/ fixtures/check_consumers/

  До заморозки в семьи пишет только architect; первым пост-фриз коммитом
  architect приземляет red_*/green_*/_toy.sh байт-в-байт (и раннер — точный
  файл fixtures/_krasnye_038.sh, ЗОНА architect); implementer добавляет туда
  case_*.sh как часть реализации (§Исполнимые команды, фаза 2). Residual
  двойного объявления: в общем каталоге implementer технически может тронуть
  байт-пиненные файлы архитектора — судит ревьюер по диффу (зоны —
  параллелизм и границы, не ловля дефектов: шапка check_zones); тот же
  принятый остаток уже заморожен в 027 и 020.
- fixtures/drill_contract_change/ — потребитель freeze (copy-list): обновление
  copy-list на новые вызываемые freeze скрипты — implementer, верифицируется гейтом
  потребителей (п3: путь в ЗОНА-строках этой пачки).
- scripts/verify_ci_parity.sh — правится implementer ТОЛЬКО если несёт собственный
  перечень ключей (сумма-инвариант должен поглотить новые ключи).

## Приёмочный критерий

Стаб-привязки Н-39 живут В КОДЕ фикстур (шапки case-файлов), не в прозе контракта:
каждый стаб обязан разойтись с честным предметом ровно на своём входе, иначе фикстура
красна именем стаба. Каждый вход красен СВОИМ предъявлением; объединённых кейсов нет.
Фикстуры — игрушечные деревья в tmp, самодостаточные (прецедент scoped-ключа
`check_check_contract_ready`, шард ap2, ci.yml:63). Игрушка НЕ сетапит local user.name
(урок 016: фикстура не маскирует живое; идентичность — ТОЛЬКО конфигом игрушечного
дерева, канон fixtures/freeze_contract/_repo.sh, живое дерево не трогается). Замер
пачки v4 (числа — из прогона раннера, не из головы): R 10+2 (red_R1–R10,
green_G1/G2), D 8+3 (red_D1–D5, D7–D9; зелёные входы таблицы — green_D6,
green_D10, green_G), C 6+2 (red_C1–C6, green_G1/G2; C2 — анти-Б1: потребитель
В ЗОНА-строках, пробы нет → тот же отказ «нет ПОТРЕБИТЕЛЬ-пробы»; C6 — вход
v4→v5 (арбитраж 038-Б3-в + к4): обязательный писатель заменён произвольным
третьим, численный порог «любые ≥3» красится тем же входом): 31 пробный файл +
3 каркаса _toy.sh + 3 маркера .probe-only + раннер _krasnye_038.sh; приземление
несёт замер-строки по дереву. Игрушки с маппингом (check_consumers,
done_contract) и freeze-проба сеют НАСТОЯЩИЕ пути писателей-минимум стабами
`exit 0` по коду реальных (писатель под тестом — scripts/freeze_contract.sh):
предикат п1 судит поимённо именно эти три имени — toy-имена делали честный
зелёный C/G1, D/G и freeze-контроль недостижимыми (к3-Б1, арбитраж b0e4beb).

### R-семейство (check_provodka.sh) — red: rc 1 своим предъявлением

| # | Вход | → красная |
|---|---|---|
| R1 | поля нет | проводка: поле ПРОВОДКА отсутствует |
| R2 | строка вне грамматики (`banana=…`) | проводка: строка вне грамматики: <строка> |
| R3 | `guard=scripts/ghost.sh` | проводка: guard-файл не существует: <путь> |
| R4 | guard есть, вызова нет (.githooks/.github чисты) | проводка: guard не подключён: <путь> … |
| R5 | role-файл есть, строки нет | проводка: норма-строка не найдена в role-файле: <путь> |
| R6 | норма-строка ПОДСТРОКОЙ чужой строки | та же (якорь `-x`) |
| R7 | charter-строка в ДРУГОЙ секции устава | проводка: норма-строка не найдена в теле секции … |
| R8 | шапка `ПРОВОДКА:` без канальных строк | проводка: поле пусто |
| R9 | `§Воркфлоу` (префикс) при живом полном заголовке | проводка: секция устава не найдена: §Воркфлоу |
| R10 (В1) | guard-only, строки ПРОВОДКА-ЭНФОРСМЕНТ нет | проводка: guard-only поле без предметного канала и строки ПРОВОДКА-ЭНФОРСМЕНТ |
| G1 | полный честный вход (3 канала); charter-якорь — живая опечатка байт-в-байт (Б4) | rc 0 |
| G2 (В1) | guard-only СО строкой ПРОВОДКА-ЭНФОРСМЕНТ: | rc 0 |

### D-семейство (done_contract.sh) — писатель предъявлен красным (Б1)

| # | Вход | Честный предмет |
|---|---|---|
| D1 | первая строка вердикта — заголовок | отказ «вне грамматики», тега нет |
| D2 | первая строка `FAIL` | отказ, тега нет |
| D3 | `Acceptance criteria met` / `accepted` | отказ (равенство ≠ префикс) |
| D4 | ПРОВОДКА красна (ghost), РАЗРЕШИЛ нет; канон барьера rc2 на призраке + писатель на призраке (§4) | отказ «ПРОВОДКА красна», тега нет; ветвь rc2 у писателя — residual (§Риски-9) |
| D5 | done/…/1 есть, попытка v2, РАЗРЕШИЛ нет | отказ, тега нет (Н-117) |
| D6 | v2 СО строкой `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` (ASCII-дефис) в причине | тег done/…/2 ставится (green) |
| D7 | контракт не закоммичен | отказ |
| D8 | причина `""` | отказ ДО всех проверок |
| D9 | два вердикта: старый accept, свежий FAIL | отказ, в выводе имя свежего |
| D10 | отравленный GIT_DIR → чужое дерево с FAIL-вердиктом | тег ставится (env-гигиена; своё дерево accept) |
| G | полное честное дерево: accept + ПРОВОДКА зелёная + v1 + причина | тег ставится, реестр НЕ тронут (diff пуст) |

### C-семейство (check_consumers.sh) — новое требование

| # | Вход | Честный предмет |
|---|---|---|
| C1 | писатель в окне, пробы потребителя нет (вне ЗОНА) | отказ «не верифицирован: нет ПОТРЕБИТЕЛЬ-пробы» |
| C2 (анти-Б1, новый v3) | писатель в окне, потребитель В ЗОНА-строках черновика, пробы нет | отказ ТОТ ЖЕ «не верифицирован: нет ПОТРЕБИТЕЛЬ-пробы» — ЗОНА не засчитывается |
| C3 | ПОТРЕБИТЕЛЬ-проба есть, исполняется rc≠0 | отказ «ПОТРЕБИТЕЛЬ-проба красна» |
| C4 | маппинг: путь не существует | отказ «путь маппинга не существует» |
| C5 | контракт правит писателя (маппинг на его frozen-дереве есть), замер-строки нет | отказ «замер маппинга отсутствует» |
| C6 (v4→v5, арбитраж 038-Б3 + к4) | маппинг с ЗАМЕНЁННЫМ писателем-минимума: файл его пары изъят, добавлена пара произвольного третьего писателя (писателей три, замер несётся и честен; обязательного состава нет) | отказ «писатели-минимум не зарегистрированы: <W>»; слабый п1 «любые ≥3» даёт на входе rc0 → проба красна (различимость, fe11b3b) |
| G1 | писатель в окне, проба зелёная, ЗОНА-строки потребителя НЕТ | rc 0 — покрытие даёт только проба |
| G2 | писатель в окне НЕ тронут (vacuous) | rc 0 |

Соответствие 036-В4 (критик спросит): семьи fixtures/check_provodka/ и
fixtures/check_consumers/ имеют барьеры scripts/check_<key>.sh (В4-1 ✓); приёмка несёт
живые пробы вызова каждого барьера (В4-3 ✓); fixtures/done_contract/ — семья ПИСАТЕЛЯ
по прецеденту fixtures/freeze_contract/ (не check_-семья, В4 её не судит — названо).

**Живая проба (урок 016)**: финальная приёмка — прогон на САМОМ дереве харнеса, где
ПРОВОДКА этого контракта уже висит: done ставится ЧЕРЕЗ собственный done_contract.sh;
гейт потребителей на этой пачке даёт rc 0 только при обновлённых copy-list/пробах.

### Исполнимые команды приёмки (v3 — закрытие к2)

Размещение пачки и форма предъявления — ДВЕ ФАЗЫ, названные явно (Б5: scoped-ключи
активируются С реализацией; до неё прямые red/green прогоны — единственная форма;
раннер батареи требует case_*.sh — scripts/verify_antiplacebo.sh:573–576, — поэтому
файлы фазы 1 в дереве НЕ лежат и discovery раннера их не видит: CI не красен
отсутствием case-файлов у нереализованных семей). Прецеденты размещения: 034
(красные прогоны на пред-предметном HEAD, приземление пост-фриз 5ac1478) и 036-Б1
(red-файлы в УЖЕ покрытом каталоге — так лежит проба freeze-семьи). rc — единица
приёмки; PASS не печатает никто.

- **Фаза 1 — до реализации (снято живьём на судимом HEAD)**:
  `bash /tmp/dev-harness-verify/scratch/n116-red/_krasnye_038.sh` → таблица всех
  файлов с rc (31 × rc=1 «ПРЕДМЕТ 038 НЕ РЕАЛИЗОВАН»), итоговый rc 0 в режиме
  EXPECT_RC=1; пустая выборка — отказ раннера (А-199: rc проверяется до разбора).
  Одиночный вход:
  `bash /tmp/dev-harness-verify/scratch/n116-red/check_provodka/red_R1_pole_net.sh`
  → rc 1 (до реализации) — и симметрично для каждого из 31 файла семей.
- **Фаза 2 — с реализацией (пост-фриз, implementer)**: (а) приземление
  red_*/green_* и каркасов _toy.sh ПЕРВЫМ коммитом архитектора в
  fixtures/check_provodka/, fixtures/done_contract/, fixtures/check_consumers/
  байт-в-байт — правится ТОЛЬКО строка REPO каркаса _toy.sh (пин скратча →
  вычисление от $HERE/../..); имена red_*/green_* — прямой запуск, вне
  case_*-глоба раннера (А-82); тем же коммитом приземляется раннер пачки —
  точный файл `fixtures/_krasnye_038.sh` (ЗОНА architect; арбитраж 038-Б5-г);
  (б) implementer СОЗДАЁТ case_*.sh-презентации по
  протоколу раннера ($BARRIER/$WORK/$REPO, каноническая шапка кодов) — НОВЫЕ
  файлы реализатора, не часть architect-пачки: конфликта «байт-в-байт против
  case-глоба» нет (Б5); (в) тем же коммитом, что барьеры, implementer подключает
  scoped-ключи `check_provodka`, `done_contract`, `check_consumers` в ci.yml и
  сумму-инвариант verify_ci_parity — до этого момента новых CI-ключей НЕТ, прямые
  прогоны фазы 1 проводкой CI не являются (правило «гейт не зеленее CI» — ключ
  рождается вместе с case-файлами и барьером). Контроль фазы 2:
  `EXPECT_RC=0 bash fixtures/_krasnye_038.sh` после приземления —
  31 × rc=0; приземлённый вход: `bash fixtures/check_provodka/red_R1_pole_net.sh`
  → rc 0; `bash scripts/verify_antiplacebo.sh --scope check_provodka done_contract
  check_consumers` → rc 0.
- **freeze-семья (закоммичена; каталог покрыт зоной 027-architect; файл обновлён
  до v4 тем же коммитом, что контракт)**:
  `bash fixtures/freeze_contract/red_consumers_gejt_038.sh` — до реализации rc 1
  «ПРЕДМЕТ 038 НЕ РЕАЛИЗОВАН» (живой красный отсутствием гейта, 034-паттерн), после
  реализации rc 0 (ворота п3 / п3-проба / vacuous — шапка файла). Зелёный контроль
  семьи жив: `bash fixtures/freeze_contract/red_spec_preflight_036.sh` → rc 0.
- **census-совместимость замер-формы (Б3, живой свидетель против НЕИЗМЕНЁННОГО
  спек-гейта)**: `bash /tmp/dev-harness-verify/scratch/census-v3-probe/setup.sh`
  → `OK`, rc 0 — честные три писателя, три пары, три файла: замер
  «cat scripts/consumers.d/*.tsv | wc -l = 3 census scripts/consumers.d/*.tsv»
  согласован обеими мерами (census — файлы, команда — строки). Для сравнения
  v2-форма на тех же трёх давала живой красный «заявлено 3, дерево даёт 1» —
  репро критика /tmp/dev-harness-verify/scratch/compat-038-k2.py. Замер-строка
  самим контрактом 038 НЕ несётся: 038 — регистратор маппинга (п1), на его
  заморозке каталога ещё нет, а 036-В2 исполняет каждую замер-строку носителя.

## Само-применение

ПРОВОДКА этого контракта (charter-строка — ЕДИНЫЙ якорь, посимвольно ≡ строке 2
устав-дельты; побайтовая сверка — §Меры):

```
ПРОВОДКА:
- guard=scripts/check_provodka.sh
- guard=scripts/done_contract.sh
- guard=scripts/check_consumers.sh
- role=roles/architect.md «Пиши поле ПРОВОДКА в каждом контракте: поведенческая норма — role-каналом (roles/*.md), чистый энфорсмент — guard-каналом со строкой ПРОВОДКА-ЭНФОРСМЕНТ: <обоснование>; charter — только для норм самого устава.»
- role=roles/reviewer.md «Сверь ПРОВОДКА контракта: каждый канал существует и подключён; guard обязан быть барьером именно этого предмета, не любым вызванным скриптом дерева; семантику нормы судишь по целям поля; поведенческая норма (029-класс) обязана идти role-каналом — guard-only со строкой ПРОВОДКА-ЭНФОРСМЕНТ принимай только при честном обосновании чистого энфорсмента.»
- role=roles/orchestrator.md «До done/тега прогони done_contract.sh; ручной git tag done/… — обход гейта.»
- charter=AGENTS.md §Воркфлоу майлстоуна «Тег done/contracts/<NNN>/<v> ставится только через bash scripts/done_contract.sh: вердикт ревьюера accept и ПРОВОДКА контракта зелёная; поведенческая норма идёт role-каналом (roles/*.md), чистый энфорсмент — guard-каналом с обоснованием в контракте; v>1 и контракт без поля ПРОВОДКА — только со строкой РАЗРЕШИЛ-ВЛАДЕЛЕЦ: в причине; ручной git tag — обход гейта.»
```

Начальный состав маппинга (§Freeze-верификация, п1) — ШЕСТЬ пар, ТРИ писателя;
реализация материализует его в scripts/consumers.d/ ДОСЛОВНО, один файл на пару
(имя файла = базовые имена сторон, `.tsv`, см. п1):

| файл | пара |
|---|---|
| freeze_contract.sh__drill_contract_change.tsv | scripts/freeze_contract.sh → fixtures/drill_contract_change/ |
| freeze_contract.sh__spawn_agent_sh.tsv | scripts/freeze_contract.sh → scripts/spawn_agent.sh |
| freeze_contract.sh__check_check_contract_ready.tsv | scripts/freeze_contract.sh → fixtures/check_check_contract_ready/ |
| done_contract.sh__check_spec_ready_sh.tsv | scripts/done_contract.sh → scripts/check_spec_ready.sh |
| lib_registry.sh__spawn_agent_sh.tsv | scripts/lib_registry.sh → scripts/spawn_agent.sh |
| lib_registry.sh__check_spec_ready_sh.tsv | scripts/lib_registry.sh → scripts/check_spec_ready.sh |

ПОТРЕБИТЕЛЬ-пробы этой пачки (п3; пары писателей, тронутых ЕЁ пост-фриз
реализацией, — freeze правится проводкой гейта, done создаётся; пробы исполняет
шаг 6а на собственном done контракта 038; таймаут 60 с; все четыре — зелёные
батареи существующего раннера, замер каждой ~3–5 с живьём на судимом HEAD):

ПОТРЕБИТЕЛЬ fixtures/drill_contract_change/: bash scripts/verify_antiplacebo.sh --scope drill_contract_change
ПОТРЕБИТЕЛЬ scripts/spawn_agent.sh: bash scripts/verify_antiplacebo.sh --scope spawn_agent
ПОТРЕБИТЕЛЬ fixtures/check_check_contract_ready/: bash scripts/verify_antiplacebo.sh --scope check_check_contract_ready
ПОТРЕБИТЕЛЬ scripts/check_spec_ready.sh: bash scripts/verify_antiplacebo.sh --scope check_spec_ready

CI-ключи (правило 6): `check_provodka`, `done_contract`, `check_consumers` — scoped-ключи
семей по прецеденту `check_check_contract_ready` (шард ap2, ci.yml:63); все три входят в
сумму-инвариант `scripts/verify_ci_parity.sh` (имя сверено по дереву этой сессией).
Активация — С реализацией, фаза 2 §Исполнимых команд (Б5): ключ подключается в
ci.yml тем же коммитом, что барьер и case_-презентации; до реализации новых
ключей нет и CI не красен отсутствием case-файлов.

## Устав-дельта AGENTS.md (ДОСЛОВНО — для РАЗРЕШИЛ владельца)

Текущий шаг 8 (AGENTS.md:169–171, сверен с деревом этой сессией): «**done +
приземление.** Тег `done/contracts/<NNN>/1` ставится после вердикта ревьюера; слияние и
приземление нормы — только после него. Кругов сверх регламента нет — только решением
владельца: это цена, а не правота.»

Заменяющий текст (строка 2 — ЕДИНЫЙ якорь, физическая строка целиком, без обрамления;
посимвольно ≡ charter-строке §Само-применение):

```
8. **done + приземление.**
Тег done/contracts/<NNN>/<v> ставится только через bash scripts/done_contract.sh: вердикт ревьюера accept и ПРОВОДКА контракта зелёная; поведенческая норма идёт role-каналом (roles/*.md), чистый энфорсмент — guard-каналом с обоснованием в контракте; v>1 и контракт без поля ПРОВОДКА — только со строкой РАЗРЕШИЛ-ВЛАДЕЛЕЦ: в причине; ручной git tag — обход гейта.
ПРОВОДКА — обязательное поле контракта (guard=scripts/<барьер> ∨ role=roles/<роль>.md «<норма-строка>» ∨ charter=AGENTS.md §<полный заголовок секции> «<норма-строка>»): каждый канал нормы существует и подключён; поведенческая норма — role-каналом, guard без предметного канала — только со строкой ПРОВОДКА-ЭНФОРСМЕНТ: <обоснование>; грамматика — контракт 038.
Слияние и приземление нормы — только после тега. Кругов сверх регламента нет — только решением владельца: это цена, а не правота.
```

В3 (слово владельца): последняя фраза сохраняет дословный дубль правила 12 — дубль
существует в уставе УЖЕ; снятие — отдельный кандидат-фикс, НЕ в этом пакете.
РАЗРЕШИЛ-ВЛАДЕЛЕЦ на дельту — на заморозке этого контракта (roles/*.md — не устав,
вне подписи).

## Риски и остатки (именованные)

1. **В2 (решение владельца)**: фенс/цитата в role-файле — норма-строка внутри кодового
   фенса роли засчитывается подключённой; fence-aware матч (~10 строк awk + 1 фикстура)
   — вне пакета, добавление ПО ПЕРВОЙ РЕАЛЬНОЙ БОЛИ.
2. Орфан по упоминанию, не исполнению (г2) — named cognitive (класс 016); С2 срезал
   синтаксическую часть (комментарии строкой вызова не считаются).
3. Клапан — единственная точка доверия: РАЗРЕШИЛ отпирает ТОЛЬКО административную
   крась (ПРОВОДКА/v>1), никогда фактическую (вердикт/закоммиченность); след — в
   аннотации тега.
4. Смена поведения оркестратора (ручной тег = обход) — принимается уставом, не кодом;
   детектор «done-тег без прогона писателя» не строим.
5. Legacy 001 (единственный живой без поля): дойдёт до done — клапан, не список.
6. consumers.d — новая поверхность: неполнота маппинга = residual (незарегистрированный
   потребитель невидим гейту); дисциплина — замер-строка носителя при каждой правке
   писателя (п1; регистратор 038 исключён объективным признаком пустого ls-tree) +
   шесть якорных пар начального состава обязательны; расширение — пачкой с пробами
   её тронутых писателей (голая правка маппинга вне пачки гейтами не ловится —
   named residual класса «зоны не пускают, но не судят намерение»).
7. Ветвь «check_provodka rc2 → отказ rc1 fail-closed» у писателя (шаг 6) — защитный
   закон, end-to-end toy-недостижим: все входы rc2 барьера (контракт/HEAD/git)
   отсечены ранними шагами писателя 1–4 (совет к2-1); предъявлена каноном барьера
   (D4б-1: rc 2 на призраке) и отказом писателя на призраке шагом 4 (D4б-2).
8. Честность ПОТРЕБИТЕЛЬ-проб — семантика ревьюера: гейт судит наличие строки и
   rc пробы, «проба-пустышка» (exit 0 без исполнения потребителя) механически
   неотличима; ревьюер сверяет пробу с парой по целям (тот же расклад, что
   ПРОВОДКА-ЭНФОРСМЕНТ у В1).
9. Пара done→check_spec_ready: проба — зелёная батарея check_spec_ready; ветка
   В3 «окно закрывается done-тегом» (check_spec_ready.sh:493–498) отдельного
   case не имеет — добавление ПО ПЕРВОЙ правке check_spec_ready, до того
   named residual.
10. Кап кругов ревьюера НЕ зеркалим (СОревью «что сошлось»: норму кругов несёт правило 12;
   РАЗРЕШИЛ-клапан v>1 частично покрывает; полная зеркальность не требуется).
11. Заголовок/первая строка вердиктов старого формата (031-стиль) — честный отказ «вне
   грамматики» до естественной выработки; дельта reviewer.md (А-198) выравнивает новые.

## Не в пакете

- provodka-preflight на стороне freeze для НОВЫХ контрактов (Н-115-класс; смежен, не
  дублируется — граница названа в Frontier).
- fence-aware матч role-строки (В2 — residual §Риски-1).
- Снятие дубля шаг8↔правило12 (В3 — отдельный кандидат-фикс).
- Инвентарь осиротевших пре-зонных путей и назначение им ЗОН (Н-118-предмет-1).
- Детектор ручного done-тега.

## Меры этой сессии (против лжи)

- `grep -n '^## ' AGENTS.md` → `## Воркфлоу майлстоуна` :115 (живой якорь charter-канала).
- `sed -n '169,171p' AGENTS.md` → текущий шаг 8 дословно (цитирован в §Устав-дельта).
- `ls scripts/{freeze_contract,next_id,verify_ci_parity}.sh` → все существуют.
- Числа базы (35 done-тегов / 34 distinct / без done 001 и 009; md5 якоря frontier) —
  замеры frontier-сессии, цитируются по ссылке; при формальном цикле — перепрогон.
- Побайтовая сверка якоря §Само-применение ≡ строке 2 устав-дельты: ВЫПОЛНЕНА
  (v5 — совет к4: прежняя запись e1108d2f…/585 устарела) живой мерой
  `python3 /tmp/dev-harness-verify/scratch/n116-red/_v5_anchor_md5.py <контракт>`
  → равенство байт True, 584/584 байта без завершающего LF, md5 обоих вхождений
  c3f7d03cce8f0d99ddf408c901c1bc69; по одному вхождению каждой меры (норма из «…»
  charter-строки ∧ строка дельты `^Тег done/`). Дефис `РАЗРЕШИЛ-ВЛАДЕЛЕЦ` в якоре
  — ASCII (финальный контракт перепроверяет при минте).
- Census-совместимость (Б3), живой прогон этой сессии против НЕИЗМЕНЁННОГО
  спек-гейта: `bash /tmp/dev-harness-verify/scratch/census-v3-probe/setup.sh`
  → `OK`, rc 0 (три писателя, три пары, три файла; форма
  «cat scripts/consumers.d/*.tsv | wc -l = 3 census scripts/consumers.d/*.tsv»).
  Контрольный красный той же меры: v2-форма на тех же трёх → rc 1 «замер
  расходится: заявлено 3, дерево даёт 1» (репро критика compat-038-k2.py).
- Правописание живого якоря (Б4), мерой по байтам:
  `bash /tmp/dev-harness-verify/scratch/census-v3-probe/spell_audit.sh
  <файлы предмета>` → лексема с «ю» (байты d0 8e) = 0 вхождений в контракте,
  toy-уставах и честных входах фикстур; байты живого заголовка AGENTS.md:115 —
  d0 92…d183 (окончание «у»), hexdump сверен; сравнение по методу критика
  (compat-038-k2.py) — запрошенный G1 заголовок == созданному в toy == живому
  AGENTS.md:115: `python3 /tmp/dev-harness-verify/scratch/n116-red/_v3_b4_proof.py`
  → три True, rc 0 (Б4: зелёный G1 достижим честно).
- Пробы потребителей (§Само-применение) — прогнаны живьём на судимом HEAD,
  каждая зелёная и укладывается в таймаут 60 с: --scope drill_contract_change
  (1 фикстура, ≈4 с), --scope spawn_agent (2, ≈5 с), --scope
  check_check_contract_ready (4, ≈5 с), --scope check_spec_ready (8, ≈10 с) —
  counts/timing из живых прогонов time(1) этой сессии.
- Проба арбитра п1 (вердикт b0e4beb, замер 4) воспроизведена на исправленном
  каркасе v4: `bash /tmp/dev-harness-verify/scratch/n116-red/_v4_writers_probe.sh`
  → rc 0 — три писателя-минимума зарегистрированы настоящими путями
  (freeze_contract/lib_registry/done_contract), стабы сеются, census
  3 файла = 3 пары = 3 писателя обеими мерами; контрольный красный той же
  меры (изъятие пары done_contract) красит состав.
- Проба арбитра зон (arbiter-038-zones-v4.py, замер 3) воспроизведена на ТОЧНОМ
  блобе v4 (коммит, без правок строк): `python3
  /tmp/dev-harness-verify/scratch/arch038v4-zones-repro.py` → живой
  check_zones rc 0 на case-коммите implementer в трёх семьях при двойном
  объявлении.
- Пачка перегнана после правки каркасов: `bash
  /tmp/dev-harness-verify/scratch/n116-red/_krasnye_038.sh` → итог
  «31 файлов, расхождений 0 (режим ожидания rc=1)», итоговый rc 0;
  freeze-проба на дереве — rc 1 «ПРЕДМЕТ 038 НЕ РЕАЛИЗОВАН» (честный красный
  034-паттерна); зелёный контроль семьи red_spec_preflight_036.sh — rc 0;
  census-проба против живого спек-гейта — OK rc 0 (перепрогон этой сессии).
- Дискриминация C6 (v5 — закрытие к4 fe11b3b): `bash
  /tmp/dev-harness-verify/scratch/n116-red/_v5_c6_discrim.sh` → «слабый п1-стаб
  rc=1 … сильный поимённый rc=0», итог rc 0: вход red_C6 краснит слабый п1
  «любые ≥3 разных писателя» (обход, отвергнутый арбитром) и пропускает сильный
  поимённый предикат; стабы изолируют только п1, слабый — по коду-репро критика
  (critic038k4-c6.py).
- Пачка перегнана после правки входа C6 (А-195 — полная семья после правки):
  `HARNESS_ROOT=<судимое-дерево> bash
  /tmp/dev-harness-verify/scratch/n116-red/_krasnye_038.sh` → итог «31 файлов,
  расхождений 0 (режим ожидания rc=1)», rc 0; одиночный red_C6 → rc 1 «ПРЕДМЕТ
  038 НЕ РЕАЛИЗОВАН» (честный красный отсутствием гейта, 034-паттерн).

Сверка якоря (v5, живая мера _v5_anchor_md5.py): равенство байт True; md5 c3f7d03cce8f0d99ddf408c901c1bc69 (оба вхождения); 584 байта без LF.
