# Контракт 017 — де-флейк метеринга: события вместо сканов и окон (port:0 до конца, наблюдаемость отказа раннера)

Вариант А frontier (круг 1, tmp/frontier-045.md §1) + решения владельца Q1–Q6 (грилинг
2026-08-31, принятые дословно). Источник решений — НАСТОЯЩИЙ КОНТРАКТ: каждое решение
несёт текст ниже с тегом (Q1)…(Q6). Рабочие материалы разведки лежат в `tmp/` — каталог
вне git и источником решений не объявляется.
**ЗАКРЫВАЕТ Н-45**, цена из записи: два красных CI-цикла (013; 016 — рецидив №3, дважды
на одном case_b «нет положительного контроля»), re-run-налог ~треть дня на 016, локальный
scoped-прогон вне 15-мин бюджета без диагноза; локальная репродукция сигнатуры 1/24
(frontier §0.1) осталась без причины — улику уничтожил сам раннер (А-19), потому срез 5
входит в предмет (Q3). Побочно применяет А-18 (потолок ожидания — живость процесса) и
закрывает минимальную часть А-19. Красное сегодня воспроизведено детерминированно:
p0.stub/p0.i/p0.noreport красны поимённо против сканирующей редакции, RUN-каталог
уничтожается при fails>0 (прогоны — §Красное сейчас).

## Предмет

Пять срезов; 1 ⊥ 2 ⊥ 5, срез 3 опирается на 1–2 (событие уже существует), срез 4 — пробы
архитектора, красные ДО раздачи (Н-39: привязка стабов к входам — по коду проб).

**Срез 1 — upstream-стаб на событии (Q1).** `stub_upstream`: listen(0) + запись
фактического порта в файл В listen-callback (событие, симметрично репорту прокси
контракта 007); из фикстурного пути убираются free_port (сейчас scripts/check_metering.sh:103)
и ss-поллинг готовности (:151-153). Ожидание готовности — СОБЫТИЕ: файл-порт от
listen-callback при живом процессе (kill -0), затем healthz-попытка. Потолок ожидания —
живость процесса: тикать до смерти/бюджета, не ранний break (А-18); вариант Б (широкое
окно времени) допустим ТОЛЬКО как потолок бюджета тиков внутри А, не как самостоятельный
механизм (Q1). Гибель до listen — именованный отказ с причиной; живой процесс без записи
порта — именованный отказ «не репортит порт»: различимость «медленный» vs «мёртвый» vs
«не репортит» — предмет класса, а не угадывание по таймауту.

**Срез 2 — ветвь (и) на :0 (Q1).** Свой прокси ветви (и) — proxy_port=0 вместо free_port
(:913); после proxy_up порт перечитывается из конфига (proxy_up уже переписывает cfg.port
на фактический) — запросы ветви идут на фактический порт.

**Срез 3 — proxy_up: событие → healthz (Q1).** Ждать файл-порт (процесс жив по kill -0),
потом мерить healthz; окно времени умирает как класс — медленный старт node перестаёт
выглядеть отказом. Убрать мёртвый portup в gen_config (:295 — main немедленно переписывает
upstream на порт стаба); retry-ветка EADDRINUSE с free_port (:235) умирает вместе с
классом: на bind :0 гонки нет по построению. free_port остаётся только вне фикстурного
пути (cold-start и прочие режимы).

**Срез 4 — расширение mode_port0 (пробы архитектора).** Три под-пробы рядом с зелёными
p0.gen/p0.up/p0.conc/p0.src, привязка к входам — по коду в scripts/check_metering.sh
(Н-39): p0.stub — исходник тела stub_upstream (без free_port/ss), запись порта только
слушателем (погибший до listen node не оставляет порта), подъём и ответ при нерабочем ss
(тень ss: exit 1); p0.i — тело branch_и без free_port и без фиксированного порта в
конфиге ветви; p0.noreport — живой стаб без порт-файла даёт именованный отказ «не
репортит порт» (литерал пинует проба — единый источник), не молчаливый таймаут.

**Срез 5 — А-19 в раннере + честный комментарий (Q3, Q5).** release()
(scripts/verify_antiplacebo.sh:441-452) хранит $RUN при fails>0 (~3 строки): сообщение
«прогон оставлен в …» становится правдой, die-причина барьера сохраняется в
invocations.log; стартовая чистка раннера по-прежнему убирает мёртвые run-*; при rc=0
RUN убирается как раньше (течь каталогов не заводим — райдер (ii) контракта 012).
Комментарий scripts/check_metering.sh:97-100 («анти-плацебо крутит их параллельно
десятки» — ложь о последовательном раннере) переписать: дезинформация стоит на месте
будущей работы над флейком. Остальное А-33 — вне предмета (Q3).

### Инварианты (каждый с rc-командой)

- **И-1** фикстурный путь барьера не сканит порты и не поллит ss готовности:
  `bash scripts/check_metering.sh --port0` → 0, включая p0.stub и p0.i (до предмета —
  красное, см. §Красное сейчас).
- **И-2** отказ «не репортит порт» именованный, различим с таймаутом: та же команда
  `--port0`, под-проба p0.noreport (красная до предмета).
- **И-3** RUN-каталог раннера переживает отказ с непустым invocations.log; при rc=0 —
  не переживает: `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh`
  (красная до предмета).
- **И-4** ветви барьера не потеряны и не замедлены: `bash scripts/check_metering.sh` → 0;
  счёт ветвей — из финальной строки барьера одной мерой до/после предмета (число не
  морожено, урок 015/016); замер пачки 6.5 с, регресс времени > 2× — красное.

## Зоны

ЗОНА architect: contracts/017-deflejk-metiringa.md scripts/check_metering.sh fixtures/verify_antiplacebo/ NABLIUDENIA_ARCHITECT.md
ЗОНА implementer: scripts/check_metering.sh scripts/verify_antiplacebo.sh
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/
РАБОТА НЕ РАЗДАЁТСЯ: fixtures/check_metering/ (нулевой churn — фикстуры зовут $BARRIER без аргументов и портов не выбирают) scripts/proxy/metering_proxy.ts (предмет 005, репорт уже есть) config/metering.json

Зона implementer вступает в силу ПОСЛЕ заморозки; до заморозки scripts/ не правит никто
(прецедент 013/014). ЗОНА architect на scripts/check_metering.sh — ТОЛЬКО расширение
секции mode_port0 пробами среза 4 (фикстурный путь и прочие режимы не тронуты: срезы 1–3
реализует implementer после заморозки). Каталог fixtures/verify_antiplacebo/ у architect —
проба среза 5; существующие фикстуры каталога не трогаются.

## Приёмочный критерий

Счёт фикстур/проб/ветвей НЕ морожен (урок 015/016): число — из факта дерева или вывода
барьера, не из константы в этом тексте. Приёмка «не-флейк» двухчастная (Q2): (i) механизм
доказывает — инварианты И-1…И-3 красные до / зелёные после, класс устранён, не приручен;
(ii) частотный дым — трижды зелёный scoped под контеншеном; закрытие Н-45 — по CI.

### Красное сейчас (прогоны этой пачки; срезы 1–3, 5 не реализованы)

- `bash scripts/check_metering.sh --port0` → rc=1, пять поимённых FAIL: p0.stub ×3
  (тело stub_upstream сканит free_port/ss; порт-файл записан до слушателя — пережил
  немедленно погибший node; под ss-тенью exit 1 стаб умирает молча «за 15 с»), p0.i
  (free_port в теле branch_и), p0.noreport (живой стаб без порт-файла умирает безымянно
  «не поднялся за 15 с»); зелёный контроль в том же прогоне: p0.gen/p0.up/p0.conc/p0.src
  — ok. Замер ~40 с — это умирающие вместе с классом 15-с окна текущей редакции.
- `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` → rc=1: «RUN-каталог
  уничтожен при fails>0 — „прогон оставлен в …“ врёт» (ветвь хранит); зелёный контроль
  ветви «убирает» — ok (rc=0 run-* не копит).

### После реализации (приёмка implementer; rc-переход красное→зелёное)

- обе команды выше → rc=0 каждая;
- И-4: команда `bash scripts/check_metering.sh` → rc=0; счёт ветвей — из финальной
  строки барьера одной мерой до/после предмета (число не морожено, урок 015/016);
  замер пачки 6.5 с, регресс времени > 2× — красное;
- дым-триггер (Q2): `taskset -c 0,1` × 3 повтора
  `bash scripts/verify_antiplacebo.sh . --scope check_metering` → rc=0 каждый, ноль
  строк «нет положительного контроля» в выводах; при любом таком отказе RUN-каталог
  обязан существовать с invocations.log (обеспечено срезом 5 — без него дым
  недиагностируем);
- закрытие Н-45: 2 зелёных полных CI подряд, с записью «доказано отсутствие КЛАССА
  недоказуемо-отсутствующий» (Q2). CI-счётчик флейков не заводится (механизма нет,
  очередь автономности).

## Не-цели

- Cold-start порты (:1505, :1575, :1681, фикс 18765 :1708) — вне фикстурного пути и вне
  CI-флейка, не этот предмет (Q5).
- Вариант В (unix-socket, мок/виртуальное время, общий прокси-пул) — отвергнут (Q1):
  ломает контрактную поверхность 005 (http://127.0.0.1:<port>, реальный omp, TCP-healthz),
  плацебо-изирует антиплацебо-барьер (ветви к1/к2 — рантайм-энфорсмент реальными флагами
  node), сериализует состояние, которое барьер намеренно мутирует.
- Н-46(б) listen-monkeypatch (standard A) — ловец рантайм, поток A; p0.src-греп остаётся
  как есть.
- Н-49 (literal ${HOME} в конфиге), Н-50 — ортогональны.
- Н-41 replay/отмена застрявших — не нужна: «scoped не уложился в 15 мин» — арифметика
  трёх прогонов барьера на фикстуру, лечится бюджетом, не механизмом; бюджет
  scoped-метеринга в заданиях ~35 мин — НОРМА заданий, не код (Q4).
- Н-60 (изоляция спавна) — очередь после 017, контекст приоритетов, в предмет не входит
  (Q6).
- Полный verify_antiplacebo судьёй не гоняется (Н-48); раннер остаётся последовательным.
- Проводка `--port0` в CI — отдельное слово владельца: зона проводки (ci.yml,
  package.json) в предмет не входит.

## Незаполненные требования

- Размер и выражение бюджета тиков ожидания события (константа/переменная) — деталь
  реализации; потолок обязан покрывать «медленный node под контеншеном», не ранний break
  (А-18).
- Способ записи порта стабом (имя файла, формат) — деталь; обязательна запись ИЗ
  listen-callback живым слушателем (пинует p0.stub).
- Тексты прочих именованных отказов, кроме литерала «не репортит порт» (пинует
  p0.noreport — единый источник литерала).
- Места сохранения free_port вне фикстурного пути (cold-start) — не ограничиваются.
