# Контракт 005 — автономия: свой прокси учёта

Номер выдан механизмом: `next_id.sh CONTRACT` → 005, тег `id/CONTRACT/005`. Предмет —
решение владельца 2026-08-19: «dev-harness полностью независим; фабрика контента —
лишь один из будущих проектов; прокси запускается ЗДЕСЬ».

## Предмет

Харнес поднимает **свой** прокси учёта трафика моделей: OpenAI-совместимый
ретранслятор с потолком расхода и журналом вызовов, без внешних зависимостей
(`node:` builtins только), со своей базой на файловой системе. Дом секретов уже
развязан с фабрикой (6a8480b); этот контракт закрывает вторую половину автономии —
учёт. Решения кругов 1 (01b4dc1) и 2 (b67dd2f) закрыты в тексте и помечены [К1-N]/
[К2-N]; повторённые причины — по арбитражу `proksi-rotaciya-passthrough-live.md`
(4c03aa7, обязательный минимум исполнен дословно).

1. **Прокси-ретранслятор** `scripts/proxy/metering_proxy.ts` (запуск
   `node scripts/proxy/metering_proxy.ts`): OpenAI-совместимый; ЛЮБЫЕ пути
   провайдера методами GET и POST [К1-5]; запрос клиента доходит до upstream
   ДОСЛОВНО: метод, путь, ТЕЛО байт-в-байт, content-type, x-request-id
   [К4-6]; ответ upstream возвращается клиенту КАК ЕСТЬ: статус, тело
   байт-в-байт, `content-type` (сверяются именно эти три; прочие заголовки
   в минимум не входят — арбитраж). Конфиг — путь аргументом
   `--config <файл>`; фикстуры пишут свой конфиг с ПОРОЖДЁННЫМИ при каждом
   прогоне значениями (токены, provider id, model id, upstream-пути) [К1-5].
2. **Приписывание роли токеном** [К1-1, К2-1]: токены живут ТОЛЬКО в доме
   секретов (`~/.config/dev-harness/secrets.env`, строки `METERING_TOKEN_<ROLE>=…`);
   в репозитории буквальных токенов НЕТ НИГДЕ — барьер ветви (з) грепает
   ЛЮБЫЕ файлы репозитория на шаблон значения секрета, не только конфиги [К2-2].
   `.omp/models.yml` ссылается `${METERING_TOKEN_<ROLE>}`. Прокси строит карту
   токен→роль чтением конфигурации НА КАЖДЫЙ запрос; кэш на старте запрещён и
   ДОКАЗЫВАЕТСЯ ветвью ротации (арбитраж): один живой процесс (тождество pid
   утверждается) — T1 принят → секрет перезаписан (T1 удалён, T2 порождён) →
   T2 принят → T1 даёт 401 без новой строки журнала; реализация со стартовым
   кэшем падает на шаге T2. Неизвестный токен — 401, строки нет.
3. **Журнал** `data/metering/calls.jsonl` — append-only, строка на вызов, ВСЯ
   схема обязательна и сверяется [К2-6]: `ts, role, provider, model, path,
   tokens_in, tokens_out, usd, latency_ms, status, request_id` (коррелятор —
   арбитраж [К1-6]: значение заголовка `x-request-id` вызова, порождённое
   вызывающим; прокси доносит его в строку). Деньги — ЦЕЛЫЕ микро-доллары
   (int64 minor units). Пишется и для неуспешного вызова (обрыв: usage нет →
   tokens 0, usd 0 — записанная граница). НЕ пишется для unpriced-модели и
   неизвестного токена — ветви требуют ОТСУТСТВИЕ строки [К2-6]. Направление
   производности доказывается: `--rebuild-budget` пересобирает `budget.json`
   из журнала; барьер удаляет budget.json, запускает пересборку и сверяет.
   `data/metering/` — локальное состояние, в `.gitignore` [К1-С3].
4. **Тарификация** [К2-3]: цены в `config/metering.json` per provider+model:
   `per_m_tokens: { in: <int микро-USD>, out: <int> }`; формула закреплена:
   `usd = (tokens_in*in + tokens_out*out) / 1_000_000` в целых микро-USD
   (округление вниз — консервативно для бюджета? НЕТ — вверх: списываем не
   меньше начисленного провайдером; выбор закреплён: ОКРУГЛЕНИЕ ВВЕРХ).
   Барьер предъявляет вызов с ИЗВЕСТНЫМИ порождёнными usage и тарифом и
   сверяет вычисленный ненулевой ЦЕЛЫЙ usd с формулой. Модель без цены —
   503 `model_unpriced` ДО запроса, upstream не вызван, строки нет [К1-2, К2-6].
5. **Потолок ДО запроса, списание ПОСЛЕ** [К1-8]: `ceilings: { "<provider>":
   { "usd_per_month": <int микро-USD> } }`; период — КАЛЕНДАРНЫЙ МЕСЯЦ UTC,
   `periodAt(ts)` чистая функция UTC `YYYY-MM`, вызывается НА КАЖДОМ запросе
   (selftest на стыке месяцев фиксированным временем). Исчерпан — 402 с
   состоянием бюджета ДО запроса; перерасход на один вызов — записанная граница.
6. **Запуск лаунчером** [К2-7]: `workshop` перед сессией проверяет `GET /healthz`
   порта из конфига (умолчание 8765); не отвечает — поднимает прокси фоновым
   процессом (pid-файл `~/.config/dev-harness/proxy.pid`), ждёт ОКНО
   `W_healthz` (именованная настройка конфига, умолчание 5 с); не поднялся
   или погиб на старте — отказ при запуске с причиной. Недоступный upstream
   НЕ повод гибнуть: прокси с мёртвым upstream поднимается и здоров, отказ
   upstream — пер-запросный (502 со строкой журнала, ветвь (д)) — арбитраж
   proksi-cold-start, замер референса.
   Проверяется командой `check_metering.sh --cold-start` (критерий 4).
7. **Живой маршрут — механизмом с коррелятором** [К1-6, арбитраж]:
   `check_metering.sh --live` посылает через ЖИВОЙ omp вызов с порождённым
   uuid в заголовке `x-request-id` (канал заголовков omp — до исполнения
   замеряется автором; не доносит — коррелятор вычисляется как функция от
   uuid, детерминированная и восстановимая из строки), затем ищет в
   `calls.jsonl` строку с ЭТИМ коррелятором и ожидаемой ролью. Найдена → 0;
   прямой маршрут строки с коррелятором не рождает → 1. Роль+ts отдельно
   критерием не считаются.

## Критерий готовности

1. `node scripts/proxy/metering_proxy.ts --selftest` → 0: `periodAt` на стыке
   месяцев; приписывание токена; свёртка журнала в бюджет (целые микро-USD);
   append-only запись; формула тарификации с округлением вверх.
2. `bash scripts/check_metering.sh` → 0 с подставным upstream. ДЕСЯТЬ ветвей
   — (а), (б), (в), (в2), (г), (д), (е), (ж), (з), (и) — каждая отдельным
   красным вызовом собственной фикстуры [К1-4], все порождаемые значения
   порождаются при каждом прогоне [К1-5]:

   - (а) прокси не поднят → код 2;
   - (б) неизвестный ПОРОЖДЁННЫЙ токен → 401, строки нет;
   - (в) составной POST [арбитраж, К4-1, К4-6, К5-1, К5-2]: порождённые
     provider/model/путь ≠ chat/completions, порождённые ТЕЛО запроса (не-JSON,
     порождённые байты), request content-type ≠ application/json и заголовок
     x-request-id; upstream-счётчик фиксирует метод POST, путь, ТЕЛО байт-в-байт,
     REQUEST content-type и request_id — все пять дословно; upstream отвечает
     порождёнными статусом ≠200, response content-type ≠ application/json,
     телом-не-JSON → клиент получил байт-в-байт тело+статус+content-type; строка
     ПОЛНОЙ схемы: роль по токену, usage из ответа, ненулевой ЦЕЛЫЙ usd по
     формуле, все поля. ОБМАННЫЕ ПРЕДЪЯВЛЕНИЯ [арбитраж, К5-2]: стаб POST-only
     ретранслятора (GET превращает в POST) и стаб с пересборкой ответа из
     usage — ОБЯЗАНЫ краснеть на этой ветви;
   - (в2) составной GET [арбитраж, К4-1, К5-1]: порождённый путь; upstream
     фиксирует метод GET и путь; та же сверка нетривиальной тройки ответа
     (статус ≠200, response content-type ≠ application/json, тело-не-JSON)
     байт-в-байт; строка полной схемы; обманные предъявления (в) краснеют и
     здесь.
   - (г) исчерпанный потолок → 402 ДО запроса (счётчик upstream не вырос);
   - (д) обрыв upstream → строка со статусом ошибки, tokens/usd = 0;
   - (е) маркер append-only жив после прогона; удалённый budget.json
     пересобран `--rebuild-budget` и равен свёртке;
   - (ж) модель без цены → 503 ДО запроса, upstream не вызван, СТРОКИ НЕТ;
   - (з) в РЕПОЗИТОРИИ нет буквальных значений секретов: grep шаблона
     значения секрета по ВСЕМ файлам репозитория — пуст [К2-2];
   - (и) РОТАЦИЯ [арбитраж, К4-2]: собственный файл секретов фикстуры,
     доступный прокси через цепочку `--config` (реальный дом секретов НЕ
     трогается); один живой процесс (тождество pid утверждается); T1 приписан
     роли R → строка с R сверена; файл секретов перезаписан: T1 удалён, T2
     приписан ТОЙ ЖЕ роли R; T2 принят → строка с R сверена; T1 даёт 401 и
     НОВОЙ строки нет. Красное: реализация со стартовым кэшем падает на T2.
3. `bash scripts/check_metering.sh --live` → 0 [К1-6, арбитраж, К4-3]: uuid-вызов
   через живой omp → строка с КОРРЕЛЯТОРОМ, ожидаемой ролью И свежим
   `ts` (строго позже старта пробы — сверка тройки); прямой маршрут → 1.
4. `bash scripts/check_metering.sh --cold-start` → 0 [К2-7, К3-4, арбитраж
   proksi-cold-start, К4-4, К4-5]: (1) kill по pid-файлу →
   `workshop --check-metering` → healthz отвечает, pid жив; (2) подставной
   слушатель на порту → код 1 с причиной; (3а) ФАТАЛЬНО битый конфиг
   (невалидный JSON) → процесс прокси гибнет на старте (собственный ненулевой
   код гибели ребёнка фиксируется фикстурой) → переход лаунчера → код 1 за
   полное настенное время ≤ W_probe, замер фикстурой; СОБСТВЕННОЕ красное
   предъявление (3а): лаунчер-заглушка, не наблюдающий гибель ребёнка и
   ждущий W_probe+1 → ветвь красная [К4-5]; (3б) процесс жив, healthz молчит →
   отказ по окну W_healthz → код 1 за время ≥ W_healthz и ≤ W_probe, замер
   фикстурой; СОБСТВЕННОЕ красное (3б) [арбитраж, К5-3]: лаунчер-заглушка БЕЗ
   общего колпака времени — безгранично ждёт живого молчащего ребёнка →
   фикстура валит её по внешней границе W_probe (полное настенное время
   превысило W_probe — ветвь красная). Дополняющее красное нижней границы
   (возврат раньше W_healthz) остаётся. ДВА ИМЕНОВАННЫХ ЧИСЛА:
   W_healthz = 5 с (окно ожидания лаунчера, конфиг), W_probe = 15 с (внешняя
   граница фикстуры, строго больше внутреннего).
5. `npm run check:antiplacebo` → 0; `check:ci-parity`: в CI — только
   `--selftest` (CI без сети) — объявленное исключение с причиной.

## Исполнители и зоны

ЗОНА architect: contracts/005-avtonomija-proksi.md scripts/proxy/ scripts/check_metering.sh fixtures/check_metering/ config/metering.json NABLIUDENIA.md HANDOFF.md roles/ .omp/config.yml .omp/agents/ .gitignore
ЗОНА implementer: package.json .github/workflows/ci.yml workshop .omp/models.yml

Связность: прокси, барьер, фикстуры и конфиг учёт — архитектору (красные ДО
раздачи); проводка (npm-команды, CI), запуск лаунчером и назначение
псевдонимов-токенов в models.yml — исполнителю; роли и их modelRoles —
архитектору (соответствие держит `check:gen`); `.gitignore` — архитектору.

## Приёмка

1. `node scripts/proxy/metering_proxy.ts --selftest` → 0.
2. `bash scripts/check_metering.sh` → 0 (подставной upstream, все порождения).
3. `bash scripts/check_metering.sh --live` → 0.
4. `bash scripts/check_metering.sh --cold-start` → 0.
5. `npm run check:antiplacebo` → 0; `check:ci-parity` → 0.
6. `bash scripts/check_skills.sh --live` → 0 (прокси не сломал живую пробу).

## Незаполненные требования:

- порт 8765 занят чужим процессом — решение за владельцем при живом прогоне:
  до его слова лаунчер отказывает с причиной, авто-выбор порта НЕ делается
  (молчаливая смена адреса — расход мимо учёта).

## Статус

Статуса задач нет: контракт заморожен побайтово, ход работ — в `HANDOFF.md`.
