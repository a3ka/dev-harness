# Контракт 026 — жнец ./tmp: реап in-tree скратча при close-out (слой 1 из Н-97)

Микроконтракт по слову владельца 2026-09-15 (Н-97): жнец — МЕХАНИЗМ при close-out,
расширение `scripts/gc_agent_branches.sh`; НЕ агент-жнец, НЕ уборка-ролью (роль,
заменяемая механизмом, заменяется). Боль измерена 2026-09-15 и перепроверена 2026-09-16
(живой основной чекаут): `du -sh tmp` = **1.2 ГБ**; `git ls-files --others -- tmp/` =
**12 584** untracked-файла; верхнеуровневых записей: **227** по git (python3 lstat видит
234: +7 пустых каталогов, git-невидимых, и tracked `tmp/README.md`); возраст по mtime
записи: **219 из 227** старше 7 дней; **94** записи несут номер контракта 004–025 (все
эти контракты done). Происхождение — клоны ролей (adversary004, omp-17.3.5), барьерные
прогоны, убитые без trap (Н-68: мусор убитых прогонов), десятки `*-task.md`. Первым этот
объём увидел детектор 024 (нога-2 читает мимо .gitignore) — по Н-97 НЕ чистить ради
детектора: уборка отдельной гигиеной, этим предметом, через механизм.

## Предмет (инварианты)

Расширение `scripts/gc_agent_branches.sh` ветвью TMP-РЕАП. Существующие ветви (снос
слитых wip/*, СПИСОК зависших, `--expect-kept`, sweep остатков worktrees) не меняются.

1. **Источник кандидатов — только untracked**: `git ls-files --others -z -- tmp/` БЕЗ
   `--exclude-standard` — тот же источник, что нога-2 детектора 024. `tmp/` сидит в
   `.gitignore` — источник с `--exclude-standard` слеп по построению. Кандидат —
   ВЕРХНЕУРОВНЕВАЯ запись: путь сворачивается до первого компонента после `tmp/`
   (клон из 3947 файлов — один юнит удаления; само `tmp/` никогда не удаляется).
2. **tracked не трогается НИКОГДА**, двумя мерами: (а) источник не перечисляет tracked;
   (б) перед каждым удалением контроль `git ls-files -- tmp/<запись>` ПУСТ (tracked =
   запись индекса, включая staged-новые). Непустой контроль при untracked-кандидате под
   той же записью = СМЕШАННЫЙ вход: реап останавливается (уже удалённое остаётся
   удалённым), именованный отказ rc 1 «смешанный вход tmp/<запись>: tracked+untracked —
   владельцу», запись ЦЕЛА (fail-closed).
3. **Критерий реапа кандидата** (дизъюнкция):
   - **(а) done-тег**: имя записи содержит NNN из `0[0-9][0-9]` И в репозитории есть тег
     `done/contracts/NNN/*` → реап независимо от возраста;
   - **(б) возраст**: запись не несёт РАСПОЗНАННОГО NNN И mtime записи старше N дней
     (по умолчанию 7; замер: 219/227 git-видимых записей старше 7 дней, свежие анонимы
     1–7 дней — 5 записей — выживают, пауза выходных покрыта; флаг `--tmp-reap-age`).
   Распознанный NNN = есть `contracts/0NN-*.md` в дереве ИЛИ done-тег NNN. Запись несёт
   распознанный NNN без done-тега = АКТИВНЫЙ контекст → ВЫЖИВАЕТ независимо от
   возраста (скратч активного майлстоуна; долгий контракт не гибнет от возраста).
   Активный и done NNN в одном имени → выживает (консервативно). NNN без файла
   контракта и без тега — аноним, правило возраста.
4. **Dry-run по умолчанию**: каждый прогон gc печатает в stderr TMP-РЕАП список
   кандидатов (путь + причина: `done NNN` / `возраст Xd`) и счёт. Удаление — ТОЛЬКО
   явным флагом `--tmp-reap-apply` (Н-59: молчание ≠ разрешение; симметрия СПИСКА
   зависших). Остаточный риск назван: done-тег предполагает закрытие сессий контракта
   до close-out — та же норма, что у gc для worktrees («после того как НИ ОДИН спавн
   жив»).
5. **mtime — lstat самой записи, python3** (Н-60: GNU find слеп к fifo/симлинкам; та же
   мера, что sweep остатков worktrees). Нет python3 → rc 2 NOT_IMPLEMENTED.
6. **Коды возврата**: 0 — порядок (сухой список напечатан / применено всё); 1 —
   смешанный вход, не удалось удалить кандидата, плюс прежние причины (заявленная
   зависшая, смена OID); 2 — нечем проверить. Пустой tmp/ и сухой список — rc 0.
7. **REGRESS-024** (критическая приёмка владельца): реап НЕ ослабляет ногу-2. Свежая
   (≤ N дней, без done-имени) утечка в `tmp/` остаётся ловимой `check_no_leak.sh
   --check` rc 1 после применения реапа; реап стейла между `--snapshot` и `--check` —
   только исчезновение строк манифеста, ложного rc 1 не даёт (дельта 024 судит новые
   строки, исчезновение — чистка).

## Зоны

ЗОНА architect: contracts/026-zhnec-tmp.md fixtures/gc_agent_branches/ NABLIUDENIA_ARCHITECT.md
ЗОНА implementer: scripts/gc_agent_branches.sh
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/
ЗОНА orchestrator: NABLIUDENIA.md HANDOFF.md roles/orchestrator.md (одна строка нормы
close-out: gc с `--tmp-reap-apply` после done при отсутствии живых спавнов; не блокер
приёмки предмета)

Заморожено вне зон: `scripts/check_no_leak.sh` (контракт 024 — пиннованный оракул пробы
4, не предмет), `scripts/verify_antiplacebo.sh` (011/012/014; carve-out 011 — скратч
раннера вне дерева, мотивация Н-48), `AGENTS.md` правило 16 (строка 255: «Временное — в
`./tmp`» — норма; её отмена — слой 2, контракт 027, требует строки РАЗРЕШИЛ-ВЛАДЕЛЕЦ по
прецеденту 1737953).

## Приёмочный критерий

счёт: 7 фикстур в fixtures/gc_agent_branches/ — 4 предъявления
(`red_zhnets_reap_stale.sh`, `red_zhnets_aktiv_vyzhivaet.sh`,
`red_zhnets_tracked_netrogat.sh`, `red_regress_024_noga2.sh`) + 3 постоянных
(`case_zhnets_reapstale.sh`, `case_zhnets_aktiv_vyzhivaet.sh`,
`case_zhnets_tracked_netrogat.sh` — те же инварианты 1–3 батареей мета-прогона, входят
в глоб вместе с существующими `case_zavisshaja_*`). Все 7 красны на текущем HEAD, зелены
после предмета; краснота предъявлений доказывается свапом на пред-фикс блоб
`scripts/gc_agent_branches.sh` (церемония 024). Стабы привязаны к входам, где дефект
наблюдаем (Н-39); каждая ветвь пробы красна СВОИМ предъявлением.

1. `red_zhnets_reap_stale.sh` — toy-репо: аноним mtime −10д + done-именованный (тег
   `done/contracts/021/1`, имя `critic021v3`, mtime −1ч); `gc --root T
   --tmp-reap-apply` → rc=0, ОБЕ записи отсутствуют. Сейчас rc=1 (gc не знает флаг:
   usage). Стабы: **S-exclude** (источник с `--exclude-standard` — tmp под .gitignore,
   реап пуст) — красна этим входом; **S-ageonly** (возраст без done-правила) — свежая
   done-именованная выживает, красна этим входом.
2. `red_zhnets_aktiv_vyzhivaet.sh` — toy: `arch026task.md` при живом
   `contracts/026-*.md` без done-тега, mtime −40д + свежий аноним (−1ч); apply → rc=0,
   ОБЕ целы. Стабы: **S-ageonly** сносит старый активный; **S-sweepall** (всё tmp без
   разбора) сносит обе.
3. `red_zhnets_tracked_netrogat.sh` — две ветви, каждая своим предъявлением:
   (а) tracked `tmp/old021/x` (в индексе), mtime −40д, done-имя → не кандидат: цел,
   rc=0; (б) смешанный `tmp/mix/` (tracked y + untracked z) → именованный отказ rc=1
   «смешанный вход», ОБА целы, отказ называет запись (правило 7). Стаб: **S-find**
   (find-подметание по mtime мимо git-источника) — сносит tracked, красна обеими
   ветвями.
4. `red_regress_024_noga2.sh` — toy-репо + живой `scripts/check_no_leak.sh` (оракул,
   не предмет): (а) снимок → свежая утечка `tmp/leak-026.txt` + реап стейла
   (`--tmp-reap-apply`) → `--check` rc=1, утечка поимённо; (б) без утечки, только реап
   стейла между снимком и сверкой → `--check` rc=0 (исчезновение ≠ утечка). Стаб:
   **S-overreach** (реап хватает и свежие) — ветвь (а) даёт rc=0, красна этим входом.

Прогоны (точные команды; сейчас → после):

    bash fixtures/gc_agent_branches/red_zhnets_reap_stale.sh        # rc 1 → 0
    bash fixtures/gc_agent_branches/red_zhnets_aktiv_vyzhivaet.sh   # rc 1 → 0
    bash fixtures/gc_agent_branches/red_zhnets_tracked_netrogat.sh  # rc 1 → 0
    bash fixtures/gc_agent_branches/red_regress_024_noga2.sh        # rc 1 → 0
    bash scripts/gc_agent_branches.sh --root <живой корень>         # rc 0, TMP-РЕАП dry-run список

После реализации: все 4 red → rc=0; мета-прогон семьи `fixtures/gc_agent_branches/`
зелёный (существующие `case_zavisshaja_*` целы); полный прогон — на CI (приёмка v2,
Н-48).

## Не-цели

- Слой 2 ($HARNESS_SCRATCH durable) — отдельный контракт 027; развилка ниже.
- Агент-жнец и уборка-ролью — отвергнуты владельцем (Н-97).
- Ручная чистка существующих 1.2 ГБ — только через механизм при close-out (Н-97:
  грязное дерево — реальный мир для детектора 024).
- Пустые untracked-каталоги tmp/ — git-невидимы, вне источника; отдельная ветвь по
  боли (замер: 7 штук).
- Архивирование перед удалением, touch/reset mtime — не вводятся.
- Правки check_no_leak.sh, verify_antiplacebo.sh, AGENTS.md — заморожено (зоны).

## Развилка слоёв (frontier; рекомендация архитектора: 026 = слой 1, слой 2 = 027)

Измерение миграции слоя 2 (grep по scripts/ и roles/, эта пачка): **19**
скриптов-писателей в `<корень>/tmp` (check_approval, check_charter,
check_check_contract_ready, check_contract_frozen, check_judge_gate, check_metering,
check_protected, check_runner_hygiene, check_scope_select, check_scoped_run,
check_zones, drill_contract_change, drill_exit_marker, drill_gate_draft,
drill_next_id_race, drill_path_guard, drill_protected_exception, drill_startup_digest,
land_agent) + **2** роли-клона (`roles/critic.md:155`, `roles/implementer.md:36` —
клоны в `./tmp`) + норма `AGENTS.md:255` (правило 16 — отмена нормы) + **2** ветви
барьера пиннуют текст нормы (`check_runner_hygiene` carveout :667-676, klon :832-839) +
семантика ветви «з» `check_metering` (:852-866, исключение ./tmp из поиска секрета).
Миграция TMP_ROOT 19 скриптов переносит и игрушки фикстур автоматически (фикстуры пишут
в tmp игрушечных деревьев под этими TMP_ROOT); 13 фикстур, ссылающихся на tmp, смены
почти не требуют.

Почему раздельно: (1) слово Н-97 само разделяет — «расширить gc_agent_branches» (механизм
сейчас) и «durable — обобщение carve-out 011 … в очередь»; (2) поверхность слоя 2 —
контракт размера 023/025 сам по себе, связка удваивает судейскую поверхность одного
круга (уроки 007/Н-47: предмет, растущий в прогоне, рождает круги); (3) слой 1 не зависит
от нормы и снимает боль (1.2 ГБ) на первом же close-out; (4) REGRESS-024 несётся в ОБЕИХ:
026 — реап не ослепляет ногу-2, 027 — carve-out не ослабляет (утечка в
`./tmp/что-угодно-не-в-carve-out` остаётся ловимой rc 1). При выборе владельцем 1+2
единым контрактом — respec этого файла ДО заморозки с расширением приёмки пробой durable.

Затравка 027 — `red_durable_scratch_vne_dereva.sh` (спека): producer при пустом
HARNESS_SCRATCH пишет под `${TMPDIR:-/tmp}/dev-harness-scratch/<hash8-канон-корня>/` ВНЕ
дерева (каноникализация меры 014: спуск до существующего предка + `pwd -P`); ЯВНЫЙ
HARNESS_SCRATCH, разрешающийся внутрь корня → именованный отказ rc 2 «внутри стерегомого
дерева» ДО создания (событийно, мера монитора 014); спаренный REGRESS-024: утечка в
`./tmp/` мимо carve-out ловима ногой-2 rc 1.

## Незаполненные требования

- Форма строки сухого списка (путь + причина) и точка врезки в выводе gc — деталь
  реализации; пиннуты счёт и stderr.
