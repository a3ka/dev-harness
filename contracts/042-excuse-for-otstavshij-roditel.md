# Контракт 042 — `excuse_for()` теряет ALLOW за мержем с отставшим родителем (Н-127)

Статус: самоприменение (architect, одним заходом — обоснование в §Зоны). Точечный фикс
`excuse_for()` и распознавания мерж-артефактов (`is_lagging_merge_artifact`, `state_at`) в
`scripts/check_protected.sh`; красная пачка на toy-репозиториях написана и прожита живыми
прогонами ДО и ПОСЛЕ правки в дереве до раздачи; предмет приземляется с кодом уже живым,
критик проверяет его прогоном, а не заявлением.

## Предмет

`check_protected.sh:excuse_for()` требует строку `ALLOW-ARTIFACT-DELETE:` ИМЕННО в удаляющем
коммите пути `p`; удаляющий коммит ищется командой

```
git log HEAD --full-history --no-renames -m --diff-filter=D --format=%H -- <p> | sed -n 1p
```

`-m` показывает диф МЕРЖ-коммита ОТДЕЛЬНО против КАЖДОГО его родителя (находка круга 2
контракта 039, нужна для честного modify/delete в теле мержа). Если удаление уже случилось на
одной ветке РАНЬШЕ, а вторая ветка форкнулась ДО удаления и слита в первую уже ПОСЛЕ него, то
относительно этой второй, «отставшей» (не видевшей удаления) ветки-родителя сам мерж-коммит
показывает путь как `D` — он и есть первая (самая свежая) строка вывода команды выше, хотя
ничего не удалял: он лишь унаследовал уже-отсутствующее состояние от НЕ-отстающего родителя.
`sed -n 1p` брал именно его, тело мержа не несёт ALLOW-строку (её незачем повторять — разрешение
уже сказано в настоящем удаляющем коммите), и `excuse_for` честно отвечал «не разрешено» —
легитимное удаление читалось как нарушение.

## Диагноз (живые доказательства на реальном дереве dev-harness)

Живой прогон ДО фикса: `bash scripts/check_protected.sh .` → `rc=1`, три FAIL на файлах,
легитимно удалённых коммитом `4b3666f` (три корректные строки `ALLOW-ARTIFACT-DELETE:`):
`verdicts/critic/contracts-040-v{2,3,4}.md`.

Прямая мера `excuse_for` на одном из путей:

```
$ git log HEAD --full-history --no-renames -m --diff-filter=D --format=%H \
    -- verdicts/critic/contracts-040-v2.md | sed -n 1p
b2240a451a4c9c34a8acc9d68b415c1394e9bc5b
```

Это `b2240a4` — мерж «приземление контракта 041», тело ПУСТОЕ (без ALLOW), НЕ настоящий
удаляющий коммит. Настоящий — `4b3666f` (единственный родитель `560bc554`, тело несёт ровно три
строки `ALLOW-ARTIFACT-DELETE:`, по одной на каждый из трёх путей), и он — предок HEAD
(`git merge-base --is-ancestor 4b3666f HEAD` → 0). Родители `b2240a4` разошлись ИМЕННО так, как
описано в §Предмет:

| родитель | `4b3666f` — предок? | путь `contracts-040-v2.md` в дереве родителя |
|---|---|---|
| `b465471` (первый, main-линия ПОСЛЕ удаления) | да | отсутствует (уже удалён) |
| `dfbb210` (второй, ветка 041, форкнута ДО удаления) | нет | присутствует (блоб `2021934b…`) |

Второй родитель — отставший (ещё не видел `4b3666f`), путь у него ЖИВ; относительно него мерж
`b2240a4` и показан диф-фильтром как `D`. Источник в NABLIUDENIA.md — Н-127 (там же —
дословный диагноз владельца/оркестратора и оба кандидата фикса, не решённые записью).

## Направление фикса — выбор и обоснование

Запись Н-127 называет два кандидата, не решая между ними. Оба разобраны здесь.

**Кандидат А (отвергнут): искать ALLOW по ВСЕМ diff-filter=D событиям пути, а не только по
первому.** Реализация — тривиальная (снять `sed -n 1p`, пройти по всем строкам, принять первое
совпадение). Но `excuse_for` — это ИМЕННО тот механизм, что уже был ослаблен и уже был закрыт
после адверсария на плане 005 (комментарий шапки, :202-205 текущего файла): «Прежняя редакция
собирала разрешения из тел ВСЕХ достижимых коммитов… разрешение годилось ПОВТОРНО после
восстановления файла, и его можно было положить ЗАРАНЕЕ в посторонний коммит». Кандидат А не
собирает разрешения из ЛЮБЫХ коммитов — только из diff-filter=D-кандидатов ЭТОГО пути — но это
ТОЧНО ТА ЖЕ дыра в узком виде: путь `p`, удалённый С разрешением коммитом `D1`, восстановленный
коммитом `R`, затем удалённый БЕЗ разрешения коммитом `D2` — `D1` и `D2` ОБА присутствуют в
списке diff-filter=D для `p` (оба — реальные события «путь исчез из дерева относительно
родителя»), и «искать по всем» нашло бы ALLOW у `D1`, ошибочно извинив `D2`. Закрывать ЭТУ
дыру кандидату А потребовалось бы отдельной логикой «событие следует засчитывать, только если
после него путь не восстанавливался» — а это по сложности не меньше кандидата Б, при этом
идёт ПРОТИВ явно защищённого антигейминг-инварианта, а не рядом с ним. Риск: реген старой,
уже найденной и закрытой дыры чужим адверсарием (план 005) — неприемлемо для точечного фикса.

**Кандидат Б (принят): исключать мерж-коммит из кандидатов на «удаляющий коммит», если
бесконфликтный трёхсторонний мерж его родителей дал бы то же самое отсутствие пути `p` БЕЗ
единого ручного действия.** Наивная формулировка «путь отсутствует хотя бы у одного родителя»
НЕДОСТАТОЧНА: путь `p`, удалённый С разрешением (`D1`), может быть ВОССТАНОВЛЕН на главной
ветке (`R`) и УДАЛЁН ЗАНОВО явным действием внутри мерж-коммита (`git merge --no-ff
--no-commit` независимой ветки `side`, не трогавшей `p`, затем `git rm p` и коммит `D2` БЕЗ
ALLOW). У ВТОРОГО родителя `D2` (`side`) путь действительно отсутствует — но НЕ потому, что
мерж унаследовал это состояние: бесконфликтный трёхсторонний мерж этих же двух родителей взял
бы версию ПЕРВОГО родителя (путь присутствует, восстановлен `R`), а не отсутствие. «У родителя
пути нет» и «мерж унаследовал его отсутствие» — РАЗНЫЕ утверждения, и наивный предикат путает
их. Правильный вопрос — сравнение с базой слияния (`git merge-base` двух родителей): мерж —
артефакт, ТОЛЬКО если СУЩЕСТВУЕТ родитель, чьё состояние пути `p` совпадает с базой (родитель
«молчит» — ничего не менял), И состояние пути `p` у ДРУГОГО родителя — тоже отсутствие (то,
что бесконфликтно взял бы мерж от «молчащего» родителя, совпадает с заявленным исчезновением
в самом мерже). Если путь присутствует У ВСЕХ родителей, ИЛИ «молчащий» родитель есть, но
состояние другого родителя — присутствие (случай `D2` выше), — отсутствие в дереве мержа
создано ИМ САМИМ (настоящее удаление, требующее собственного ALLOW), и мерж остаётся
кандидатом без изменений. `excuse_for` пропускает распознанные артефакты по порядку `git log`
и ОСТАНАВЛИВАЕТСЯ на первом кандидате, который артефактом не является, — это и есть настоящий
удаляющий коммит; ДАЛЬШЕ поиск не идёт. Именно это «дальше не идёт» держит антигейминг-
инвариант: разрешение всё ещё берётся из РОВНО ОДНОГО, однозначно определённого коммита, а не
из произвольного более старого — уточнение критерия НЕ ослабляет эту границу, оно лишь чинит
то, ПО КАКОМУ признаку коммит считается артефактом. Сложность — три функции (`blob_at`,
`is_lagging_merge_artifact`, неизменённый `excuse_for`), один `git merge-base` и три
`git ls-tree` на кандидата — не на историю целиком (Н-126-класс рисков не касается: вызывается
только для коммитов, УЖЕ найденных diff-filter=D-фильтром, которых для одного пути единицы).
Октопус-мержи (3+ родителя) и неоднозначная/отсутствующая база слияния — fail-closed, никогда
не признаются артефактом (§Риски). Не-ослабление уточнения проверено живым прогоном на ИМЕННО
той топологии восстановления-и-повторного-удаления через мерж (§Приёмка, R6): наивная
формулировка «путь отсутствует хотя бы у одного родителя» на этом дереве ошибочно извиняла
`D2` разрешением `D1` (rc=0); уточнённая — распознаёт `D2` как настоящий удаляющий коммит без
ALLOW (rc=1).

Выбран кандидат Б.

## Семантика (точная, реализация несёт эти строки побайтово в логике, не в тексте)

Мерж-коммит `c` с РОВНО двумя родителями `P1`,`P2` — артефакт по пути `p` (унаследованное
отсутствие; `excuse_for` пропускает такой коммит и ищет настоящий удаляющий коммит дальше),
ЕСЛИ И ТОЛЬКО ЕСЛИ все три условия сразу (формулировка — арбитраж `verdicts/arbitration/
042-dostatochnost-merge-base-semantiki.md`, «Решение по существу», дословно):

1. `git merge-tree --write-tree --no-messages -z P1 P2` завершился rc=0 или rc=1
   (0 — чисто; 1 — есть конфликты; rc>1 и любой сбой — fail-closed, НЕ артефакт);
2. `p` НЕ входит в секцию конфликтных путей вывода (`-z`-форма, побайтовое сравнение путей;
   при rc=0 секция пуста);
3. в результирующем дереве авто-мержа `p` ОТСУТСТВУЕТ (state снимается робастной формой
   ниже).

Во ВСЕХ прочих случаях (`c` не имеет ровно 2 родителей; rc>1 у `merge-tree`; `p` в
конфликтах; `p` присутствует в результирующем дереве) — `c` НЕ артефакт, остаётся
кандидатом на «настоящий удаляющий коммит» на общих основаниях, требует собственный ALLOW.
Октопус-мержи (3+ родителя) — fail-closed без исключений; `git merge-tree --write-tree`
недоступен (git < 2.38) — именной rc=2 на пробе при старте файла (§Риски).

СНЯТИЕ СОСТОЯНИЯ ПУТИ — ТОЛЬКО РОБАСТНОЙ ФОРМОЙ (арбитраж 042, класс 2 решения). Условие 2
читает секцию конфликтных путей `-z`-формой с NUL-разбором: имя файла в записи `<mode>
<object> <stage>\t<path>` — всё ПОСЛЕ первого TAB, mode/oid/stage его не содержат. Условие 3
снимает state ОДНОГО пути формой `git ls-tree --format='%(objectmode) %(objectname)'
<дерево> -- ':(literal)<путь>'` — state ПАРА mode+oid, НЕ голый oid, присутствие судится по
непустой строке. НИКАКОГО awk-разбора по пробельным полям ни в одном из двух мест: путь с
пробелом обязан читаться тем же способом, что и путь без него (класс дефекта, не единичная
дыра).

`excuse_for(p)`: перебирает коммиты из `git log HEAD --full-history --no-renames -m
--diff-filter=D --format=%H -- p` В ПОРЯДКЕ ВЫВОДА; пропускает (`continue`) кандидатов, для
которых три условия выше выполнены; НА ПЕРВОМ не-артефакте — читает его тело ТОЙ ЖЕ
логикой разбора строк `ALLOW-ARTIFACT-DELETE:`, что была всегда (кавычки, первая колонка,
безымянное/безпричинное разрешение — `bad()`), и ЗАВЕРШАЕТСЯ на этом кандидате: `return 0`
при найденном валидном разрешении для `p`, иначе `return 1` БЕЗ дальнейшего поиска. Пустой
список кандидатов (не должно происходить для пути из `missing`, но fail-closed) → `return 1`.
НИКАКАЯ другая функция (`moved_for`, область по `roles/*.md`, разбор поля `verdict`, `comm`-
разность, env-гигиена, коды 0/1/2) не тронута.

## Приёмка

Стаб-привязки Н-39 — в шапках фикстур (кому какая ветка кода делает узел развилки видимым).
Команды — из корня дерева (реального ИЛИ клона, где сказано явно).

| # | Проба | Команда | ДО фикса | ПОСЛЕ фикса |
|---|---|---|---|---|
| R1 | Живой свидетель на реальном дереве dev-harness | `bash scripts/check_protected.sh .` | rc 1, 3×FAIL (`contracts-040-v{2,3,4}.md`) | rc 0 |
| R2 | ГЛАВНЫЙ КРАСНЫЙ ТЕСТ — toy-репозиторий, топология Н-127 буквально (A создаёт → B форкнут до удаления, живёт независимо → A удаляет с ALLOW → `git merge --no-ff` B в A без ALLOW в теле мержа) | `bash fixtures/check_protected/repro_n127_otstavshij_roditel.sh` | rc 1, `FAIL защищённый артефакт существовал и на HEAD его нет: verdicts/adversary/v-1.md` (та же строка/класс ошибки, что и R1) | rc 0, `ok Н-127: … (rc=0, «исчез с явного разрешения: verdicts/adversary/v-1.md»)` |
| R3 | НЕ-ОСЛАБЛЕНИЕ — ТА ЖЕ топология (мерж с отставшим родителем), но настоящий удаляющий коммит БЕЗ ALLOW нигде | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_slijanie_otstavshego_roditelja` | rc 0 (красное предъявлено: зелёный контроль + повторный прогон красный «существовал и на HEAD его нет: verdicts/adversary/v-a.md») | rc 0 (та же красная — фикс НЕ извиняет топологию саму по себе, только настоящее разрешение в настоящем коммите) |
| R4 | Семья `check_protected` целиком, включая R3, R6–R9 | `bash scripts/verify_antiplacebo.sh --scope check_protected` | 20/20 красных (до контракта 042) | 25/25 красных предъявлено (`расхождений: 0` по этой семье; v1/v2 дали 22/22, v3 добавила R7–R9) |
| R5 | Синтаксис | `bash -n scripts/check_protected.sh` | rc 0 (правка не вносит синтаксических ошибок) | rc 0 |
| R6 | НЕ-ОСЛАБЛЕНИЕ уточнения предиката Б — путь удалён с ALLOW (`D1`), восстановлен (`R`) на главной ветке; независимая ветка `side` путь не трогает; `git merge --no-ff --no-commit side` + явный `git rm` + коммит `D2` БЕЗ ALLOW: разрешение `D1` не обосновывает `D2` | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_merge_povtornogo_udalenija` | rc 0 (красное предъявлено: без этого контракта `excuse_for` берёт `D2` напрямую — своей ALLOW нет — FAIL) | rc 0 (та же красная — уточнённый предикат Б распознаёт `D2` как настоящий удаляющий коммит; наивная, ОТВЕРГНУТАЯ формулировка «путь отсутствует хотя бы у одного родителя» на этом же дереве вернула бы rc=1 у verify_antiplacebo, потому что барьер под ней даёт rc=0 — красное НЕ предъявлено, ложно извиняя `D2` разрешением `D1`) |
| R7 | НЕ-ОСЛАБЛЕНИЕ оракула (v3), З1 таблицы арбитра — mode-only modify/delete: side меняет ТОЛЬКО режим пути (OID тот же), D1 с ALLOW, `git merge --no-ff` даёт живой git-конфликт modify/delete, разрешён `git rm` БЕЗ ALLOW в теле мержа | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_merge_konflikt_moda_bez_allow` | rc 0 у предиката ДО v3 (сравнение голого oid не видит смену режима — ложно-зелёная, измерено живым прогоном) | rc 0 (красное предъявлено: путь В СЕКЦИИ КОНФЛИКТОВ оракула → не артефакт → своей ALLOW нет → FAIL) |
| R8 | НЕ-ОСЛАБЛЕНИЕ оракула (v3), З2 таблицы арбитра — criss-cross через `commit-tree`, ДВЕ равноправные базы (ни одна не предок другой), путь вручную восстановлен в P1, M убирает БЕЗ ALLOW | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_merge_criss_cross_bez_allow` | предикат ДО v3 берёт ОДНУ `git merge-base` без `--all` — историко-зависимо ложно-зелёная на части построений того же класса (арбитраж, замер З2) | rc 0 (красное предъявлено: путь ПРИСУТСТВУЕТ в авто-мерже оракула → удаление в M ручное → FAIL) |
| R9 | РОБАСТНОСТЬ разбора (v3), З3 таблицы арбитра, ОБЕ полярности — путь с пробелом: легитимная Н-127-топология (половина 1, зелёная) поверх неё повторное удаление БЕЗ ALLOW (половина 2, красная) | `bash scripts/verify_antiplacebo.sh --scope check_protected/case_probel_v_puti` | `blob_at` ДО v3 режет `ls-tree` awk-полем `$4==pth` — пробел даёт «пути нет» ВСЕГДА; на итоговом дереве этой фикстуры обе половины rc=0 у предиката (красная половина — ложно) | rc 0 (зелёный контроль — половина 1 легитимна, красное — половина 2 (повторное удаление); `state_at` снимает пару mode+oid без awk, устойчив к пробелу в обе стороны) |

R1–R2 — живые прогоны ДО/ПОСЛЕ проведены architect'ом в одноразовом клоне (байт-в-байт main на
момент старта) через `git stash`/`stash pop` вокруг правки `excuse_for`: сначала фикстуры
написаны и прогнаны на НЕТРОНУТОМ `check_protected.sh` (ДО), затем правка внесена и те же
фикстуры прогнаны повторно (ПОСЛЕ) — один и тот же файл, две точки во времени, не два разных
барьера. R3/R6 проверены вручную (`WORK`/`BARRIER` окружение, минуя IPC-обвязку
`verify_antiplacebo`) на ОБЕИХ сторонах правки И ОТДЕЛЬНО — полным прогоном
`verify_antiplacebo.sh --scope check_protected` уже ПОСЛЕ фикса v1/v2 (22/22, три расхождения вне
семьи — `check_threat_model.sh`/`fixtures/check_threat_model`/`fixtures/parsing_hygiene_battery`,
ПРЕДСУЩЕСТВУЮЩИЙ остаточный риск контракта 041, А-216 NABLIUDENIA_ARCHITECT.md, не этот
предмет и не тронут этим контрактом). Для R6 отдельно проверена ДИСКРИМИНАЦИЯ (Н-39): та же
фикстура против БУКВАЛЬНОГО содержимого барьера ДО этого контракта (`git archive` коммита
перед первым коммитом контракта) даёт rc=1 у самого барьера (красная — правильно); против
барьера С ПЕРВОЙ, наивной формулировкой предиката Б («путь отсутствует хотя бы у одного
родителя», без сравнения с базой слияния) — rc=0 у барьера (ложно-зелёная, разрешение `D1`
ошибочно переиспользовано для `D2`); против ФИНАЛЬНОГО, принятого предиката — rc=1 у барьера
(красная — правильно). Три разных содержимого барьера, три разных результата на одном и том
же дереве — фикстура различима по коду, не тавтологична.

`case_slijanie_otstavshego_roditelja.sh`, `case_merge_povtornogo_udalenija.sh` (круг 1/2) и
`case_merge_konflikt_moda_bez_allow.sh`, `case_merge_criss_cross_bez_allow.sh`,
`case_probel_v_puti.sh` (v3) подхватываются ДЕЙСТВУЮЩИМ сканированием `fixtures/
check_protected/case_*.sh` в `verify_antiplacebo.sh` — новых ключей CI/`package.json`/
`.github/workflows/ci.yml` эта пачка не заводит и не трогает.

R7–R9 (v3) верифицированы Н-39-дискриминацией живым прогоном architect'а (одноразовые
проверки, не постоянные файлы — постоянны только сама фикстура и код оракула): R7
(mode-only конфликт) — предикат ДО v3 на этом же дереве даёт rc=0 (ложно-зелёная, смена
РЕЖИМА не видна голому-oid-сравнению), оракул — rc=1 (верно); R8 (criss-cross) — предикат
ДО v3 на этом дереве историко-зависим (единственная `git merge-base` без `--all`), мутант
самого оракула, пропускающий условие 3 (присутствие пути в результирующем дереве), даёт
rc=0 (ложно-зелёная) ДЕТЕРМИНИРОВАННО на ЭТОЙ же топологии, полный оракул — rc=1 (верно);
R9 (пробел в пути) — предикат ДО v3 на ИТОГОВОМ дереве фикстуры даёт rc=0 на ОБЕИХ
половинах (вакуумная пустота awk-разбора извиняет любое состояние пути), новый предикат —
rc=0/rc=1 по половинам верно.

## Зоны

Самоприменение (обоснование, вне строк ЗОНА ниже — механический читатель зон, `scripts/
lib_zones.sh`, разбирает ТОЛЬКО строки, начинающиеся ровно с «ЗОНА », автор — всё до первого
`:`, пути — слова после него через пробел; проза рядом им не мешает и в них не входит). Фикс —
несколько функций в одном уже существующем файле, семантика полностью зафиксирована и
обоснована в этом же контракте (§Направление фикса, §Семантика), спека и код — на одном языке;
implementer не задействован. Срочность — CI-блокер, транзитивно блокирующий done контрактов
038, 040, 041. Самоприменение даёт критику код ЖИВЫМ до заморозки: все девять проб таблицы
§Приёмка прогнаны лично, живьём, до раздачи на суд.

ЗОНА architect: contracts/042-excuse-for-otstavshij-roditel.md scripts/check_protected.sh fixtures/check_protected/repro_n127_otstavshij_roditel.sh fixtures/check_protected/case_slijanie_otstavshego_roditelja.sh fixtures/check_protected/case_merge_povtornogo_udalenija.sh fixtures/check_protected/case_merge_konflikt_moda_bez_allow.sh fixtures/check_protected/case_merge_criss_cross_bez_allow.sh fixtures/check_protected/case_probel_v_puti.sh
ЗОНА orchestrator: HANDOFF.md NABLIUDENIA.md
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/

В `scripts/check_protected.sh` правка ограничена функциями `excuse_for`, `is_lagging_merge_
artifact` и вспомогательным `state_at` (в v1/v2 — `blob_at`; переименован в v3 — state
теперь пара mode+oid, не голый oid, §Семантика) — `moved_for`, область по `roles/*.md`,
`comm`-разность, env-гигиена и коды возврата не тронуты. В `fixtures/check_protected/` —
ТОЛЬКО шесть перечисленных файлов (три из круга 1/2, три новых из v3 —
`case_merge_konflikt_moda_bez_allow.sh`, `case_merge_criss_cross_bez_allow.sh`,
`case_probel_v_puti.sh`); `red_predel_git_vyzovov_ours.sh` и все прочие существующие
`case_*.sh` (предмет контракта 040) не тронуты. Оркестратор помечает Н-127 закрытым в
`NABLIUDENIA.md` после critic/freeze/done — не этим коммитом.

## ПРОВОДКА

ПРОВОДКА:
- guard=scripts/check_protected.sh

ПРОВОДКА-ЭНФОРСМЕНТ: норма — инвариант дерева не меняется этим контрактом (защищённый артефакт,
однажды существовавший, обязан жить на HEAD либо нести именную ALLOW-строку в НАСТОЯЩЕМ
удаляющем коммите); меняется ТОЛЬКО то, какой коммит признаётся «настоящим удаляющим» при
non-ff слиянии с отставшим родителем. Судится прогоном барьера на дереве истории (R1/R2),
поведение ролей предмета не несёт — guard-канал без role-строки, тот же класс, что контракт 039.

## Риски (named residual)

1. **Каскад из НЕСКОЛЬКИХ подряд «отставших» слияний одного пути теоретически разобран**
   (каждый пропущенный артефакт по определению указывает на «молчащего» родителя с историей,
   строго не включающей мерж — рекурсия терминирует по конечности DAG), но живым прогоном
   проверена только ОДНА глубина отставания (реальное дерево, R2, R3, R6). Второй уровень
   (мерж мержа) на дереве dev-harness не встречался — не заводится отдельной фикстурой этим
   контрактом; кандидат для будущего адверсария, если встретится живьём.
2. **Октопус-мержи (3+ родителя) — fail-closed ПО ПОСТРОЕНИЮ, не «покрыты кодом».**
   `is_lagging_merge_artifact` требует РОВНО двух родителей (`[ "${#parents[@]}" -eq 2 ]`);
   при 3+ родителях функция сразу возвращает «не артефакт», и мерж остаётся кандидатом на
   общих основаниях — безопасное направление ошибки (требует ALLOW, а не прощает), но не
   решает семантику октопуса, а уклоняется от нее. На дереве dev-harness октопус-мержей нет;
   отдельной фикстурой не заводится.
3. Один `git merge-tree --write-tree` и одно-два робастных `git ls-tree` (`state_at`) на
   КАЖДОГО двухродительского diff-filter=D-кандидата пути (не на всю историю; v3 сменила
   механизм с `git merge-base` + три `ls-tree` на оракул, риск и его класс — те же) —
   Н-126-класс риска (масштаб на органическом росте) не активирован: количество кандидатов
   на один пропавший путь ограничено числом реальных событий удаления этого пути, а не
   размером репозитория.
4. Критерий сравнивает состояние пути `p` С ОБЕИХ сторон мержа независимо для каждого
   diff-filter=D-кандидата; если у мержа ОБА родителя одинаково изменили путь (например, оба
   независимо удалили его без ALLOW) — ни один не «молчит» относительно базы, и мерж НЕ
   признаётся артефактом, остаётся кандидатом сам по себе (fail-closed: требует собственный
   ALLOW). Отдельной фикстурой этот под-случай не заведён — на дереве dev-harness не
   встречался, поведение выведено из кода и рассуждения выше, не из живого прогона.
5. **Запись объектов.** `merge-tree --write-tree` пишет деревья в object DB. Барьер уже
   пишет в `$ROOT/tmp`; осиротевшие деревья собираются gc. Именованный остаток, не дыра.
6. **Дрейф мерж-семантики между версиями git.** Вердикт «артефакт/нет» теперь совпадает с
   тем, что сделал бы `git merge` ЭТОЙ версии. Это принимается сознательно: предмет барьера
   — «наследование против ручного действия», и авторитет здесь git, а не наша
   реконструкция. Именованный остаток.
