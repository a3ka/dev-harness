# Контракт 011 — приёмка судьи v2 + гигиена раннера antiplacebo

Номер выдан механизмом: `next_id.sh CONTRACT` → 011, тег `id/CONTRACT/011`. Предмет —
решение грилинга владельца 2026-08-24 (V3=(а), FIXED): гейт-приёмка Н-48/Н-41 — норма
приёмки v+1 плюс инженерия раннера. Смешанный контракт: КОДОВАЯ половина (раннер)
принимается красными тестами, НОРМ-половина (AGENTS.md/010) — исполняемой формой
(grep-маркеры); прецедент 010 (ФОРМА исполняема / СЕМАНТИКА у ревьюера) здесь не нужна —
вся приёмка механическая.

**ЗАКРЫВАЕТ Н-48** (ОТКРЫТО; цена: 3+ часа на закрытии 010 — ревьюер 1h49m FAIL по
единственной причине п.8, ~6 перепрогонов архитектора 400–2130с, взаимные kill; пять
сцепленных дефектов гейта измерены поимённо) **и операционные остатки Н-41 в раннере**
(статус в реестре: ОТКРЫТО; параллельные прогоны и kill по имени). Полный прогон как
приёмка судьёй отменяется; сам раннер получает изоляцию, которой ему не хватало.

## §Предмет — что делаем

### Половина А: норма приёмки v+1 (устав)

1. **`AGENTS.md`** — в раздел «Воркфлоу майлстоуна» вносится норма приёмки судьи v2.
   Текст ПИНАЕТСЯ ДОСЛОВНО, тремя строками ниже; барьер грепает ВЕСЬ блок целиком —
   пин двух подстрок проходил и противоположную по смыслу норму (обход круга 1
   критика), а полный текст — решение АВТОРА контракта, не круга:

   ```
   ПРИЁМКА-СУДЬИ (v2, Н-48): судья (критик/адверсарий/ревьюер) гоняет ТОЛЬКО scoped-регресс
   затронутых барьеров (`verify_antiplacebo --scope <ключ>`) и git-diff неизменности
   frozen-барьеров; полный прогон — только CI, от судьи он не требуется.
   ```

   МЕСТО и ЕДИНСТВЕННОСТЬ (обходы кругов 2–3 критика; закрытие круга 3 — РЕШЕНИЕ
   арбитра по контракту 011, вопрос 1): блок обязан лежать ВНУТРИ раздела
   «Воркфлоу майлстоуна», и разделом является только текст ВНЕ code fence — строки
   `` ``` ``/`~~~` в первой колонке переключают fence-состояние, и ни граница
   раздела, ни содержимое внутри fence не читаются: строка «## Воркфлоу
   майлстоуна» внутри архивного fence заголовком в Markdown не является. Границы
   раздела — от его заголовка до следующего «## » вне fence; «###» внутрь входят.
   Маркеры «ПРИЁМКА-СУДЬИ (v2» и «полный прогон — только CI» обязаны встречаться
   во всём AGENTS.md РОВНО по одному разу — второе вхождение может отменять смысл
   нормы. Оба требования наблюдаемы ветвью norma. Смысл: каждому судье назначены
   ТОЛЬКО scoped-регресс затронутых барьеров и git-diff неизменности
   frozen-барьеров; полный прогон — работа CI (clean checkout, без bash-лимита), от
   судьи не требуется. Правка устава — коммитом со строкой `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`
   (правило 11; слово владельца на грилинге 2026-08-24 уже есть). Граница
   механического: дословный пин ловит искажение текста, МЕСТО с fence-состоянием —
   помещение нормы не в тот раздел и «раздел» внутри code fence, ЕДИНСТВЕННОСТЬ —
   отменяющее второе вхождение маркеров. cognitive-only остаток (правило 1):
   экзотика Markdown — fence с отступом 1–3 пробела, fence внутри blockquote/списка,
   HTML-блоки — этим гейтом не разбирается; ловец — судья, читающий AGENTS.md
   глазами. Противоположная формулировка БЕЗ маркеров внутри раздела и «судьи
   реально исполняют норму» — процессное, держится кругами критика/ревьюера и этим
   гейтом не объявлено (правила 1 и 8).
2. **`contracts/010-…-arhitektor.md` §Приёмка п.8** — у строки
   `bash scripts/verify_antiplacebo.sh → 0` появляется пометка
   `(отменено грилингом 2026-08-24, см. contracts/011: судье — scoped-регресс, полный
   прогон — CI, Н-48)`. Форма — аннотация (не зачёркивание); оба варианта меняют блоб,
   поэтому v+1 обязателен в любом случае. Это правка ЗАМОРОЖЕННОГО текста, и процедура
   v+1 идёт ПОСЛЕДОВАТЕЛЬНО — вердикт судит ЗАКОММИЧЕННЫЙ блоб (правила 9–11;
   «тот же круг» из первой редакции требовал accept по будущему блобу — обход круга 1):
   (i) implementer коммитит раннер + маркер AGENTS.md + аннотацию 010 одной пачкой со
   строкой `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` в теле; (ii) critic СЛЕДУЮЩИМ, ОТДЕЛЬНЫМ кругом судит
   закоммиченный блоб 010 и пишет `verdicts/critic/contracts-010-v2.md` (первая строка
   `accept`); (iii) implementer перезамораживает: `bash scripts/freeze_contract.sh` →
   тег `frozen/contracts/010/2`. Между (i) и (iii) `check:contract-frozen` КРАСНЫЙ
   («изменён без новой заморозки») — по умыслу: это и есть механизм, принуждающий v+1
   (наблюдаем приёмкой 8).

   ЗАКРЕПЛЕНИЕ РАЗДЕЛЬНОСТИ АКТОВ (обход круга 2 критика: «одним коммитом с автором
   critic внести раннер, устав, аннотацию и заранее написанный вердикт»): конечные
   проверки 7–8 видят разрешение, вердикт и тег, но не ПОРЯДОК коммитов. Порядок и
   состав пачки держат три механики: (механика-1) critic объявлен ниже ЗОНА-строкой
   с единственным путём `verdicts/critic/contracts-010-v2.md` — с заморозки 011 его
   коммиты судятся `check_zones`, и выход в AGENTS.md, contracts/010-* или scripts
   раннера — красный (предъявлено фикстурой fixtures/check_zones/, регресс 3а);
   (механика-2) ветвь барьера `porjadok` читает ИСТОРИЮ: последний коммит, тронувший
   contracts/010-*.md (акт (i)), обязан быть ДРУГИМ и БОЛЕЕ РАННИМ коммитом, чем
   добавивший verdicts/critic/contracts-010-v2.md с первой строкой accept (акт (ii));
   (механика-3, РЕШЕНИЕ арбитра по контракту 011, вопрос 3 — обход круга 3 «пачка
   доехала после вердикта»: аннотация 010 первым коммитом, accept-вердикт вторым,
   раннер и норма AGENTS.md третьим, уже ПОСЛЕ вердикта) тот же коммит A обязан
   своим составом (`git show --format= --name-only A`) нести И contracts/010-*.md,
   И AGENTS.md, И scripts/verify_antiplacebo.sh, и `git show A:AGENTS.md` обязан
   уже нести маркер «ПРИЁМКА-СУДЬИ (v2» — пачка везёт норму, а не касание файла.
   Смысл: критик судит УЖЕ закоммиченный полный блоб, а не смотрит, как половина
   предмета доезжает после его вердикта. cognitive-only остаток (правило 1): состав
   пачки судится по именам путей и маркеру, полноценность содержимого раннера в
   коммите A механизмом не измеряется; ловцы — конечные ветви (lock/race/scratch/
   scratchdef/norma/a010 судят итоговое дерево) и судья, читающий дифф A.
3. **Carve-out правила 16 уже в уставе** (коммит 1737953) — предмет его НЕ трогает и
   НЕ теряет: барьер охраняет (`ИСКЛЮЧЕНИЕ (carve-out` + `VERIFY_ANTIPLACEBO_SCRATCH`).

### Половина Б: инженерия раннера (ПИННОВАННЫЙ API)

`scripts/verify_antiplacebo.sh` — правки минимально-необходимые; scoped-фильтр, цикл
прогона, учёт вызовов и слепок НЕ трогаются (фрозены 006/007/008; регресс — приёмка 6).

1. **Scratch вне стерегомого дерева.** Все run-каталоги, логи и артефакты раннера —
   под `$VERIFY_ANTIPLACEBO_SCRATCH`; при пустой переменной — `mktemp -d` под
   `${TMPDIR:-/tmp}` (carve-out правила 16). Стерегомое дерево байт-в-байт неизменно
   на пишущем раннере — ни во время прогона, ни после (Н-48-2/5) — В ЛЮБОМ режиме:
   и с явной переменной, и с default-скратчем (обходы кругов 2–3: «при пустой
   переменной работать в `<корень>/tmp`, под TMPDIR оставить пустой каталог-приманку,
   подмести trap'ом до выхода»; «работать во ВЛОЖЕННОМ подкаталоге существующего
   поддерева — например `scripts/run-work.*` — и убрать до выхода» — наблюдается
   РЕКУРСИВНЫМ зондом: слепок состава путей + тип + размер и байтовый слепок на
   каждом тике, финальная проверка — байтовая; РЕШЕНИЕ арбитра по контракту 011,
   часть 2а). Найденный под `${TMPDIR:-/tmp}` путь обязан нести lock
   `verify_antiplacebo-<hash8>.lock` этого дерева (п.2) — пустая приманка
   отсекается. cognitive-only остаток (правило 1, РЕШЕНИЕ арбитра, часть 2б):
   наполненная приманка (lock и скопированные признаки работы) при настоящей работе
   в третьем месте вне TMPDIR барьером не наблюдаема — механически доказать
   «фактический скратч» неподвижной точки не имеет; ловцы — судьи, читающие
   закоммиченный исходник раннера (mktemp/пути пишутся в нём открытым текстом;
   правило 8), и рекурсивный зонд части 2а, который единственный стерегомый актив —
   дерево — держит байт-в-байт независимо от того, где живёт настоящий scratch.
2. **Lock против параллельных прогонов.** Файл
   `$SCRATCH/verify_antiplacebo-<hash8>.lock`, где `hash8` — первые 8 hex sha256
   КАНОНИЧЕСКОГО пути проверяемого корня (разные деревья друг друга не блокируют:
   взаимная порча снапшотов измерена для одного дерева, Н-48-3). Содержимое:
   `<pid> <pgid> <epoch>`. Захват атомарен и БЕСШОВЕН: lock-файл обязан появляться
   УЖЕ с тремя полями — create-с-содержимым, эталон `ln` заполненного временного
   файла (`set -C` с ОТДЕЛЬНОЙ записью полей открывает окно «создан пустой —
   заполняется»: второй старт считает владельца неназванным, удаляет lock и
   захватывает его — два rc=0; обход круга 2 критика). Живой владелец → раннер
   отказывает: stderr `занят: …` (слово «занят» — пин, правило 7) и код 3; шапка
   раннера дополняется `3 — занят другим прогоном`. Существующий прогон НЕ убивается
   и не портится. Одновременная гонка наблюдаема напрямую — ветвью race: два
   ОДНОВРЕМЕННЫХ старта дают ровно один rc=3 «занят» и один rc=0, и lock ни в одной
   пробе не читается пустым.
3. **Стартовая чистка** мусора убитых прогонов (Н-48-5): перед захватом lock удаляются
   lock-файлы и `run-<pid>` каталоги МЁРТВЫХ владельцев (по `kill -0`), а также файлы
   в scratch, не являющиеся ни lock, ни run-каталогом живого pid (base64-обрывки
   путей). Чужой live lock другого дерева и run-каталоги живых pid неприкосновенны.
4. **Дисциплина убийства.** Раннер убивает ТОЛЬКО собственных потомков по pgid
   (существующий `kill -- -<case_pid>`); `pkill`/`pgrep -f` по имени процесса
   запрещён (Н-48-4: так убивали чужие прогоны).

Зелёный образец API — `fixtures/check_runner_hygiene/_ref_runner.sh` (эталон вокруг
тривиальной работы; предмет исполняет ту же дисциплину вокруг полной пачки).

## §Зоны

ЗОНА architect: contracts/011-prijomka-sudi-i-gigiiena-rannera.md scripts/check_runner_hygiene.sh fixtures/check_runner_hygiene/ fixtures/check_zones/ package.json .github/workflows/ci.yml

ЗОНА implementer: scripts/verify_antiplacebo.sh AGENTS.md contracts/010-topologija-orkestrator-arhitektor.md

ЗОНА critic: verdicts/critic/contracts-010-v2.md
СПАСЕНО architect: 4a73d71d2657cc9be1ef14d79635fccf1d930e49 a2aaf5ee2963a5c434bb4894449bbbac6e4ee37d 320d67f52a4a12add583f6fb76d02267d494ae70 a534e4b6bd45fed582ca042de091d20c09ae1481 — четыре операционных коммита оркестратора (правки NABLIUDENIA.md, roles/orchestrator.md, .omp/agents/orchestrator.md, HANDOFF.md — Н-51/Н-52, тайм-бокс делегирования, правило «сырое наверх», оверлей-лог) ошибочно атрибутированы автору architect из-за некорректного git user.name сессии (исправлено на orchestrator); содержимое НЕ относится к конструкции 011, решение о способе закрытия — владелец, 2026-08-25 (вариант А, дополнено 4-м хешем словом владельца 2026-08-25).

Последовательность исполнителей (замок на время исполнения; совет круга 1 — «кто и
после какого закоммиченного блоба»):

1. **architect** — контракт + барьер + фикстуры + проводка 011 ДО правки 010 (сделано;
   правки кругов 1–2 + исполнение РЕШЕНИЯ арбитра по контракту 011 (`228ed97`) до
   заморозки: fence-состояние norma, РЕКУРСИВНЫЙ зонд scratch/scratchdef, состав
   коммита A в porjadok, три cognitive-only остатка, 28 фикстур).
2. **Заморозка 011** — после accept критика (`freeze_contract.sh`).
3. **implementer** — раннер + маркер AGENTS.md + аннотация 010, ОДИН коммит со строкой
   `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` (правило 11), ПОСЛЕ заморозки 011. На этом шаге
   `check:contract-frozen` красен (аннотация без перезаморозки); шаг
   `check:runner-hygiene` выравнивается вместе с предметом.
4. **critic** — ОТДЕЛЬНЫМ кругом судит закоммиченный блоб 010 →
   `verdicts/critic/contracts-010-v2.md` (первая строка `accept`).
5. **implementer** — перезаморозка `bash scripts/freeze_contract.sh` →
   `frozen/contracts/010/2`; после неё `check:contract-frozen` зелёный.
6. **Приёмка конечного состояния** — пробы 1–2 зелёные, регресс 3–3а и 4–8 зелёный
   (включая `check:contract-frozen` и `check:charter`), регламентный пуш 4б.

## §Приёмочный критерий — красные тесты + форма нормы

Барьер `scripts/check_runner_hygiene.sh`, 10 ветвей: lock, race, scratch, scratchdef,
chistka, pgid, norma, carveout, a010, porjadok. Против ТЕКУЩЕГО дерева красны
lock/race/scratch/scratchdef/chistka/norma/a010/porjadok (предмет не реализован —
это и есть красные тесты); pgid/carveout зелены как охрана (раннер сегодня по имени
не убивает, carve-out внесён 1737953) — их красное предъявлено обманками в фикстурах.
Полный список:
счёт: 28 фикстур в fixtures/check_runner_hygiene/
(маркер на своей строке — потребляется `check_contract_ready`, сверяет фактический
счёт `case_*.sh`). Объявление
critic ЗОНА-строкой (совет круга 2) предъявлено красным фикстурой
`fixtures/check_zones/case_zones_critic_v_others.sh` (регресс 3а); сам скрипт
check_zones не тронут.

Что пинуют усиленные ветви (обходы кругов 1–2 критика закрыты наблюдением на предмете):
- **lock** — ВЕСЬ lock-API: lock-файл ровно `<pid> <pgid> <epoch>` (три числовых поля;
  pid — владелец-раннер, pgid — его группа), отказ второго прогона при живом владельце
  РОВНО кодом 3 со словом «занят», и отказ БЕЗ перезаписи чужого lock (байт-в-байт
  до/после); неатомарная запись — затирание до проверки владельца — предъявлена
  красным фикстурой (обход круга 1).
- **race** — РЕАЛЬНАЯ одновременная гонка: два старта над одним деревом и одним
  scratch дают ровно один rc=3 «занят» и один rc=0; зонд 0.02с ловит lock, читающийся
  БЕЗ трёх полей, — окно «создан пустой — заполняется» (обход круга 2: обманка
  создаёт lock пустым `set -C`, пауза, затем обычный printf; конкурент считает
  владельца неназванным, удаляет lock и захватывает — оба rc=0; последовательная
  проба круга 1 это окно не наблюдала).
- **scratchdef** — раннер с ПУСТОЙ `VERIFY_ANTIPLACEBO_SCRATCH` под подставным
  `TMPDIR`: скратч обязан появиться под `${TMPDIR:-/tmp}` и НЕ в стерегомом дереве
  (in-tree `./tmp` при пустой переменной — обход круга 1); найденный путь обязан
  нести lock `verify_antiplacebo-<hash8>.lock` этого дерева (пустой
  каталог-приманка — обход круга 2), и стерегомое дерево чисто И ВО ВРЕМЯ прогона —
  РЕКУРСИВНЫЙ зонд 0.1с: слепок состава путей + тип + размер и байтовый слепок на
  каждом тике, финально — байтовый (обход круга 2: работа в дереве + подметание
  trap'ом или rm до выхода; обход круга 3: работа во ВЛОЖЕННОМ подкаталоге
  существующего поддерева — `scripts/run-work.*` — с уборкой до выхода; РЕШЕНИЕ
  арбитра, часть 2а). Той же правкой рекурсивен и зонд ветви scratch — у неё тот
  же класс зонда.
- **norma** — ПОЛНЫЙ текст нормы (три строки из §Предмет А.1) дословно, ВНУТРИ
  раздела «Воркфлоу майлстоона», причём раздел — только текст ВНЕ code fence:
  строки `` ``` ``/`~~~` в первой колонке переключают fence-состояние, строка «## …»
  внутри fence заголовком не является (обход круга 2: блок в архиве отклонённых +
  противоположное правило в разделе; обход круга 3: фальшивый «раздел» и дословный
  блок целиком внутри архивного fence — РЕШЕНИЕ арбитра, вопрос 1), и маркеры
  «ПРИЁМКА-СУДЬИ (v2» / «полный прогон — только CI» ЕДИНСТВЕННЫ во всём файле
  (обход круга 2: отменяющее второе вхождение); норма, противоположная по смыслу и
  несущая оба маркера, предъявлена красным фикстурой (обход круга 1).
- **porjadok** — раздельность актов 010-v2 по ИСТОРИИ коммитов (§Предмет А.2,
  механика-2): последний коммит, тронувший contracts/010-*.md, — ДРУГОЙ и БОЛЕЕ
  РАННИЙ, чем добавивший verdicts/critic/contracts-010-v2.md с первой строкой
  accept («всё одним коммитом» и «вердикт раньше аннотации» — обходы круга 2;
  «вердикта нет вовсе» — обход круга 1); И состав того же коммита A (механика-3,
  §Предмет А.2): И contracts/010-*.md, И AGENTS.md, И
  scripts/verify_antiplacebo.sh, и маркер «ПРИЁМКА-СУДЬИ (v2» в `A:AGENTS.md`
  («пачка доехала после вердикта» — обход круга 3, РЕШЕНИЕ арбитра, вопрос 3).
  Ветвь требует, чтобы проверяемый корень был git-репозиторием; иначе
  NOT_IMPLEMENTED.

Приёмочные пробы (каждая — сейчас красная, после предмета — зелёная):

- `bash scripts/check_runner_hygiene.sh`
- `bash scripts/check_runner_hygiene.sh . norma`

Регресс и проводка (зелёные на пачке; на КОНЕЧНОМ состоянии предмета обязаны быть
зелёными — у пункта 8 промежуточный красный по умыслу):

3. `bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` → 0 (28/28 фикстур
   предъявляют барьер красным на обманках; замер — ниже).
3а. `bash scripts/verify_antiplacebo.sh --scope check_zones` → 0 (12/12; фикстура
   объявления critic добавлена этой правкой, барьер не тронут).
4. `npm run check:ci-parity` → 0 (`check:runner-hygiene` в package.json и ci.yml).
5. `bash scripts/check_zones.sh` → 0.
6. `bash scripts/check_scoped_run.sh` → 0 (поведенческий контракт раннера на игрушках —
   правка раннера его не ломает).
7. `npm run check:charter` → 0 — уставная правка AGENTS.md обязана идти коммитом со
   строкой `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`; коммит правки без строки красит этот шаг (правило 11;
   обход круга 1: «закоммитить уставные правки без РАЗРЕШИЛ-ВЛАДЕЛЕЦ»).
8. `bash scripts/check_contract_frozen.sh` → 0 на КОНЕЧНОМ состоянии: зелёный только
   когда аннотация 010 закоммичена И `frozen/contracts/010/2` поставлен. На промежутке
   «аннотация закоммичена, перезаморозки нет» шаг КРАСНЫЙ по умыслу — «изменён без
   новой заморозки» и есть механизм, принуждающий вердикт 010-v2 и перезаморозку
   (обход круга 1: «не создавать вердикт 010-v2, не ставить frozen/contracts/010/2»);
   регламентный пуш 4б — на конечном состоянии.

Замер сборки 4а-v4 (архитектор, исполнение РЕШЕНИЯ арбитра, до судей): проба 1 →
rc=1, по ветвям отдельно: lock «lock-файл не появился за 8с … не захватывает lock»
(Н-48-3), race «вышли кодами 1 и 1», scratch «появились новые пути (./tmp …)»
(Н-48-2), scratchdef «не появился скратч», chistka «stale lock … не убран», norma
«нет маркера приёмки судьи v2», a010 «не помечен „отменено грилингом“», porjadok
«вердикт 010-v2 не закоммичен»; pgid/carveout — ok. Зелёный контроль: все 10 ветвей
на эталонном корне — rc=0, 13с. Проба 3 → rc=0, «барьеров: 1 · фикстур: 28 ·
предъявлено красным повторным прогоном: 28», 3м48с; проба 3а → rc=0, «фикстур: 12
… 12», 48с; check_contract_ready на судейском корне (клон рабочего дерева,
contract.md → 011) → OK rc=0. Пробы 4–8 → rc=0 на пачке (замер правки: ci-parity
22·34·0, zones 0, charter 30/30, contract-frozen 0, ceilings 0). Три обхода
арбитра воспроизведены заново (замеры 1/3/6 решения): на барьере из HEAD
(до правки) — rc=0 каждый, на этой правке — rc=1 с именованными причинами
«внутри code fence…», «…во вложенном подкаталоге существующего поддерева…»,
«пачка доехала после вердикта»; маркерный вариант (A несёт все три файла,
A:AGENTS.md без нормы) — тоже rc=1.

**CI-примечание:** шаг `check:runner-hygiene` в ci.yml красен до предмета ПО УМЫСЛУ
(красный тест в проводке). Регламентный пуш (гейт 4б) выполняется на состоянии ПОСЛЕ
предмета; пуш одной пачки без предмета CI не проходит, и это не дефект.

## §Не-цели (урок 007: объём срывает работу)

- (б) переворот `models_actual`, (в) оправдание болью в `check_contract_ready`,
  (г) триггер делегирования, (д) fallback `@slow` — контракт 012 (граница V3).
- Изоляция фикстур Н-45/Н-36 и Н-49 (literal `${HOME}`) — 013.
- Добивание осиротевших ГРУПП фикстур убитого прогона: lock пишет pgid для диагностики,
  но чистка процессы не убивает — убийство чужих групп по записи есть риск Н-48-4
  наоборот; осиротевшие группы — 013/поток A.
- Worktree-на-прогон — отложено решением D1 (требует `РАЗРЕШИЛ-ВЛАДЕЛЕЦ` на слепок 001).
- Скрипты `check_zones` / `check_contract_ready` / `check_ci_gate` — заморожены 008,
  не трогаются (красная фикстура объявления critic в `fixtures/check_zones/` — эта
  правка, тест-актив; сам скрипт побайтово цел).
- Барьер `check_metering` и его `./tmp` — правило 16 действует как писано; carve-out ТОЛЬКО у
  раннера antiplacebo.
- Ночные полные прогоны CI — не добавляются: полный уже fail-safe на любом пуше,
  трогающем барьеры.

## Незаполненные требования:
- Длительности в барьере (сон игрушки 2с, окно опроса lock 8с) — деталь барьера, не предмета; менять их — правка барьера, не контракта.
## §История правок

- Круг 1 критика (`6bffd2d`, FAIL: 4 обхода + 1 совет) → эта правка: norma пинует
   полный трёхстрочный текст + фикстура противоположной нормы; новая ветвь scratchdef
   + фикстура (default-скратч под `${TMPDIR:-/tmp}`); lock пинует формат/владельца/
   ровно код 3/отказ-без-перезаписи + 3 фикстуры; приёмка дополнена пунктами 7–8
   (check:charter, check:contract-frozen); процедура 010-v2 переформулирована
   последовательностью исполнителей (§Предмет А.2, §Зоны 1–6); заголовок раздела
   требований приведён к грамматике check:ceilings (у v1 — без двоеточия, красный на
   HEAD). Счёт фикстур 12 → 17.

- Круг 2 критика (`00b6bc9`, FAIL: 4 обхода + 1 совет) → эта правка: norma пинует
   МЕСТО блока (раздел «Воркфлоу майлстоуна») и ЕДИНСТВЕННОСТЬ маркеров + фикстуры
   «вне раздела» и «удвоение»; scratchdef — зонд дерева ВО ВРЕМЯ default-прогона,
   запрет пустой приманки (связь с lock) + 3 фикстуры; захват lock переведён на
   create-с-содержимым (ln темпа; эталон обновлён) + ветвь race с реальной гонкой
   двух стартов + фикстура; раздельность актов 010-v2 закреплена ЗОНА-строкой critic
   (совет круга 2, фикстура в fixtures/check_zones/) + ветвь porjadok по истории
   коммитов + 2 фикстуры. Ветви 8 → 10; счёт фикстур 17 → 25 (+1 в check_zones:
   11 → 12).

- Арбитраж (`228ed97`, РЕШЕНИЕ по трём вопросам после круга 3; исполнение до
   заморозки) → эта правка: norma — fence-состояние в извлечении раздела + фикстура
   «раздел внутри code fence архива»; scratchdef/scratch — РЕКУРСИВНЫЙ зонд дерева
   на каждом тике (состав путей + тип + размер И байтовый слепок; финальная
   проверка — байтовая) + фикстура «вложенный подкаталог с уборкой до выхода»;
   формулировка §Предмет Б.1 приведена к измеряемой («обязан нести lock; пустая
   приманка отсекается»); porjadok — состав коммита A ⊇ {contracts/010-*.md,
   AGENTS.md, scripts/verify_antiplacebo.sh} + маркер в `A:AGENTS.md` + фикстура
   «пачка доехала после вердикта»; три cognitive-only остатка записаны (экзотика
   Markdown у norma; наполненная приманка + третье место у scratchdef; содержимое
   пачки A у porjadok). Счёт фикстур 25 → 28.
