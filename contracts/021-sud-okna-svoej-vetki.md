# Контракт 021 — ядро Н-66: суд открытого окна по merge-дельтам своей ветки (С2) + признание draft-коммитов (Н-77(б))

Источники: Н-66 (NABLIUDENIA.md:1652, боль числом); Н-77 пункт (б) — draft-признание
(NABLIUDENIA.md:2120-2123); ROADMAP §10 («механизм Н-66 — прямой prerequisite»);
frontier-доклад architect 2026-09-04 (tmp/frontier-parallel-N66.md — сырьё грилинга,
числа сверены по коду HEAD b749632). Решения владельца 2026-09-04: нарезка **В1**,
семантика **С2**, Р1–Р6 по рекомендациям архитектора; ДВА обязательных усиления:
(1) мера параллельности §10 — МЕХАНИЗМ приёмки, определяется здесь, ДО обкатки
022∥023; (2) инвариант регресса — оконная модель строгое обобщение линейного
диапазона, суд 013–020 неизменен, красная на расхождение. Правка 1 (круг 2) — по
четырём блокерам вердикта `verdicts/critic/contracts-021-v1.md` (760e895): красные
фикстуры меры и регресса существуют и предъявлены (ниже, «Красное сейчас»), проза
пар «слабая реализация ↔ ветвь» из текста УДАЛЕНА — привязка стабов к входам живёт
кодом фикстур (шапки red/stab/probe в fixtures/check_zones/), решение о парной
заморозке закреплено в приёмке обкатки. Правка 2 — по CI-факту cc1ac79 (шаг
«Анти-плацебо» rc 1 на обоих новых case): раннер судит каждый case по стандарт-А и
не имеет режима «видеть красный кейс нереализованного предмета, не уронив CI» —
пред-заморозочные красные ВЫНЕСЕНЫ из case_*-глоба раннера в red_*-файлы с прямым
запуском (та же боль, тот же замер — ниже); блокеры к1 круга 1 НЕ ослаблены: красные
предъявлены до заморозки адресными прогонами, которые критик исполняет напрямую.
Инвойкеры раннера выбора не дают (А-82): CI зовёт `--changed`, локальная приёмка —
`--scope`; первый всегда выбирает СЕМЬЮ (ключ — каталог fixtures/*/*), второй умеет
сузить до кейса, но кейсы перечисляет сам case_*-глоб раннера — режима «только
изменённые кейсы» не существует, red_* вне глоба не виден ни одному инвойкеру.

**ЗАКРЫВАЕТ Н-66, цена: 2 блокера круга + 1 виток разрыва (Y1+H1) + 1 арбитраж + 2 круга критика против окна, закрывавшегося бы нулём** (Н-66 «ЧИСЛО», NABLIUDENIA.md:1665-1670). Поглощает Н-77(б): draft-признание — та же семантика «что судится» и те же файлы; расщепление на два контракта по одним файлам пере-фризовало бы семантику друг друга (отклонённый вариант В2, класс «смена обесценивает сданную работу»).

## Предмет

Четыре ветви; А и Б — одна семантика «что судится» в одном судьё (check_zones +
lib_zones), В — новая мера, Г — инвариант-ограничение на А.

**Ветвь А — семантика диапазона ОТКРЫТОГО окна (С2 «ветка-дифф»).**
Сегодня (код): диапазон контракта N = frozen/contracts/N/1..done/contracts/N/1, а при
отсутствии либо недостижимости done-тега — хвост frozen/contracts/N/1..HEAD
(lib_zones.sh:157-167); обход — линейный `rev-list --no-merges` диапазона
(check_zones.sh:265-267); merge-коммиты как единицы суда пропускаются (check_zones.sh:44).
Для ПОСЛЕДОВАТЕЛЬНЫХ контрактов хвост пуст либо свой; для ПАРАЛЛЕЛЬНЫХ открытых окон
хвост одного контракта накрывает чужие коммиты — измеренная боль Н-66 (013/014).

После 021 судимое множество ОТКРЫТОГО окна N:

1. **Свои ветки — по merge-дельтам.** Для каждого merge-коммита диапазона, тело которого
   несёт маркер «чей merge» — строку `land: wip/<N>/<автор>` (примитив уже живёт:
   land_agent.sh:175, `merge --no-ff -m "land: $branch_arg"`; stdout дублирует, :254) —
   окну N принадлежат принесённые коммиты `rev-list <merge>^1..<merge>` без merge-единиц
   (дельта к первому родителю — тот же родительский примитив, что charter И-7).
2. **Прямые коммиты.** Не-merge коммиты первого-родителя линии main в диапазоне от
   авторов, объявленных в N, — против зон автора в N; код-путь check_zones.sh:263-293
   (авторы → зоны → дельта путей коммита) не меняется.

Вердикт-функция НЕ меняется: единица суда — по-прежнему не-merge коммит со своим автором
(merge-коммит как единица пропускается, его содержимое судит ревьюер — как сегодня);
меняется ТОЛЬКО множество судимых коммитов открытого окна. Линейный хвост «все не-merge
до HEAD» уходит. Окно чужого контракта не судится ПО ПОСТРОЕНИЮ: чужие рабочие коммиты
приходят в main только merge'ями со своим маркером (§9 merge-only; обкатка 019 — ноль
прямых коммитов рабочих в main, 12 land-сведений). Cross-track касание файла чужой зоны
объявляется ЗОНА-строкой СВОЕГО контракта точным именем (глобы запрещены
path_prefix_valid, прецедент 010) — без СПАСЕНО-рекурсии: суд чужого окна на этот коммит
больше не смотрит.

**Ветвь Б — признание draft-коммитов (Н-77(б), поглощение).**
Не-merge коммит, несущий дельту по пути contracts/<NNN>-<slug>.md и отмеченный тегом
выдачи `id/CONTRACT/<NNN>` (тег на САМОМ коммите; ставит оркестратор при сверке реестра —
Н-76/Н-77(а)): дельта по этому пути из суда зон выводится — легальный draft-пуск, сигнал
легальности есть тег выдачи. Та же дельта без тега судится как обычное касание вне зон.
Прочие пути того же коммита судятся по общим правилам: тег — не индульгенция на весь
коммит. Одна семантика «что судится» для staged-суда и history-аудита: страж судит
draft-пуск по ветви 2 контракта 019 (architect + следующий свободный номер,
next_id_peek), history-аудит — по тегу выдачи; номер оба читают из next_id; check_staged
и next_id НЕ правятся — их семантика верна.

**Ветвь В — мера параллельности §10 (усиление 1 владельца).**
Новый скрипт `scripts/measure_parallel_windows.sh` (имя предложено архитектором; мера, а
не барьер: 0 — параллельность окон доказана, 1 — «не параллельно» либо нечем доказать, с
именованной причиной). Вход: `measure_parallel_windows.sh <корень> <A> <B>` — корень
репо и два номера контрактов в форме тегов (трёхзначные: 022, 023). Источник времени —
коммиттер КОММИТА, на который указывает тег (аннотированный тег разыменовывается до
коммита). Окна ПЕРЕКРЫЛИСЬ ⟺ frozen/contracts/A/1 раньше done/contracts/B/1 И
frozen/contracts/B/1 раньше done/contracts/A/1. Последовательный прогон (окно B открылось
не раньше done A) → rc 1 со строкой «не параллельно» и именами тегов с временами.
Недостающий тег → rc 1 со строкой «недостающий тег <имя-тега>» — пустая выборка красная,
не зелёная. Анти-плацебо меры — `red_mera_parallelnosti_okon` (правка 2: имя ВНЕ
case_*-глоба раннера, предъявление — прямой запуск, красное сейчас — ниже): ворота меры —
прямые вызовы предмета с договорёнными исходами; до реализации раннер эти ворота не видит
(и не может видеть, не уронив CI), после реализации файл конверсируется в case с
протоколом раннера.

**Ветвь Г — инвариант регресса (усиление 2 владельца).**
Оконная модель — СТРОГОЕ обобщение линейной: на ПОСЛЕДОВАТЕЛЬНОЙ истории (done N
закрывает окно N до открытия N+1) множества судимых коммитов и вердикты оконной и
линейной моделей СОВПАДАЮТ — ни потерянных нарушений, ни добавленных. Совпадение держится
по построению: линейный rev-list диапазона — это в точности прямые первого-родителя плюс
коммиты, принесённые merge'ями диапазона; в последовательной истории все merge'и
диапазона — merge'и самого N. Расхождение моделей допускается ровно на классе боли Н-66:
чужой land-merge в открытом окне (маркер чужого номера) — линейная модель судит
принесённый коммит объявленного автора, оконная обязана быть зелёной. Суд 013–020
неизменен: done-ветка диапазона (Н-14, lib_zones.sh:158-164) не трогается,
вердикт-функция не меняется, меняется только наполнение открытого окна. Красная
фикстура регресса — `red_regress_posledovatel_naja_istorija` (правка 2: та же форма
прямого запуска, красное сейчас — ниже): линейная сторона — ожидания, записанные в коде
файла (истории построены по документированной линейной семантике), оконная — сам барьер;
тождественное сравнение одного вычислителя исключено по построению.

### Инварианты (каждый с rc-командой)

- **И-1 чужое окно не судится** (ветвь А). Вход: два открытых контракта A,B; одно имя
  автора объявлено в обоих (измеренный случай Н-66 — cross-track касание тем же именем);
  land-merge ветки B (маркер land: wip/B/…) приносит коммит этого автора на путь зоны B,
  вне зон A — коммит внутри старого хвоста A. Честная реализация: зелёный. rc: `bash
  scripts/verify_antiplacebo.sh . --scope check_zones/case_chuzhoe_okno_ne_suditsja`.
- **И-2 своя merge-дельта судится** (ветвь А). Вход: land-merge ветки A приносит коммит
  автора A на путь вне зон A. Честная: красный «вне зоны». rc: `--scope
  check_zones/case_svoja_merge_delta_suditsja`.
- **И-3 прямой коммит объявленного автора — красный** (ветвь А, охрана §9). Вход: прямой
  (без merge) коммит объявленного автора в main на путь вне зон его открытого контракта.
  Красный до и после 021. rc: `--scope
  check_zones/case_prjamoj_kommit_objavlennogo_avtora` + живой `bash
  scripts/check_zones.sh .`.
- **И-4 draft-признание** (ветвь Б). Вход: коммит с дельтой contracts/<NNN>-<slug>.md; с
  тегом id/CONTRACT/<NNN> → зелёный; тот же коммит без тега → красный. Живая проба: тег
  id/CONTRACT/021 на draft-коммите 021 (ставит оркестратор после land) → суд зелёен. rc:
  `--scope check_zones/case_draft_priznanie`.
- **И-5 done-диапазоны неизменны** (Н-14). Вход: закрытый контракт; коммиты после его
  done-тега не судятся им. rc: `--scope check_zones/case_done_diapazony_neizmenny` +
  живой `bash scripts/check_zones.sh .` — сводка равна базлайну.
- **И-6 регресс-обобщение** (ветвь Г). Точное равенство вердиктов оконной и линейной
  моделей на последовательной истории; расхождение допускается только на классе Н-66
  (чужой land-merge). Линейная сторона — ожидания в коде red-файла, не второй вызов
  нового вычислителя. ДО реализации rc: `bash
  fixtures/check_zones/red_regress_posledovatel_naja_istorija.sh` → 1 (именованный
  отказ «чужой land-merge судится»; свежий WORK готовит сам red-файл, вне дерева — А-78);
  ПОСЛЕ реализации (конверсия в case) rc: `bash
  scripts/verify_antiplacebo.sh . --scope
  check_zones/case_regress_posledovatel_naja_istorija` + `bash
  fixtures/check_zones/probe_slabye_realizacii.sh` — оба rc 0.
- **И-7 мера параллельности** (ветвь В). Перекрывшаяся пара → rc 0; последовательная →
  rc 1 «не параллельно»; недостающий тег → rc 1 с именем тега. ДО реализации rc: `bash
  fixtures/check_zones/red_mera_parallelnosti_okon.sh` → 1 (именованный отказ «меры
  нет»; свежий WORK — кодом red-файла, вне дерева — А-78);
  ПОСЛЕ реализации (конверсия в case) rc: `bash
  scripts/verify_antiplacebo.sh . --scope check_zones/case_mera_parallelnosti_okon` +
  `bash fixtures/check_zones/probe_slabye_realizacii.sh` + живой `bash
  scripts/measure_parallel_windows.sh <корень> <A> <B>` — все rc 0.
- **И-8 охрана семьи.** Прежние case семейства check_zones зелёные, полный scoped-прогон
  rc 0. rc: `bash scripts/verify_antiplacebo.sh . --scope check_zones` → 0, фикстур 13
  (правка 2: пред-заморозочные красные в счёт семьи не входят — они вне case_*-глоба).

Привязка слабых реализаций к входам — кодом фикстур: шапки red-файлов, стабов
`stab_mera_*.sh` и пробы `probe_slabye_realizacii.sh` в fixtures/check_zones/. Контракт
пары «слабая реализация ↔ ветвь» не перечисляет (Н-39: проза не декларирует
недоказуемого — AGENTS.md:124-127).

## Демаркация конформности (R1, с первой редакции — roles/architect.md:92-97)

Конформный вход построен по документированной грамматике предмета: ЗОНА-строки литерала
«ЗОНА <автор>: <путь>…» и СПАСЕНО-грамматика 003 v3, читанные из замороженного блоба
(lib_zones — единственный читатель); теги frozen/contracts/<NNN>/<v>,
done/contracts/<NNN>/1, id/CONTRACT/<NNN>; merge --no-ff с телом, начинающимся строкой
`land: wip/<NNN>/<автор>` (land_agent.sh:175); пути относительные, без шаблонов, «..» и
пробелов (path_prefix_valid). Валидный контрпример = инвариантность к значениям
(подстановка номеров, имён, путей не меняет исход) ∧ расхождение честной реализации и
слабой НА КОНФОРМНОМ входе (арбитражи ebc57db — значения, fa0d354 — грамматики). Стаб
подставляется тому входу, где его дефект наблюдаем; назначение входа — код фикстуры
(Н-39, NABLIUDENIA.md:688).

## Зоны

ЗОНА architect: contracts/021-sud-okna-svoej-vetki.md fixtures/check_zones/ NABLIUDENIA_ARCHITECT.md
ЗОНА implementer: scripts/check_zones.sh scripts/lib_zones.sh scripts/measure_parallel_windows.sh
РАБОТА НЕ РАЗДАЁТСЯ: scripts/verify_antiplacebo.sh (раннер не правится) .githooks/ (pre-commit не меняется) roles/ (норма — владелец-канал); судьи verdicts/critic/ verdicts/adversary/ verdicts/review/ (судейский канал)

Зона implementer вступает в силу ПОСЛЕ заморозки (прецедент 013–019). Правки минимальны
по построению: код-путь прямых коммитов (check_zones.sh:263-293) и done-ветка диапазона
(lib_zones.sh:158-164) не меняются; новый — читатель merge-дельт открытого окна и мера
В; формат вывода lib_zones обратно совместим (новые данные — новыми файлами, состав —
деталь исполнителя). Red-файлы меры и регресса лежат в fixtures/check_zones/ (активная
зона architect на момент пачки); их код адресует предмет напрямую — каналу размещения
семантика не приписывается.

## Приёмочный критерий

Счёт фикстур НЕ морожен: число — из факта дерева, команда `ls
fixtures/check_zones/case_*.sh | wc -l` (13 после правки 2; два пред-заморозочных
красных — red_*.sh вне глоба раннера, в счёт семьи не входят). Независимая сверка счёта
с критерием контракта 013 — `bash fixtures/check_zones/_schet_fixtur.sh` → rc 0 (правка
2 восстановила счёт 013, правка 1 его ломала). Привязка входов — по шапкам red_*.sh /
stab_*.sh / probe_*.sh, не по прозе (Н-39). Оракул — в памяти проверяющего (правило 8):
ожидания сняты до вызова субъекта; сверка структурная. Прогон фикстур — только свежий
WORK, одноразовый клон для живых проб (А-78, Н-74).

### Красное сейчас (замеры правки 2, ветка wip/021/architect, коммит d5eae12)

- `bash fixtures/check_zones/red_mera_parallelnosti_okon.sh` → rc 1, «ОТКАЗ: меры нет —
  scripts/measure_parallel_windows.sh (реализация за implementer после заморозки 021)»:
  красное предъявлено ДО реализации адресным прогоном (AGENTS.md:121-124);
- `bash fixtures/check_zones/red_regress_posledovatel_naja_istorija.sh` → rc 1, «ОТКАЗ:
  чужой land-merge (маркер wip/002) судится окном 001 — линейная модель на входе, где
  оконная обязана быть зелёной (Н-66)» поверх живого «FAIL коммит вне зоны: agent-x …
  plans/chuzoj.md» (ша toy-коммита недетерминирован — два прогона дали разные, в тексте
  не пинуется) — сегодняшний линейный код красен на входе, где оконная
  модель обязана быть зелёной: это и есть красное регресс-инварианта;
- `bash fixtures/check_zones/probe_slabye_realizacii.sh` → rc 0: слабые реализации
  «всегда 0», «пусто-зелёная», «теряет merge-принесённые» ловятся поимённо (прямые
  прогоны red-файлов в подставных корнях против стабов), честная форма меры проходит
  ворота целиком (ожидания фаз стабильны до и после реализации);
- `bash scripts/verify_antiplacebo.sh . --scope check_zones` → rc 0, фикстур: 13,
  предъявлено красным повторным прогоном: 13 — раннер семью зелёный, CI на каждом пуше
  до реализации зелёный (замер правки 2; до правки — rc 1, фикстур 15, расхождений 2 —
  воспроизведение CI-факта cc1ac79 локально);
- `bash fixtures/check_zones/_schet_fixtur.sh` → rc 0 (case-фикстур 13 = критерий 013);
- scoped И-1…И-5 → rc 1 «case отсутствует» (замер формы — вердикт 760e895; контроль
  правки 2: `--scope check_zones/case_chuzhoe_okno_ne_suditsja` → rc 1); файлы —
  следующие пачки architect в своей зоне, у каждой инварианта своя rc-команда выше;
- живой базлайн: `bash scripts/check_zones.sh .` → rc 0, сводка «замороженных
  контрактов: 18 · объявленных авторов: 5 · коммитов в диапазонах: 456 · проверено по
  зонам: 337» — неизменна от правки 1.

### После реализации (rc-переход красное→зелёное)

- red-файлы → rc 0 каждый (`red_mera` — ворота против живой меры; `red_regress` — все
  три входа по договору): прямой прогон перестаёт быть красным сам по себе;
- конверсия red_* → case_* (пачка architect в своей зоне, вместе с И-1…И-5): протокол
  раннера — ≥1 зелёный контроль, ≥1 красный повторным прогоном с именованной причиной,
  различающие ассерты ДО первой зелёно-красной пары (А-82); проба возвращается к
  scoped-прогонам в тех же подставных корнях; счёт семьи растёт до 15, критерий 013
  пополняется по своему канону;
- все scoped-команды И-1…И-8 → rc 0 каждая;
- probe_slabye_realizacii.sh → rc 0 (стабильно и до, и после);
- охраны остаются красными как нарушения: прямой коммит вне зон (И-3), draft-дельта без
  тега (И-4), коммит после done (И-5) — 021 эти входы НЕ позеленяет;
- И-5 живой: check_zones rc 0, сводка равна базлайну — суд 013–020 неизменен;
- И-7: перекрывшаяся пара → rc 0; последовательная → rc 1 «не параллельно»; недостающий
  тег → rc 1 с именем тега;
- полный CI зелёный на done-коммите.

### Приёмка обкатки §10 (определена ЗДЕСЬ, до старта 022∥023 — усиление 1)

«Два done одновременно» — механизм приёмки, не наблюдение. **Парная заморозка НЕ
требуется: независимые заморозки — доказываемое следствие устранения рекурсии (С2:
чужой merge не входит в судимое множество окна, взаимная СПАСЕНО-рекурсия Н-66
невозможна по построению).** Приёмка требует ВСЕ ЧЕТЫРЕ пункта:

1. два РАЗДЕЛЬНЫХ акта заморозки: `bash scripts/freeze_contract.sh 022 "<причина>"` и
   `bash scripts/freeze_contract.sh 023 "<причина>"` — отдельными вызовами, каждый со
   своей причиной; парный акт одним вызовом либо один коммит с взаимными исключениями
   приёмкой НЕ принимается (независимость заморозок закреплена механически);
2. перекрытие окон по мере: `bash scripts/measure_parallel_windows.sh . 022 023` → rc 0
   на реальной истории пары;
3. анти-плацебо меры: scoped case_mera (команда И-7, после конверсии) → rc 0 —
   последовательная пара красна, «всегда 0» не проходит;
4. независимость окон на живом дереве: `bash scripts/check_zones.sh .` → rc 0 при ОБЕИХ
   открытых окнах пары — взаимная рекурсия отсутствует наблюдаемо.

### Остаточные риски, названы прямо

- **Bootstrap-тайминг 021:** draft 021 посажен из ветки wip/020/architect (peek-дверь:
  ветка своего номера захлопнула бы её, номер — источник 2 next_id). Если land этой ветки
  придёт ПОСЛЕ frozen/contracts/020/1, принесённые коммиты судятся окном 020: draft-файл
  021 закрыт признанием по тегу, а правка contracts/019 v3 — вне зон 020 → красный.
  Держится порядком (land до заморозки 020); при инциденте — ЗОНА/СПАСЕНО-строка 020 по
  слову владельца.
- **Правка 2, до конверсии:** прямые red-прогоны не проходят протокол раннера (нет
  зелёного контроля с повторным прогоном) — их держит чтение кода двух ролей и проба
  слабых реализаций; окно ровно до конверсии, закрытие — той же пачкой, что И-1…И-5.
- Содержимое merge-разрешений конфликтов (пути только в merge-коммите) вне суда зон — как
  сегодня; судит ревьюер.
- user.name не удостоверение — прежний cognitive-only барьера check_zones.

## Не-цели

- СПАСЕНО-грамматика 003 v3 (замороженная ветвь check_zones) — не трогается.
- Charter И-7, семантика раннера verify_antiplacebo, делегирование устава — не трогаются.
- Замороженное трогается ровно одно: семантика диапазона ОТКРЫТОГО окна check_zones — код
  барьера, через красные этого контракта; done-диапазоны Н-14 неизменны.
- Резервация номера при draft-коммите и check_ids (Н-77(а), рецидив Н-76) — предмет 023;
  land_agent и норма слияния с РАЗРЕШИЛ (Н-78) — предмет 022; шардирование CI — draft 020.
- Многодорожечный трекинг оркестратора (Р5) — норма-only: боль не измерена (Н-47), первый
  прогон пары — измеритель.
- Peek-сериализация механизмом (Р3) — норма спавна --nnn одним актором + ловец check_ids;
  механизм — предмет 023.

## Незаполненные требования:
нет
