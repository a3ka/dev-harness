# Контракт 012 — изоляция прогонов (шаг 2 решения владельца 2026-08-26)

## Боль (свежий замер шага 1, НЕ устаревший Н-54)

Contention Класс-1 воспроизведён на дереве 2026-08-26 (эксперимент оркестратора,
дважды независимо): два параллельных `verify_antiplacebo` над живым деревом +
реальная мутация живого дерева → ОБА прогона RC=1 «FAIL дерево изменилось» (279с
и 29с). Один класс предмета: «прогоны и мутации вне живого дерева». Решение
владельца: **А+В одной пачкой, Б (топология) отложена**.

## Предмет

### А — норма изоляции (некодовая, шаги А/В)

1. `.omp/config.yml` — вложенный ключ `task.isolation.mode: btrfs` (сейчас ключа
   нет → режим `none`; корень ФС — btrfs, замерено). Изоляция включается
   ВСТРОЕННОЙ harness-фичей, НЕ кодом раннера и НЕ нашим скриптом.
2. `roles/orchestrator.md` — параллельные пачки architect/implementer спавнятся
   с `isolated: true` (правило вызова, не код); длинные прогоны
   `verify_antiplacebo` — только в disposable-клоне:
   `git clone <дерево> ${TMPDIR}/... && verify_antiplacebo.sh <копия>` — API
   корня-аргумента у раннера уже есть (шапка, строки :78-80).
3. `roles/architect.md` — правило клона роли: `${TMPDIR}/dev-harness-<роль>/repo`,
   НЕ `./tmp/<имя>/repo` — клон внутри дерева сам является мутацией стерегомого.
   Architect был единственной ролью без правила (implementer.md:36,
   critic.md:155, adversary.md:26, reviewer.md:43 — уже несут).

### Б — райдеры раннера (кодовая, ровно три)

- **(i) мёртвый default-lock**: при ПУСТОЙ `VERIFY_ANTIPLACEBO_SCRATCH` скратч
  обязан быть ОБЩИМ и детерминированным — `${TMPDIR:-/tmp}/verify_antiplacebo-<hash8
  корня>`; уникальный `mktemp` делал rc=3 «занят» недостижимым по построению
  (lock в каталоге, который никто больше не видит). Выбор владельца предоставил
  архитектору: чинить детерминированностью ИЛИ документировать прозой — выбрано
  ЧИНИТЬ: это же закрывает наблюдённую гонку двух прогонов одного дерева.
- **(ii) течь скратчей**: завершившийся прогон (rc=0) убирает созданный им
  default-скратч на выходе (замер владельца: 47→49 каталогов за два прогона);
  существовавшее до прогона неприкосновенно; явный скратч — собственность
  вызывающего, не убирается; отказ rc=3 чужой скратч не трогает.
- **(iii) pid-recycling**: живость владельца lock — pid И pgid (поля lock
  «<pid> <pgid> <epoch>» pgid уже несут); `kill -0 <pid>` без сверки принимает
  посторонний процесс с перерождённым pid за владельца — stale lock не убирается
  никогда.

## Зоны

ЗОНА architect: contracts/012-izoljacija-progonov.md scripts/check_runner_hygiene.sh fixtures/check_runner_hygiene/
ЗОНА implementer: scripts/verify_antiplacebo.sh roles/ .omp/
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/

## Приёмочный критерий

Барьер `scripts/check_runner_hygiene.sh` расширен с 12 до 18 ветвей:
`lockdef techka pidrec izolcfg klon izolnorm` (наследие 011 не тронуто —
прогоны существующих ветвей на текущем дереве без изменений, scoped-гейт ниже
это доказывает). Все шесть новых ветвей зелёные на дереве ПОСЛЕ предмета; на
момент сдачи пачки — красные именованными причинами (райдеры реальны, норм нет).
Эталон `_ref_runner.sh` реализует райдеры честно (зелёный контроль независим от
предмета), зелёные корни `_lib.sh` несут нормы шага А/В.

- `bash scripts/check_runner_hygiene.sh . lockdef`
- `bash scripts/check_runner_hygiene.sh . techka`
- `bash scripts/check_runner_hygiene.sh . pidrec`
- `bash scripts/check_runner_hygiene.sh . izolcfg`
- `bash scripts/check_runner_hygiene.sh . klon`
- `bash scripts/check_runner_hygiene.sh . izolnorm`

счёт: 39 фикстур в fixtures/check_runner_hygiene/

Гейт БАРЬЕРА сдан архитектором ДО круга критика (rc=0, 39/39 красных
предъявлений, зелёный контроль в каждой):
`bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` — зелёный уже
на дереве до предмета, это проверка фикстур, а не предмета. Пробы выше — гейт
ПРЕДМЕТА, зеленеют после implementer. Полный прогон — только CI.

CI-примечание: шаг `check:runner-hygiene` КРАСЕН до предмета ПО УМЫСЛУ (барьер
красен на текущем дереве — это приёмка). Пуш предмета — на состоянии после
реализации (норма регламента, прецедент — 011).

Cognitive-only остатки (названы, не спрятаны): ветви `izolcfg`/`klon`/`izolnorm`
пинуют НАЛИЧИЕ маркеров (grep-ветви по прецеденту 011 norma), не размещение и не
единственность — держится кругом критика и чтением ролей (в 011 усиление norma
до fence/единственности шло кругами критика; здесь норма моложе). Ветвь `techka`
не судит прогоны, убитые SIGKILL (их убирает стартовая чистка следующего прогона
— ветвь chistka, не растёт в два предмета).

## Не-цели

- Топология `models_actual`/fallback — ОТДЕЛЬНЫЙ контракт (в HANDOFF на
  2026-08-25 значилась как «012»; фактически номер выдаёт `next_id.sh CONTRACT`
  регистром тегов — дерево есть истина, расхождение оговорено здесь).
- Изоляция КОДОМ раннера — запрещена решением владельца: только встроенная
  `task.isolation.mode` (harness-фича, не наш скрипт).
- Н-49 (config/metering.json literal), 011-bis (тайм-бокс делегирования) — не здесь.
- Расширение предмета за рамки А+В+3 райдера — не здесь («ровно эти три, не
  больше», решение владельца 2026-08-26).

## Незаполненные требования:
- Бюджеты и интервалы опроса зондов барьера (100 тиков ~13с у scratch/scratchdef/scratchexpl, 160 тиков = окно 8с у lockdef, паузы 0.05/0.1/0.02) — деталь барьера, не предмета; менять их — правка барьера, не контракта.
- Тайминги игрушек и обманок (сон игрушки 2с и 0.4с, decoy `setsid sleep 30`) — те же измерительные длительности барьера, не нормы шага А/В.
- Длина хеша в имени lock (hash8, `cut -c1-8`) — предмет (райдер i) требует ОБЩИЙ детерминированный путь и lock этого дерева, не конкретную длину хеша; длина — согласование барьера с раннером.
