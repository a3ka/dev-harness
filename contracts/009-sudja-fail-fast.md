# Контракт 009 — судья fail-fast (поток A, срез Н-41)

Номер выдан механизмом: `next_id.sh CONTRACT` → 009, тег `id/CONTRACT/009`. Предмет — решение
владельца (2026-08-24): закрыть второй механизм Н-41. Срез 1 уже сделан (008 → `judge_gate.sh
<sha>` через CI-сигнал). Срез 2: судья НЕ ВИСИТ в bash-вызове при долгом CI.

Из NABLIUDENIA Н-41: лимит bash-вызова судьи 600с, а CI может длиться дольше. Bash-вызов
таймаутится на 600с, и `hub` для отмены внутрипроцессного субагента нет (Н-41). `fail-fast`
дополняет существующее API: при превышении тайм-бокса `judge_gate.sh` отказывает СРАЗУ (rc=1 + сообщение),
не дожидаясь bash-таймаута.

## §Предмет — что делаем

Расширить `scripts/judge_gate.sh` режимом `--fail-fast[=<N>]` (поток A, срез Н-41).
008-закреплённое API (`judge_gate.sh <sha>` без флага) сохраняется 1:1. Новое:
- `--fail-fast` без значения → тайм-бокс 540с по умолчанию (90% от 600с лимита bash-вызова судьи);
- `--fail-fast=N` → тайм-бокс N секунд (N — натуральное > 0; не-цело и N≤0 — отказ с названной причиной).

Реализация: `check_ci_gate.sh` запускается через `timeout --foreground TBOX`. При срабатывании
timeout (rc=124) `judge_gate.sh` печатает в stderr дословно `FAIL-FAST: превышен тайм-бокс N с`
и выходит RC=1. Прочие RC пробрасываются; маркер `OK` на rc=0.

Защиты (отказ с названной причиной, RC=1):
- `command -v timeout` пусто → дословно `FAIL-FAST: timeout не найден` (даже если --fail-fast запрошен);
- `--fail-fast=N` с N≤0 ИЛИ N не целое → дословно `FAIL-FAST: тайм-бокс должен быть > 0 (дано: <N>)`.

## §Что значит готово — критерий (красные предъявлены ДО круга критика)

Барьер `scripts/check_judge_gate.sh` имеет 11 ветвей (2 унаследованных от 008 + 9 новых).
Фикстуры в `fixtures/check_judge_gate/case_*.sh` — ОДИННАДЦАТЬ (3 унаследованных от 008 + 8 новых).
Каждая фикстура предъявляет барьер КРАСНЫМ против сломанного предмета и ЗЕЛЁНЫМ против
heredoc-эталона с правильной реализацией.

### 11 ветвей барьера

**(красный)** (008) — fake реагирует на sha; `judge_gate.sh <deadbeef>` обязан RC≠0 + маркер FAKE.

**(зелёный)** (008) — fake реагирует на `$PASS_SHA`; `judge_gate.sh <PASS_SHA>` обязан RC=0 +
«OK» + fake видит `sha=$PASS_SHA`.

**(фailfast-медленный)** (009) — fake спит `$SLEEP_FOR=3с` ПЕРЕД реакцией; `judge_gate.sh
--fail-fast=2 <SLOW_SHA>` обязан: RC≠0 + дословно «FAIL-FAST: превышен тайм-бокс 2 с» + **в
выводе НЕТ «rc=1»**. Анти-плацебо: bash в script-режиме не пробрасывает SIGTERM в дочерний
sleep, и без sleep-перед-реакцией fake стал бы orphan. С sleep-перед-реакцией fake становится
«спящим», timeout SIGTERM убивает fake с обрывом маркера «rc=» (без «rc=0»/«rc=slow»/«rc=1»).
Стаб-имитатор (без timeout, синхронно ждёт fake 3с, потом печатает «FAIL-FAST») — fake проживает
все 3с и печатает «rc=1» → подстрока есть → ветвь красная. Это ОБХОД круга 1: имитация fail-fast
без timeout ловится не замером времени (ненадёжен из-за ~1с overhead от setsid+env-i), а
детерминированной подстрокой маркера fake.

**(фailfast-быстрый-ok)** (009) — fake отвечает мгновенно rc=0 на `$PASS_SHA`; `judge_gate.sh
--fail-fast=2 <PASS_SHA>` обязан: RC=0 + «OK» + fake видит `sha=$PASS_SHA`. Стаб, не реализующий
timeout и пробрасывающий `--fail-fast=2` как sha, на этом входе получит rc=1 от fake (потому
что `$1 = --fail-fast=2` ≠ `PASS_SHA`) → die.

**(фailfast-дефолт)** (009) — fake отвечает мгновенно rc=0 на `$PASS_SHA`; `judge_gate.sh
--fail-fast <PASS_SHA>` (bare, без `=<N>`) обязан: RC=0 + «OK». Стаб, который обрабатывает
только `--fail-fast=N` (не bare), пробрасывает `--fail-fast` как sha в check_ci_gate → rc=1 →
die.

**(фailfast-дефолт-540)** (009) — доказательство, что bare-дефолт РЕАЛЬНО 540 (а не 0/1/2/3/...).
fake спит 2с ПЕРЕД реакцией. Без fake-timeout стаб с `TBOX=3` пройдёт как зелёный (fake
спит 2с, успевает за 3с) — это ОБХОД круга 2. Закрытие: fake-timeout через PATH-minimal
(`$WORK/nopath/timeout`) — это скрипт, который пишет $@ в `$WORK/timeout_args.log` и затем exec
настоящего `/usr/bin/timeout`. Барьер проверяет, что в args.log содержится `--foreground 540`.
Стаб с `TBOX=1` (как в `case_failfast_default_540.sh`) запишет `--foreground 1` → die с
подстрокой «FAIL-FAST: превышен тайм-бокс 1 с» (вывод стаба, который содержит это, попадает
в die через `Вывод: $OUT`).

**(фailfast-произвольный-N)** (009) — `judge_gate.sh --fail-fast=7 <PASS_SHA>` (произвольный
N, не 2) обязан: RC=0 + «OK» + fake видит `sha=$PASS_SHA`. Стаб, который принимает только
`--fail-fast=2` (а другое отвергает), провалится: на N=7 он отвергнет вход → RC=1 (вместо
RC=0 + OK) → die. Это закрывает ОБХОД круга 2: «предмет принимает только N=2, отвергает
обязательное `--fail-fast=7`».

**(фailfast-защита-N-невалидный)** (009) — `judge_gate.sh --fail-fast=0 <PASS_SHA>` обязан: RC≠0 +
дословно «тайм-бокс должен быть > 0». Стаб (008), пробрасывающий `--fail-fast=0` как `$1` в
check_ci_gate, даст rc=1 + «CI не зелёный» — НЕ «тайм-бокс должен быть > 0» → die.

**(фailfast-защита-N-нецелый)** (009) — `judge_gate.sh --fail-fast=abc <PASS_SHA>` обязан: RC≠0 +
дословно «тайм-бокс должен быть > 0» (закрывает «предмет отвергает только N=0, но принимает
N=abc»). Стаб (008) пробрасывает → rc=1 → die.

**(фailfast-защита-timeout-отсутствует)** (009) — PATH без timeout (через `$WORK/nopath/` с
симлинками на bash/sh/cat/date/echo/printf/sleep/dirname/pwd, БЕЗ timeout). `judge_gate.sh
--fail-fast=2 <PASS_SHA>` обязан: RC≠0 + дословно «timeout не найден». Стаб (008), не
проверяющий наличие timeout, пробрасывает в check_ci_gate → rc=1 + «CI не зелёный» — НЕ
«timeout не найден» → die.

Старые ветви `(красный)` и `(зелёный)` (008) НЕ ТРОГАЕМ — обратная совместимость.

### Конструкция fake

Fake `check_ci_gate.sh`:
- `printf '${FAKE_MARK} sha=%s rc=' "$1" >&2` — печатает маркер с `$1`, **БЕЗ newline** (обрыв на `rc=`).
- `sleep ${eff_sleep}` — ПЕРЕД реакцией (см. комментарий в `check_judge_gate.sh` про bash+SIGTERM).
- Если `$1 = $PASS_SHA` → `printf '0\\n'; exit 0` (rc=0, без задержки — sleep уже выполнен).
- Иначе → `printf '1\\n'; exit 1` (rc=1 после sleep).

`eff_sleep = ${SLEEP_FOR:-0}` — задаётся на вызов через `SLEEP_FOR=N run_gate ...`. По умолчанию 0.

### Анти-плацебо (стабы НЕ проходят)

| стаб | что ловит |
|---|---|
| стаб-всегда-RC0 | (красный) — пропустил не-зелёный CI |
| стаб-всегда-RC≠0 | (зелёный) — пропустил зелёный CI |
| стаб-без-вызова-fake | ОБЕ — нет маркера FAKE |
| стаб-игнорирующий-SHA | (зелёный) — fake без matching SHA вернёт rc≠0 |
| стаб-RC0-без-OK | (зелёный) — нет маркера «OK» |
| стаб-без-fail-fast | (фailfast-*) — RC≠0 на PASS_SHA с --fail-fast (вместо RC=0 + OK) |
| стаб-имитирующий-fail-fast | (фailfast-медленный) — `rc=1` от fake, прожившего все 3с |
| стаб-не-отвергающий-N≤0 | (фailfast-защита-N-невалидный) — `rc=1` от fake (008 пробрасывает) |
| стаб-не-отвергающий-N-нецелый | (фailfast-защита-N-нецелый) — `rc=1` от fake |
| стаб-без-проверки-timeout | (фailfast-защита-timeout-отсутствует) — `rc=1` от fake |
| стаб-с-TBOX≠540 | (фailfast-дефолт-540) — timeout_args.log содержит `--foreground TBOX≠540` |
| стаб-с-N≠7 | (фailfast-произвольный-N) — RC=1 на `--fail-fast=7` |

## §Зоны

ЗОНА implementer: scripts/judge_gate.sh
ЗОНА architect: scripts/check_judge_gate.sh fixtures/check_judge_gate/ NABLIUDENIA.md HANDOFF.md
contracts/009-sudja-fail-fast.md

Связность: implementer расширяет ПРЕДМЕТ режимом --fail-fast (правка ~30 строк поверх 008:
разбор флага + валидация N + проверка timeout + timeout-обёртка + три сообщения об ошибке).
Architect расширяет БАРЬЕР (9 новых ветвей + fake с sleep-перед-реакцией + fake-timeout через
PATH + проверка timeout_args.log) и пишет 8 фикстур (heredoc эталона с правильной реализацией
+ красный стаб). 008-закреплённое API сохраняется 1:1 — это инвариант, проверяется
регресс-фикстурами 008.

## §Приёмка (исполняемая)

1. `bash scripts/check_judge_gate.sh` → 0. Все ОДИННАДЦАТЬ ветвей зелены.
2. `npm run check:judge-gate` → 0 (паритет с 008).
3. `npm run check:ci-parity` → 0 (нет новых команд в проводке — расширение того же барьера).
4. Регресс 008: `bash scripts/check_judge_gate.sh . зелёный && bash scripts/check_judge_gate.sh . красный`
   → 0 на 0. Фикстуры `case_dispatcher_sha` / `case_krasnyj_lax` / `case_zelenyj_bypass_sha` зелёные
   на реальном judge_gate.sh без флага (008 API сохраняется 1:1).
5. Полный CI на чистом чекауте: `bash scripts/check_ci_gate.sh` (запушенный HEAD) → 0.

## §Незаполненные требования

- Поведение при `--kill-after` для подвисших процессов (если SIGTERM игнорируется): контракт
  требует невисения, а не конкретного механизма. Деталь свободная.
- Контракт НЕ различает «CI не зелёный» и «превышен тайм-бокс» по RC — оба RC≠0. Различие в
  сообщении. Это сделано, чтобы стаб, возвращающий «CI не зелёный» на медленном входе, проваливал
  ветвь `(фailfast-медленный)` — что и требуется.

## §История правок

- Круг 1 архитектора (c0ce87b): контракт + барьер (5 ветвей) + 3 fail-fast фикстуры.
- Круг 2 архитектора (4953943): правки по 2 блокирующим находкам (защиты default=540,
  N≤0, timeout-отсутствует; отказ от проверки elapsed в пользу детерминированной подстроки).
- Круг 3 архитектора (текущий): правки по повтору обеих блокирующих находок (2 раунд):
  fake упрощён до двух исходов (rc=0 для PASS_SHA, rc=1 для всего остального — закрывает обход
  стаба-имитатора с shift); fake-timeout через PATH-minimal для проверки TBOX=540 (закрывает
  обход стаба с TBOX=3); новая ветка `(фailfast-произвольный-N)` с N=7 (закрывает обход стаба,
  принимающего только N=2); новая ветка `(фailfast-защита-N-нецелый)` с `--fail-fast=abc` (закрывает
  обход стаба, отвергающего только N=0).