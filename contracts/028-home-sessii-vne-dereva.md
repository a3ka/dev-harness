# Контракт 028 — структурный фикс судимого дерева: сессионный HOME/.zones наружу чекаута (Н-98)

Слово владельца 2026-09-16 (Н-98): структурный фикс — ПРИОРИТЕТ очереди; именной
паттерн №5 для composer ОТВЕРГНУТ («пятый виток треугольника, осуждённого кодом
:637 и ревьюером 024»). Слово владельца 2026-09-17: «composer-фикс ПЕРВЫМ», mint 028
выдан (6b5eb8a). Frontier (agent://ArchN98Frontier) выбрал Вариант 1 — перенос HOME
наружу чекаута; Вариант 2 (исключение по идентичности job-id) фатален: идентичности
в env нет (printenv-замер), `status.json` — монолитный учёт ВСЕХ джобов демона
(«исключить свой job» из целостного sha256 нельзя — это паттерн №5, переодетый в
идентичность), атрибуция писателя из bash недоступна, version-coupling остаётся,
потребовал бы правки ЗАМОРОЖЕННОГО check_no_leak.sh.

## Боль (Н-98, измерено 2026-09-16)

Шесть последовательных `check_no_leak.sh --check` → rc=1 «основной чекаут мутировал
во время сверки» при: чистых `git status`/`.git`-наблюдателях (13 status-сэмплов +
18 stat-сэмплов, ноль мутаций), подтверждённо тихой параллельной сессии, СОЛО-режиме
(единственный живой джоб — сама сверка). Решающий замер `find -newermt` по окну
шестой сверки: единственный НЕ накрытый carve-out (`:642-647`) мутатор —
`.zones/dev/.omp/profiles/dev/agent/cache/composer/<hash>/status.json`, учёт джобов
демона omp. **Цена:** ~1,5 ч сессии (6 сверок по 5–11 мин + два наблюдателя +
find-диагностика — TOCTOU-петля ≥600 с), блок круга критика 027; дублирующий симптом
— таймауты 120 с/600 с и «чистота основного чекаута не подтверждена» в стенограмме
критика 026 (dbeffd4). **Сцепление с версией** (диагноз Н-98): класс composer
отсутствовал в фиксе 2026-09-15; мера этой пачки (case-глобами по `:642-647`,
вывод в отчёте архитектора): `cache/composer/<hash>/status.json`, `welcome.json`,
`.npm/_logs/*.log`, `agent.db` (чекпойнт, не -wal/-shm), `autoqa.db` — все
NOT-COVERED → каждый апдейт omp = новый красный класс = новый круг. Перечисление
имён этот класс не закрывает (Н-39: «новые классы — новый круг адверсария, не
перечисление»); закрывает ТОПОЛОГИЯ: рантайм-писатели вне судимого дерева.

Producer — рантайм-профиль omp; канал управления путём ЕДИНЫЙ: `$HOME`
(workshop:45 `ZONE="$HERE/.zones/dev"` → :437/:502 `export HOME="$ZONE"` →
:465/:546 `exec omp --profile dev`; всё состояние в `$HOME/.omp/…` — зонды frontier
A/B/C: `omp config path` следует за HOME). Это обобщение carve-out 011
(AGENTS.md:256-261, причина-паттерн Н-48: in-tree scratch ловит собственную запись)
и продолжение затравки слой-2 контракта 026 (`:195-200`: явный HARNESS_SCRATCH,
разрешающийся внутрь корня → именованный отказ ДО создания).

## Предмет (инварианты)

1. **Одна ручка и одна формула.** Сессионный дом вычисляется в workshop блоком
   разрешения ZONE (сейчас :45) из
   `ZONE="${HARNESS_SCRATCH:-${XDG_STATE_HOME:-$HOME/.local/state}/dev-harness-sessions/<hash8-канон-корня>}/zones/dev"`
   — профиль `dev` остаётся субдиректорией, внутренняя раскладка `.omp/…` не
   меняется (трейсы/agent.db едут ЦЕЛИКОМ, `--resume` workshop:338-348 непрерывен).
   Блок разрешения ZONE — ЕДИНЫЙ источник: `scripts/models_actual.sh:20` несёт его
   побайтово (второй источник разрешения запрещён — гигиена единой грамматики).
   Блок стоит ДО подмены HOME (дефолт раскрывается из РЕАЛЬНОГО `$HOME`) и до
   ветки `--check-metering` (:377-383 — не задета, стоит позже).
   Дефолт — durable, НЕ `${TMPDIR:-/tmp}` затравки 026 (та — для одноразового
   раннер-скратча): замер этой пачки — `/tmp` есть tmpfs, живой `.zones` = 2,6 ГБ
   судебных улик; состояние сессии обязано переживать ребут.
2. **Именованный отказ до подмены.** ЯВНЫЙ `HARNESS_SCRATCH`, канонически
   разрешающийся внутрь канон-корня чекаута (каноникализация меры 014: спуск до
   существующего предка + `pwd -P`), → `die` «HARNESS_SCRATCH внутри стерегомого
   дерева: <путь>» ДО создания каталогов, ДО `export HOME`, ДО `exec` — в блоке
   ZONE, раньше разбора входов (дешёвая проверка на дешёвом этапе). Дефолтный
   XDG-путь внутрь корня не разрешается (не под чекаутом), отказ — только про
   явную ручку.
3. **Ноль новых путей рантайма в дереве.** Старт сессии (`./workshop` до `exec omp`
   включительно) не создаёт НИ ОДНОГО пути под `<корень>/.zones` — workshop и
   omp следуют за HOME (измерено зондами frontier), composer/agent.db/npm-логи
   уезжают целиком: version-coupling снят по корню, а не по списку имён.
4. **Инвариант :638-641 пережил перенос.** Замороженный carve-out
   `check_no_leak.sh:642-647` НЕ трогается: evil-класс под `.zones/`
   (`.zones/…/evil.txt`, `.zones/…/agent/evil.db`, `.zones/…/logs/evil.bin`)
   остаётся ловимым `--check` rc 1 ПОИМЁННО (проба 3 — регресс-пара).
5. **Миграция mv, не удаление.** Старые трейсы/agent.db — судебные улики
   (NABLIUDENIA ссылается на них); порядок церемонии — ниже, исполнитель —
   владелец/оркестратор на живом дереве (разовая, вне git).
6. **Замороженное не трогается:** `scripts/check_no_leak.sh` (024),
   `scripts/verify_antiplacebo.sh` (011/012/014; prune `./.zones` в `:520`
   остаётся безвредным no-op — исключение каталога, которого нет),
   `AGENTS.md` правило 16 (норма `./tmp` для временного и carve-out 011 — без
   изменений), `contracts/001:63` (`.zones` — дом чужих процессов: остаётся
   согласованным).
7. **models_actual.sh** (критерий Н-50-семьи) следует той же ручке; его фикстура
   `fixtures/models_actual/case_model_razoshlas.sh` переводится на
   `HARNESS_SCRATCH`-игрушку (иначе ложный NOT_IMPLEMENTED: трейсы «исчезли»).
8. **config/ci_parity_exceptions.txt:26-28** — текст исключения `models:actual`
   обновляется под новый корень трейсов (правка текста, не поведения).
9. **.gitignore:44 `.zones/` остаётся на переход** (неперемещённые/чужие деревья);
   удаление строки — отдельная гигиена после вымирания старых деревьев.

## Зоны

ЗОНА architect: contracts/028-home-sessii-vne-dereva.md fixtures/workshop/ fixtures/check_judge_gate/ NABLIUDENIA_ARCHITECT.md
ЗОНА implementer: workshop scripts/models_actual.sh config/ci_parity_exceptions.txt fixtures/models_actual/ .gitignore
ЗОНА orchestrator: AGENTS.md roles/orchestrator.md HANDOFF.md NABLIUDENIA.md
ЗОНА critic: verdicts/critic/
ЗОНА adversary: verdicts/adversary/
ЗОНА reviewer: verdicts/review/

Пробы до freeze НЕ коммитятся (А-155: зоны читаются только из freeze): живут
staged-копией в `${TMPDIR}/dev-harness-verify/028-architect/`, садятся
land_probes.sh по тегу `frozen/contracts/028/1` (прецедент 020). Семья
`fixtures/check_judge_gate/` — дом красных/канареек детектора по замороженному
тексту 024 (`check_no_leak.sh:3-7`); `fixtures/workshop/` — новая семья лаунчера.

## Приёмочный критерий

Счёт: 4 предъявления, все исполняемые (755), привязка стабов к веткам — КОД проб
(Н-39), проза здесь несёт инварианты + rc-команды. Постоянные case_* не заводятся:
предмет — лаунчер живой сессии и живое дерево, не барьер мета-прогона;
повторяемость — прямым прогоном предъявлений.

1. `bash fixtures/workshop/red_scratch_vnutri_dereva.sh` — rc 1 → 0.
   Ветка: блок ZONE workshop (:45) / отказ. Вход: явный `HARNESS_SCRATCH`,
   указывающий внутрь клона. Честная реализация: именованный отказ (пин
   подстроки «внутри стерегомого дерева»), rc≠0, каталог скратча НЕ создан,
   `.zones` в дереве НЕ создан, omp НЕ запущен. Стаб «молча принять» (сегодня:
   ветки нет — HOME молча уходит в дерево) красен ИМЕННО на этом входе.
2. `bash fixtures/workshop/red_zony_net_v_dereve.sh` — rc 1 → 0.
   Ветки: :437/:502 `export HOME` + :465/:546 `exec`. Вход: внешний
   `HARNESS_SCRATCH` + запуск workshop в одноразовом клоне (omp — PATH-подставной,
   объявлен в исходнике пробы, прецедент А-156; живая сессия НЕ спавнится).
   Честная реализация: подставной omp видит `HOME=<скратч>/zones/dev` (внешний),
   `session-prompt-*` лежит там же, в клоне НИ ОДНОГО нового пути `.zones/`.
   Стаб «выключить composer конфигом» красен здесь: HOME остаётся в дереве,
   `.zones/` в клоне появляется (agent.db/autoqa.db писались бы всё равно —
   классы NOT-COVERED по мере этой пачки). Живой end-to-end (настоящая сессия
   omp на живом дереве) — канарейка 4.
3. `bash fixtures/check_judge_gate/red_regress_028_zony_evil.sh` — регресс-пара,
   зелёная сегодня и ОБЯЗАННА остаться зелёной после предмета.
   Инвариант 4. Живой оракул `$REPO/scripts/check_no_leak.sh` (не предмет):
   снимок игрушки → `.zones/x/agent/evil.txt` + `.zones/x/logs/evil.bin` →
   `--check` rc 1 «основной чекаут загрязнён», ОБА пути названы; без утечки →
   rc 0 «чисто». Красное предъявление против стаба «глушить .zones целиком по
   профилю»: sed-копия оракула в игрушке с расширенным до `.zones/*` пропуском
   (объявлена в исходнике пробы) даёт rc 0 на ТОМ ЖЕ входе — проба различает
   честное от стаба поимённо. Дефект стаба наблюдаем ровно на этом входе
   (evil-класс под `.zones/` невидим стабу, виден оракулу).
4. `bash fixtures/check_judge_gate/canary_solo_028.sh <абс-корень живого дерева>`
   — rc 1 → 0. Живая канарейка Н-98 (форма canary_zhivoj_024.sh): снимок →
   сверка → повторная сверка, все rc 0. Сегодня красна самотрипом Н-98
   (6×rc=1, Н-98:47; свежий прогон — в отчёте архитектора). Стаб-паттерн №5
   (именной carve-out composer) опровергнут МЕРОЙ, не прогоном: NOT-COVERED-набор
   (`composer/status.json`, `welcome.json`, `.npm/_logs/*.log`, `agent.db`,
   `autoqa.db`) шире паттерна — каждый апдейт omp переоткрывает класс.

Прогоны (точные команды; сейчас → после):

    bash fixtures/workshop/red_scratch_vnutri_dereva.sh            # rc 1 → 0
    bash fixtures/workshop/red_zony_net_v_dereve.sh                # rc 1 → 0
    bash fixtures/check_judge_gate/red_regress_028_zony_evil.sh    # rc 0 → 0 (регресс-пара + стаб-предъявление)
    bash fixtures/check_judge_gate/canary_solo_028.sh <абс-корень> # rc 1 → 0 (живое дерево)

## Порядок миграции (церемония владельца, mv — не удаление)

1. Остановить живые сессии чекаута (все omp профиля dev на нём), включая демона
   (broker.sock переезжает вместе с `.zones`).
2. `mkdir -p "${XDG_STATE_HOME:-$HOME/.local/state}/dev-harness-sessions/<hash8-корня>"`.
3. `mv <корень>/.zones/dev "${XDG_STATE_HOME:-$HOME/.local/state}/dev-harness-sessions/<hash8>/zones/dev"`
   — только mv; трейсы/agent.db — улики; ничего не удаляется.
4. Перезапуск `./workshop`; контроля живой сессии: `omp config path` → внешний
   путь; `bash scripts/models_actual.sh` → находит трейсы, rc 0.
5. `bash fixtures/check_judge_gate/canary_solo_028.sh <абс-корень>` → rc 0
   (живая приёмка предмета: соло-сессия больше не валит сверку).
6. Остаток: пустой/чужой `.zones` в дереве остаётся как есть; `.gitignore:44`
   держит переход (инвариант 9).

## Не-цели

- **Н-89** (ФС-enforcement) — паркован, 6 развилок владельца; перенос HOME
  не средо-зависим и не ждёт его.
- **Имена carve-out `:642-647` не расширять и не сужать**: сужение — отдельный
  предмет (k12 / 024-Р1 «carve-out судит ИМЯ»); расширение — отвергнутый
  паттерн №5.
- **Слой-2 $HARNESS_SCRATCH для раннер-скратча** (19 скриптов, миграция TMP_ROOT —
  затравка 026:171-185) — отдельный предмет: здесь только сессионный HOME.
- Правки ролей не нужны (замер: grep `\.zones` по roles/ пуст); чистка старых
  `.zones` чужих/старых деревьев; отмена правила 16; смена роли/модели сессии.
