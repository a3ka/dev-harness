# Контракт 006 — быстрый изолированный гейт (поток A)

Номер выдан механизмом: `next_id.sh CONTRACT` → 006, тег `id/CONTRACT/006`. Предмет —
решение владельца (HANDOFF «ГДЕ МЫ», поток A): закрыть налог Н-41 — полный гейт
`verify_antiplacebo.sh` идёт 11–23 мин и прогоняется ~4 раза за контракт. Проект конструкции
решён советом fable (Anthropic) + terra (OpenAI) за 2 круга — `history://CouncilFable`,
`history://CouncilTerra`, `history://CouncilFable2`, `history://CouncilTerra2`. D1 (worktree) и
D2 (граница) — решения архитектора; замороженный слепок контракта 001
(`frozen/contracts/001/1`) НЕ трогается.

## Предмет

Гейт получает **scoped-режимы**: прогонять только ЗАДЕТЫЕ барьеры вместо всех. Три части:

- `scripts/scope_select.sh` — **юнит-селектор** (объявляет `НЕ БАРЬЕР:`, тестируется через
  `check_scope_select.sh`, как `metering_proxy.ts` через `check_metering.sh`): состояние дерева
  + флаги → множество ключей + режим + код;
- `scripts/check_scope_select.sh` — **барьер приёмки**: гоняет `scope_select.sh` по сценариям и
  УТВЕРЖДАЕТ выбор/режим/код/причину (по образцу `check_metering.sh`);
- интеграция в `scripts/verify_antiplacebo.sh`: в scoped-режиме прогоняет ТОЛЬКО выбранные
  барьеры; слепок дерева и ВСЕ ветви отказа полного прогона сохраняются.

1. **Интерфейс.** `scope_select.sh <корень> --changed <база>` — выводит задетое из
   `git diff --name-status -z <база>`, исследуя ОБА конца диффа. `scope_select.sh <корень>
   --scope <ключ>[/<case>]…` — явные ключи барьеров, опционально case-уровень. `verify_antiplacebo.sh`
   без флага — ПОЛНЫЙ прогон (умолчание, поведение не меняется ни на байт); с `--changed`/`--scope`
   — потребляет селектор и фильтрует цикл.
2. **Отображение (консервативный откат, fail-closed, ВЫВЕДЕНО из шапок).** Барьер = файл
   `scripts/`, чья первая непрерывная шапка объявляет `Коды возврата:`; библиотека/support =
   объявляет `НЕ БАРЬЕР:`. Сужение до ключа только если файл присутствует ТЕМ ЖЕ барьером в
   ОБОИХ концах диффа, роль/коды/шапка не менялись, нет динамического `source`, его не сорсит
   другой eligible-барьер. Любое add/delete/rename, смена роли, неизвестный/неклассифицированный
   файл, правка библиотеки/раннера/самого `scope_select.sh` → ПОЛНАЯ выборка. Скан
   классификации всего `scripts/` и сверка «барьер↔фикстура» — полнодеревные в ЛЮБОМ режиме.
3. **Коды и маркировка неавторитетности.** Пустая выборка (дифф вне барьеров/фикстур) → код 2 +
   строка «0 задетых — scoped ничего не доказал, полный гейт в CI». Инструмент недоступен (нет
   `git`, база не резолвится) → 2 (fail-closed), НЕ «0 задетых успех». Неизвестный ключ/case → 1
   с названным ключом; пустой явный `--scope` → 1. Scoped-режим печатает машинно-отличимый
   маркер (`MODE: scoped|needs-full` + строку `SCOPED: … — не для приёмки`), отличный от полного
   прогона, и НЕ содержит пути публикации CI-статуса. Требование потребителя проверять ИМЕННО
   полный контекст `antiplacebo-full` — следующий пункт потока A (см. «Незаполненные требования»).
4. **Узкая изоляция ресурсов.** Порты/HOME/TMPDIR per-fixture (`env -i`, свой `TMPDIR`, ретраи
   EADDRINUSE — большей частью уже есть; предмет — аудит и добор недостающего). Замороженный
   слепок (исключения контракта 001) НЕ трогается. Конкурентный локальный гейт даёт ИСТИННОЕ
   громкое красное «прогон недостоверен целиком» (не false-green) — сохранённая ветвь.

## Критерий готовности

Приёмка в ДВА тира. **Тир 1 (селектор)** — красные фикстуры `check_scope_select` предъявлены
КРАСНЫМИ против обманного стаба ДО круга критика; краснота засчитана ПОВТОРНЫМ прогоном
(предъявлено). **Тир 2 (интеграция в раннер)** требует уже существующего `--changed`-режима,
поэтому предъявляется красным-затем-зелёным на СБОРКЕ (шаг 4а, после раздачи), а не до критика:
режима ещё нет — красное до критика было бы обещанием несуществующего.

**`fixtures/check_scope_select/case_*.sh`** — барьер `check_scope_select.sh` предъявляется
красным на каждой ветви (обманный `scope_select` не ловится):
- (а) `--scope` неизвестный ключ → 1, названный ключ;
- (б) `--scope` пустой явный список → 1;
- (в) `--scope <ключ>/<неизвестный_case>` → 1, названный case;
- (г) `--changed` дифф только по докам → 2 + строка «0 задетых…», НЕ 0;
- (д) `--changed` при недоступном `git` / нерезолвимой базе → 2, НЕ «0 задетых успех»;
- (е) `--changed` правит барьер `b` (тот же барьер в обоих концах, роль не менялась, нет
  динамического `source`) → `MODE: scoped`, выбран РОВНО ключ `b` (не `a`);
- (ж) `--changed` правит библиотеку (`НЕ БАРЬЕР`, сорсится барьером) → `MODE: full`;
- (з) `--changed` add / delete / rename / смена роли шапки → `MODE: full` (или 2), НЕ частичный
  scoped;
- (и) case-уровень: `--scope check_metering/case_X` → выбран РОВНО этот case; `--changed` самого
  `scripts/check_metering.sh` → ВЕСЬ набор его case, а не один;
- (к) scoped/needs-full печатает `MODE`-маркер и строку `SCOPED: …`, отличные от полного; путь
  публикации CI-статуса ОТСУТСТВУЕТ (фикстура сверяет маркер и его отсутствие у полного).

**`fixtures/verify_antiplacebo/case_*.sh`** (новые, ТИР 2 — предъявляются на сборке 4а, после
интеграции `--changed`) — scoped-режим в раннере:
- (л) `--changed`, выбран `b` → прогнаны ТОЛЬКО фикстуры `b` (фикстуры `a` не запускались,
  наблюдаемо), полный прогон — все;
- (м) выбранный барьер, чья ветвь отказа сломана, краснеет в scoped-режиме так же, как в полном
  (scoped сохраняет ветви отказа);
- (н) запись вне точного `tmp/antiplacebo/run-$$` при scoped-прогоне → «прогон недостоверен
  целиком» (регресс существующей ветви слепка под узкой изоляцией; замер 1 `oblast-slepka`).

## Исполнители и зоны

ЗОНА architect: contracts/006-scoped-izolirovannyj-gejt.md scripts/check_scope_select.sh fixtures/check_scope_select/ fixtures/verify_antiplacebo/ NABLIUDENIA.md HANDOFF.md
ЗОНА implementer: scripts/scope_select.sh scripts/verify_antiplacebo.sh package.json .github/workflows/ci.yml config/ci_parity_exceptions.txt

Связность по разделению «предмет ↔ проверка»: архитектор владеет ПРОВЕРКОЙ — барьер приёмки
`check_scope_select.sh` и его фикстуры `fixtures/check_scope_select/` — и пишет их одной пачкой,
предъявляя красными ДО раздачи (честный барьер ловит сломанные scope_select-стабы в игрушечных
деревьях фикстур; так красное не тавтологично). Исполнителю раздаётся ПРЕДМЕТ — реальный
`scope_select.sh` — плюс интеграция scoped-режима в `verify_antiplacebo.sh`, узкая изоляция,
новые интеграционные фикстуры `fixtures/verify_antiplacebo/` и проводка (npm-команды, CI, файл
исключений паритета). Исполнитель не судит свою работу: приёмочный барьер и его фикстуры —
архитектора; реальный `scope_select.sh` обязан пройти `check_scope_select.sh` на ВСЕХ ветвях
(имена флагов, формат `MODE:`/`KEY:`/`SCOPED:` и подстроки причин закреплены барьером и
фикстурами). `scope_select.sh` объявляет `НЕ БАРЬЕР:` (предмет проверки, не её исполнитель).

## Приёмка

1. `bash scripts/check_scope_select.sh` → 0: ветви (а)–(к) на честном `scope_select.sh`.
2. `bash scripts/verify_antiplacebo.sh` → 0: `fixtures/check_scope_select/` и новые
   `fixtures/verify_antiplacebo/` (л)–(н) предъявлены красным повторным прогоном; полный прогон
   всех барьеров зелёный.
3. `npm run check:antiplacebo` → 0; `npm run check:ci-parity` → 0.
4. Ускорение предъявлено прогоном: `bash scripts/verify_antiplacebo.sh --changed <база>` при
   правке одного барьера печатает выбранные ключи и число барьеров < полного; при правке
   библиотеки — полный набор.

## Незаполненные требования:

- **worktree-на-прогон** (конкурентные локальные ПОЛНЫЕ гейты) — ОТЛОЖЕН, решение владельца:
  требует строки `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` (меняет замороженное множество исключений слепка 001);
  арбитра недостаточно (граница проведена самим `oblast-slepka`). Совет назвал два замера против
  worktree (проверяет КОММИТ, не рабочее дерево → вчерашний код; смешанный режим коллидирует,
  т.к. `tmp/gate-runs` не в исключениях). Следующий пункт потока A (судья через CI-сигнал+async)
  убирает ИЗМЕРЕННЫЙ источник Н-41; остаток (перекрытие исполнителей на общем чекауте) —
  fail-noisy, не false-green.
- **ужесточение `check_ci_gate.sh`** до требования ТОЧНОГО контекста `antiplacebo-full` (сейчас
  строки 77–86 принимают любое непустое множество success) — ВЫНЕСЕНО в следующий пункт потока A
  («судья гейтит через CI-сигнал»). До него переходное окно generic-all-success — записанный
  ИЗВЕСТНЫЙ риск, не замолчанный.

## Статус

Статуса задач нет: контракт замораживается побайтово, ход работ — в `HANDOFF.md`.
