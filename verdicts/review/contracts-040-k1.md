FAIL

# Ревью контракта 040 — круг 1 (первый ревьюерский взгляд на предмет целиком)

Предмет: контракт `contracts/040-batching-git-vyzovov.md` (`frozen/contracts/040/1`
= тег-объект `dd627f8e`, коммит `44e97b73`, блоб `fe69e982`), вся реализация A2 и
все пост-фризовые круги: 9 адверсарных вердиктов (`62fe511`, `16f8f0f`, `d5f3cc3`,
`8e6123c`, `d46a615`, `837cf42`, `6702d73`, `3fb4c84`, `033ec64`) и 3 арбитража
(`5c24017`→переименован `14f3a96`, `7c66a70`, `a0838bd`). Судья: reviewer, семейство
отлично от автора работы (architect — `anthropic`). Все 12 судебных файлов прочитаны
целиком, не по пересказу. Прогоны — на `main` (HEAD `033ec64`) плюс одноразовый клон
`/tmp/dev-harness-verify/rev040k1/repo` для мутанта; клон в дерево не вносится.

Вердикт: **FAIL**, две блокирующие находки (Б1, Б2) и три замечания (О1-О3).
Инженерное ядро предмета — batched-техника, её семантика и граница доверия —
проверено и держится: Р2 жив и красен по построению, Р3/Р4/Р5/Р6/Р7/Р9 зелены
СВОЕЙ мерой, батчинг реально куплен (`check_protected` 23.5с → 3.47с). Обе
блокирующие находки лежат в ИЗМЕРИТЕЛЬНОМ контуре: решающий критерий Р1 на HEAD
измеряет пустоту, а машинный пин Р8 замороженной таблицы приёмки на HEAD КРАСЕН.

---

## Б1 (БЛОКЕР). Решающий критерий Р1 — пусто-зелёная мера: 0 вызовов на обоих деревьях, и мутант «полный возврат к per-commit» проходит её зелёным

Арбитраж 040-I (`5c24017`, §Б1) сделал дифференциальный call-count РЕШАЮЩИМ
утверждением контракта — именно потому, что абсолютная граница как класс
обходима. §Приёмка Р1 («ДИФФЕРЕНЦИАЛЬНЫЙ (решающий)») и §Инварианты п.2
воспроизводят это дословно. На HEAD этот критерий не измеряет ничего.

Живой прогон Р1 на `main` (`033ec64`), моя мера:

```
$ bash fixtures/check_zones/red_predel_git_vyzovov.sh
toy LOW:  K=12 M=60 L=5 -> 133 коммитов judged, git-вызовов=0 (нарушение поймано)
toy HIGH: K=12 M=60 L=35 -> 493 коммитов judged, git-вызовов=0 (нарушение поймано)
абсолютная граница (грубая, на LOW) a*(K+M)+b = 6*(12+60)+20 = 452
дифференциал: C_high(0) - C_low(0) = 0 - граница S = 15 (Δ на окно = 30, Δ>=2S держится: 30>=30)
ПРЕДЕЛ ДЕРЖИТСЯ: LOW=0<=452 (грубо) И дифференциал 0<=15 (решающе)
rc=0   (58.91 s)
```

`C_low = C_high = 0`. Оба утверждения — и грубое `0<=452`, и решающее `0<=15` —
истинны ВАКУУМНО. Причина структурная: `run_trace` (`fixtures/check_zones/
red_predel_git_vyzovov.sh:149-155`) измеряет через `env SHELLOPTS=xtrace
BASH_XTRACEFD=9 bash "$CZ" "$R"`, а субъект с v6/v7 первой же строкой делает
`exec /usr/bin/env -i PATH=… LC_ALL=… /usr/bin/bash "${BASH_SOURCE[0]}" "$@"`
(`scripts/check_zones.sh:202-205`), который стирает ОБЕ измерительные переменные.
Хуже: re-exec срабатывает ИМЕННО ПОТОМУ, что они установлены — условие гарда
сравнивает `/proc/$$/environ` с канонической строкой, и наличие `SHELLOPTS`
делает её неканоничной. Канал измерения и гард субъекта взаимно исключают друг
друга ПО ПОСТРОЕНИЮ: счётчик равен нулю для ЛЮБОЙ реализации.

**Красное предъявлено — его нет.** В одноразовом клоне `033ec64` я вернул в
горячий цикл ровно тот per-commit вызов, замену которого контракт объявляет
предметом (§Инварианты п.1: «автор коммита (сегодня: `git log -1 --format=%an
"$c"` в цикле, `:333`)»), не трогая ничего другого:

```
$ grep -n -A1 'commits=$((commits + 1))' scripts/check_zones.sh
824:    commits=$((commits + 1))
825-    an_regress="$(g log -1 --format=%an "$c")"

$ bash fixtures/check_zones/red_predel_git_vyzovov.sh
toy LOW:  K=12 M=60 L=5 -> 133 коммитов judged, git-вызовов=0 (нарушение поймано)
toy HIGH: K=12 M=60 L=35 -> 493 коммитов judged, git-вызовов=0 (нарушение поймано)
дифференциал: C_high(0) - C_low(0) = 0 - граница S = 15
ПРЕДЕЛ ДЕРЖИТСЯ: LOW=0<=452 (грубо) И дифференциал 0<=15 (решающе)
rc=0   (96.54 s)
```

Полный возврат к O(коммитов) — то самое Н-126, ради которого заведён контракт, —
проходит решающий критерий с ТЕМ ЖЕ выводом байт-в-байт. Регрессия реальна и
измерима (96.54с против 58.91с на тех же toy, +64% на одном добавленном
per-commit вызове) — и невидима барьеру. Барьер не различает предмет контракта
от его полного отсутствия.

**Контрольный контраст, та же техника рядом.** `fixtures/check_protected/
red_predel_git_vyzovov_ours.sh` использует тот же `run_trace`-приём, и он ЖИВ,
потому что `check_protected.sh` re-exec не делает:

```
$ bash fixtures/check_protected/red_predel_git_vyzovov_ours.sh
п.Б держится НА ОБОИХ деревьях (M=60 и M=360): ours-пропажа И дубликат-блоб-пропажа пойманы
git-подпроцессов: LOW(M=60)=29, HIGH(M=360)=29
абсолютная граница (грубая, на LOW): 29 - граница: 30
дифференциал: C_high(29) - C_low(29) = 0 - граница S = 15 (Δ=300, Δ>=2S: 300>=30)
риск 1/2/3 подтверждены
ПРЕДЕЛ ДЕРЖИТСЯ: LOW=29<=30 (грубо) И дифференциал 0<=15 (решающе)
rc=0   (4.08 s)
```

29 против 0 — это не калибровка, это разница между живой мерой и мёртвой.

**Класс находки.** Это не новый дефект, найденный ревьюером первым: architect
записал его САМ — `NABLIUDENIA_ARCHITECT.md`, А-232, статус **ОТКРЫТО**, с
честным диагнозом («мера пуста, а не подтверждает нуль вызовов») и честной
ценой («потенциально дорого для БУДУЩЕГО круга, если тот доверится „ПРЕДЕЛ
ДЕРЖИТСЯ: 0<=452“ как живому доказательству batching-дисциплины»). Запись
датируется кругом 7 (`4bccfba`) и отложена как «вне предмета круга 7» — это
было верно для круга 7. Для круга **done** это не отсрочка: §Приёмка — гейт
контракта, а Р1 в нём назван РЕШАЮЩИМ. Кроме того §Инварианты п.8 делает эту
же фикстуру ПОСТОЯННЫМ CI-стражем, и она им стала (`ci.yml:333`, ключ
`check:zones-call-budget`) — то есть в CI сейчас висит страж, зелёный по
построению. Это ровно тот класс, который арбитраж 040-I отклонил своими же
словами в §Б3: «постоянный страж, зеленеющий от недоступности сети, хуже
отсутствующего».

**Что закрывает.** Мера, переживающая `env -i` (А-232 называет направление:
трассировка СНАРУЖИ субъекта — временная подмена канонического `git` на
PATH-уровне вне процесса, либо любой иной канал, не переносимый переменными
окружения), плюс обязательное предъявление КРАСНОГО: тот же мутант
«per-commit вернулся» обязан давать rc=1. Правка `red_predel_git_vyzovov.sh` —
зона architect и требует нового architect-круга; она же тянет за собой Б2.

---

## Б2 (БЛОКЕР). Машинный пункт приёмки Р8 на HEAD КРАСЕН — запиненное пространство имён architect нарушено, а пин внутри замороженного текста

`§Приёмка Р8` — дословная команда с ожидаемым `rc=0`. Моя мера на HEAD:

```
$ git diff --exit-code 1b67f790fd922b2248622a289ec29ad7889826de HEAD --stat -- \
    ':(glob)fixtures/check_zones/red_*' ':(glob)fixtures/check_zones/probe_*' \
    ':(glob)fixtures/check_zones/stab_*' ':(glob)fixtures/check_zones/_*' \
    ':(glob)fixtures/check_protected/red_*' ':(glob)fixtures/check_protected/probe_*' \
    ':(glob)fixtures/check_protected/stab_*' ':(glob)fixtures/check_protected/_*'
 fixtures/check_zones/red_predel_git_vyzovov.sh | 37 ++++++++++++--------------
 1 file changed, 17 insertions(+), 20 deletions(-)
rc=1
```

Правка внесена `fac8fe6` (architect, круг v4): `run_spycount` (PATH-spy) заменён
на `run_trace` (`SHELLOPTS=xtrace`), потому что обязательный по арбитражу 040-II
доверенный PATH убил прежний канал измерения. Мотив законный и вынужденный —
но §Инварианты п.11 и §Зоны говорят дословно: «правка или добавление в них
легальны ТОЛЬКО новым architect-кругом, **обновляющим и сам пин Р8**». Пин не
обновлён и обновлён быть не может: он — литерал внутри ЗАМОРОЖЕННОГО текста,
защищённого `check:contract-frozen` (эту связку контракт называет сам, §Инварианты
п.11, четвёртый буллит). То есть Р8 останется красным при любом дальнейшем
развитии — гейт приёмки контракта содержит пункт, который нельзя удовлетворить
без новой версии и повторной заморозки.

**Диспенсации не было.** Я сверил все три арбитража целиком. 040-II §5 санкционирует
правку ДВУХ spy-фикстур `case_batch_avtorov_otkaz.sh` / `case_batch_avtorov_obrezka.sh`
и прямо квалифицирует их как «пост-пиновые (`f0b6de6`-пин Р3/Р4 их не держит), правка
легальна» — то есть арбитр про пин ДУМАЛ и назвал ровно те файлы, которых пин НЕ
касается. `red_predel_git_vyzovov.sh` в санкцию не входит и пином Р8 держится.
Единственное место, где отклонение признано, — вердикт адверсария круга 4
(`verdicts/adversary/contracts-040-v4-circle.md`): «**Принимаю отклонение от
буквального „не трогать“**». Адверсарий диспенсацию от замороженного пункта
приёмки не выдаёт: замороженный критерий меняется новой версией + заморозкой либо
арбитражем, и ничем иным. Находка засчитывается ОБЕИМ сторонам как пропущенная:
ни одна из них не вынесла её в канал, который мог бы её разрешить.

**Что закрывает** (выбор — владельца, не судьи): либо новый architect-круг + v4
контракта с обновлённым пином и повторной заморозкой, либо арбитраж, прямо
санкционирующий дельту `fac8fe6` в пространстве `red_*` и объявляющий Р8
удовлетворённым от нового пина. Оба пути закрывают и Б1 тем же ходом — Б1
всё равно требует правки этого же файла.

---

## О1 (замечание, требует действия перед done). «Условие done» контракта не исполнено: записи о живом CI-наблюдении в `HANDOFF.md` нет

Контракт заканчивается разделом «Условие done — ОДНОРАЗОВОЕ живое наблюдение CI»
(РЕШЕНИЕ арбитража 040-I §Б3 часть 3) и объявляет дословно: «Отсутствие этой
записи в `HANDOFF.md` — самостоятельный блокер `done`, РАВНОЗНАЧНЫЙ красной
команде §Приёмка: `done_contract.sh`/ревьюер обязаны требовать её явно».
Требую явно — записи нет. Моя мера:

```
$ grep -n 'actions/runs' HANDOFF.md NABLIUDENIA.md
NABLIUDENIA.md:1255:2026-08-31T18:27:44Z; проверено 2026-09-01 API `actions/runs?head_sha=` и
$ grep -n -i 'call-budget' HANDOFF.md
410:P1/P3/P8 не ослаблены (141/141 call-budget не вырос). **Adversary040v2 диспетчен, в полёте.**
610:наблюдение зелёного CI на реальном origin-прогоне НОВЫХ call-budget-шагов, SHA+ссылка — в
```

Единственное попадание в `HANDOFF.md:610` — формулировка самой ОБЯЗАННОСТИ
(«ДО/на шаге done 040 (ручной, не-фикстурный шаг)»), не её исполнение: ни SHA
прогона, ни ссылки `https://github.com/.../actions/runs/<ID>` в файле нет.
Вынесено замечанием, а не третьим блокером, только потому, что это работа
оркестратора на ФИНАЛЬНОЙ пачке, а пачка после закрытия Б1/Б2 будет другой —
наблюдение всё равно придётся брать заново. Но перед `done` оно обязательно и
не факультативно.

Побочно: наблюдение, взятое сейчас, подтвердило бы зелёным шаг
`check:zones-call-budget`, который по Б1 зелен вакуумно. Порядок обязан быть
обратным: сначала живая мера, потом наблюдение.

## О2 (замечание). Триггер §A1-эскалация фактически СРАБОТАЛ и нигде не зафиксирован

Контракт: «Триггер: смоук-тест implementer'а (после A2, живой прогон `time bash
scripts/check_zones.sh .` на реальном дереве) показывает **>40 секунд**. Только
тогда A1 … переходит из „зафиксировано“ в „предлагается к реализации“ —
ОТДЕЛЬНЫМ контрактом/кругом». Моя мера на `main`, дважды:

```
$ time bash scripts/check_zones.sh .
... замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1876 · проверено по зонам: 1258
real 1m4.387s      rc=0

$ time timeout 60 bash scripts/check_zones.sh . | tail -2
... коммитов в диапазонах: 1876 · проверено по зонам: 1258
real 0m57.924s     rc=0 (PIPESTATUS[0])
```

57.9с и 64.4с — обе выше 40с с большим запасом; это не пограничный случай
§Риски-5 («если A2 даёт, например, 39с, триггер формально не сработал»). Адверсарий
круга 8 намерил 44.43с — то есть триггер был перейдён уже тогда и не назван
ни одной стороной. Действие по контракту — не правка 040, а письменное
предложение A1 отдельным контрактом; здесь фиксирую факт срабатывания.

Смежно: Р7 (`timeout 60`) на этой машине держится с запасом 2.1 секунды на
дереве из 1876 коммитов — прямое закрытие Н-126 куплено, но его срок конечен и
виден невооружённым глазом. Это же наблюдение родственно Н-135 (`check_charter.sh`,
тот же класс).

## О3 (замечание, без действия). Зона контракта включает `scripts/verify_ci_parity.sh`, и он контрактом 040 не тронут — это правильно

§Зоны объявляет `.github/workflows/ci.yml`, `package.json`,
`scripts/verify_ci_parity.sh`. Первые два тронуты 040 по делу (`b4c6e50`,
implementer — два прямых шага и два npm-ключа). `verify_ci_parity.sh` за всю
историю контракта 040 не трогался ни разу: его правки (`f202891`, `5a72260`,
`2bcfcd3`) принадлежат контракту 041. Это ровно то, что контракт и предписывал
(«только если несёт собственный перечень ключей, который новые два ключа не
поглощают автоматически»), и совпадает с прямым запретом арбитража 040-I §Б3
часть 2 п.2: «`verify_ci_parity.sh` не правится ни на байт». Наблюдение, не
дефект.

---

## Что проверено и держится (сырой вывод, моя мера)

### 1. Замороженный текст не тронут

```
$ git ls-tree 'frozen/contracts/040/1^{commit}' -- contracts/040-batching-git-vyzovov.md
100644 blob fe69e9829fa973640e88040cf51a5ef072f632d8	contracts/040-batching-git-vyzovov.md
$ git ls-tree HEAD -- contracts/040-batching-git-vyzovov.md
100644 blob fe69e9829fa973640e88040cf51a5ef072f632d8	contracts/040-batching-git-vyzovov.md
$ git diff --stat 'frozen/contracts/040/1^{commit}' HEAD -- contracts/040-batching-git-vyzovov.md ; echo rc=$?
rc=0
```

Блоб идентичен, 105 коммитов после заморозки. Последняя правка текста
(`b465471`) — предок коммита заморозки `44e97b73` (`git merge-base --is-ancestor`
→ YES), то есть пост-фризовых правок нет вовсе. Реестр: одна строка
`040 → dd627f8e` (тег-объект), без перезаписей.

### 2. Область правки — внутри зон

```
$ bash scripts/check_zones.sh .
замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1876 · проверено по зонам: 1258
rc=0   (1m4.387s)
```

Живой прогон на реальной истории, 1258 судимых коммитов — ни одного выхода за
зону. Отдельно: агрегатный дифф `44e97b73..HEAD` по CI-зоне 040 показывает, что
`.github/workflows/ci.yml` и `package.json` тронуты в том числе коммитом 040
(`b4c6e50`, implementer) — зона это разрешает прямо.

### 3. Атомарность и авторство судебных коммитов

Каждый из 12 судебных файлов — ровно ОДИН коммит, автор соответствует роли,
перезаписей нет:

```
verdicts/adversary/contracts-040-oracle-a2-diff-tree.md    :: 62fe511/adversary
verdicts/adversary/contracts-040-oracle-a2-diff-tree-v2.md :: 16f8f0f/adversary
verdicts/adversary/contracts-040-oracle-a2-diff-tree-v3.md :: d5f3cc3/adversary
verdicts/adversary/contracts-040-v4-circle.md              :: 8e6123c/adversary
verdicts/adversary/contracts-040-v5-circle.md              :: d46a615/adversary
verdicts/adversary/contracts-040-v6-circle.md              :: 837cf42/adversary
verdicts/adversary/contracts-040-v7-circle.md              :: 6702d73/adversary
verdicts/adversary/contracts-040-v8-circle.md              :: 3fb4c84/adversary
verdicts/adversary/contracts-040-v9-circle.md              :: 033ec64/adversary
verdicts/arbitration/contracts-040-batching-kriterii-i-tehnika.md    :: 14f3a96/arbiter
verdicts/arbitration/contracts-040-batching-dostatochnost-oraculu.md :: 7c66a70/arbiter
verdicts/arbitration/contracts-040-vrazhdebnoe-okruzhenie-predel.md  :: a0838bd/arbiter
```

Ни один судья не коммитил под именем автора работы; ни один вердикт не
перезаписан после подачи. Переименование арбитражных файлов (`14f3a96`, блокер
Н-129 `check_ids.sh`) содержание не тронуло — сверено блоб-тождеством:
`5c24017:verdicts/arbitration/040-batching-kriterii-i-tehnika.md` и
`HEAD:verdicts/arbitration/contracts-040-batching-kriterii-i-tehnika.md` — оба
`554ebf05e31dd2f333079aea60387b3b9f1efd10`. Мелочь без последствий: вердикт
круга 4 присутствует двумя коммитами с одним сообщением (`ace5b92` на
`fac8fe6`, `8e6123c` на `ead8180`) — переподача с ветки на main, оба под
`adversary`, содержание на main одно.

### 4. Проверка не переписана под реализацию

`case_*`-пространства обеих семей — Р3/Р4 зелены, то есть НИ ОДИН существующий
case-файл не правился, не удалялся и не переименовывался:

```
$ git diff --exit-code --diff-filter=MD --no-renames f0b6de66 HEAD -- ':(glob)fixtures/check_zones/case_*'     ; echo rc=$?   → rc=0
$ git diff --exit-code --diff-filter=MD --no-renames f0b6de66 HEAD -- ':(glob)fixtures/check_protected/case_*' ; echo rc=$?   → rc=0
```

Счёт моей мерой (`git ls-tree -r --name-only`): `fixtures/check_zones/case_*`
21 на заморозке → 29 на HEAD; `fixtures/check_protected/case_*` 20 → 25. Рост —
только ДОБАВЛЕНИЕМ, что §Инварианты п.7 разрешает прямо. 29 совпадает с
«фикстур: 29» раннера, то есть счётное утверждение круга 9 подтверждено
независимой командой, а не повтором той же. Единственное нарушение пин-дисциплины
— `red_*`-пространство, вынесено в Б2.

### 5. Живые прогоны (все — моя мера, на HEAD `033ec64`)

```
$ bash scripts/verify_antiplacebo.sh --scope check_zones
барьеров: 1 · фикстур: 29 · предъявлено красным повторным прогоном: 29
rc=0   (55.08 s)

$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_zones
БАТАРЕЯ check_zones: класс delimiter-collision      — закрыт (rc 0)
БАТАРЕЯ check_zones: класс regex-injection          — закрыт (rc 0)
БАТАРЕЯ check_zones: класс silent-drop              — закрыт (rc 0)
БАТАРЕЯ check_zones: класс self-application-green   — закрыт (rc 0)
БАТАРЕЯ check_zones: итог 4/4 классов закрыто
rc=0   (102.33 s)

$ bash scripts/check_zones.sh .                     → rc=0   (1m4.387s)   [Р5]
$ timeout 60 bash scripts/check_zones.sh .          → rc=0   (57.924s)    [Р7]
$ bash scripts/check_protected.sh .                 → rc=0   (3.472s)     [Р6]
   коммитов пройдено: 2082 · существовало: 432 · на HEAD: 399 · исчезло: 0 · с разрешения: 24 · перенесено: 9
$ bash fixtures/check_protected/red_predel_git_vyzovov_ours.sh → rc=0     [Р2]
$ bash fixtures/check_zones/probe_ci_dostizhimost_predelov.sh .            [Р9]
   CI-ДОСТИЖИМОСТЬ ДЕРЖИТСЯ (СТРУКТУРНО, fail-closed): check:zones-call-budget -> джоба ci,
   rc(npm)=0=rc(прямой)=0; check:protected-call-budget -> джоба ci, rc(npm)=0=rc(прямой)=0;
   безусловный триггер push/pull_request; verify_ci_parity rc=0
   rc=0
$ bash scripts/verify_ci_parity.sh
   workflow-команд: 42 · скриптов в приёмке: 54 · объявленных исключений: 23 · matrix-ключей: 44 ·
   анти-плацебо-запусков: 2 · расхождений: 0
   rc=0
```

`check_protected` 23.538с (замер frontier до контракта) → **3.472с** — предмет
Н-126 на этой стороне куплен целиком и с запасом. CI-проводка честная: оба
ключа существуют (`package.json:58-59`), оба подключены ОТДЕЛЬНЫМИ ПРЯМЫМИ
шагами джобы `ci` (`ci.yml:328-339`), без `if:`, без `continue-on-error`, без
операторов оболочки — ровно форма, предписанная §Б3 частью 1.

### 6. ПРОВОДКА (норма 038/Н-116)

```
$ bash scripts/check_provodka.sh . contracts/040-batching-git-vyzovov.md ; echo rc=$?
rc=0
```

Поле несёт два guard-канала (`scripts/check_zones.sh`, `scripts/check_protected.sh`)
и маркер `ПРОВОДКА-ЭНФОРСМЕНТ` с полным обоснованием вне поля. Обоснование
guard-only честное и по целям поля: предмет — инвариант производительности и
корректности двух существующих артефактов `scripts/`, не поведение роли;
`roles/implementer.md` несёт только ОБЩУЮ норму красной проверки, и контракт
прямо отказывается пинить общую норму под видом предметной. Оба guard'а —
барьеры ИМЕННО этого предмета (их call-count и семантика и есть норма), не
произвольные скрипты дерева. Это не 029-класс поведенческой нормы. Канал
принят.

### 7. Граница арбитража 040-III соблюдена всеми последующими кругами

Арбитраж `a0838bd` вынес класс «враждебное ОКРУЖЕНИЕ ВЫЗОВА» за границу кругов
и сузил категорию (i), изъяв из неё `BASH_ENV`/`ENV`, импортированные (голые и
slash) функции, `set --`, ловушки, `shopt`, `SHELLOPTS`, сорсинг. Прочитал все
три последующих вердикта целиком — ни один класс не переоткрыт:

- **круг 7** (`6702d73`): «Ни один опыт не использует `BASH_ENV`, `ENV`,
  импортированные/slash-функции, `set --`, ловушки, `shopt`, `SHELLOPTS`,
  сорсинг или подмену процесса барьера». Обе находки — (ii)+(iii): UTF-8/LF имя
  и легальный 40-hex top-level путь на ЧЕСТНОМ git.
- **круг 8** (`3fb4c84`): та же оговорка дословно; находка — путь, равный SHA
  РАНЕЕ поглощённого коммита, ложный красный на честном git, (ii)+(iii).
- **круг 9** (`033ec64`): «Векторы `BASH_ENV`/PATH-spy и иная отрава
  вызывающего намеренно не повторялись: арбитраж вывел их из предмета».

Ни один из трёх не пытался засчитать вектор из таблицы A-K арбитража, что тот
запретил прямо (п.3 «Границы следующего круга»). Граница держится.

Предписание арбитража 040-III п.1 («форма прецедента ДОСЛОВНО») исполнено
байт-в-байт — сверено моей мерой, не чтением:

```
$ diff <(sed -n '123,126p' scripts/check_provodka.sh) <(sed -n '202,205p' scripts/check_zones.sh)
$ sed -n '123,126p' scripts/check_provodka.sh | md5sum   → c44d39468404f8b79360e6b66887be8f
$ sed -n '202,205p' scripts/check_zones.sh    | md5sum   → c44d39468404f8b79360e6b66887be8f
BYTE_IDENTICAL=yes
```

Предписание п.2 (замена ложного утверждения шапки правдой + четвёртый
остаточный риск + ссылка на файл арбитража) исполнено: `check_zones.sh:164-175`
несёт измеренную правду про slash-функцию через `BASH_ENV`, прямую ссылку
`verdicts/arbitration/contracts-040-vrazhdebnoe-okruzhenie-predel.md, векторы
D/E` и отсылку к ЧЕТВЁРТОМУ ОСТАТОЧНОМУ РИСКУ. Предписание п.3 («ничего сверх»)
соблюдено: в блоке нет ни argv-sentinel, ни второго детектора, ни проверки
`BASH_ENV` изнутри, ни `unset -f`-списков.

### 8. Ход спора — 9 кругов прочитаны, эпициклы разорваны сменой вопроса, не правкой

Круги 1→3 (`62fe511`/`16f8f0f`/`d5f3cc3`): rc-отказ → полнота author-потока →
подлинность полей + усечение `diff-tree`; третий круг сам назвал арбитраж, а не
четвёртую правку. Арбитраж 040-II расщепил класс на доставку (структурная
сверка + `--always`) и подлинность (граница доверия — пин PATH) и объявил
подкласс 2 незакрываемым в арифметике вывода. Круги 4→6 — три подряд
пост-арбитражных FAIL на одном векторе (функция `git`, функция `unset`,
`BASH_ENV` slash-функция); арбитраж 040-III признал класс структурно
незакрываемым (вектор F: `BASH_ENV` с `exit 0` кладёт ВСЕ три формы, включая
кандидат самого арбитра) и вынес его за границу. Круги 7→9 — уже в суженной
границе: два ложных красных на честных именах, затем ложный красный на
prior-SHA, затем accept после структурного фикса (конечный автомат по
литеральному `:`-байту `--raw`-формата вместо сравнения формы значения). Форма
спора здоровая: каждый эпицикл разорван СМЕНОЙ ВОПРОСА через арбитраж, счёт
сторон в каждом арбитраже записан явно и не в пользу автора (040-I 4/4, 040-II
(б) целиком адверсарию, 040-III «автору не присуждено ничего»). Асимметрия
семейств (арбитр на семействе автора) объявлена в каждом из трёх файлов, и в
040-III арбитр отверг СВОЙ ЖЕ кандидат своим же замером (вектор K) — контроль
уклона предъявлен, а не обещан.

---

## Порог следующего круга

Круг закрывается закрытием Б1 и Б2; оба требуют правки одного файла
(`fixtures/check_zones/red_predel_git_vyzovov.sh`) и оба лежат в зоне architect,
а Б2 дополнительно — в канале владельца (новая версия + заморозка либо
арбитраж; выбор пути не мой). Предмет следующего ревьюерского круга ограничен:
(а) живая мера Р1, предъявленная в ОБЕИХ полярностях — зелёная на честном
батчинге и КРАСНАЯ на мутанте «per-commit вернулся» (мой мутант годится как
вход, он тривиально воспроизводим: один `g log -1 --format=%an "$c"` в цикле
`:824`); (б) статус Р8 — либо rc=0 от обновлённого пина, либо письменная
санкция того канала, который вправе её дать; (в) запись «Условие done» в
`HANDOFF.md` с SHA и ссылкой на прогон (О1), взятая ПОСЛЕ (а). Найденное и
проверенное в этом круге (разделы 1-8) не переоткрывается; несогласие с Б1/Б2
предъявляется новым основанием, а не повтором той же меры.

Инженерная часть предмета — batched-техника `diff-tree --stdin` с
`--root -m --no-renames`, конечный автомат разбора, доверенный PATH, форма
прецедента re-exec, семантика окон — проверена и держится. FAIL — про
измерительный контур и про машинный пункт приёмки, не про технику.
