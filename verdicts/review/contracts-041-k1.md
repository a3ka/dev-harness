FAIL

# Ревью контракта 041 — круг 1 (первый ревьюерский взгляд на предмет целиком)

Предмет: контракт `contracts/041-gigiena-parsjaschih-gardov.md`
(`frozen/contracts/041/1` = `f813b6d`, блоб `33b18e9`), вся его реализация и
все пост-фризовые правки. Судья: reviewer, семейство отлично от автора работы.
Прогон — одноразовый клон `/tmp/dev-harness-verify/rev041` на `fac8fe6`;
предметные блобы сверены с `main` на момент записи (шесть путей —
`contracts/041-*.md`, `scripts/check_threat_model.sh`, оба профиля батареи,
`fixtures/_krasnye_041.sh`, `config/ci_parity_exceptions.txt` — SAME).

Вердикт: **FAIL**, три находки (одна блокирующая, две замечания). Предмет
САМОГО контракта — гард, батарея, додел арбитража — проверен и держится;
находки лежат в пост-фризовой CI-обвязке, которая вывела новый барьер
из-под единственного механического анти-плацебо.

---

## Что проверено и держится (сырой вывод)

### 1. Заморожённый текст не тронут после фриза

```
$ git rev-parse frozen/contracts/041/1:contracts/041-gigiena-parsjaschih-gardov.md
33b18e97f72976b1036cab53676d2453eebaf54f
$ git rev-parse HEAD:contracts/041-gigiena-parsjaschih-gardov.md
33b18e97f72976b1036cab53676d2453eebaf54f
```

Файл контракта менялся ровно двумя коммитами (`dfbb210`, `ec5fddf`), оба —
ДО фриза. Ни один пост-фризовый коммит (`e6a924c`, `ab27450`, `3514786`,
`b9f20bf`, `e27d4db`) текста контракта не касается.

### 2. Область правки каждого 041-коммита

`git show --stat` по всем семи коммитам предмета: все пути — внутри
`ЗОНА architect: roles/architect.md roles/critic.md scripts/check_threat_model.sh
fixtures/check_threat_model/ fixtures/parsing_hygiene_battery/ fixtures/_krasnye_041.sh`
плюс сам файл контракта. Выходов за зону нет. `bash scripts/check_zones.sh .` →
`замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1762 ·
проверено по зонам: 1178`, rc 0.

Регенерация `.omp/agents/{architect,critic}.md` (`5a10077`) — ровно 2 файла,
+12 строк, механический `gen:harness` после норм-строк `dfbb210`. Кроме двух
норм-строк 041 ничего не добавлено.

### 3. Атомарность и авторство

Один коммит — один предмет, каждый называет находку/круг, на который отвечает.
Авторы судебных коммитов различны и соответствуют ролям: `critic` (`29e9f92`,
`d6af5e7`, `1a230c8`), `adversary` (`ed9fc25`, `f89c5e2`, `f721333`),
`arbiter` (`735d67a`), `architect` (правки). Судья на семействе автора нигде
не подменяет автора.

### 4. Проверка НЕ переписана под реализацию

Консолидация вердиктов критика (`1a230c8`, заявлено «byte-identical»)
проверена своей мерой, а не пересказом:

```
$ git rev-parse d6af5e7:verdicts/critic/contracts-041-v2.md 1a230c8:verdicts/critic/contracts-041-v1.md
034c3e529e5eed183e7bd740f7e8073472e07f0f
034c3e529e5eed183e7bd740f7e8073472e07f0f
```

Блобы совпадают — содержание вердикта при перезаписи не изменено. Правки
фикстур (`ec5fddf` case_07, `3514786`/`b9f20bf`/`e27d4db` профили) — ответы на
находки ЧУЖИХ судей, и каждая УСИЛИВАЕТ проверку (хардкод `2` → single-source
`n=6`/`m=5` с ассерцией, собранной из тех же переменных; точечная
path-константа → живой glob). Ослаблений не найдено.

### 5. Живые прогоны (клон, сырой вывод)

```
$ npm run check:threat-model-selftest
check_threat_model/case_01_section_absent_vacuous.sh rc=0
... (10 файлов) ...
итог: 10 файлов, провалов 0
rc=0

$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_threat_model
БАТАРЕЯ check_threat_model: класс delimiter-collision      — закрыт (rc 0)
БАТАРЕЯ check_threat_model: класс regex-injection          — закрыт (rc 0)
БАТАРЕЯ check_threat_model: класс silent-drop              — закрыт (rc 0)
БАТАРЕЯ check_threat_model: класс self-application-green   — закрыт (rc 0)
БАТАРЕЯ check_threat_model: итог 4/4 классов закрыто
rc=0

$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_provodka
... итог 4/4 классов закрыто
rc=0

$ bash scripts/verify_antiplacebo.sh --scope check_zones
барьеров: 1 · фикстур: 24 · предъявлено красным повторным прогоном: 24
rc=0            (регресс-контроль area-scan: probe-only каталог батареи
                 больше не флагается и ничего не сломал)

$ bash scripts/check_provodka.sh . contracts/041-gigiena-parsjaschih-gardov.md   → rc 0
$ bash scripts/check_threat_model.sh . contracts/041-gigiena-parsjaschih-gardov.md
модель угроз: секция валидна (ЗАЩИЩАЕТ 3 буллет(ов), НЕ ЗАЩИЩАЕТ 3 буллет(ов))
rc=0
$ printf 'привет' | LC_ALL=C.UTF-8 tr -dc '[:alnum:]' | wc -c   → 0   (Р0)
```

### 6. ПРОВОДКА контракта 041 (норма 038)

Оба канала — `role=roles/architect.md`, `role=roles/critic.md` — существуют,
норма-строки лежат в файлах ролей дословно (`check_provodka.sh` rc 0 на самом
041). Поведенческая норма идёт role-каналом, guard-only не применён — верная
форма для 029-класса. Возражений нет.

### 7. Додел арбитража (п.2) — предъявлен МОЕЙ мерой, обе полярности

Арбитраж (`735d67a`, §Решение п.2) предписал: множество путей негативного
контроля выводится из живого репозитория, путь в ассерции — из той же
переменной цикла; «против стаба круга 3 „пробитий 1“, против честного
„пробитий 0“; додел обязан воспроизвести обе полярности».

Моя мера — не повтор авторской команды, а свои стабы, написанные после чтения
исходника честного гарда:

(а) `check_provodka`, воспроизведён стаб круга 3 (хардкод РОВНО строки
`contracts/038-provodka-done-gejt.md`, минуя чтение содержимого):

```
БАТАРЕЯ check_provodka: self-application-негатив пробит на пути contracts/038-provodka-done-gejt.md (rc=0)
БАТАРЕЯ check_provodka: класс self-application-green   — ПРОБИТ (rc 1)
БАТАРЕЯ check_provodka: итог 3/4 классов закрыто
rc=1
```

(б) `check_threat_model`, стаб класса «принять по ПАТТЕРНУ пути
`contracts/041-*`, иначе делегировать честному»:

```
БАТАРЕЯ check_threat_model: self-application-негатив пробит на пути contracts/041-gigiena-parsjaschih-gardov.md (rc=0)
БАТАРЕЯ check_threat_model: класс self-application-green   — ПРОБИТ (rc 1)
БАТАРЕЯ check_threat_model: итог 3/4 классов закрыто
rc=1
```

Ровно одно пробитие в каждом случае, на честном гарде — ноль (п.5 выше).
Счётное утверждение арбитража «40 путей» проверено своей мерой, ДРУГОЙ
командой: `git ls-files 'contracts/*.md' | wc -l` → 40 и
`printf '%s\n' contracts/*.md | wc -l` → 40. Совпадает.

Додел исполнен честно, обе полярности воспроизведены независимо.

---

## Находки

### Н1 (БЛОКИРУЮЩАЯ) — новый барьер 041 выведен из-под единственного механического анти-плацебо, а замена доказывает только зелёное

`5a72260` (author `implementer`, ПОСЛЕ фриза) расширил
`SELF_COVERED_KEYS=(verify_antiplacebo)` до
`SELF_COVERED_KEYS=(verify_antiplacebo check_threat_model)`
(`scripts/verify_ci_parity.sh`) и добавил запись в
`config/ci_parity_exceptions.txt`, заменив шардовое анти-плацебо-покрытие
нового барьера отдельным CI-шагом `npm run check:threat-model-selftest`
(= `fixtures/_krasnye_041.sh`).

Что это стоит — измерено, а не рассуждено. Один и тот же стаб (принять по
паттерну пути `contracts/041-*`, иначе делегировать честной реализации):

```
$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_threat_model   → rc 1  (ПРОБИТ, п.7б выше)
$ bash fixtures/_krasnye_041.sh
check_threat_model/case_10_self_application_on_041.sh rc=0
итог: 10 файлов, провалов 0
rc=0                                                                          ← CI-шаг ЗЕЛЁН на том же стабе
```

Механизм, который стаб ловит (батарея), в CI не вызывается НИКЕМ — проверено
двумя разными командами:

```
$ grep -rn 'red_battery|run_battery|parsing_hygiene' --include=*.yml --include=*.json --include=*.sh --include=*.txt . | grep -v '^./fixtures/parsing_hygiene_battery/'
(пусто, rc 1)
$ git grep -n -e run_battery -e red_battery -e parsing_hygiene -- . ':!fixtures/parsing_hygiene_battery/'
(только .omp/agents/architect.md, HANDOFF.md, NABLIUDENIA_ARCHITECT.md — документация, ни одного вызова)
$ grep -n 'check_threat_model|threat-model' .github/workflows/ci.yml
131: - name: Сам-тесты барьера check_threat_model (контракт 041)
144:   run: npm run check:threat-model-selftest
```

Итог класса: на `main` регрессия `check_threat_model.sh` в принятие-по-пути
проходит ВЕСЬ CI. Для любого другого обычного барьера дерева эту дыру
закрывает `verify_antiplacebo` («зелёный контроль есть, повторный прогон
красный» — 24/24 на `check_zones` выше); для нового барьера 041 её теперь не
закрывает ничто.

Почему это находка, а не вкусовщина: прецедент `SELF_COVERED_KEYS` создан
арбитражем `c8aaa67` для РОВНО одного ключа — `verify_antiplacebo`, который
не может шардировать сам себя (само-ссылка). `check_threat_model` — обычный
барьер по собственной шапке (так его называет и комментарий в
`verify_ci_parity.sh`). Расширение само-ссылочного исключения на обычный
барьер — другой класс, и оно принято решением исполнителя CI-задачи, без
арбитража, внутри задачи о механизме. Норма ослаблена не громко, но и не
предъявлена как ослабление: запись в `exceptions.txt` описывает её как
«аналог `verify_antiplacebo`», хотя аналогии нет — там невозможность,
здесь неудобство протокола.

Что закрывает находку (любое из двух; решать владельцу или арбитру, не мне —
судья называет, не проектирует): конверсия `fixtures/check_threat_model/` на
протокол шардового раннера (вся работа — ВНУТРИ `ЗОНА architect` контракта
041), либо явный арбитраж, признающий исключение и называющий его цену.

### Н2 (ЗАМЕЧАНИЕ) — причина исключения названа неполно: она структурная, а не «нет шапки `# ПРИЧИНА:`»

`config/ci_parity_exceptions.txt` и `.github/workflows/ci.yml` объясняют
исключение тем, что 10 case писались «без шапки `# ПРИЧИНА:` шардового
verify_antiplacebo». Проверено экспериментом (шапка добавлена в `case_02`,
прогон, откат):

```
$ bash scripts/verify_antiplacebo.sh --scope check_threat_model/case_02_zashita_absent
FAIL check_threat_model/case_02_zashita_absent.sh: не вызвала барьер через $BARRIER (код фикстуры 0)
расхождений: 1
rc=1
```

Шапка — не причина. Причина в том, что `fixtures/check_threat_model/_toy.sh`
зовёт барьер по ЖЁСТКОМУ пути (`SUBJ="$REPO/scripts/check_threat_model.sh"`),
а не через внедряемый раннером `$BARRIER`: подстановка плацебо невозможна по
построению, и семья фикстур 041 в нынешнем виде структурно недоказуема
анти-плацебо. Это делает Н1 дороже, чем её описание: в `exceptions.txt`
сказано «протокол расходится», фактически — «доказательство красного
невозможно без правки каркаса». Формулировку обязана нести та же запись,
что вводит исключение.

### Н3 (ЗАМЕЧАНИЕ) — заявленное шире сделанного: «закрывает мёртвую проводку»

`ab27450` заявляет дословно: «закрывает мёртвую проводку (батарея
существовала, никем не вызывалась) и CI-блокер „фикстура без барьера“»; то же
повторено в `HANDOFF.md`.

Второе утверждение верно (area-scan зелен — п.5). Первое — нет: после фикса
батарею по-прежнему не вызывает никто (измерения в Н1). Добавленные
`red_battery_check_provodka.sh` / `red_battery_check_threat_model.sh` сами
лежат ВНЕ глоба любого раннера — это зафиксировано самим деревом:
`scripts/drill_exit_marker.sh` дословно «red_* вне case_*-глоба раннера»,
А-82. Их единственный эффект — выполнить предикат `is_probe_only_legal()`
(`verify_antiplacebo.sh`: маркер ∧ ≥1 `red_*.sh` ∧ нет `case_*.sh`), после
чего area-scan каталог ПРОПУСКАЕТ.

Класс probe-only при этом легален (контракт 034, инв. 1), и прецеденты
(`fixtures/qa_kanal_xd/`, `fixtures/workshop_inventory/`,
`fixtures/verify_antiplacebo_probe/`) тоже не имеют вызывающего — проверено.
Поэтому это не ослабление нормы, а неточность отчёта: закрыт был блокер
area-scan, «мёртвая проводка» — нет. Текст самого `.probe-only` несёт ту же
неточность дословно («которую в живой прогон запускают red_battery_*.sh
ниже»); маркер обязан быть правдой — его содержание судится чтением.

---

## Чего в находках НЕТ (названо явно, чтобы молчание не читалось как согласие)

- Предел example-based батареи (стаб, различающий harness от боевого
  окружения) — закрыт арбитражем `735d67a` как именованный остаток, правило
  остановки принято. Новым спором не заводится, в находки не входит.
- Р15 (стаб-демонстрации не коммитятся) — замороженный выбор контракта, не
  претензия. Н1 — про механизм дерева, а не про этот пункт контракта.
- Стиль, изложение, новые сценарии, новые замыслы — не предмет суда.

## Круг

Круг 1 ревьюера. Две находки класса «механизм» (Н1 блокирует, Н2 уточняет её
формулировку) и одна класса «отчёт» (Н3). Адресат Н1/Н2 — не зона architect
041, а пост-фризовая CI-обвязка (`scripts/verify_ci_parity.sh`,
`config/ci_parity_exceptions.txt`, `.github/workflows/ci.yml`); адресат Н3 —
отчёт и текст `.probe-only`.

Чужой код не правлен: судья называет, не чинит. Стабы Н1/Н2/п.7 прогнаны в
одноразовом клоне и откачены (`git status` чист) — не коммитятся.
