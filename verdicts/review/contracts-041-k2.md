accept

# Ревью контракта 041 — круг 2 (проверка закрытия находок круга 1)

Предмет: контракт `contracts/041-gigiena-parsjaschih-gardov.md`
(`frozen/contracts/041/1`, блоб `33b18e9`), его реализация и пост-фризовая
CI-обвязка. Круг 1 — `verdicts/review/contracts-041-k1.md`, FAIL, три находки
(Н1 блокирующая, Н2/Н3 замечания). После круга 1 приземлились три коммита:
`f202891` (architect), `d167040` (merge, orchestrator), `2bcfcd3`
(implementer).

Судья: reviewer, семейство отлично от автора работы. Прогон — одноразовый клон
`/tmp/dev-harness-verify/rev041k2`, склонированный из `origin`
(`ssh://git@github.com/a3ka/dev-harness.git`), HEAD `87966c3`.

Вердикт: **accept**. Блокирующая Н1 круга 1 закрыта — предъявлено МОЕЙ мерой,
а не сообщением коммита. Н2 и Н3 закрыты. Две новые находки класса
«замечание» (Н4, Н5) и два наблюдения (О1, О2) — ни одна не блокирует.

---

## I. Закрытие Н1 (блокирующая) — предъявлено своим стабом, обе полярности

Н1 круга 1: новый барьер `check_threat_model` был выведен из-под единственного
механического анти-плацебо через `SELF_COVERED_KEYS`-исключение, а замена
(CI-шаг `fixtures/_krasnye_041.sh`) была доказано ЗЕЛЕНА на том же стабе, на
котором батарея красна.

Я не повторял команду автора. Мера — СВОЙ стаб, написанный после чтения
исходника честного гарда, того же класса, что стаб круга 1 п.7б: принять по
ПАТТЕРНУ ПУТИ `contracts/041-*` БЕЗ чтения содержимого, на всех прочих путях
делегировать честной реализации (копия честного барьера вынесена в
`/tmp/dev-harness-verify/honest_ctm.sh`, sha256
`52215183857b11ee57b28f10836e79690786bd7d6ab16c7c4dced069a5e9900f`).

Санити стаба — он честен везде, кроме 041:

```
$ bash scripts/check_threat_model.sh . contracts/041-gigiena-parsjaschih-gardov.md
модель угроз: секция валидна (ЗАЩИЩАЕТ 3 буллет(ов), НЕ ЗАЩИЩАЕТ 3 буллет(ов))
rc041=0                                        ← содержимое НЕ читалось вовсе
$ bash scripts/check_threat_model.sh . contracts/038-provodka-done-gejt.md
модель угроз: секция отсутствует (вне применимости барьера; присутствие — cognitive-only критика)
rc038=0                                        ← делегирование честной работает
```

### Зелёная полярность (честный барьер на HEAD)

```
$ bash scripts/verify_antiplacebo.sh --scope check_threat_model
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_threat_model/case_01_section_absent_vacuous.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «модель угроз: список ЗАЩИЩАЕТ: отсутствует»
  ok   check_threat_model/case_02_zashita_absent.sh: … красный кодом 1 — «модель угроз: список ЗАЩИЩАЕТ: отсутствует»
  ok   check_threat_model/case_03_ne_zashita_absent.sh: … «модель угроз: список НЕ ЗАЩИЩАЕТ: отсутствует»
  ok   check_threat_model/case_04_zashita_empty.sh: … «модель угроз: список ЗАЩИЩАЕТ: пуст (нет буллета)»
  ok   check_threat_model/case_05_ne_zashita_empty.sh: … «модель угроз: список НЕ ЗАЩИЩАЕТ: пуст (нет буллета)»
  ok   check_threat_model/case_06_bullet_below_threshold.sh: … «модель угроз: буллет ниже порога формы: мало слов тут»
  ok   check_threat_model/case_07_delimiter_in_bullet.sh: … «модель угроз: буллет ниже порога формы: дефис-в-буллете»
  ok   check_threat_model/case_08_regex_meta_in_bullet.sh: … «модель угроз: буллет ниже порога формы: . * + ? [ ] ( ) ^ $ |»
  ok   check_threat_model/case_09_full_honest_green.sh: … «модель угроз: список ЗАЩИЩАЕТ: пуст (нет буллета)»
  ok   check_threat_model/case_10_self_application_on_041.sh: … «модель угроз: список ЗАЩИЩАЕТ: отсутствует»
  ok   check_threat_model/case_11_self_application_path_pattern_negative.sh: … «модель угроз: список ЗАЩИЩАЕТ: пуст (нет буллета)»

барьеров: 1 · фикстур: 11 · предъявлено красным повторным прогоном: 11
rc=0
```

### Красная полярность (МОЙ стаб установлен на `scripts/check_threat_model.sh`)

```
$ bash scripts/verify_antiplacebo.sh --scope check_threat_model
  ok   check_threat_model/case_01_section_absent_vacuous.sh: …
  … (case_02 … case_10 — ok) …
  FAIL check_threat_model/case_11_self_application_path_pattern_negative.sh: барьер остался зелёным на обманном дереве — красное не предъявлено

барьеров: 1 · фикстур: 11 · предъявлено красным повторным прогоном: 10
расхождений: 1 · прогон оставлен в /tmp/verify_antiplacebo-308f8b15/run-3239429
rc=1
```

**Это и есть доказательство закрытия Н1, а не слова коммита:** барьер 041
СЕЙЧАС действительно шардируется стандартным раннером, и регрессия
«принятие по паттерну пути» ловится ИМЕННО шардовым CI-путём — ровно один
case красен (`case_11`), десять честны, ложных срабатываний нет.

Дополнительно измерено то, что в круге 1 было зелено на стабе, — прямой
CI-шаг. Он теперь тоже ловит:

```
$ bash fixtures/_krasnye_041.sh                      # = npm run check:threat-model-selftest
case_11 (зелёный: self-application на реальном 041): rc 0
ОТКАЗ: case_11 (красный: тот же путь contracts/041-*, форженое содержимое): rc 0 (ожидался 1)
вывод:
модель угроз: секция валидна (ЗАЩИЩАЕТ 3 буллет(ов), НЕ ЗАЩИЩАЕТ 3 буллет(ов))
итог: 11 файлов, провалов 1
rc=1                                    ← в круге 1 на том же классе стаба было rc=0, 10/10
$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_threat_model
БАТАРЕЯ check_threat_model: self-application-негатив пробит на пути contracts/041-gigiena-parsjaschih-gardov.md (rc=0)
БАТАРЕЯ check_threat_model: класс self-application-green   — ПРОБИТ (rc 1)
БАТАРЕЯ check_threat_model: итог 3/4 классов закрыто
rc=1                                    ← контроль: батарея красна, как и в круге 1
```

Три независимых пути красны на одном стабе, тогда как в круге 1 красным был
только один (батарея, никем в CI не вызываемая). Н1 закрыта.

Дерево после стаба восстановлено: `git checkout --` + `cmp` с сохранённой
копией честного барьера — байт-идентичен, `git status --short` пуст. Стаб не
коммитится (Р15).

## II. Закрытие Н2 и Н3

**Н2 (причина исключения названа неверно).** Закрыта устранением носителя:
запись в `config/ci_parity_exceptions.txt` удалена целиком (`2bcfcd3`), а
СТРУКТУРНАЯ причина, которую круг 1 назвал настоящей, устранена в коде и
названа честно в `fixtures/check_threat_model/_toy.sh`: `run_barrier` зовёт
`"$BARRIER"`, а комментарий каркаса говорит дословно «барьер вызывается через
внедряемую `$BARRIER` … — НЕ жёстким `$REPO/scripts/check_threat_model.sh`».
Это ровно тот диагноз, который круг 1 противопоставил ложному «нет шапки
`# ПРИЧИНА:`». Проверено чтением исходника, а не сообщением коммита.

**Н3 (отчёт перезаявил: «закрывает мёртвую проводку»).** Закрыта. Текст
`fixtures/parsing_hygiene_battery/.probe-only` теперь несёт дословно: «`red_battery_*.sh`
САМИ ничем автоматически не вызываются в CI — не мёртвая проводка (probe-only
легален БЕЗ вызывающего по контракту 034, прецеденты `fixtures/qa_kanal_xd/`,
`fixtures/workshop_inventory/`), а осознанный ручной регресс-net». Маркер
перестал утверждать неправду; заодно честно назван предел `case_11` — «Это
НЕЗАВИСИМАЯ реализация одного и того же теста, не общий источник — расхождение
между ними ловит только чтение обоих файлов, не механизм». Возражений нет.

## III. Остальной круг (AGENTS.md: ревьюер перепроверяет ВСЁ)

### 1. `SELF_COVERED_KEYS` — чтением исходника

```
$ grep -n 'SELF_COVERED' scripts/verify_ci_parity.sh
1155:# и один ключ с покрытием отдельным шагом вне matrix (SELF_COVERED_KEYS) — `verify_antiplacebo`
…
1216:SELF_COVERED_KEYS=(verify_antiplacebo)
$ grep -n 'check_threat_model\|threat' scripts/verify_ci_parity.sh      → (пусто, rc 1)
$ grep -n 'threat' config/ci_parity_exceptions.txt                      → (пусто, rc 1)
```

Содержит ТОЛЬКО `verify_antiplacebo`; шапка согласована («один ключ»). Ни
одного остаточного упоминания `check_threat_model` в обоих файлах — в отличие
от stale-класса, которым закончился 038.

### 2. Ключ действительно вшит в шард, и покрытие действительно проверяется

```
$ sed -n '69p' .github/workflows/ci.yml
            keys: verify_ci_parity check_ids check_ceilings check_contract_frozen drill_next_id_race drill_contract_change models_actual check_threat_model
```

Что это покрытие ЖИВОЕ, а не декларация, — предъявлено контрфактом. Ключ снят
из шарда `ap5`, всё остальное не тронуто:

```
# HEAD (SELF_COVERED_KEYS=(verify_antiplacebo)):
  FAIL check_threat_model не покрыт шардингом — ключ не назван ни одним шардом
  … matrix-ключей: 43 · расхождений: 1
rc=1
# тот же снятый ключ + ДО-2bcfcd3 SELF_COVERED_KEYS=(verify_antiplacebo check_threat_model):
  … matrix-ключей: 43 · расхождений: 0
rc=0
```

### 3. Живые прогоны (клон, сырой вывод, rc)

```
$ npm run check:ci-parity
workflow-команд: 42 · скриптов в приёмке: 54 · объявленных исключений: 23 · matrix-ключей: 44 · анти-плацебо-запусков: 2 · расхождений: 0
rc=0

$ bash scripts/check_zones.sh .
замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1861 · проверено по зонам: 1246
rc=0

$ bash fixtures/_krasnye_041.sh                       → итог: 11 файлов, провалов 0 · rc=0   (Р11)
$ bash …/run_battery.sh check_threat_model            → итог 4/4 классов закрыто · rc=0      (Р13)
$ bash …/run_battery.sh check_provodka                → итог 4/4 классов закрыто · rc=0      (Р12)
$ bash scripts/check_provodka.sh . contracts/041-*.md → rc=0                                 (Р14)
$ bash scripts/check_threat_model.sh . contracts/041-*.md
модель угроз: секция валидна (ЗАЩИЩАЕТ 3 буллет(ов), НЕ ЗАЩИЩАЕТ 3 буллет(ов))
rc=0                                                                                          (Р10)
```

### 4. Замороженный текст не тронут

```
$ git rev-parse frozen/contracts/041/1:contracts/041-gigiena-parsjaschih-gardov.md
33b18e97f72976b1036cab53676d2453eebaf54f
$ git rev-parse HEAD:contracts/041-gigiena-parsjaschih-gardov.md
33b18e97f72976b1036cab53676d2453eebaf54f
```

Норма не тронута молча: ни один из трёх пост-фризовых коммитов не правит
нормативный текст — ни контракта, ни `AGENTS.md`, ни `roles/*.md`.

### 5. Проверка НЕ переписана под реализацию — проверено, риск снят мерой

Риск реальный и его надо назвать: `f202891` написан **architect**, то есть
автором самой реализации `check_threat_model.sh`, и переписывает ВСЕ десять
существующих `case_*.sh` плюс каркас `_toy.sh`. Это ровно та конфигурация, в
которой проверка обычно подгоняется под код.

Ослабления не найдено, и это предъявлено, а не заявлено:

- Дискриминирующие входы сохранены ПОБАЙТОВО. Сверка `case_07` (входа,
  усиленного ещё до фриза) до и после конверсии: зелёный буллет
  `- мало-совсем: достаточно длинное продолжение которое нельзя терять при
  разборе` перенесён дословно; к нему ДОБАВЛЕН красный вход, которого не
  было. Было одно утверждение (`accept`), стало два (`accept` + `refuse` с
  дословной причиной).
- Сила семьи измерена внешним судьёй, а не её автором: все 11 фикстур красны
  на подставном барьере (раздел I, зелёная полярность), а на МОЁМ стабе
  семья ловит регрессию. Подгонка под код дала бы обратное.

### 6. Атомарность и авторство

```
f202891  architect     <architect@dev-harness.local>    CI-wiring: шардовый протокол   15 файлов
d167040  orchestrator  <orchestrator@dev-harness.local> merge + разрешение А-229/А-230
2bcfcd3  implementer   <implementer@dev-harness.local>  text-cleanup SELF_COVERED_KEYS  2 файла
```

Один коммит — один предмет; каждый называет находку/круг, на который отвечает
(`f202891` и `2bcfcd3` оба ссылаются на `verdicts/review/contracts-041-k1.md`).
Авторы различны и соответствуют ролям; судья нигде не коммитит под именем
автора. Коллизия нумерации наблюдений (А-229) разрешена явно в `d167040`, а не
затёрта. Возражений по атомарности нет.

### 7. Область правки

`check_zones.sh` (механизм записи) — rc 0 на всём дереве. Предметные пути
`f202891` — `fixtures/check_threat_model/` (12 файлов) и
`fixtures/parsing_hygiene_battery/.probe-only` — внутри `ЗОНА architect`
контракта 041 дословно. Пути `.github/workflows/ci.yml`,
`NABLIUDENIA_ARCHITECT.md` (`f202891`) и `config/ci_parity_exceptions.txt`,
`scripts/verify_ci_parity.sh` (`2bcfcd3`) в списке `§Зоны` 041 не значатся —
это пост-фризовая CI-обвязка, та же территория, которую круг 1 уже признал
легитимным адресатом Н1/Н2 (`5a72260`, implementer, те же два файла), и
которую механизм зон не судит после заморозки. Превышения зоны не
констатирую — иначе я задним числом ввёл бы правило против собственного
прецедента кругом ранее. Остаётся О1 ниже.

### 8. ПРОВОДКА (норма 038) и §Приёмка

Оба канала 041 — `role=roles/architect.md`, `role=roles/critic.md` —
существуют и несут норму-строки дословно; `check_provodka.sh` на самом 041
rc 0. Поведенческая норма (029-класс) идёт role-каналом, guard-only не
применён — верная форма. Пост-фризовые коммиты каналов не касались.
Пункты §Приёмка Р10–Р14 прогнаны живьём выше, все зелёные.

### 9. Счётные утверждения отчётов — СВОЕЙ мерой

`f202891` заявляет «`npm run check:antiplacebo -- --scope <весь шард ap5>` →
rc=0, барьеров 8, фикстур 64, красных 64». Проверено не повтором чужой
команды, а построенной мной из восьми ключей, вычитанных из `ci.yml:69`:

```
$ npm run check:antiplacebo -- --scope verify_ci_parity --scope check_ids --scope check_ceilings \
    --scope check_contract_frozen --scope drill_next_id_race --scope drill_contract_change \
    --scope models_actual --scope check_threat_model
барьеров: 8 · фикстур: 64 · предъявлено красным повторным прогоном: 64
rc=0
```

Совпадает точно. Заявленные «фикстур 11 / красных 11» — тоже (раздел I).

---

## Находки круга 2

### Н4 (ЗАМЕЧАНИЕ) — `ci.yml` описывает состояние, которого больше нет; одна ссылка висячая

`2bcfcd3` снял `SELF_COVERED_KEYS`-исключение и удалил запись из
`config/ci_parity_exceptions.txt`, но два комментария в
`.github/workflows/ci.yml` продолжают описывать удалённое как живое:

- `ci.yml:140-150` — «`SELF_COVERED_KEYS`-исключение для него в
  `scripts/verify_ci_parity.sh` — устаревший follow-up ВНЕ зоны architect …
  **implementer снимает отдельным проходом**» и «Оплата — запись
  `check:threat-model-selftest = <причина>` в `config/ci_parity_exceptions.txt`
  (устаревший текст записи — тот же follow-up)». Проход УЖЕ состоялся
  (`2bcfcd3`): исключения нет, записи нет. Будущее время описывает прошедшее.
- `ci.yml:172` — «…тот же случай, что `check:threat-model-selftest`
  (**см. запись в конце exceptions.txt**)». Ссылка висячая: записи в
  `exceptions.txt` нет вовсе.

```
$ grep -n 'threat' config/ci_parity_exceptions.txt     → (пусто, rc 1)
$ git grep -n 'threat-model-selftest' -- .github/
.github/workflows/ci.yml:148:        # запись `check:threat-model-selftest = <причина>` в
.github/workflows/ci.yml:151:        run: npm run check:threat-model-selftest
.github/workflows/ci.yml:172:        # `check:threat-model-selftest` (см. запись в конце exceptions.txt).
```

Механизм при этом здоров: `npm run check:threat-model-selftest` покрыт
правилом 6 как `npm run <имя>`, `check:ci-parity` rc 0 — отдельная запись и не
нужна. Дефект чисто документарный, поведения не меняет, поэтому замечание, а
не блокирующая. Класс тот же, которым только что закончился 038 (Б1
`verdicts/review/contracts-038-k2.md`: «блок обязан перестать описывать
несуществующий интерфейс») — называю его здесь, пока он стоит одной строкой
правки, а не пятьюдесятью коммитами позже.

### Н5 (ЗАМЕЧАНИЕ, класс «отчёт») — Н3 круга 1 не исчезла как класс, а повторилась инвертированно

Круг 1 назвал Н3: заявленное шире сделанного. Задание кругу 2 — проверить, не
повторяют ли той же ошибки `f202891`/`d167040`/`2bcfcd3`. Ответ: `f202891` и
`d167040` — не повторяют (все их счётные и качественные утверждения сверены
выше и верны; `f202891` честно называет собственный незакрытый остаток
«остаётся неточным текстом до отдельного implementer-прохода»). А `2bcfcd3`
повторяет — в обратную сторону:

> «Два файла, чисто текстовая правка, **поведение НЕ меняется**.»

Неверно. `SELF_COVERED_KEYS` — не текст, а массив, управляющий потоком:
`IS_SELF_COVERED[$k]` даёт `continue` перед проверкой «не покрыт шардингом»
(`verify_ci_parity.sh:1262`, `:1273`). Контрфакт раздела III.2: на ОДНОМ И ТОМ
ЖЕ дереве со снятым из `ap5` ключом до-правочная редакция молчит (rc 0), а
HEAD — ловит (rc 1). Правка не «чисто текстовая»: она ВЗВЕЛА живую проверку.

Направление безопасное (заявлено МЕНЬШЕ сделанного, норма усилена, не
ослаблена), поэтому замечание, а не блокирующая. Но класс — тот же: отчёт не
равен сделанному, и в другой раз то же самое «поведение НЕ меняется» прикроет
правку, которая проверку снимет. Утверждение о поведении обязано опираться на
контрфакт, а не на вид диффа.

## Наблюдения (не находки — названы, чтобы молчание не читалось как согласие)

**О1.** `f202891` проводит границу зоны сам и непоследовательно: его
сообщение объявляет `config/` и `verify_ci_parity.sh` «вне зоны architect» и
оставляет их implementer'у, но тем же коммитом правит
`.github/workflows/ci.yml`, которого в `§Зоны` 041 тоже нет. Линия проведена
не по тексту контракта. Отказом не является (раздел III.7), но если бы
`ci.yml` был чужой активной зоной, эта же логика привела бы к коллизии.

**О2.** `HANDOFF.md:467-470` описывает механизм в снятом состоянии:
«`SELF_COVERED_KEYS` + отдельный CI-шаг … — НЕ вшит в шард (10 case … без
`# ПРИЧИНА:`)». На HEAD — вшит в `ap5`, 11 case с `# ПРИЧИНА:`,
`SELF_COVERED_KEYS` снят. Это журнал чекпойнта #4 (исторический, чужой
документ оркестратора, вне предмета 041), поэтому наблюдение, не находка.

## Чего в находках НЕТ

- Предел example-based батареи и независимость `case_11` от неё — честно
  назван самим `.probe-only` как остаточный риск; арбитраж `735d67a` правило
  остановки уже принял. Новым спором не заводится.
- Р15 (стаб не коммитится) — замороженный выбор контракта; мой стаб прогнан в
  клоне и откачен.
- Стиль, изложение, новые сценарии, новые замыслы — не предмет суда.

## Круг

Круг 2 ревьюера, предмет — контракт 041. Блокирующая находка круга 1 закрыта
и закрытие предъявлено независимой мерой в обеих полярностях; оба замечания
круга 1 закрыты. Новые находки — два замечания (Н4 документарный stale-класс,
Н5 отчёт ≠ сделанное) и два наблюдения; ни одна не блокирует приёмку.

**accept.** Чужой код не правлен: судья называет, не чинит.
