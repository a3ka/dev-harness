FAIL

# Ревью 037 — первый круг (текст `frozen/contracts/037/2`, реализация на `f966955`)

Предмет: `contracts/037-samodostatochnyj-cwd-i-priemnik-task.md`, заморожен
`frozen/contracts/037/2` (tag-object `b09f389`, коммит `d7a51dd`).
Проверочный SSH-клон `ssh://git@github.com/a3ka/dev-harness.git`, HEAD `f966955`
(совпал с основным на момент клонирования). Ни один ревьюер-круг по этому
предмету ранее не проходил: `verdicts/review/contracts-037-*.md` отсутствовали.

Прочитано целиком: контракт (780 строк) и вся история его правок `2e2119e`→`0872189`;
вердикты критика `contracts-037-v1.md`, `contracts-037-v2.md` (плюс снятые
консолидацией `contracts-037-b4-resolution-v1/v2.md` из истории); вердикты
адверсария `contracts-037-m1m2-adversary.md`, `contracts-037-m1m2-recheck.md`;
реализация `.omp/extensions/path-guard.ts`, `scripts/accept_task_commit.sh`,
`fixtures/self_contained_cwd/`, `fixtures/accept_task_commit/`,
`fixtures/parsing_hygiene_battery/profiles/accept_task_commit.sh`.

Все приёмочные критерии Р1–Р8 переснял СВОИМИ прогонами (ниже сырой вывод) —
отчётам не верил. Все зелёные. Тем не менее круг отказной: блокер Р1 ниже — не
о том, что код неверен, а о том, что его верность НИЧЕМ не прижата.

---

## БЛОКЕР Р1 (класс: барьер отсутствует — критерий 4 «Красное предъявлено»)

**Фикс блокера M1 адверсария (`20d821d`, обязательное п.4 `isWithin(realHome,
canonicalActual)`) не прижат НИ ОДНОЙ клеткой ни одной фикстуры.**

Мутационная проба. Откатываю `20d821d` целиком (`git revert --no-commit`, минус
12 строк — ровно то состояние, которое адверсарий назвал блокирующим), фикстура
предмета остаётся ПОЛНОСТЬЮ зелёной:

```
$ git revert --no-commit --no-edit 20d821d && git diff --cached --stat
 .omp/extensions/path-guard.ts | 12 ------------
 1 file changed, 12 deletions(-)

$ bash fixtures/self_contained_cwd/red_self_contained_cwd.sh .
ИТОГ 037 (self-contained-cwd): ветвей 8, красных 0, зелёных 8
fixture rc=0
```

Дыра при этом РЕАЛЬНО открыта. Различающий зонд (мой, `probe_p4.sh`; база
СОЗНАТЕЛЬНО вне `${TMPDIR}/dev-harness-verify` — иначе `pass` даёт скратч-
allowlist 025 и зонд не доказывает ничего, ровно та ловушка, которую адверсарий
назвал в своём вердикте), кандидат удовлетворяет п.2/п.3/п.5/п.6 и лежит ВНЕ
`$HOME`:

```
########## MUTANT A (п.4 откачен = состояние, найденное адверсарием) ##########
control_legit_under_home=pass   (ожидание: pass)
outside_home_bypass=pass   (ожидание М1 п.4: block)
home_itself_bypass=pass   (ожидание М1 п.4/п.6: block)

########## РЕАЛЬНЫЙ КОД (frozen 037/2, дерево чистое) ##########
control_legit_under_home=pass   (ожидание: pass)
outside_home_bypass=block   (ожидание М1 п.4: block)
home_itself_bypass=block   (ожидание М1 п.4/п.6: block)
```

Итог замера — таблица, в которой фикстура слепа к предмету своей защиты:

| состояние кода | control | outside_home | home_itself | Р1 фикстура |
|---|---|---|---|---|
| реальный (`f966955`) | pass | **block** | **block** | 8/8 зелёных, rc 0 |
| `20d821d` откачен | pass | **pass** ← дыра | **pass** ← дыра | 8/8 зелёных, rc 0 |

Код ВЕРЕН — это я подтверждаю. Прижатости нет: барьер даёт ОДИН И ТОТ ЖЕ
зелёный ответ и над исправленным кодом, и над кодом с открытой дырой. Зелёное
без красного не доказывает ничего.

Адверсарий потребовал ИМЕННО эти клетки дословно
(`verdicts/adversary/contracts-037-m1m2-recheck.md`): «Нужен явный fail-closed
отказ сразу после получения `realHome` … **и отдельные различающие проверки:
вне-HOME с валидными п.2, п.3 и п.5; `canonicalActual === HOME` с теми же
прочими валидными условиями**». Он же заранее назвал причину слепоты:
«зелёная п4 существующей фикстуры не различает его, поскольку подаёт
`sessionName`/owner.json так, что п.5 сама отказывает раньше п.4».

Исполнено было только первое (код), второе (клетки) — нет. Исполнитель
`20d821d` тронул РОВНО `path-guard.ts`, ни одной фикстуры:

```
$ git show --stat --format='' 20d821d
 .omp/extensions/path-guard.ts | 12 ++++++++++++
 1 file changed, 12 insertions(+)
```

Требование: две различающие клетки в `fixtures/self_contained_cwd/red_self_contained_cwd.sh`
(architect-зона) — вне-HOME и `canonicalActual === HOME`, обе с ВАЛИДНЫМИ
п.2/п.3/п.5/п.6, обе КРАСНЫЕ при откате `20d821d`. Предъявить мутационным
прогоном, не пересказом.

## БЛОКЕР Р2 (класс: реализация изменена после последнего суда — критерий 7)

**Последний вердикт адверсария по 037 — `fail`, и реализация менялась ПОСЛЕ
него. Ни один адверсарий-круг эту правку не перепроверял.**

Первая строка вердикта — своя мера, не пересказ:

```
$ git cat-file -p HEAD:verdicts/adversary/contracts-037-m1m2-recheck.md | sed -n 1p
fail
```

Хронология (`%cI`, моя мера — не отчёт):

```
70d290d 2026-09-24T15:58:18+02:00 implementer  037 v2: М1 п.6 + М2 4а + argv-фикс RANGE_SHAS
5edeab8 2026-09-24T16:24:23+02:00 adversary    повторно пробить контракт 037 M1+M2   ← fail
5e9a37a 2026-09-24T16:39:28+02:00 critic       037 v2: первый критик-круг — FAIL
20d821d 2026-09-24T16:44:53+02:00 implementer  037 M1: вернуть обязательный п.4     ← ПОСЛЕ
```

`20d821d` — последний коммит в `.omp/extensions/path-guard.ts` вообще (проверено
`git log -- .omp/extensions/path-guard.ts`), лёг через 20 минут после последнего
адверсарий-круга, и адверсарий-круга после него нет.

Сопровождающая формулировка раздачи — «реализация полностью проверена
адверсарием (2 круга)», «реализация НЕ трогалась с последнего adversary-раунда» —
моей мерой опровергнута: `path-guard.ts` получил +12 строк ПОСЛЕ последнего
адверсарий-круга, и тот круг закончился `fail`. Открытым остаётся не код (он
верен — Р1 выше), а ПОРЯДОК: правка защитной ветви гарда не прошла ни машинной
клетки (Р1), ни адверсария.

Требование: после закрытия Р1 — адверсарий-круг по `20d821d` либо явное
владельческое решение, что круг не нужен. Ревьюер этот выбор не делает.

---

## Проверки задания — все три ПРОЙДЕНЫ

**(1) Текст на `frozen/037/2` совпадает с судимым критиком.** Сверено блобом,
не датой и не пересказом:

```
frozen/contracts/037/2       d7a51dd  blob=df46abda954b65ef2b8abe774eabf9e30715f818
33bd665 (критик accept)      33bd665  blob=df46abda954b65ef2b8abe774eabf9e30715f818
```

Блоб тождествен. Критик судил ровно замороженные байты. Ни одной правки текста
после `0872189` (последняя правка контракта, она же родитель судимого блоба) не
было.

**(2) `РАЗРЕШИЛ-ВЛАДЕЛЕЦ` присутствует — в аннотации тега, как и положено.**

```
$ git cat-file -p frozen/contracts/037/2
...
РАЗРЕШИЛ-ВЛАДЕЛЕЦ: владелец, текстом в сессии («даю РАЗРЕШИЛ-ВЛАДЕЛЕЦ по обоим
вопросам», в ответ на батч-запрос подписи для 037 v2 и 043 v2)
```

**(3) Кап кругов НЕ обойдён, и ложного арбитраж-требования нет.** Пересчитал
СВОЕЙ мерой по кап-логике `scripts/freeze_contract.sh:261-297` (глоб
`verdicts/critic/contracts-037-v*.md`, события A/M/R<100, класс — первая строка,
сжатие подряд-одинаковых):

```
179808b  contracts-037-v1.md  [FAIL]
20853ae  contracts-037-v2.md  [FAIL]
579da46  contracts-037-v1.md  [accept]
5e9a37a  contracts-037-v2.md  [FAIL]
33bd665  contracts-037-v2.md  [accept]
```

Сжатие `FAIL,FAIL,accept,FAIL,accept` → **circles = 4**, не 5: кап считает
СМЕНУ ПЕРВОЙ СТРОКИ, а `contracts-037-b4-resolution-v*.md` под глоб `-v*`
не попадают вовсе. Кап (`circles ≥ 3`) СРАБОТАЛ и закрыт законным клапаном (2)
— словом владельца. Клапан (1) был недоступен: ни один титул в
`verdicts/arbitration/` не несёт маркера `(контракт 037)` — единственный
037-файл `037-b4-freeze-authority.md` имеет титул `К ВЛАДЕЛЬЦУ`. Ложного
арбитраж-требования нет; тихого обхода нет. Реестр согласован:
`registry/contracts.tsv:46` = `037 → b09f38908f42aad063c83bba2f136746d3873b09`
= `git rev-parse frozen/contracts/037/2`.

## Сырой вывод — приёмочные критерии, мои прогоны

```
### Р1  bash fixtures/self_contained_cwd/red_self_contained_cwd.sh .
ИТОГ 037 (self-contained-cwd): ветвей 8, красных 0, зелёных 8            rc=0
### Р2  bash fixtures/accept_task_commit/red_accept_task_commit.sh .
ИТОГ 037 (accept_task_commit): ветвей объявлено 7, прогнано 7, красных 0, зелёных 7   rc=0
### Р3  grep -Fq 'accept_task_commit.sh' roles/orchestrator.md            rc=0
### Р4  bash fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh .
ИТОГ 032: ветвей 31, красных 0, зелёных 31                               rc=0
### Р5  red_pin_allowlist rc=0 · red_granica_nepin_pipe_tee rc=0 · red_strazh_vectora_utechki rc=0
### Р6  bash scripts/check_threat_model.sh . contracts/037-*.md
модель угроз: секция валидна (ЗАЩИЩАЕТ 6 буллет(ов), НЕ ЗАЩИЩАЕТ 5 буллет(ов))  rc=0
### Р7  bash scripts/check_provodka.sh . contracts/037-*.md               rc=0
### Р8  bash fixtures/parsing_hygiene_battery/run_battery.sh accept_task_commit
БАТАРЕЯ accept_task_commit: итог 4/4 классов закрыто                     rc=0
```

Все восемь зелёные моей мерой. Р1-фикстура зелена и над сломанным кодом — см.
блокер Р1.

## Наблюдения (не блокеры, класс назван у каждого)

- **О1 (механизм слепоты Р1; класс: замечание).** Клетка `п4` фикстуры —
  плацебо-канарейка ИМЕННО для HOME-условия: она не подаёт `sessionName` вовсе,
  а `dirname($MAINLIKE)` не несёт owner.json, поэтому отказ приходит от п.5
  (владелец-корреляция), а до п.4 исполнение не доходит. Клетка названа
  «КАНАРЕЙКА ДЫРЫ B» и выглядит защитой п.4, но измеряет другой предикат.
- **О2 (атомарность, критерий 5; класс: замечание).** `70d290d` собирает ТРИ
  задачи одним коммитом — дословно в его сообщении: «М1 п.6 (анцестор-проверка)
  + М2 4а (мерж-политика) + М2 argv-фикс RANGE_SHAS … три задачи одним кругом».
  Все три принадлежат §Незаполненным требованиям одного предмета, поэтому это не
  «связка из пяти задач», но откатить п.6 отдельно от мерж-политики нельзя —
  ровно та потеря, которой критерий 5 сопротивляется.
- **О3 (прослеживаемость; класс: замечание).** Замороженный текст 4 раза
  ссылается на `verdicts/critic/contracts-037-b4-resolution-v1.md` как на
  источник БЛОКИРУЮЩЕЙ находки Н1, но этого файла на HEAD нет — он снят
  консолидацией `579da46` по конвенции Н-69. Содержание восстановимо
  (`contracts-037-v1.md` несёт Н1 и разбор `getSessionFile`), файл — из истории
  (`0a11318`), поэтому это не FAIL; но читатель замороженного текста по ссылке
  не попадает никуда.
- **О4 (зоны, критерий 1; класс: чисто).** `scripts/check_zones.sh .` даёт
  rc 1, но НИ ОДНА строка FAIL не упоминает 037 (проверено счётчиком: 0). Оба
  отказа — пути 043 (`implementer 80e1ea84 fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh`,
  `implementer 8766e769 contracts/043-precizionnyj-prefriz-gejt.md`), чужой
  предмет. По 037 строка `ok`. Границы зон предмета соблюдены; `b77e61b`, чей
  заголовок упоминает `verify_antiplacebo` (путь из «РАБОТА НЕ РАЗДАЁТСЯ»),
  фактически тронул только `scripts/accept_task_commit.sh` — в зоне.
- **О5 (критерий 3 «проверка не подгнана под код»; класс: чисто).** Обе красные
  фикстуры предмета (`red_self_contained_cwd.sh`, `red_accept_task_commit.sh`)
  правлены ИСКЛЮЧИТЕЛЬНО architect'ом, и последняя их правка (`19678a0`/`0872189`)
  предшествует обоим коммитам исполнителя — подгонки под реализацию нет.
  Профиль батареи `parsing_hygiene_battery/profiles/accept_task_commit.sh`
  написан исполнителем ТЕМ ЖЕ коммитом, что сам гард (`9c10c29`) — это
  самопроверка, но она прямо предписана 041 §Инварианты п.5 (автор гарда
  поставляет батарею) и контракт раздвоенное авторство обсуждает открыто;
  не вменяю.
- **О6 (критерий 6 «норма не тронута молча»; класс: чисто).** Правки
  `roles/architect.md`/`roles/orchestrator.md` объявлены в §Зонах, снабжены
  одиннадцатью строками ПЕРЕСЕЧЕНИЕ и тремя ПРОВОДКА-каналами; все три
  норма-строки присутствуют побайтово (проверил `grep -c`: 1/1/1), все три —
  role-каналы, guard-only подмены нет. `check_provodka.sh` rc 0. Тихой правки
  нормы не нашёл.
- **О7 (вне зоны 037; класс: наблюдение).** Живьём в СВОЕЙ сессии наблюдал
  ложные срабатывания стража записи на не-путях: `2>&1` разобран как путь
  («путь `/tmp/&1` не в null-allowlist») и `/dev/null` отвергнут как цель
  записи. Это поведение null-allowlist'а 025/032, а не новой ветви 037 (моя
  мутационная проба к нему не прикасалась); architect уже завёл его как А-255
  (`8f73885`). Называю, не вменяю 037.
- **О8 (поправка к учёту; класс: наблюдение).** В раздаче усиление
  Р2-доказательности приписано коммиту `8f73885` — это неверно: `8f73885`
  тронул только `NABLIUDENIA_ARCHITECT.md` (+19 строк, наблюдение А-255).
  Фикстуру `red_accept_task_commit.sh` усилил `0872189`, и он же —
  последняя правка текста контракта.

## Вердикт

**FAIL.** Два блокера: Р1 (фикс блокера M1 адверсария не прижат ни одной
клеткой — доказано мутацией: откат `20d821d` оставляет 8/8 зелёных при реально
открытой дыре) и Р2 (последний адверсарий-круг закончился `fail`, реализация
менялась после него и этой правки никто не судил). Текст, кап, подпись
владельца, зоны, ПРОВОДКА и все восемь приёмочных критериев — чисты.

Вердикт связан с блобом контракта `df46abda954b65ef2b8abe774eabf9e30715f818`
(`frozen/contracts/037/2`) и реализацией на `f966955`; новая редакция ни того,
ни другого принятия не наследует.

Спора со стороной ревьюера здесь нет: оба блокера — о недостающем
предъявлении, не о существе дизайна. Арбитр не требуется.

— reviewer, первый круг
