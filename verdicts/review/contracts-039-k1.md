accept

# Ревью контракта 039 — перенос защищённого артефакта (круг 1, последний гейт перед done)

Судимый HEAD: `1194a7ea4285e57fe0546839de70a0d013acbd9a` (`verdict: adversary 039 round 2`).
Предмет: `contracts/039-check-protected-rename.md`, блоб `bc3a38f124403d3fc3a96cc20c06464257707591`.
Все прогоны — в одноразовом клоне `/tmp/dev-harness-verify/rev039` (checkout `1194a7e`), основное
дерево не трогалось.

Итог: **0 блокирующих находок, 2 замечания, 0 советов**. Гейт пройден; замечания названы ниже
и ни одно из них не о предмете 039.

## Что измерено своей мерой (сырой вывод, rc)

### 1. Живой свидетель барьера на судимом HEAD

```
bash scripts/check_protected.sh
  ok   перенесён, контент жив на HEAD: verdicts/arbitration/036-v4-3-zhivoj-vyzov.md
  ok   перенесён, контент жив на HEAD: verdicts/arbitration/038-dostizhimost-i-zony.md
область: :(literal)decisions/proxy/ :(literal)plans/ :(literal)verdicts/adversary/ :(literal)verdicts/arbitration/ :(literal)verdicts/consultant/ :(literal)verdicts/critic/ :(literal)verdicts/review/ · коммитов пройдено: 1899 · существовало: 358 · на HEAD: 337 · исчезло: 0 · с разрешения: 19 · перенесено: 2
rc=0
```

ok-строка переноса совпадает с §Семантика п.3 ПОБАЙТОВО: `перенесён, контент жив на HEAD: <путь>`.
Остальные 19 путей идут прежней ok-строкой разрешения — ветвь `excuse_for` не тронута.

### 2. КРАСНОЕ ПРЕДЪЯВЛЕНО — тот же барьер ДО фикса, тем же прогоном

Проверка не «зелёная по построению»: откат клона на `d799a72` (последний коммит до реализации
`222a82a`, дерево истории то же) и тот же вызов:

```
git checkout -q d799a72 && bash scripts/check_protected.sh
  FAIL защищённый артефакт существовал и на HEAD его нет: verdicts/arbitration/036-v4-3-zhivoj-vyzov.md
  FAIL защищённый артефакт существовал и на HEAD его нет: verdicts/arbitration/038-dostizhimost-i-zony.md
область: … · исчезло: 2 · с разрешения: 19
rc=1
```

Цвет переворачивает ИМЕННО правка барьера, а не правка проб: ДО — rc 1 на тех же двух путях,
ПОСЛЕ — rc 0 с именованным переносом. Это R3а таблицы §Приёмка в обоих мирах.

### 3. Дрилл зелёной ветви и его собственное красное

```
bash scripts/drill_protected_rename.sh
  ok   перенос внутри области (v-1.md → v-2.md) — перенесён, контент жив на HEAD: verdicts/adversary/v-1.md
  ok   перенос наружу области (plans/001-p.md → notes/001-p.md) — перенесён, контент жив на HEAD: plans/001-p.md
rc=0

bash scripts/verify_antiplacebo.sh --scope drill_protected_rename
  ok   drill_protected_rename/case_perenos_ne_priznan.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «перенос не распознан»
барьеров: 1 · фикстур: 1 · предъявлено красным повторным прогоном: 1
rc=0
```

Оба случая п.5 присутствуют (внутри области и наружу), оба названы ok-строкой из п.3.

### 4. Семья check_protected целиком

```
bash scripts/verify_antiplacebo.sh --scope check_protected
барьеров: 1 · фикстур: 20 · предъявлено красным повторным прогоном: 20
rc=0
```

20/20, включая все четыре пробы красной пачки: `case_pereimenovanie_s_pravkoj` (R4, «блоб другой»),
`case_pereimenovanie_naruzhu` (R5, смерть носителя), `case_perenos_ciklom` (R6, цикл),
`case_kopija_zaranee` (R7, конъюнкция А∧Б — контрпример блокера Б1). Счёт 20 перепроверен
СВОЕЙ мерой, другой командой: `ls fixtures/check_protected/case_*.sh | wc -l` → `20`. Заявленное
в вердикте адверсария к2 «20/20» и «перенесено: 2» подтверждены независимо, не повтором его команд.

### 5. Соседние затронутые барьеры (scoped-регресс, не полный CI)

```
bash scripts/verify_ci_parity.sh
workflow-команд: 38 · скриптов в приёмке: 50 · объявленных исключений: 23 · matrix-ключей: 43 · анти-плацебо-запусков: 2 · расхождений: 0
rc=0

bash scripts/check_zones.sh
замороженных контрактов: 37 · объявленных авторов: 6 · коммитов в диапазонах: 1519 · проверено по зонам: 991
rc=0
```

`verify_ci_parity.sh` не правился этой пачкой и остался зелёным: сумма-инвариант поглотил новый
ключ ровно так, как предсказано §Приёмка. `check_zones.sh` зелен на всём дереве, включая диапазон
039 от первой заморозки.

## Байт-сверка красной пачки с пинами §Приземление

```
sha256sum fixtures/check_protected/case_*.sh
b5f059f72c9a8c2bb017e61c9e07d81b60d94cb265a41072923b150acfac844b  case_pereimenovanie_naruzhu.sh
15c631a9c63eb9b0325fe0e72eaf2f04e11c8c0a91c253f2edead4e66e3b8d91  case_pereimenovanie_s_pravkoj.sh
af31f0a640930c21f3581e3bccccc3b19a4e6438305ee8dff35a16c7d316b5d4  case_perenos_ciklom.sh
fd930692e76cfeb143e70313f7341fbdb77a7bdedb454a19e7dd4b7cb49fbd50  case_kopija_zaranee.sh
```

Все четыре совпали с пинами контракта посимвольно. Расхождения — правки пачки без круга критика —
нет.

## Проверка не подогнана под реализацию

Смотрел в историю, а не в текст вердиктов:

* все четыре пробы красной пачки приземлены ОДНИМ коммитом `8a042da` автора `architect` — ДО
  реализации `222a82a`; sha256 пиняты в замороженном тексте ЕЩЁ РАНЬШЕ (блоб контракта
  `bc3a38f1`, заморозка `frozen/contracts/039/1` → `a2d06ea`);
* ни один коммит `implementer` (`222a82a`, `00b883f`) не трогает `fixtures/check_protected/`;
* единственный файл проверок под рукой реализации — `fixtures/drill_protected_rename/
  case_perenos_ne_priznan.sh`, и это прямо разрешено §Зоны (двойное объявление, union-семантика
  033: спецификация случая — architect, case-файл — реализация);
* `scripts/verify_antiplacebo.sh` и `fixtures/verify_antiplacebo/` этой пачкой не тронуты
  (правка `c7c7f71` в диапазоне принадлежит контракту 038, не 039).

Тождество судимого и замороженного текста проверено блобами, а не пересказом:

```
blob@201fdcc (что судил критик, bc3a38f) = bc3a38f124403d3fc3a96cc20c06464257707591
blob@a2d06ea (цель frozen/contracts/039/1) = bc3a38f124403d3fc3a96cc20c06464257707591
blob@HEAD 1194a7e                          = bc3a38f124403d3fc3a96cc20c06464257707591
```

Заморожен ровно тот текст, который принял критик; пост-фриз правок контракта нет.

## Канонизация вердикта критика — суждение круга 1 не потеряно

```
git log --follow -- verdicts/critic/contracts-039-v1.md
c7faef1 critic 2026-09-21 critic: 039 v2 на канонический путь v1.md — суждение то же, accept
dccde2d critic 2026-09-21 critic: 039 круг 1 — FAIL, приёмка не удерживает добавление q в удаляющем коммите
```

`git show dccde2d:verdicts/critic/contracts-039-v1.md` начинается строкой `FAIL` и несёт блокер Б1
целиком; на HEAD тот же путь начинается строкой `accept` (круг 2). Оба круга читаются по одному
пути, ни одно суждение канонизацией не стёрто. Удаление промежуточного `contracts-039-v2.md`
законно и видно самим барьером: `ok исчез с явного разрешения: verdicts/critic/contracts-039-v2.md`.

## Зоны — каждый коммит внутри зоны своего автора

| коммит | автор | пути | вердикт |
|---|---|---|---|
| 89d214f | architect | `NABLIUDENIA_ARCHITECT.md`, `contracts/039-…md` | в зоне |
| 4e984e6 | architect | `contracts/039-…md` | в зоне |
| dccde2d | critic | `verdicts/critic/contracts-039-v1.md` | в зоне |
| b86b23a | critic | `verdicts/critic/contracts-039-v2.md` | в зоне |
| c7faef1 | critic | `verdicts/critic/contracts-039-v{1,2}.md` | в зоне |
| 8a042da | architect | 4 файла `fixtures/check_protected/` | в зоне |
| 222a82a | implementer | `scripts/check_protected.sh`, `scripts/drill_protected_rename.sh`, `.github/workflows/ci.yml`, `package.json`, `fixtures/drill_protected_rename/case_perenos_ne_priznan.sh` | в зоне |
| 3d88a41 | adversary | `verdicts/adversary/039-symlink-i-path.md` | в зоне |
| 00b883f | implementer | `scripts/check_protected.sh` | в зоне |
| 1194a7e | adversary | `verdicts/adversary/039-mode-path-octopus-round2.md` | в зоне |

Ни одного файла вне объявленных зон. Пять land-коммитов (`e955736`, `201fdcc`, `d799a72`,
`4de2123`, `65e7edb`) по `--stat` несут ровно содержимое своей ветки и ничего сверх — контрабанды
в слияниях нет. Атомарность держится: реализация — один коммит `222a82a`; второй коммит
`00b883f` — отдельный предмет (закрытие FAIL адверсария), и это правильное деление, а не связка.

Нормативные документы (`AGENTS.md`, `ROADMAP.md`, `roles/*.md`) пачкой 039 не тронуты: правка
`AGENTS.md` в диапазоне — `a846ee3`, автор `owner`, устав-дельта контракта 038. Норма молча не
ослаблена.

## Адверсарий круга 2 действительно перепроверил ОБА обхода круга 1

Прочитан текст обоих вердиктов, не только итоговая строка. Круг 1 (`3d88a41`) назвал два обхода:
(1) blob-инвариант принимал dangling symlink `120000` с тем же OID; (2) подмена `comm` через
унаследованный `$PATH` делала любое удаление пустым `missing`. Круг 2 (`1194a7e`) повторяет их
ИМЕННО как повтор: symlink в удаляющем коммите → rc 1 с именованным FAIL; `fakebin/comm` +
`fakebin/git` одновременно → rc 1 с тем же FAIL. Сверх того круг 2 добавил новый слой
(gitlink `160000` с бывшим OID в двух формах, октопус с отсутствующим p у части родителей и
октопус с одним расходящимся родителем) и назвал позитивные контроли зелёными, негативные —
красными. Закрытия в коде видны прямым чтением `00b883f`: `export PATH=/usr/bin:/bin` до первой
внешней команды с именным rc=2 при отсутствии `git`/`comm`; фильтр `$1 ~ /^(100644|100755)$/` и
на множестве блобов HEAD, и на пути q в c; `read -r -a parents` вместо построчного чтения `%P`.

## ПРОВОДКА — канал существует, подключён и это барьер ЭТОГО предмета

`guard=scripts/check_protected.sh`. Файл существует; это ровно тот барьер, который контракт и
правит, а не произвольный вызванный скрипт дерева. Подключение проверено в двух местах:

```
.github/workflows/ci.yml:271:  run: npm run check:protected
package.json:28: "check:protected": "bash scripts/check_protected.sh",
```

Орфаном не является. Новый дрилл подключён отдельной строкой `ci.yml:281` (`npm run
drill:protected-rename`) и ключом `drill_protected_rename` в шарде `ap4` (`ci.yml:67`) — обе
проводки требовались §Приёмка и обе на месте.

Строка `ПРОВОДКА-ЭНФОРСМЕНТ` (guard-only без role-канала) обоснована честно: норма 039 — инвариант
ДЕРЕВА (блоб исчезнувшего защищённого артефакта обязан жить на HEAD либо нести именную
ALLOW-строку), она судится прогоном барьера по истории и поведения ролей не описывает. Это не
029-класс, role-канал здесь был бы фиктивным. Принято.

## Замечания (класс: не блокирует 039)

**З1 — реализация СТРОЖЕ замороженного текста, и текст об этом молчит.** Класс: расхождение
реализации с замороженным контрактом в сторону ужесточения, санкционированное кругом адверсария.
§Семантика п.1, четвёртый подпункт говорит «блоб B присутствует в `git ls-tree -r HEAD` хотя бы
под одним путём — ЛЮБЫМ, внутри или вне области защиты»; реализация после `00b883f` требует
носителя `100644`/`100755` и отбрасывает `120000`/`160000`/`040000` (`scripts/check_protected.sh`,
фильтр множества блобов HEAD и фильтр пути q). Ослаблением это НЕ является — ровно наоборот,
fail-closed, и без него стоит обход 1 адверсария круга 1; сам текст контракта никто молча не
правил (блоб `bc3a38f1` тождествен на всех трёх точках). Но замороженная норма теперь описывает
свою реализацию НЕПОЛНО, а читать её будут по тексту. Лечится преемником или v3 контракта одной
фразой о пригодном носителе — предложение владельцу, не требование этого круга.

**З2 — строка реестра заморозки 039 не закоммичена; основное дерево грязное на гейте.** Класс:
процессный дефект вне предмета 039, адресован владельцу/оркестратору. В рабочем дереве основного
репозитория висит незакоммиченная правка:

```
-039 → d4c5aea80edc78f842e6c0bf518ee48e951f6811   (тег id/CONTRACT/039)
+039 → dc3fdb1022ecb2fe2b2fe0077dce6fbbbcce3551   (тег frozen/contracts/039/1)
```

Это ровно запись §8 `freeze_contract.sh` («реестр заморозок», контракт 036 г5б), и по прецедентам
`56ec133` (036) и `472eb21` (038) перезапись минт-строки — канал ВЛАДЕЛЬЦА: дверь минта 031 судит
только-добавление, а здесь удаление+добавление. Пока строка не закоммичена, факт заморозки 039
живёт только в теге. Риск назван прямо и он не гипотетический: Н-122 — `land_agent` сносит
некоммиченный контент рабочего дерева, и этот же риск уже один раз убил пачку именно этого
контракта. `done_contract.sh` реестр не читает, поэтому done не блокируется, но строку надо
закоммитить каналом владельца до следующего land.

## Область моей правки

Этим вердиктом изменён ровно один путь — `verdicts/review/contracts-039-k1.md`. Чужой код не
правился: находки названы, не исправлены.

Н-111: текст подготовлен вне основного дерева (`/tmp/dev-harness-verify/`), его блоб записан
`git hash-object -w`, внесён ровно этим путём через `git update-index --cacheinfo`, коммит создан
с `user.name=reviewer`, `user.email=reviewer@dev-harness.local`; затем путь materialized только
`git checkout HEAD -- <путь>`. Push не выполнялся. Приёмка-судьи v2 (Н-48): scoped-регресс
затронутых барьеров + сверка неизменности замороженных барьеров по git-diff; полный npm-прогон
не делался — это CI.
