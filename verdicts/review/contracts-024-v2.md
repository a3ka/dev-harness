accept

# Ревью контракта 024 v2 — финальный гейт перед done (детектор утечек основного чекаута)

Судимый HEAD: `39adb18` (`verdict: adversary contract 024 k15`). Все прогоны — в СВОЁМ клоне
`/tmp/dev-harness-verify/rev024v2/run` (тот же SHA, `git status --porcelain` пуст: 0 строк).
Клон сохранён. Правок в чужой код не вносил.

Стенограмма: `bash scripts/check_no_leak.sh --check /home/aka/Documents/dev-harness` → rc=1, `ОТКАЗ: основной чекаут загрязнён: основной чекаут мутировал во время сверки — повторное чтение разошлось с первым` (555 с; разбор — находка Р3, НЕ блокер, НЕ ложное зелёное).

Прочитано целиком: контракт (459 строк, включая правку v6 и приёмку path-1),
`verdicts/adversary/contracts-024-k15.md`, `verdicts/review/contracts-024-v1.md`,
`scripts/check_no_leak.sh` (1215 строк), фикстура `red_rootcut_untracked_024.sh` по коду
целиком, шапки и ассерты `red_dotgit_info_exclude_024.sh`, `red_excludes_target_024.sh`,
`red_symlink_target_content_024.sh`, `red_info_refs_drift_024.sh`, `red_toctou_manifest_024.sh`.

---

## 1. Область правки — соответствует заявленному, лишнего нет

Диапазон `frozen/contracts/024/5..39adb18` — **71 коммит** (мой счёт `git log --format=%h | wc -l`),
из них implementer 18, architect 13. Множество тронутых путей ПО АВТОРУ
(`git log --author=X --format='' --name-only | sort -u`):

```text
=== implementer ===
scripts/check_no_leak.sh
=== architect ===
NABLIUDENIA_ARCHITECT.md
contracts/024-detektor-utechek-osnovnogo-chekauta.md
fixtures/check_judge_gate/canary_vremya_024.sh
fixtures/check_judge_gate/red_excludes_target_024.sh
fixtures/check_judge_gate/red_git_config_env_024.sh
fixtures/check_judge_gate/red_gitignore_selfhide_024.sh
fixtures/check_judge_gate/red_index_flags_024.sh
fixtures/check_judge_gate/red_rootcut_tracked_bytes_024.sh
fixtures/check_judge_gate/red_rootcut_untracked_024.sh
fixtures/check_judge_gate/red_separate_gitdir_024.sh
fixtures/check_judge_gate/red_symlink_target_content_024.sh
fixtures/check_judge_gate/red_toctou_manifest_024.sh
=== adversary ===  verdicts/adversary/contracts-024-{k8..k15}.md
=== critic ===     verdicts/critic/contracts-024-v6.md
=== arbiter ===    verdicts/arbitration/orakul-podsmotrennaya-konstanta-024.md
=== orchestrator === HANDOFF.md NABLIUDENIA.md
```

Зона implementer — РОВНО `scripts/check_no_leak.sh` за все 18 коммитов, ни одного выхода.
Зона architect — ровно объявленная тройка (контракт + `fixtures/check_judge_gate/` +
NABLIUDENIA_ARCHITECT.md). **Нормативные документы (`AGENTS.md`, `roles/**`) в окне v5→HEAD
НЕ тронуты никем** — проверено тем же перечислением путей: ни одного файла под `roles/`.
Замороженные барьеры (`check_judge_gate.sh`, `judge_gate.sh`, `verify_antiplacebo.sh`,
`spawn_agent.sh`, `.githooks/`) в диапазоне не появляются. Пункт «норма не тронута молча» —
чист; пункт «превышение области» — чист.

Атомарность: каждый фикс-круг к8–к15 — отдельный коммит implementer'а со ссылкой на
вердикт-предмет в теле (`1e279d9` → к14, `fc771ff` → к13, `6c67c4d` → к12, `c46c65a` → к11,
`ffd6a7f` → к10, `6a94f76` → к9, `5a188af` → к8). Откатывается по частям, читается.

## 2. Сырой вывод — мои прогоны, не пересказ

**Все 15 `red_*.sh` (мой счёт `ls | wc -l` = 15), последовательно, rc печатался после каждой:**

```text
RC=0 red_detektor_utechek.sh          (18/18 ворот)
RC=0 red_dotgit_info_exclude_024.sh   (3 контроля)
RC=0 red_excludes_target_024.sh       (3)
RC=0 red_git_config_env_024.sh        (4)
RC=0 red_gitignore_selfhide_024.sh    (3)
RC=0 red_index_flags_024.sh           (3)
RC=0 red_info_refs_drift_024.sh       (3)
RC=0 red_norma_stroka_024.sh          («норма-строка 024 приземлена дословно (grep -F, побайтово)»)
RC=0 red_path_fake_utilit_024.sh      (15 ветвей подмены + честный контроль)
RC=0 red_rootcut_tracked_bytes_024.sh (8 контролей, вход случаен)
RC=0 red_rootcut_untracked_024.sh     (6 контролей, вход случаен)
RC=0 red_separate_gitdir_024.sh       (4)
RC=0 red_stenogrammy_sudej_024.sh     (граница 17583d00…, все вердикты несут строку с rc)
RC=0 red_symlink_target_content_024.sh(4)
RC=0 red_toctou_manifest_024.sh       (5/5 попыток гонки)
```
(95,5 с на всю пачку; имена утечек в выводе разные на каждом контроле — входы случайны,
константный dispatch по именам не собирается.)

**Проба слабых форм** `probe_slabyh_detektora.sh` → **rc 0**:
```text
  ok   честная форма: 18 ворот зелены
  ok   всегда-чисто: ворота 1 … ok   константа-тире: ворота 18, причина «константа '-'»
  ok   плацебо exit-1 отвергнуто: смерть без диагностики фазы не удовлетворяет
пойманы по имени ворота и причине: 17 слабых форм; честная форма проходит все 18 ворот
```
Это и есть предъявленное КРАСНОЕ: у каждого из 18 ворот есть прогон, где оно красное против
сломанной реализации (18 stab-файлов в каталоге = 17 слабых + 1 честный — мой счёт сходится).

**Канарейки и охрана:**
```text
RC=0 canary_zhivoj_024.sh <мой клон>   (снимок → сверка → повторная сверка, все rc 0)
RC=0 verify_antiplacebo.sh . --scope check_judge_gate  (барьеров 1 · фикстур 3 · красным предъявлено 3)
```

**Чистый клон, снимок/сверка своими руками:**
```text
rc_snapshot_clone=0
основной чекаут чист
rc_check_clone=0
```

**Перф-канарейка** `canary_vremya_024.sh` — 4 прогона, см. находку Р2.

## 3. Проверки НЕ подогнаны под реализацию

- **Авторство.** Ни одного коммита implementer'а в `fixtures/` за весь диапазон v5→HEAD
  (раздел 1: множество путей implementer'а = один файл). Фикстуры пишет architect,
  реализацию — implementer; разделение держится измеренно, не на слово.
- **Ассерты судят ПРЕДМЕТ.** `red_rootcut_untracked_024.sh` прочитан по коду целиком:
  оракул вычисляется ПРОБОЙ до вызова субъекта (пин `sha256sum` из TRUSTED_PATH +
  sanity-хэш пустого ввода), имя и байты входа берутся из `/dev/urandom` (≥64 бит на прогон),
  сверка двухсостоянная (A → снимок → строка обязана нести sha(A); B≠A → снимок → sha(B)
  обязан появиться, sha(A) исчезнуть). Плюс `assert_porcelain_blind` — проба ДОКАЗЫВАЕТ,
  что носитель реально ослепляет `git status`, иначе контроль доказывал бы обычный untracked.
  Ассерты — `rc`, именованная фраза из Демаркации, ИМЯ УТЕЧКИ, файл жив на диске. Ни одного
  ассерта вида «функция X вызвалась».
- **Имена внутренних функций** (`emit_dotgit_manifest`, `emit_untracked_manifest`, `enc_path`)
  встречаются в фикстурах ТОЛЬКО в комментариях-шапках «что закрывает класс» — проверено
  сплошным grep'ом по каталогу: ни одного вхождения в исполняемой части. Подгонки нет.
- **Правки фикстур в диапазоне** — `199129d` (re-анкер `red_excludes_target`/`red_toctou` с
  ИМЕНИ НОСИТЕЛЯ на ПАЙЛОАД) прямо предписан приёмкой path-1 п. 3 (контракт :322-325),
  потому что нога EXCLUDES удалена срезом; `1b00a17`/`53d40fe`/`f97aac2` — НОВЫЕ красные под
  находки адверсария, не ослабление старых. Ослабления ассертов в существующих воротах не
  нашёл: `red_detektor_utechek.sh` (18 ворот, ядро приёмки) в диапазоне v5→HEAD не менялся
  ВООБЩЕ.

## 4. §Предмет РЕАЛИЗОВАН (не только оттестирован)

Сверял пункты §Предмет и Инварианты И-1…И-13 с кодом `scripts/check_no_leak.sh`, а не с
фикстурами. Реализующие точки (строки на HEAD 39adb18):

| пункт контракта | реализация |
|---|---|
| два режима + абсолютный корень ДО cd | `:236-256` (диспетчер `usage()`, `case "$ROOT_ARG" in /*)`) |
| манифест = объединение трёх ног, единый sort | `manifest()` :962-979 |
| нога-1 tracked-байты + staged sha1 + @head + рекурсия | `emit_tracked_manifest()` :461-590 |
| нога-2 untracked МИМО ignore | `emit_untracked_manifest()` :592-660 (`ls-files --others` без `--exclude-standard`) |
| нога-3 dot-git: симлинки/CONTENT/DANGLING, gitdir-резолв, carve-out refs | `emit_dotgit_manifest_walk()` :782-862, `emit_dotgit_manifest()` :863-960 |
| обратимое кодирование путей (фиксы к13/к14) | `enc_path()` :433-445, применён во ВСЕХ ногах |
| снимок вне дерева, hash8 канон-корня, TMPDIR | `:383-396` |
| root-строка, verify-строка, mode-lock, симлинк-запреты | `do_snapshot()` :981-1033, `do_check()` :1077-1110 |
| дельта подмножеством, имена, fail-closed «снимок отсутствует» | `do_check()` :1035-1100 |
| двойное чтение (Б4 к8) | `:1184-1193` (`cur2` + расхождение → именованный отказ) |
| env-санитизация (BASH_ENV/GIT_*/HOME/XDG/IFS/GIT_CONFIG_COUNT) | `:216-224` |
| пин 15 утилит через TRUSTED_PATH + sanity-хэш | `:288-382` |
| И-10 норма-строка ветви B | `roles/orchestrator.md` — приземлена, `red_norma_stroka_024.sh` rc 0 (мой прогон) |

Каждый пункт имеет код, а не только ворота. Пунктов §Предмет без реализации не нашёл.

## 5. Полнота приёмки контракта

| критерий (контракт, «После реализации» + приёмка path-1) | мой прогон |
|---|---|
| red-ворота rc 0 | да, 15/15 (раздел 2) |
| обе пробы среза, входы случайны | да, `red_rootcut_*` rc 0 |
| проба слабых форм rc 0, фазы стабильны | да |
| И-9 живая канарейка rc 0 | да на клоне; на живом дереве — находка Р3 |
| И-11 scoped-семья rc 0 | да |
| И-10 норма-строка приземлена дословно | да, rc 0 |
| (г) стенограммы судей rc 0 | да, граница `17583d00…` |
| п. 4 приёмки: диф замороженных барьеров ПУСТ | да (раздел 1: этих путей в диапазоне нет) |
| п. 5 перф-канарейка rc 0 | 3 из 4 моих прогонов rc 0 — находка Р2 |

Б1/Б2 моего вердикта v1 закрыты и предъявлены МОИМ прогоном, а не ссылкой:
Б1 (`.git/info/exclude`) — `red_dotgit_info_exclude_024.sh` rc 0, контроли 2/3 называют
носитель; сверх того нога-2 видит саму утечку мимо правила (`red_rootcut_untracked_024`
контроль-2). Б2 ((г)-якорь слеп к `verdicts/review/`) — путь исправлен на `verdicts/review`,
и якорь СУДИТ этот вердикт: он положен туда же.

---

## Находки — каждая с классом. Ни одна не блокирует done.

**Р1 — carve-out харнесс-рантайма судит ИМЯ, не природу (класс: названное сужение предмета
с гранью в имени файла).** `emit_untracked_manifest` :642-647 пропускает `.zones/*/agent/*.db-wal`,
`*.db-shm`, `.zones/*/agent/sessions/*.jsonl`, `.zones/*/logs/*.log`; `emit_dotgit_manifest_walk`
пропускает `.git/info/refs`. Сужение названо в коде исчерпывающим списком и предъявлено
красной фикстурой (`red_info_refs_drift_024.sh` контроль-3: `.git/info/exclude` по-прежнему
краснеет). Граница «утечка с именем `.zones/x/agent/evil.db-wal`» остаётся открытой по
построению. Кандидат на сужение ОТДЕЛЬНЫМ предметом (природа файла вместо имени), не правка
024 задним числом.

**Р2 — перф-канарейка проходит с запасом в единицы процентов (класс: мера с околопороговым
запасом, флаки вне параллельной нагрузки).** Мои 4 прогона подряд на простаивающей машине:
```text
t_check=3459 мс > порог=3033 мс → rc 1   (первый прогон, БЕЗ параллельных red-прогонов)
t_check=2261 мс, порог 3015 мс → rc 0
t_check=2971 мс, порог 3015 мс → rc 0    (запас 44 мс = 1,5 %)
t_check=2396 мс, порог 3054 мс → rc 0
```
Известный остаток «canary_vremya под параллельной нагрузкой» ШИРЕ, чем записано: красный
прогон случился без внешней нагрузки от меня. Приёмочный п. 5 сегодня выполним (3/4,
изолированно воспроизводится зелёным), но критерий `3×t_raw + 3 c` при `t_raw≈5 мс`
вырождается в константу 3 с и краснеет от фонового шума машины. Уточнение меры — предмет
владельца, не блокер 024.

**Р3 — живая сверка основного дерева: 555 с и fail-closed rc 1 (класс: операционная цена
церемонии; НЕ ложное зелёное).** Мой прогон на `/home/aka/Documents/dev-harness` (стенограмма
вверху) вернул именованный отказ «основной чекаут мутировал во время сверки» — механизм Б4
сработал правильно: дерево пишется харнессом ВО ВРЕМЯ девятиминутной сверки. Замер источника
шума своей мерой (`find -newer`, окно 45 с на живом дереве) дал РОВНО три пути, и все три
лежат под carve-out Р1:
```text
./.zones/dev/.omp/profiles/dev/agent/agent.db-shm
./.zones/dev/.omp/profiles/dev/agent/agent.db-wal
./.zones/dev/.omp/profiles/dev/agent/sessions/…/Reviewer024v2.jsonl
```
Значит источник расхождения на девятиминутном окне — ВНЕ 45-секундной выборки (кандидат:
чекпойнт самой `agent.db`, не покрытой carve-out'ом `-wal`/`-shm`); изолировать не стал —
честно называю неизолированным. Историческая запись подтверждает, что зелёной живая сверка
не была никогда: `contracts-024-k5.md:17` → rc=1, `contracts-024-k6.md:7` → rc=1
(`.git/hooks/*.sample, .git/config` — реальная дельта дерева). Механизм при этом ни разу не
соврал зелёным. Названо как остаток церемонии (перф + грань carve-out), а не как дефект
предмета: приёмочные критерии контракта формулируют И-9 через явный корень и меряются
канарейкой, что зелено.

**Р4 — Н-1/Н-2/Н-3/Н-95 вердикта v1 остаются в силе** (`check_zones.sh` не судит коммит
автора вне зон контракта; провенанс респека v4 непроверяем — объект `7de219a8` отсутствует;
замороженная проза :298 говорит «14 ворот» там, где фикстура несёт 18 — фактический критерий
СТРОЖЕ записанного; `check_no_leak.sh` даёт ложный pipefail-rc на worktree). Ни одно не
ослабляет барьер; класс у всех — «зелёное не покрывает названное», не «ложное зелёное».

---

## Вердикт

**accept.** Область правки чиста и атомарна, норма не тронута молча, сырой вывод предъявлен
моими прогонами с кодами возврата, проверки судят предмет (дельту манифеста и имя утечки),
а не реализацию, каждый пункт §Предмет имеет код, каждое из 18 ворот предъявлено красным
против слабой формы, все rc-команды приёмки зелены. Четыре находки названы поимённо, все —
названные сужения и операционные остатки, ни одна не есть ложное зелёное механизма.

Контракт 024 готов к done-тегу.
