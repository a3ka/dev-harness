accept

# Вердикт ревьюера — контракт 025 (среда cwd/rc сессий), круг v1

Судимый HEAD: `9bfd8c91964bb53607386be38ba178c88275e489` (свежий клон
`https://github.com/a3ka/dev-harness.git`, `git rev-parse HEAD` совпал с заявленным).
Прогоны — в ОТДЕЛЬНОМ клоне, синтетика и декои — вне субъекта, в
`/tmp/dev-harness-verify/`. Ни одна находка не взята из пересказа вердиктов:
каждое счётное утверждение перемерено СВОЕЙ командой.

## 1. Область правки — соответствует зонам

`git log --format='%an' 3a44cee..HEAD -- <путь>` по зонам контракта (§Зоны):

```text
fixtures/check_runner_hygiene/                       17 architect
.omp/extensions .omp/config.yml scripts/drill_*.sh   17 implementer, 2 aka
contracts/025-sreda-cwd-rc.md                         9 architect
```

Ни одного коммита исполнителя в файлах проверок и ни одного коммита архитектора в
файлах механизма. Оба `aka`-коммита (`a9ea528`, `0f7e056`) — владельца, топология
моделей/фолбека, вне предмета 025.

Нормативные документы 025-агентами не тронуты: в `3a44cee..HEAD` по путям
`AGENTS.md roles/` изменения есть ровно в одном коммите — `a9ea528` (`aka`,
владелец). Правки `scripts/` в диапазоне принадлежат контракту 024
(`check_no_leak.sh`, зона 024) и двум `drill_*.sh` исполнителя 025 (его зона).

Область правки 025 внутри границ и по обе стороны от `git log`-авторства:
`bash scripts/check_zones.sh` → `rc=0`
(`замороженных контрактов: 23 · объявленных авторов: 5 · коммитов в диапазонах: 722 ·
проверено по зонам: 460`; обе строки СПАСЕНО 025 — `32d580c7`, `80eff00e` — приняты
механизмом).

Предмет после accept адверсария к7 НЕ менялся:
`git diff --stat b5e9cc0..HEAD -- contracts/025-sreda-cwd-rc.md fixtures/check_runner_hygiene/ .omp/extensions/ .omp/config.yml scripts/drill_*.sh`
— пустой вывод.

## 2. Сырой вывод — прогнал сам, все 14 фикстур

**Шесть статических инвариантных (мой счёт совпал с к7) на судимом HEAD:**

```text
red_strazh_vectora_utechki.sh .      rc=0
red_pipefail_prefiks.sh .            rc=0   (+ ИМЕНОВАННЫЙ ОСТАТОК Р3 в stdout)
red_marker_exit.sh .                 rc=0
red_pin_allowlist.sh .               rc=0
red_deni_patte_rny_zapisi.sh .       rc=0   (живой omp/18.1.18 config get)
red_granica_nepin_pipe_tee.sh .      rc=0
  ЗЕЛЁНОЕ 025-дыра-B: fail-closed предъявлен — непиннованная запись block Н-85 на
  чекаут-цели (1), свободном абсолюте (4) и local:// (8), pass только на скратч (2)
  и artifact (3); относительный вектор блок (5); под пинном чекаут-цель блок (6),
  в-пинне pass (7)
```

**Восемь `red_zond_*.sh` на судимом HEAD — все rc 0, каждая со своими фазами:**

```text
red_zond_dubl_toolcallid.sh              honest 0 · leakdup 2 · leak 1 · calldup 2
red_zond_osirotevshij_toolcallid.sh      honest 0 · orphan_result 2 · orphan_call 2
red_zond_nesoglasovannost_imeni.sh       honest 0 · mismatch_edit 2 · mismatch_bash 2
red_zond_uspeshnyj_povtor_kanona.sh      honest 0 · leak_edit 1 · leak_bash 1 · doubleblock_edit 0
red_zond_rc_vektor_agregacija.sh         honest 0 · second_success 1 · first_success 1 · doubleblock 0
red_zond_null_id_skleyka.sh              honest 0 · null_call_shadow 2 · null_result 2
red_zond_obratnyj_porjadok_para.sh       honest 0 · reverse_mismatch 2 · forward_mismatch_control 2 · reverse_match_leak 1
red_zond_pozitivnyj_ventil_agregacija.sh canary_late_fail 1 · canary_early_fail 1 · grep_bash_late_fail 1 ·
                                         grep_tool_late_fail 1 · scratch_late_fail 1 · doubleok_control 0
```

**ЖИВОЙ прогон И-6 — выполнен МНОЙ на судимом дереве** (не `--parse`, не пересказ:
две настоящие omp-сессии, настоящий дочерний субагент `Zond025Kid`, ~5 мин):

```text
bash fixtures/check_runner_hygiene/probe025_dochernij_vector.sh /tmp/dev-harness-verify/rev025_run
зонд 025-И-6: сессия MAIN (cwd=/tmp/dev-harness-probe025.cujGhA/repo, без пинна)…
зонд 025-И-6: сессия CHILD-спавнер (cwd=пинн /tmp/dev-harness-probe025.cujGhA/kidwt, WORKTREE=он же)…
зонд 025-И-6 диск-наблюдение CHILD: скратч-файл zond_child_scratch_13995.txt создан
зонд 025-И-6 таблица: MAIN edit=0 bash=0 false|true=ERR канарейки=OK/OK;
                      CHILD edit=0 bash=0 false|true=ERR канарейки=OK/OK
зонд 025-И-6 пинн-ребёнок: чекаут-запись=0 скратч-запись=OK
ЗЕЛЁНОЕ 025-И-6: канонические формы блокированы именованным отказом стража (Н-85) в
MAIN и CHILD, чекаут-запись пиннованного ребёнка блокирована стражем, его скратч-запись
жива, rc-вектор ERR, канарейки живы
rc=0
```

Это же прогон закрывает досягаемость до дочерних (И-6.2/И-6.3) и подтверждает
ДЕЙСТВУЮЩИЙ носитель B-1 (`tool_result`-патч): `false | true` красный, `yes | head -1`
и `grep` зелёные у ОБЕИХ сессий на omp 18.1.18.

## 3. Красное предъявлено — против сломанной реализации, не только против отсутствия

**(а) Каждая `red_zond_*` — красная против СВОЕЙ пред-фиксной версии парсера**
(новая фикстура наведена на worktree `<фикс>^`):

```text
red_zond_dubl_toolcallid.sh        87e44ec^  rc=1  «leakdup: ожидался rc 2, получен rc 0»
red_zond_osirotevshij_toolcallid.sh a6fb3c1^ rc=1  «orphan_result: ожидался rc 2»
red_zond_nesoglasovannost_imeni.sh 33a17cf^  rc=1  «mismatch_edit: ожидался rc 2»
red_zond_uspeshnyj_povtor_kanona.sh 33a17cf^ rc=1  «leak_edit: ожидался rc 1, получен rc 0»
red_zond_rc_vektor_agregacija.sh   a356d44^  rc=1  «second_success: ожидался rc 1»
red_zond_null_id_skleyka.sh        a356d44^  rc=1  «null_call_shadow: ожидался rc 2, получен rc 0»
red_zond_obratnyj_porjadok_para.sh 4f5a0e4^  rc=1  «reverse_mismatch: ожидался rc 2, получен rc 0»
red_zond_pozitivnyj_ventil_agregacija.sh 4f5a0e4^ rc=1 «canary_late_fail: ожидался rc 1, получен rc 0»
```

**(б) Шесть статических — красные на дереве ДО реализации** (`1218704^`): каждая с
именованным диагнозом «механизм отсутствует …» либо «deny-правил меньше трёх (0)».

**(в) Мои собственные декои** (не из фикстур; дерево-копия в скратче, мутирован ровно
один модуль):

```text
path-guard.ts := judge всегда pass
  red_strazh_vectora_utechki.sh  rc=1  «ветвь edit-относительный — ожидалось block, получено pass»
  red_pin_allowlist.sh           rc=1  «ветвь запись-в-чужой-корень — ожидалось block, получено pass»
  red_granica_nepin_pipe_tee.sh  rc=1  «ветвь непиннованный-чекаут-запись — ожидалось block, получено pass»
                                        (TMPDIR переопределён — иначе rc 2 по геометрии, честно)
exit-marker.ts := всегда [exit=0]
  red_marker_exit.sh             rc=1  «ветвь код-7-из-details — ожидалось [exit=7], получено [exit=0]»
rc-prefix.ts := присвоение чужой строки
  red_pipefail_prefiks.sh        rc=1  «кандидатная форма сломала канарейку (yes|head → 141)»
```

Все пять слабых форм умирают именованно. Зелёное здесь предъявлено вместе с красным.

## 4. Структурный аудит к7 (`[0]`-паттерн) — перепроверен МОЕЙ мерой, утверждение верно

Прочитал `probe025_dochernij_vector.sh` (713 строк) целиком. Полная инвентаризация
`[0]` — 16 попаданий, из них рабочих 8; классификация каждой:

| место | что | класс |
|---|---|---|
| `:236,:239` `sorted(orphan_*)[0]` | образец id в тексте уже принятого `exit 2` | диагностика |
| `:331` `bad[0]` в `positive_verdict` | образец для сообщения; решение — `if bad:` по ВСЕМУ списку | диагностика |
| `:344` `os.path.basename(g[0])` в `derive()` | выбор ИМЕНИ мишени, не вердикта | вывод имени |
| `:372,:376,:379` `tops[0]/hits[0]/kids[0]` | под охраной `len(...) == 1` | singleton |
| `:414,:436,:463` `canon[0]['text'][:120]` | цитата в ветви `other`, которая и так даёт `unknown → rc 2` | диагностика |
| `:576` `m[0]…c[4]` | индексы кортежа таблицы | печать |

Все три вентильные функции агрегируют по ВСЕЙ канонической группе:
`attribute()` — `any(not isError)` → `any(refused_by_guard)` → `any(killed_by_policy)`;
`rc_vector_verdict()` — `any(exit == 0)`; `positive_verdict()` — `bad = [все неуспешные]`.
Grep-вентиль объединяет `cg_bash + cg_tool` в ОДНО множество (прежнее `if/elif`,
глотавшее вторую форму целиком, снято).

**Своя мера — контрпримеры, которых нет ни в одной `red_zond_*`** (скрипт мой,
`rev025_probe_audit.py`): вентиль чекаут-записи CHILD (`child_x['checkout']`) и
first-wins в `derive()`:

```text
HEAD:            honest rc=0 · co_late_leak rc=1 · co_early_leak rc=1 · derive_decoy rc=2
пред-к6 4f5a0e4^: honest rc=0 · co_late_leak rc=1 · co_early_leak rc=1 · derive_decoy rc=2
```

`co_late_leak`/`co_early_leak` — вторая УСПЕШНАЯ попытка той же чекаут-записи, поздняя
и ранняя: обе дают `УТЕЧКА чекаут-запись CHILD`, порядок не влияет. `derive_decoy` —
подложенный в скратч-корень чужой `zond_child_scratch_00001.txt`, сортирующийся раньше
настоящего: `derive()` действительно берёт `g[0]` и берёт декой, но исход
ВЫРОЖДАЕТСЯ В `rc 2` («каноническая скратч-запись … не найдена»), а не в молчаливое
зелёное. Fail-closed держит.

**Заключение по пункту (3) задания: утверждение к7 «нет оставшегося first-wins
`[0]`-паттерна в семантических вентилях» ПОДТВЕРЖДАЮ** — независимым чтением и
независимыми контрпримерами, а не повтором его команды.

## 5. Проверки не переписаны под реализацию

- Файлы проверок правил ТОЛЬКО architect (пункт 1), реализацию — ТОЛЬКО implementer.
- Сравнение критериев заморозки v1 (`3a44cee:contracts/025-sreda-cwd-rc.md`) с
  текущими: движение **строго в сторону ужесточения**.

| инвариант | v1 | HEAD | направление |
|---|---|---|---|
| И-1 | проза «selftest» | judge-CLI, 11 входов: 6 блок-ветвей + 5 канареек | усиление |
| И-2 | «config.yml несёт `prefix:`» | побайтовый оракул + 3 полосы суда | смена носителя (ключа `prefix` в схеме НЕТ — замер: 453 ключа, 4 пробы rc 1) |
| И-3 | «маркер секции + ≥3 правил» (ТЕКСТ) | РАСПАРСЕННЫЙ рантайм `omp config get`, ночные формы обязаны матчиться, канарейки — нет | усиление (декой-комментарии умирают) |
| И-4 | голая rc-строка | judge-CLI, 7 входов, эхо-ложь в обе стороны | усиление |
| И-5 | голая rc-строка | judge-протокол, 9 входов, realpath/симлинк/`..`/сверка `actual` | усиление |
| И-6 | «процедуры, НЕ файлы»; И-6.3 «пусто = не блокер» | ОДИН запускаемый файл, дочерний субагент, И-6.3 — блокер | усиление |
| И-7 | конверсия — отдельная пачка | без изменений | без изменений |

Единственное ослабление за цикл, которое я нашёл, — вывод `tee` из ОБЯЗАТЕЛЬНОГО
deny-набора A-2 (`84761ef`, закрытие tee-блока критика `d141dd9`). Проверил основание:
`tee` НЕ входит в измеренный ночной канал Б2 (там `edit` относительным путём, `sed -i`,
`printf >>`); грамматика паттернов (литерал + `*`) не выражает «tee с относительным
операндом», а любой tee-deny убивает зондовый шаг 2 И-6 ДО стража и делает И-6
неизмеримым. Ветвь `tee` при этом НЕ осталась без стража: её стережёт A-1 — что я
проверил живьём (шаги 2/3/4 зонда — именно `tee`, все блокированы Н-85, п. 2).
Ослабление обосновано структурно и разведено в тексте явно; проверкой под код не
является.

## 6. Атомарность и норма

Каждый круг — свой коммит со ссылкой на предмет: `a6fb3c1`/`ad4d9b9` (B-025-r3-1),
`33a17cf` (B-025-k4-1/k4-2), `a356d44` (B-025-k5-1/k5-2), `4f5a0e4` (B-025-k6-1/k6-2),
`f2fa54c` (А-137/А-138). Связок «пять задач в одном коммите» нет. Норма внутри задачи
о механизме не правилась (пункт 1).

## 7. Заявленное равно сделанному — сверено СВОЕЙ мерой

```text
curl .../commits/9bfd8c9.../check-runs   → ci completed success
curl .../actions/jobs/103877838066       → head_sha=9bfd8c91964bb53607386be38ba178c88275e489
                                            conclusion=success · steps_total=34 · success: 34
                                            06:19:41Z → 07:02:02Z  (42 мин 21 с)
```

«Все 34 шага success, ~43 мин» — подтверждено (34/34, 42 мин 21 с).
«Шесть статических инвариантных фикстур» (к7) — мой независимый счёт: ровно 6.

## Находки — каждая названа и классифицирована

**Н-1. Класс: фактическая неточность ССЫЛКИ В ЗАДАНИИ (не в работе).**
`https://github.com/a3ka/dev-harness/actions/runs/34813088277` не существует — API
отдаёт `null`. Job `103877838066` принадлежит прогону **`34812985458`**
(`https://github.com/a3ka/dev-harness/actions/runs/34812985458/job/103877838066`).
Сам факт зелёного CI на судимом HEAD подтверждён (п. 7).

**Н-2. Класс: инвариант стережёт МЁРТВЫЙ носитель; действующий носитель без
статического инварианта. НЕ блокирующая.**
`§B-1` сам объявляет env-канал `PI_SHELL_PREFIX` мёртвым на omp 18.1.18, а И-6
называет действующим носителем `tool_result`-патч. При этом И-2
(`red_pipefail_prefiks.sh`) побайтово судит ИМЕННО присвоение
`process.env.PI_SHELL_PREFIX` (`:93-96`, `mod.default(); const v =
process.env.PI_SHELL_PREFIX`) и НИ ОДНОЙ строкой не касается обработчика
`p.on('tool_result', …)`. Следствие: удаление живого патча из `rc-prefix.ts` оставит
И-2 ЗЕЛЁНЫМ; строка `process.env.PI_SHELL_PREFIX = PREFIX` живёт в коде только ради
замороженного оракула. Единственный ловец действующего носителя — И-6, который по
тексту контракта «Не CI-шаг: приёмочная процедура» и требует omp + модель + сеть.
Почему не блокер: покрытие ЗАЯВЛЕНО, а не умолчано (§B-1 прямо отправляет семантику
в И-6), И-6 сегодня зелён живым прогоном (п. 2), а перевод оракула — заморожен и
требует слова владельца (Н-39). Адрес: очередь — при первой же конверсионной пачке
И-7 завести статическую ветвь на `tool_result`-патч.

**Н-3. Класс: автор проверки = автор предмета (для И-6). НЕ блокирующая.**
Предмет И-6 — парсер `probe025_dochernij_vector.sh`, и его красные `red_zond_*.sh` —
один автор (architect). Это ровно та конструкция, которую роль ревьюера стережёт.
Смягчение измерено, а не предположено: (а) все восемь красных я предъявил КРАСНЫМИ
против пред-фиксных версий парсера (п. 3а) — самоотчётом это быть не может; (б) четыре
круга адверсария независимо исполняли контрпримеры; (в) мой собственный контрпример
(п. 4) не совпадает ни с одной фикстурой и дал ожидаемый исход. Достаточно для accept;
названо, чтобы не стало прецедентом «архитектор судит сам себя» по умолчанию.

**Н-4. Класс: критерий отложен ТЕКСТОМ контракта. НЕ блокирующая.**
И-7 — единственный инвариант без rc-команды: конверсия `red_* → case_*` объявлена
«пачкой ARCHITECT ПОСЛЕ land исполнителя». Готовность к done по 025 НЕ включает
исполнение И-7; при закрытии предмета это остаётся открытым хвостом, и его следует
поставить в очередь явно, а не считать сделанным.

**Н-5. Класс: расхождение задания с нормой (исполнено по норме).**
Задание называет `verdicts/reviewer/contracts-025-v1.md`. Норма — `roles/reviewer.md:4`
`verdict: verdicts/review/` и §Зоны контракта `ЗОНА reviewer: verdicts/review/`;
каталога `verdicts/reviewer/` в дереве нет. Вердикт положен в зонно-верный
`verdicts/review/contracts-025-v1.md`: создание `verdicts/reviewer/` само было бы
правкой вне объявленной зоны — ровно то, что этот гейт стережёт.

**Н-6. Наблюдение, не находка.** Зонд И-6 пишет клон улик в
`${TMPDIR:-/tmp}/dev-harness-probe025.*` — ВНЕ allowlist Г3. Это преднамеренная
геометрия (§ГЕОМЕТРИЯ А-130: чекаут-цель обязана быть вне allowlist, иначе
чекаут-ветвь неотличима от скратч-ветви) и документировано в шапке файла.

## Вердикт

**accept.** Область правки в границах зон и подтверждена авторством по всей цепочке
к3→к7. Сырой вывод предъявлен по всем 14 фикстурам МОИМИ прогонами, включая живой
И-6 с настоящим дочерним субагентом (rc 0). Красное предъявлено трижды независимо:
против пред-фиксных парсеров, против до-реализационного дерева и против моих
собственных декоев. Структурный аудит к7 перепроверен своей мерой и подтверждён.
Проверки за цикл ужесточались, не подгонялись; единственное ослабление (`tee` вне
обязательного deny-набора) обосновано структурно и перекрыто A-1, что проверено
живьём. Заявленные числа сошлись с моими (34/34 шага, 42 мин, шесть статических
фикстур). Блокирующих находок нет; Н-1…Н-6 названы выше и адресованы.
