# Вердикт ревьюера — контракт 035 (жнец ./tmp, Н-97), круг 1, финальный (post-v2 close-out)

ВЕРДИКТ: **FAIL** — одна блокирующая находка (БЛОКЕР-1, п.3(в)). Остальные шесть проверок
задания — зелёные, предъявлены сырым выводом ниже.

МОДЕЛЬ: anthropic/claude-opus-5 (автор дельты v2 — owner+architect f2d079c; критик v2 —
отдельное семейство; семейство автора не наследуется).
ПРЕДМЕТ: замороженный текст `frozen/contracts/035/2` = tag-object `0e0300172b18bb23c88624a57a5565ca17ab68a5`
(объект-коммит `1f933ec6`), close-out-стенограмма `e2a71dd` (pre-rebase `2add934`, тела
побайтово совпадают), цепочка v1→census-v2.
КЛОН СУДА: `/tmp/dev-harness-verify/rev-035-fin/repo` @ `82bf879839cf9d5a513bae777498e861d0309c69`.
ОПЕРАНДЫ ЗАКЛЮЧ-ПРОВЕРКИ (фиксация по строке 214 замороженного текста):
CHECKOUT = `/tmp/dev-harness-verify/rev-035-fin/repo`; LIVE_ROOT = `/home/aka/Documents/dev-harness`
— живой корень выбран мною из известного фактического корня close-out, не из самоотчёта
проверяемой стороны (СОВЕТ критика v2 к строке 211 исполнен).

---

## БЛОКЕР-1 — п.3(в): дословная форма стенограммы не исполнена; ни одного поля шаблона нет

Замороженное обязательство `frozen/contracts/035/2`, строки 113–119 и 149–187: «в теле
close-out-коммита — сырая apply/census-стенограмма **дословной формы (блок ниже)**: исходный
живой корень, время начала/окончания наблюдения, HEAD до apply, блоб исполняемого жнеца,
точные команды (dry-run и apply) с их **реальными rc** и stdout/stderr, наблюдения отсутствия
двух имён после apply, census-команда и её полный вывод»; и далее: «угловые скобки
заполняются только сохранёнными фактическими значениями, **неизвестное обозначается
„НЕ СОХРАНЕНО“**, а не восстанавливается по ожидаемому результату».

Мера — поиск каждого поля шаблона в теле `e2a71dd` (`git log -1 --format=%B`), своя команда,
не повтор авторской:

```
$ for k in "СЫРАЯ СТЕНОГРАММА" "ИСТОЧНИК:" "НАЧАЛО-UTC:" "КОНЕЦ-UTC:" "LIVE_ROOT:" \
    "HEAD-ДО-APPLY:" "ЖНЕЦ-БЛОБ:" "CWD:" "ARGV:" "RC:" "STDOUT-BEGIN" "STDOUT-END" \
    "STDERR-BEGIN" "STDERR-END" "ПОСЛЕ-APPLY:" "CENSUS-ARGV:" "CENSUS-RC:" \
    "CENSUS-STDOUT-BEGIN" "CENSUS-STDERR-BEGIN" "НЕ СОХРАНЕНО"; do
      echo "$(git log -1 --format=%B e2a71dd | grep -Fc "$k")  $k"; done
0  СЫРАЯ СТЕНОГРАММА      0  ИСТОЧНИК:        0  НАЧАЛО-UTC:      0  КОНЕЦ-UTC:
0  LIVE_ROOT:             0  HEAD-ДО-APPLY:   0  ЖНЕЦ-БЛОБ:       0  CWD:
0  ARGV:                  0  RC:              0  STDOUT-BEGIN     0  STDOUT-END
0  STDERR-BEGIN           0  STDERR-END       0  ПОСЛЕ-APPLY:     0  CENSUS-ARGV:
0  CENSUS-RC:             0  CENSUS-STDOUT-BEGIN                  0  CENSUS-STDERR-BEGIN
0  НЕ СОХРАНЕНО
```

**0 из 20 обязательных полей. Тело — свободная проза (12 непустых строк).**

Класс находки: блокирующий, и он НЕ зависит от того, какая из двух оговорок п.3(в) применима.
Оговорка о «честном переносе сохранившейся исходной записи» разрешает позднюю запись —
она не отменяет форму и не отменяет поля. Оговорка «если сырьё не сохранилось» требует
НЕ ослабления формы, а отдельного слова владельца; сама форма в обоих случаях остаётся
обязательной, для неизвестного предусмотрен маркер «НЕ СОХРАНЕНО» — он тоже не употреблён
ни разу.

Отдельно тяжело то, что отсутствуют поля **свежего** прогона. Строки 3–4 тела — «СЫРОЙ ВЫВОД
(dry-run повтор 2026-09-20)»: этот прогон делался оркестратором в момент написания коммита,
и `LIVE_ROOT`, `CWD`, `ARGV`, `RC`, `HEAD-ДО-APPLY`, `ЖНЕЦ-БЛОБ` были доступны даром. Не
записано ни одно. Приведён один stdout без команды и без кода возврата — ровно то, что
роль ревьюера называет пересказом: по такой записи нельзя установить ни в каком корне мели,
ни каким блобом жнеца, ни чем кончился прогон.

Цена находки не косметическая. Обязательная стенограмма — единственная компенсация, под
которую v2 принял названную границу P3 (строки 137–147: «числа при честном LIVE_ROOT
механической проверкой не отвергаются… исторические утверждения проверяются когнитивно
по обязательной сырой apply/census-стенограмме (в)»). Критик v2 (`931e8ba`,
`verdicts/critic/contracts-035-v2.md`, §3) зафиксировал это же прямым текстом: «Дословная
форма стенограммы не проверяется заключительной shell-командой. Её наличие, полноту и
историческую согласованность рассматривает судья когнитивно… Один rc 0 не означает
исполнения п.3(в)». Принять close-out по rc 0 заключ-команды означало бы принять P3-границу
без компенсации, ради которой её и согласились назвать.

Порядок судов усугубляет: accept критика v2 датирован 11:45:48, стенограмма `e2a71dd`
появилась ПОСЛЕ — ни один суд её содержимое не рассматривал. Критик прямо оставил это
открытым: «Этот accept… не заменяет… последующий суд и условия done».

Что закрывает БЛОКЕР-1: пере-запись стенограммы в теле нового коммита ровно по шаблону
строк 153–187, с фактическими значениями свежего повторного прогона и маркером
«НЕ СОХРАНЕНО» в полях исходного apply, которые не сохранились.

---

## З-1 (замечание, не блокер) — основание оговорки «без сырья» названо не тем словом

Тело `e2a71dd` обосновывает приём так: «Владелец принимает P3-остаток как cognitive-only
(слово 2026-09-20)». Это слово о ДРУГОМ: P3-остаток — про то, что census-числа при честном
LIVE_ROOT не отвергаются механикой (строки 137–147). Оговорка п.3(в) требует иного —
«отдельное явное решение владельца о принятии **исторического свидетельства без сырья**».
Критик v2 §3 перечисляет их как альтернативу: «перенос сохранившейся исходной записи
**либо** отдельное явное решение владельца о принятии исторического свидетельства без
сырья». Поиск такого отдельного решения в `HANDOFF.md`, `NABLIUDENIA.md` и
`verdicts/consultant/` результата не дал; подпись консультанта
(`census-v2-035-klass-v1.md:4`) прямо говорит обратное: «эта подпись не разрешает v2, не
снимает блокировку done/035».

Не делаю это блокером: тело одновременно утверждает, что исходные строки apply
(`TMP-РЕАП: tmp/draft020-task.md done 020` и пара) перенесены, то есть претендует на первую
оговорку — «честный перенос». Какая из двух оговорок заявлена, из текста не определяется.
Это неоднозначность изложения, разрешаемая владельцем или переписью по БЛОКЕР-1.

## З-2 (вне предмета 035; наблюдение по дереву приземления)

`registry/contracts.tsv` на живом HEAD `82bf879` несёт **неразрешённые маркеры слияния**:

```
$ grep -n '^\(<<<<<<<\|=======\|>>>>>>>\)' registry/contracts.tsv
41:<<<<<<< HEAD
42:=======
43:>>>>>>> parent of 0717eda (orchestrator: registry update ot freeze 032/2 …)
$ wc -l < registry/contracts.tsv
43
```

Источник — `8b3aa2b` (owner-канал, dual-control зона), не коммит 035. Строки 035
(`0e030017` = frozen/contracts/035/2, `2a5a2f46` = id/CONTRACT/035) целы и на суд 035 не
влияют. Существенно другое: ни `check_zones.sh` (rc 0), ни `check_ids.sh` (rc 0, «номера
уникальны и согласованы с регистром выдачи») маркеров не видят — механизма-ловца на
конфликт-маркер в исключительной dual-control зоне нет. Не правлю чужой файл, называю.

---

## Зелёное — предъявлено сырым выводом

**П1. Заключ-команда v2.** Тело взято ДОСЛОВНО из `frozen/contracts/035/2`, строки 218–224,
и сверено побайтово (`diff` вырезки контракта с телом скрипта → `DIFF-RC=0`).

```
$ bash zakl.sh /tmp/dev-harness-verify/rev-035-fin/repo /home/aka/Documents/dev-harness
A-CLEAN-RC=0
$ bash zakl.sh /home/aka/Documents/dev-harness /home/aka/Documents/dev-harness
A2-LIVE-SELF-RC=0
```

Подделка (обход адверсария `df7ff99`): каталог с документами дерева, census-строка подменена
на «999 верхнеуровневых (0 непустых, 999 пустых)», `tmp/` отсутствует; оба операнда —
подделка:

```
$ grep -n '^035 close-out: census' fake999/HANDOFF.md
245:035 close-out: census ./tmp: 999 верхнеуровневых (0 непустых, 999 пустых), draft020 ×2 снесены
$ test -d fake999/tmp   ->   test-d-tmp-rc=1
$ bash zakl.sh …/fake999 …/fake999
B-FAKE-RC=1
```

P1/P2 закрыты механически. P3 (числа при честном корне) остаётся открытым по построению —
названная граница, а не обход; именно она и опирается на стенограмму (см. БЛОКЕР-1).

**П3. Н-97 свёрнут.** Статус-группа `### Н-97.` … до следующей `### ` содержит дословно
`ЗАКРЫТО — 035` и не содержит `ОТКРЫТО — адрес` (оба конъюнкта входят в rc 0 выше;
awk-вырезка прочитана отдельно).

**П4. Приёмка v1.** Задание называет `case_zhnets_ostatok_036.sh` — такого файла в дереве нет;
фикстура-страж 035 называется `case_zhnets_ostatok_035.sh` (зона architect, строка 191
контракта). Измерено по ней:

```
$ bash fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh
FIX035-RC=0
$ bash scripts/verify_antiplacebo.sh --scope gc_agent_branches/case_zhnets_ostatok_035
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   gc_agent_branches/case_zhnets_ostatok_035.sh: зелёный контроль есть,
       повторный прогон красный кодом 1 — «не удалось удалить»
барьеров: 1 · фикстур: 1 · предъявлено красным повторным прогоном: 1
SCOPED-035-RC=0
```

Красное предъявлено: барьер даёт rc 1 против испорченного входа, не только зелёное. Семья
`gc_agent_branches` — 8 `case_*.sh` по моему счёту, совпадает с объявленным в строке 205.

**П5. check_zones — чисто, на живом дереве:**

```
$ bash scripts/check_zones.sh   (живой корень, HEAD 82bf879)
замороженных контрактов: 35 · объявленных авторов: 6 · коммитов в диапазонах: 1461 ·
проверено по зонам: 942
CHECK-ZONES-LIVE-RC=0
```

**П6. Детектор утечек 024:**

```
$ bash scripts/check_no_leak.sh --check /home/aka/Documents/dev-harness
основной чекаут чист
DETECTOR-RC=0
```

**Своя мера census-чисел.** HANDOFF:245 заявляет «10 верхнеуровневых (3 непустых, 7 пустых)».
Проверено НЕ авторской командой, а независимым перечислением живого `tmp/`: 10
непосредственных записей — непустые `critic/`, `charter.RERj63/`, `ci_job_103282533944.log`
(3); пустые `ci_parity/`, `check_approval/`, `critic-tmp/`, `fix005limits/`, `coldstart005/`,
`barrier005/`, `scratch/` (7). N=10, a=N−b=3, b=7 — совпало. `tmp/draft020-task.md` и
`tmp/fix5-draft020-task.md` в перечислении отсутствуют. Арифметика и согласованность
сошлись; происхождение свидетельства из прошлого дерева — cognitive-only по строкам 143–147,
и именно поэтому стенограмма обязательна.

**Область правки и атомарность.** `f2d079c` — ровно один файл `contracts/035-zhnets-tmp.md`
(+109/−8), зона architect. `e2a71dd` — diff ПУСТ (`git diff-tree --name-only -r` → 0 строк):
стенограмма живёт в теле, файлов не трогает, зону не расширяет. Связок из нескольких задач
в одном коммите нет.

**Норма не тронута молча.** Ни один коммит цепочки 035 не касается `AGENTS.md`, `roles/`,
`.omp/agents/`. Инвариант п.1 («`scripts/gc_agent_branches.sh` НЕ меняется») держится:
последняя правка жнеца — `15d8125` от 2026-09-19 (контракт 030), до v2-цепочки 035.

**Проверка не переписана под реализацию.** Единственный автор фикстуры
`case_zhnets_ostatok_035.sh` — `14fa1af` (architect), и подгонять её было не подо что:
предмет 035 по п.1 кода не имеет, механизм 026 заморожен и в окне не менялся. Стаб-привязка
и различающий вход — в КОДЕ фикстуры (Н-39), не в прозе.

---

## Итог

Механика v2 исполнена и предъявлена: обход адверсария `df7ff99` закрыт по P1/P2, барьер
красен против сломанного входа, зоны и атомарность чисты, census-числа выдержали мою
независимую меру. Не исполнено ровно одно — и именно то, чем v2 расплатился за названную
границу P3: сырая стенограмма п.3(в) дословной формы. Сейчас на её месте пересказ без
единого поля шаблона, без корня, без блоба, без кода возврата.

**FAIL. `done/035` не ставить до закрытия БЛОКЕР-1.** З-1 разрешается тем же переписыванием
или словом владельца; З-2 — вне предмета 035, адресуется владельцу/оркестратору отдельно.

Судья: reviewer. Вердикт приземлён в основной репозиторий под именем `reviewer`
(зона reviewer: `verdicts/review/`).
