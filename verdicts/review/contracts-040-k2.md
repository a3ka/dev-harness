FAIL

# Ревью контракта 040 — круг 2 (сверка ЗАКРЫТИЯ Б1+Б2 круга 1)

Предмет круга ОГРАНИЧЕН порогом, объявленным моим же вердиктом круга 1
(`verdicts/review/contracts-040-k1.md`, `d23086f`, прочитан целиком): (а) живая
мера Р1 в ОБЕИХ полярностях; (б) статус Р8 от обновлённого пина; (в) запись
«Условие done» в `HANDOFF.md`; плюс — не сломалось ли что-то ЕЩЁ между кругом 1
и сейчас. Разделы 1-8 круга 1 не переоткрываются.

Контракт судится в редакции **v2** (`frozen/contracts/040/2` = тег-объект
`ccabba52`, коммит `f2254239`, блоб `ecca7ff3`). Прогоны — в одноразовом клоне
`https://github.com/a3ka/dev-harness` → `/tmp/dev-harness-verify/rev040k2/repo`
(барьеры — на `c16b014`, повторная сверка Р8/заморозки — на актуальном
`origin/main` = `eab8ce6`); мутант — в отдельной копии
`/tmp/dev-harness-verify/rev040k2/mutant`. Основной чекаут не менялся (Н-85/А-122).
Судья: reviewer, семейство отлично от автора работы (architect — `anthropic`).

Вердикт: **FAIL**. **Обе блокирующие находки круга 1 ЗАКРЫТЫ и предъявлены моей
мерой** (Б1 — в обеих полярностях, Б2 — от нового пина). FAIL несут ДВА НОВЫХ
блокера, оба вне инженерного ядра предмета: Б3 — «Условие done» контракта
по-прежнему не исполнено (наследует О1 круга 1, контракт сам называет его
самостоятельным блокером `done`); Б4 — джоба `ci` на `main` КРАСНАЯ на HEAD
четыре прогона подряд, и красна НЕ по 040, а по грамматике наблюдений — пока
она красна, обязательное «живое наблюдение зелёного CI» взять на HEAD нельзя.
Плюс четыре замечания (О1-О4).

---

## Б1 круга 1 — ЗАКРЫТ. Мера Р1 жива: 141 вызов вместо 0, и мой мутант теперь КРАСЕН (rc=1, был rc=0)

Закрытие внесено `b38e78a` (architect): `run_trace` переведён с
`env SHELLOPTS=xtrace BASH_XTRACEFD=9` (стиралось `env -i` re-exec'ом субъекта)
на внешнюю `strace -f -e trace=execve` — канал ядра, не переносимый окружением.

**Зелёная полярность (честное дерево, `c16b014`), моя мера:**

```
$ bash fixtures/check_zones/red_predel_git_vyzovov.sh
toy LOW:  K=12 M=60 L=5 -> 133 коммитов judged, git-вызовов=141 (нарушение поймано)
toy HIGH: K=12 M=60 L=35 -> 493 коммитов judged, git-вызовов=141 (нарушение поймано)
абсолютная граница (грубая, на LOW) a*(K+M)+b = 6*(12+60)+20 = 452
дифференциал: C_high(141) - C_low(141) = 0 - граница S = 15 (Δ на окно = 30, Δ>=2S держится: 30>=30)
ПРЕДЕЛ ДЕРЖИТСЯ: LOW=141<=452 (грубо) И дифференциал 0<=15 (решающе)
rc=0   (226.04 s)
```

`141` вместо `0` круга 1 — мера больше не пуста. Число независимо совпало с
счётным утверждением `HANDOFF.md:410` («141/141 call-budget не вырос»): это
подтверждение ДРУГОЙ командой, а не повтор чужой.

**КРАСНАЯ полярность — предъявлена.** В копии того же клона я вернул в горячий
цикл ровно тот per-commit вызов, замену которого контракт объявляет предметом
(§Инварианты п.1), не трогая ничего другого — тот же мутант, что в круге 1:

```
$ git diff -- scripts/check_zones.sh
@@ -822,6 +822,7 @@ while IFS=$'\t' read -r nnn since; do
   while IFS= read -r c; do
     [ -n "$c" ] || continue
     commits=$((commits + 1))
+    an_regress="$(g log -1 --format=%an "$c")"

$ bash fixtures/check_zones/red_predel_git_vyzovov.sh
toy LOW:  K=12 M=60 L=5 -> 133 коммитов judged, git-вызовов=1659 (нарушение поймано)
toy HIGH: K=12 M=60 L=35 -> 493 коммитов judged, git-вызовов=5979 (нарушение поймано)
абсолютная граница (грубая, на LOW) a*(K+M)+b = 6*(12+60)+20 = 452
ОТКАЗ: git-вызовов на LOW 1659 > грубой границы 452 — батчинга похоже нет вовсе (Н-126)
rc=1   (338.98 s)
```

Круг 1: тот же мутант давал **rc=0** и вывод байт-в-байт равный честному. Сейчас
**rc=1** с именованной причиной. Полярность восстановлена; барьер отличает предмет
контракта от его отсутствия. Дифференциал того же прогона тоже красен по
построению (5979−1659 = 4320 ≫ S=15) — краснота обоснована дважды, грубой
границей И решающим дифференциалом, то есть не зависит от калибровки `a`,`b`.

**Оговорка о канале измерения (моя мера, не пересказ).** Эта рабочая станция
`strace` не несёт, и фикстура отвечает fail-closed, а НЕ зелёным:

```
$ bash fixtures/check_zones/red_predel_git_vyzovov.sh
NOT_IMPLEMENTED: нет strace (run_trace требует внешнюю syscall-трассировку — см. комментарий там)
rc=2
```

Гард честный и назван в шапке фикстуры прямо. Чтобы судить Б1 СВОЕЙ мерой, а не
пересказом CI, я добыл трассировщик сам (пакет `strace-7.2-1-x86_64` с
`geo.mirror.pkgbuild.com`, распакован в клон-каталог, `strace -- version 7.2`) —
оба прогона выше сделаны под ним. Замер CI, на который ссылается architect,
я проверил независимо через API GitHub, а не по рассказу:

```
$ curl -s .../actions/runs/35878468996 | jq …
{"id":35878468996,"name":"ci","event":"push","status":"completed",
 "conclusion":"success","head_sha":"9bcb8468…","head_branch":"main",
 "created_at":"2026-09-23T15:01:19Z"}
$ curl -s .../actions/runs/35878468996/jobs | jq …
JOB ci success
   STEP Счётчик git-вызовов check_zones (контракт 040, §Приёмка Р1) :: success
        2026-09-23T15:08:13Z -> 2026-09-23T15:11:19Z
```

3 минуты 6 секунд работы шага на раннере против 0.14 с локального
`NOT_IMPLEMENTED` — шаг на CI действительно СЧИТАЛ, а не вырождался. Б1 закрыт.

---

## Б2 круга 1 — ЗАКРЫТ. Р8 зелен от нового пина, текст перезаморожен как v2, подпись владельца на месте

Моя мера, дословная команда §Приёмка Р8 — на `c16b014` и повторно на актуальном
`origin/main` (`eab8ce6`):

```
$ git diff --exit-code b38e78ac1e353be132cda2abbe6152b03fa9c275 HEAD -- ':(glob)fixtures/check_zones/red_*' ':(glob)fixtures/check_zones/probe_*' ':(glob)fixtures/check_zones/stab_*' ':(glob)fixtures/check_zones/_*' ':(glob)fixtures/check_protected/red_*' ':(glob)fixtures/check_protected/probe_*' ':(glob)fixtures/check_protected/stab_*' ':(glob)fixtures/check_protected/_*'
rc=0            (HEAD=c16b014)
rc_P8_head=0    (HEAD=eab8ce6)
```

Пин не «выбранный HEAD»: моя мера подтверждает, что `b38e78a` — ПОСЛЕДНИЙ
коммит, тронувший хоть один из 8 глобов:

```
$ git log --oneline -3 -- <те же 8 глобов>
b38e78a architect: контракт 040 Б1 — red_predel_git_vyzovov.sh переведён на внешнюю strace-трассировку…
fac8fe6 040 v4: исполнение Границы v4 арбитража 040-II (доверенный PATH…)
1b67f79 040 v3 fix: probe_ci_dostizhimost_predelov.sh — ИЗЙ1/ИЗЙ2
```

Машинная мера заморозки — живьём, строка 040 дословно требуемая:

```
$ bash scripts/check_contract_frozen.sh .
  ok   contracts/040-batching-git-vyzovov.md — заморожен v2, блоб совпадает побайтово, вердикты v1..v2 разрешают
планов и контрактов на HEAD: 44 · черновиков: 3 · заморожено: 41 · реестр: full
rc=0
```

**Подпись владельца — в теле ИМЕННО того коммита, которым внесён текст:**

```
$ git log -1 --format='author=%an committer=%cn' 4895299
author=architect committer=owner
$ git log -1 --format=%B 4895299 | grep -n '^РАЗРЕШИЛ-ВЛАДЕЛЕЦ:'
32:РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/040-batching-git-vyzovov.md актуализация пина §Приёмка Р8
   (1b67f790 -> b38e78a, последний коммит запиненного пространства из 8 глобов); коррекция
   точки отсчёта, НЕ ослабление — то же пространство и семантика; санкция — критик-accept
   contracts-040-v2 + закрытие находки Б2 ревьюера (review-k1); …
rc_grep=0
```

`author=architect` сохранён — это существенно, а не косметика: `check_zones.sh`
судит зону по АВТОРУ (`:564`, `git log --no-walk --format='%H%x00%an%x00'`, поле
`%an`), и правка `contracts/040-*.md` легальна именно как architect-коммит.
`committer=owner` несёт подпись. Живой `check_zones.sh` на дереве это
подтверждает механически (rc=0, ниже).

**Критик судил ТОТ ЖЕ текст, что заморожен.** Цепочка блобов, моя мера:

```
$ for r in 4895299 f225423 c16b014 HEAD 'frozen/contracts/040/2^{commit}'; do git ls-tree $r -- contracts/040-batching-git-vyzovov.md; done
все пять: 100644 blob ecca7ff30fb00f2e48cce3d10e365aca0d7b9703
$ git cat-file -p frozen/contracts/040/2 | head -4
object f22542399713187eef30d9033d4cacc3eb74efda   type commit   tag frozen/contracts/040/2
tagger orchestrator <orchestrator@dev-harness.local>
$ grep -n '^040' registry/contracts.tsv
44:040 → ccabba523d1e38d057a27a821954381fc93e4bd8      (= rev-parse frozen/contracts/040/2)
```

Вердикт критика (`verdicts/critic/contracts-040-v2.md`, `f225423`, accept) назвал
предметом суда коммит `506d03f` из СВОЕГО клона с родителем `9bcb846`; на `main`
текст приземлён как `4895299` с родителем `1d4518e` (ребейз), поэтому SHA
коммита-предмета в основном дереве не разрешается. Сверил не по рассказу, а по
привязкам вердикта к строкам: `:404` — строка Р8 с `b38e78a`; `:480` — дословный
контрфакт `b38e78a~1`; `:39-41` — «ТЕМ ЖЕ коммитом» (его СОВЕТ 2); `:393` —
«подтверждено ниже).» с лишней закрывающей скобкой (его СОВЕТ 1). Все четыре
якоря совпадают с замороженным блобом `ecca7ff3` побайтово в этих местах —
судился тот же текст. Оба совета критика неблокирующие и в предмет Б2 не входят.
Б2 закрыт.

---

## Б3 (БЛОКЕР, наследует О1 круга 1). «Условие done» контракта не исполнено: записи о живом наблюдении CI в `HANDOFF.md` нет — а материал для неё ЕСТЬ и проверен

Контракт (раздел «Условие done») дословно: «Отсутствие этой записи в `HANDOFF.md`
— самостоятельный блокер `done`, РАВНОЗНАЧНЫЙ красной команде §Приёмка:
`done_contract.sh`/ревьюер обязаны требовать её явно». Требую явно второй раз.
Моя мера на актуальном `origin/main` (`eab8ce6`):

```
$ grep -n 'actions/runs' HANDOFF.md
(пусто)  grep_rc=1
$ grep -n -i 'call-budget\|35878468996' HANDOFF.md
410:P1/P3/P8 не ослаблены (141/141 call-budget не вырос). **Adversary040v2 диспетчен, в полёте.**
610:наблюдение зелёного CI на реальном origin-прогоне НОВЫХ call-budget-шагов, SHA+ссылка — в
```

Строка 610 — формулировка самой ОБЯЗАННОСТИ, не её исполнение; ни SHA прогона,
ни ссылки в файле нет. В отличие от круга 1, теперь это блокер, а не замечание:
пачка финальная (Б1/Б2 закрыты), и материал для записи существует — я сам его
проверил выше (прогон `35878468996`, `success`, `head_sha=9bcb846`, шаг
call-budget `success`, 3м06с; ссылка
`https://github.com/a3ka/dev-harness/actions/runs/35878468996`). Порядок,
который круг 1 требовал (сначала живая мера, потом наблюдение), соблюдён:
прогон взят НА `9bcb846`, то есть ПОСЛЕ strace-фикса `b38e78a`. Закрытие —
механическое: внести SHA+ссылку в `HANDOFF.md`. Что не годится — объявить
обязанность исполненной без файла: контракт называет отсутствие записи красной
командой.

## Б4 (БЛОКЕР для `done` этой пачки, вне предмета 040). Джоба `ci` на `main` КРАСНАЯ на HEAD — четвёртый прогон подряд, и падает грамматика наблюдений, а не 040

Моя мера, API прогонов `main` (новейшие сверху):

```
35887841184 push completed/failure   eab8ce6 2026-09-23T16:18:32Z   <- HEAD
35887603055 push completed/cancelled c16b014 2026-09-23T16:16:28Z
35885464391 push completed/failure   f225423 2026-09-23T15:58:32Z
35880508920 push completed/failure   1d4518e 2026-09-23T15:17:59Z
35878468996 push completed/success   9bcb846 2026-09-23T15:01:19Z   <- последний зелёный
```

Падающий шаг во всех трёх failure-прогонах один и тот же:

```
$ curl -s .../runs/{35880508920,35885464391,35887841184}/jobs | jq …
JOB ci :: failure
   STEP Грамматика наблюдений (контракт 015)
```

Воспроизведено локально на HEAD, моя мера:

```
$ git checkout -q origin/main   # eab8ce6
$ bash scripts/check_nabludenia.sh .
  FAIL А-235: адрес не называет цель (Н-39-рецидив: unicode-склейка к якорю — круг 4 критик, круг 5 блокер 1)
rc=1
```

Источник — `1d4518e` (architect, запись А-235, приземлена оркестратором в ходе
сессии закрытия Б2): с неё `main` красен и красен до сих пор. К технике 040 это
не относится НИКАК — ни один барьер контракта от этого не краснеет (все зелёные
прогоны ниже сделаны на дереве с этим же дефектом). Но для `done` 040 это
блокирует именно то, что контракт требует своим последним разделом: «живое
наблюдение ЗЕЛЁНОГО CI» на HEAD сейчас недоступно, поскольку джоба `ci`
красная. Классифицирую блокером ПАЧКИ, а не предмета: закрытие — правка адреса
А-235 под грамматику `check_nabludenia.sh` (зона architect, один коммит), после
чего либо берётся новое наблюдение на зелёном HEAD, либо в `HANDOFF.md`
фиксируется уже состоявшийся прогон `35878468996` (Б3) с явным указанием SHA
`9bcb846` — второй путь контракту не противоречит, он требует ОДНОРАЗОВОГО
живого наблюдения нового шага, а не наблюдения на последнем HEAD.

---

## Замечания

**О1 (замечание, следа в предмете не оставляет, но мешает читать спор).**
Путь `verdicts/critic/contracts-040-v2.md` переиспользован под ДРУГОЙ документ.
Его история, моя мера:

```
f225423 critic 2026-09-23 critic: accept 040 v2 — актуализация пина Р8 без ослабления   (+130 строк, файла не было)
4b3666f critic 2026-09-22 040: консолидация вердиктов по конвенции — … v2/v3/v4 в историю
ff7cb19 critic 2026-09-22 critic: контракт 040 v2 — FAIL, измеренные обходы приёмки
```

Замороженный текст контракта (преамбула) ссылается на этот путь как на FAIL
круга 2 критика — «`verdicts/critic/contracts-040-v2.md`, `ff7cb19`»; на HEAD по
этому пути лежит ACCEPT v2-амендмента. Ссылка формально разрешима (SHA указан),
содержание старого вердикта достижимо по `ff7cb19`, ослабления нет — поэтому
замечание, не блокер. Цена: читатель, идущий по пути, а не по SHA, получит
документ с противоположным вердиктом. Переименование задним числом не предлагаю
(это правка чужого судебного файла); фиксирую коллизию имени.

**О2 (замечание).** Р1 стал зависеть от наличия `strace` на машине: без него
`rc=2 NOT_IMPLEMENTED` (предъявлено выше). Гард fail-closed — ненулевой код
валит шаг CI, «зелёного от недоступности утилиты» не возникает, и это прямо
лучше прежнего пусто-зелёного нуля. Но §Приёмка Р1 ожидает `rc=0`, и на рабочей
станции без `strace` этот пункт приёмки НЕ воспроизводим — судье пришлось
добывать трассировщик самому. Стоит назвать зависимость в тексте приёмки при
следующей же правке контракта (не ради этой находки открывать версию не нужно).

**О3 (замечание, подтверждение круга 1).** Триггер §A1-эскалация по-прежнему
пройден: `bash scripts/check_zones.sh .` — 55.59с моей мерой на 1892 коммитах
(круг 1: 57.9/64.4с). Порог контракта — 40с. Р7 (`timeout 60`) держится с
запасом 4.4с. Круг 1 это уже назвал (О2); повторяю как неизменившийся факт, а
не как новую находку.

**О4 (наблюдение, без действия).** А-232 («мера пуста») закрыт архитектором
собственной записью `f22031a` ПОСЛЕ landed-фикса, а не обещанием — порядок
правильный.

---

## Что ещё проверено и держится (сырой вывод, моя мера, `c16b014`)

```
$ bash scripts/check_zones.sh . | tail -1
замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1892 · проверено по зонам: 1272
rc=0   (55.587s)                                                    [Р5/Р7, зоны всех 7 новых коммитов]

$ bash scripts/verify_antiplacebo.sh --scope check_zones | tail -1
барьеров: 1 · фикстур: 29 · предъявлено красным повторным прогоном: 29
rc=0   (59.8s)

$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_zones | tail -1
БАТАРЕЯ check_zones: итог 4/4 классов закрыто
rc=0   (81.7s)

$ bash fixtures/check_protected/red_predel_git_vyzovov_ours.sh              [Р2]
git-подпроцессов: LOW(M=60)=29, HIGH(M=360)=29 · дифференциал 0<=15 · риски 1/2/3 подтверждены
rc=0

$ bash scripts/check_protected.sh . | tail -1                               [Р6]
коммитов пройдено: 2090 · существовало: 433 · на HEAD: 401 · исчезло: 0 · с разрешения: 23 · перенесено: 9
rc=0

$ bash fixtures/check_zones/probe_ci_dostizhimost_predelov.sh .             [Р9]
CI-ДОСТИЖИМОСТЬ ДЕРЖИТСЯ (СТРУКТУРНО, fail-closed): check:zones-call-budget -> джоба ci,
rc(npm)=2=rc(прямой)=2; check:protected-call-budget -> джоба ci, rc(npm)=0=rc(прямой)=0;
безусловный триггер push/pull_request; verify_ci_parity rc=0
rc=0

$ bash scripts/verify_ci_parity.sh | tail -1
workflow-команд: 42 · скриптов в приёмке: 54 · объявленных исключений: 23 · matrix-ключей: 44 ·
анти-плацебо-запусков: 2 · расхождений: 0
rc=0

$ bash scripts/check_provodka.sh . contracts/040-batching-git-vyzovov.md
rc=0

$ git diff --exit-code --diff-filter=MD --no-renames f0b6de66 HEAD -- ':(glob)fixtures/check_zones/case_*'      → rc=0   [Р3]
$ git diff --exit-code --diff-filter=MD --no-renames f0b6de66 HEAD -- ':(glob)fixtures/check_protected/case_*'  → rc=0   [Р4]
```

Р9 зелен по равенству кодов (`rc(npm)=2=rc(прямой)=2` — следствие О2 на этой
машине), структурная часть предиката — безусловный триггер, отсутствие `if:`/
`continue-on-error` — держится; на CI тот же шаг дал `success` (выше).

**Область правки между кругами — внутри границ, атомарность соблюдена.** Семь
коммитов после `d23086f`, каждый — один предмет, автор соответствует роли:

```
eab8ce6 orchestrator/orchestrator  NABLIUDENIA Н-138 (два поздних frozen-текст-дефекта)
c16b014 owner/owner                реестр заморозки 040 v2 (ccabba52)
f225423 critic/critic              accept 040 v2
4895299 architect/owner            040 v2-амендмент: Р8-пин поднят на b38e78a  (подпись владельца)
1d4518e architect/orchestrator     NABLIUDENIA_ARCHITECT А-235                  <- источник Б4
9bcb846 architect/architect        NABLIUDENIA_ARCHITECT А-234 адрес по грамматике
f22031a architect/architect        NABLIUDENIA_ARCHITECT А-232 закрыт
b38e78a architect/architect        040 Б1: внешняя strace-трассировка           <- закрытие Б1
```

Ни один судья не коммитил под именем автора работы; `scripts/` и `fixtures/`
после `b38e78a` не тронуты ни разу (Р8 rc=0 это и означает); норма
(`roles/*.md`, `AGENTS.md`) в этой пачке не тронута вовсе.

## Порог следующего круга

Предмет круга 3 ОГРАНИЧЕН двумя пунктами: (а) запись «Условие done» в
`HANDOFF.md` — SHA прогона и ссылка (Б3); (б) зелёная джоба `ci` на `main` либо
письменно обоснованная опора на уже состоявшийся прогон `35878468996` (Б4, через
правку адреса А-235). Б1 и Б2 закрыты этим вердиктом и не переоткрываются;
инженерное ядро предмета (batched-техника, дифференциальный критерий, доверенный
PATH, форма re-exec, семантика окон) проверено кругом 1 и подтверждено здесь
живыми прогонами — повторному суду не подлежит. Несогласие с Б3/Б4
предъявляется новым основанием, а не повтором той же меры.
