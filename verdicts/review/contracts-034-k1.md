# Review 034 — круг 1: перепроверка всей цепочки

**Вердикт: FAIL** — одна блокирующая находка (Б1). Механизм 034 как таковой предъявлен верно и
мной перепроверен исполнением (включая дифференциальные мутации); отказ вызван ПОСТОРОННЕЙ
правкой внутри диффа реализации, не дефектом probe-only класса.

Суд проведён в двух одноразовых клонах `/tmp/dev-harness-verify/rev-034/{repo,repo2}` на
`6ebb394d3bc04a9e597ae6cce32e1062528db521`; клоны удалены после снятия всех замеров.
Основной чекаут, ветки и main не трогались. Предмет: frozen 034/1 + `a2fee13` (implementer)
+ `5ac1478` (architect, квартет) + `37e6a57` (оркестратор, дериват) + вердикты критика
`39aa9fc`/`8998993` и адверсария `6ebb394`.

---

## Б1 — БЛОКИРУЮЩАЯ. Тихая посторонняя правка в диффе реализации ломает дословную ветвь замороженного 006

Класс: **превышение объявленных границ предмета + молчаливая порча текста, который цитирует
замороженный и закрытый контракт**.

`a2fee13` несёт, помимо трёх инвариантов 034, четвёртый гунк, не относящийся ни к §2, ни к
`--changed`, ни к селектору:

```
@@ -728,7 +759,7 @@ if [ "$before" != "$after" ]; then
-  printf '       (соседный агент, редактор) — прогон недостоверен целиком, а не в одной строке\n' >&2
+  printf '       (соседний агент, редактор) — прогон недостоверен целом, а не в одной строке\n' >&2
```

Наблюдаемое (мои прогоны в клоне):

```
$ grep -n 'недостоверен цел' scripts/verify_antiplacebo.sh
762:  printf '       (соседний агент, редактор) — прогон недостоверен целом, а не в одной строке\n' >&2

$ grep -n 'недостоверен целиком' contracts/006-scoped-izolirovannyj-gejt.md fixtures/check_scoped_run/_ref_va.sh
contracts/006-scoped-izolirovannyj-gejt.md:49:   громкое красное «прогон недостоверен целиком» (не false-green) — сохранённая ветвь.
fixtures/check_scoped_run/_ref_va.sh:491:  printf '       (соседний агент, редактор) — прогон недостоверен целиком, а не в одной строке\n' >&2

$ git tag -l 'frozen/contracts/006/*' 'done/contracts/006/*'
done/contracts/006/1
frozen/contracts/006/1
```

Почему это отказ, а не замечание:

1. `contracts/006:49` — текст ЗАМОРОЖЕННОГО И ЗАКРЫТОГО контракта — называет «прогон
   недостоверен целиком» дословно и квалифицирует как **«сохранённая ветвь»**. Строка вывода
   есть единственное наблюдаемое этой ветви. Она изменена исполнителем ДРУГОГО контракта,
   вне предмета его задания, без единого слова в сообщении коммита. Это ровно класс «норма
   ослаблена тихо»: сам нормативный файл не тронут, но его наблюдаемое — тронуто.
2. Правка **ухудшает** текст: «недостоверен целом» неграмматично. Ни один инвариант 034 её не
   требует; ни одно предъявление 034 её не проверяет.
3. Расхождение с независимым референс-раннером `fixtures/check_scoped_run/_ref_va.sh:491`
   (семья ЧУЖОЙ заморозки, вне зоны 034) введено молча. Механически оно сегодня безвредно
   (`_ref_va.sh` — самостоятельная реализация, не байт-зеркало), но это уже второй разъезд
   текста в дереве.
4. Ни один барьер её не ловит: в 1366 с полного прогона (семьи `check_approval`…`check_metering`)
   ни одной строки FAIL, зона автора не нарушена — `scripts/verify_antiplacebo.sh` в зоне
   implementer 034. То есть механики, которая остановила бы такую правку, НЕТ; останавливает
   её только судья.
5. Это последняя сеть перед первым автономным минтом. Пропущенная правка «по пути» внутри
   предметного диффа становится прецедентом ровно в момент, когда прецеденты наследуются.

**Средство**: один коммит implementer — возврат слова `целиком` на строке 762. Ни переделки
механизма, ни пересмотра приёмки, ни нового круга архитектора не требуется. Всё остальное в
работе 034 мной принято (ниже — сырьё).

---

## Не блокирующие находки

### З1. Приёмочный критерий 2 не предъявлен НИ ОДНИМ судьёй (класс: дефект приёмки, не предмета)

`bash scripts/verify_antiplacebo.sh` → rc не получен ни разу:

- адверсарий (`verdicts/adversary/contracts-034-v1.md`, п.2): обрыв по лимиту 600 s;
- я: `real 22m46.476s`, **rc 143** (SIGTERM), дошёл до семьи `check_metering`, все показанные
  строки `ok`, ни одной `FAIL`;
- изолированный `--scope check_staged` у меня тоже не завершился: **1200 s, interrupted**
  (последние строки `ok check_staged/case_staged_sentr_{D,M,R}.sh`). Заявленное адверсарием
  «rc 0, 28/28» я СВОЕЙ мерой подтвердить не смог — не опровергнуто, но и не подтверждено.

Существо критерия при этом закрыто другой мерой, и это важно зафиксировать: **§2 судит КАЖДЫЙ
каталог `fixtures/` ДО scoped-фильтра** (`scripts/verify_antiplacebo.sh:200-225`), поэтому
rc 0 критерия 1 (`--scope check_zones`) уже доказывает отсутствие ec11c86-красноты во ВСЁМ
дереве, а не только в семье `check_zones`. Красноты probe-каталогов нет.

Что названо: замороженная приёмочная команда физически неисполнима в тайм-боксе судьи
(≈30 мин) на этой машине — у неё нет ни бюджета, ни разбиения. Это свойство приёмки 034,
унаследованное будущими контрактами, и оно уже дважды стоило кругов.

### З2. Приёмочный критерий 6 «CI на собранном дереве зелёный» НЕ выполнен — по причине вне 034

```
$ npm run --silent check:zones ; echo RC=$?
  FAIL коммит вне зоны: implementer 136ab1f0 fixtures/check_staged/case_dver_minta_priznanie.sh …
  FAIL коммит вне зоны: implementer 67f7437c fixtures/check_staged/case_dver_minta_priznanie.sh …
  FAIL коммит вне зоны: implementer b11943e1 fixtures/check_staged/case_dver_minta_priznanie.sh …
замороженных контрактов: 32 · объявленных авторов: 3 · коммитов в диапазонах: 1193 · проверено по зонам: 766
RC=1
```

`check:zones` — шаг CI (`.github/workflows/ci.yml:246`). Все три коммита — предки `6c4adc5`,
т.е. ДО 034 (проверено `git merge-base --is-ancestor <sha> 6c4adc5` → ДА ×3). Это 031-v3
переходник (Н-113) — тот самый, из-за которого ворота 1 `red_zamer_podstavnoj_schetchik.sh`
именованно пропускают rc-ветвь.

Вывод для владельца/оркестратора: **`done/contracts/034/1` НЕ означает «CI зелёный»**.
Замороженный критерий 6 недостижим силами 034; его измеримое существо («ec11c86-класса
красноты probe-каталогов больше нет ни в одном шарде») — выполнено.

### З3. Заявление задания «overlay rc 0» читается неверно (своя мера дала другое)

```
$ npm run --silent check:overlay ; echo RC=$?
  ok   пин совпал: omp/18.1.18
  ok   агенты соответствуют roles/
  FAIL модель vision-тира MiniMax-M3 не умеет картинки
ОТКАЗ: модель не соответствует задаче: запуск на ней даёт тихий брак, а не отказ
RC=1
```

Различение: `check:overlay` — НЕ шаг CI (`config/ci_parity_exceptions.txt:22` — «предмет
локальный: сверяет sha256 бинаря omp 185 МБ с пином, в CI бинаря и пина нет»). В CI зелен
анти-плацебо ключ `overlay` шарда ap2:

```
$ bash scripts/verify_antiplacebo.sh --scope overlay
  ok   overlay/case_pin_razoshelsja.sh: … красный кодом 1 — «харнес разошёлся с пином»
барьеров: 1 · фикстур: 1 · предъявлено красным повторным прогоном: 1
RC=0
```

Дериват синхронизирован — строка `ok агенты соответствуют roles/` и `npm run check:gen` →
`харнес соответствует roles/ (9 ролей)`, rc 0 (детектор стенограммы Н-112: последний
`--check` rc 0). Остаточный FAIL `check:overlay` — вопрос `.omp/models.yml`, вне зоны и вне
предмета 034. Названо, чтобы «overlay rc 0» не ушло в сводку как выполненное целиком.

### З4. `is_probe_only_legal` продублирован — второе место истины, ничем не связанное

```
$ grep -n 'is_probe_only_legal()' scripts/verify_antiplacebo.sh scripts/scope_select.sh
scripts/verify_antiplacebo.sh:195:is_probe_only_legal() {  # <каталог>
scripts/scope_select.sh:61:is_probe_only_legal() {
```

Контракт сам отверг «реестр ключей в отдельном конфиге — второе место истины, разъезжается»
(frontier п.1) и назвал эту пару «зеркалом». Зеркало ничем не держится. Мутация M4
(ослабление `[ -s ]` → `[ -e ]` в ОДНОЙ копии, `verify_antiplacebo.sh:197`) не покраснила
ничего:

```
M4_granicy_RC=0      (ворота 1/2/3 зелены)
M4_katalog_RC=0      (ворота 1–5 зелены)
```

Следствия два: (а) расхождение двух копий не наблюдаемо; (б) frontier-решение «маркер непуст»
(отвергнутое «объявление без причины неотличимо от опечатки») не держится ни одним
предъявлением — обход адверсария «пустой `.probe-only` → rc 1» верен для текущего кода, но
не защищён воротами. Не нарушение инварианта (в §Инварианты непустоты нет) — назван риск.

### З5. `red_zamer` ворота 1 остаются именованным пропуском

`ворота 1: живое дерево в переходнике 031-v3 … — именованный пропуск rc-ветви; счётчик жив: 766`.
Это ровно то, что заморожено в §Замеры (там `N=757`, рост до 766 — прирост коммитов, не
расхождение). Пропуск честен и назван, но приёмка 034 на живом дереве rc-ветвь не мерит:
ворота 2–5 несут всю различающую силу. Назван как унаследованный долг 031-v3.

---

## Что перепроверено и принято (сырьё)

### Заморозка — rc 0
```
$ git diff --exit-code frozen/contracts/034/1 HEAD -- contracts/034-*.md ; echo rc=$?
rc=0
```

### Приёмочные команды — мои прогоны

| # | команда | мой rc | наблюдаемое |
|---|---|---|---|
| 1 | `verify_antiplacebo.sh --scope check_zones` | **0** | `барьеров: 1 · фикстур: 21 · предъявлено красным повторным прогоном: 21`; ни одного §2-FAIL по всему дереву |
| 2 | `verify_antiplacebo.sh` (полный) | **143 (SIGTERM, 22m46s)** | см. З1 — rc не получен; до обрыва ни одной FAIL, дошёл до `check_metering` |
| 3 | `red_katalog_ne_flagaetsa.sh` | **0** | ворота 1–5 поимённо, включая к1-A (`нулевая выборка не легализует нелегальный каталог — rc 1`) и к1-B (`селектор needs-full/2 без KEY, раннер MODE: none без case`) |
| 4 | `red_granicy_klassa.sh` | **0** | ворота 1/2/3: без маркера → «фикстура без барьера»; маркер+case → «роль каталога одна»; маркер в барьерном → «противоречие не молчит» |
| 5 | `red_zamer_podstavnoj_schetchik.sh` | **0** | ворота 2 `toy K=3, счётчик 3`; ворота 3 `старый N>0-гвард ПРИНИМАЕТ подмену (N=1)`; ворота 4 `известный ответ 5 ≠ подменённый 1 — гвард КРАСЕН`; ворота 5 `на абсолютном корне подмена честна (5)` |
| 6 | CI зелёный | **нет** | см. З2 (`check:zones` rc 1, причина вне 034) |

### Красное предъявлено — СВОИ дифференциальные мутации (не повтор чужих прогонов)

Роль требует красного против сломанной реализации. Предъявил четыре мутации предмета; каждая
возвращалась `cp`-восстановлением, `git status --porcelain` после — пуст.

```
M1  снят  is_probe_only_legal "$d" && continue      (verify_antiplacebo.sh:211)
    → red_katalog RC=1: «ОТКАЗ: ворота 1 — rc 1 / FAIL fixtures/probe_subject: probe-only каталог с case_*.sh»

M2  снят  [ "$fails" -gt 0 ] && exit 1              (verify_antiplacebo.sh:271, fail-closed early exit)
    → red_katalog RC=1: «ОТКАЗ: ворота 4 — rc 0 / FAIL fixtures/stranaja: фикстура без барьера / MODE: none»
       — ровно контрпример A критика к1 воскрешён и пойман

M3  возвращено keys="$keys $k" безусловно          (scope_select.sh:198)
    → red_katalog RC=1: «ОТКАЗ: ворота 3 — rc 1 / FAIL scope_select вернул scoped, но ни один барьер
       не попал в выборку (want=probe_subject)» — контрпример B пойман

M4  [ -s ] → [ -e ] в ОДНОЙ копии предиката        (verify_antiplacebo.sh:197)
    → red_granicy RC=0, red_katalog RC=0  — НЕ пойман (см. З4)
```

Вывод: ворота 1, 3, 4 несут подлинную различающую силу — доказано красным против сломанной
реализации, а не зелёным на честной. Ворота 5 и 2 держатся выводом (`MODE: needs-full`/rc 2
без `KEY:`, `MODE: none` без исполненного case) — форма наблюдаемости соблюдена.

### Проверки не переписаны под реализацию

```
$ git log --format='%h %an %s' -- fixtures/verify_antiplacebo_probe/
5ac1478 architect  (034 пост-фриз приземление probe-квартета)

$ git log --format='%h %an …' -- fixtures/check_zones/     # верхушка
468403b architect  (034 черновик: red_zamer предъявление)
67f7437 implementer (031, mv — ДО 034, вне предмета)

$ git log --format='%h %an …' -10 -- scripts/verify_antiplacebo.sh scripts/scope_select.sh
a2fee13 implementer (034)   — единственный 034-коммит в scripts/
```

Фикстуры и предъявления 034 — только архитектор (`5ac1478` 20:37, `468403b`), реализация —
только implementer (`a2fee13` 20:57). Порядок верен: предъявления легли ДО реализации; после
`a2fee13` ни один файл предъявлений не тронут. Подгонки проверки под код НЕТ.

### Область правки (кроме Б1) и атомарность

```
$ git show --stat a2fee13
 scripts/scope_select.sh       | 19 ++++++++++++++++++-
 scripts/verify_antiplacebo.sh | 37 ++++++++++++++++++++++++++++++++---
$ git diff --stat 576f66b 71c6a08      # land не принёс постороннего
 scripts/scope_select.sh | 19 … ; scripts/verify_antiplacebo.sh | 37 …
```

Зоны 034 соблюдены: `check_zones` не флагает ни одного коммита 034 (три его FAIL — предки
`6c4adc5`, З2). Один предмет — один коммит у каждого исполнителя, сообщения называют предмет
и инварианты. Норма (`roles/`, `contracts/*`, `registry/`) внутри 034 не тронута; `37e6a57`
правит только дериват `.omp/agents/orchestrator.md` под уже приземлённую норму 031/2
(`gen-harness --check` rc 0) — легитимная проводка, не изменение нормы.

### Заявленное равно сделанному (моя мера, другой командой)

- адверсарий: «8 исполненных обходов» → `grep -c '^- \*\*обход' verdicts/adversary/contracts-034-v1.md` → **8**. Сходится.
- адверсарий: «`--scope check_zones` rc 0, 21/21» → мой прогон **rc 0, 21/21**. Сходится.
- адверсарий: «`--scope check_staged` rc 0, 28/28» → **не воспроизведено** (1200 s, interrupted). Не подтверждено (З1).
- адверсарий честно НЕ заявил полную зелень и CI, назвав обрыв 600 s. Это правильное поведение,
  и оно перепроверено: у меня тот же класс обрыва.
- вердикты закоммичены под своими именами: `6ebb394` author=adversary, `8998993`/`39aa9fc`
  author=critic. Судья, неотличимый от автора, отсутствует.

---

## Резюме

Механизм 034 — probe-only класс в обоих режимах, fail-closed переживание нулевой выборки,
наблюдаемый нуль выбора, известный ответ против подставного счётчика — **верен и предъявлен
красным против сломанной реализации моей собственной мерой**. Круг 2 нужен ровно для одного:
изъять постороннюю правку строки 762 (Б1). Остальные пять находок — замечания и
унаследованный долг, они не требуют круга и адресованы владельцу/оркестратору.

Стороной спора не становлюсь: находка одна, круг первый, арбитр не нужен.
