FAIL

# Контракт 029 — ревьюер, круг 1 (ПОЛНЫЙ предмет)

Предмет суда: вся цепочка 029 — механизм (два барьера), норма-проводка, судейские круги,
реализация против §Предмет замороженного `frozen/contracts/029/1`. Не диф последнего фикса.

Прогоны — в одноразовом клоне `/tmp/dev-harness-verify/rev-k1-029/repo` на `b5019ae`;
мера закрытия дополнительно снята на ЖИВОМ корне `/home/aka/Documents/dev-harness`
(`--schet` — чтение, дерево не мутировалось, `git status --porcelain` пуст до и после).

Вердикт: **FAIL**, один блокер и его прямое следствие. Механизм построен и выдерживает
обманный стаб; НЕ ВЫПОЛНЕН приёмочный критерий, который сам замороженный контракт
поставил условием close-out.

## Б1 (БЛОКЕР) — приёмочный критерий «живая приёмка меры закрытия» не выполнен

Место: `contracts/029-auto-konsultant-forkov.md` §Приёмочный критерий, абзац «Живая
приёмка меры закрытия (инв. 5)», дословно:

> Живая приёмка меры закрытия (инв. 5): на close-out 029 — `bash scripts/check_fork_route.sh
> --root <живой корень> --schet` печатает `просмотрено N` при N > 0 и `передаточных M`
> при M ≤ 1; число заносится строкой в HANDOFF.

Измерено (сырой вывод ДОСЛОВНО, живой корень `/home/aka/Documents/dev-harness`):

```text
$ bash scripts/check_fork_route.sh --root . --schet
просмотрено 0
rc=1
```

Тот же вывод и rc в клоне на `b5019ae`. `forks/` не существует ни в дереве, ни в индексе
(`git ls-tree -r --name-only HEAD | grep -c '^forks/'` → `0`; `ls -d forks` → `No such file
or directory`). Строки `передаточных M` в выводе НЕТ вовсе — второе из двух обязательных
чисел не напечатано. В `HANDOFF.md` строки с числом нет: `grep -c 'просмотрено' HANDOFF.md`
→ `0`.

Итог по букве критерия: требуется `N > 0` — получено `N = 0`; требуется печать `M ≤ 1` —
`M` не напечатан; требуется строка в HANDOFF — строки нет. Три из трёх не выполнены, rc
барьера 1, а не 0.

### Суд по §Предмет: требует ли предмет живой записи ДО done — ДА

Оркестратор поставил развилку: «требует ли предмет живой записи/смоука целого маршрута до
done, либо проводка+барьеры+фикстуры достаточны, а эксплуатация наступает при первом
реальном форке». Решаю текстом контракта, не соображением.

1. Чтение «эксплуатация наступает потом» опровергается самим §Приёмочный критерий: живая
   приёмка привязана к **`на close-out 029`**, а не к «первому реальному форку». Close-out —
   это ровно текущий шаг (тег `done/contracts/029/1`). Критерий, поставленный на close-out,
   нельзя удовлетворить после close-out: тогда он не критерий, а пожелание.

2. §Предмет, инв. 5 называет счётчик **«МЕХАНИЧЕСКАЯ МЕРА ЗАКРЫТИЯ Н-104»** и ставит цель
   предмета `M за сессию 5 → 0–1`. Мера закрытия боли — часть предмета, а не отчётность о
   нём: боль Н-104 измерена числом (8 касаний, 5 передаточных), и закрытие измеряется тем же
   числом. При `просмотрено 0` величина `M` не наблюдаема вовсе — ни `0`, ни `1`, ни `5`.
   Заявить достижение цели `5 → 0–1` нечем.

3. Инв. 5 ПРЯМО называет текущее состояние main красным и объясняет, почему: «**Пустая
   выборка — красное**: журнал отсутствует или пуст → rc 1 с печатью `просмотрено 0`
   («нечего проверять» ≠ «проверено»: молчаливый ноль выдал бы **невключённый механизм за
   достигнутую цель**)». Контракт предвидел ровно этот исход и сделал его отказом. Поставить
   `done` на состоянии, которое собственный барьер предмета объявляет красным по названной
   причине «невключённый механизм», — это и есть та подмена, против которой инвариант писан.

4. Барьер сейчас работает ПРАВИЛЬНО. `rc=1` — не дефект реализации, это верное показание:
   механизм не введён в эксплуатацию. Чинить нужно не барьер.

Прецедент 012 приложен верно: барьер + фикстуры без эксплуатации = ложное done. Заметим, что
это записано не мной задним числом, а самой приземлённой нормой — см. Б2.

**Выход (Б1).** До `done/contracts/029/1`: завести в `forks/` живые записи по грамматике
§Предмет — минимум те, что покрывают уже состоявшиеся касания владельца этой сессии (см. Б2),
прогнать `bash scripts/check_fork_route.sh --root .` (ожидание rc 0 либо поимённый отказ,
разобранный по существу), затем `--schet` до состояния `просмотрено N` при `N > 0` и
`передаточных M` при `M ≤ 1`, и занести сырое число строкой в `HANDOFF.md`. Заведение записей
и строка в HANDOFF — ЗОНА orchestrator по 029 (`HANDOFF.md`); `forks/` ни одному автору не
объявлен ни в 029, ни в прежних заморозках — путь новый, и его первичное заполнение идёт
каналом владельца/оркестратора по его слову, как шли реестр и `.omp/`.

## Б2 (БЛОКЕР, тот же выход, что Б1) — приземлённая норма не действует в собственной сессии

Место: `roles/orchestrator.md:53-83`, секция «Эскалация владельцу по форку — запись и барьер
(Н-104/029)», приземлена `9c57c81` → land `b0e44c8` (2026-09-18 20:48:23 +0200). Дословно
строки 55-57:

> Перед ЛЮБЫМ касанием владельца по предмету форка — обязательный чеклист
> (пропуск = ложное done, прецедент 012: барьер+фикстуры 029 есть, но не
> эксплуатируются — реализация без использования, поймана ревьюером).

После приземления нормы (всё, что ниже, — строго позже `b0e44c8`) сессия касалась владельца
не менее трёх раз, и НИ ОДНОЙ записи `forks/<id>.md` при этом не заведено:

| когда | касание | источник |
|---|---|---|
| 2026-09-18 23:00:56 | санкция владельца на созыв арбитра по TCB-скоупу | `56f58c8`; HANDOFF «арбитр созван мной на кап (мой звонок, санкция владельца получена явно)» |
| 2026-09-19 00:15:19 | слово владельца «не дублировать абзац арбитра в замороженный контракт» | `f1f8ba3` (тема коммита: «без правки замороженного контракта — слово владельца 2026-09-19») |
| 2026-09-19 01:05:06 | четыре инструкции ночи (очередь/консультант/чекпойнт/параллелизм) | `47ee668`, HANDOFF «Владелец подтвердил план … и добавил ЧЕТЫРЕ инструкции» |

Второе и третье — законные касания маршрута (б) (норма/приоритет — зона воли владельца), и
именно поэтому они обязаны были дать записи с `КЛАСС: воля-владельца`, `ПРИЗНАК` из перечня,
`АВТОР` и свидетельством второй роли: чеклист покрывает **ЛЮБОЕ** касание, а не только
незаконное. Норма, которую её собственная сессия не исполнила ни разу за 14 часов и три
касания, приземлена как текст, но не как правило.

Это не отдельный выход: он совпадает с выходом Б1 — завести записи и предъявить `--schet`
с `N > 0`. Назван отдельно, потому что наблюдаемое иное: Б1 — непройденный критерий, Б2 —
неработающая норма, и починка только счётчика (например, одной синтетической записью) Б2 не
закрывает.

## Что ПРОВЕРЕНО и ЗЕЛЕНО (эти классы к FAIL отношения не имеют)

### П1. Красные предъявления живы и судят БАРЬЕР, а не себя

Своя мера, а не повтор адверсария: обоим барьерам подставлен обманный стаб
`#!/usr/bin/env bash\nexit 0` (всегда-зелёный), все девять красных прогнаны против него.

```text
# на реальной реализации b5019ae            # против стаба «exit 0»
red_marshrut_a_inzhenernyj.sh   rc=0        red_marshrut_a_inzhenernyj.sh   rc=1
red_marshrut_b_volja_batch.sh   rc=0        red_marshrut_b_volja_batch.sh   rc=1
red_marshrut_v_soreview.sh      rc=0        red_marshrut_v_soreview.sh      rc=1
red_flush_batcha.sh             rc=0        red_flush_batcha.sh             rc=1
red_schet_peredatochnyh.sh      rc=0        red_schet_peredatochnyh.sh      rc=1
red_zapret_dvojnoj_roli.sh      rc=0        red_zapret_dvojnoj_roli.sh      rc=1
red_degradacija_taimer.sh       rc=0        red_degradacija_taimer.sh       rc=1
red_deko_bez_rc.sh              rc=0        red_deko_bez_rc.sh              rc=1
red_lozhnyj_rc.sh               rc=0        red_lozhnyj_rc.sh              rc=1
```

Девять зелёных И девять красных предъявлены. Клон восстановлен, `git status --porcelain` пуст.

### П2. Проверки не подогнаны под реализацию

`git log -- fixtures/check_fork_route/ fixtures/verify_consultant/` за всю историю путей:
`5056174`, `c85274f`, `c5dd05b`, `f1f8ba3` — **architect**; `d5c69a3` — **implementer**, и
только три `case_*`, прямо назначенные ему ЗОНА-строкой 029. Ни одного красного предъявления
архитектора автор реализации не трогал. Привязки обманных стабов лежат комментариями кода
проб (`S-openlist`, `S-presence`, `S-classonly`, `S-selfdecl`, `S-filethere`, `S-modelonly`,
`S-noauthor`, `S-allred` в `red_marshrut_b_volja_batch.sh`), каждый назван у входа, где его
дефект наблюдаем; проза контракта их не несёт. Н-39 дословно: «стабы к ветвям привязывает
architect по коду, НЕ проза контракта; контракт несёт инварианты + rc-команды» — исполнено.
У каждой пробы есть позитивный контроль (вход 1 в `red_marshrut_b_volja_batch.sh` — rc 0),
так что «всегда красный» исключён конструкцией самой пробы.

### П3. Замороженный текст не тронут

`git diff --exit-code frozen/contracts/029/1 HEAD -- contracts/029-auto-konsultant-forkov.md`
→ rc=0, вывод пуст. Абзац решения арбитра в контракт НЕ внесён (слово владельца, `f1f8ba3`) —
норма замороженного документа сохранена, суд источника истины остался за вердиктом арбитра.

### П4. Механизм и проводка на месте

* `npm run check:antiplacebo -- --scope check_fork_route verify_consultant` → rc=0;
  «барьеров: 2 · фикстур: 3 · предъявлено красным повторным прогоном: 3».
* `npm run gen:harness -- --check` → rc=0, «харнес соответствует roles/ (9 ролей)».
* Тир `consultant` в реестре есть (`model: openai-codex/gpt-6-astra`, `fallback:
  anthropic/claude-opus-5`), маппинг `"consultant": "consultant"` в `roles` есть;
  `roles/consultant.md` и дериват `.omp/agents/consultant.md` существуют.
* CI-шард: `.github/workflows/ci.yml:67` — `ap4` несёт `check_fork_route verify_consultant`;
  `bash scripts/verify_ci_parity.sh` → rc=0, «расхождений: 0».
* `bash scripts/check_zones.sh .` → rc=0 (28 замороженных контрактов, 710 коммитов проверено
  по зонам).

### П5. Своя мера счёта (не повтор чужой команды)

Инвентарь путей `git ls-tree -r --name-only HEAD -- fixtures/check_fork_route/
fixtures/verify_consultant/`: 7 `red_*` в `check_fork_route`, 2 `red_*` в `verify_consultant`
= **9**; `case_*` = **3**; каркасы `_forks.sh`, `_konsult.sh` = 2, в счёт не идут. Совпадает
с §Приёмочный критерий («9 … + 3») и с числами `verify_antiplacebo`. Расхождений нет.

### П6. Атомарность и каналы

Каждый коммит дельты несёт один предмет и ссылку на него; связок «пять задач одним коммитом»
нет. Owner-канал законен и предъявлен: `038b00d` (реестр), `c8659e0` и `1795ba7` (`.omp/`
дериваты) — автор `owner`, ровно те пути, что 029 §Зоны заморозил вне зон как зону владельца
(Н-91). Нормативные документы вне мандата 029 не тронуты.

## Замечания (НЕ блокеры; названы, потому что вердикт называет каждую находку)

**З1 — раздача работы 029 не соблюдена, но норму это не нарушает.** §Зоны 029 объявляет
`roles/orchestrator.md` зоной orchestrator и пишет «оркестратор несёт проводку нормы»; фактически
её сделал implementer (`9c57c81`). Он же правил `.github/workflows/ci.yml` (`bfcef61`), которого
ЗОНА implementer контракта 029 не содержит. `check_zones.sh` зелен — и по своей объявленной
семантике прав: «Зона автора — ОБЪЕДИНЕНИЕ путей, объявленных этому имени во ВСЕХ замороженных
контрактах»; `roles/orchestrator.md` дан implementer'у контрактом 012, `.github/workflows/ci.yml` —
контрактами 002/003/004/005/006/007/008/016/020/021/027. Выхода за зону НЕТ. Наблюдение для
владельца, не отказ: union-семантика делает пораздельную раздачу внутри одного контракта
необязывающей — «кто несёт проводку нормы» перестаёт быть механической мерой, как только
исполнителю тот же путь однажды выдан другим контрактом. Предмет для отдельного разбора, не для
029.

**З2 — применено не то, что предложено; законно, но должно быть названо.** §Предложение в реестр
моделей просит `"model": "anthropic/claude-fable-5"`, `"fallback": "openai-codex/gpt-6-astra"`.
Владелец применил обратное: `openai-codex/gpt-6-astra` основной, `anthropic/claude-opus-5`
фолбек (`038b00d`). Это прямо разрешено контрактом («здесь она ПРЕДЛОЖЕНА точным текстом …, а не
применена», правка реестра — зона владельца, Н-91), и инв. 6 от этого только прочнее: коллизия
`fable` (тир `plan` = арбитр) с тиром консультанта исчезает вовсе, а не разводится фолбеком.
Дефекта нет; расхождение зафиксировано, чтобы §Предложение не читали как описание реестра.

## Вне скоупа (не открывалось)

PATH/TCB-класс среды вызова (`verdicts/arbitration/tcb-granica-put-029.md` §4, РЕШЕНИЕ b);
достаточность границы Г1–Г4 (`granitsa-readonly-ispolnenija-029.md`); обходы кругов 1–5
адверсария и accept критика `328d9ef`; текст замороженного контракта; полный прогон всех
барьеров (CI 6/6 на `b5019ae`).

## Сводка прогонов

| команда | rc |
|---|---|
| `npm run check:antiplacebo -- --scope check_fork_route verify_consultant` | 0 |
| `npm run gen:harness -- --check` | 0 |
| `git diff --exit-code frozen/contracts/029/1 HEAD -- contracts/029-auto-konsultant-forkov.md` | 0 (пусто) |
| 9 × `bash fixtures/*/red_*.sh` (реальная реализация) | 0 каждый |
| 9 × `bash fixtures/*/red_*.sh` (против стаба `exit 0`) | 1 каждый |
| `bash scripts/check_zones.sh .` | 0 |
| `bash scripts/verify_ci_parity.sh` | 0 |
| `bash scripts/check_fork_route.sh --root . --schet` (клон `b5019ae`) | **1** |
| `bash scripts/check_fork_route.sh --root . --schet` (ЖИВОЙ корень) | **1** |

Сырое `--schet` ДОСЛОВНО:

```text
просмотрено 0
```

## Что закрывает FAIL

Живые записи в `forks/`, прогон `check_fork_route.sh --root .`, `--schet` с `просмотрено N`
(N > 0) и `передаточных M` (M ≤ 1), строка с этим числом в `HANDOFF.md`. После этого —
ревьюерский круг 2 по Б1/Б2 (остальное перепроверять не нужно: П1–П6 привязаны к блобам
`b5019ae` и новыми записями в `forks/` не затрагиваются).

Два отказа по одной причине созывают арбитра; это первый.
