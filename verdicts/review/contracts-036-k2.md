FAIL

# Ревьюер 036, круг 2 — подтверждающий круг после ре-фриза 036/2

Предмет: заморозка `frozen/contracts/036/2` (тег-объект `a534f72e`, коммит `81a2e52`,
блоб контракта `bce81ad8`), дельта владельца `fb4cedd` (блоб `4360258`), суд критика
`90bcafb` (`verdicts/critic/contracts-036-v4.md` — accept), реализация `a9610a7..1f933ec`.
Клон `/tmp/dev-harness-verify/rev-036-conf/repo`, пин HEAD `52a4aed`; контрольная сверка —
живой tip `origin/main` `2add934`.

Круг созван по моему вердикту к1 (`2cb78be`): требование было — «узаконить зону 4-й
фикстуры законным маршрутом, после чего `bash scripts/check_zones.sh` обязан быть `rc 0`
предъявленной стенограммой, и подтверждающий круг».

**Вердикт: FAIL.** Оба блокера круга 1 — **закрыты** (доказано ниже моей мерой). Отказ
несут ДВЕ НОВЫЕ находки, ни одна из которых не повторяет причину к1: один блокер лежит
ВНЕ 036 и запирает только приземление, второй лежит ВНУТРИ 036 и требует круга.

Второго отказа по той же причине здесь нет — арбитр по правилу «два отказа по одной
причине» не созывается: причины к1 (Б1 самосанкция зоны, Б2 мера отсутствует в приёмке)
сняты механизмом, а не повторной самосанкцией.

---

## ЗАКРЫТО-1 (было Б1 к1) — зона узаконена ре-фризом. Класс: закрыт механизмом

Замороженная дельта `frozen/036/1 → frozen/036/2` содержит ровно заявленное — сверено
моей мерой, `git diff frozen/contracts/036/1 frozen/contracts/036/2`:

```
-ЗОНА implementer: … fixtures/check_check_spec_ready/case_prichina_predmeta.sh fixtures/check_check_spec_ready/case_zamer.sh fixtures/check_check_spec_ready/case_perenos_zony.sh …
+ЗОНА implementer: … fixtures/check_check_spec_ready/ …
+СПАСЕНО implementer: b98896acabd9d810c7bc746c2dd6d0839b4fbc0a — 036/2 Б1: три поимённых case-файла поглощены каталогом семьи …
```

плюс снятие пост-фриз-двери v1 («каталог для избыточных red_* архитектора … этим
ре-фризом снята: семья едина и принадлежит исполнителю»), плюс ворота г6, плюс пункт
`check_zones` в «Зелёное ПОСЛЕ». Маршрут, который к1 называл законным (ре-фриз 036/2 с
каталожной ЗОНА-строкой), пройден ровно им.

**Красное-до / зелёное-после предъявлено моей мерой** (бисект по цепочке, каждый прогон —
живой `check_zones` по репозиторию клона):

```
$ for c in 2cb78be fb4cedd 90bcafb 1f933ec 2d70fc5 3e9a368 0717eda 52a4aed; do
      git checkout -q $c; bash scripts/check_zones.sh . ; done   # свод по FAIL-строкам
2cb78be  rc=1   FAIL … implementer 0a4336ac fixtures/check_check_spec_ready/case_symlink_vne_kornja.sh   (стенограмма к1)
fb4cedd  rc=0   FAILs: 0
90bcafb  rc=0   FAILs: 0
1f933ec  rc=0   FAILs: 0
2d70fc5  rc=0   FAILs: 0
3e9a368  rc=0   FAILs: 0
0717eda  rc=1   FAIL коммит вне зоны: orchestrator 0717eda5 registry/contracts.tsv — дверь минта 031: дельта манифеста не только-добавление
52a4aed  rc=1   (та же единственная причина)
```

Коммит `0a4336a`, бывший ЕДИНСТВЕННЫМ FAIL дерева на к1, на всём отрезке после `fb4cedd`
из отказов исчез; вместо него в стенограмме стоит строка признания:

```
  ok   контракт 036: коммит b98896ac (implementer) — СПАСЕНО, из суда зон выведен
```

Б1 закрыт: зона легализована замороженным текстом, покрытие переноса — СПАСЕНО, живой
потребитель `check_zones` грамматику принял.

## ЗАКРЫТО-2 (было Б2 к1) — мера внесена в приёмку. Класс: закрыт механизмом

Замороженный текст 036/2 теперь несёт и красное, и зелёное по этой линии (цитаты из
`frozen/contracts/036/2`):

```
- `bash scripts/check_zones.sh .` → красная: коммит вне зоны
  Ворота: г6 боль Б1/Б2 ревьюера 2cb78be — живой гейт зон ПО РЕПОЗИТОРИЮ, не toy…
…зоны репозитория зелены (Б2, добавка v2) — `bash scripts/check_zones.sh .` → rc 0
стенограммой по ВСЕМУ репозиторию, не только внутри toy-фикстур…
```

Мера, отсутствовавшая во всех семи судейских кругах v1, в приёмке есть. Требование к1
выполнено как ТЕКСТ обязательства; как ФАКТ на дереве — не выполнено, см. БЛОКЕР-1.

## Заморожённый текст не дрейфовал

```
blob@frozen/036/2: bce81ad8d1ff049e48f15802aab449987271a838
blob@3eb0ca8 (HEAD суда критика): bce81ad8d1ff049e48f15802aab449987271a838
blob@52a4aed (HEAD круга):        bce81ad8d1ff049e48f15802aab449987271a838
```

Тег аннотированный, тело несёт основание: «kritik v2-zona accept + RAZRESHIL vladelca
(owner fb4cedd)». Таггер `implementer` — прецедент общий для 032/1, 034/1, 035/1, 035/2,
036/1 (писатель тега — `freeze_contract`), находкой не является.

---

## БЛОКЕР-1. Класс: приёмочный пункт 036/2 не выполняется на живом дереве — причина ВНЕ 036

Приёмка 036/2 требует безусловно: «зоны репозитория зелены … `rc 0` стенограммой по ВСЕМУ
репозиторию». Стенограмма (пин круга и живой tip — одинаково):

```
$ bash scripts/check_zones.sh .                                   # HEAD 52a4aed
  FAIL коммит вне зоны: orchestrator 0717eda5 registry/contracts.tsv — дверь минта 031: дельта манифеста не только-добавление
замороженных контрактов: 35 · объявленных авторов: 6 · коммитов в диапазонах: 1446 · проверено по зонам: 934
rc=1

$ git checkout -q origin/main && bash scripts/check_zones.sh .    # живой tip 2add934
  FAIL коммит вне зоны: orchestrator 0717eda5 registry/contracts.tsv — дверь минта 031: дельта манифеста не только-добавление
замороженных контрактов: 35 · объявленных авторов: 6 · коммитов в диапазонах: 1449 · проверено по зонам: 936
rc=1
```

Причина — коммит `0717eda` («registry update ot freeze 032/2»), НЕ коммит 036. Его дельта:

```
-032 → a6c8abb6aa7ba15d59f77994982b5a0671710689
 033 → 99d424cc…
 034 → f7668198…
-035 → 2a5a2f460dde3b0d4addfa8fb89a5a2c29c36b98
-036 → f1476610beb6f76df7df38116018970dae0ff15c
+036 → a534f72eb7e39c7cf641f8d4c62fd036508cb2ef
+035 → 0e0300172b18bb23c88624a57a5565ca17ab68a5
+032 → 9f0ee662825d5b26085b6a9dbc092cd498dcd337
```

Три строки удалены и переписаны — дверь минта 031 (ветвь ii, `check_zones.sh:406-410`)
требует **только-добавления**, и отказывает верно.

Находка подтверждена НЕЗАВИСИМО: ревьюер 032 (`Rev032V2`) измерил тот же отказ в своём
клоне и в отдельном worktree на чистом `52a4aed` — «RC_zones=1», та же единственная
FAIL-строка. Две независимые меры, разные деревья, один корень.

**Вторая, более глубокая половина той же находки** (своя мера — сверка каждой строки с
предикатом двери `check_zones.sh:443-447` «sha строки == tag-object-sha живого
`id/CONTRACT/<NNN>`»):

```
032 строка → 9f0ee662…   id/CONTRACT/032 → a6c8abb6…   frozen/contracts/032/2 → 9f0ee662…
035 строка → 0e030017…   id/CONTRACT/035 → 2a5a2f46…   frozen/contracts/035/2 → 0e030017…
036 строка → a534f72e…   id/CONTRACT/036 → f1476610…   frozen/contracts/036/2 → a534f72e…
037 строка → ed15a3e8…   id/CONTRACT/037 → ed15a3e8…   (совпадает — минт 52a4aed корректен)
```

Три строки манифеста теперь несут **frozen-теги вместо id-тегов**. Дверь отказывает на
первом предикате (не-только-добавление) и до второго не доходит; после «починки
add-only» отказ воспроизведётся на втором — `sha строки ≠ tag-object-sha живого тега`.
Это правка семантики исключительной dual-control зоны, сделанная служебным коммитом.

**Судю по зоне ответственности:** к 036 как к механизму это не относится — коммиты 036
зелены все до одного. Но строка `036 → a534f72e` — это строка ре-фриза 036/2, и она
внесена именно этим коммитом. Объявлять `done/036` поверх дерева, где живой гейт зон
красен, а приёмка 036/2 прямым текстом требует его зелени, нельзя: это ровно класс
FAIL-2 моего круга 1 («ACCEPT выдан на дереве, где собственный барьер зон красен»).

Правка — не в зоне 036 и не в моей: её делает оркестратор/владелец по двери 031.
036 повторного круга по этому блокеру НЕ требует — требует зелёного дерева.

## БЛОКЕР-2. Класс: замороженное обязательство В2 сужено ПОСЛЕ всех судов, судьёй не смотрено

Задание этого круга называло реализацию v1 «нетронутой». **Это неверно как сказано.**
Моя мера — диф от HEAD, на котором адверсарий выдал ACCEPT (`45ff915`):

```
$ git diff --stat 45ff915 HEAD -- scripts/ fixtures/ .github/ package.json
 scripts/check_spec_ready.sh | 10 ++++++----

$ git log --format='%h %an %s' 45ff915..HEAD -- scripts/check_spec_ready.sh
1f933ec implementer  036 scope fix v4 — V3 zone-chtenie i SPASENO-poisk tozhe na DRAFT_FULL (V1/V2 ostajutsja na prijomka-scope DRAFT_BODY)
2ee80b6 implementer  036 scope fix v3 — DRAFT_FULL … + DRAFT_BODY (tolko prijomka: V1 proby)
59ce4fa implementer  036 scope fix dop — propushhena zakryvajushhaja kavychka podstanovki
d684357 implementer  036 spec-preflight scope fix — DRAFT_BODY tolko sektsija Prijomochnyj kriterij … (035 freeze upal na frontier-ssylke)
```

Четыре коммита в предмет контракта, ПОСЛЕ адверсария к4, после моего к1, после критика
036/2 и после самой заморозки `frozen/036/2`. По зонам они законны
(`scripts/check_spec_ready.sh` — ЗОНА implementer, `check_zones` на них зелен) — это не
рецидив Б1. Но ни один судейский круг их не судил, а сделаны они под давлением живого
прогона (собственная формулировка автора: «035 freeze upal na frontier-ssylke»).

**Сужение В1 — конформно** замороженному тексту: «Каждая **приёмочная** проба черновика…»
(036/2, В1). Претензии нет.

**Сужение В2 — не конформно и наблюдаемо дырявит ветвь.** Замороженный текст:
«Census-подобное число **в контракте** … обязано жить в машиночитаемой строке
`замер: …` … расхождение любой из двух мер с N = именованный отказ. Закрывает 035-класс
**ЦЕЛИКОМ**». Код на HEAD читает В2 из `DRAFT_BODY` — только секция `## Приёмочный
критерий` (`check_spec_ready.sh:68,264`).

Моя проба (клон-копия `toy`, один и тот же ложный замер, разные секции; скрипт —
`/tmp/dev-harness-verify/rev-036-conf/probe_scope.sh`):

```
=== A. контроль: честный черновик
OK
rc=0
=== B. ЛОЖНЫЙ замер ВНУТРИ приёмки (3 -> 999)
спек-гейт 036: замер расходится: заявлено 999, команда даёт 3
rc=1
=== C. ТОТ ЖЕ ложный замер ПЕРЕНЕСЁН в «## Предмет» (вне приёмки)
21:замер: `ls fixtures/freeze_contract/red_*036.sh | wc -l` = 999 census fixtures/freeze_contract/red_*036.sh
OK
rc=0
```

Одно и то же ложное утверждение: отвергнуто в одной секции, принято молча — в другой.
Секцию выбирает **сам автор черновика**, то есть проверяемый. Гейт, чей вердикт зависит
от выбора проверяемого, ветвь В2 не исполняет.

Попадание ровно в мотивирующий класс: census-числа контракта 035 живут в секции
`## Замер остатка` (`contracts/035-zhnets-tmp.md:38`) — ВНЕ приёмки, то есть в слепой зоне
нынешнего В2. Утверждение «закрывает 035-класс ЦЕЛИКОМ» на HEAD не держится.

Отмечаю различие причин: у сужения В1 названная причина есть (проба во frontier 035
исполнялась), у сужения В2 названной причины нет — оно приехало прицепом одним и тем же
`DRAFT_BODY`. Лечение не в моей зоне; называю: В2 обязан читать `DRAFT_FULL`, как это уже
сделано для В3 и СПАСЕНО коммитом `1f933ec`, либо сужение обязано быть внесено в
замороженный текст ре-фризом — и предъявлено кругом.

---

## Подтверждено зелёным — СВОЕЙ мерой

1. **Четыре red — `rc 0`** (прогон в клоне на `52a4aed`, последние строки несут различение
   стабов Н-39):
   `red_prichina_predmeta_036.sh` rc=0 («стаб-ворота с1… расходится с честным (rc 0 ≠ 1)»,
   «с6-зелёную-валит: расходится (rc 1 ≠ 0)») · `red_zamer_036.sh` rc=0
   («с2-замер-маркер-игнор: расходится») · `red_spec_preflight_036.sh` rc=0 («г5г
   отрицательный В3-вход отвергнут концом-в-конец, тега v2 нет») · `red_perenos_zony_036.sh`
   rc=0 («г4б-жив: настоящий check_zones вывел коммит из суда (rc 0)», «с3», «с4 —
   расходятся»).

2. **Scoped — ровно 4 / 15 / 21, `rc 0`** (совпало с заявленным; счёт мой):
   `check_check_spec_ready` → «барьеров: 1 · фикстур: 4 · предъявлено красным повторным
   прогоном: 4» · `freeze_contract` → 15/15 · `check_zones` → 21/21. Все три rc=0.
   В наборе `check_check_spec_ready` присутствует и легализованная 4-я фикстура:
   «ok check_check_spec_ready/case_symlink_vne_kornja.sh: … красный кодом 1 — «спек-гейт
   036: census вне корня»».

3. **Суды v1 — на месте и сверены по первой строке файла:** адверсарий к4
   `verdicts/adversary/contracts-036-v4.md` → `accept`; критик 036/2-зона
   `verdicts/critic/contracts-036-v4.md` → `accept` (коммит `90bcafb`, судил блоб
   `4360258` на `3eb0ca8` — блоб тождествен замороженному, сверка выше); ревьюер к1
   `verdicts/review/contracts-036-k1.md` → `FAIL`, оба его блокера закрыты этим кругом.

4. **Детектор утечек по ОСНОВНОМУ чекауту** (стенограмма):

```
$ bash scripts/check_no_leak.sh --check /home/aka/Documents/dev-harness
ОТКАЗ: основной чекаут загрязнён: registry/contracts.tsv, contracts/036-spec-tochnost-prefriz-gejt.md,
scripts/check_spec_ready.sh, verdicts/critic/contracts-032-v2.md, verdicts/critic/contracts-035-v2.md,
verdicts/adversary/contracts-036-v4.md, HANDOFF.md, verdicts/review/contracts-036-k1.md,
contracts/032-spawn-pinning-worktree.md, contracts/035-zhnets-tmp.md, verdicts/critic/contracts-036-v2.md,
verdicts/critic/contracts-036-v4.md
rc=1
$ git -C /home/aka/Documents/dev-harness status --porcelain
(пусто)
```

   **Сужу: утечкой не является** — список целиком состоит из отслеживаемых, закоммиченных
   в своих зонах путей; рабочее дерево чисто. Это дрейф базовой линии манифеста (12 путей
   против 2 на круге к1 — дрейф растёт), лечится `--retake`. Повторяю замечание к1:
   детектор в его нынешнем базлайне о чистоте чекаута не свидетельствует, и «`--snapshot`
   затем `--check` того же дерева» свидетельством не станет никогда.

## Итог

Заморозка 036/2 сделана законным маршрутом и мои блокеры круга 1 закрыла: зона узаконена
замороженным текстом, перенос покрыт СПАСЕНО, живой гейт зон принял грамматику, мера
`check_zones` по репозиторию внесена в приёмку, красное-до/зелёное-после предъявлено моей
мерой. Предмет ре-фриза — принят.

**036 → НЕ done**, по двум новым причинам:

- **БЛОКЕР-1** (вне 036, приземление): живой `check_zones` красен на дереве приземления
  (`52a4aed` и `2add934`), причина — `0717eda` оркестратора; приёмка 036/2 прямым текстом
  требует `rc 0` по всему репозиторию. Дополнительно названо: три строки манифеста несут
  frozen-теги вместо `id/CONTRACT`-тегов и уронят второй предикат двери 031 после починки
  первого. Круга 036 не требует — требует зелёного дерева.
- **БЛОКЕР-2** (внутри 036): ветвь В2 после заморозки читает только секцию приёмки;
  тождественное ложное census-утверждение отвергается в одной секции и принимается в
  другой — выбор секции принадлежит проверяемому. Требует круга: `DRAFT_FULL` для В2 либо
  ре-фриз с честно суженным текстом.

Ни одна из причин не повторяет к1 — арбитраж по правилу двух отказов не созывается.

— reviewer, 2026-09-20, круг 2, пин HEAD `52a4aed`, сверка на `2add934`
