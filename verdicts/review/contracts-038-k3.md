ACCEPT

# Ревьюер — контракт 038 «ПРОВОДКА done-gate» (Н-116), re-check Б1 (после круга 2)

Область этого вердикта — УЗКАЯ и объявляется явно: единственный блокер Б1 вердикта
`verdicts/review/contracts-038-k2.md` (`87966c3`, FAIL) и отсутствие коллатерали от его
починки. Это НЕ третий полный круг: всё остальное в предмете судилось на круге 2 и на
том же дереве, дельта с тех пор — один коммит из четырёх строк комментария.

Судимый HEAD — `a6a3204`. Клон суда: `/tmp/dev-harness-verify/reviewer038-k3`, свежий
`git clone https://github.com/a3ka/dev-harness` (не локальный чекаут). Ниже — сырой
вывод с кодами возврата.

## Вердикт

**ACCEPT** — Б1 закрыт по существу: блок «Контракт API» теперь описывает ровно тот
интерфейс, который исполняет код, все три задокументированные ветви rc 2 предъявлены
живьём, поведение не изменилось ни на одном из 65 предъявлений. Новых блокеров в
дельте нет. Остатки О1–О3 круга 2 остаются открытыми и НЕ блокирующими — перенесены
ниже, чтобы не исчезнуть вместе с окном.

## Дельта, поданная на re-check

```
$ git log --oneline --name-only 87966c3..HEAD
a6a3204 implementer: контракт 038 блокер Б1 — переписать Контракт API
        (scripts/check_provodka.sh:104-112) под живой код (двухаргументный вход,
        три rc2-причины)
scripts/check_provodka.sh

$ git show --stat --format='author=%an committer=%cn' a6a3204
author=implementer <implementer@dev-harness.local> committer=implementer
 scripts/check_provodka.sh | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

$ git show a6a3204 -- scripts/check_provodka.sh
-#   вход: $1 = <отн-путь-контракта> (относительно cwd — канон барьеров дерева);
+#   вход: $1 = <корень-дерева> (канон барьеров дерева); $2 = <отн-путь-контракта> (от $1);
-#   rc 2 — нечем проверить: контракта нет / нет git / пустое тело контракта.
-#
-#   bash scripts/check_provodka.sh <отн-путь-контракта>
+#   rc 2 — нечем проверить (живые причины ровно три):
+#     • корня нет: $ROOT; • контракта нет: $ROOT/$CONTRACT_PATH; • тело контракта пусто: …
+#   bash scripts/check_provodka.sh <корень-дерева> <отн-путь-контракта>
```

Правка — ТОЛЬКО комментарий, и это не заявление автора, а моя мера:

```
$ git diff 87966c3 HEAD -- scripts/check_provodka.sh | grep -cE '^[+-][^+-]'
8                              # восемь изменённых строк (4+/4−)
$ git diff 87966c3 HEAD -- scripts/check_provodka.sh | grep -E '^[+-][^+-]' | grep -vcE '^[+-]#'
0                              # из них НЕ-комментарных: ноль
```

Ни одной исполняемой строки барьер не потерял и не приобрёл.

## Б1 — ЗАКРЫТ. Каждое утверждение шапки предъявлено исполнением

**1. Двухаргументный вход.** Шапка обещает `$1 = <корень-дерева>`, `$2 =
<отн-путь-контракта>`; код и поведение совпадают:

```
$ sed -n '145,147p' scripts/check_provodka.sh
ROOT="${1:?использование: $0 <корень-дерева> <отн-путь-контракта>}"
CONTRACT_PATH="${2:?использование: $0 <корень-дерева> <отн-путь-контракта>}"
ROOT="$(cd "$ROOT" 2>/dev/null && pwd -P)" || skip "корня нет: $ROOT"

$ bash scripts/check_provodka.sh . contracts/038-provodka-done-gejt.md
rc=0
$ bash scripts/check_provodka.sh .
scripts/check_provodka.sh: line 146: 2: использование: scripts/check_provodka.sh
<корень-дерева> <отн-путь-контракта>
rc=1
```

Форма совпадает и с замороженным §Инварианты 3 v2 (`check_provodka.sh <корень>
<отн-путь-контракта>`) — противоречия «код против нормы», бывшего сутью Р1 круга 1,
больше нет ни в одну сторону.

**2. Ровно три живые причины rc 2 — все три предъявлены, четвёртой нет.** Шапка
перечисляет «корня нет / контракта нет / тело контракта пусто»; исчерпывающий
перечень `skip`-вызовов файла — ровно эти три, и каждая взята живьём:

```
$ grep -n 'skip "' scripts/check_provodka.sh
147:ROOT="$(cd "$ROOT" 2>/dev/null && pwd -P)" || skip "корня нет: $ROOT"
153:[ -f "$ROOT/$CONTRACT_PATH" ] || skip "контракта нет: $ROOT/$CONTRACT_PATH"
155:[ -n "$body" ] || skip "тело контракта пусто: $ROOT/$CONTRACT_PATH"

$ bash scripts/check_provodka.sh /net/sushchestvuet contracts/038-provodka-done-gejt.md
NOT_IMPLEMENTED: корня нет:
rc=2
$ bash scripts/check_provodka.sh . contracts/404-ghost.md
NOT_IMPLEMENTED: контракта нет: /tmp/dev-harness-verify/reviewer038-k3/contracts/404-ghost.md
rc=2
$ bash scripts/check_provodka.sh /tmp/dev-harness-verify/k3toy contracts/999-pusto.md
NOT_IMPLEMENTED: тело контракта пусто: /tmp/dev-harness-verify/k3toy/contracts/999-pusto.md
rc=2
```

**3. Несуществующая ветвь «нет git» из текста ушла** — и её отсутствие в коде
перепроверено мной, а не принято на слово:

```
$ grep -nE 'git (show|cat-file|rev-parse|ls-tree|archive|worktree)|GIT_' scripts/check_provodka.sh
129:unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_OBJECT_DIRECTORY \
130:      GIT_ALTERNATE_OBJECT_DIRECTORIES GIT_TEMPLATE_DIR GIT_CEILING_DIRECTORIES
131:export GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null
                               # только env-гигиена; git-объекты барьером не читаются
```

Требование круга 1 («в любом случае блок «Контракт API» обязан перестать описывать
несуществующий интерфейс») исполнено буквально и полностью.

## Коллатерали нет (все меры перегнаны на исправленном дереве)

```
$ bash scripts/verify_antiplacebo.sh --scope check_provodka
барьеров: 1 · фикстур: 28 · предъявлено красным повторным прогоном: 28
rc=0

$ EXPECT_RC=0 bash fixtures/_krasnye_038.sh
итог: 37 файлов, расхождений 0 (режим ожидания rc=0)
rc=0

$ bash scripts/check_contract_frozen.sh .
планов и контрактов на HEAD: 44 · черновиков: 3 · заморожено: 41 · реестр: full
rc=0

$ bash scripts/check_zones.sh .
замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1864 ·
проверено по зонам: 1248
rc=0
```

Замороженный текст правкой не тронут — блоб тот же, что судил критик и что стоит под
тегом:

```
$ git rev-parse HEAD:contracts/038-provodka-done-gejt.md
9b1507ac800fab9c63d9c796601540f122d5cb09
$ git rev-parse frozen/contracts/038/2^{commit}:contracts/038-provodka-done-gejt.md
9b1507ac800fab9c63d9c796601540f122d5cb09
```

Зона и атомарность: единственный тронутый путь `scripts/check_provodka.sh` объявлен
ЗОНА implementer (`contracts/038-provodka-done-gejt.md:364`), автор коммита —
`implementer`, коммит один, предмет один, сообщение ссылается на Б1. `check_zones.sh`
судит этот коммит и даёт rc 0 (вывод выше, 1248 проверенных против 1246 до правки).
Нормативные документы не тронуты: дельта — один файл.

## Остатки круга 2 — по-прежнему открыты, по-прежнему НЕ блокируют

Повторяю их здесь дословно по классу, чтобы они не исчезли вместе с окном 038:

- **О1** — реализация несёт именованную причину вне замороженного списка §Инварианты 3:
  «проводка: role-канал обязан ссылаться строго на roles/<роль>.md, получено: …
  (резолв: …)». Все 10 замороженных причин присутствуют дословно; эта — одиннадцатая.
  Статус зафиксирован арбитражем `contracts-038-role-validacija-podhod.md`
  (§Семантические границы 3: «здесь не судился»). Усиление, не ослабление → не блокер;
  при следующей правке §Инварианты 3 обязана войти в список причин либо в §Риски.
- **О2** — §Инварианты 4 замороженного текста ссылается на арбитраж `78785ec` П3 и не
  называет Н-136 (`grep '136' contracts/038-*.md` → 0). Условие подписи владельца
  (регистрация остатка) исполнено записью `d6b747d`; ссылка односторонняя. Обратная
  ссылка — в следующую редакцию текста.
- **О3** — `HANDOFF.md:188-198` хранит неисполненную раннюю директиву владельца по Р1
  («выбросить 8 фикстур, frozen 1-арг не трогаем»), перекрытую поздним актом того же
  владельца (`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` в `4e0db83`). Противоречия норм нет; запись HANDOFF
  устарела и вводит в заблуждение читателя истории.

## Что этот вердикт НЕ утверждает

Он не переоткрывает и не подтверждает заново предмет круга 2 — подпись владельца,
блоб-тождество критик-accept, Н-136, живость CI-проводки Р2 и четыре машинные меры
проверены и зафиксированы в `contracts-038-k2.md` на дереве, отличающемся от этого
ровно четырьмя строками комментария; повторять их прогоном как «новую проверку» было бы
согласием, а не мерой (три из четырёх мер я тем не менее перегнал заново — вывод выше).
Он не заменяет решение владельца и не ставит тег: `done_contract.sh` увидит этот файл
как свежий вердикт ревьюера с первой строкой `ACCEPT`.

Контракт, исходники, фикстуры и теги ревьюером не изменены; freeze/done не запускались.
