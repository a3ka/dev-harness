accept

# Ревьюер, круг 2 — контракт 029 (auto-консультант форков, Н-104)

ПРЕДМЕТ: применение выхода Б1/Б2 вердикта к1 (`verdicts/review/contracts-029-k1.md`,
§«Что закрывает FAIL»): живой журнал `forks/`, мера `--schet`, строка меры в `HANDOFF.md`.
СУДЬЯ: reviewer. МОДЕЛЬ: anthropic/claude-opus-5 (семейство ≠ авторского zai/glm-5.3).
ДЕРЕВО: HEAD `74a15b9f01874e16b7223b937e2a8ec3b52d2bdd` (== origin/main).
СКОУП к2 (ограничен заданием): П1–П6 к1 привязаны к блобам `b5019ae`, новыми записями
`forks/` не затрагиваются и НЕ перепроверялись. Канал owner-коммита `e84d574`
санкционирован к1 (Б1) и не переоткрывался. Класс обходов PATH/TCB закрыт арбитражем
`verdicts/arbitration/tcb-granica-put-029.md` и не искался.

Прогоны — в одноразовом клоне `/tmp/dev-harness-verify/rev-k2-029/repo`
(`git clone /home/aka/Documents/dev-harness`; `git status --porcelain` пуст,
HEAD == `74a15b9`, тег `frozen/contracts/029/1` = `294980d`). Мера `--schet`
дополнительно прогнана на живом корне как чтение.

## Сырьё меры (п.3, СВОЙ прогон, HANDOFF не принят на веру)

Клон, `bash scripts/check_fork_route.sh --root .`:

    передаточное касание: инженерный форк адресован владельцу: sankcija-arbitra-kap3
    RC=1

Клон, `bash scripts/check_fork_route.sh --root . --schet`:

    просмотрено 3
    передаточных 1
    RC=0

Живой корень `/home/aka/Documents/dev-harness`,
`bash scripts/check_fork_route.sh --root . --schet`:

    === status live BEFORE ===
    rc=0 (empty above = clean)
    === LIVE ROOT --schet ===
    просмотрено 3
    передаточных 1
    RC=0
    === status live AFTER ===
    rc=0 (empty above = clean)

`git status --porcelain` живого корня пуст ДО и ПОСЛЕ прогона (обе строки выше пусты):
чтение мутации не внесло.

Критерий к1 сверен по трём пунктам, каждый своей мерой: `просмотрено 3` → N = 3 > 0
ЗЕЛЁНОЕ; `передаточных 1` → M = 1 ≤ 1 ЗЕЛЁНОЕ; строка с числом в `HANDOFF.md` —
собственный `grep 'Мера закрытия инв'` даёт `HANDOFF.md:287`, дословно:
`**Мера закрытия инв. 5 (Н-104): просмотрено 3 / передаточных 1** (N>0, M≤1 — критерий
close-out 029 выполнен)` — ЗЕЛЁНОЕ. Числа строки совпадают с МОИМ прогоном (3 / 1), а не
только с прогоном автора.

`rc 1` нормального режима — предусмотренный к1 выход («rc 0 либо поимённый отказ,
разобранный по существу»). Отказ ПОИМЁННЫЙ (`sankcija-arbitra-kap3`), разбор по существу
предъявлен в самой записи (`КЛАСС: инженерный` + `МАРШРУТ: батч` фиксируют ФАКТИЧЕСКОГО
адресата касания; перемаркировка в «консультант» обнулила бы счётчик, инв. 5) и в
`HANDOFF.md:287-292`. Разбор ПРИНЯТ: честная мера Н-104 требует, чтобы состоявшееся
передаточное касание оставалось видимым, а не исчезало разметкой; `--schet` при этом даёт
rc 0 — инвариант 5 («счёт работает и на грязном журнале») исполнен буквально.

## п.1 — живой журнал и грамматика §Предмет — ЗЕЛЁНОЕ

`git ls-tree -r --name-only HEAD | grep '^forks/'` → ровно 3 записи:
`forks/instrukcii-nochi.md`, `forks/sankcija-arbitra-kap3.md`,
`forks/zamorozenyj-tekst-029.md` (`wc -l` = 21 + 21 + 19 = 61 строка — совпало с
заявленным «3 записи (61 строка)» и с `git show --stat e84d574`:
`3 files changed, 61 insertions(+)`).

Грамматика проверена механически, не глазом:
- `ФОРК` == basename файла на всех трёх (`instrukcii-nochi`, `sankcija-arbitra-kap3`,
  `zamorozenyj-tekst-029`), каждое имя матчит `^[a-z0-9-]+$` (Н-99) — OK/OK/OK;
- `^КЛАСС:` встречается РОВНО 1 раз в каждой записи (`grep -c` → 1, 1, 1); упоминание
  «КЛАСС инженерный» в теле `sankcija-arbitra-kap3` двоеточия после токена не несёт и
  полем не является;
- `ЗАВЕДЁН` матчит ISO `ГГГГ-ММ-ДДTчч:мм:ссZ` в 3 из 3 (`2026-09-19T10:25:21Z`);
- поля `ФОРК/ВОПРОС/ПРИЗНАК/МАРШРУТ/ЗАВЕДЁН/АВТОР/БЛОКИРУЕТ/ОТВЕЧЕНО` присутствуют ровно
  по одному разу в каждой из трёх записей (счётчики 1 по всем 8 ключам × 3 файла).

## п.2 — свидетельства второй подписи (инв. 2) — ЗЕЛЁНОЕ

Обе записи `КЛАСС: воля-владельца` (`instrukcii-nochi` — `ПРИЗНАК: приоритет`;
`zamorozenyj-tekst-029` — `ПРИЗНАК: норма`; оба токена из закрытого перечня инв. 2) несут
свидетельство в дереве:

    verdicts/consultant/instrukcii-nochi-klass-v1.md:6:ПОДТВЕРЖДЕНО: воля-владельца
    verdicts/consultant/zamorozenyj-tekst-029-klass-v1.md:6:ПОДТВЕРЖДЕНО: воля-владельца

Строка САМОСТОЯТЕЛЬНАЯ (якорный `^…$`-grep, не подстрока). `МОДЕЛЬ:` обоих свидетельств —
`openai-codex/gpt-6-astra`; `АВТОР:` обеих записей — `zai/glm-5.3`. МОДЕЛЬ ≠ АВТОР →
самоподтверждения нет. Третья запись (`sankcija-arbitra-kap3`, `КЛАСС: инженерный`)
свидетельства не требует — верно по инв. 2.

## п.4 — неизменность заморозки — ЗЕЛЁНОЕ

`git diff --exit-code frozen/contracts/029/1 HEAD -- contracts/029-auto-konsultant-forkov.md`
→ `RC=0`, вывод пуст. Усилено второй мерой: блоб тега и блоб HEAD совпали побайтово —
`67dccebe6d195fc8e96db05193e7aab14924c830` == `67dccebe6d195fc8e96db05193e7aab14924c830`.
Норма молча не тронута.

## п.5 — scoped-регресс затронутых барьеров — ЗЕЛЁНОЕ

`npm run check:antiplacebo -- --scope check_fork_route verify_consultant`:

    SCOPED: барьеров 2 из выборки — не для приёмки
      ok   check_fork_route/case_fork_route_peredatochnoe.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «передаточное касание»
      ok   check_fork_route/case_fork_route_soreview_rutina.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «СОревью на рутине»
      ok   verify_consultant/case_verify_consultant_deko.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «расхождение вывода»

    барьеров: 2 · фикстур: 3 · предъявлено красным повторным прогоном: 3
    RC=0

Красное предъявлено, а не только зелёное: каждая из трёх фикстур даёт красный повторный
прогон кодом 1 с пиннованной подстрокой отказа. Полный прогон — CI (зелёный 6/6 по
`74a15b9`), приёмка-судьи v2 (Н-48).

## Проверка не переписана под реализацию

`git diff --name-status b5019ae HEAD` — ни одного файла из `scripts/` или `fixtures/`:
изменились только `HANDOFF.md` (M) и добавлены `forks/*` (3), `verdicts/consultant/*` (2),
`verdicts/review/contracts-029-k1.md`. Барьеры и фикстуры, по которым идут п.3 и п.5, —
те же блобы, что судил к1. Подгонки проверки под результат нет.

Атрибуция коммитов различима по `user.name`: `e84d574 owner <owner@dev-harness.local>`,
`ba4e7ab orchestrator <orchestrator@dev-harness.local>`, `9c8570a consultant`,
`ab233af reviewer`. Автор свидетельств (`consultant`) и автор записей (`zai/glm-5.3` в
owner-коммите) — разные роли.

## Находки

Дефектов класса FAIL не найдено.

Наблюдение (НЕ FAIL, класс — изложение): строка меры в `HANDOFF.md:287` перенесена по
ширине и обёрнута `**…**`; числа и смысл сверены дословно и совпали.

## Вердикт

ACCEPT. Выход Б1/Б2 вердикта к1 применён: журнал `forks/` живой (3 записи в HEAD), мера
`--schet` красным пустой выборки больше не отвечает (`просмотрено 3` при N > 0,
`передаточных 1` при M ≤ 1, rc 0), строка меры в `HANDOFF.md` есть и числом совпадает с
моим независимым прогоном. Заморозка контракта не тронута (rc 0 и равенство блобов),
scoped-регресс затронутых барьеров зелёный при предъявленном красном (rc 0).
Цель предмета Н-104 (M за сессию 5 → 0–1) достигнута механической мерой: M = 1.

Контракт 029 со стороны ревьюера закрыт; препятствий к `done/contracts/029/1` нет.
