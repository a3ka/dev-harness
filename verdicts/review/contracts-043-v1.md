FAIL

Предмет: контракт 043 «прецизионный пре-фриз гейт», `frozen/contracts/043/1`
(= `f2259f8`). Диапазон суда: `id/CONTRACT/043..82e1a61` по путям обеих зон 043
(architect: `contracts/043-*.md`, `NABLIUDENIA_ARCHITECT.md`, `roles/critic.md`,
`scripts/check_precision_gate.sh`, `fixtures/check_precision_gate/`,
`fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh`,
`fixtures/_krasnye_043.sh`; implementer: `scripts/check_precision_gate.sh`,
`fixtures/check_precision_gate/`, `scripts/freeze_contract.sh`,
`.github/workflows/ci.yml`, `package.json`). Пачка 037 в том же диапазоне —
ДРУГОЙ предмет, вне этого суда.

Клон: `ssh://git@github.com/a3ka/dev-harness.git` →
`/tmp/dev-harness-verify/rev043-20260924-a` (`git remote -v` сверен до работы).
Все мутации — в одноразовых копиях этого клона, дерево возвращено
`git checkout --` после каждой (проверено `git status --porcelain` — пусто).

Итог: **6 находок, из них 1 БЛОКИРУЕТ `done_contract.sh`** (Д1), 1 маршрутизуется
владельцу (Д2), 4 — не-блокирующие (Н1–Н4).

---

## Д1 (БЛОКЕР, класс «заявленное ≠ сделанное», ревьюер п.7)

Барьер описывает СВОЙ ЖЕ порядок исполнения задач неверно, и это измерено, а не
вычитано. `scripts/check_precision_gate.sh:39-44` утверждает:

```text
#   rc 1 — отказ, ИМЕНОВАННАЯ причина первой красной (порядок исполнения — а → в →
#          б: зона-коллизия — ядро предмета — и паритет CI — обе cost-сопоставимы,
#          идут первыми; задача (б) — самая дорогая, растёт линейно с числом
#          case-файлов затронутых семей (владелец: «цена растёт с числом
#          case-файлов — назови это честно») — исполняется ПОСЛЕДНЕЙ, названо
#          честно, не скрыто);
```

Фактический порядок — **а → б → в**. Дифференциальная проба ревьюера: toy-дерево,
на котором ОДНОВРЕМЕННО красны (б) «полярность не заявлена» и (в) «паритет CI»;
независимый оракул `verify_ci_parity.sh` подтверждает красноту (в) ДО прогона
гейта:

Проба — одноразовый скрипт ревьюера на штатных хелперах
`fixtures/check_precision_gate/_toy.sh` (`mk_toy_repo`/`mk_mint`/`mk_dummy_barrier`/
`mk_family_case`/`put_draft`), лежал вне дерева-предмета и удалён после замера:

```text
ОРАКУЛ verify_ci_parity rc=1 :   FAIL скрипт «orphan» есть в приёмке, но отсутствует в CI и не объявлен исключением
ГЕЙТ rc=1
--- вывод ---
precision-гейт 043: полярность не заявлена: fixtures/check_dummy/case_bez_prefiksa.sh (basename не начинается red_ или green_)
ГЕЙТ (только (в) красна) rc=1
--- вывод ---
precision-гейт 043: fixtures/check_dummy/red_s_prefiksom.sh: барьер dummy не вызван живьём (/proc-наблюдение не нашло exec)
```

Первой доложена (б) — значит (б) исполняется ДО (в). Структурное подтверждение:
цикл задачи (б) заканчивается на `scripts/check_precision_gate.sh:519`, вызов
`verify_ci_parity.sh` — на `:523`, отказ — на `:524`.

Тем же ложным порядком мотивирован порядок шагов CI
(`.github/workflows/ci.yml`, коммит `bf1874a`):

```text
# Запускается ПОСЛЕ check:ci-parity (порядок — verify_ci_parity
# сначала, иначе при красном паритете check_precision_gate будет
# блокирован на своём же шаге (в) раньше времени).
```

Посылка «блокирован на своём же шаге (в) раньше времени» ложна: до шага (в) гейт
доходит ПОСЛЕДНИМ, а не первым.

И то же утверждение несёт сообщение коммита `bf1874a`: «порядок а → в → б
(противоречие устранено)» — противоречие НЕ устранено, текст п.6 тем коммитом не
тронут (см. Н4), а код исполняет а → б → в.

Тяжесть: ОБХОДА нет — все три задачи исполняются, защита не ослаблена. Блокирует
по другому основанию: барьер, который лживо описывает собственный механизм, учит
себе не верить (правило 7 AGENTS.md, «отказ обязан говорить ПРАВДУ»), а
следующий правящий этот гейт будет рассуждать из ложной посылки. Лечение дешёвое
и ЦЕЛИКОМ внутри зоны implementer (`scripts/check_precision_gate.sh`,
`.github/workflows/ci.yml`) — замороженный текст трогать не требуется.

## Д2 (К ВЛАДЕЛЬЦУ, класс «внутреннее противоречие замороженного критерия»)

`contracts/043-precizionnyj-prefriz-gejt.md:221-226` (блоб внутри
`frozen/contracts/043/1`) несёт ДВА взаимоисключающих утверждения в одном пункте:

```text
6. **Порядок исполнения задач — а → б → в.** Задача (б) — самая дорогая, растёт
   линейно с числом case-файлов затронутых семей (владелец: «цена растёт с
   числом case-файлов — назови это честно») — исполняется ПОСЛЕДНЕЙ; (а) и (в)
   — cost-сопоставимы …, (а) первой как ядро предмета этого контракта.
```

«а → б → в» и «(б) … исполняется ПОСЛЕДНЕЙ» одновременно истинны быть не могут.
Реализация удовлетворяет стрелку и НЕ удовлетворяет второй клаузе — той самой,
которая несёт прямую цитату владельца про цену. Критерий, у которого две
взаимоисключающие клаузы, нельзя отличить от имитации ни в одну, ни в другую
сторону.

Не блокирую: ОБХОДА нет, обе клаузы касаются порядка, а не полноты проверок, и
правка ТЕКСТА замороженной спеки возможна только как v2 + строка
`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` (AGENTS.md §Воркфлоу п.3) — это решение владельца, не судьи.
Называю и маршрутизую: владелец решает, какая из клауз связывающая. Если
связывающей признаётся «(б) ПОСЛЕДНЕЙ» — реализация подлежит правке; если
стрелка — правке подлежит предложение про цену.

## Н1 (не-блокер, класс «красное предъявлено не полностью»)

`fixtures/check_precision_gate/red_15_forged_trace_via_xtracefd.sh:47-58` — две
ветки принятия сверяют ТОЛЬКО фразу в выводе и не сверяют rc:

```sh
  if printf '%s' "$LAST_OUT" | grep -Fq 'барьер dummy не вызван живьём'; then
    printf 'case_15: … поймана /proc-наблюдением\n' >&2
    exit 0
  fi
```

Мера: в одноразовой копии мутирован `die()` — `exit 1` → `exit 0` (гейт
по-прежнему ПЕЧАТАЕТ именованную причину, но не отказывает — ровно класс правила
2 AGENTS.md «результат — код возврата, не печать»). Прогон всей пачки:

```text
$ bash fixtures/_krasnye_043.sh     # die() → exit 0
check_precision_gate/red_02_zona_collision_undeclared.sh rc=1   FAIL
check_precision_gate/red_04_peresechenie_grammar.sh rc=1        FAIL
check_precision_gate/red_05_peresechenie_unbound.sh rc=1        FAIL
check_precision_gate/red_06_case_polarity_undeclared.sh rc=1    FAIL
check_precision_gate/red_07_case_no_live_call.sh rc=1           FAIL
check_precision_gate/red_08_case_self_check_fails.sh rc=1       FAIL
check_precision_gate/red_13_multi_collision_same_path.sh rc=1   FAIL
check_precision_gate/red_14_freeze_refuses_red_precision.sh rc=1 FAIL
check_precision_gate/red_15_forged_trace_via_xtracefd.sh rc=0   ← НЕ поймал
check_precision_gate/red_16_xtrace_only_pass.sh rc=1            FAIL
check_precision_gate/red_21_ci_parity_orphan_script.sh rc=1     FAIL
итог: 18 файлов, провалов 10
[rc=1]
```

Десять красных из одиннадцати мутацию ловят, `red_15` — нет. ОБХОДА это не даёт:
собственный класс `red_15` (отключение `$live`-детекции) держит `red_07` —
измерено отдельной точечной мутацией, где обезоружен ровно
`check_precision_gate.sh:514`:

```text
$ bash fixtures/check_precision_gate/red_07_case_no_live_call.sh   rc=1   ← поймал
$ bash fixtures/check_precision_gate/red_15_forged_trace_via_xtracefd.sh rc=0
$ bash fixtures/check_precision_gate/red_16_xtrace_only_pass.sh    rc=0
```

Лечение — однострочное: добавить `[ "$LAST_RC" -eq 1 ]` в обе ветки принятия.

## Н2 (не-блокер, класс «фикстура заявляет покрытие, которого не даёт»)

`fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh`,
`battery_silent_drop` (коммит `bf1874a`, автор implementer) зовёт
`mk_foreign_frozen` ДВАЖДЫ с ОДНИМ NNN 991:

```sh
  mk_foreign_frozen "$w" 991 alice shared/file.txt
  mk_foreign_frozen "$w" 992 bob   shared/file.txt
  mk_foreign_frozen "$w" 991 alice one/first.txt
```

Второй вызов с 991 не создаёт тега — `mk_foreign_frozen` (`_toy.sh`) не сверяет rc
`git tag`, и отказ уходит в stderr, виден в КАЖДОМ прогоне батареи:

```text
$ npm run check:precision-battery
БАТАРЕЯ check_precision_gate: класс delimiter-collision      — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс regex-injection          — закрыт (rc 0)
fatal: tag 'frozen/contracts/991/1' already exists
БАТАРЕЯ check_precision_gate: класс silent-drop              — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс self-application-green   — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: итог 4/4 классов закрыто
[rc=0]
```

`zones_load` читает union ИЗ ТЕГОВ, поэтому `one/first.txt` в union не попадает,
коллизии на нём нет, и объявленное самим комментарием покрытие — «отдельная
коллизия на ДРУГОМ пути … для покрытия исходного класса silent-drop» — инертно.
Класс закрыт только оставшейся половиной (два NNN на одном пути). Что половина
живая, измерено: под мутацией `die() → exit 0` класс краснеет —

```text
$ npm run check:precision-battery   # die() → exit 0
silent-drop: коллизия молча пропущена, rc 0: precision-гейт 043: зона-коллизия: shared/file.txt …
БАТАРЕЯ check_precision_gate: класс silent-drop              — ПРОБИТ (rc 1)
БАТАРЕЯ check_precision_gate: итог 3/4 классов закрыто
[rc=1]
```

Лечение: NNN 993 для третьего вызова (либо `mk_foreign_frozen` обязан краснеть на
отказе `git tag` — проглоченный отказ установки и есть корень).

## Н3 (не-блокер, класс «стале-счёт», ревьюер п.7)

Два закоммиченных артефакта несут счётные утверждения, которые сегодня ложны.
Моя мера — прямой листинг дерева, а не повтор чужой команды:

```text
$ git ls-tree --name-only HEAD -- fixtures/check_precision_gate/
… red: 11 · green: 7 · всего 18
$ bash fixtures/_krasnye_043.sh
итог: 18 файлов, провалов 0   [rc=0]
```

- `fixtures/check_precision_gate/.probe-only`: «агрегатор всех 9 сценариев
  Р1-Р9» — фактически 18 файлов, §Приёмка идёт до Р14. (На момент написания,
  `53b145a`, счёт 9 был верен — проверено `git ls-tree` на том коммите.)
- `.github/workflows/ci.yml`, шаг «Сам-тесты семьи precision-гейт (контракт
  043)»: «гоняет ВСЕ 14 red_*/green_*-файлов» — на `56d6c67` счёт 14 был верен
  (проверено `git ls-tree 56d6c67` → 14), после `7d8d864` их 18.

## Н4 (не-блокер, класс «сообщение коммита ≠ его диффу»)

Сообщение `bf1874a` перечисляет правки, которых в его диффе нет:

- «Contract §Инварианты п.2.4/п.3.3/п.6/п.7 обновлены … порядок а → в → б
  (противоречие устранено)» — весь дифф `contracts/043-*.md` в `bf1874a` это
  ДВЕ добавленные строки `ПЕРЕСЕЧЕНИЕ … 015`, и ничего больше. п.3.3 обновлён
  ПОЗЖЕ и ДРУГИМ коммитом (`feecc42`), п.6 не тронут никогда.
- «§Приёмка: Р13б (Б1), Р14 …, Р15 …, Р16 …, Р17 (CI-проводка)» — в
  замороженном тексте `Р13б`, `Р15`, `Р16`, `Р17` отсутствуют (проверено
  подстрочным поиском по блобу; есть Р1–Р14).

---

## Что проверено и оказалось ЧИСТЫМ

**1. Область правки.** `bash scripts/check_zones.sh .` → rc 0 («замороженных
контрактов: 41 · коммитов в диапазонах: 1907 · проверено по зонам: 1283»).
Названное, но НЕ ставшее находкой: четыре коммита implementer
(`bf1874a`, `56d6c67`, `feecc42`, `2fca0de`) правят
`contracts/043-precizionnyj-prefriz-gejt.md` и
`fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh` — пути,
объявленные §Зоны ТОЛЬКО под architect. Не находка по двум основаниям: зонный
энфорсмент до заморозки не действует (это установленная практика сессии и
названа в теле `53b145a`), и каждая из этих правок была после этого судима
критиком (круги 3–6, accept `cf8cfe7`). Коммит orchestrator `53b145a` в
`scripts/check_precision_gate.sh` — комментарий-классификация `НЕ БАРЬЕР:` и
маркер `.probe-only`, 0 строк исполняемого кода, причина названа в теле.

**2. Сырой вывод — прогнан лично, не со слов отчётов.**

```text
$ bash fixtures/_krasnye_043.sh
итог: 18 файлов, провалов 0                                    [rc=0]

$ npm run check:precision-battery
БАТАРЕЯ check_precision_gate: итог 4/4 классов закрыто          [rc=0]

$ bash scripts/verify_ci_parity.sh .
workflow-команд: 45 · скриптов в приёмке: 57 · объявленных исключений: 23 ·
matrix-ключей: 44 · анти-плацебо-запусков: 2 · расхождений: 0                [rc=0]

$ bash scripts/check_precision_gate.sh . contracts/043-precizionnyj-prefriz-gejt.md
precision-гейт 043: семья precision_gate — барьер правится этим же черновиком,
задача (б) для неё пропущена (прецедент 036 В4-3)
OK                                                              [rc=0]

$ bash scripts/check_provodka.sh . contracts/043-precizionnyj-prefriz-gejt.md   [rc=0]
$ bash scripts/check_contract_frozen.sh .
… contracts/043-… — заморожен v1, блоб совпадает побайтово, вердикты v1..v1 разрешают
планов и контрактов на HEAD: 46 · черновиков: 4 · заморожено: 42 · реестр: full [rc=0]
$ bash scripts/check_protected.sh .
существовало: 449 · на HEAD: 411 · исчезло: 0 · с разрешения: 29 · перенесено: 9 [rc=0]
```

**3. Проверка не переписана под реализацию.** `--diff-filter=M` по
`fixtures/check_precision_gate/` за весь диапазон даёт ПУСТО: ни один уже
существовавший case-файл семьи никогда не правился — все круги Б5/Б6/Б7 и
адверсарий только ДОБАВЛЯЛИ файлы (`green_17`←`19d9d88`, `green_18`←`ab5e795`,
`green_19`←`f5e88af`, `green_20`←`2fca0de`, `red_21`←`7d8d864`). Единственная
правка существующей проверки — `battery_silent_drop` в `bf1874a`, и она
УСИЛЯЮЩАЯ (требует ловить ВТОРОЙ необъявленный NNN на том же пути там, где
прежний вход краснел тривиально); её дефект — Н2, но он не ослабление критерия.
Правка текста п.3.3 (`feecc42`) ОСЛАБЛЯЕТ rc-ожидание для файлов, заявленных
«Красное сейчас», — сделана рукой автора кода, но: причина записана архитектором
РАНЬШЕ и независимо (`8151eee`, А-242/А-243), ослабление явно компенсировано
сохранением Б2/Б3 (живость барьера обязательна в обеих ветках), и после этого
предмет прошёл круги критика 3–6 с accept. Подгонкой под код не признаю.

**4. Красное предъявлено — живьём, а не rc честного прогона.** `red_21`
(`7d8d864`, самый новый) проверен ЛИЧНО мутацией, а не повтором его honest-rc:

```text
$ python3 …  # в одноразовой копии: [ "$ci_rc" -eq 0 ] || die → true || die
$ BARRIER=<мутированная копия> WORK=<mktemp> bash fixtures/check_precision_gate/red_21_ci_parity_orphan_script.sh
ОТКАЗ: red_21 (…): rc 0 (ожидался 1)
вывод:
OK
[rc=1]                                       ← фикстура ловит отключение задачи (в)

$ BARRIER=<честный scripts/check_precision_gate.sh> WORK=<mktemp> bash …/red_21_…sh
red_21 (…): отказ rc 1, причина названа дословно
[rc=0]
```

Для всей пачки живой негативный контроль — мутация `die() → exit 0` выше: 10 из
11 красных фикстур краснеют, батарея краснеет (3/4). Исключение — Н1.

**5. Атомарность.** По одной задаче на коммит, каждый со ссылкой на предмет:
`19d9d88` (Б5 /proc-гонка), `ab5e795` (Б6 argv[1]), `f5e88af` (БС7 argv[2]),
`2fca0de` (БС7 literal-скан всех полей argv, по решению арбитра `d467f4e`),
`56d6c67` (Б4 два прямых CI-шага), `feecc42` (task-б «красное до реализации»),
`7d8d864` (находка адверсария). `bf1874a` сводит Б1–Б4 в один коммит — это
ОДИН круг критика, дифф в зоне, каждая находка поимённо в теле; отказом не
считаю (сообщение того же коммита — Н4).

**6. Норма не тронута молча.** `roles/critic.md` за весь диапазон правится
ровно одним коммитом (`4db81e5`, architect) и ровно одним способом — ДОБАВЛЕНИЕМ
нового раздела в конец файла:

```text
@@ -183,3 +183,11 @@
+## Норма precision-гейта (контракт 043)
+Норма-строка (ПРОВОДКА roles/critic.md, заморожена 043/1):
+Перед вынесением accept контракту, вводящему НОВУЮ ЗОНА-строку …
```

Ни одна строка «Пяти вопросов» не тронута — правило 14 AGENTS.md соблюдено.
Дословность норм-строки сверена своей мерой (точное равенство строки целиком,
не подстрока): `- role=roles/critic.md «…»` из §ПРОВОДКА контракта присутствует
в `roles/critic.md` как ПОЛНАЯ строка → True. Р14 (проводка CI) сверена тем же
способом: обе `npm run`-команды в `ci.yml` и обе пары ключ-значение в
`package.json` — присутствуют.

**7. Н-69 — рецидива НЕТ.** `verdicts/critic/contracts-043-v1.md` несёт accept
круга 6 БАЙТ-В-БАЙТ, доказано равенством блобов, а не чтением:

```text
$ git ls-tree -r cf8cfe7 -- verdicts/critic/   # коммит «critic: accept … round 6»
100644 blob f0e02d749f67c379ec4f5f8f5575155602d7a49a  verdicts/critic/contracts-043-v6.md
$ git rev-parse f2259f8:verdicts/critic/contracts-043-v1.md
f0e02d749f67c379ec4f5f8f5575155602d7a49a
```

Один и тот же блоб — консолидация действительно перезапись без изменения байтов.
Удаления v2–v6 несут по строке `ALLOW-ARTIFACT-DELETE:` каждое, и
`check_protected.sh` их признаёт («исчез с явного разрешения», исчезло: 0).

**8. Замороженный текст не тронут после заморозки.**
`git log frozen/contracts/043/1..HEAD -- contracts/043-precizionnyj-prefriz-gejt.md`
— пусто. `red_21` добавлен ПОСЛЕ заморозки — законное усиление гейта
(AGENTS.md §Воркфлоу п.3, прецедент 005).

**9. Находка адверсария закрыта по существу.** Вердикт
`verdicts/adversary/contracts-043-precision-gate-adversary.md` требовал красную
пробу задачи (в); `red_21` её даёт и ловит именно ту мутацию, которой адверсарий
пробил пачку — проверено мной заново в п.4, а не принято со слов.

---

## Условие снятия FAIL

Достаточно закрыть Д1 (обе правки — внутри зоны implementer, замороженный текст
не трогается):

1. `scripts/check_precision_gate.sh:39-44` — привести описание порядка в
   соответствие с измеренным а → б → в (либо изменить КОД так, чтобы описание
   стало истинным; выбор — за автором по коду, не по этой прозе).
2. `.github/workflows/ci.yml`, шаг «Пре-фриз precision-гейт (контракт 043)» —
   убрать ложную посылку про «блокирован на своём же шаге (в) раньше времени».

Д2 — владельцу, отдельно от снятия FAIL. Н1–Н4 — советом; Н1 и Н2 стоят по
одной строке и их разумно взять тем же кругом.

Ревьюер, круг 1, 2026-09-24.
