FAIL

# Ревью 020, круг к3 — шардирование CI анти-плацебо (`d2c4d6d`)

Предмет: `d2c4d6d19f20e20e5d4f874ee21f7424db299a9b` (merge `wip/020/implementer`,
вершина ветки — `52f0079`, поверх `05e41f7`). Контракт
`contracts/020-shardirovanie-ci-antiplatsebo.md`. Круг третий по блокеру Б1 вердикта к2
(`verdicts/review/contracts-020-k2.md`), арбитраж `c8aaa67`
(`verdicts/arbitration/contracts-020-samtesty.md`).

Канал: одноразовый клон `/tmp/dev-harness-verify/rev-020-k3/repo`
(`git clone /home/aka/Documents/dev-harness …`), удалён после прогонов. Основной чекаут не
трогался. Гигиена круга: Н-39 (обманный стаб — в ветви, где дефект наблюдаем), Н-101 (блоб
предмета сверен, не пересказ), Н-52 (ниже сырые факты с командной строки).

## Сверка предмета (Н-101)

`git rev-parse HEAD` в клоне → `d2c4d6d19f20e20e5d4f874ee21f7424db299a9b`.

`git rev-parse HEAD:<файл>` в клоне == `git rev-parse d2c4d6d:<файл>` в основном репозитории
== `git hash-object <файл>` в рабочем дереве клона, все три совпали:

```
38af80961d71289586f04f368359f1301e45de5b  .github/workflows/ci.yml
5985cdd0ed1caf52db3eb455bc4cdd7b8a4c63d9  scripts/verify_ci_parity.sh
3b9688e691e0c36b15a13be997d776fae8b1bbd1  config/ci_parity_exceptions.txt
4196ca31b4308c095eaafef9afba77fda8012ee0  scripts/verify_antiplacebo.sh
```

Тот же `4196ca31…` для раннера снят повторно ПОСЛЕ всех прогонов — tracked-файл клона не
правился ни разу, саботаж жил только в копии внутри `$TMP_ROOT` (замер 2 ниже).

Уточнение к формулировке задания: `config/ci_parity_exceptions.txt` в задании назван
«не менявшимся этим кругом». Своей мерой:
`git show --stat 05e41f7` → `config/ci_parity_exceptions.txt | 20 ++++++++`. Файл ЭТИМ
кругом менялся (+20 строк, семь записей `команда:` и шапка). На вердикт не влияет — файл
в ЗОНА implementer, — но заявленное и сделанное здесь расходятся.

## Приёмка шага арбитража — 4 пункта, каждый перепроверен исполнением

### П.1 — шаг зелён на честном дереве: ПОДТВЕРЖДЁН

Рецепт CI-шага воспроизведён в клоне дословно (`mktemp`-корень, копия раннера, копия
фикстур, `npm run check:antiplacebo -- "$TMP_ROOT"`). Итог с моей командной строки:

```
барьеров: 1 · фикстур: 23 · предъявлено красным повторным прогоном: 23
RC=0
```

Эталон замера 5 арбитража — «фикстур: 23 · предъявлено красным: 23». Совпало дословно.
Все 23 строки — `ok`. Стена: 43,95 с (арбитраж мерил 52 с).

### П.2 — шаг красен на дереве с саботажем: ПОДТВЕРЖДЁН

Саботаж по Н-39 — в КОПИИ `$TMP_ROOT/scripts/verify_antiplacebo.sh`, строка 694, ветвь
«барьер остался зелёным на обманном дереве» переведена в безусловный `ok`:

```
694:      bad "$cname: барьер остался зелёным на обманном дереве — красное не предъявлено"
  →   reds=$((reds + 1)); ok "$cname: барьер остался зелёным на обманном дереве — красное не предъявлено"
```

Судья (`npm run check:antiplacebo` из честного клона) на этом дереве:

```
барьеров: 1 · фикстур: 23 · предъявлено красным повторным прогоном: 19
расхождений: 4 · прогон оставлен в /tmp/verify_antiplacebo-d23d3e45/run-669829
RC=1
```

Красные case — ровно четыре, имена дословно:

```
FAIL verify_antiplacebo/case_barjer_zelen.sh
FAIL verify_antiplacebo/case_neobjavlennoe_okruzhenie.sh
FAIL verify_antiplacebo/case_perepisala_uchet.sh
FAIL verify_antiplacebo/case_tmpdir_raven_koren.sh
```

Список совпал с эталоном замера 6 арбитража поимённо и по мощности.

**Блокер Б1 круга к2 ЗАКРЫТ.** 23 case `fixtures/verify_antiplacebo/` теперь гейтятся
шагом CI, и гейт КРАСЕН против сломанного раннера — красное предъявлено, не только зелёное.
Четвёртого круга по причине Б1 не возникает; адрес арбитра по Б1 не открывается.

### П.3 — `verify_ci_parity.sh` rc=0 и канон residual: ПОДТВЕРЖДЁН ЧАСТИЧНО

`bash scripts/verify_ci_parity.sh` в чистом клоне, итоговая строка:

```
workflow-команд: 36 · скриптов в приёмке: 46 · объявленных исключений: 24 · matrix-ключей: 35 · анти-плацебо-запусков: 2 · расхождений: 0
RC=0
```

Шесть команд шага сам-тестов прошли как `ok … приёмкой не является — объявленное
исключение: …` с настоящей причиной у каждой. Имя `npm run check:antiplacebo` в
`.github/workflows/ci.yml:87` и `:129` статично, индирекции (`$ANTI_SCRIPT`, `$NPM_BIN`)
в предмете нет — сверено чтением `ci.yml` и грепом. Канон residual соблюдён.

**Механизм проверки, затребованный заданием отдельно, — НЕ соблюдён. См. Б2 ниже.**

### П.4 — шапка `UNSCOPABLE_KEYS`/`SELF_COVERED_KEYS`: ПОДТВЕРЖДЁН

Ложного утверждения о косвенном исполнении сам-тестов шардами в шапке нет. Утверждение
прямо опровергнуто в самом тексте (`scripts/verify_ci_parity.sh:1117-1122`):

```
#     verify_antiplacebo/case_*.sh`) раннер НЕ гоняет на шардах — арбитраж c8aaa67
#     показал ложность прежней компенсации (scoped-прогоны раннера на каждом шарде
#     НЕ есть прогон сам-тестов: исполнение раннера не есть исполнение его 23 case
#     через FIFO-протокол). Изъятие из UNSCOPABLE_KEYS (ниже) и перенос в
#     SELF_COVERED_KEYS: покрытие — ОТДЕЛЬНЫМ шагом сам-тестов на минимальном
#     mktemp-корне
```

Грепом `косвен|сам-раннер анти-плацебо|scoped-прогон` по файлу — четыре попадания
(`:1111`, `:1118`, `:1166`, `:1167`), все в составе опровержения («ложность прежней
компенсации», «ложная компенсация scoped-прогонов»). Ключ вынесен в
`SELF_COVERED_KEYS=(verify_antiplacebo)` (`:1170`), `UNSCOPABLE_KEYS=(gen-harness
check_metering)` (`:1157`).

## Находки

### Б2 — БЛОКЕР. Оплата инварианта 4 читается подстрокой по всему блоку `run:`; не-шардный прогон анти-плацебо легализуется чужой записью, в том числе из shell-комментария

Класс: барьер, который не гейтит. НЕ класс Б1 (Б1 — «сам-тесты нечем гейтить»; здесь —
«исключительность шардного запуска нечем гейтить»). Внесён ЭТИМ кругом, коммитом `52f0079`.

Место — `scripts/verify_ci_parity.sh:1264-1281`, ветвь 4b.2:

```
while IFS=$'\t' read -r path ln jobname cmd has_scope in_matrix; do
  …
  paid=0
  for c in "${!EXC_CMD[@]}"; do
    if [[ "$cmd" == *"$c"* ]]; then
      EXC_CMD_USED["$c"]=1
      paid=1
    fi
  done
  [ "$paid" -eq 1 ] && continue
```

`$cmd` — это ВЕСЬ блок `run:` шага, схлопнутый в одну строку (писатель ANTI_TSV,
`:930`: `f.write(f'…\t{" ".join(cmd.split())}\t…')`). `$c` — любой из семи ключей
`EXC_CMD`. Направление чтения: ключ исключения ищется КАК ПОДСТРОКА ВНУТРИ команды CI.
Прямое следствие — запись оплачивает не свою команду, а любой блок `run:`, в тексте
которого она встретилась.

Замер (мой, в клоне; никакой записи в `config/ci_parity_exceptions.txt` не добавлялось):

Контроль — шаг вне matrix, в джобе `ci`, полный не-шардный прогон:

```
      - name: Скрытый не-шардный прогон анти-плацебо
        run: npm run check:antiplacebo
```
```
FAIL .github/workflows/ci.yml:132 (ci): запуск анти-плацебо вне шардной matrix (не несёт --scope с ключами): npm run check:antiplacebo
workflow-команд: 37 · … · анти-плацебо-запусков: 3 · расхождений: 1
```

Тот же шаг, к строке дописан SHELL-КОММЕНТАРИЙ с текстом чужой записи (запись
`команда: mkdir -p "$TMP_ROOT/scripts"` объявлена для ДРУГОГО шага — сборки mktemp-корня
сам-тестов — и об анти-плацебо-прогоне не говорит ничего):

```
      - name: Скрытый не-шардный прогон анти-плацебо
        run: |
          npm run check:antiplacebo # mkdir -p "$TMP_ROOT/scripts"
```
```
workflow-команд: 37 · скриптов в приёмке: 46 · объявленных исключений: 24 · matrix-ключей: 35 · анти-плацебо-запусков: 3 · расхождений: 0
```

Барьер СЧИТАЕТ этот запуск (`анти-плацебо-запусков: 3`) и молчит. Инвариант 4 — «либо все
запуски анти-плацебо шардные, либо это красное» — обойдён текстом в комментарии.

Что это регрессия ИМЕННО `52f0079`, а не наследство: то же обманное дерево прогнано
редакцией барьера из `05e41f7` (`git show 05e41f7:scripts/verify_ci_parity.sh`):

```
FAIL .github/workflows/ci.yml:132 (ci): запуск анти-плацебо вне шардной matrix (не несёт --scope с ключами): npm run check:antiplacebo # mkdir -p "$TMP_ROOT/scripts"
```

До `52f0079` оплата искалась ПОСТРОЧНО и ТОЧНЫМ совпадением нормы строки
(`if [ -n "${EXC_CMD[$line_norm]:-}" ]`, в диффе `52f0079` — удалённые строки блока);
`52f0079` заменил это на подстроку по всему блоку. Красное стало зелёным.

Смежная форма того же дефекта (тоже зелёная на предмете, оплата чужой записью в
исполняемой позиции, не в комментарии):

```
        run: |
          mkdir -p "$TMP_ROOT/scripts"
          npm run check:antiplacebo
```
```
workflow-команд: 38 · … · анти-плацебо-запусков: 3 · расхождений: 0
RC=0
```

Санитарный контроль, что ветвь вообще работает: снятие `--scope` с шардного шага
(`ci.yml:87` → `npm run check:antiplacebo -- ${{ matrix.keys }}`) даёт

```
FAIL .github/workflows/ci.yml:87 (antiplacebo): шардный запуск анти-плацебо не несёт --scope с ключами — форма: npm run check:antiplacebo -- ${{ matrix.keys }}
… расхождений: 1
```

то есть ветвь жива и красна — обойдена именно оплатой.

Ответ на вопрос приёмки дословно: подстрочное сравнение со всеми ключами `EXC_CMD` —
НЕПРАВИЛЬНОЕ направление чтения. Оно легализует лишние записи: чужая запись, не
относящаяся к шагу вообще, делает шаг оплаченным.

### Н1 — находка того же класса, НЕ внесена этим кругом: `has_scope_keys` тоже читается подстрокой по всему блоку `run:`

`scripts/verify_ci_parity.sh:912`:

```
has_scope_keys = 1 if ('--scope ${{ matrix.keys }}' in norm_for_anti) else 0
```

`norm_for_anti` — весь блок `run:`. В ветви 4b.2 `[ "$has_scope" -eq 1 ] && continue`
(`:1267`) выкидывает шаг из проверки ДО оплаты. Шаг вне matrix, несущий этот литерал в
shell-комментарии, не проверяется вовсе — без единой записи исключения:

```
      - name: Скрытый не-шардный прогон анти-плацебо
        run: |
          npm run check:antiplacebo # --scope ${{ matrix.keys }}
```
```
workflow-команд: 37 · … · анти-плацебо-запусков: 3 · расхождений: 0
```

Граница формы измерена: та же подпись во flow-скаляре (`run: npm run check:antiplacebo #
--scope ${{ matrix.keys }}`) НЕ проходит — YAML срезает `#` как свой комментарий, барьер
краснеет (`FAIL … ci.yml:132`). Обход требует блочного скаляра `|`.

Происхождение: `git log -S'has_scope_keys' -- scripts/verify_ci_parity.sh` → `ece5eaa`
(базовый коммит контракта 020). Этим кругом не вносилось, в дельте `05e41f7~1..d2c4d6d`
эта строка не менялась. Называю, потому что это ТОТ ЖЕ дефект направления чтения в ТОМ ЖЕ
файле: чинить Б2, не закрыв Н1, бессмысленно — инвариант 4 останется обходимым.

### З2 — замечание круга к2, ОТКРЫТО (не блокер)

`ci.yml:40,44,45` ссылаются на редакции контракта, которых нет:

```
40:        # Р1 (контракт 020, v3.1): подъём шардов N=6 → N=5. v3.1 вынес `check_metering`
44:        # от числа шардов не зависит — сводка ключей динамическая. v3.2 ОТКАЗЫВАЕТСЯ от
45:        # шарда ap6 (контракт 020 v3.2, метеринг — флейки-выброс по решению владельца
```

Моя мера: заголовок контракта — `Контракт 020 (DRAFT v3: …)`;
`grep -c 'v3\.1\|v3\.2' contracts/020-shardirovanie-ci-antiplatsebo.md` → `0`;
`git tag -l '*020*'` → `frozen/contracts/020/1`, `frozen/contracts/020/2`,
`id/CONTRACT/020`. Редакций v3.1/v3.2 нет ни в документе, ни в тегах. Класс — прослеживаемость.

## Зоны и атомарность — чисто

`git log --format='%an' 05e41f7..d2c4d6d -- .github/workflows/ci.yml scripts/verify_ci_parity.sh`:

```
implementer | 52f0079 | 020 implementer: фикс 3 сцепленных дефектов паритета анти-плацебо (арбитраж c8aaa67, контракт 020)
implementer | 05e41f7 | 020 implementer: сам-тесты раннера анти-плацебо вне matrix (арбитраж c8aaa67)
```

Merge `d2c4d6d` — автор `orchestrator | orchestrator@dev-harness.local`.

Пофайлово оба коммита implementer лежат в ЗОНА implementer контракта
(`.github/workflows/ci.yml scripts/verify_ci_parity.sh config/ci_parity_exceptions.txt
fixtures/verify_ci_parity/`):

```
05e41f7: .github/workflows/ci.yml | config/ci_parity_exceptions.txt | scripts/verify_ci_parity.sh
52f0079: .github/workflows/ci.yml | scripts/verify_ci_parity.sh
```

Превышения области нет. Оба коммита несут ссылку на предмет (арбитраж `c8aaa67`,
контракт 020). Норма (`AGENTS.md`, нормативные документы) в дельте не тронута.

## Раннер заморожен — подтверждено

`git diff --stat 05e41f7~1..d2c4d6d -- scripts/verify_antiplacebo.sh` → пусто. Блоб
раннера `4196ca31b4308c095eaafef9afba77fda8012ee0` на всём диапазоне не менялся.
Фикстуры `fixtures/verify_antiplacebo/` в дельте не фигурируют: проверка не переписана под
реализацию.

## Вердикт

FAIL по Б2.

Приёмка арбитража закрыта по п.1, п.2, п.4 полностью; по п.3 закрыта в части rc=0 и канона
residual и НЕ закрыта в части механизма — прямо затребованной заданием проверки
направления подстрочного сравнения.

Блокер Б1 закрыт, и это первое предъявление Б2 — арбитр по Б1 не созывается и адреса не
получает. Б2 против несогласия автора в следующем круге был бы вторым отказом по одной
причине; сейчас — нет.

Что достаточно для accept следующего круга: ветвь 4b.2 обязана сопоставлять оплату с
КОМАНДОЙ запуска анти-плацебо, а не с текстом блока `run:` (и не подстрокой), а
`has_scope_keys` — с argv того же запуска, а не с блоком; Н1 закрывается тем же
изменением. Обманные деревья для красного предъявления — три формы выше, дословно.

Рекомендую держать красное предъявление в `fixtures/verify_ci_parity/` (каталог объявлен в
ЗОНА implementer и сейчас в дельте отсутствует): барьер `verify_ci_parity` входит в шард
ap5, и обходы такого класса иначе ловятся только руками судьи.
