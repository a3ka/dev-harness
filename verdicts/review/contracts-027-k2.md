FAIL

# Ревью контракта 027 — круг к2 (проверка фикса блокера F1)

Предмет круга: фикс `d47b0a5` (land `d9ceff1`) против блокера F1 вердикта
`verdicts/review/contracts-027-k1.md` (`c5d5c84`). Судимый HEAD —
`d9ceff1ba91ec97d2ab39902e0855b5b9d464e73`; клон собственной рукой —
`/tmp/dev-harness-verify/rev-027/repo2` (свежий `git clone` основного дерева).
Все rc ниже получены в этом клоне.

Привязка блобами (Н-101; новая редакция не наследует принятие старого блоба):

| файл | блоб на HEAD |
|---|---|
| `scripts/doc_contract.ts` | `70713ed801` (было `54a5016ed1`) |
| `scripts/check_contract_ready.sh` | `a92090170a` (было `09aa8d3ede`) |
| `scripts/freeze_contract.sh` | `e2d8b902fd` (было `252e391461`) |
| `contracts/027-doc-priemka.md` | `5d3f9f31aa` (без изменений) |
| `fixtures/check_check_contract_ready/_doc027.py` | `d6bf95920c` (без изменений) |

Область правки: `d47b0a5` трогает ровно три файла, все в ЗОНА implementer;
барьеры и каркас `_doc027.py` не тронуты — проверка под реализацию не
переписана. `git diff frozen/contracts/027/3 HEAD -- contracts/027-doc-priemka.md`
→ rc=0, 0 байт вывода: замороженный текст не тронут.

## F1 — ЗАКРЫТ (предъявлено моим контрпримером, не пересказом)

`scripts/doc_contract.ts:791-827` — `detectDocTypeFromFile` и CLI-вход `--type`
через тот же `parseSpecFromMarkdown`; `check_contract_ready.sh:103` и
`freeze_contract.sh:259` вызывают его вместо парного `grep`. Мой прежний
контрпример (`/tmp/dev-harness-verify/rev-027/probe_typeline.py`, расширен до
`probe_round2.py`) на новом коде:

```
[КОНТРОЛЬ honest single] ready rc=0 | preflight rc=0 | freeze rc=0 | frozen-теги: frozen/contracts/001/1
[КОНТРОЛЬ honest split]  ready rc=0 | preflight rc=0 | freeze rc=0 | frozen-теги: frozen/contracts/001/1
[A0 single/empty]        ready rc=1 | preflight rc=1 | freeze rc=1 | frozen-теги: (нет)
[A  split/empty]         ready rc=1 | preflight rc=1 | freeze rc=1 | frozen-теги: (нет)
```

Строка A — ровно тот вход, который в к1 давал `ready rc=0` и записывал тег:
теперь `ОТКАЗ doc-preflight: ОТКАЗ DOC: schema: product: spec.required.sections пуст`,
freeze rc=1 БЕЗ тега (атомарность сохранена). Оба положительных контроля зелёные
в обоих форматах — фикс не куплен отказом конформному входу.

## Собственные прогоны на фиксе (сырые коды)

```
rc=0 fixtures/check_check_contract_ready/red_doc_obligations.sh
rc=0 fixtures/check_check_contract_ready/red_doc_product.sh
rc=0 fixtures/check_check_contract_ready/red_doc_architecture.sh
rc=0 fixtures/check_check_contract_ready/red_doc_evidence.sh
rc=0 fixtures/check_check_contract_ready/red_doc_assertions.sh
rc=0 fixtures/check_check_contract_ready/red_doc_status_render.sh
rc=0 fixtures/check_check_contract_ready/red_doc_oracle.sh
rc=0 fixtures/freeze_contract/red_doc_lifecycle.sh
rc=0 python3 fixtures/check_check_contract_ready/_doc027.py regressions
rc=0 npm run check:document
rc=0 npm run check:contract-ready
rc=0 npm run check:ci-parity
rc=0 scoped antiplacebo :: барьеров: 4 · фикстур: 22 · предъявлено красным повторным прогоном: 22
rc=0 git diff frozen/contracts/027/3 HEAD -- contracts/027-doc-priemka.md (0 байт)
```

Классификация `doc_contract.ts` как НЕ БАРЬЕР после появления в нём CLI с
кодами возврата проверена мерой, а не прочтением шапки: раннер по-прежнему
насчитывает 4 барьера и 22 фикстуры, коллизии ключей нет.

## F5 — НОВЫЙ БЛОКЕР. Неоднозначный doc-контракт не отвергается: разбирается ПЕРВЫЙ блок, остальное невидимо, freeze проходит

Обязательство (замороженный текст, §Грамматика doc-контракта, дословно):
«У будущего doc-контракта **ровно один раздел `## Док-приёмка` и ровно один
fenced `json`-блок внутри него**»; далее в перечне именованных отказов:
«Дубли ID …, неизвестный профиль/статус/тип проверки, неверные типы полей и
**неоднозначный блок — именованный отказ rc=1**».

Точный фрагмент результата — `scripts/doc_contract.ts:82-120`
(`parseSpecFromMarkdown`, блоб `70713ed801`, фиксом не изменялся): внешний цикл
находит ПЕРВЫЙ `## Док-приёмка` и делает `break`; внутренний находит ПЕРВЫЙ
```` ```<слово> ```` и делает `break`. Второй раздел и второй блок не считаются
и не диагностируются; отказ выдаётся только при полном отсутствии
раздела/блока (`:113-115`).

Мои контрольные эксперименты (`probe_round2b.py`, `probe_round2c.py`;
игрушка та же, что в фикстуре `lifecycle`; «нарушающий» блок — те же пустые
`required.sections`, которые сам checker признаёт отказом):

```
[D good+bad]      ready rc=0 | preflight rc=0 | freeze rc=0 | frozen-теги: frozen/contracts/001/1
[E bad+good]      ready rc=1 | preflight rc=1 | freeze rc=1 | frozen-теги: (нет)
[F two-sections]  ready rc=0 | preflight rc=0 | freeze rc=0 | frozen-теги: frozen/contracts/001/1
```

D — два fenced json-блока в одном разделе, первый конформный, второй с пустыми
обязательствами: принято и **заморожено** (`frozen/contracts/001/1`).
F — два раздела `## Док-приёмка`, второй с пустыми обязательствами: то же самое.
E — зеркальный порядок (нарушающий блок первым) даёт rc=1: значит расхождение
вызвано ПОЗИЦИЕЙ блока, а не его значением — валидный контрпример по
§Демаркация (инвариантность к значениям при проходящем честном контроле;
оба честных контроля выше зелёные).

Почему блокер, а не замечание. Контракт называет неоднозначность отдельным
основанием rc=1 наравне с дублями ID и неизвестным профилем — те реализация
ловит, эту нет. Последствие то же, за которое остановлен круг к1: предмет
заморозки расходится с тем, что прочла машина. На этом стоит §Устойчивое
основание принятия («frozen-тег и OID блоба контракта + …»): замороженный блоб
несёт два взаимно противоречивых нормативных блока, из которых машина судила
один, а читатель вправе прочесть другой. Правка по коду (Н-39) дешёвая: считать
разделы и fenced-блоки внутри раздела, отказывать именованно при count≠1.

Это НЕ повтор причины F1 (та была о подстрочной эвристике ВНЕ общего модуля;
эта — о самом общем модуле) и не повтор причин арбитража 1/2 (argv, регистр OID).
Дефект пре-существующий: эта часть `parseSpecFromMarkdown` не менялась с
`ff26c04`, три круга адверсария и арбитраж её не касались; фикс `d47b0a5` его
не вносил и устранять не был обязан. Назван потому, что найден моей мерой.

## F6 — локальная ветвь `mktemp` в freeze fail-open, но недостижима (класс: наблюдение, НЕ блокер)

Фикс ввёл в `freeze_contract.sh:255-256` конструкцию
`_doc_type_tmp="$(mktemp … || true)"` + `if [ -n "${_doc_type_tmp:-}" ]`: при
отказе `mktemp` ВЕСЬ doc-гейт был бы молча пропущен (а не rc=1/2), что
противоречило бы rc-семантике контракта. Измерено:
`TMPDIR=/nonexistent-tmp-rev027` + нарушающий doc-контракт → freeze rc=1, тег
не записан (`probe_round2.py`, строка `C`). Причина зелёного результата не в
самой ветви: под тем же условием раньше падает `_events="$(mktemp)"` на
`:169` (без `|| true`, при `set -e`), поэтому до `:255` управление не доходит.
Ветвь fail-open по построению, но недостижима; названа, чтобы `|| true` не
пережил будущую правку `:169`.

## Перенесённые находки круга к1 (не блокеры, состояние не изменилось)

- **F2** — «нечем проверить» в `resolveGitSource` (`doc_contract.ts:510-512`)
  отображается в rc=1 с именем «дрейфует», а не в rc=2. Фиксом не затронуто.
- **F3** — residual argv назван в комментарии кода (`doc_contract.ts:187-203`),
  но до адресата (ревьюер при freeze) не доставлен: `roles/reviewer.md` и
  §Мандат контракта о probe.argv молчат.
- **F4** — residual по регистру OID не имеет носителя в дереве, а комментарий
  `doc_contract.ts:29-32` по-прежнему объявляет класс «ровно 40/64 hex» при
  lower-case-регулярке на `:32`.

## Граница вердикта

Судья стороной спора не стал: F1 признан закрытым по собственному измерению,
F5 — иная причина, не повтор. Правило «два отказа по одной причине — арбитр»
здесь не срабатывает; созывать арбитра или принять F5 именованным остатком —
решение оркестратора/владельца, не моё. Чужой код я не правил: F5 назван, не
исправлен. Полный CI, содержательный суд конкретного doc-пакета и предметы
020/026/028 этим вердиктом не закрываются.

Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза контракта;
контракт несёт инварианты + rc-команды».

Н-101: блоб этого текста записан `git hash-object -w` и добавлен в индекс
`update-index --cacheinfo`; identity коммита задана явно как `reviewer`.
Н-73: использована нейтральная терминология контрольных экспериментов и
контрпримеров.
