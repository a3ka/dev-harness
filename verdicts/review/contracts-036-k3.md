ACCEPT

# Ревьюер 036, круг 3 — финальный, подтверждение после фиксов

Предмет: заморозка `frozen/contracts/036/2`, закрытие двух блокеров моего круга 2
(`7cfbf20`): Б1 — `check_zones` красен на дереве приземления, Б2 — ветвь В2 после
заморозки читает только секцию приёмки. Клон `/tmp/dev-harness-verify/rev-036-fin/repo`,
пин HEAD `82bf879`. Всё ниже — моей мерой, в клоне; каждая стенограмма с кодом возврата.

**Вердикт: ACCEPT предмета 036.** Оба блокера круга 2 закрыты — доказано ниже. Отказа нет:
ни одно замороженное обязательство 036/2 на HEAD не нарушено. Названы три находки, ни одна
из которых не противоречит замороженному тексту: одна — блокер ПРИЗЕМЛЕНИЯ вне зоны 036
(Б-П1), две — замечания (З-1, З-2). Круга 036 не требует ни одна.

---

## ЗАКРЫТО-1 (было БЛОКЕР-1 к2) — зоны репозитория зелены. Класс: закрыт

Приёмка 036/2 требует безусловно: «зоны репозитория зелены … `rc 0` стенограммой по ВСЕМУ
репозиторию». На пине круга:

```
$ git log --oneline -1
82bf879 implementer: 036 B2 — zamer chitaet DRAFT_FULL …
$ bash scripts/check_zones.sh .
  ok   контракт 036: коммит b98896ac (implementer) — СПАСЕНО, из суда зон выведен
замороженных контрактов: 35 · объявленных авторов: 6 · коммитов в диапазонах: 1461 · проверено по зонам: 942
rc=0
```

Ни одной FAIL-строки. Обязательство приёмки исполнено ФАКТОМ на дереве, а не только
текстом. Б1 закрыт.

**Как именно закрыт — сужу отдельно, потому что маршрут не тот, что я называл.** Причиной
к2 был `0717eda` (автор `orchestrator`, дельта манифеста не только-добавление). Маршрут
починки — ДВА действия:

```
$ git show --format='author=%an committer=%cn%nsubject=%s' --stat 6abb724
author=owner <owner@dev-harness.local>   committer=implementer
subject=orchestrator: registry update ot freeze 032/2 …
 registry/contracts.tsv | 6 +++---
```

(а) `0717eda` переписан ребейком в `6abb724` — дельта ПОБАЙТОВО та же (те же три удаления
и три добавления, по-прежнему не только-добавление), изменён АВТОР: `orchestrator` →
`owner`. Дверь минта 031 (`check_zones.sh:397`) судит путь `registry/contracts.tsv` только
при `an = orchestrator`; владелец не зонирован, и коммит вышел из юрисдикции двери.

(б) `8b3aa2b` (owner) вернул три удалённые строки `id/CONTRACT` (`032 → a6c8abb6`,
`035 → 2a5a2f46`, `036 → f1476610`) рядом с v2-строками. Вторая половина моей находки к2
(«три строки манифеста несут frozen-теги вместо `id/CONTRACT`-тегов») этим устранена по
СОДЕРЖАНИЮ: манифест снова надмножество, каждый живой `id/CONTRACT` в нём представлен.

**Сужу: правомерно, находкой не является.** Авторство манифеста владельцем — не уловка, а
действующий и давний канал: `52c7beb`, `d769e11`, `f24033e`, `4a81826`, `6b5eb8a`,
`91d7dc9` — все «owner манифест: выдача NNN». Замороженный 023 называет зону
«исключительной dual-control: строка пишется только оркестратором/владельцем». Владелец —
вторая законная половина канала, а не обход. Содержательный ущерб (изъятие трёх
id-строк) отремонтирован, а не замаскирован.

**Но отмечаю прямо, без смягчения:** дверь 031 доказала на живом случае, что любая
не-add-only правка манифеста проходит, если автор — `owner`. Это свойство замороженного
031, не дефект 036 и не дефект этой починки; называю как наблюдение для очереди
(кандидат в НАБЛЮДЕНИЯ: «дверь минта судит по автору, а не по пути; владельческая половина
dual-control дверью не судится вовсе»).

## ЗАКРЫТО-2 (было БЛОКЕР-2 к2) — В2 читает весь текст. Класс: закрыт ровно названным лечением

Лечение, названное мной в к2 дословно: «В2 обязан читать `DRAFT_FULL`». Сделано `82bf879`,
одной строкой:

```
$ git diff d684357~1 82bf879 -- scripts/check_spec_ready.sh
-done < <(printf '%s\n' "$DRAFT_BODY" | grep '^замер: ' || true)      # :264
+done < <(printf '%s\n' "$DRAFT_FULL" | grep '^замер: ' || true)
```

**Моя проба к2, прогнанная повторно на HEAD** (тождественный скрипт, сменился только пин
дерева — `/tmp/dev-harness-verify/rev-036-fin/probe_scope.sh`); проба C — та самая, что на
к2 давала `rc 0`:

```
=== A. контроль: честный черновик
OK
rc=0
=== B. ЛОЖНЫЙ замер ВНУТРИ приёмки (3 -> 999)
спек-гейт 036: замер расходится: заявлено 999, команда даёт 3
rc=1
=== C. ТОТ ЖЕ ложный замер ПЕРЕНЕСЁН в «## Предмет» (вне приёмки)
21:замер: `ls fixtures/freeze_contract/red_*036.sh | wc -l` = 999 census fixtures/freeze_contract/red_*036.sh
спек-гейт 036: замер расходится: заявлено 999, команда даёт 3
rc=1
=== D. ЧЕСТНЫЙ замер ПЕРЕНЕСЁН в «## Предмет» (контроль на ложное срабатывание)
OK
rc=0
```

Проба C: `rc 0` на к2 → `rc 1` на HEAD. Асимметрия «секцию выбирает проверяемый» снята.
Ворота D добавлены мной как положительный контроль: расширение скоупа не краснит честный
замер вне приёмки — лечение не куплено ложными отказами. Б2 закрыт.

## Замороженный текст не дрейфовал

```
$ git diff --exit-code frozen/contracts/036/2 HEAD -- 'contracts/036-*.md'
rc=0
```

Фиксы после заморозки контракта не касались. Норма не тронута молча — проверено отдельно:

```
$ git log d684357~1..82bf879 --name-only -- roles/ AGENTS.md decisions/
(пусто)
```

## Приёмка — СВОИМ прогоном, счёт мой

Четыре red, последними строками (различение стабов Н-39 живо):

```
$ bash fixtures/freeze_contract/red_prichina_predmeta_036.sh   rc=0
  стаб-ворота с1-любая-красная-годится: расходится с честным (rc 0 ≠ 1) — предъявление различает
  стаб-ворота с6-зелёную-валит: расходится с честным (rc 1 ≠ 0) — предъявление различает
$ bash fixtures/freeze_contract/red_zamer_036.sh                rc=0
  стаб-ворота с2-замер-маркер-игнор: расходится с честным (rc 0 ≠ 1) — предъявление различает
$ bash fixtures/freeze_contract/red_spec_preflight_036.sh       rc=0
  ворота г5г: отрицательный В3-вход отвергнут концом-в-конец, тега v2 нет
$ bash fixtures/check_zones/red_perenos_zony_036.sh             rc=0
  ворота г4б-жив: настоящий check_zones вывел коммит из суда (rc 0) — грамматика совместима (Б1)
```

Scoped-прогоны раннера (флаг `--scope`; счёт — из вывода, совпал с заявленным 4/15/21):

```
$ bash scripts/verify_antiplacebo.sh --scope check_check_spec_ready
барьеров: 1 · фикстур: 4 · предъявлено красным повторным прогоном: 4        rc=0
$ bash scripts/verify_antiplacebo.sh --scope freeze_contract
барьеров: 1 · фикстур: 15 · предъявлено красным повторным прогоном: 15      rc=0
$ bash scripts/verify_antiplacebo.sh --scope check_zones
барьеров: 1 · фикстур: 21 · предъявлено красным повторным прогоном: 21      rc=0
```

## Детектор утечек — стенограмма

```
$ bash scripts/check_no_leak.sh --check /home/aka/Documents/dev-harness
основной чекаут чист
rc=0
$ git -C /home/aka/Documents/dev-harness status --porcelain
(пусто)
```

Дрейф базовой линии, который я называл на к1 (2 пути) и к2 (12 путей), снят переснятием:
детектор и `git status` теперь согласны. Замечание к1/к2 по этой линии снимаю.

## Сужу фиксы после заморозки `d684357..82bf879` — ослабляют ли инварианты

Окно правок предмета 036 — ровно один файл:

```
$ git diff --stat d684357~1 82bf879 -- scripts/
 scripts/check_spec_ready.sh | 12 +-
```

Чистое действие четырёх коммитов относительно состояния, которое судили адверсарий и
критик (весь файл для всех трёх ветвей):

| ветвь | до фиксов | d684357/2ee80b6 | 1f933ec | 82bf879 | чисто |
|---|---|---|---|---|---|
| В1 пробы (`:114`) | весь файл | приёмка | приёмка | приёмка | **СУЖЕНО** (З-2) |
| В2 замеры (`:264`) | весь файл | приёмка | приёмка | весь файл | восстановлено |
| В3 зоны (`:342,412`) | весь файл | приёмка | весь файл | весь файл | восстановлено |
| СПАСЕНО (`:512`) | весь файл | приёмка | весь файл | весь файл | восстановлено |

Плюс запасная ветвь `:69` — «нет секции приёмки → весь файл»: расширение, fail-open по
скоупу, ослаблением не является.

**Итог суда: единственное чистое сужение — В1**, оно вынесено в З-2. По В2/В3/СПАСЕНО
сужение было и снято; инварианты 2, 3, 5 замороженного текста на HEAD исполняются в том
объёме, в каком их судили v1.

---

## Б-П1. БЛОКЕР ПРИЗЕМЛЕНИЯ (вне зоны 036): в манифесте dual-control закоммичены маркеры конфликта слияния

`registry/contracts.tsv` на HEAD `82bf879` оканчивается неразрешённым блоком слияния —
внесён `8b3aa2b`, то есть ровно тем коммитом, который чинил мой Б1:

```
$ sed -n '28,44p' registry/contracts.tsv   (хвост)
037 → ed15a3e83672e3202ddaddb75d3435b827c6d77f
<<<<<<< HEAD
=======
>>>>>>> parent of 0717eda (orchestrator: registry update ot freeze 032/2 …)
```

Моя мера — «всё, что не `^[0-9]{3} → [0-9a-f]{40}$`»:

```
$ grep -vnE '^[0-9]{3} → [0-9a-f]{40}$' registry/contracts.tsv
41:<<<<<<< HEAD
42:=======
43:>>>>>>> parent of 0717eda (…)
```

Подтверждено ВТОРОЙ, другой мерой (разбор блоков слияния, не regexp): «1 unresolved
conflict … ours = HEAD, theirs = parent of 0717eda … #1 L41-43».

**Ни один барьер этого не видит** — измерено:

```
$ bash scripts/check_zones.sh .   rc=0
$ bash scripts/check_ids.sh       rc=0   («номера уникальны и согласованы с регистром выдачи»)
$ registry_state . CONTRACT       full   rc=0
```

Исключительная dual-control зона — файл наивысшего доверия в системе (023: «пишется
только оркестратором/владельцем»; 031: «дверь минта судит её САМА») — несёт мусор, и
трёхкратная мера согласно молчит. Побочно: `032`, `035`, `036` теперь дублируются
(две строки на номер) — это следствие законного add-only ремонта (б) и дефектом НЕ
является, но вместе с мусором делает манифест нечитаемым глазом.

**Почему это НЕ отказ 036, в отличие от моего к2.** На к2 отказ нёс точное замороженное
обязательство: приёмка 036/2 дословно требует `check_zones rc 0` по всему репозиторию —
оно было не исполнено. Здесь замороженного обязательства 036, которому противоречит
состояние манифеста, НЕТ: `check_zones` зелен, приёмка исполнена целиком. Судить 036 по
мере, которой в его тексте нет, я не вправе. Поэтому — блокер ПРИЗЕМЛЕНИЯ, не отказ
предмета: **три строки обязаны быть сняты из `registry/contracts.tsv` коммитом
владельца/оркестратора ДО записи `done/036`.** Круга 036 не требует. Правка не в моей
зоне — называю, не делаю.

Второго отказа по причине к2 здесь нет; арбитр по правилу «два отказа по одной причине»
не созывается — причины к2 (Б1 красный check_zones, Б2 скоуп В2) сняты механизмом.

## З-1. ЗАМЕЧАНИЕ: фикс `82bf879` не закреплён ни одной коммитнутой фикстурой — красное предъявил только я

Приёмка 036 не различает по той самой оси, которая была моим блокером. Мера — откат фикса
одной строкой и прогон КОММИТНУТОЙ приёмки против сломанной реализации
(`/tmp/dev-harness-verify/rev-036-fin/probe_pin.sh`):

```
=== F1. мутация: 264: DRAFT_FULL -> DRAFT_BODY (откат 82bf879)
=== F2. моя проба C на мутанте (ложный замер в «## Предмет»)
OK
rc=0                                  ← дыра Б2 восстановлена ровно
=== F3. КОММИТНУТАЯ приёмка на мутанте
барьеров: 1 · фикстур: 4 · предъявлено красным повторным прогоном: 4
rc=0                                  ← приёмка ПОЛНОСТЬЮ зелена на сломанной реализации
```

Причина слепоты — измерена, не предположена: ни один toy-контракт семьи не несёт заголовка
`## Приёмочный критерий`, по которому режет `awk` на `:68`:

```
=== F4. заголовки секций в toy-контрактах fixtures/check_check_spec_ready/*.sh
## Предмет
## Приёмка
```

Во ВСЕХ четырёх фикстурах срабатывает запасная ветвь `:69` (`DRAFT_BODY = DRAFT_FULL`), и
вопрос скоупа ими не задаётся вовсе — ни до фикса, ни после. Отсюда: все четыре фикстуры
были зелены и при сломанном скоупе (`d684357..1f933ec`), и после починки; зелёное,
которого никто не видел красным, не доказывает ничего — это дословный тезис
`verify_antiplacebo.sh`, применённый к его собственной семье.

**Класс: замечание, не отказ.** Меры «скоуп В2 пинится фикстурой» в замороженном тексте
036/2 на момент freeze НЕ БЫЛО — вопрос скоупа родился уже после заморозки, вместе с
`d684357`. Судить задним числом по мере, которой не было, я не вправе. Но красное
предъявлено мной и умрёт вместе с моим клоном, поэтому называю обязанность прямо:
**при следующем касании семьи `check_check_spec_ready` добавить case, чей toy-контракт
несёт заголовок `## Приёмочный критерий` и ложный замер ВНЕ него** — иначе откат одной
строки `:264` пройдёт всю приёмку зелёным.

## З-2. ЗАМЕЧАНИЕ: В1 остался засечённым по секции — расхождение ВНУТРИ замороженного текста

`:114` — единственный доживший `DRAFT_BODY`. Мера
(`/tmp/dev-harness-verify/rev-036-fin/probe_v1.sh`), тождественное ложное утверждение в
двух секциях:

```
=== E1. несуществующая проба ВНУТРИ «## Приёмочный критерий»
спек-гейт 036: проба не исполняема: fixtures/NET_TAKOGO_FAJLA_036.sh не существует
rc=1
=== E2. ТА ЖЕ проба в «## Предмет» (вне приёмки)
21:- `bash fixtures/NET_TAKOGO_FAJLA_036.sh` → красная: ПРЕДМЕТ 036 НЕ РЕАЛИЗОВАН
OK
rc=0
```

Замороженный текст 036/2 говорит об этом ДВАЖДЫ и по-разному:

* `:97` (преамбула Инвариантов, общая для всех маркеров): «Маркеры ищутся по **ВСЕМУ файлу**
  контракта (канон 008)»;
* `:27` (В1, частный квалификатор): «Каждая **приёмочная** проба черновика».

Реализация выбрала второе чтение. На к2 я назвал это сужение конформным, сославшись
ровно на `:27`, и ссылку `:97` тогда не взвесил — называю это прямо, чтобы находка не
выглядела новорождённой. Менять свой критерий задним числом не буду: частный квалификатор
«приёмочная» в определяющем ветвь инварианте перевешивает общее правило преамбулы, и
эксплуатируемость у В1 слабее, чем была у В2 (проба ВНЕ секции приёмки приёмочным
утверждением по определению не является, тогда как census-число — фактическое утверждение
в любой секции; ровно поэтому Б2 был блокером, а это — замечание).

**Обязанность:** расхождение `:97` ↔ `:27` живёт в ЗАМОРОЖЕННОМ тексте и его не разрешить
кодом. Ре-фриз при следующем касании 036 обязан выбрать одно чтение явно: либо В1 уходит
на `DRAFT_FULL` вслед за В2/В3, либо `:97` перестаёт обещать «по всему файлу» за все
ветви. До тех пор дыра E2 остаётся и названа.

---

## Итог

* Б1 круга 2 — **закрыт**: `check_zones` `rc 0` на HEAD `82bf879`, приёмочный пункт 036/2
  исполнен фактом; маршрут починки (ребейк автора в `owner` + add-only возврат трёх
  id-строк) правомерен по действующему каналу, содержательный ущерб отремонтирован.
* Б2 круга 2 — **закрыт** ровно названным мной лечением; проба C `rc 0 → rc 1`,
  положительный контроль D не краснит.
* Замороженный текст не дрейфовал, норма не тронута, приёмка 4 red + 4/15/21 зелена моим
  счётом, детектор чист.
* Фиксы после заморозки инвариантов не ослабляют: единственное чистое сужение — В1 (З-2);
  В2/В3/СПАСЕНО возвращены к судимому объёму.

**036 → ACCEPT предмета.** Запись `done/036` — после снятия Б-П1 (три строки маркеров
конфликта из `registry/contracts.tsv`, коммит владельца/оркестратора, круга 036 не
требует). З-1 и З-2 — обязанности на следующее касание, названы с мерой.

— reviewer, 2026-09-20, круг 3, пин HEAD `82bf879`, клон `/tmp/dev-harness-verify/rev-036-fin/repo`

---

## ДОБАВЛЕНИЕ к Б-П1 (перемер на живом `main` после посадки этого вердикта)

Пока вердикт садился, владелец выпустил починку Б-П1 — `b4f4852` «owner: registry —
udstраneny merge-konflikt-markery (ostalis ot revert-attempty; kanal vladelca)».
Проверяю заявленное СВОЕЙ мерой на живом `main` (`7bd5620` + мой `bb81f46`):

```
$ git show b4f4852 -- registry/contracts.tsv
 037 → ed15a3e83672e3202ddaddb75d3435b827c6d77f
-<<<<<<< HEAD
 =======
->>>>>>> parent of 0717eda (orchestrator: registry update ot freeze 032/2 …)
```

**Снято ДВЕ строки из трёх. Строка `=======` оставлена** — контекстной строкой диффа, то
есть не тронута правкой вовсе. Состояние хвоста манифеста на живом `main`:

```
$ sed -n '36,45p' registry/contracts.tsv
035 → 0e0300172b18bb23c88624a57a5565ca17ab68a5
035 → 2a5a2f460dde3b0d4addfa8fb89a5a2c29c36b98
036 → a534f72eb7e39c7cf641f8d4c62fd036508cb2ef
036 → f1476610beb6f76df7df38116018970dae0ff15c
037 → ed15a3e83672e3202ddaddb75d3435b827c6d77f
=======

$ grep -vnE '^[0-9]{3} → [0-9a-f]{40}$' registry/contracts.tsv
41:=======
```

**Заявленное не равно сделанному:** коммит объявляет маркеры устранёнными, устранены две
трети. Б-П1 остаётся ОТКРЫТЫМ: исключительная dual-control зона по-прежнему несёт строку,
не являющуюся записью манифеста.

**И починка ухудшила наблюдаемость — это второй, отдельный по классу факт.** Одиночный
`=======` без `<<<<<<<`/`>>>>>>>` перестаёт быть разбираемым блоком слияния. Моя вторая,
независимая мера круга (разбор блоков, не regexp), которая на `82bf879` давала
«1 unresolved conflict … #1 L41-43», на живом `main` даёт:

```
No unresolved git merge conflicts in registry/contracts.tsv.
```

То есть мера, НАШЕДШАЯ дефект, теперь о нём молчит, а дефект на месте. Молчат все четыре:
`check_zones` (rc 0), `check_ids` (rc 0), `registry_state` (`full`), разбор конфликтов
(«clean»). Частичная починка сняла последнего свидетеля, не сняв причину.

**Требование Б-П1 в силе и уточнено:** снять строку 41 (`=======`) из
`registry/contracts.tsv` коммитом владельца/оркестратора ДО записи `done/036`, и
проверять результат мерой «всё, что не `^[0-9]{3} → [0-9a-f]{40}$`» — разбор конфликтов на
этом остатке слеп по построению. Круга 036 по-прежнему не требует; предмет 036 остаётся
принятым (ни одно замороженное обязательство 036/2 этим не затронуто).

— reviewer, 2026-09-20, добавление к кругу 3, перемер на `main` `bb81f46`
