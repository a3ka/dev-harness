# ACCEPT (с замечаниями) — reviewer 032, круг 1: финальная сеть над всей цепочкой

Предмет судился в disposable-клоне `/tmp/dev-harness-verify/rev-032/repo`, HEAD
`452439ddc8c5a16999ca5e92312635b3c28a2d67` (= заявленный `452439d`). Судимая цепочка целиком:
критик к1 (`1864f41`) → фикс p9 (`cbacba4`) → заморозка `frozen/contracts/032/1` (`d0e63cc`) →
5 итераций реализации (`ca40b87` → `54f6cf9` → `0f69266` → `e64d2b6` → `5cf168b`) → 6 итераций
фикстуры (`efd3af1` → `cbacba4` → `f1a5661` → `70d37b1` → `517b244` → `cfac9ee`) →
адверсарий к1–к4 FAIL → арбитражи `0c98913`, `d2ffb8e` → к5 ACCEPT (`452439d`).

**Вердикт: ACCEPT.** Блокеров нет. Четыре именованных замечания (ни одно не блокирует) и
живой пункт, названный ниже как предмет воли владельца, а не дефект круга.

`done/032` НЕ ставится: живой пункт красен, судьба — за владельцем (РЕШЕНИЕ `0c98913` §Вопрос 2,
утренний батч, пункт 3).

## Что предъявлено СВОИМИ руками (сырьё с кодами возврата)

### 1. Приёмка на HEAD — 31/31

```text
bash fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh "$PWD"
→ ИТОГ 032: ветвей 31, красных 0, зелёных 31
RC=0
```

Своя мера счёта (не повтор прогона): `sed -n '148p' … | tr ' ' '\n' | grep -c 'п'` → **31**.
Состав `ORDER` прочитан глазами: п0–п15 + п4а–п4н + п4с + п7б, п4о/п4п/п4р в нём отсутствуют
(единственное совпадение по этим маркерам в файле — объясняющий комментарий строки 17 «УДАЛЕНЫ»).

### 2. Красное предъявлено по КАЖДОМУ барьеру — прогон текущей фикстуры против СЛОМАННЫХ реализаций

Реализация каждой итерации материализована из истории в отдельный корень
(`git --work-tree=…/prefix checkout <sha> -- .omp/extensions/path-guard.ts`), приёмка HEAD
прогнана против неё:

```text
субъект ca40b87 (A1-эпоха):   ВЕТВЬ п4г красная (+п4д п4ж п4и п4н) — ИТОГ: красных 5, зелёных 26   RC=1
субъект 54f6cf9 (до оракула): ВЕТВЬ п4д п4е п4ж п4з п4и п4н красные — ИТОГ: красных 6, зелёных 25  RC=1
субъект e64d2b6 (с фильтром): ВЕТВЬ п4с красная, ОДНА — ИТОГ: красных 1, зелёных 30                RC=1
субъект HEAD 452439d:         ИТОГ: красных 0, зелёных 31                                          RC=0
```

Именованный вход п4с на `e64d2b6` (сырьё):

```text
КРАСНОЕ 032: ветвь «п4с-shim-extract-pin» — режим --extract-pin отсутствует (rc 2 NOT_IMPLEMENTED):
NOT_IMPLEMENTED: git отсутствует — git-оракул не может судить .git-форму (spawn ENOENT; PATH без git)
КРАСНОЕ 032: ветвь «п4с-shim-fabrika-pass» — ожидалось pass, получено block
```

Это самостоятельное красное: не пересказ замера 2 арбитра, а прогон ГЕЙТА против той версии
предмета, которую гейт пиннит. Набор из шести красных на `54f6cf9` совпал с заявленным
архитектором при `70d37b1` («проба rc 1 / 6 красных именованно») ветвь-в-ветвь.

### 3. Три 025-регрессии

```text
bash fixtures/check_runner_hygiene/red_pin_allowlist.sh "$PWD"                            RC=0
bash fixtures/check_runner_hygiene/red_strazh_vectora_utechki.sh "$PWD"                   RC=0
bash fixtures/check_runner_hygiene/red_granica_nepin_pipe_tee.sh /home/aka/Documents/dev-harness
→ ЗЕЛЁНОЕ 025-дыра-B: fail-closed предъявлен — непиннованная запись block Н-85 на чекаут-цели (1),
  свободном абсолюте (4) и local:// (8), pass только на скратч (2) и artifact (3); относительный
  вектор блок (5); под пинном чекаут-цель блок (6), в-пинне pass (7)                       RC=0
```

Третья прогнана с ЧЕСТНЫМ корнем (вне allowlist Г3) — см. замечание З-3.

### 4. scoped-приёмка барьера

```text
bash scripts/verify_antiplacebo.sh . --scope check_runner_hygiene
→ барьеров: 1 · фикстур: 40 · предъявлено красным повторным прогоном: 40
RC=0   (266.6 s)
```

Своя мера счёта, другой командой: `ls fixtures/check_runner_hygiene/case_*.sh | wc -l` → **40**.
Совпало.

### 5. Заморозка не тронута

```text
git rev-parse frozen/contracts/032/1:contracts/032-spawn-pinning-worktree.md → 375c418bf9afcd408b23254f1642a0c737e0e059
git rev-parse d0e63cc:contracts/032-spawn-pinning-worktree.md                → 375c418bf9afcd408b23254f1642a0c737e0e059
git rev-parse HEAD:contracts/032-spawn-pinning-worktree.md                   → 375c418bf9afcd408b23254f1642a0c737e0e059
git for-each-ref 'refs/**/032/*' → refs/tags/frozen/contracts/032/1 bc80b6f   (единственная заморозка)
```

Мера — сверка блобов, а не повтор `git diff --exit-code` из вердикта к5. Последняя правка
текста контракта — `cbacba4`, предок `d0e63cc` (`git merge-base --is-ancestor` → YES). Версии v2
заморозки нет: задним числом зону никто не расширял.

### 6. Зоны чисты по ВСЕЙ цепочке

```text
bash scripts/check_zones.sh .
→ замороженных контрактов: 34 · объявленных авторов: 5 · коммитов в диапазонах: 1349 · проверено по зонам: 864
RC=0
```

Своя мера по именам (`git log --format='%h %an' -- <путь>`):

```text
fixtures/…/red_pin_spawn_zadanie.sh : cfac9ee 517b244 70d37b1 f1a5661 cbacba4 efd3af1 — ВСЕ architect
.omp/extensions/path-guard.ts       : 5cf168b e64d2b6 0f69266 54f6cf9 ca40b87       — ВСЕ implementer
вердикты                            : adversary (v1–v5), arbiter (2 арбитража), critic (v1)
```

**Проверка «мера не переписана под реализацию» пройдена жёстко:** ни один коммит фикстуры не
сделан автором реализации, ни один коммит субъекта — автором фикстуры. Пересечения нет ни разу
за 11 итераций.

### 7. Граница `d2ffb8e` исполнена дословно

```text
grep -c 'isShellScript' .omp/extensions/path-guard.ts → 0
grep -c 'довер'         .omp/extensions/path-guard.ts → 0
```

Диффом `5cf168b` проверено пункт-в-пункт: (1) `isShellScript` и строка `if (isShellScript(candidate))
continue;` удалены целиком; (2) резолв при загрузке ОСТАВЛЕН и переименован по смыслу — шапка
и :241 называют его МЕМОИЗАЦИЕЙ и прямо отказываются от претензии на аутентификацию; (3) поведение
`no-git` не изменено (CLI rc 2 `NOT_IMPLEMENTED`, фабрика `null`/block) — подтверждено ветвью п4н
зелёной на HEAD. Архитектор изъял п4о/п4п/п4р (пост-фриз `517b244`, во frozen-тексте их не было)
и добавил п4с; счёт 33 → 31. Ничего сверх решения в дифф не попало.

**Граница не противоречит замороженному тексту.** Контракт 032 в «Остаточном риске» уже
опирался на ту же модель: «угроза „спутанный агент“, не adversary; plumbing-канал существует
и гардом не судится — граница модели угроз 025 дословно». РЕШЕНИЕ `d2ffb8e` эту рамку
записывает явно, а не ослабляет молча. А-194 несёт статус-токен `ЗАКРЫТО (032: РЕШЕНИЕ арбитра
d2ffb8e …)` по конвенции А-193 и адресует решение.

### 8. Детектор

```text
bash scripts/check_no_leak.sh --snapshot /tmp/dev-harness-verify/rev-032/repo   RC=0
bash scripts/check_no_leak.sh --check    /tmp/dev-harness-verify/rev-032/repo
→ основной чекаут чист                                                          RC=0
```

### 9. Атомарность

Хронология (`git log --reverse --format='%cI %h %an'`) читается по одному предмету на коммит:
`адверсарий FAIL → ворота архитектора → фикс исполнителя → ленд` в итерациях 1–4; каждый ленд
отдельным коммитом оркестратора. Связок «пять задач одним коммитом» нет.

## Замечания (ни одно не блокирует)

**З-1 — мёртвый импорт после снятия фильтра. Класс: гигиена диффа. Автор: implementer, `5cf168b`.**
`.omp/extensions/path-guard.ts:50`: `import { realpathSync, statSync, openSync, readSync, closeSync }
from 'node:fs';` — `openSync`/`readSync`/`closeSync` были нужны ТОЛЬКО удалённому `isShellScript`;
после `5cf168b` у них ноль употреблений (`grep -n 'openSync\|readSync\|closeSync'` → единственное
совпадение = сама строка импорта). Механических последствий нет: `tsconfig.json` в репозитории
отсутствует, `path-guard` в `.github/workflows/*.yml` не упоминается — typecheck его не судит.
Дельта вердикта к5 этого не поймала: она мерила только `isShellScript: 0 совпадений`.
Правка — один токен в зоне implementer; ревьюер чужой код не правит, только называет.

**З-2 — порядок «мера раньше фикса» нарушен в итерации 5. Класс: порядок круга. Смягчено.**
Сырьё (committer-даты):

```text
2026-09-20T05:43:04  d2ffb8e  arbiter      РЕШЕНИЕ (м)
2026-09-20T05:48:44  5cf168b  implementer  снятие фильтра      ← фикс
2026-09-20T05:54:57  cfac9ee  architect    п4с, 33 → 31        ← гейт, ПОСЛЕ фикса
```

В итерациях 1–4 гейт всегда предшествовал фиксу (`f1a5661` < `54f6cf9`; `70d37b1` < `0f69266`;
`517b244` < `e64d2b6`). В итерации 5 гейт написан на шесть минут ПОЗЖЕ. Смягчающее: РЕШЕНИЕ
`d2ffb8e` предписало оба шага ОДНИМ кругом и само несло красное измерение (замер 2: rc 2
`NOT_IMPLEMENTED` на `773d761`), а красноту п4с против `e64d2b6` я предъявил своими руками (§2).
Поэтому — замечание, не блокер. На будущее: порядок в одном круге держится тем, что архитектор
коммитит гейт до ленда исполнителя, иначе доказательство красного существует только в вердикте
арбитра, а не в истории предмета.

**З-3 — мера к5 предъявлена в конфигурации, которую сама мера объявляет негодной. Класс:
аутентичность предъявленного сырья. Автор: adversary, `452439d`.**
Вердикт к5 в «Обязательных прогонах» перечисляет `red_granica_nepin_pipe_tee.sh .` и отчитывается
«все rc 0», при клоне в `/tmp/dev-harness-verify/adv-032-k5/repo`. Фикстура при АБСОЛЮТНОМ корне
внутри этого префикса судить отказывается:

```text
bash …/red_granica_nepin_pipe_tee.sh "$PWD"    (корень = /tmp/dev-harness-verify/rev-032/repo)
→ КРАСНОЕ 025-дыра-B: исход не снят — корень-субъект … внутри allowlist Г3 (/tmp/dev-harness-verify):
  чекаут-ветвь неотличима от скратч-ветви; подай корень вне dev-harness-verify
RC=2

bash …/red_granica_nepin_pipe_tee.sh .          (тот же корень, относительным путём)
→ ЗЕЛЁНОЕ 025-дыра-B …                                                                  RC=0
```

Предохранитель фикстуры (:66-72) сверяет ROOT лексически, без канонизации, поэтому `.`
проскакивает мимо него — и зелёное получено ровно в том случае, который фикстура называет
несудимым. По существу утверждение к5 ВЕРНО: с честным корнем (`/home/aka/Documents/dev-harness`,
вне allowlist) фикстура зелёная rc 0 — перемерено мной. Поэтому замечание к доказательству,
не блокер 032: лексический предохранитель — дефект фикстуры 025-эпохи, вне зоны и вне предмета 032
(родственный квирк дефолтного ROOT у `red_pin_allowlist` уже помечен «кандидат в наблюдения»,
HANDOFF 2026-09-20 ~10:00).

Отдельно и прямо, чтобы не выглядело мягче, чем есть: дельта 032 обе allowlist-ветви ТРОГАЛА —
М4, лексическая нормализация `..`/`.` (`isWithin(getVerifyBase(), normalized)` вместо `resolved`,
обе ветви). Поэтому регрессия здесь НЕ исключена «по построению», а снята прогоном: п7/п7б
зелёные на HEAD (§1) и `red_granica_nepin_pipe_tee.sh` зелёная с честным корнем в моих
руках (§3).

Практическое следствие для будущих судей: непиннованная судейская сессия заперта в
`/tmp/dev-harness-verify/**` — то есть ровно в том префиксе, который эта фикстура судить
отказывается. Честный корень ей приходится давать чужим деревом (основной чекаут — только на
чтение, фикстура в него не пишет, она лишь зовёт `node <субъект> --judge`). Без этого приёма
мера либо отказывает (rc 2), либо зеленеет ложно.

**З-4 — граница живёт вне нормативного текста. Класс: место записи нормы. К владельцу, не дефект круга.**
Граница модели угроз объявлена ЗАКОНОМ предмета 032/025, но записана только в
`verdicts/arbitration/yakor-git-032.md`, А-194 и шапке `path-guard.ts`. Замороженный контракт 032
её не несёт — и не должен был: арбитр прямо запретил трогать заморозку, я это подтверждаю (§5),
молчаливой правки нормы в цепочке нет. Следствие называю прямо: читатель, открывший ОДИН контракт,
границы не увидит — а именно её отсутствие стоило четырёх кругов (А-194). Маршрут: внести границу
в нормативный текст при следующем открытии 032 v2 или 025. Ставить это условием ACCEPT нельзя —
новый замысел в круг судьи задним числом не входит.

## Живой пункт — КРАСЕН, назван, НЕ блокер этого вердикта

Пин task-спавннутой сессии в живом проводе не поднимается. Три транскрипта:

* `cb1b125` — оба захода дословно: `--extract-pin '<канон-форма>'` → `{"worktree":"/tmp/…/wip-032-implementer"}`
  rc 0 (ИЗВЛЕЧЕНИЕ работает), но живая сессия пина не получает: обе записи — и в worktree, и в
  main-чекаут — блокируются причиной «запись в чекаут из непиннованной сессии запрещена —
  Н-85/А-122 … не в null-allowlist». Приёмка «После реализации», команда 1, требует `succeeded`.
* `0b3073c` — разрыв ПЕРЕЖИЛ вариант D (попытка 3 дословно): `getBranch()` в реальном проводе
  не несёт текста задания.
* `11a09e3` — адверсарий к2 подтвердил красноту трижды по транскриптам.

На HEAD принимающая сторона реализована: `p.on('session_start'|'session_branch'|'session_tree', lifecycle)`
(:1045-1047) и ленивый подъём `extractPinFromBranch(getBranch(ctx))` в `tool_call` (:1073). Разрыв —
на стороне omp-транспорта, ВНЕ субъекта; ни один коммит цепочки его не касался, и по РЕШЕНИЮ
`0c98913` §Вопрос 2 доставка текста задания в lifecycle объявлена omp-транспортом вне предмета.

**Судьба живого критерия — за владельцем (утренний батч, пункт 3).** Мой вердикт он не блокирует;
`done/032` до слова владельца не ставится.

Побочное живое наблюдение, полученное на этом же прогоне: моя собственная непиннованная
судейская сессия получила от предмета
`запись в чекаут из непиннованной сессии запрещена — Н-85/А-122: путь /tmp/&1 не в null-allowlist
(/tmp/dev-harness-verify/**, artifact://)` — половина 025 «непиннованная запись» работает живьём,
и именно поэтому этот вердикт приземляется оркестратором, а не моей рукой.

## Приземление

Текст приземляет оркестратор в `verdicts/review/contracts-032-k1.md` СТРОГО с
`git -c user.name=reviewer -c user.email=reviewer@dev-harness.local commit` (различение
исполнителей в `scripts/check_zones.sh` — по `user.name`; арбитраж
`verdicts/arbitration/reglament-ispolnitel.md`, замер 4). Клон
`/tmp/dev-harness-verify/rev-032/repo` сохранён до того, как коммит станет виден в основном
репозитории.

Итог: **суды 032 завершены, `done` ждёт слова владельца.**
