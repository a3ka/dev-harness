FAIL

# Ревьюер, контракт 026 (жнец ./tmp), круг k1

Предмет: замороженный `contracts/026-zhnec-tmp.md` (v4, `frozen/contracts/026/4`),
реализующие коммиты `358b12a` → `d57923d` (к6, правило-12) и фикстуры семьи
`fixtures/gc_agent_branches/`. Судимая база — HEAD `55be77b1a6034f2a14865ed398e70760c7f60636`
(= живой origin/main на момент суда). Суд выполнен в собственноручном одноразовом клоне
`/tmp/dev-harness-verify/rev-026/repo`; все игрушки, PATH-стабы и драйверы — вне клона,
под `/tmp/dev-harness-verify/rev-026/`.

Блокер один, и он НЕ в коде implementer: незакрытое ОБЯЗАТЕЛЬНОЕ приёмочное обязательство
Б5(i) в зоне orchestrator. Фикс-раунды к3–к6 не переоткрываются (правило 12).

## Сверка §Предмет ↔ коммиты (реализующие, не только барьеры)

| коммит | автор | пути | зона |
|---|---|---|---|
| `358b12a` TMP-РЕАП слой 1 | implementer | `scripts/gc_agent_branches.sh` | implementer ✔ |
| `20df037` wipe_ro в cleanup раннера | implementer | `scripts/verify_antiplacebo.sh` | implementer ✔ (зона v2, `f859ff4`) |
| `b7630fd` 5 фиксов к1 | implementer | `scripts/gc_agent_branches.sh` | ✔ |
| `3abdb2f` 2 блокера + 2 не-блокера к2 | implementer | `scripts/gc_agent_branches.sh` | ✔ |
| `3c49f7d` case-фикстуры к2 | architect | `NABLIUDENIA_ARCHITECT.md`, 2 фикстуры | architect ✔ |
| `58c746a` fail-closed tracked-guard (к3) | implementer | `scripts/gc_agent_branches.sh` | ✔ |
| `e1f595b` always-validate age, TOCTOU dev:ino (к4) | implementer | `scripts/gc_agent_branches.sh` | ✔ |
| `d57923d` runtime-предпроверка python3 (к6) | implementer | `scripts/gc_agent_branches.sh` | ✔ |

Превышения зоны нет: ни один коммит 026 не трогает путь вне своей ЗОНА-строки.
Заморожённое вне зон цело — `git log frozen/contracts/026/4..HEAD -- AGENTS.md
scripts/check_no_leak.sh` пуст (норма правила 16 и оракул 024 не тронуты молча).
Текст контракта после заморозки v4 не менялся: `check_contract_frozen.sh` rc 0,
«026 — заморожен v4, блоб совпадает побайтово».

Проверка НЕ подогнана под реализацию: все 026-фикстуры созданы architect
(`4328695`, `3c49f7d`); в истории `fixtures/gc_agent_branches/` нет ни одного
коммита implementer после `136f1a5` (август, семья `case_zavisshaja_*`).

## Собственные прогоны (сырые rc из своего клона)

Семь контрактных проб, HEAD:

```text
red_zhnets_reap_stale        rc=0
red_zhnets_aktiv_vyzhivaet   rc=0
red_zhnets_tracked_netrogat  rc=0
red_regress_024_noga2        rc=0
case_zhnets_reapstale        rc=0
case_zhnets_aktiv_vyzhivaet  rc=0
case_zhnets_tracked_netrogat rc=0
```

Scoped-мета-прогон:

```text
bash scripts/verify_antiplacebo.sh --scope gc_agent_branches
барьеров: 1 · фикстур: 7 · предъявлено красным повторным прогоном: 7
RC=0
```

frozen-diff:

```text
bash scripts/check_contract_frozen.sh
планов и контрактов на HEAD: 31 · черновиков: 3 · заморожено: 28 · реестр: full
RC=0
```

Живой dry-run (приёмочная строка 8), корень `/home/aka/Documents/dev-harness`:

```text
rc=0 · TMP-РЕАП кандидатов 221 (хвост списка: tmp/wrapXTuW.sh 27.3d, tmp/zones.pkzNa5 22.1d)
```

Церемония красноты против пред-предметного блоба `48dee20` (`358b12a^`) — свап
`scripts/gc_agent_branches.sh`, все девять фикстур семьи:

```text
SWAP-PRE026 red_zhnets_reap_stale            rc=1
SWAP-PRE026 red_zhnets_aktiv_vyzhivaet       rc=1
SWAP-PRE026 red_zhnets_tracked_netrogat      rc=1
SWAP-PRE026 red_regress_024_noga2            rc=1
SWAP-PRE026 case_zhnets_reapstale            rc=1
SWAP-PRE026 case_zhnets_aktiv_vyzhivaet      rc=1
SWAP-PRE026 case_zhnets_tracked_netrogat     rc=1
SWAP-PRE026 case_zhnets_terminal_lf          rc=1
SWAP-PRE026 case_zhnets_done_istochnik_otkaz rc=1
```

## СВОЯ мера заявленного (другой командой, не повтором)

Контракт считает верхнеуровневые записи; я свернул источник САМ, не через барьер:

```text
git ls-files --others -z -- tmp/ | tr '\0' '\n' | cut -d/ -f1,2 | sort -u | wc -l  → 229
кандидатов в сухом списке жнеца                                                    → 221
кандидаты вне моей свёртки                                                         → ПУСТО
```

Разность — ровно 8 записей; их возраст и NNN измерены моим `os.lstat`, не барьером:

```text
tmp/charter.RERj63            age= 3.90d nnn=[]      → аноним ≤7д, выживает (инв. 3б)
tmp/critic                    age= 1.05d nnn=[]      → аноним ≤7д
tmp/v1_patch.diff             age= 6.75d nnn=[]      → аноним ≤7д
tmp/ci_job_103282533944.log   age= 6.10d nnn=['032'] → 032 не распознан, аноним ≤7д
tmp/critic-026-v1             age= 1.45d nnn=['026'] → активный NNN
tmp/critic026v2-2             age= 1.29d nnn=['026'] → активный NNN
tmp/draft020-task.md          age=14.26d nnn=['020'] → активный NNN, выживает вопреки возрасту
tmp/fix5-draft020-task.md     age=14.26d nnn=['020'] → активный NNN, выживает вопреки возрасту
```

Живая семантика совпадает с замороженными инвариантами 3(а)/3(б) поимённо, включая
защиту активного контекста от возраста. Расхождений заявленного и сделанного в счётных
утверждениях реализации не нашёл.

## Находки

### Б1 (БЛОКЕР, класс: незакрытое ОБЯЗАТЕЛЬНОЕ приёмочное обязательство; зона orchestrator)

Контракт, §Зоны и §Приёмочный критерий: «Проводка close-out (Б5, ОБЯЗАТЕЛЬНА): (i) до
done в пачке 026 норма в `roles/orchestrator.md` у шага close-out называет вызов gc с
`--tmp-reap-apply`». На живом HEAD `55be77b`:

```text
grep -c "tmp-reap" roles/orchestrator.md → 0 (rc=1)
roles/orchestrator.md:125: «… close-out — `gc_agent_branches` (016 И-6).»
```

Строка нормы — ровно та, что контракт цитирует как ДО-состояние. Обязательство не
исполнено, и в HANDOFF отслеживается только пункт (ii) («6. 026 close-out: реап ./tmp
(1.2 ГБ, gc:agent-branches --tmp-reap-apply)») — разовое применение, а НЕ норма.
Разница существенна: без (i) механизм остаётся однократной ручной командой, а норма
close-out наследуется дальше в старой редакции — ровно тот способ, которым механизм
вырождается обратно в роль. Правка — одна строка в зоне orchestrator; фикс-раунды
implementer не переоткрываются.

### Б2 (НЕ блокер, класс: фикс сужен до свидетеля; заявленное шире сделанного)

Коммит к6 `d57923d` заявляет: «PATH-резолв не гарантирует запускаемости, set -e
пропускал сырой rc=127». Свидетель вердикта к5 действительно закрыт — моя мера:

```text
пред-к6 блоб 17ee703 + PATH-python3(rc 127)  → rc=127, «poison-python3», запись цела
HEAD        90af99b + тот же стаб            → rc=2,  «NOT_IMPLEMENTED: python3 найден,
                                                       но не запускается (Н-60, вердикт к5)»
```

Но предпроверка — ВЫБОРКА одного вызова (`python3 -c 'import sys; sys.exit(0)'`), а
реальные пути зовут `python3 - <файл> <<PYEOF`. Стаб, различающий `-c` и `-`
(честный на предпроверке, rc 127 на реальном вызове), проходит её насквозь:

```text
PATH=<стаб> bash scripts/gc_agent_branches.sh --root <toy> --tmp-reap-apply
→ rc=127
ЗАВИСШИХ ВЕТОК НЕТ
poison2-python3
```

Класс «инструмент найден, но отказал» остаётся открытым; закрыт ровно свидетель к5.
Структурная причина видна в коде: `set -euo pipefail` (:88) делает недостижимыми ОБА
объявленных fail-closed сторожа — `py_rc=$?` на :390-392 («свёртка кандидатов отказала»)
и :481-483 («TMP-РЕАП lstat отказал»): голый `python3 … <<PYEOF` с ненулевым rc убивает
shell ДО присваивания. Это мёртвый код, объявляющий отказ, которого не бывает.
Точечный фикс — `|| py_rc=$?` на обоих вызовах (тогда предпроверка вообще не нужна).
НЕ блокер: направление безопасное (запись цела, ничего не удаляется), замороженный
инвариант 5 буквально говорит «нет python3 → rc 2» и исполнен, круги закрыты
правилом 12. Обязано лечь в residual-запись, а формулировка к6 — сужена до свидетеля.

### Б3 (НЕ блокер, класс: фикс-раунды без постоянного красного)

Своя мера — свап блоба на пред-фиксный для каждого раунда, все девять фикстур семьи:

```text
пред-к2 (3abdb2f^) КРАСНЫЕ: case_zhnets_terminal_lf, case_zhnets_done_istochnik_otkaz
пред-к3 (58c746a^) КРАСНЫЕ: НЕТ
пред-к4 (e1f595b^) КРАСНЫЕ: НЕТ
пред-к6 (d57923d^) КРАСНЫЕ: НЕТ
```

Красное предъявлено для предмета в целом (пред-026) и для раунда к2. Ужесточения к3
(fail-closed tracked-guard на отказе git), к4 (always-validate `--tmp-reap-age`, строгое
`>0`, TOCTOU dev:ino) и к6 (runtime-python3) постоянного сторожа НЕ имеют: откат любого
из них невидим всей батарее. Сейчас их краснота живёт только в вердиктах адверсария
(к5 подтвердил к4 своими прогонами) и в этом файле (к6 — мой замер выше). Замороженный
счёт (7 проб) новых проб на фикс-раунд не требует, поэтому находка остаточная, не
блокер; но это и есть цена правила 12 на этом предмете, и она обязана быть названа,
а не унаследована молча.

### Б4 (НЕ блокер, класс: усиление сверх счёта без респека)

Контракт: «счёт: 7 проб … 4 предъявления + 3 постоянных». В дереве — 9 проб 026
(+`case_zhnets_terminal_lf`, +`case_zhnets_done_istochnik_otkaz`, `3c49f7d`, зона
architect). Усиление, не ослабление; критик v4 (`06792d3`) судил только СПАСЕНО-дельту
и о счёте не высказывался. Строка счёта в замороженном тексте стала сталой относительно
семьи — фиксирую фактом, респека не требую (заморозка v4 закрыта).

### Б5 (НЕ блокер, класс: именование residual есть, оценка опровергнута замером)

LD_PRELOAD/intra-process residual (принят владельцем по к5) ИМЕНОВАН в коде, а не только
в переписке — шапка `scripts/gc_agent_branches.sh:71-77` и врезка :568-581 у самого
удаления; это удовлетворяет прецеденту 020 (арбитраж `6469a62` → residual-строка в коде,
`eaf629e`). Достаточность именования — с одной оговоркой: обе врезки характеризуют окно
как «практически требует контроля планировщика ядра уровня root, а не обычной
параллельной мутации», тогда как вердикт к5 ЗАМЕРИЛ выигрыш этой гонки
LD_PRELOAD-интерпосером без root (`verdicts/adversary/contracts-026-k5.md:60-72`).
Фактическая граница — «мутатор, исполняющий код ВНУТРИ процесса-жнеца» (то есть
контролирующий окружение вызывающего), а не «root-планировщик». Сам замер я не
воспроизводил (интерпосер вне моего круга); структурно шов виден в коде: между
`os.lstat` и `os.remove`/`shutil.rmtree` внутри одного `python3 -c` блокировки нет.
Правка — одна фраза прозы в зоне implementer; адрес закрытия (эксклюзивная блокировка
./tmp поперёк процессов, вне слоя 1) назван верно.

## Что требуется до done

Блокирующее одно: Б1 — строка нормы `--tmp-reap-apply` у шага close-out в
`roles/orchestrator.md` (зона orchestrator). Б2, Б3, Б5 — residual-записи (Б2 и Б5 —
фразой в шапке барьера, Б3 — строкой в HANDOFF/НАБЛЮДЕНИЯХ как цена правила 12).
Б4 — факт, действия не требует.

Реализация жнеца по существу принята: зоны соблюдены, заморозка цела, проверки не
подогнаны под код, живая семантика сверена независимой мерой, все семь контрактных проб
красны против пред-предметного блоба и зелены на HEAD.
