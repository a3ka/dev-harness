accept

# Финальный review контракта 007 перед `done`

Проверен HEAD `dc3909e`. Вердикт `accept`: блокирующих дефектов зоны, подгонки проверки, ложной красноты или незакрытого обхода вне standard-A не найдено.

## 1. Область, авторы и атомарность

Сырым журналом `git log --oneline d708a39..dc3909e` и `git show --stat` проверены предметные коммиты.

| Коммит | Автор | Изменённые пути | Вывод по зоне |
|---|---|---|---|
| `b229f02` | `implementer` | `.github/workflows/ci.yml`, `scripts/verify_antiplacebo.sh` | обе позиции в зоне implementer; `scope_select` и `package.json` не менялись |
| `87f8fb0` | `architect` | `scripts/check_metering.sh`, `scripts/proxy/metering_proxy.ts` | обе позиции в зоне architect |
| `9cf197a` | `architect` | 9 файлов `fixtures/check_metering/` | зона architect |
| `f70d77d` | `adversary` | `verdicts/adversary/contracts-007.md` | изолированный вердикт адверсария, не изменение предмета |
| `dc3909e` | `architect` | `scripts/check_metering.sh`, `NABLIUDENIA.md` | приёмочный барьер и наблюдение — зона architect |

Других правок предмета этими пятью ключевыми коммитами `show --stat` не выявил. Нормативный контракт после заморозки не правился: `dc3909e` добавил только наблюдение Н-46, а не норму. Реализации срезов атомарны по предмету: `b229f02` — срез 1 implementer; `87f8fb0` и необходимая раздача `9cf197a` — срез 2 architect; закрытие конкретной атаки — `dc3909e`.

## 2. Сырые последовательные прогоны

Перед и после каждого прогона выполнены очистка `pkill -9 -f 'verify_antiplacebo|check_scoped_run|check_metering|metering_proxy|metering-stub-upstream'`, пауза 2 секунды и `rm -rf tmp/antiplacebo/run-*`. RC читался немедленно.

| Команда | Фактический RC | Сигнал из вывода |
|---|---:|---|
| `bash scripts/check_scoped_run.sh; echo $?` | 0 | зелёны ветви, включая `ц`, `ц2`, `ц3`, `ч`, `ч2`, `ci` |
| `bash scripts/check_metering.sh --port0; echo $?` | 0 | `p0.gen`, `p0.up`, `p0.conc`, `p0.src`; в `p0.conc` два разных фактических порта и оба `healthz 200` |
| `bash scripts/check_metering.sh; echo $?` | 0 | вывод: «барьер зелёный: 15 ветвей пройдены» |
| `bash scripts/check_scope_select.sh; echo $?` | 0 | зелёны все показанные ветви `а`–`щ`; замороженный контракт 006 не регрессировал |
| `npm run check:ci-parity; echo $?` | 0 | `workflow-команд: 19 · скриптов в приёмке: 31 · объявленных исключений: 12 · расхождений: 0` |

Полный локальный `verify_antiplacebo` намеренно не запускался: задание запрещает его из-за локально недоступной сети на `check_skills`; CI на `dc3909e` указан зелёным в переданном контексте.

## 3. Красные проверки не подогнаны под предмет

История отделяет красные барьеры от реализаций: `c414ef2` предъявил scoped ветви до `b229f02`; `54e15cd`, затем `419bc2d` предъявили и усилили `--port0` до `87f8fb0`. Сам `b229f02` не менял `check_scoped_run`; проверка `dc3909e` — адресное усиление после предъявленного адверсарием обхода, а не ослабление ожидания.

Независимый красный контроль выполнен в клоне `/tmp/review-contract-007`: в одном только `gen_config` возвращено `proxy_port="$(free_port)"`. `bash scripts/check_metering.sh --port0` сразу вернул **RC=1** и назвал предметный дефект:

```
FAIL port0.gen: gen_config эмитит "port":19338 (free_port-скан) — нормальный путь check_metering не мигрирован на bind:0; корень Н-45 остаётся
RC=1
```

При этом `p0.up`, `p0.conc` и `p0.src` в том же запуске остались зелёными. Значит `p0.gen` не является вечно-красным или подменённым общим сбоем: он краснеет именно от возврата предметной правки.

## 4. Закрытие адверсария и остаток

В отдельном клоне `/tmp/review-contract-007-adversary` воспроизведён показанный в `verdicts/adversary/contracts-007.md` стаб: декой `void 'server.address() .actual_port'`, отчёт в импортируемом `metering_proxy_real.ts`, и `await import /*…*/ (`.

* `bash scripts/check_metering.sh --port0` вернул **RC=1**; среди диагностик — требуемая `FAIL port0.src: нет РЕАЛЬНОГО writeFileSync(…\.actual_port) … репорт вынесен/декой`.
* `bash scripts/check_metering.sh` вернул **RC=1**; ветвь `к1` явно покраснела: `FAIL к1: загрузка модулей вне статического node:-import … → динамический-import()`.

Таким образом обе конкретные части предъявленного проходящего стаба теперь ловятся: декой-литерал не удовлетворяет `p0.src`, комментарий между `import` и `(` не обходит `к1`.

Оставшийся встроенный listen-monkeypatch с настоящим `writeFileSync` и `probe.listen(0) → address → close → server.listen(selected)` корректно отнесён к `cognitive-only` Н-46, а не скрыт как закрытый. По обязательному арбитражу `krasnye-proby-granica-primera.md` это standard-A: детерминированный чёрный ящик не отличает честный `listen(0)` от самовыбора, а конечный набор белощиковых форм не доказывает произвольный стаб. Для блокировки нужна совокупность (i)–(iii); этот вариант сложнее честной реализации (встраивание monkeypatch, отдельный probe, закрытие и повторный bind), поэтому не проходит критерий (ii). Его ловец — наблюдение bind-системных вызовов, то есть будущий поток A, не дополнительный примеро-основанный grep. Это не блокер финального гейта standard-B.
