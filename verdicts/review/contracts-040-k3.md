accept

# Ревью контракта 040 — круг 3 (узкий re-check Б3+Б4 круга 2)

Предмет круга ОГРАНИЧЕН порогом, объявленным моим же вердиктом круга 2
(`verdicts/review/contracts-040-k2.md`, `9c64263`, прочитан целиком, включая
раздел «Порог следующего круга»): (а) запись «Условие done» в `HANDOFF.md` —
SHA прогона и ссылка (Б3); (б) зелёная джоба `ci` на `main` либо письменно
обоснованная опора на уже состоявшийся зелёный прогон (Б4). **Б1 и Б2 закрыты
кругом 2 и здесь НЕ переоткрываются; инженерное ядро предмета (batched-техника,
дифференциальный критерий, доверенный PATH, форма re-exec, семантика окон)
проверено кругом 1 и подтверждено кругом 2 — повторному суду не подлежит.**

Контракт судится в редакции **v2** (`frozen/contracts/040/2`, блоб
`ecca7ff3`, подтверждён машинной мерой ниже). Прогоны — в одноразовом клоне
`https://github.com/a3ka/dev-harness` → `/tmp/dev-harness-verify/rev040k3/repo`
(HEAD `origin/main` = `8e05a2a`), контрфакт Б4 — в worktree того же клона на
`eab8ce6`. Основной чекаут не менялся (Н-85/А-122). Судья: reviewer, семейство
отлично от автора работы (architect/orchestrator — `anthropic`).

Вердикт: **accept**. **Обе находки круга 2 ЗАКРЫТЫ и предъявлены МОЕЙ мерой, не
пересказом отчёта**: Б4 — с восстановленной полярностью (красно ДО фикса, зелено
ПОСЛЕ) и живым `check_ci_gate` rc=0 на текущем HEAD; Б3 — байт-точной сверкой
цитаты и НЕЗАВИСИМОЙ проверкой прогона через GitHub API, где оба call-budget
шага `success`, не `skipped`/`cancelled`. Три замечания (О1-О3), ни одно не
блокирует `done`.

---

## Б4 круга 2 — ЗАКРЫТ. Грамматика наблюдений зелена, и зелёное НЕ куплено правкой барьера

Фикс — `faa4d6d` (architect, передан оркестратором): адрес записи А-235 приведён
к каноничной форме. Область правки, моя мера:

```
$ git show --stat --format='%an|%cn' faa4d6d
architect|orchestrator
 NABLIUDENIA_ARCHITECT.md | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)
```

Одна строка одного файла — правится ЗАПИСЬ, а не судья записи. Что барьер не
тронут, я проверяю отдельной мерой, а не доверием к статистике diff'а:

```
$ git log --oneline b38e78a..HEAD -- scripts/ fixtures/ .github/
(пусто)
```

**Полярность предъявлена — контрфакт на коммите ДО фикса** (worktree того же
клона на `eab8ce6`, родитель `faa4d6d`):

```
$ cd /tmp/dev-harness-verify/rev040k3/repo/pre && git log --oneline -1
eab8ce6 orchestrator: NABLIUDENIA Н-138 …
$ bash scripts/check_nabludenia.sh .
  FAIL А-235: адрес не называет цель (Н-39-рецидив: unicode-склейка к якорю — круг 4 критик, круг 5 блокер 1)
rc_nab_pre=1
```

```
$ cd /tmp/dev-harness-verify/rev040k3/repo && bash scripts/check_nabludenia.sh .
rc_nab=0        (вывод пуст — барьер говорит только о нарушениях)
```

rc=1 → rc=0 при нетронутом барьере: мера жива, зелёное есть следствие правки
адреса, а не молчания судьи. Именно это отличает закрытие от подгонки.

**Живой CI на текущем HEAD.** Первый мой замер застал прогон в полёте, и гард
ответил fail-closed, а не зелёным (это его правильное поведение, фиксирую как
О2):

```
$ bash scripts/check_ci_gate.sh . 8e05a2a676e93b2f14c334328d0994799047ff80
ОТКАЗ: CI не зелёный по 8e05a2a676e9: antiplacebo (ap3, …) → без завершения — судью не звать, чинить пачку
rc=1     (16:52 UTC, прогон 35891567060 = in_progress)
```

Я дождался завершения (опрос API каждые 20 с, `16:52:51 in_progress` →
`17:00:35 completed/success`) и повторил меру:

```
$ bash scripts/check_ci_gate.sh . 8e05a2a676e93b2f14c334328d0994799047ff80
  ok   CI зелёный: проверок 6, все success, по 8e05a2a676e9 (a3ka/dev-harness)
rc_gate_HEAD=0
```

Проверка гарантирована не только гардом: список джоб прогона HEAD — моей мерой
через API (джоба `ci` + пять шардов `antiplacebo`, все `success`), и ОБА
call-budget шага на нём зелены тоже:

```
$ curl -s .../actions/runs/35891567060/jobs | jq …
JOB ci :: success
   STEP Счётчик git-вызовов check_zones (контракт 040, §Приёмка Р1)    :: success 16:57:14->16:59:47
   STEP Счётчик git-вызовов check_protected (контракт 040, §Приёмка Р2) :: success 16:59:47->16:59:51
JOB antiplacebo (ap1|ap2|ap3|ap4|ap5) :: success ×5
```

2 мин 33 с работы шага Р1 на раннере — шаг СЧИТАЛ, не выродился. Б4 закрыт:
красная серия из четырёх пушей прервана, `main` зелен на HEAD.

---

## Б3 круга 2 — ЗАКРЫТ. Запись «Условие done» внесена, и названный ею прогон проверен НЕЗАВИСИМО

Контракт (§«Условие done», ч. 3 п. 2) требует записать в раздел «ГДЕ МЫ»
текущего чекпойнта `HANDOFF.md` дословно: SHA коммита прогона, прямую ссылку и
помету. Все три части — на месте; цитату проверяю фиксированной строкой, а не
глазами:

```
$ grep -c -F '040: оба call-budget шага зелены на origin — живое наблюдение done-условия §Б3 ч.3' HANDOFF.md
1        grep_rc=0
$ grep -n -F 'actions/runs/35890163182' HANDOFF.md
21:- **Б3 исполнено ЭТИМ ЧЕКПОЙНТОМ** … оркестратор лично открыл лог прогона
     `https://github.com/a3ka/dev-harness/actions/runs/35890163182` (commit `9c64263`) и глазами
     подтвердил ОБА шага: «Счётчик git-вызовов check_zones (контракт 040, §Приёмка Р1)» — success;
     «Счётчик git-вызовов check_protected (контракт 040, §Приёмка Р2)» — success.
     **040: оба call-budget шага зелены на origin — живое наблюдение done-условия §Б3 ч.3.**
$ sed -n '7p' HANDOFF.md
## ГДЕ МЫ (2026-09-23 вечер, ЧЕКПОЙНТ #14 — …)      <- строка 21 внутри ТЕКУЩЕГО чекпойнта
```

**Запись не принимается на слово — названный ею прогон проверен моей мерой через
API**, и проверен именно на то, что контракт требует увидеть (ч. 3 п. 1: оба
шага присутствуют И оба зелёные, не пропущены, не отменены):

```
$ curl -s .../actions/runs/35890163182 | jq …
run=35890163182 name=ci event=push status=completed conclusion=success
head_sha=9c64263b010c812c973b6a8db634b4190c52ee48 branch=main

$ curl -s .../actions/runs/35890163182/jobs | jq …
JOB ci :: completed/success
   STEP Счётчик git-вызовов check_zones (контракт 040, §Приёмка Р1)     :: completed/success 16:45:09->16:47:41
   STEP Счётчик git-вызовов check_protected (контракт 040, §Приёмка Р2) :: completed/success 16:47:41->16:47:44
JOB antiplacebo ap1..ap5 :: completed/success ×5
```

`status=completed`, `conclusion=success` на КАЖДОМ шаге — ни `skipped`, ни
`cancelled`, ни `null`. Счётное утверждение записи («6/6 success») подтверждено
моим ДРУГИМ инструментом (перечисление джоб API), а не повтором той же команды:
джоб ровно шесть, все `success`. `head_sha` прогона побайтово равен SHA, который
называет запись.

**Шаги — ТЕ САМЫЕ, что требует контракт**, а не одноимённые. Сверка имён шагов с
подключением, моя мера:

```
$ grep -n 'call-budget' -B3 .github/workflows/ci.yml
327:      - name: Счётчик git-вызовов check_zones (контракт 040, §Приёмка Р1)
333:        run: npm run check:zones-call-budget
335:      - name: Счётчик git-вызовов check_protected (контракт 040, §Приёмка Р2)
339:        run: npm run check:protected-call-budget
```

Контракт ч. 3 п. 1 называет именно `npm run check:zones-call-budget` и
`npm run check:protected-call-budget` — имена шагов в прогоне разрешаются в эти
две команды однозначно.

**ч. 3 п. 3 («один раз на ФИНАЛЬНОЙ implementer-пачке») соблюдён.** Прогон взят
на `9c64263` — это коммит вердикта круга 2, не голова пачки; но исполняемая
часть дерева там байт-идентична финальной пачке, и это моя мера, а не довод:

```
$ git log --oneline b38e78a..HEAD -- scripts/ fixtures/ .github/     → (пусто)
$ git diff --exit-code b38e78a HEAD -- <8 глобов §Приёмка Р8>        → rc_P8=0
$ git diff --stat eab8ce6 HEAD
 HANDOFF.md | 20 ++   NABLIUDENIA_ARCHITECT.md | 2 +-   verdicts/review/contracts-040-k2.md | 366 ++
```

После `b38e78a` (закрытие Б1) не тронуто ни одного байта `scripts/`,
`fixtures/`, `.github/` — наблюдение взято на том же исполняемом дереве, что и
финальная пачка. Сверх того, тот же факт независимо подтверждён ВТОРЫМ зелёным
прогоном на HEAD (`35891567060`, выше): запись опирается на воспроизводимое
состояние, а не на единичную удачу раннера.

Оговорка, которую обязан назвать честно: часть ч. 3 п. 1 «оркестратор открыл лог
ГЛАЗАМИ» внешней проверке не поддаётся в принципе — я проверяю артефакт записи и
ФАКТЫ, которые она утверждает. Все утверждаемые факты подтвердились моей
независимой мерой, ни одно не разошлось. Б3 закрыт.

---

## Остальные барьеры живы (быстрая сверка, не полный прогон §Приёмки)

```
$ out=$(bash scripts/check_zones.sh .); rc=$?
замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1900 · проверено по зонам: 1280
rc_zones=0

$ out=$(bash scripts/check_contract_frozen.sh .); rc=$?
  ok   contracts/040-batching-git-vyzovov.md — заморожен v2, блоб совпадает побайтово, вердикты v1..v2 разрешают
планов и контрактов на HEAD: 44 · черновиков: 3 · заморожено: 41 · реестр: full
rc_frozen=0

$ bash scripts/check_provodka.sh . contracts/040-batching-git-vyzovov.md
rc_provodka=0

$ bash scripts/check_ci_gate.sh . 9c64263b010c812c973b6a8db634b4190c52ee48
  ok   CI зелёный: проверок 6, все success, по 9c64263b010c (a3ka/dev-harness)
rc=0
```

Коды возврата сняты с САМИХ барьеров (подстановка команды), а не с хвоста
конвейера: `cmd | tail` вернул бы код `tail`, и зелёный был бы мнимым.

**Область правки, атомарность, норма.** Три коммита после меры круга 2, каждый —
один предмет, автор соответствует роли (`check_zones.sh` rc=0 подтверждает это
механически по `%an`):

```
8e05a2a orchestrator/orchestrator  HANDOFF чекпойнт #14 (+20 строк, 1 файл)   <- Б3
9c64263 reviewer/orchestrator      вердикт круга 2 (судейский канал)
faa4d6d architect/orchestrator     А-235 адрес по грамматике (1 строка)       <- Б4
$ git diff --name-only eab8ce6 HEAD -- roles/ AGENTS.md contracts/ plans/ scripts/ fixtures/ .github/ registry/
(пусто)
```

Норма (`roles/*.md`, `AGENTS.md`) и замороженный текст контракта в этой пачке не
тронуты вовсе — закрытие Б3/Б4 сделано без единой правки нормативного документа.

---

## Замечания (ни одно не блокирует)

**О1 (перенос О2 круга 2, без действия в этом круге).** §Приёмка Р1 остаётся
зависим от наличия `strace` на машине: без него фикстура отвечает
`rc=2 NOT_IMPLEMENTED` — честный fail-closed, но пункт приёмки на голой рабочей
станции не воспроизводим. Стоит назвать зависимость в тексте приёмки при
следующей же правке контракта; открывать версию ради этого не нужно.

**О2 (наблюдение, подтверждение правильного поведения).** `check_ci_gate.sh` на
незавершённом прогоне даёт rc=1 с формулировкой «без завершения», а не зелёное
от отсутствия результата — это ровно то направление ошибки, которое требуется от
гарда; фиксирую, потому что мой первый замер Б4 был именно таким и мог бы быть
прочитан как красный барьер.

**О3 (перенос О1 круга 2, остаётся открытым как замечание).** Путь
`verdicts/critic/contracts-040-v2.md` по-прежнему несёт ACCEPT v2-амендмента,
тогда как преамбула замороженного текста ссылается по этому пути на FAIL круга 2
критика (`ff7cb19`). Ссылка разрешима по SHA, ослабления нет; цена — читатель,
идущий по пути, а не по SHA, получит документ с противоположным вердиктом.
Правку чужого судебного файла задним числом не предлагаю.

---

## Что этим вердиктом НЕ судилось

Б1 и Б2 (круг 1) — закрыты кругом 2 своей мерой и не переоткрывались; инженерное
ядро 040 — судилось кругом 1, подтверждено живыми прогонами круга 2. Этот круг
узкий по объявленному мной же порогу. Все находки кругов 1-2 (Б1, Б2, Б3, Б4)
закрыты; открытыми остаются только замечания О1-О3, ни одно из которых не
является условием `done` по тексту контракта.
