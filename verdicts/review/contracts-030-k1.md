# Reviewer 030, круг 1 — ACCEPT (последняя сеть перед done/030)

Судья: `reviewer` (семейство, отличное от автора реализации и автора проб).
Предмет: ВСЯ цепочка 030 — замороженный `frozen/contracts/030/1` (`229098e`, land `f7c7ddc`),
реализация `4ace7f2` (squash автора `15d8125`), пробы `ec11c86`, три усиления
`98647fc`/`ffb4b0f`/`126c815`, вердикты критика (5 кругов), арбитражи, адверсарий к1–к4.
Клон: `git clone /home/aka/Documents/dev-harness /tmp/dev-harness-verify/rev-030/repo`,
HEAD `dd6c977`, `git status --porcelain` → 0 строк. Worktree/ветки/main не трогались;
контрреализации строились ТОЛЬКО в одноразовых копиях `/tmp/dev-harness-verify/rev-030/ci-*`.

## Вердикт

**ACCEPT.** Все ключевые проверки задания исполнены СВОЕЙ мерой, не повтором чужой:
три пробы зелены на честном дереве (hub — двумя независимыми свежеслучайными прогонами),
все СЕМЬ контрреализаций к1–к3, построенные мной заново по коду вердиктов, отвергнуты
именованно `rc 1`, текст контракта байт-в-байт равен замороженному, зоны не нарушены,
scoped-мета-прогон зелёный по обоим ключам. Ложного зелёного не найдено ни в одном классе.

Три находки ниже — НЕ блокеры (ни одна не даёт ложного `rc 0` и ни одна не касается
предмета контракта), но названы поимённо и адресованы.

## 1. Три пробы на честном дереве (hub — дважды)

```text
$ bash fixtures/workshop_inventory/red_inventar_hub.sh            # прогон 1
вход | rc суда | ожидание | вердикт
w1   | rc=1 | ожидалось: красное | ok
w2   | rc=1 | ожидалось: красное | ok
w3   | rc=1 | ожидалось: красное | ok
w4   | rc=0 | ожидалось: зелёное | ok
w5   | rc=0 | ожидалось: зелёное | ok
w6   | rc=0 | ожидалось: зелёное | ok
pos  | rc=0 | ожидалось: зелёное | ok
k1   | rc=1 | ожидалось: красное | ok
k2   | rc=1 | ожидалось: красное | ok
v1   | rc=1 | ожидалось: красное | ok
v2   | rc=1 | ожидалось: красное | ok
v3   | rc=1 | ожидалось: красное | ok
v4   | rc=0 | ожидалось: зелёное | ok
v5   | rc=1 | ожидалось: красное | ok
b1   | rc=1 | ожидалось: красное | ok
cr   | rc=1 | ожидалось: красное | ok
shape | rc=1 | ожидалось: красное | ok
a6   | rc=1 | ожидалось: красное | ok
rc=0

$ bash fixtures/workshop_inventory/red_inventar_hub.sh            # прогон 2 (свежая выборка)
<таблица побайтово та же: w1..w3 red, w4..w6 green, pos green, k1,k2,v1,v2,v3 red,
 v4 green, v5,b1,cr,shape,a6 red>
rc=0

$ bash fixtures/qa_kanal_xd/red_kanal_qa_report_issue.sh
rc=0

$ bash fixtures/gc_agent_branches/red_gonka_svezhij_spavn_vyzhivaet.sh
rc=0
```

Случайность сошлась: две независимые выборки по 6 свежих окружений дали одинаковый
зелёный итог. `v5` больше не «пропущен» — суждён красным (замечание к3 закрыто).
Таблица строк в обоих прогонах одинакова законно: строки батареи — фиксированные классы
селективности, случайны значения окружения ВНУТРИ каждого суда (И-1), не состав строк.

## 2. Семь контрреализаций — построены заново, каждая rc 1 именованно

Код взят из вердиктов к1–к3; каждая — на своей копии честного дерева, положительная
ветвь сохранена, нейтрализован ровно один инвариант.

| # | Класс | Мой вход | Наблюдённый результат |
|---|---|---|---|
| 1 | A-030-1 | `workshop:473` база без `hub` + `if [ "${ZAI_API_KEY:-}" = x ]; then TOOLS="$TOOLS,hub"; fi` | inventory **rc=1**: `ОТКАЗ: hub в условном контексте — if-охват hub (A-030-4, статическая сеть), строка 474` |
| 2 | A-030-2 | `judge()`: `edit` точного `xd://report_issue` у непинна → block, write/bash целы | QA **rc=1**, строка 10: `QA-канал у непинна, edit-форма → pass … CLI: ожидалось pass, получено block (A-030-2 counterstub)` |
| 3 | A-030-3 | обёртка над честным `gc_agent_branches.sh` + поимённый keep-list (`wip/031/architect`, `wip/030/young23`, `wip/030/no-log`), доснос `wip/999/fresh-*` | GC **rc=1**: `свежая пустая ветка с непредсказуемым именем wip/999/fresh-0086291e20da3f98 снесена — жнец узнаёт КОНСТАНТЫ игрушки` |
| 4 | A-030-4 | `if ! [[ "${ZAI_API_KEY:-}" =~ ^[0-9a-f]{16}$ ]]; then TOOLS="${TOOLS/,hub/}"; fi` | inventory **rc=1**: `ОТКАЗ: условное изъятие hub — класс ${TOOLS/,hub/} (A-030-4, статическая сеть), строка 474` |
| 5 | A-030-5 | фабричный `tool_call`: `write xd://other` → `undefined`, CLI не тронут | QA **rc=1**, строка 2: `чужой xd://other у непинна, write → block (узость) — FACTORY: ожидалось block, получено undefined (нет блока)` |
| 6 | A-030-6 | ДОСЛОВНО из к3: `TOOL_NAME=h$(printf ub)` + предикат hex-16 ∨ b64-24 ∨ len==16 | inventory **rc=1**: `ОТКАЗ: [живой-workshop-p1..p6] тул-режим F=0 (exec :516)` И `проектный путь (exec :597)`: `операнд ведущей сессии без hub (инв. 1)` — 12 именованных отказов, 6 проходов × 2 пути |
| 7 | A-030-7 | `path === 'xd://report_issue' \|\| path.startsWith('skill://')` в ОБОИХ зеркалах (`judgeEditWrite` :519, `judgeBash` :579) | QA **rc=1**, строка 19: `порождённая кросс-произведением (И-5): skill://<случайный хвост> у непинна, write → block … CLI: ожидалось block, получено pass` |

Заявление адверсария к4 «все семь rc 1 именованно» подтверждено НЕЗАВИСИМЫМ построением,
а не повтором его команд.

## 3. Заморозка текста контракта

```text
$ git diff --exit-code frozen/contracts/030/1 HEAD -- contracts/030-*.md
rc=0
$ git rev-parse frozen/contracts/030/1
229098eefc27b9e0dddf821fbd3a286cedc988d9
```

## 4. Зоны: проверки и реализация написаны РАЗНЫМИ исполнителями

```text
$ git log --format='%h %an' -- fixtures/workshop_inventory/ fixtures/qa_kanal_xd/ fixtures/gc_agent_branches/
126c815 architect | ffb4b0f architect | 98647fc architect | 468403b architect |
fe86cf4 architect | bc68777 architect | ec1f43f architect | 3c49f7d architect | …
$ git show --stat 15d8125            # реализация, автор implementer
 .omp/extensions/path-guard.ts |   6 +
 scripts/gc_agent_branches.sh  | 101 +++++++++---
 workshop                      |   2 +-
```

Ни одного коммита `implementer` в `fixtures/*` пачки 030; ни одного коммита `architect`
в `workshop`/`path-guard.ts`/`gc_agent_branches.sh`. Область правки реализации — ровно
три файла ЗОНЫ implementer (объявленные `scripts/drill_path_guard.sh`,
`scripts/verify_antiplacebo.sh` не тронуты — сужение зоны, не превышение).
Три усиления `98647fc`/`ffb4b0f`/`126c815` — только `fixtures/*` пачки 030 +
`NABLIUDENIA_ARCHITECT.md`, то есть внутри ЗОНЫ architect; нормативные документы
(`contracts/`, `roles/`, `AGENTS.md`, `NABLIUDENIA.md`) в цепочке не правились.
**Проверка не переписана под реализацию:** усиления сделаны автором ПРОБ в ответ на
именованные FAIL независимого адверсария и на РЕШЕНИЯ арбитра, а не автором кода.

## 5. scoped мета-прогон

```text
$ bash scripts/verify_antiplacebo.sh --scope gc_agent_branches
барьеров: 1 · фикстур: 7 · предъявлено красным повторным прогоном: 7
rc=0

$ bash scripts/verify_antiplacebo.sh --scope check_zones
барьеров: 1 · фикстур: 21 · предъявлено красным повторным прогоном: 21
rc=0
```

## 6. Детектор 024 `--check` — стенограмма

```text
$ bash scripts/check_no_leak.sh --check /home/aka/Documents/dev-harness
ОТКАЗ: основной чекаут загрязнён: verdicts/adversary/contracts-030-v4.md, verdicts/adversary/contracts-031-v4.md
rc=1
```

**НАХОДКА 1 (класс: устаревание снимка, адресат — оркестратор; НЕ блокер 030).**
Обе названные дорожки — ЗАКОННО приземлённые вердикты: обе tracked, `contracts-030-v4.md`
в `bcabf64` (author adversary), `contracts-031-v4.md` в `dd6c977`; `git status --porcelain`
основного чекаута пуст. Красное — дельта против снимка, снятого адверсарием к4 ДО
приземления своих вердиктов. Это состояние инструмента, а не утечка. Требуется
`--retake` оркестратором; иначе следующий судья получит ложно-красный `--check`
и (хуже) настоящая утечка утонет в уже-красном фоне.

## 7. А-186 — суд допустимости границы

Граница «пустой `ZAI_API_KEY` невозможен легально: воркшоп умирает ДО exec» проверена
мной исполнением, не чтением:

```text
$ ZAI_API_KEY= PATH=<стаб omp>:$PATH bash ./workshop
ОТКАЗ: модель сессии zai/glm-5.3, а ZAI_API_KEY пуст даже после автосборки: …
rc=1
$ sed -n '177p' workshop
    zai/*) [ -n "${ZAI_API_KEY:-}" ] || die "модель сессии $LIVE_MODEL, а ZAI_API_KEY пуст …" ;;
```

**Допустима.** Инвариант 1 части 1 говорит о СОСТОЯНИИ `$TOOLS` В ТОЧКЕ exec; при пустом
секрете точки exec не существует вовсе, значит наблюдать нечего — это не сужение предмета,
а его граница. Контраст окружения честно перенесён на ДВА и более непредсказуемых
НЕПУСТЫХ значения (в текущей редакции — шесть). А-186 сам оставляет расшивку гейта
`workshop:177` словом владельца, не решением пачки — корректный адрес.

## 8. Находки, не меняющие вердикта

**НАХОДКА 2 (класс: точность сообщения; зона implementer; НЕ блокер).**
`scripts/gc_agent_branches.sh:269,286,292` печатают `printf 'wip/%s — …' "$branch"`, тогда
как `$branch` УЖЕ содержит префикс. Наблюдено живым прогоном:

```text
wip/wip/030/no-log — reflog недоступен, выживает (грация не применима, fail-closed)
wip/wip/030/young23 — пустая, 82800s < грации 24ч, выживает (грация Н-107)
```

Строка выживания называет несуществующий ref. Инвариант 3.2 формально держится (имя ветки
присутствует подстрокой в одной строке с причиной, полная фраза контрактом не пиннится),
проба законно зелена — поэтому замечание, а не отказ. Чинить отдельной правкой, не в 030.

**НАХОДКА 3 (класс: хрупкость смягчения А-188; НЕ блокер, ложного зелёного нет).**
Именованный `rc 1` для A-030-6 достигается изъятием ДОСЛОВНЫХ маркеров `A6L1`/`A6L2`
из производных деревьев батареи (`red_inventar_hub.sh:568-569, 586-591`). Семантически
тождественный вариант той же контрреализации (`"${ZAI_API_KEY:-}"` вместо `"$ZAI_API_KEY"`)
маркерами не опознаётся и даёт:

```text
pos  | rc=2 | ожидалось: зелёное | СЛОМ
ОТКАЗ: [pos-p1] тул-режим F=0: запуск не состоялся
…
ОТКАЗ: [живой-workshop-p1..p6] … операнд ведущей сессии без hub (инв. 1)
СЛОМ: проба недостоверна (…; батарея: суждено 12 из 12 заявленных) — rc 2, не красное
rc=2
```

То есть живой суд ВИДИТ дефект всеми шестью проходами и называет его, но общий код —
`2`, а не `1`. Это ГРОМКИЙ отказ, не ложный зелёный: по замороженной границе арбитра
`910a598` (подтверждённой адверсарием к4 на его собственной «статистической»
контрреализации) новый круг порождает ТОЛЬКО ложный `rc 0`. Такого прогона нет.
Класс уже записан как А-188; настоящий замер — свежее свидетельство, что смягчение
привязано к литералу маркера, и годится как вход будущей пачки, не как отказ 030.

## 9. Счётные утверждения цепочки — своя мера

* «все семь контрреализаций» — измерено: 7 построено, 7 × `rc 1` именованно (§2). Сходится.
* «критик 5 кругов» — на HEAD лежат ДВА файла: `contracts-030-v1.md` (его заголовок —
  «критик, круг 5, scoped вердиктом транспорта», первая строка `accept`) и
  `contracts-030-v2.md` (`FAIL`, круг 2). v3/v4 сведены в историю коммитом `9fa8d4e`
  по конвенции Н-69, v2 восстановлен `bbe0d4d` после удаления merge-коммитом `ed5d802`.
  Пять кругов подтверждаются историей; на HEAD — консолидированный accept круга 5. Сходится.
* «2 арбитража» — арбитражных РЕШЕНИЙ по 030 всего **три**:
  `dostatochnost-effektivnogo-inventarja-b1-030.md` (`0375aed`),
  `redizajn-transporta-030.md` (`8758348`) — оба в фазе критика, и
  `proby-030-podognannye-kontrrealizacii.md` (`910a598`) — в фазе адверсария.
  Формулировка «5 кругов + 2 арбитража» верна только пофазно; общий счёт по 030 — 3.
  Уточнение, не дефект.

## Маршрут

Приземлить этот текст оркестратором с `author=reviewer` в
`verdicts/review/contracts-030-k1.md`. После приземления `done/030` разрешён.
До простановки тега — `bash scripts/check_no_leak.sh --retake /home/aka/Documents/dev-harness`
(НАХОДКА 1), иначе детектор 024 остаётся красным для следующего судьи.
Клон и контрреализации под `/tmp/dev-harness-verify/rev-030/` сохранены до приземления.
