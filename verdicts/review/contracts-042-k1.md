accept
предмета · БЛОКЕР DONE вне зоны 042 (Р1 — закрыт после этого вердикта, постскриптум ниже)

# Ревьюер: контракт 042 (Н-127) — финальный гейт перед done

Предмет: `contracts/042-excuse-for-otstavshij-roditel.md`, блоб
`c761235b5eecfe859ee9d6354fc427e56f810a96`; код `scripts/check_protected.sh`, блоб
`f71aaae7c01a81c9f729098222ba80a558800c73`. Заморозка `frozen/contracts/042/1` →
`10d1966a21bd452fd7b28b2b8327d4b3b82230c9` (аннотированный тег, объект
`bfca180e841394f2ae3ca40549971f7a11ea339f` — та же строка в `registry/contracts.tsv`).
Судимое дерево — HEAD `719b917` (adversary accept).

Прочитаны ЦЕЛИКОМ: контракт, `verdicts/critic/contracts-042-v1.md` (консолидированный accept
круга 3), `verdicts/arbitration/042-dostatochnost-merge-base-semantiki.md`,
`verdicts/adversary/contracts-042-oracle-adversary.md`. Ни одно заявление трёх предыдущих
судей не принято на слово: каждое счётное и каждое поведенческое утверждение ниже измерено
мной заново, СВОЕЙ мерой, на репозиториях, построенных с нуля здесь
(`/tmp/dev-harness-verify/rev042/`, драйверы `rev.sh`, `rev2.sh`, `rev3.sh`, `pv.sh`);
фикстуры architect'а, клоны критика и репозитории арбитра не копировались.

## ВЕРДИКТ

**Предмет (механизм Н-127) — accept.** Реализован, а не только оттестирован: исходный
CI-блокер закрыт на РЕАЛЬНОМ дереве живым прогоном. Область правки не превышена, ни один
из старых барьеров и ни одна старая фикстура не переписаны под код.

**Done как есть машинно невозможен** — по причине, лежащей ВНЕ зоны 042 (находка Р1).
Это не отказ предмету и не новый круг критика: правкой внутри 042 это не чинится.

---

## Р1 — БЛОКЕР DONE (класс: предсуществующий регресс контракта 038; вне зоны 042)

`scripts/check_provodka.sh` — красный на контракте 042:

```
$ bash scripts/check_provodka.sh . contracts/042-excuse-for-otstavshij-roditel.md
проводка: строка вне грамматики: однажды существовавший, обязан жить на HEAD либо нести именную ALLOW-строку в НАСТОЯЩЕМ
provodka042 rc=1
```

`scripts/done_contract.sh:124` вызывает этот барьер БЕЗУСЛОВНО и на rc≠0 делает
`die "done: ПРОВОДКА красна: …"` (шаг 6, прочитан дословно, строки 116–131). Значит
`done_contract.sh` на 042 обязан отказать; done не достижим без развязки.

Причина НЕ в контракте 042. Цикл сборки поля г0 (`check_provodka.sh:178-189`) пропускает
`continue` только ПЕРВУЮ строку маркера `ПРОВОДКА-ЭНФОРСМЕНТ:`, а каждую строку
ПРОДОЛЖЕНИЯ обёрнутого абзаца ловит катч-олл `*[![:space:]]*) die …`. Абзац
ПРОВОДКА-ЭНФОРСМЕНТ контракта 042 занимает пять строк (`:235-239`) — вторая строка и есть
предъявленная «вне грамматики».

Дискриминация (Н-39): тот же контракт, ТРИ разных содержимого барьера, детерминированно
разные ответы — красное происходит от барьера, а не от текста контракта:

| барьер `check_provodka.sh` | 042 | 039 |
|---|---:|---:|
| блоб `fa9ea8d` — действовавший на `done/contracts/039/1` | **rc=0** | **rc=0** |
| блоб `80ecadd` — HEAD | **rc=1** | **rc=1** |

Катч-олл введён `b321298` («038 — круг 11, фикс "голого канала" в г0»). Контракт 039 несёт
такой же обёрнутый абзац, красен тем же барьером сегодня — и при этом УЖЕ done
(`done/contracts/039/1`, коммит `52e5d17`), причём блоб его контракта с тех пор не менялся
(`bc3a38f` на теге и на HEAD), а блоб барьера менялся (`fa9ea8d` → `80ecadd`). То есть
done-гейт 039 сегодня уже не проходит по собственному барьеру.

Это РОВНО тот класс, который авторы 038 назвали сами в круге 15 своей же шапки:
«done_contract.sh:124 безусловно вызывает этот барьер … rc=1 ОТСЮДА становится блокером done
навсегда». Круг 15 закрыл только форму «markdown code-fence»; форма «обёрнутый абзац
ПРОВОДКА-ЭНФОРСМЕНТ» осталась открытой.

Замер по всем 40 контрактам: вне грамматики красны ровно два — 039 и 042; 037/038/040/041
зелены (у них ПРОВОДКА-ЭНФОРСМЕНТ уложен в ОДНУ строку); остальные 35 — «поле ПРОВОДКА
отсутствует» (доконтрактные 038, ожидаемо).

Развязку не проектирую и не выбираю — это предмет владельца/оркестратора; называю, что обе
двери существуют и обе лежат вне 042: правка `check_provodka.sh` (зона контракта 038, своим
контрактом — при этом она задевает и уже-done 039) либо перезаморозка 042 v2 с абзацем
ПРОВОДКА-ЭНФОРСМЕНТ в одну строку. Правку чужого кода «по пути» не делаю.

## ПРОВОДКА 042 по сути (норма роли) — принято

Канал `guard=scripts/check_protected.sh` существует и подключён, и это барьер ИМЕННО этого
предмета, а не любой вызванный скрипт дерева: он же судит R1/R2 и он же — файл правки.
Строка ПРОВОДКА-ЭНФОРСМЕНТ обосновывает guard-only честно и проверяемо: инвариант дерева
(«существовал → обязан жить на HEAD либо нести именную ALLOW в НАСТОЯЩЕМ удаляющем коммите»)
не меняется, меняется только то, какой коммит признаётся настоящим удаляющим. Это чистый
энфорсмент; поведенческой нормы 029-класса контракт не несёт, role-канал не требуется (тот же
класс, что 039). Претензия Р1 — к механическому читателю поля, не к смыслу поля.

## (1) Область правки — не превышена

`git diff --name-status 625e61e^ 719b917` даёт ровно: контракт (A),
`scripts/check_protected.sh` (M), шесть НОВЫХ фикстур (A) + процессные/судейские файлы чужих
каналов (`HANDOFF.md`, `NABLIUDENIA_ARCHITECT.md`, `registry/contracts.tsv`, три вердикта).
Три коммита architect'а — `625e61e`, `e84313e`, `4a22764`, все под `user.name=architect`.
`f9ae635` тронул ТОЛЬКО `NABLIUDENIA_ARCHITECT.md` (33 вставки) — процессный файл, выведенный
из суда зон.

Зона 040 не тронута: `git diff --name-only 625e61e^ 719b917 --
fixtures/check_protected/case_slijanie_s_ours.sh
fixtures/check_protected/red_predel_git_vyzovov_ours.sh scripts/verify_antiplacebo.sh
.github/workflows/ci.yml package.json` → ПУСТО.

Внутри `scripts/check_protected.sh` диф прочитан целиком: стартовая проба
`merge-tree --write-tree` (рядом с пробой `comm`), блок комментариев, НОВЫЕ `state_at` и
`is_lagging_merge_artifact`, переписанный цикл `excuse_for`. `moved_for`, область по
`roles/*.md`, разбор `verdict`, `comm`-разность, env-гигиена, коды 0/1/2 — не тронуты.
`bash scripts/check_zones.sh` → **rc=0** (40 замороженных, 1093 коммита по зонам).

СОВЕТ критика v3 (:220 «правка ограничена функциями» не покрывает стартовую пробу) в тексте
контракта не исполнен — проба действительно вне трёх названных функций. Файловая зона её
покрывает, `check_zones.sh` зелёный; класс — незакрытый СОВЕТ, не находка.

## (2) Сырой вывод — перепроверен мной живьём

**R1, реальное дерево dev-harness** (`bash scripts/check_protected.sh .`) — **rc=0**:

```
  ok   исчез с явного разрешения: verdicts/critic/contracts-040-v2.md
  ok   исчез с явного разрешения: verdicts/critic/contracts-040-v3.md
  ok   исчез с явного разрешения: verdicts/critic/contracts-040-v4.md
…
область: … · коммитов пройдено: 2002 · существовало: 401 · на HEAD: 372 · исчезло: 0 · с разрешения: 25 · перенесено: 4
rc=0
```

**R2** `bash fixtures/check_protected/repro_n127_otstavshij_roditel.sh` → **rc=0**,
`ok Н-127: легитимное удаление за отставшим родителем мержа распознано`.
**R5** `bash -n scripts/check_protected.sh` → **rc=0**.

**R4** `bash scripts/verify_antiplacebo.sh --scope check_protected` — своим прогоном:
`барьеров: 1 · фикстур: 25 · предъявлено красным повторным прогоном: 25 · расхождений: 3`.
Все 25 — с зелёным контролем и именованным красным диагнозом (лог полностью прочитан).
**Общий rc процесса — 1, НЕ 0**, и это названо, а не спрятано за счётчиком: три расхождения
вне семьи — `check_threat_model.sh` не классифицирован, `fixtures/check_threat_model` и
`fixtures/parsing_hygiene_battery` без распознанного барьера (предсуществующий остаток
контракта 041, А-216). Зелёным полным CI это не является и таковым не объявляется.

## (3) Проверка не переписана под реализацию

Собственная мера, блоб-в-блоб, все 22 файла `fixtures/check_protected/`, существовавшие ДО
042 (`625e61e^` → HEAD): **22/22 SAME**, включая `red_predel_git_vyzovov_ours.sh` (`b4970ca`,
предмет 040), `case_slijanie_s_ours.sh` (`3611f2d`) и каркас `_repo.sh` (`c36d43d`).
`git diff --exit-code 625e61e^ HEAD -- <22 пути>` → **rc=0**.

СВОЙ счёт фикстур (норма 7 — своей мерой, не повтором чужой команды):
`case_*.sh` на `625e61e^` — **20**; на `fc4de39` (перед v3) — **22**; на HEAD — **25**.
Добавлено ровно 5 `case_*.sh` за три круга + `repro_n127_otstavshij_roditel.sh`.

## (4) Предмет РЕАЛИЗОВАН, не только оттестирован

Исходный баг Н-127 закрыт на реальном дереве: три пути `verdicts/critic/contracts-040-v{2,3,4}.md`,
удалённые легитимно коммитом `4b3666f`, распознаны как «исчез с явного разрешения» (R1 выше),
барьер целиком зелёный. Транзитивный блок done 038/040/041 по ЭТОЙ причине снят.

Дискриминация на моём собственном дереве (одна и та же история, два содержимого барьера):

| моя проба (построена с нуля) | новый барьер | `625e61e^:check_protected.sh` |
|---|---:|---:|
| R-C законная топология Н-127, путь С ПРОБЕЛОМ `verdicts/critic/rv 1.md` | **rc=0** ok | **rc=1** FAIL |
| R-F мерж с конфликтом по СОСЕДНЕМУ пути, `p` унаследованно отсутствует | **rc=0** ok | **rc=1** FAIL |
| R-G КАСКАД: мерж мержа, ДВА уровня отставания, 3 кандидата diff-filter=D | **rc=0** ok | **rc=1** FAIL |
| R-D restore + повторное удаление внутри мержа, ALLOW только у D1 | **rc=1** FAIL | rc=1 FAIL |
| R-A mode-only modify/delete (З1) × пробел в пути (З3), ALLOW только у D1 | **rc=1** FAIL | rc=1 FAIL |
| R-B criss-cross через `commit-tree`, две равноправные базы (З2) | **rc=1** FAIL | rc=1 FAIL |
| R-E октопус (3 родителя), путь отсутствует, тела без ALLOW | **rc=1** FAIL | — |

Пересборка столов арбитра МОИМИ репозиториями:
- **З1** — `git merge` предъявил живой `CONFLICT (modify/delete): verdicts/critic/rv 1.md
  deleted in HEAD and modified in side`; состояния `100644 3690714…` / `100755 3690714…`
  (тот же OID, разный режим — подтверждаю, что голый oid эту разницу не видит).
- **З2** — `git merge-base --all P1 P2` → **2** базы, без `--all` → одна (`9248c3e`); оракул
  rc=0, путь **ПРИСУТСТВУЕТ** в авто-мерже (`100644 3690714…`) → не артефакт → барьер rc=1.
  Строка арбитра воспроизведена независимо.
- Формат `-z` оракула проверен побайтово (`od -c`): `<tree-oid>NUL<mode> <oid> <stage>\t<path>NUL…`
  — разделение по ПЕРВОМУ TAB в коде соответствует фактическому формату git 2.55.0.

Две проверки, которых не делал ни один из трёх предыдущих судей, и обе зелёные:
- **R-F**: непустая секция конфликтов, в которой `p` НЕТ (конфликт по соседнему пути). Это
  единственный вход, где цикл разбора конфликтов доходит до конца без совпадения; проверено
  отдельно, что идиома `[ … ] && return 1` в теле `while` под `set -euo pipefail` не рвёт
  барьер (`reached-after-loop`, rc функции сохраняется).
- **R-G**: КАСКАД двух подряд отставших слияний — §Риски-1 контракта прямо говорит, что живьём
  проверена только ОДНА глубина. Проверена вторая: 3 кандидата diff-filter=D, оба артефакта
  пропущены, ALLOW взят у `D1`, rc=0. Остаток 1 контракта теперь измерен, а не только выведен.

Отдельно проверил свою гипотезу об остаточном Н-127-классе в НЕ тронутой `moved_for`
(она сохранила `sed -n 1p`): построил перенос за мержем с отставшим родителем (H2) —
**rc=0, «перенесён, контент жив на HEAD»**, как и линейный контроль H1. Гипотеза
ОПРОВЕРГНУТА измерением: `-m --name-status` на мерже показывает и `D p`, и `A q` против
отставшего родителя, условие А выполняется. Находкой это не является.

## (5) Атомарность

Три коммита architect'а — по одному на круг, каждый со ссылкой на предмет (контракт/круг/
Н-127/арбитраж). Судейские коммиты — отдельные, под своими именами (`critic`, `arbiter`,
`adversary`, `owner`). Связки из нескольких задач в одном коммите нет.

## (6) Норма не тронута молча

Нормативных документов (`AGENTS.md`, `roles/*.md`, `NORMS`-класс) диапазон не касается —
проверено `git diff --name-status`. Усиления/ослабления нормы внутри задачи о механизме нет:
инвариант защищённого артефакта остался буква в букву, изменился только критерий «какой
коммит считается настоящим удаляющим». Антигейминг-граница («разрешение ровно из ОДНОГО,
однозначно определённого коммита; дальше поиск не идёт») сохранена — предъявлено моим R-D.

## (7) Заявленное равно сделанному — расхождения найдены, оба не в предмете

- **Н1 (класс: неточность счётной формулировки в вердикте адверсария).**
  `verdicts/adversary/contracts-042-oracle-adversary.md`: «including the **22 pre-042 cases**
  and the three round-3 cases». Моя мера: pre-042 `case_*.sh` — **20**, не 22; 22 — это срез
  на `fc4de39`, уже включающий две фикстуры кругов 1/2 САМОГО 042. Итог 25 верен, ярлык — нет.
  Ровно тот класс, что уже стоил дорого («около 41 скрипта» против 34): называю.
- **Н2 (класс: неточность атрибуции в вердикте критика).**
  `verdicts/critic/contracts-042-v1.md`: «на HEAD `f9ae635` (изменение v3 — `4a22764`,
  сохранение длинных строк — `f9ae635`)» читается как правка контракта коммитом `f9ae635`.
  Мера: `f9ae635` тронул только `NABLIUDENIA_ARCHITECT.md`; блоб контракта на `4a22764`,
  `f9ae635` и HEAD — один и тот же `c761235b`. Судимый блоб от этого не меняется.
- Все прочие счётные заявления трёх судей своей мерой подтверждены: 22/22 неизменных старых
  фикстур, 25 фикстур / 25 красных, rc=0 на реальном дереве, rc=0 у `bash -n`, 2 базы в З2,
  путь в конфликтах в З1, октопус fail-closed.

## Классификация находок (полный перечень)

| # | находка | класс | блокирует |
|---|---|---|---|
| Р1 | `check_provodka.sh` rc=1 на 042 → `done_contract.sh:124` die; причина — катч-олл г0 круга 11 контракта 038; тот же красный у уже-done 039 | предсуществующий регресс ВНЕ зоны 042 | **done** (не предмет, не заморозку) |
| Н1 | «22 pre-042 cases» в вердикте адверсария (фактически 20) | неточность счёта в вердикте | нет |
| Н2 | атрибуция «сохранение длинных строк — f9ae635» в вердикте критика | неточность атрибуции в вердикте | нет |
| Н3 | СОВЕТ критика v3 к :220 (стартовая проба вне «правка ограничена функциями») не исполнен | незакрытый совет | нет |
| Н4 | §Риски-1 (каскад глубины 2 «живьём не проверен») теперь измерен мной, rc=0 верно | остаток сузился | нет |

Действительных находок в предмете 042 — **ноль**. Ни одной новой дыры оракула, ни одного
ослабления гейта, ни одной переписанной под код проверки.

Одноразовые драйверы и toy-деревья — вне репозитория, `/tmp/dev-harness-verify/rev042/`.
Единственный постоянный артефакт этой роли — этот файл.

---

## Постскриптум (дописан тем же судьёй, правка форматирования первой строки)

Две правки этого файла относительно коммита `b6c5d6f`, обе — формат и факт, ни одна не
меняет находок, доказательств и вердикта:

1. **Первая строка приведена к `accept` РОВНО.** `scripts/done_contract.sh:94-96` читает
   первую строку свежего вердикта ревьюера, тримит, опускает регистр и сравнивает СТРОГИМ
   равенством (`case "$norm" in accept) ;; … *) die "done: вердикт вне грамматики: …"`), а
   не префиксом. Строка `accept предмета · БЛОКЕР DONE вне зоны 042` уходила в `*)` и сама
   блокировала done — дефект формата МОЕГО файла, предъявлен оркестратором, проверен мной по
   исходнику барьера; известный-хороший прецедент — `verdicts/review/contracts-039-k1.md`,
   первая строка которого ровно `accept`. Квалификатор перенесён на вторую строку дословно.

2. **Находка Р1 закрыта — но не мной и не внутри 042.** После `b6c5d6f` приземлился круг 16
   контракта 038: `b40f984` («implementer: 038 круг 16 — фикс регресса "ЭНФОРСМЕНТ-абзац
   перенесён на несколько строк"»), судимый `c7d3ab3` (adversary, in_enf boundary). Своей
   мерой, не на слово:

   ```
   $ bash scripts/check_provodka.sh . contracts/042-excuse-for-otstavshij-roditel.md ; echo rc=$?
   rc=0
   $ bash scripts/check_provodka.sh . contracts/039-check-protected-rename.md ; echo rc=$?
   rc=0
   ```

   Развязка выбрана та, что я называл первой (правка `check_provodka.sh` своим контрактом,
   не перезаморозка 042) — и она сняла тот же красный у уже-done 039. Блоб контракта 042
   (`c761235b`) и блоб `scripts/check_protected.sh` (`f71aaae7`) при этом не менялись, поэтому
   accept предмета остаётся привязан к тем же блобам, что судились: Р1 был отказом done-шагу,
   а не предмету, и закрыт вне зоны 042 — ровно там, где я его и назвал.

Н1–Н4 остаются в силе как записаны. Действительных находок в предмете 042 — по-прежнему ноль.
