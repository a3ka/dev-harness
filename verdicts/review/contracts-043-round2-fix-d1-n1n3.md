ACCEPT

Предмет: коммит `80e1ea842f17332fa11d91cba35aade740f00515` (implementer, «контракт 043 —
закрытие FAIL ревьюера-v1 (4 правки в одной зоне)») против находок Д1, Н1, Н2, Н3 вердикта
`verdicts/review/contracts-043-v1.md` (коммит вердикта `3af3e06`).

Область суда — РОВНО дифф этого коммита (5 файлов: `.github/workflows/ci.yml`,
`fixtures/check_precision_gate/.probe-only`,
`fixtures/check_precision_gate/red_15_forged_trace_via_xtracefd.sh`,
`fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh`,
`scripts/check_precision_gate.sh`). Округ контракта 043 целиком проверен кругом 1 и здесь
НЕ переоткрывается.

**Д2 вне области — решение владельца об отсрочке, Н4 вне области — неисправимо** (история
не переписывается; сообщение `bf1874a` остаётся как есть). Коммиты `8766e769` и `4de64aa`
(СПАСЕНО-строка v2-черновика `contracts/043-*.md`) — ОТДЕЛЬНЫЙ судебный трек, в этом
приговоре не участвуют.

Клон: `ssh://git@github.com/a3ka/dev-harness.git` →
`/tmp/dev-harness-verify/reviewer-043-r2`, `git remote -v` сверен до работы (обе строки
`ssh://`), HEAD `96dcb449b95db2c212b81e57e3084008c8a9e111`. Все мутации — в одноразовых
копиях клона (`/tmp/dev-harness-verify/probe-n1`) и в пробах ВНЕ дерева-предмета
(`/tmp/dev-harness-verify/probe_n2_liveness.sh`), дерево-предмет не мутировалось.

Итог: **4/4 находок круга 1 закрыты измеримо**; найдено 3 НОВЫХ не-блокирующих находки
(Н5, Н6, Н7) и 1 совет (С1). Блокеров в диапазоне коммита нет.

---

## Д1 — ЗАКРЫТА (проверено живым трейсом, не чтением)

Комментарий `scripts/check_precision_gate.sh:39-44` на HEAD говорит «порядок исполнения —
а → б → в». Моя мера — не повтор чужой пробы и не структурный аргумент по номерам строк, а
трассировка РЕАЛЬНОГО прогона с `PS4='+L${LINENO} '`:

```text
$ PS4='+L${LINENO} ' bash -x scripts/check_precision_gate.sh . contracts/043-precizionnyj-prefriz-gejt.md 2> trace_d1.txt
OK   [rc=0]   (44872 строки трассы)

$ grep -n -m1 '^\+L184 ' trace_d1.txt   # задача (а), первая исполняемая строка блока
4014:+L184 state=full
$ grep -n -m1 '^\+L273 ' trace_d1.txt   # задача (б), цикл FAMILIES
44845:+L273 for _self_key in "${!FAMILIES[@]}"
$ grep -n -m1 '^\+L291 ' trace_d1.txt   # задача (б), счётчик глубины
44862:+L291 _PGATE_TASK_B_DEPTH=0
$ grep -n -m1 '^\+L521 ' trace_d1.txt   # задача (в), проверка наличия verify_ci_parity.sh
44865:+L521 '[' -f …/scripts/verify_ci_parity.sh ']'
$ grep -n -m1 '^\+L523 ' trace_d1.txt   # задача (в), живой вызов verify_ci_parity.sh
44869:+L523 ci_out=…
```

Позиции в трассе строго возрастают: 4014 (а) < 44845/44862 (б) < 44865/44869 (в). Порядок
исполнения — **а → б → в**, ровно как теперь написано в комментарии. Ложная формулировка
«а → в → б» из блоба круга 1 отсутствует (`git show 80e1ea84` — строки 39-44 переписаны
целиком).

Структурная привязка на HEAD (номера строк ПЕРЕСЧИТАНЫ мной, не взяты из вердикта круга 1,
где они были :519/:523/:524): заголовок ЗАДАЧА (а) — `:183`, ЗАДАЧА (б) — `:250`, конец
блока (б) — `:519` (`fi`), заголовок ЗАДАЧА (в) — `:520`, вызов `verify_ci_parity.sh` —
`:523`, отказ — `:524`.

Второй канал Д1 — `.github/workflows/ci.yml`. Ложная посылка «иначе при красном паритете
check_precision_gate будет блокирован на своём же шаге (в) раньше времени» УДАЛЕНА; на её
месте (`ci.yml:208-210`):

```text
# Запускается ПОСЛЕ check:ci-parity: паритет CI проверяется отдельным
# шагом раньше, чтобы красный паритет был виден как самостоятельный
# результат, а не только как именованная причина задачи (в) гейта.
```

Утверждение «запускается ПОСЛЕ check:ci-parity» сверено своей мерой — порядок шагов в
самом workflow: `run: npm run check:ci-parity` — `ci.yml:110`, `run: npm run
check:precision-gate` — `ci.yml:213`. 110 < 213, утверждение истинно. Новая формулировка
ложных посылок о порядке задач гейта не несёт.

## Н1 — ЗАКРЫТА (воспроизведена РОВНО мутация круга 1)

Мутация круга 1 повторена в одноразовой копии клона: `scripts/check_precision_gate.sh:61`,
`die()` — `exit 1` → `exit 0` (гейт по-прежнему ПЕЧАТАЕТ именованную причину, но не
отказывает). Прогон ВСЕЙ пачки:

```text
$ python3 mutate.py scripts/check_precision_gate.sh 61 'exit 1; }' 'exit 0; }'
scripts/check_precision_gate.sh:61 -> die()  { printf 'precision-гейт 043: %s\n' "$*" >&2; exit 0; }

$ bash fixtures/_krasnye_043.sh          # в мутированной копии
check_precision_gate/red_02_zona_collision_undeclared.sh rc=1   FAIL
check_precision_gate/red_04_peresechenie_grammar.sh rc=1        FAIL
check_precision_gate/red_05_peresechenie_unbound.sh rc=1        FAIL
check_precision_gate/red_06_case_polarity_undeclared.sh rc=1    FAIL
check_precision_gate/red_07_case_no_live_call.sh rc=1           FAIL
check_precision_gate/red_08_case_self_check_fails.sh rc=1       FAIL
check_precision_gate/red_13_multi_collision_same_path.sh rc=1   FAIL
check_precision_gate/red_14_freeze_refuses_red_precision.sh rc=1 FAIL
check_precision_gate/red_15_forged_trace_via_xtracefd.sh rc=1   FAIL   ← ТЕПЕРЬ ЛОВИТ
  ОТКАЗ: case_15 (обход Б2 не отвергнут): rc 0 (ожидался 1)
check_precision_gate/red_16_xtrace_only_pass.sh rc=1            FAIL
check_precision_gate/red_21_ci_parity_orphan_script.sh rc=1     FAIL
итог: 18 файлов, провалов 11
[rc=1]
```

Круг 1: **провалов 10**, `red_15 rc=0` («НЕ поймал»). Круг 2: **провалов 11**, `red_15
rc=1`. Одиннадцать красных из одиннадцати ловят мутацию — дыра закрыта.

Честная ветка не сломана: на НЕмутированном клоне `bash fixtures/_krasnye_043.sh` →
`итог: 18 файлов, провалов 0 [rc=0]`, `red_15 rc=0`.

Правка — УСИЛЕНИЕ, не ослабление: в обе ветки принятия добавлен конъюнкт `[ "$LAST_RC" -eq
1 ]` ПЕРЕД grep-ом фразы; ни одно прежнее условие не снято (сверено по диффу
`git show 80e1ea84 -- fixtures/check_precision_gate/red_15_forged_trace_via_xtracefd.sh`:
две строки изменены, обе — добавление конъюнкта).

## Н2 — ЗАКРЫТА (и заявленное покрытие ПРОВЕРЕНО на живость, а не на молчание stderr)

Первая мера — прогон батареи на чистом клоне:

```text
$ npm run check:precision-battery
БАТАРЕЯ check_precision_gate: класс delimiter-collision      — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс regex-injection          — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс silent-drop              — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс self-application-green   — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: итог 4/4 классов закрыто
[rc=0]
```

Строки `fatal: tag 'frozen/contracts/991/1' already exists` в выводе НЕТ (в круге 1 она
печаталась перед строкой silent-drop). Симптом снят.

Отсутствие симптома, однако, само по себе не доказывает, что покрытие ОЖИЛО — это ровно
тот класс, за который Н2 и была названа. Поэтому вторая, различающая мера: проба ревьюера
ВНЕ дерева-предмета на штатных хелперах `fixtures/check_precision_gate/_toy.sh`
(`mk_toy_repo`/`mk_foreign_frozen`/`put_draft`). Обе коллизии на `shared/file.txt`
ОБЪЯВЛЕНЫ строками ПЕРЕСЕЧЕНИЕ — единственным возможным источником красноты остаётся
необъявленная коллизия на `one/first.txt`, то есть РОВНО тот третий вызов
`mk_foreign_frozen`, покрытие которого было инертным:

```text
$ bash probe_n2_liveness.sh <клон> 993        # NNN третьего вызова ПОСЛЕ фикса
NNN третьего вызова=993  rc=1
precision-гейт 043: зона-коллизия: one/first.txt заявлен architect (этот контракт,
NNN 043), уже в union под NNN 993 для alice — нужна строка ПЕРЕСЕЧЕНИЕ architect:
one/first.txt — 993 <причина>

$ bash probe_n2_liveness.sh <клон> 991        # NNN третьего вызова ДО фикса (дубль)
fatal: tag 'frozen/contracts/991/1' already exists
NNN третьего вызова=991  rc=0
OK
```

Дифференциал однозначен: со старым дублирующим 991 третья коллизия НЕ существует (тег не
создан, union её не знает, гейт зелен), с 993 — существует и краснит гейт ИМЕНОВАННОЙ
причиной на `one/first.txt`. Заявленное комментарием покрытие «отдельная коллизия на
ДРУГОМ пути» стало настоящим. Строка ПЕРЕСЕЧЕНИЕ в черновике фикстуры синхронизирована
(`— 993`), иначе класс краснел бы тавтологически.

Правка — УСИЛЕНИЕ входа фикстуры, не ослабление критерия: условие принятия класса
(`[ "$rc" -eq 0 ] || ! grep -Fq 'зона-коллизия:'` → провал) коммитом не тронуто.

## Н3 — ЗАКРЫТА (счёт сверен ДВУМЯ независимыми мерами)

```text
$ git ls-tree --name-only HEAD -- fixtures/check_precision_gate/ | wc -l
20
$ git ls-tree --name-only HEAD -- fixtures/check_precision_gate/ | grep -c '/red_'
11
$ git ls-tree --name-only HEAD -- fixtures/check_precision_gate/ | grep -c '/green_'
7
$ git ls-tree --name-only HEAD -- fixtures/check_precision_gate/ | grep -cE '/(red|green)_'
18
$ bash fixtures/_krasnye_043.sh
итог: 18 файлов, провалов 0   [rc=0]
```

Двадцать объектов в каталоге = 18 сценариев + `.probe-only` + `_toy.sh` (последние два —
не сценарии и раннером не подбираются). Обе меры — листинг дерева и собственный счётчик
раннера — дают 18.

- `.github/workflows/ci.yml:218`: «гоняет ВСЕ 18 red_*/green_*-файлов» — ТОЧНО (18 = 11
  red + 7 green). Было 14.
- `fixtures/check_precision_gate/.probe-only`: «агрегатор всех 18 сценариев» — счёт ТОЧЕН.
  Было 9. (К сопутствующей нумерации — Н6 ниже.)

Своей мерой сверено также, что стале-счёт нигде больше не остался: подстрочный поиск
`9 сценариев|14 red_|ВСЕ 14|всех 9` по всему дереву даёт единственные попадания в тексте
самого вердикта круга 1 (где старые формулировки цитируются законно) и в несвязанной
фикстуре `fixtures/check_judge_gate/stab_detektor_svoja_cwd.sh` («ворота 1–6, 8–14»).

---

## Н5 (НОВАЯ, не-блокер, класс «правка внесла новый дефект в правимый ею же текст»)

Фикс Н3 уничтожил фрагмент `land»).` в `fixtures/check_precision_gate/.probe-only`: цитата
осталась незакрытой, и две фразы слиплись в одну бессвязную.

```text
$ git show 80e1ea84^:fixtures/check_precision_gate/.probe-only   (фрагмент)
…«конверсия — пачка ARCHITECT после
land»). Предъявление — прямой прогон fixtures/_krasnye_043.sh (агрегатор всех
9 сценариев Р1-Р9, contracts/043-*.md §Приёмка), исполняется судьями/архитектором

$ git show HEAD:fixtures/check_precision_gate/.probe-only        (фрагмент)
…«конверсия — пачка ARCHITECT после
Предъявление — прямой прогон fixtures/_krasnye_043.sh (агрегатор всех
18 сценариев Р1-Р21; §Приёмка контракта покрывает Р1-Р9, остальные — dogfood
после freeze), исполняется судьями/архитектором

$ grep -c 'land»)' fixtures/check_precision_gate/.probe-only
0    [rc=1]
```

Заодно утрачена ссылка `contracts/043-*.md §Приёмка` — единственное указание, ГДЕ искать
пункты приёмки. Тяжесть: rc-эффекта нет, маркер `.probe-only` читается барьерами по факту
существования, а не по телу; но файл-маркер, объясняющий ИСКЛЮЧЕНИЕ семьи из area-scan,
теперь содержит оборванную цитату прецедента — следующий читающий не сможет проверить
прецедент по ссылке. Лечение — однострочное: вернуть `land»).` и ссылку на §Приёмка.

## Н6 (НОВАЯ, не-блокер, класс «нумерация не соответствует источнику»)

Новый текст `.probe-only` — «агрегатор всех 18 сценариев Р1-Р21» — вводит диапазон
Р1-Р21, которого в замороженном тексте контракта нет. Моя мера — перечисление всех Р-меток
блоба `frozen/contracts/043/1`:

```text
$ git show frozen/contracts/043/1:contracts/043-… | grep -o 'Р[0-9]\+[а-я]*' | sort -u -V
Р1 Р2 Р3 Р4 Р5 Р6 Р7 Р8 Р9 Р10 Р11 Р12 Р13 Р14
```

Р15–Р21 в контракте отсутствуют (тот же факт круг 1 зафиксировал в Н4 про `bf1874a`).
«Р» в `.probe-only` фактически означает индекс в имени файла (`red_21` → «Р21»), а в
контракте — пункт §Приёмка; одна буква на два разных ключа. Не блокер и не ложь по счёту
(18 верно), но нумерация, которую нельзя разрешить по названному источнику.

Вторая половина той же фразы — «§Приёмка контракта покрывает Р1-Р9» — ПРОВЕРЕНА и
истинна в своём классе: пункты Р1–Р9 замороженной §Приёмки поимённо называют файлы семьи
(green_01, red_02, green_03, red_04, red_05, red_06, red_07, red_08, green_09), а Р10–Р14
— агрегатные и CI-пункты, отдельных сценариев не вводящие. Формулировку круга 1 «§Приёмка
идёт до Р14» это не опровергает и ею не опровергается: речь о разных срезах.

## Н7 (НОВАЯ, не-блокер ЗДЕСЬ, класс «область правки»; закрытие — в ДРУГОМ треке)

`bash scripts/check_zones.sh .` на HEAD красен и называет `80e1ea84` ПРЯМО:

```text
  FAIL коммит вне зоны: implementer 80e1ea84 fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh …
  FAIL коммит вне зоны: implementer 8766e769 contracts/043-precizionnyj-prefriz-gejt.md …
замороженных контрактов: 42 · объявленных авторов: 2 · коммитов в диапазонах: 1926 ·
проверено по зонам: 1290
[rc=1]
```

Факт подтверждён первоисточником — §Зоны замороженного блоба:

```text
:275 ЗОНА architect: … fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh …
:277 ЗОНА implementer: scripts/check_precision_gate.sh fixtures/check_precision_gate/
     scripts/freeze_contract.sh .github/workflows/ci.yml package.json
```

Путь батареи объявлен ТОЛЬКО под architect. Значит фикс Н2 (один из четырёх предметов
этого коммита) исполнен вне зоны автора. Называю прямо, потому что задание этого круга
утверждало «ноль FAIL на 80e1ea84» — это утверждение фактически неверно, оба живых FAIL
названы поимённо выше, и один из них — на судимом коммите.

Не блокирую по двум основаниям, и только по ним: (1) закрытие уже маршрутизовано — строка
`СПАСЕНО implementer: 80e1ea842f17332fa11d91cba35aade740f00515 8766e76946658b548bb9d4be870dfcf0b97ef288 …`
внесена архитектором (`4de64aa`) в v2-черновик `contracts/043-*.md:305` и судится
ОТДЕЛЬНЫМ треком; (2) содержание самой правки — замена одного числа во входе мёртвой
фикстуры, обхода не даёт (доказано живостью класса выше).

Явная граница этого ACCEPT: он НЕ покрывает зона-нарушение. Пока v2-черновик со
СПАСЕНО-строкой не заморожен, `check_zones.sh` остаётся красным, и `done_contract.sh` по
этому основанию не пройдёт независимо от настоящего вердикта.

## С1 (совет, не находка)

Комментарий `scripts/check_precision_gate.sh:44` несёт замер «(1.574с на здоровом HEAD)».
Своей мерой на своём железе получаю втрое меньше:

```text
$ time bash scripts/verify_ci_parity.sh .
… расхождений: 0
real 0m0.534s   [rc=0]
```

Противоречием не считаю — стеночный замер зависит от машины, а класс утверждения («(в)
дешевле всего») мой замер ПОДТВЕРЖДАЕТ: весь гейт на реальном HEAD — 5.65 с стены, из них
(в) — 0.53 с. Совет: числа-с-секундами в комментариях стареют молча; либо указывать
машину замера, либо оставлять порядок величины.

---

## Стандартные обязанности ревьюера — в границах ЭТОЙ области

**1. Область правки.** Дифф `80e1ea84` — 5 файлов, 18 вставок / 17 удалений. Четыре из
пяти путей (`scripts/check_precision_gate.sh`, `fixtures/check_precision_gate/.probe-only`,
`fixtures/check_precision_gate/red_15_…sh`, `.github/workflows/ci.yml`) — внутри
implementer-зоны 043 (:277; для `ci.yml` есть ПЕРЕСЕЧЕНИЕ implementer — 011/015, :299-300).
Пятый — вне зоны, см. Н7. Замороженный текст контракта этим коммитом НЕ тронут (в
`--stat` его нет; `git log 80e1ea84 -1 --name-only` подтверждает).

**2. Сырой вывод.** Каждая цифра и каждый вердиктный факт выше получены МОИМ прогоном в
МОЁМ клоне, с кодами возврата; ни одна строка не взята из сообщения коммита `80e1ea84` и
ни одна не перенесена из вердикта круга 1 без пересчёта (номера строк пересчитаны,
счётчики перемерены, мутация воспроизведена заново).

**3. Проверка не переписана под реализацию.** Мера: `git log --diff-filter=M 80e1ea84^..HEAD
-- fixtures/check_precision_gate/` → единственный коммит `80e1ea8`; `git log 80e1ea84..HEAD`
по всем четырём предметным путям → ПУСТО (после фикса семью никто не трогал). Внутри
`80e1ea84` из проверок изменены ровно две, обе — в сторону УЖЕСТОЧЕНИЯ:
`red_15` получил `[ "$LAST_RC" -eq 1 ]` в обе ветки принятия (прежде принималась печать без
rc), профиль батареи получил живой третий тег вместо мёртвого дубля. Ни одно условие
провала не ослаблено и ни одна проверка не подогнана под вывод кода.

**4. Красное предъявлено.** Предъявлено для обоих барьеров, правленных этим коммитом:
`red_15` — мутацией `die() → exit 0` (rc 1 под мутацией, rc 0 на честном коде); класс
silent-drop батареи — дифференциалом 993 против 991 (rc 1 против rc 0 на одном и том же
входе). Зелёное без красного здесь нигде не принято.

**5. Атомарность.** Одна задача — один коммит: `80e1ea84` сводит четыре находки ОДНОГО
вердикта одного круга, каждая названа в теле поимённо и локальна. Отказом не считаю (тот
же критерий, по которому круг 1 не отказал `bf1874a`).

**6. Норма не тронута.** Ни `AGENTS.md`, ни `roles/*`, ни `contracts/*` в диффе нет —
сверено `--stat`. Правки — только код барьера (комментарий), фикстуры, маркер и CI.

**7. Заявленное равно сделанному.** Все четыре счётных/поведенческих утверждения сообщения
`80e1ea84` перемерены МОЕЙ мерой и подтвердились: порядок а→б→в (трассой, не чтением), 18
файлов (двумя независимыми счётчиками), red_15 ловит мутацию (прогоном пачки), батарея 4/4
без `fatal: tag` (прогоном) плюс живость покрытия (дифференциальной пробой, которой в
сообщении коммита не было). Расхождений «заявленное ≠ сделанное» в этом коммите не найдено
— за исключением Н7, где неверно было утверждение ЗАДАНИЯ, а не коммита.

---

## Условие вердикта

ACCEPT узкого предмета: Д1, Н1, Н2, Н3 закрыты измеримо, не текстом. Новые находки Н5, Н6
— не-блокирующие, лечение однострочное, в implementer-зоне. Н7 — зона-нарушение судимого
коммита, закрытие маршрутизовано СПАСЕНО-строкой v2-черновика и судится отдельным треком;
настоящий ACCEPT его НЕ покрывает и красноту `check_zones.sh` не снимает.

Ревьюер, круг 2, 2026-09-24.
