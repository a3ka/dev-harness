FAIL

# Ревьюер — контракт 038 «ПРОВОДКА done-gate» (Н-116), круг 2 (финальный перед done)

Предмет суда: замороженный текст `frozen/contracts/038/2` (тег-объект
`76065d0a378a6a62df8a88c1ae07f1b604e665bc`, коммит `7aa36bf`) против живого дерева.
Судимый HEAD — `3222564` (на момент начала круга был `73a6808`; единственная дельта
между ними — `HANDOFF.md`, проверено `git log --name-only 73a6808..HEAD`; блоб контракта
на обоих HEAD один и тот же).

Судится ВСЁ окно, а не последняя правка: текст v2 целиком, вердикты критика v1/v6/v7/v8
и переподача v2, четыре арбитражных файла `verdicts/arbitration/contracts-038-*.md`,
реализация (`scripts/check_provodka.sh`, `scripts/done_contract.sh`,
`scripts/check_consumers.sh`, три семьи фикстур, раннер), закрытие Р1/Р2 круга 1
(`verdicts/review/contracts-038-k1.md`, `5dad795`).

Клон суда: `/tmp/dev-harness-verify/reviewer038-k2`. Ниже — сырой вывод с кодами
возврата; пересказов без вывода в этом вердикте нет. Каждое счётное утверждение чужих
отчётов перемерено МОЕЙ мерой, другой командой.

## Вердикт

**FAIL** — один блокер (Б1: находка Р1 круга 1 закрыта только в нормативной половине,
кодовая половина не тронута ни одним коммитом) и три именованных остатка (О1–О3), не
блокирующих. Предмет по существу работает: все четыре обязательные машинные меры
зелены, ПРОВОДКА самого 038 честна, зоны и нормативные документы посторонними руками
не тронуты, подпись владельца и блоб-тождество критик-accept проверены и подтверждены.

Отказ узкий и дешёвый: правка комментария в файле зоны implementer, БЕЗ v+1, БЕЗ новой
подписи владельца, БЕЗ переработки замороженного текста.

## Четыре особых вопроса круга — что подтверждено моей мерой

### 1. Подпись владельца — в теле коммита, который РЕАЛЬНО меняет файл. ПОДТВЕРЖДЕНО

```
$ git show --stat --format=fuller 4e0db83
commit 4e0db839cf0a65814bbca5fb596347c1fab67e17
Author:     architect <architect@dev-harness.local>
AuthorDate: Wed Sep 23 11:47:09 2026 +0200
Commit:     owner <owner@dev-harness.local>
CommitDate: Wed Sep 23 13:41:03 2026 +0200

    architect: 038 v6 — исполнение арбитража 78785ec (HEAD-граница): …

    РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/038-provodka-done-gejt.md честная коррекция ложного
    описания механизма §Инв.3 (2-арг, чтение с диска) + честное имя пробела
    провдка-целей на HEAD (П2) + П3 как отслеживаемый остаток; основание — арбитр
    78785ec + критик-accept; не ослабление (П1)

 contracts/038-provodka-done-gejt.md | 53 ++++++++++++++++++++++++++++++++++++-
 1 file changed, 52 insertions(+), 1 deletion(-)
rc=0
```

Строка `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` стоит в теле ТОГО ЖЕ коммита, который меняет
`contracts/038-provodka-done-gejt.md`, и никакого другого файла этот коммит не трогает
(52+/1−, один путь). Committer — `owner`: подпись отличается от «агент написал строку за
владельца» (прямо запрещено правилом 11; собственный инцидент зафиксирован в
`HANDOFF.md:182-186`). Несигнатурный предшественник `979d4eb` (author=committer=architect,
13:05) в историю НЕ входит:

```
$ git merge-base --is-ancestor 979d4eb HEAD && echo IS || echo NOT
NOT ancestor (dangling/amended)
```

Подписанный коммит — единственный носитель этого блоба в истории main.

### 2. Критик-accept судил ТОТ ЖЕ текст, что заморожен. ПОДТВЕРЖДЕНО блоб-сверкой

Мера — не «тот же коммит», а тождество блоба контракта в пяти точках плюс контроль:

```
$ git rev-parse frozen/contracts/038/2^{commit}:contracts/038-provodka-done-gejt.md
9b1507ac800fab9c63d9c796601540f122d5cb09
$ git rev-parse HEAD:contracts/038-provodka-done-gejt.md
9b1507ac800fab9c63d9c796601540f122d5cb09
$ git rev-parse 4e0db83:contracts/038-provodka-done-gejt.md        # подпись владельца
9b1507ac800fab9c63d9c796601540f122d5cb09
$ git rev-parse 0c6ebe2:contracts/038-provodka-done-gejt.md        # HEAD, названный в v8
9b1507ac800fab9c63d9c796601540f122d5cb09
$ git rev-parse b962a13:contracts/038-provodka-done-gejt.md        # коммит accept-вердикта
9b1507ac800fab9c63d9c796601540f122d5cb09
$ git rev-parse frozen/contracts/038/1^{commit}:contracts/038-provodka-done-gejt.md
524a0cca32489b932f747fd6777827aec97f20af   # контроль: v1 ≠ v2, мера различает
```

`contracts-038-v8.md` объявляет судимым `0c6ebe298b7dc02513217925dc717a324e583bf7`; этот
коммит НЕ предок текущего HEAD (`git merge-base --is-ancestor 0c6ebe2 HEAD` → `NO`), но
блоб контракта на нём побайтово равен замороженному. Судимый критиком текст и
замороженный текст — ОДИН блоб; вердикт связан с точным блобом пакета.

Переподача `contracts-038-v2.md` (`7aa36bf`) историю не разрушила: исторический k2-FAIL
того же имени был снят консолидацией `56d19e2` («038: консолидация вердиктов критика»,
`verdicts/critic/contracts-038-v2.md | 107 ------`), переподача создала файл заново
(18 insertions, 0 deletions). Содержательно она ссылается на v8 и нового суждения не
выносит — это техническое имя для `freeze_contract.sh`, не второй accept.

Дельта v1→v2 — ТОЛЬКО предписанное арбитражем:

```
$ git diff frozen/contracts/038/1 frozen/contracts/038/2 -- contracts/038-provodka-done-gejt.md
 три гунка: changelog-запись v6; замена строки §Инварианты 3 на двухаргументную форму;
 буллет «Граница механизма» в §Инварианты 3; буллет «ПРОВОДКА-цели на HEAD» в §Инварианты 4.
 Удалена РОВНО одна строка (старая сигнатура «корень = HEAD-дерево»), всё прочее — добавление.
rc=0
```

Тихого расширения области правки нет: ни одна другая строка замороженного текста не
изменена; `scripts/` и `fixtures/` этим коммитом не тронуты.

### 3. Н-136 — закоммичен; обратной ссылки из §Инварианты 4 НЕТ. ЧАСТИЧНО (см. О2)

```
$ git log -1 --format='%h %an %s' --name-only d6b747d
d6b747d orchestrator NABLIUDENIA: Н-136 (ПРОВОДКА-цели на HEAD, обязательный остаток П3
арбитража 038 — условие подписи владельца, не факультативно)
NABLIUDENIA.md            # единственный файл коммита
$ grep -c 'Н-136' NABLIUDENIA.md
2
$ grep -n '136' contracts/038-provodka-done-gejt.md
rc=1                      # ни одного вхождения
```

Запись Н-136 существует, помечена `ОТКРЫТО`, называет цену, класс (родствен Н-118/Н-115)
и «ЧЕМ ЗАКРЫТЬ» (отдельный майлстоун ПРОВОДКА-семейства; различающий вход уже определён
текстом v6). Обязательность зафиксирована дословной волей владельца. Условие подписи
ИСПОЛНЕНО.

Но §Инварианты 4 замороженного текста ссылается на арбитраж `78785ec` П3 и НЕ называет
Н-136: ссылка односторонняя (Н-136 → контракт есть, контракт → Н-136 нет). Не блокер
(обратная ссылка потребовала бы v3 и новой подписи; адрес предмета однозначен через П3),
но остаток назван — О2.

### 4. Р2 (CI-проводка) жива на текущем HEAD. ПОДТВЕРЖДЕНО

```
$ grep -n 'provodka' package.json
61:    "check:provodka": "bash scripts/check_provodka.sh",
64:    "check:provodka-family-selftest": "EXPECT_RC=0 bash fixtures/_krasnye_038.sh"
$ sed -n '153p;173p' .github/workflows/ci.yml
      - name: Сам-тесты семьи ПРОВОДКА (контракт 038)
        run: npm run check:provodka-family-selftest
$ sed -n '63p' .github/workflows/ci.yml
            keys: … check_provodka done_contract check_consumers
```

Два независимых канала на HEAD: шардовый ключ (28 `case_*.sh`) и отдельный шаг раннера
(37 `red_*/green_*`). Регрессии за ночь нет:

```
$ git log --oneline 5dad795..HEAD -- fixtures/check_provodka/ fixtures/done_contract/ \
      fixtures/check_consumers/ fixtures/_krasnye_038.sh
                          # пусто: семьи после круга 1 не тронуты вообще
```

Счёт 37 перемерен МОЕЙ мерой, не повтором раннера: `18` (check_provodka red/green)
`+ 11` (done_contract) `+ 8` (check_consumers) `= 37` — сходится с итоговой строкой
раннера. Счёт 28 перемерен так же: `ls fixtures/check_provodka | grep -c '^case_'` → `28`,
совпадает с отчётом батареи.

## Обязательные машинные меры — все четыре зелены (сырой вывод)

```
$ bash scripts/verify_antiplacebo.sh --scope check_provodka
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_provodka/case_01_policies_r_txt_LEX.sh: зелёный контроль есть, повторный
       прогон красный кодом 1 — «проводка: role-канал обязан ссылаться строго на
       roles/<роль>.md, получено: policies/r.txt»
  …  (28 строк ok: case_02/03 — traversal, case_04/08/09/21/22 — symlink и канон,
      case_14/16/19 — подмена цели после открытия (TOCTOU), case_26 — FIFO,
      case_27 — ERE-метасимволы, case_pole_otsutstvuet — контроль)
барьеров: 1 · фикстур: 28 · предъявлено красным повторным прогоном: 28
rc=0

$ EXPECT_RC=0 bash fixtures/_krasnye_038.sh
check_provodka/red_R1…R16, green_G1…G3       (18 строк rc=0)
done_contract/red_D1…D9, green_D6/D10/G      (11 строк rc=0)
check_consumers/red_C1…C6, green_G1/G2       ( 8 строк rc=0)
итог: 37 файлов, расхождений 0 (режим ожидания rc=0)
rc=0

$ bash scripts/check_contract_frozen.sh .
  ok   contracts/038-provodka-done-gejt.md — заморожен v2, блоб совпадает побайтово,
       вердикты v1..v2 разрешают
планов и контрактов на HEAD: 44 · черновиков: 3 · заморожено: 41 · реестр: full
rc=0

$ bash scripts/check_zones.sh .
  ok   contracts/038-provodka-done-gejt.md — работа не раздаётся: registry/contracts.tsv …
замороженных контрактов: 40 · объявленных авторов: 5 · коммитов в диапазонах: 1855 ·
проверено по зонам: 1242
rc=0
```

Дополнительно, моя мера (не из чужих отчётов):

```
$ bash scripts/check_provodka.sh . contracts/038-provodka-done-gejt.md
rc=0
$ grep -n '^038' registry/contracts.tsv
44:038 → 76065d0a378a6a62df8a88c1ae07f1b604e665bc
$ git rev-parse frozen/contracts/038/2
76065d0a378a6a62df8a88c1ae07f1b604e665bc      # строка реестра = тег-объект v2, сходится
```

## Б1 — БЛОКЕР. Р1 закрыт только в нормативной половине; кодовая половина не тронута

**Класс: находка предыдущего ревьюерского круга, не исполненная и никуда не
переназначенная; самоописание барьера противоречит только что замороженной норме.**

Круг 1 (`verdicts/review/contracts-038-k1.md:142-143`, Р1) закончился требованием,
которое НЕ зависело от выбора владельца между двумя путями, — дословно:

> «В любом случае блок «Контракт API» `check_provodka.sh:106,110` обязан перестать
> описывать несуществующий интерфейс.»

Нормативная половина Р1 закрыта честно и дорого: арбитраж `78785ec`, текст v6, подпись
владельца, accept критика, re-freeze v2. Кодовая половина не сделана:

```
$ sed -n '104,112p' scripts/check_provodka.sh
# Контракт API:
#   вход: $1 = <отн-путь-контракта> (относительно cwd — канон барьеров дерева);
#   rc 0 — проводка зелёная;
#   rc 1 — отказ с ИМЕНОВАННОЙ причиной первой красной строки;
#   rc 2 — нечем проверить: контракта нет / нет git / пустое тело контракта.
#
#   bash scripts/check_provodka.sh <отн-путь-контракта>
#
# Коды возврата: 0 — проводка зелёная, 1 — отказ, 2 — нечем проверить.

$ sed -n '145,147p' scripts/check_provodka.sh
ROOT="${1:?использование: $0 <корень-дерева> <отн-путь-контракта>}"
CONTRACT_PATH="${2:?использование: $0 <корень-дерева> <отн-путь-контракта>}"
ROOT="$(cd "$ROOT" 2>/dev/null && pwd -P)" || skip "корня нет: $ROOT"

$ bash scripts/check_provodka.sh .
scripts/check_provodka.sh: line 146: 2: использование: scripts/check_provodka.sh
<корень-дерева> <отн-путь-контракта>
rc=1

$ git log --oneline 5dad795..HEAD -- scripts/check_provodka.sh
        # пусто: файл не тронут ни одним из 57 коммитов после вердикта круга 1
```

Три отдельные неправды в девяти строках, и каждая — о ПРЕДМЕТЕ этого контракта:

1. `$1 = <отн-путь-контракта>` и строка вызова `check_provodka.sh <отн-путь-контракта>`
   описывают одноаргументный интерфейс, которого нет ни в коде (rc 1 выше), ни в
   замороженном тексте v2 (§Инварианты 3: `check_provodka.sh <корень> <отн-путь-контракта>`).
   Это ровно та неправда, из-за которой ночь стоила двух FAIL критика и арбитража; текст
   исправлен, а в самом барьере она осталась.
2. `rc 2 — … нет git`: ветви не существует. Перемерено мной:
   `grep -nE 'git (show|cat-file|rev-parse|ls-tree|archive|worktree)|GIT_' scripts/check_provodka.sh`
   → три строки, все — env-гигиена (`unset GIT_DIR …`, `export GIT_CONFIG_GLOBAL=/dev/null`).
   Чтения git-объектов нет (совпадает с замером З1 арбитра по 17 ревизиям).
3. Живая ветвь rc 2 `skip "корня нет: $ROOT"` в списке rc 2 НЕ названа — прямое следствие
   второго аргумента, существование которого блок отрицает.

Почему блокер, а не замечание:

- Это НЕ новая находка и не расширение зоны: требование предъявлено вердиктом круга 1,
  никем не оспорено, никем не исполнено и никуда не переназначено — в §Риски контракта
  его нет, в наблюдениях его нет (`grep -c 'Контракт API' NABLIUDENIA.md` → `0`).
- Арбитр `78785ec` (П3) сам сослался на этот блок как на живой остаток: «правка масштаба
  implementer-остатка, того же класса, что архитектор в этой же правке уже оставил
  implementer'у (блок «Контракт API» :104-112)». Арбитраж считал его открытым — закрытия
  не было.
- Приняв сейчас, я хороню находку: после done-тега окно закрывается, а единственный её
  носитель — вердикт круга 1 — перестаёт быть гейтом. Это ровно тот способ, которым в
  этом проекте уже дважды пропадали найденные дефекты.
- Предмет 038 — «норма существует и ПОДКЛЮЧЕНА». Барьер этого предмета документирует
  несуществующий интерфейс: тот же класс, ради ловли которого контракт заведён.

Чем закрывается (форма, не проектирование): строки 105, 108, 110 привести в соответствие
с кодом и §Инварианты 3 v2 — два аргумента (`<корень> <отн-путь-контракта>`), список rc 2
= «корня нет / контракта нет / тело контракта пусто». Зона — implementer
(`contracts/038-provodka-done-gejt.md:364` — ЗОНА implementer, первый путь строки). Замороженный
текст НЕ трогается, v+1 и подпись НЕ требуются. Правка безопасна для раннера и гардов —
проверено, что формат шапки ничем не парсится:

```
$ grep -rn 'Контракт API' scripts/ .githooks/ fixtures/
scripts/judge_gate.sh:8 · scripts/check_contract_ready.sh:7 · scripts/check_spec_ready.sh:10 ·
scripts/done_contract.sh:12 · scripts/check_consumers.sh:21 · scripts/check_provodka.sh:104 ·
scripts/check_threat_model.sh:39      # только сами шапки; потребителя формата нет
```

После правки для подтверждения отсутствия коллатерали достаточно перепрогона
`--scope check_provodka` (28) и `EXPECT_RC=0 bash fixtures/_krasnye_038.sh` (37).

## О1 — остаток: именованная причина реализации вне замороженного списка §Инварианты 3

Моя мера — перечисление ВСЕХ причин барьера против списка контракта:

```
$ grep -oE '(die|skip) "[^"]*"' scripts/check_provodka.sh | sort -u
die "проводка: guard не подключён: $guard не вызывается в .githooks/ или .github/workflows/"
die "проводка: guard-only поле без предметного канала и строки ПРОВОДКА-ЭНФОРСМЕНТ"
die "проводка: guard-файл не существует: $guard"
die "проводка: role-канал обязан ссылаться строго на roles/<роль>.md, получено: $path (резолв: $resolved)"
die "проводка: role-канал обязан ссылаться строго на roles/<роль>.md, получено: $path"
die "проводка: role-файл не существует: $path"
die "проводка: норма-строка не найдена в role-файле: $path"
die "проводка: норма-строка не найдена в теле секции §${header_text}"
die "проводка: поле ПРОВОДКА отсутствует"
die "проводка: поле пусто"
die "проводка: секция устава не найдена: §${header_text}"
die "проводка: строка вне грамматики: $ln"   /   "$orig"
skip "контракта нет: …"  /  "корня нет: …"  /  "тело контракта пусто: …"
```

Все 10 замороженных причин §Инварианты 3 присутствуют дословно. СВЕРХ списка — одна:
«проводка: role-канал обязан ссылаться строго на roles/<роль>.md, получено: …» (+ хвост
«(резолв: …)»). Контракт объявляет себя ЕДИНЫМ источником грамматик («реализация несёт
фразы ПОБАЙТОВО»), и этой фразы в нём нет — но арбитраж
`verdicts/arbitration/contracts-038-role-validacija-podhod.md`, §Семантические границы 3,
записал это прямо в протокол: «этой фразы нет в замороженном списке §Инварианты 3 …
статус закреплён поведением сторон и здесь не судился». Причина УСИЛИВАЕТ гейт (новый
класс отказа, не разрешение) — усиление законно (AGENTS.md:150-152, прецедент 005).
Поэтому остаток, а не блокер: при следующей правке §Инварианты 3 фраза обязана войти
либо в список причин, либо в §Риски именованным остатком — иначе единственный источник
грамматики останется неполным по факту.

## О2 — остаток: ссылки §Инварианты 4 → Н-136 нет

Разобрано в п. 3. Условие подписи владельца («регистрация остатка обязательна»)
исполнено записью `d6b747d`; двусторонность ссылки обеспечена только со стороны
NABLIUDENIA. Адрес будущего майлстоуна однозначен (арбитраж `78785ec`, П3, назван в обоих
документах), поэтому «раствориться» предмету нечем — но при любой следующей редакции
текста 038 ссылка на Н-136 обязана появиться в буллете «ПРОВОДКА-цели на HEAD».

## О3 — наблюдение: ранняя директива владельца по Р1 не исполнена и перекрыта поздней

`HANDOFF.md:188-198` фиксирует дословное решение владельца чекпойнта #11: «ВЫБРОСИТЬ эти
8 [фикстур] … frozen 1-арг текст НЕ трогаем, НЕТ v+1/РАЗРЕШИЛ/ре-критика». Исполнен
ПРОТИВОПОЛОЖНЫЙ путь: 8 `case_*` symlink/traversal/TOCTOU на месте (моя мера: 28
case-файлов, все зелены в прогоне выше), текст изменён, v+1 поставлен, подпись и
ре-критика состоялись. Приоритет — за поздним актом владельца: строка
`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` в `4e0db83` прямо санкционирует амендмент и называет основание
(«арбитр 78785ec + критик-accept»). Противоречия норм нет; устаревшая запись HANDOFF —
не норма. Называю, потому что иначе читатель истории видит неисполненное слово владельца
без следа отмены.

## Что проверено отдельно и ЗЕЛЕНО (чтобы починка была узкой)

- **Область правки.** Амендмент тронул РОВНО один файл (52+/1−). По ночным 038-актам
  (`git log --name-only 5dad795..HEAD`): `4e0db83` — только контракт; `b962a13` и
  `7aa36bf` — только свой вердикт критика; `d6b747d` — только `NABLIUDENIA.md`;
  `73a6808` — только `registry/contracts.tsv`. Превышения границ нет.
- **Атомарность.** Пять ночных 038-актов — пять коммитов, каждый со ссылкой на предмет
  (арбитраж / вердикт / наблюдение / строка реестра). Связок «пять задач в одном
  коммите» в окне нет.
- **Проверка не переписана под реализацию.** Семьи фикстур и раннер после круга 1 не
  тронуты вообще (лог выше пуст) — рецидива Р1-класса (implementer правит байт-пиненный
  вход архитектора) в этом окне нет. `fixtures/done_contract/red_D4_provodka_krasna.sh:25`
  зовёт барьер двухаргументно, и теперь это СОГЛАСОВАНО с замороженным текстом:
  исполняемый свидетель сигнатуры восстановлен исправлением нормы, а не подгонкой пробы.
- **Норма не тронута молча.** `git log 5dad795..HEAD -- AGENTS.md roles/` → пусто. Ни
  устав, ни роли за это окно не менялись; правки нормативного документа внутри задачи о
  механизме нет.
- **Красное предъявлено.** 28 scoped-предъявлений — зелёный контроль плюс повторный
  красный кодом 1 самой батареей, каждая строка с именованной причиной (вывод выше);
  37 предъявлений приёмочной таблицы — раннер, подключённый к CI отдельным шагом.
- **ПРОВОДКА самого 038 — честна (моя прямая обязанность по строке роли).** Три
  guard-канала — барьеры ИМЕННО этого предмета (`check_provodka.sh`, `done_contract.sh`,
  `check_consumers.sh`), не «любой вызванный скрипт дерева»; подключение живое —
  `ci.yml:63` (шардовые ключи) и `ci.yml:173` (шаг раннера семьи). Три role-канала несут
  норма-строки дословно, charter-канал — `AGENTS.md §Воркфлоу майлстоуна`. Поведенческая
  норма (029-класс) идёт role-каналом; guard-only со строкой ПРОВОДКА-ЭНФОРСМЕНТ здесь
  не заявлен и не требуется. Барьер на самоприменении — rc 0.
- **Семантика исполнения П1–П3 — по целям, а не по машине.** П1: §Инварианты 3 называет
  механизм честно (чтение с диска по канонизированному корню), ложная пометка «корень =
  HEAD-дерево» удалена — коррекция, не ослабление (сверено мной по коду: git-объекты
  барьером не читаются). П2: фраза «гарантию несёт ВЫЗЫВАЮЩИЙ» в нормативной части
  отсутствует — `grep -c 'несёт ВЫЗЫВАЮЩИЙ'` → `1`, единственное вхождение в changelog
  (:21) под явным «НЕ вводится»; пробел назван честно («СЕЙЧАС НЕ ЗАКРЫТА НИКЕМ»). П3:
  буллет «ПРОВОДКА-цели на HEAD» в §Инварианты 4 называет носитель
  (`done_contract.sh:63-67`), обе категории целей (`roles/<роль>.md` и `AGENTS.md`),
  отказ без тега и различающий вход будущей приёмки, помечен «ЕЩЁ НЕ РЕАЛИЗОВАНО».
  Разрыв живёт как названный красный остаток, не как норма — требование арбитража
  исполнено по содержанию, а не по форме.

## Маршрут

Б1 — зона implementer: один файл, комментарий, без v+1, без подписи владельца, без
ре-критика. О1 и О2 адресуются следующей редакции текста 038 (не работа этого круга),
О3 — запись истории.

Спора, в котором судья стал стороной, здесь нет: блокер предъявлен исполняемым прогоном
(`bash scripts/check_provodka.sh .` → rc 1 против строки 110 собственной шапки) и пустым
логом файла после круга 1, а не мнением. Контракт, исходники, фикстуры и теги ревьюером
не изменены; freeze/done не запускались; push не выполнялся.
