FAIL

# Контракт 020 (шардирование CI «Анти-плацебо») — ревьюер, круг к1

Предмет судился на СОБСТВЕННОМ клоне `/tmp/dev-harness-verify/rev-020/repo`,
HEAD `55be77b1a6034f2a14865ed398e70760c7f60636`. Блобы предмета (Н-101):

| артефакт | блоб |
|---|---|
| `contracts/020-shardirovanie-ci-antiplatsebo.md` (заморожен `frozen/contracts/020/1`) | `7311f32b69e59319ec91ee964ee7be41e3feca39` |
| `.github/workflows/ci.yml` | `bf6749ec8c655dc387ebbb3a63c1e5b0977b1943` |
| `scripts/verify_ci_parity.sh` | `ad2e3e55136d6753ca012939f0f1e4450dbad2e2` |
| `config/ci_parity_exceptions.txt` | `fc5e6dff6ae87b1ce54b42b95af9224028d99812` |

Вердикт связан ИМЕННО с этими блобами: новая редакция любого из них принятия не наследует.
Прочитаны: замороженный контракт, вердикты критика v1, адверсария к1/к2/к3, арбитраж
`verdicts/arbitration/contracts-020-iskljuchitelnost.md`, все восемь коммитов пачки,
реализация (`ci.yml`, `verify_ci_parity.sh`), 26 проб `fixtures/verify_ci_parity/`.
Раскрытия приняты: CI green на 55be77b; Н-98 (самотрип детектора утечек) — миграция после
done×4, не трогалась.

## Мои прогоны (сырые rc из собственного клона)

| # | команда | rc | сводка |
|---|---|---|---|
| 1 | `bash scripts/verify_ci_parity.sh` | **0** | `workflow-команд: 29 · скриптов в приёмке: 46 · объявленных исключений: 17 · matrix-ключей: 35 · анти-плацебо-запусков: 1 · расхождений: 0` |
| 2 | `bash scripts/verify_antiplacebo.sh --scope verify_ci_parity` | **0** | `барьеров: 1 · фикстур: 26 · предъявлено красным повторным прогоном: 26` (32,1 с) |
| 3 | `git diff frozen/contracts/020/1 -- contracts/020-shardirovanie-ci-antiplatsebo.md` | **0** | пусто — текст контракта после заморозки не правился |
| 4 | `git diff frozen/contracts/020/1 -- scripts/verify_antiplacebo.sh` | **0** | пусто — не-цель «раннер не трогается» соблюдена |
| 5 | ap1: `npm run check:antiplacebo -- --scope check_runner_hygiene drill_startup_digest check_ci_gate gc_agent_branches check_no_rewrite render_document next_id` | **0** | `фикстур: 72 · предъявлено красным: 72` (336 с) |
| 6 | ap2: `… --scope check_scope_select freeze_contract check_skills land_agent check_decisions check_check_contract_ready drill_nabludenia_nechitaemo overlay` | **0** | `барьеров: 8 · фикстур: 72 · предъявлено красным: 72` (92,6 с, повторный прогон — см. З3) |
| 7 | ap3: `… --scope check_staged check_zones check_nabludenia check_scoped_run check_approval check_judge_gate spawn_agent` | **0** | `фикстур: 71 · предъявлено красным: 71` (140 с) |
| 8 | ap4: `… --scope check_protected check_hooks check_charter drill_gate_draft check_document drill_protected_exception` | **0** | `фикстур: 46 · предъявлено красным: 46` (76 с) |
| 9 | ap5: `… --scope verify_ci_parity check_ids check_ceilings check_contract_frozen drill_next_id_race drill_contract_change models_actual` | **0** | `фикстур: 53 · предъявлено красным: 53` (70 с) |
| 10 | тот же scoped-прогон 26 проб против барьера ДО фиксов к1/к2 (`git checkout ece5eaa -- scripts/verify_ci_parity.sh`) | **1** | `предъявлено красным: 22` из 26 — отказали РОВНО четыре пробы, добавленные автором реализации |

Каждый шард гонялся в ОТДЕЛЬНОЙ копии дерева со своим `VERIFY_ANTIPLACEBO_SCRATCH` —
как в CI (отдельный чекаут на джобу). Метеринг в прогоны не входит (исключение задания).

Собственные меры счётных утверждений (другой командой, не повтором авторской):

* каталогов фикстур с `case_*.sh` — **38**; всего `case_*.sh` — **361** (`find`);
* ключей в `matrix.include` — **35** в **5** шардах (разбор `ci.yml` на python3);
* case в шардах: ap1 72, ap2 72, ap3 71, ap4 **46**, ap5 **53** — **сумма 314**;
* вне шардов (`UNSCOPABLE_KEYS`): `verify_antiplacebo` 23, `check_metering` 22,
  `gen-harness` 2 — **47**. 314 + 47 = 361 ✓ (баланс сходится, но см. Б1);
* проб в `fixtures/verify_ci_parity/` — **26** (заявленное совпадает).

## Б1 — БЛОКЕР: замороженный инвариант 3 ослаблен в барьере; для двух ключей из трёх ослабление не записано и компенсации нет

Замороженный текст (блоб `7311f32`) говорит дословно: «ни одна `fixtures/<барьер>/case_*.sh`
не выпадает из всех шардов» (инвариант 3) и «каждый пуш прогоняет ВСЕ фикстуры статическими
шардами» (§Предмет п.3). Реализация вводит именованное изъятие
`UNSCOPABLE_KEYS=(verify_antiplacebo gen-harness check_metering)`
(`scripts/verify_ci_parity.sh:1140`), и обе стороны сумма-инварианта их пропускают
(`[ -n "${IS_UNSCOPABLE[$k]:-}" ] && continue`).

Наблюдаемое следствие, замеренное мной: в CI **ровно один** запуск анти-плацебо
(прогон 1: `анти-плацебо-запусков: 1`), и он шардный; полного прогона в `.github/**` нет
ни одного. Значит **47 из 361 `case_*.sh` (13 %) не гейтятся в CI ничем** — включая
23 фикстуры `verify_antiplacebo`, то есть красные ветви САМОГО раннера, на котором стоит
весь анти-плацебо-контур.

Разбор по ключам:

* `check_metering` — **претензии нет**: расхождение закрыто по существу словом владельца
  2026-09-17 (вариант «в») и записано наблюдением **Н-103** с причиной, компенсацией
  (ручной полный прогон перед done×4, cognitive-only) и адресом корневого фикса. Это
  образец того, как ослабление нормы оформляется не молча.
* `verify_antiplacebo` (23 case) и `gen-harness` (2 case) — **дефект**. Само изъятие
  вынужденно технически, и это я замерил: `bash scripts/verify_antiplacebo.sh --scope
  gen-harness` → **rc=1** «scope_select отказал (код 1)», то же для `--scope
  verify_antiplacebo`; включение их в `keys:` ломало бы шард целиком. Но:
  1. компенсация для них НЕ названа. Для `gen-harness` в шапке барьера стоит
     «full-прогон подхватывает `.ts`-барьер в общем цикле по `scripts/`» — это свойство
     режима, которого в CI после чистого cutover НЕ существует (прогон 1). Для
     `verify_antiplacebo` не названо ничего;
  2. наблюдения с адресом (как Н-103 для метеринга) нет ни одного — мой поиск по
     `NABLIUDENIA.md`/`NABLIUDENIA_ARCHITECT.md` даёт только Н-103, и он про метеринг;
  3. норма при этом не правилась (прогон 3: frozen-diff пуст) — то есть замороженный
     инвариант и поставленный механизм расходятся, и расхождение живёт ТОЛЬКО в
     комментарии кода, написанном автором реализации.

Класс: **ослабление замороженного инварианта автором реализации внутри барьера, который
этот инвариант и стережёт** — то же, что «проверка, подогнанная под код», в самой
чувствительной точке. Ни один судья по 020 этого не рассматривал: критик замораживал текст
ДО появления `UNSCOPABLE_KEYS` (введён `5005cae`/`a459784` после land), адверсарий к1–к3 и
арбитраж судили инвариант 4 (исключительность), не инвариант 3.

Что требуется (механизм не проектирую — называю обязательство): либо покрытие этих 25 case
в CI восстановлено, либо расхождение оформлено ТЕМ ЖЕ способом, что метеринг — наблюдение с
адресом плюс слово владельца, — а фраза про «full-прогон» в шапке заменена на фактическую
(в CI полного прогона нет). До этого зелёный `check:ci-parity` утверждает полноту, которой
в дереве нет.

## З1 — замечание: residual-строка указывает на несуществующий путь

`scripts/verify_ci_parity.sh:93` ссылается на `verdicts/arbitration/020-iskljuchitelnost.md`.
Такого файла в дереве нет: он переименован в `contracts-020-iskljuchitelnost.md`
(`b4522bf`, финализация `55be77b`). Вина не автора residual: коммит `eaf629e` (17.09 23:37)
старше переименования, а текст внесён ДОСЛОВНО — сверено мной посимвольно (python3,
нормализация пробелов: «дословно совпадает: True»); ту же мёртвую ссылку несёт и сам текст
арбитража. Но residual — механизм cognitive-only, и его единственная ценность — довести
читателя до решения; битый путь эту ценность обнуляет. Правится одной строкой; при
переименовании артефакта citers обязаны были обновиться тем же коммитом.

## З2 — замечание: ссылки на несуществующие редакции контракта

`.github/workflows/ci.yml:47-50` и коммиты `cdd2506`/`a459784` ссылаются на «контракт 020
v3.1» и «контракт 020 v3.2». Таких редакций нет: блоб контракта не менялся с `7d632e8`
(прогон 3), в дереве лежит DRAFT v3 без разделов v3.1/v3.2. Фактическое основание решения —
Н-103 и слово владельца 2026-09-17; ссылаться надо на них. Класс: заявленное ≠ сделанному
(ссылка, по которой читатель не найдёт ничего).

## З3 — замечание (вне предмета 020, но по классу Н-103): второй зависающий класс фикстур

Первый прогон ap2 в точной CI-форме (одна команда, все восемь ключей) **завис на 29+ мин**
и был снят мной; зависание — в `check_decisions/case_poluok_net_zapisi` (зелёный контроль
уже отпечатался: «записей: 7 · нарушений: 0»), процесс барьера стоял в
`wchan: wait_for_partner` на FIFO `req` протокола раннера. Тот же набор ключей, разбитый на
две команды, прошёл зелёным за 74 с + 10 с; повторный прогон одной командой без конкуренции —
зелёный за 92,6 с (прогон 6). Воспроизводимость: 1 из 2, при пяти параллельных шардах на
одной машине. Предмет 020 (`ci.yml`, `verify_ci_parity.sh`) тут ни при чём — течь в
раннере/протоколе, — но класс ровно тот, что уже стоил трёх CI-кругов по Н-103: зависший
case съедает `timeout-minutes: 20` целиком и отменяет шард. Называю как измеренный факт для
очереди де-флейка, не как дефект пачки.

## Что проверено и оказалось в порядке

* **Область правки.** Все восемь коммитов пачки (`1377db5`, `ece5eaa`, `5005cae`, `cdd2506`,
  `a459784`, `6eee2a5`, `3bd6cf6`, `eaf629e`) трогают только
  `.github/workflows/ci.yml`, `scripts/verify_ci_parity.sh`,
  `fixtures/verify_ci_parity/` — внутри `ЗОНА implementer`. Превышения нет.
* **Атомарность и порядок пачки.** `1377db5` — только `concurrency`, ПЕРВЫМ (§Порядок
  пачки п.1), `ece5eaa` — matrix + удаление `--changed`-шага + ветви полноты и
  исключительности одним коммитом (п.2). Каждый коммит называет предмет и контракт.
* **Проверка не переписана под реализацию.** Четыре из 26 проб добавлены автором
  реализации (`6eee2a5`, `3bd6cf6`) — зона это разрешает, но различающую силу я предъявил
  СВОИМ прогоном, а не отчётом автора: против барьера ДО фиксов (прогон 10) отказали ровно
  эти четыре — `case_shard_legacy_shag_psevdonim` («барьер остался зелёным на обманном
  дереве — красное не предъявлено») и три к2-пробы («покраснел, но не назвал причину»).
  Остальные 22 остались зелёными — значит пробы бьют в предмет фикса, а не в фон.
* **Красное предъявлено.** Каждая из 26 проб даёт зелёный контроль и красное повторным
  прогоном с дословной причиной (прогон 2); байтовые ядра сообщений §Приёмки совпадают с
  наблюдаемыми («не покрыт шардингом — ключ не назван ни одним шардом», «приписан двум
  шардам: ap1, ap2», «мёртвый ключ», «запуск анти-плацебо вне шардной matrix …»).
* **Грамматика §Предмет.** В `ci.yml`: `fail-fast: false`, `timeout-minutes: 20`,
  `fetch-depth: 0`, node 26, `concurrency: group: ci-${{ github.ref }}` +
  `cancel-in-progress: true`, имя скрипта статично, шаблон только в `--scope`. Прежний
  `--changed`-шаг удалён, `package.json` не расширялся.
* **Достаточность именования residual (заданный вопрос).** По содержанию — **достаточно**:
  названы все три формы к3 поимённо, названа четвёртая (находка арбитра) с измеренной
  границей вреда, названа цена каждой (дословная запись `команда:` в файле исключений) и
  названа операция ревьюера. Я эту операцию исполнил (план закрытия арбитража, п.3): поиск
  строк `команда:` в `config/ci_parity_exceptions.txt` → **0 совпадений**; записей класса
  красного флага нет вовсе, 17 объявленных исключений — по именам скриптов, с причинами,
  салата среди них не вижу. Единственный дефект именования — мёртвый путь (З1).

## Границы этого суда

Живой CI я не гонял (раскрытие green 55be77b принято); полный прогон всех 38 каталогов не
делал — гонял ровно пять шардных команд и scoped-пачку предмета; метеринг исключён заданием;
Н-98 не трогал. Слабости раннера вне ключей 020 не судил (см. З3). Чужой код не правил —
только называю.

## Итог

**FAIL** по Б1. З1 и З2 — замечания, закрываются одной строкой каждое; З3 — измеренный факт
для очереди, круга не открывает. Механизм шардирования как таковой работает и проверен:
пять шардов зелены, 26 проб красны против сломанных деревьев, паритет и заморозки чисты.
Отказ ровно об одном: механизм объявляет полноту, которой в дереве нет, и делает это
изъятием, которое для двух ключей из трёх никем, кроме автора реализации, не записано.

— reviewer, 2026-09-18, клон HEAD 55be77b
