FAIL

# Ревью контракта 027 — круг к3 (проверка фикса блокера F5)

Предмет круга: фикс `b4f40f3` (land `cf0d8d9`) против блокера F5 вердикта
`verdicts/review/contracts-027-k2.md` (`8bd4475`). Судимый HEAD —
`cf0d8d984439d99ab5970842eef887f9c99b0df2`; клон собственной рукой —
`/tmp/dev-harness-verify/rev-027/repo2` (свежий `git clone` основного дерева).
Все rc ниже получены в этом клоне.

Привязка блобами (Н-101; новая редакция не наследует принятие старого блоба):

| файл | блоб на HEAD | было (к2) |
|---|---|---|
| `scripts/doc_contract.ts` | `4e11da682a` | `70713ed801` |
| `scripts/check_contract_ready.sh` | `e8b281aaff` | `a92090170a` |
| `scripts/freeze_contract.sh` | `67f302e971` | `e2d8b902fd` |
| `contracts/027-doc-priemka.md` | `5d3f9f31aa` | без изменений |

Область правки: `b4f40f3` трогает три файла, все в ЗОНА implementer; барьеры,
фикстуры и каркас `_doc027.py` не тронуты; замороженный текст не тронут
(`git diff frozen/contracts/027/3 HEAD -- contracts/027-doc-priemka.md` → rc=0,
0 байт).

## F5 — ЗАКРЫТ (предъявлено моим контрпримером)

`parseSpecFromMarkdown` (`doc_contract.ts:89-139`) теперь считает разделы по
всему markdown и fenced-блоки внутри раздела, `detectDocTypeFromFile`
(`:823-844`) различает «нет раздела» (rc=1) и «мальформный doc-контракт» (rc=2).
Мои контрпримеры из к2 (`probe_round3.py`, игрушка та же, что в фикстуре
`lifecycle`):

```
[CTRL honest 1+1  ] --type rc=0 | ready rc=0 | freeze rc=0 | frozen-теги: frozen/contracts/001/1
[D    good+bad    ] --type rc=2 | ready rc=1 | freeze rc=2 | frozen-теги: (нет)
[E    bad+good    ] --type rc=2 | ready rc=1 | freeze rc=2 | frozen-теги: (нет)
[F    two-sections] --type rc=2 | ready rc=1 | freeze rc=2 | frozen-теги: (нет)
[A    split/empty ] --type rc=0 | ready rc=1 | freeze rc=1 | frozen-теги: (нет)
```

D/E/F — входы, которые в к2 замораживались с rc=0: теперь ready отказывает
именованно («неоднозначный блок: fenced-блоков в разделе «## Док-приёмка» = 2
(требуется ровно 1)», «разделов «## Док-приёмка» = 2»), тег не пишется, и
результат больше не зависит от позиции блока. Строка A подтверждает, что фикс
F1 не регрессировал. Положительный контроль CTRL зелёный.

## F8 — НОВЫЙ БЛОКЕР. `freeze_contract.sh` больше не замораживает НИ ОДИН не-doc контракт: молчаливый rc=1 без причины, ветвление по RC мертво

Обязательства: §Freeze — «Старые кодовые контракты продолжают прежний путь»;
§Док-пакет и публичные команды — «1 — именованное нарушение»; шапка самого
скрипта (`freeze_contract.sh:34`) — «0 — заморожено, 1 — отказ, 2 — нечем
проверить».

Точный фрагмент результата (`freeze_contract.sh`, блоб `67f302e971`):
`:35` включает `set -euo pipefail`; `:273` присваивает
`_doc_type_out="$(cd "$ROOT" && node "$SELF_DIR/doc_contract.ts" --type … 2>&1)"`,
а `:274` — `_doc_type_rc=$?`. Под `set -e` отказ подстановки в ПРОСТОМ
присваивании завершает скрипт на `:273`, поэтому `:274` и весь `case`
(`:277-293`: ветви rc=1 «пропустить», rc=2 «die мальформный», rc=3 «usage/IO»)
недостижимы. Ровно от этой ловушки предостерегал комментарий прежней редакции
(«`set -e` трактует отказ команды в `$(...)` как отказ присваивания и ВЫХОДИТ»);
фикс убрал `|| true` и не заменил его захватом rc. В `check_contract_ready.sh`
(`:114-116`) тот же код безопасен — там `set -e` не включён
(`grep -c 'set -e'` = 0), поэтому ready ведёт себя правильно, и дефект виден
только на freeze.

Мои измерения:

1. Кодовый контракт БЕЗ раздела «## Док-приёмка» (`probe_round3d.py`):
   `--type rc=1 :: нет раздела «## Док-приёмка»`, далее
   `freeze rc=1`, `STDOUT: ''`, `STDERR: ''`, `теги: (нет)`.
   Отказ без причины на входе, который обязан замораживаться.
2. Мера самого проекта, тот же клон, та же команда, что в к2:
   `bash scripts/verify_antiplacebo.sh --scope check_document render_document
   freeze_contract check_check_contract_ready` → **rc=1**,
   `барьеров: 4 · фикстур: 22 · предъявлено красным повторным прогоном: 8 ·
   расхождений: 14`. Все 14 расхождений — фикстуры `freeze_contract/case_*.sh`
   с одним диагнозом: «нет положительного контроля — фикстура обязана
   предъявить барьер ЗЕЛЁНЫМ до порчи». В круге к2 на `d9ceff1` эта же команда
   в моём клоне давала rc=0 и 22/22. Красное предъявлено против фикса, зелёное
   — против его предшественника; полный лог оставлен в
   `/tmp/dev-harness-verify/rev-027/out_ap.log`.
3. Побочное следствие того же дефекта: на мальформном doc-контракте freeze
   выходит с rc=**2** (кодом CLI), а не с предписанным rc=1 (§Freeze: «rc=2 —
   rc=1 (fail-closed)»); строки D/E/F выше. Направление fail-closed, тег не
   пишется, но код и причина не те, что объявлены.

Приёмочная строка контракта `bash scripts/verify_antiplacebo.sh --scope … → rc=0`
на этом HEAD НЕ выполняется. Это блокер сам по себе, независимо от F9.

## F9 — БЛОКЕР того же класса, что F5: раздел есть, блок — валидный JSON, но не объект → гейт молча пропущен

Обязательство: §Грамматика — «неверные типы полей и неоднозначный блок —
именованный отказ rc=1». Собственное правило фикса (комментарий
`doc_contract.ts:805-822`): «раздел есть → любая ошибка parse = (2) мальформный
doc-контракт».

Фрагмент результата: `doc_contract.ts:841` —
`if (spec == null || typeof spec !== 'object' || Array.isArray(spec)) return 1`
— возвращает «НЕ doc-контракт» БЕЗ учёта `hasSection` (`:833`), в обход
собственного правила восемью строками выше.

Измерено (`probe_round3.py`, `probe_round3b.py`): контракт с разделом
«## Док-приёмка» и единственным fenced json-блоком `[]` либо `"ёж"`:

```
[G array-block ] --type rc=1 | ready rc=0 (OK) | freeze rc=1 (молча, см. F8)
[H string-block] --type rc=1 | ready rc=0 (OK) | freeze rc=1 (молча, см. F8)
```

`ready` объявляет такой контракт готовым, doc-preflight не запускается —
именованного отказа, которого требует грамматика, нет. Честный контроль
(единственный конформный блок-объект) в том же прогоне даёт rc=0 законно.

Класс совпадает с F5 («мальформный doc-контракт минует гейт»), причина — иная
строка кода. Отмечаю прямо: это мой ВТОРОЙ отказ по этому классу. По правилу
«два отказа по одной причине — созывается арбитр» спор о достаточности правки
этого класса я решать не вправе. F8 к этому спору не относится: он предъявлен
мерой самого проекта.

## Собственные прогоны на фиксе (сырые коды)

```
rc=0 fixtures/check_check_contract_ready/red_doc_obligations.sh
rc=0 fixtures/check_check_contract_ready/red_doc_product.sh
rc=0 fixtures/check_check_contract_ready/red_doc_architecture.sh
rc=0 fixtures/check_check_contract_ready/red_doc_evidence.sh
rc=0 fixtures/check_check_contract_ready/red_doc_assertions.sh
rc=0 fixtures/check_check_contract_ready/red_doc_status_render.sh
rc=0 fixtures/check_check_contract_ready/red_doc_oracle.sh
rc=0 fixtures/freeze_contract/red_doc_lifecycle.sh
rc=0 python3 fixtures/check_check_contract_ready/_doc027.py regressions
rc=0 npm run check:document
rc=0 npm run check:contract-ready
rc=0 npm run check:ci-parity
rc=1 scoped antiplacebo (4 барьера, 22 фикстуры, красным 8, расхождений 14)  ← F8
rc=0 git diff frozen/contracts/027/3 HEAD -- contracts/027-doc-priemka.md (0 байт)
```

Наблюдение о покрытии: все восемь `red_doc_*` и `_doc027.py regressions`
остались зелёными при сломанном freeze для кодовых контрактов — они судят
doc-ветвь и не содержат положительного контроля «кодовый контракт
замораживается». Красное предъявил только `verify_antiplacebo`; приёмочная
батарея контракта 027 в одиночку этот регресс не ловит.

## Перенесённые находки (не блокеры, состояние не изменилось)

- **F2** — «нечем проверить» в `resolveGitSource` (`doc_contract.ts:510-512`)
  даёт rc=1 с именем «дрейфует» вместо rc=2.
- **F3** — residual argv назван только в комментарии кода; адресат (ревьюер при
  freeze) его не получает.
- **F4** — residual по регистру OID без носителя; комментарий `:29-32` объявляет
  «40/64 hex» при lower-case-регулярке.
- **F6** — `mktemp`-guard в freeze восстановлен этим фиксом (`:268-272`); ветвь
  по-прежнему пропускала бы doc-гейт при отказе `mktemp`, но недостижима
  (раньше падает `:169`).

## Граница вердикта

F5 закрыт по собственному измерению; F8 — новый дефект, внесённый судимой
дельтой и предъявленный мерой самого проекта; F9 — второй отказ по классу F5,
и стороной этого спора я быть не могу. Чужой код не правил: находки названы,
не исправлены. Полный CI, содержательный суд конкретного doc-пакета и предметы
020/026/028 этим вердиктом не закрываются.

Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза контракта;
контракт несёт инварианты + rc-команды».

Н-101: блоб этого текста записан `git hash-object -w` и добавлен в индекс
`update-index --cacheinfo`; identity коммита задана явно как `reviewer`.
Н-73: использована нейтральная терминология контрольных экспериментов и
контрпримеров.
