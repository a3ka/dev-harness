FAIL

# Контракт 020 — ревьюер, круг к2 (по фиксу `af6a8e7`)

Судился фикс `af6a8e7` («020 k1 fix: именованные обоснования UNSCOPABLE_KEYS + ремонт
мёртвой ссылки З1») на СОБСТВЕННОМ клоне `/tmp/dev-harness-verify/rev-020b/repo`,
HEAD `cf0d8d984439d99ab5970842eef887f9c99b0df2`. Блобы предмета (Н-101):

| артефакт | блоб |
|---|---|
| `scripts/verify_ci_parity.sh` | `4e89c3a775da37bbed6036a03ee9d2c0aef9332c` (был `ad2e3e5`) |
| `.github/workflows/ci.yml` | `bf6749ec8c655dc387ebbb3a63c1e5b0977b1943` (не менялся) |

Вердикт к1 (`f532496`) нёс блокер Б1 и замечания З1–З3. Фикс кода не менял — только
комментарий `UNSCOPABLE_KEYS` и одну строку residual.

## Итог по каждой находке к1

| находка | состояние | чем измерено |
|---|---|---|
| **Б1** / `check_metering` | закрыт и в к1 (Н-103 + слово владельца) | — |
| **Б1** / `gen-harness` (2 case) | **ЗАКРЫТ** | мой замер, ниже |
| **Б1** / `verify_antiplacebo` (23 case) | **ОТКРЫТ; компенсация заявлена ЛОЖНО** | мой замер, ниже |
| **З1** мёртвая ссылка residual | **ЗАКРЫТ** | путь `verdicts/arbitration/contracts-020-iskljuchitelnost.md` в шапке, 1 вхождение |
| **З2** ссылки на «контракт 020 v3.1/v3.2» | **ОТКРЫТ** (не заявлялся закрытым) | `ci.yml:40,44,45` — редакций нет, блоб контракта не менялся |
| **З3** зависон раннера (класс Н-103) | вне предмета, остаётся в очереди | — |

Контрольные прогоны на новом HEAD: `bash scripts/verify_ci_parity.sh` → **rc=0**
(`workflow-команд: 29 · matrix-ключей: 35 · анти-плацебо-запусков: 1 · расхождений: 0`);
`bash scripts/verify_antiplacebo.sh --scope verify_ci_parity` → **rc=0**, 26/26.

## `gen-harness` — компенсация проверена и ПРИНЯТА

Заявлено: «покрытие обеспечено шагом „Генератор ролей под чек“ (`npm run check:gen`)».
Я проверил не чтением, а исполнением — по обоим классам, которые стерегут две фикстуры:

```
npm run check:gen                              → rc=0  «харнес соответствует roles/ (8 ролей)»
rm .omp/agents/reviewer.md; npm run check:gen   → rc=1  «Перегенерируйте: …»   (класс case_agent_poterjan)
+ .omp/agents/rogue.md без маркера; check:gen    → rc=1  «Перегенерируйте: …»   (класс case_rogue_bez_markera)
```

Оба дефектных класса гейтятся в CI на каждом пуше отдельным шагом. Уточнение к
формулировке (не находка): гейтится КЛАСС дефекта на боевом дереве, а сами фикстуры не
исполняются — способность барьера отказать в CI не предъявляется. Для двух case с живым
зелёным и живым красным контролем это приемлемая цена, и она теперь названа.

## Б1 остаётся: «покрытие обеспечено исполнителем каждого шарда» — измеренно неверно

Внесённый текст утверждает: «scoped-прогоны гоняют раннер НА КАЖДОМ шарде …, и раннер сам
исполняет свои же 23 case фикстуры через FIFO-протокол — сам-тест покрыт косвенно на каждом
пуше, отдельный шаг для этого не нужен».

Раннер на каждом шарде действительно ИСПОЛНЯЕТСЯ. Но исполнение раннера не есть исполнение
его 23 сам-тестов: шардный прогон гоняет фикстуры ТОЛЬКО своих ключей. Замер — прогон
одного ключа: `--scope check_hooks` → `барьеров: 1 · фикстур: 11`; ни одной
`fixtures/verify_antiplacebo/case_*.sh` в прогоне нет, и быть не может: ключ отвергается
`scope_select` (замер к1: rc=1 «scope_select отказал (код 1)»).

Разница между «исполняется» и «проверен» предъявлена саботажем — ровно тем методом, который
сам барьер требует от других. В копии дерева я отключил ОДНУ ветвь раннера, которую стерегут
его сам-тесты (`verify_antiplacebo.sh:694`, «барьер остался зелёным на обманном дереве —
красное не предъявлено»):

```
шард ap4 на САБОТИРОВАННОМ раннере:
  npm run check:antiplacebo -- --scope check_protected check_hooks check_charter \
      drill_gate_draft check_document drill_protected_exception
  → rc=0   «барьеров: 6 · фикстур: 46 · предъявлено красным повторным прогоном: 46»
  bash scripts/verify_ci_parity.sh → rc=0   «расхождений: 0»
```

То есть CI полностью зелен на раннере, у которого отключена несущая ветвь анти-плацебо.
Что эта ветвь стоит — проверено выбрасываемой пробой (фикстура, оставляющая барьер зелёным
на обоих прогонах; удалена после замера):

```
честный раннер        → rc=1  «FAIL … барьер остался зелёным на обманном дереве — красное не предъявлено»
саботированный раннер → rc=0  «ok … САБОТАЖ РЕВЬЮЕРА — ветвь отключена»
```

Итог замера: отключение ветви меняет вердикт раннера с красного на зелёный, ловят это
ТОЛЬКО `fixtures/verify_antiplacebo/case_*.sh`, и ни один шард их не гоняет. Заявленная
компенсация не существует; 23 сам-теста самого нагруженного механизма репозитория не
гейтятся в CI ничем, как и в круге к1.

Класс находки хуже, чем в к1: там ослабление было НЕ ЗАПИСАНО, здесь оно записано
утверждением, которое не выдерживает исполнения. Пункт «full-прогон отсутствует по дизайну
(Н-48)» при этом верен и полезен — отсутствует именно компенсация, а не её описание.

## Формально: второй отказ по одной причине → арбитраж

Это мой ВТОРОЙ FAIL по одному предмету и одной причине (Б1, ключ `verify_antiplacebo`).
По моей рамке спор дальше решаю не я: **созывается арбитр**. Сторонам он получает от меня
ровно то, что измерено выше, и три развилки, названные без выбора между ними:

1. вернуть в CI прогон, исполняющий `fixtures/verify_antiplacebo/case_*.sh` (цена — шаг
   `run:` и минуты стены; правило 6 закрывается существующим пунктом);
2. признать прореху остаточным риском ПО ОБРАЗЦУ метеринга — наблюдение с адресом плюс
   слово владельца плюс ЧЕСТНО названная компенсация (например, ручной прогон перед done×4),
   вместо утверждения о косвенном покрытии;
3. открыть предмет «`scope_select` умеет ключ сам-раннера» (зона контракта 006 заморожена) —
   тогда ключ едет в шард на общих основаниях.

Замороженный инвариант 3 при любом исходе обязан либо выполняться, либо иметь записанное
исключение: сегодня `check:ci-parity` зелёным утверждает полноту, которой в дереве нет.

## Границы

Живой CI не гонял; метеринг исключён заданием; чужой код не правил — саботаж и проба жили в
выбрасываемых копиях `/tmp/dev-harness-verify/rev-020b/*` и удалены. Предмет 027 в этом
клоне не судил.

— reviewer, 2026-09-18, клон HEAD cf0d8d9
