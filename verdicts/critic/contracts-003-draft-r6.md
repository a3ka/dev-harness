FAIL

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:113-126,181 — вторая строка с первой колонки, начинающаяся `СПАСЕНО `, не является объявлением по заданной грамматике: в `СПАСЕНО дополнен хешем 09b2e6af (гонка общего индекса при переиздании вердиктов:` механизм разбирает автором текст до двоеточия. После локальной имитации заморозки v5 на судимом HEAD команда `bash scripts/check_zones.sh` вернула 1: `автор «дополнен хешем 09b2e6af (гонка общего индекса при переиздании вердиктов» не объявлен ЗОНА-строкой этого контракта`. Это ровно предписанный строками 113–116 отказ для объявления вне грамматики, но строка 181 одновременно требует от `check:zones` код 0. Поэтому заявленное состояние готовности на этом тексте недостижимо честной реализацией объявленной грамматики.
ОБХОД: оставить контракт как есть, но изменить `scripts/check_zones.sh`, чтобы он молча отбрасывал начинающиеся с первой колонки `СПАСЕНО ` строки с необъявленным автором. Тогда после заморозки v5 `check:zones` вернёт 0 и третий хеш будет принят, хотя обещание строк 113–116 «вне грамматики — код 1 с названной причиной» не выполнено и невалидное объявление проходит молча.

Проверены пять обязательных вопросов на HEAD `c9990a2b7e0665bfe4b6a7681510b15f5f667734`:

1. Критерий готовности слабее предмета только через названный обход: зелёный `check:zones` можно получить, ослабив разбор невалидных `СПАСЕНО`-строк, не исполнив объявленную грамматику. Остальные требования замороженной v4 этой минимальной правкой не ослаблены.
2. `check:zones` назван кодовой командой, но на тексте v5 не может дать требуемый 0 одновременно с правилом «вне грамматики — код 1». Проба в отдельном клоне: локальный тег `frozen/contracts/003/5` на судимом HEAD, затем `bash scripts/check_zones.sh` → 1 по строке 126. Без тега v5 тот же барьер ожидаемо красит шесть сторон трёх переименований коммита `09b2e6af`; с тегом сам коммит печатается как `СПАСЕНО`, а единственным отказом остаётся ложное объявление из строки 126.
3. Решения о полноте хеш-исключения и отрицательной мере названы; исполнительного пробела там нет. Дефект иной: история правки синтаксически попала в уже полностью определённый вход механизма, поэтому текст требует несовместимых кодов возврата.
4. Исполнители и непересекающиеся границы названы `ЗОНА`-строками 102–103. Файлы исходной работы распределены; `verdicts/` намеренно не расширяют зону architect. Третий хеш — конечное исключение, а не новая зона.
5. Новых противоречий норме `AGENTS.md` кроме названного обхода механизма не найдено: команды дают коды возврата, отрицательная фикстура предъявлена, область и исполнители названы. `npm run check:antiplacebo` → 0, 130/130 фикстур предъявлены красным повторным прогоном.

Отдельно проверен третий хеш: `09b2e6afcbae7401ebfb437357c3fa845a0f1e34` существует, автор — `architect`; `git show --name-status` показывает `NABLIUDENIA.md` внутри зоны architect и ровно три `R100`-переименования судейских файлов вне зоны, то есть содержимое неизменно и лишних внезонных правок нет. `СПАСЕНО` сопоставляется с полным неизменяемым хешем и автором, а `fixtures/check_zones/case_spaseno_ne_nazvannyj_hash.sh` в прогоне анти-плацебо сохраняет следующий неназванный хеш красным. Поэтому сам третий хеш не покрывает больше названной причины; блокирует только коллизия строки истории с грамматикой.

Это первая находка этой причины; арбитр не созывается.

Советов нет.
