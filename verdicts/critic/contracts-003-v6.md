FAIL

Судимый предмет: `contracts/003-skills-metta-adaptacija.md` на HEAD `6243fa2bfb1058c92ce67e63f6b9cd4c9f898faf` — реестровая v6.

Набор артефактов полон: предмет и критерий готовности закоммичены, исполнители названы, обе `ЗОНА`-строки присутствуют. Сравнение с `frozen/contracts/003/5` подтвердило заявленную предметную правку: в обе зоны добавлен `.omp/agents/`, прежнее отрицание его участия заменено объяснением перегенерации и добавлена запись истории v6. Коммит `6243fa2` несёт строку `РАЗРЕШИЛ-ВЛАДЕЛЕЦ` для этого контракта.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:102-103,121-123 — обе `ЗОНА`-строки разрешают исполнителям весь каталог `.omp/agents/`, но названная причина разрешения уже: перегенерация файлов из `roles/`, якобы удерживаемая `check:gen`. Этот держатель не отвергает новый напрямую написанный агент без маркера генератора: `scripts/gen-harness.ts` считает лишним только файл, содержащий собственный `MARK`, а остальные дополнительные `.md` намеренно игнорирует. Поэтому зона даёт больше, чем объявленное механическое следствие правки ролей.
ОБХОД: после заморозки `architect` коммитит новый `.omp/agents/rogue.md` с валидным фронтматтером, без строки `СГЕНЕРИРОВАНО dev-harness/scripts/gen-harness.ts` и без исходника в `roles/`. `check_zones.sh` принимает коммит, потому что вся `.omp/agents/` входит в `ЗОНА architect`; `npm run check:gen` возвращает 0, потому что чужой маркеру генератора файл не считается дрейфом; `npm run check:overlay` также возвращает 0. В дереве появляется исполняемый проектный агент, не являющийся перегенерацией роли, при зелёных названных держателях ограничения.

Обход предъявлен исполнением в изолированном клоне. С локальным тегом `frozen/contracts/003/6` на судимом HEAD исходный `bash scripts/check_zones.sh` вернул 0 и включил диапазон с `89063a1` (итог: 3 замороженных контракта, 2 автора, 96 коммитов в диапазонах, 61 проверен по зонам). Затем временный коммит `architect` добавил ровно описанный `.omp/agents/rogue.md`: `npm run check:zones` → 0 (62 проверенных по зонам), `npm run check:gen` → 0, `npm run check:overlay` → 0. С тем же обходом также зелёные `check:skills`, `check:antiplacebo` (130/130), `check:ci-parity` и `check:charter`; они не являются держателями происхождения агента.

Проверены пять обязательных вопросов:

1. Критерий слабее предмета v6 именно в найденном состоянии: целый каталог разрешён, а заявлено только порождение из ролей.
2. Пункты приёмки имеют команды с кодами возврата, однако `check:gen`, названный доказательством ограничения, не различает найденный обход.
3. Решение о допустимости новых, не порождённых из `roles/` файлов внутри `.omp/agents/` молча оставлено исполнителю.
4. Исполнители названы, но граница обоих исполнителей шире названной связки «файл роли → его порождение»; это механически показано зелёным `check:zones` на обходе.
5. Отдельного противоречия норме `AGENTS.md`, помимо того же разрыва между правилом и названным механизмом, не обнаружено.

Это второй `FAIL` по причине безусловно расширенной зоны для будущих правок: первая находка той же причины записана в `verdicts/critic/contracts-003-draft-r1.md` для пересекающихся путей реализации. Тогда широкие пути заменили конечным поимённым `СПАСЕНО`; v6 возвращает ту же конструкцию для целого каталога.

АРБИТР: `arbiter` — спор о том, достаточно ли каталожной зоны `.omp/agents/` вместе с `check:gen`, который игнорирует дополнительные файлы без своего маркера, либо граница обязана разрешать только названные порождённые файлы/иметь механизм полноты каталога.

Советов нет.
