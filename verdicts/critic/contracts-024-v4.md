accept

# Контракт 024, v3 → v4: остаточный риск окружения bash

Судимый HEAD main: `7dcea609f37b964eb167b1c5296d53b4e636efe0`.
Источник — свежий клон `https://github.com/a3ka/dev-harness.git`; чтение и прогоны —
в отдельном клоне `tmp/critic/repo` внутри `/tmp/dev-harness-verify/critic024v4-main`.
Локальный чекаут оркестратора не читался. Предмет суда — только респек текста,
не реализация, не повторный круг адверсария и не готовность 024 к done.

Прочитаны целиком контракт, `verdicts/arbitration/bash-env-predel-024.md` и
`verdicts/adversary/contracts-024-k6.md`, также `AGENTS.md` и предыдущий вердикт v3.
Набор артефактов полон: закоммиченный предмет —
`contracts/024-detektor-utechek-osnovnogo-chekauta.md:41-105`, инварианты с rc —
`:123-169`, критерии — `:283-318`, исполнители architect/implementer, зоны и
явные исключения — `:264-281`.

## Три проверки респека

1. **Слова и пунктуация совпадают с решением арбитра.** Буллет контракта
   `:254-262` сопоставлен с блок-цитатой
   `verdicts/arbitration/bash-env-predel-024.md:51-58` под заголовком «Строка в
   Демаркацию 024». Прямое сравнение строк после снятия маркеров цитаты/списка,
   выделения `**` и нормализации пробельных переносов дало равенство; никаких
   замен слов, пунктуации или порядка нет. Побайтовой копией Markdown это НЕ
   является: изменены оформление и физические переносы (совет ниже).
2. **Других изменений контракта нет.** Выполнен
   `git diff frozen/contracts/024/3 HEAD -- contracts/024-detektor-utechek-osnovnogo-chekauta.md`
   → rc 0: ровно девять добавленных строк, ноль удалений, один буллет в остаточных
   рисках. Независимо проверены закоммиченные блобы: удаление только этого буллета
   из HEAD восстанавливает весь v3-файл побайтово. Поэтому предмет, инварианты,
   приёмка, зоны и остальные пункты рисков неизменны не по выборочным цитатам.
3. **Нового противоречия или ослабления критериев нет.** Условие `:254-257`
   относится к коду, исполняемому в том же bash ДО первой строки детектора,
   а не к обычному загрязнению рабочего дерева. Оно исполняет решение арбитра
   `:40-58,60-74`: блокеры 1–2 к6 переклассифицированы в предел вызывающего
   интерпретатора. Это явная граница доверия, а не разрешение детектору игнорировать
   утечки на конформном входе. И-2/И-5/И-7 и остальные rc-обязательства остаются.
   Последнее предложение о случайной утечке читается в пределах названной
   предпосылки, не как общее освобождение от проверки обманных форм.

Арбитраж `:83-92` прямо сохраняет блокер 3 к6 — симлинк в `.git/hooks/` — вне
этого предела. Новый буллет его не снимает: такой симлинк не требует исполнения
кода в bash до первой строки. Также обычная переменная-часовой санитизации из
к5 не становится допустимым обходом: это исключение уже прямо отделено арбитром
`:63-66`. Исправление реализации по этим основаниям здесь не удостоверяется.

## Пять обязательных вопросов

1. **Критерий слабее предмета?** `contracts/024-detektor-utechek-osnovnogo-chekauta.md:123-169,283-318`
   не менялись. Дельта не позволяет заменить механизм всегда-зелёной заглушкой
   или перестать ловить обычные утечки; конкретного нового обхода не найдено.
2. **Готово недоказуемо командой?** Нового условия done не добавлено; существующие
   rc-команды сохранены. Проверяемое действие респека `:254-262` — перенесение
   названного арбитром ограничения — подтверждено сравнением блобов и текста.
3. **Решение молча оставлено исполнителю?** `:254-262` не раздаёт новую реализацию
   санитизации или доверенного загрузчика. Гигиена вызывающей стороны явно вне
   024; необязательные усиления арбитража `:76-81` не превращаются в условия done.
4. **Границы исполнения?** `:266-268` неизменны; путь контракта назван за architect,
   путь детектора — за implementer, исключения явные. Новых файлов/зон респек не
   требует; механический читатель границ — прежний `scripts/check_zones.sh`.
   Опубликованный респек внесён оркестратором по владелец-каналу разрешения устава.
5. **Противоречие AGENTS.md?** В дельте не найдено. Немеханизируемый предел явно
   назван остаточным риском, а не скрыт обещанием защиты; критерием его закрытия
   не объявляют. Коммит респека несёт собственную строку `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`;
   механизм устава принял именно `7dcea609`. Подлинность воли сверх этого
   предусмотренного нормой механизма не удостоверяется.

## Собственные прогоны и их границы

Все команды исполнены из отдельного клона, не из главного чекаута:

- `python3 tmp/critic/verify_v4.py` → rc 0. Одноразовая проба читала оба
  контрактных блоба и решение через `git show`, проверяла исходный HEAD,
  полное восстановление v3 удалением одного буллета, девять строк добавления
  и равенство текста цитате после описанной нормализации. Вывод:
  `Текст и пунктуация после снятия Markdown и переносов: совпадают`;
  `Физические строки без Markdown: буквально совпадают = False`.
- `bash scripts/check_charter.sh` → rc 0; строка по предмету:
  `ok   уставной документ изменён с разрешения владельца: contracts/024-detektor-utechek-osnovnogo-chekauta.md в 7dcea609`.
- `git diff --exit-code frozen/contracts/024/3 HEAD -- scripts/check_charter.sh scripts/check_zones.sh scripts/check_judge_gate.sh scripts/judge_gate.sh scripts/freeze_contract.sh scripts/check_contract_frozen.sh`
  → rc 0, пустой вывод: перечисленные frozen-барьеры неизменны.
- После собственного `--snapshot` того же изолированного клона → rc 0:
  `bash scripts/check_no_leak.sh --check /tmp/dev-harness-verify/critic024v4-main/tmp/critic/repo` → rc 0, `основной чекаут чист`.
  Это стенограмма собственной сверки клона, НЕ утверждение о чистоте чекаута
  оркестратора и НЕ выдуманная стенограмма его предспавнового вызова.

Scoped-регресс реализации и CI не запускались: изменён только текст риска,
барьеры этим респеком не правились. Известное расхождение живого контракта с v3
повторно не проверялось; снятие его требует отдельной заморозки v4.

## Совет

СОВЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:254-262 — называть
перенос «дословным по тексту, с переоформлением Markdown», а не «побайтовой
копией блок-цитаты»: источник содержит восемь строк с `>`, контракт — девять
строк списка с выделенным началом. Проверка буквального содержимого положительна
только после снятия этого оформления и переносов. Это не смысловая правка и не
обход критерия; по порогу роли оснований для FAIL здесь нет.

Блокирующих находок нет. Разрешена заморозка проверенного текста как v4.
Этот accept не отменяет блокер 3 к6, не объявляет done и не заменяет суд реализации.
