FAIL

# Контракт 042, круг 1 — Н-127

Судим закоммиченный `contracts/042-excuse-for-otstavshij-roditel.md` целиком:
HEAD клона `6a6f8afbb6f2ad0cbe011351215d4d0142b79e0d`, коммит предмета
`625e61e687a045bab9ee9e44febc5c5e4c683792`, blob контракта
`b9c20f7bce4ec21da44df5896a4f031a09cc4dd3`. Предмет, критерий с командами,
исполнитель и ЗОНА-строки присутствуют: набор полный. Код и контракт судья не менял.

## Блокирующие находки

БЛОКИРУЕТ contracts/042-excuse-for-otstavshij-roditel.md:76 — выбранный предикат Б
«путь отсутствует хотя бы у одного родителя» не отличает наследование удаления от
ПОВТОРНОГО удаления восстановленного пути в теле мержа. Утверждение :82–85, что
остановка на первом не-артефакте сохраняет антигейминг, неверно. Приёмка :117–121
этот случай не различает: исходный послефиксовый код удовлетворяет R1/R2/R5 и всем
21 отрицательным case, но переиспользует старое разрешение.
ОБХОД: D1 удаляет `verdicts/adversary/v-a.md` с ALLOW; от D1 ответвляется side,
которая меняет только посторонний файл; main восстанавливает защищённый путь
коммитом R; затем `git merge --no-ff --no-commit side`, `git rm` восстановленного
пути, commit D2 БЕЗ ALLOW. У второго родителя D2 путь отсутствует, поэтому Б
пропускает D2 и принимает ALLOW из D1. На этом дереве послефиксовый барьер дал
rc=0 «исчез с явного разрешения», дофиксовый барьер из `625e61e^` дал rc=1
«защищённый артефакт существовал и на HEAD его нет». Это живое ослабление,
а не гипотетическое требование к будущему адверсарию.

Наблюдённая история отдельного toy-репозитория:

```text
*   a2c3587 D2: повторное удаление в мерже, разрешения нет
|\
| * ec242f6 Независимая ветка после D1
* | 4a896ef R: вердикт восстановлен
|/
* b3a98ac D1: первое разрешённое удаление
* eed5fa5 Основание
```

До D2 восстановленный путь проверен барьером: rc=0, на HEAD 1, исчезло 0.
После D2 текущий барьер: на HEAD 0, с разрешения 1, rc=0. Та же история со
старым барьером: на HEAD 0, исчезло 1, с разрешения 0, rc=1.

Нужная правка контракта: различить исходный Н-127 и восстановление-повторное
удаление через merge; предъявить второй случай отдельным красным критерием.
Возвращаться к кандидату А нельзя: его самостоятельный мутант действительно
переиспользовал разрешение уже на ЛИНЕЙНОЙ истории D1 → R → D2 (см. проверки ниже).
Конкретную исправленную семантику выбирает автор; судья её здесь не подменяет.

БЛОКИРУЕТ contracts/042-excuse-for-otstavshij-roditel.md:140 — объявленные границы
не переданы действующему читателю зон. Он читает одну строку
`ЗОНА <git user.name>: <путь> [<путь>…]`, а не следующие markdown-пункты.
Здесь автором становится `architect (самоприменение — обоснование)`, путями —
слова `фикс — одна функция ...`; настоящий `architect` не объявлен. В :158
`HANDOFF.md,` включает запятую, комментарий становится путями; в :160–162
`verdicts/critic/.` и аналоги не являются каталогами с завершающим `/`.
ОБХОД: скопировать байт-в-байт этот контракт в минимальный git-репозиторий,
поставить подставной аннотированный `frozen/contracts/042/1`, затем коммитом
с `user.name=architect` добавить `outside.txt`. Настоящий
`bash scripts/check_zones.sh <toy>` дал rc=0:
`замороженных контрактов: 1 · объявленных авторов: 5 · коммитов в диапазонах: 1 · проверено по зонам: 0`.
Все пять критериев R1–R5 остаются удовлетворены в своей заявленной области,
но механизм не держит названные контрактом границы. Это также противоречит
`AGENTS.md`, воркфлоу §4 и правилу 1 «правило без механизма не существует».
Исправление — литеральные однострочные ЗОНА с точными именами и путями;
обоснование самоприменения и запреты чужих правок остаются отдельной прозой.

## Живые проверки — что именно доказано

| Проверка | Наблюдённый результат |
|---|---|
| `bash scripts/check_protected.sh .` в РЕАЛЬНОМ `/home/aka/Documents/dev-harness` | rc=0; `contracts-040-v2.md`, `v3.md`, `v4.md` признаны исчезнувшими с явного разрешения; 1990 коммитов, 396 существовало, 369 на HEAD, 23 с разрешения, 4 перенесено |
| `bash fixtures/check_protected/repro_n127_otstavshij_roditel.sh` на исходном фиксированном коде | rc=0, легитимное удаление распознано |
| Та же неизменённая R2-фикстура во временной копии, барьер заменён содержимым `625e61e^:scripts/check_protected.sh` | rc=1, предметный FAIL на `verdicts/adversary/v-1.md` |
| В той же копии возвращён послефиксовый барьер, та же R2 | rc=0; обе полярности проверены самостоятельно |
| `bash scripts/verify_antiplacebo.sh --scope check_protected` в чистом клоне | 21/21: каждый case предъявил зелёный контроль и красный повтор кодом 1. Все 20 старых сохранены; новая `case_slijanie_otstavshego_roditelja.sh` красна без ALLOW |
| `bash -n scripts/check_protected.sh` | rc=0 |
| Прямой запуск существующей `case_razreshenie_povtorno.sh` с настоящим барьером, отдельные WORK/BARRIER | D1 зелёный, восстановление зелёное, линейный D2 без ALLOW красный, rc=1 |
| Кандидат А как самостоятельный мутант `excuse_for`: просматривать все D-события, не останавливаться после первого неразрешённого | Та же линейная фикстура дала rc=0: D1 ошибочно извинил D2; в `git log --diff-filter=D` действительно присутствуют оба удаляющих коммита |
| Восстановление → D2-merge, описанное в Б1 | текущий Б: rc=0; дофиксовый: rc=1 |
| Буквальные зоны на подставной заморозке, коммит architect вне зоны | rc=0, проверено по зонам 0 — Б2 |

Scoped-команда в целом завершилась rc=1, НЕ rc=0: три ровно предсуществующих
расхождения классификации — `check_threat_model.sh`, `fixtures/check_threat_model`,
`fixtures/parsing_hygiene_battery`. Это не регрессия 042 и не основание FAIL.
Прочитан полный diff `625e61e`; отдельно
`git diff --exit-code 625e61e^ 625e61e -- scripts/check_threat_model.sh scripts/verify_antiplacebo.sh fixtures/check_threat_model fixtures/parsing_hygiene_battery`
дал rc=0, без вывода. Эти чужие расхождения не переоткрываются.

Счёт семьи проверен также перечнем файлов: 21 `case_*.sh`, из них ровно один
добавлен `625e61e`. Полный diff содержит только контракт, два новых файла
`fixtures/check_protected/` и блок `is_lagging_merge_artifact`/`excuse_for` с
непосредственным комментарием. `moved_for`, область, env-гигиена и конечные коды
не менялись. Отдельный diff старых `case_*.sh` с исключением нового case и
`red_predel_git_vyzovov_ours.sh` пуст, rc=0: зона 040 не тронута.

Прочитаны Н-127 в `NABLIUDENIA.md`, `plans/005-four-mechanisms.md` (§4 и
уточнения исключений), `check_protected.sh:202–205`, существующая
`case_razreshenie_povtorno.sh`. Исторический аргумент против кандидата А
подтверждён исполнением, но не доказывает безопасность кандидата Б.

## Пять вопросов гейта

1. Критерий слабее предмета: да, Б1 — все заявленные проверки не отличают
   сохранение антигейминга от переиспользования ALLOW через восстановление и merge.
2. Команды доказательства названы и исполнены; три внешних расхождения R4
   учтены явно, семейный результат не выдан за общий rc=0.
3. Семантика сформулирована точно, но ошибочна: вопрос, какое удаление признаётся
   новым после восстановления, нельзя оставить неразличённым перед заморозкой — Б1.
4. Фактический diff не вышел за описанные границы; будущий машинный контроль
   буквальными объявлениями не обеспечен — Б2.
5. Противоречие механически удерживаемым зонам нормы — Б2. Дополнительных
   блокеров из остальной прозы документа не выводится.

Первая итерация; повторной причины спора нет, арбитраж не созывается.
Вердикт не разрешает заморозку текущего текста.

Эксперименты выполнены `python3 /tmp/dev-harness-verify/Critic042/probe042.py`:
он строит все перечисленные toy-репозитории, отдельные копии барьера и мутанта,
проверяет ожидаемые коды возврата; сырой вывод и деревья находятся в
`/tmp/dev-harness-verify/Critic042/experiments/`. Исходные контракт, барьер и
фикстуры в клоне не изменялись. Внешний scratch использован из-за действующего
Н-85: write непиннованной сессии отказал даже в `tmp/critic/` основного дерева.
