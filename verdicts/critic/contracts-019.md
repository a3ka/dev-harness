FAIL

БЛОКИРУЕТ contracts/019-ustav-draft-klyuchi.md:119 — объявленная octal-проба не предъявлена красной и не кодирует требуемое значение ветки: на закоммиченном main команда с `--nnn 042` возвращает rc 0 и печатает `BRANCH=wip/034/architect`, а не заявленные rc 1 и invalid octal. Реально красный вход текущего кода — `--nnn 019`. Это также противоречит правилу 3 `AGENTS.md`: кодовая проверка должна быть предъявлена красной до круга критика.
ОБХОД: оставить `scripts/spawn_agent.sh:122` без octal-фикса. В текущем виде первый вызов с `042` уже даёт требуемый критерием rc 0 и создаёт worktree, повторный вызов уже даёт rc 1 «уже существует»; неверное значение `wip/034/…` не превращает ни одну из названных команд в ненулевую проверку результата. Ветвь 3 предмета не сделана, а rc-последовательность строки 125 выполнена.

БЛОКИРУЕТ contracts/019-ustav-draft-klyuchi.md:123 — четыре scoped-фикстуры не доказывают объявленное на строках 19–24 кольцо уставного класса. Все три уставные фикстуры подают только `AGENTS.md`/`ROADMAP.md`; нет ни положительного frozen `plans/NNN-*`/`contracts/NNN-*`, ни отрицательного такого же пути без тега первой заморозки.
ОБХОД: экспортировать из `check_charter.sh` предикат, который признаёт уставными только `AGENTS.md` и `ROADMAP.md`, и использовать его в `check_staged.sh`, не переводя замороженные plans/contracts с зонного суда на `check_charter`. Все четыре новые scoped-команды и существующие scoped-регрессии проходят, но ветвь 1 предмета для двух классов артефактов не сделана.

БЛОКИРУЕТ contracts/019-ustav-draft-klyuchi.md:130 — утверждение «без строки — ловит CI check:charter» шире фактического судьи. `check_charter.sh` и `check_zones.sh` обходят историю через `rev-list --no-merges`; контракт ограничивает правку `check_charter.sh` библиотечным экспортом и не называет этот остаток в измеренной границе.
ОБХОД: после реализации создать merge-коммит без `РАЗРЕШИЛ-ВЛАДЕЛЕЦ`, у которого изменение `ROADMAP.md` внесено только в результат merge, а родители ROADMAP.md не меняют. Pre-commit делегирует проверку истории до текущего коммита и проходит; CI после коммита пропускает сам merge в обоих судьях. Новые scoped-фикстуры и полный CI остаются зелёными, хотя устав изменён без требуемой строки.

СОВЕТ contracts/019-ustav-draft-klyuchi.md:50 — `next_id_peek` без резервации оставляет окно: два architect-worktree/клона от одной вершины одновременно видят один и тот же следующий NNN и оба получают rc 0 от `check_staged`. Слитые разные slug с одним NNN затем краснит существующий `check_ids`, поэтому обход полного критерия «CI зелёный на done-коммите» здесь не предъявлен; границу и точку сериализации стоит назвать прямо.

СОВЕТ contracts/019-ustav-draft-klyuchi.md:145 — разрешённая здесь «отдельная библиотека» не входит в перечисление файлов зоны implementer на строке 96, тогда как строка 103 разрешает только экспорт из `check_charter.sh`. Убрать неисполняемую альтернативу либо назвать путь библиотеки в зоне.

## Круг 2

Блокер 1 круга 1 закрыт: `bash scripts/verify_antiplacebo.sh . --scope spawn_agent/case_octal_sdvig_znachenija` → rc 1, дословно «барьер остался зелёным на обманном дереве — красное не предъявлено». Фикстура сверяет и `BRANCH=wip/042/architect`, и наличие одноимённой ссылки; обход с сохранённой съезжкой в `wip/034/architect` краснит scoped.

Блокер 2 круга 1 закрыт: `bash scripts/verify_antiplacebo.sh . --scope check_staged/case_ustav_koltso_zamorozki` → rc 1, дословно «нет положительного контроля — фикстура обязана предъявить барьер ЗЕЛЁНЫМ до порчи, иначе вечно-красный барьер неотличим от работающего». Case содержит frozen+ для contracts и plans, frozen− без тегов и нарушение замороженного плана; обход с предикатом только для `AGENTS.md`/`ROADMAP.md` краснит scoped.

Блокер 3 круга 1 закрыт: `bash scripts/verify_antiplacebo.sh . --scope check_charter/case_merge_delta_bez_razreshenija` → rc 1, дословно «барьер остался зелёным на обманном дереве — красное не предъявлено». В репро merge-коммит имеет два родителя; `ROADMAP.md` присутствует в дельте к первому родителю, оба родителя его не меняют, строка разрешения отсутствует. Обход с пропуском merge-коммита краснит scoped.

Красный баланс совпал с объявленным: `--scope check_staged` → rc 1, 23 фикстуры, 18 прежних предъявлены красными повторным прогоном, 5 новых дали «нет положительного контроля»; `--scope check_charter` → rc 1, 7 прежних зелёны и новый merge-case красен; `--scope spawn_agent` → rc 1, прежний case зелён и новый octal-case красен; `--scope next_id` → rc 0, 1/1; `--scope check_hooks` → rc 0, 7/7.

Зоны v2 покрывают новые файлы architect в `fixtures/check_staged/`, `fixtures/spawn_agent/` и `fixtures/check_charter/`; зона implementer называет все пять затрагиваемых scripts. Альтернатива отдельного файла библиотеки удалена: экспорт предиката закреплён в `scripts/check_charter.sh`. Окно `next_id_peek` и downstream-проверка `check_ids` названы, сериализация вынесена в §10.

БЛОКИРУЕТ contracts/019-ustav-draft-klyuchi.md:125 — И-7 предъявляет только запрещённый merge без строки разрешения. Ни эта scoped-команда, ни перечисленные регрессии не доказывают, что разрешённая уставная дельта merge-коммита по-прежнему проходит по неизменённой грамматике `РАЗРЕШИЛ-ВЛАДЕЛЕЦ`; чистая история в зелёном контроле merge-ветвь не исполняет.
ОБХОД: реализовать читатель истории так, чтобы он находил merge-коммиты с уставной дельтой к первому родителю, но отклонял каждый такой merge до проверки строки разрешения, печатая «изменён без разрешения владельца». Merge-case без строки краснеет требуемой причиной, все прежние grammar-case на не-merge коммитах и все семь scoped-команд после реализации проходят, полный CI зелёный; при этом merge-коммит с корректной строкой `РАЗРЕШИЛ-ВЛАДЕЛЕЦ: ROADMAP.md <причина>` ошибочно отклоняется, то есть объявленные неизменные грамматика и разрешённая ветвь предмета не реализованы.
