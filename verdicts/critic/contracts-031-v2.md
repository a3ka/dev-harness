accept

# Контракт 031 — критик v2, амендмент к замороженному тексту

Предмет: только дельта `frozen/contracts/031/1 → d6ceb3102ccec7a812d369eebee3bbd746e52676`, файл `contracts/031-strazh-semejstvo-mint-i-bazlajn.md`, ветка `wip/031/architect`. Логика двери/признания, инварианты и остальные ЗОНА-строки не переоткрывались. Прежний accept круга 3 остаётся основанием по закрытым вопросам.

**Действительных блокирующих находок в предъявленной дельте нет.** Есть один совет о последствиях переноса для уже существующей истории реализации. Это не обход приёмки и не основание FAIL по порогу роли.

## 1. Предмет и полнота артефактов

Предмет закоммичен: HEAD отдельного клона — `d6ceb3102ccec7a812d369eebee3bbd746e52676`, родитель — `4bf65beb56fbc61e4b96c9a2bb2f7326100e1b0c`. Предмет работы, критерии с командами, именованные исполнители и ЗОНА-строки присутствуют.

Проверено:

- `git diff --exit-code frozen/contracts/031/1 4bf65be -- contracts/031-strazh-semejstvo-mint-i-bazlajn.md` → rc 0: исходный текст родителя совпадает с v1.
- `git diff --numstat 4bf65be d6ceb31` → rc 0: один файл, 9 добавленных и 5 удалённых строк.
- Полный diff содержит ровно три заявленных хунка: путь исполнителя, пояснение границ, семейство приёмки.
- `git diff --exit-code frozen/contracts/031/1 d6ceb31 -- scripts fixtures .github/workflows/ci.yml` → rc 0: амендмент не меняет барьеры, фикстуры, helper, раннер или CI.

Отсутствие разрешающей подписи в предъявленном коммите не выдается за разрешение менять frozen: этот суд выполнен ДО подписи по явно заданному владельцем порядку. Вердикт не заменяет разрешающий коммит и freeze v2.

## 2. Точность дельты и границы исполнения

Места предмета: `contracts/031-strazh-semejstvo-mint-i-bazlajn.md:181,189–200,275–278`.

Замена `fixtures/check_staged/case_dver_minta_priznanie.sh` на `fixtures/check_zones/case_dver_minta_priznanie.sh` соответствует уже объявленному предмету case — признанию закоммиченного минта в `check_zones`, а не staged-двери.

Между собственными ЗОНА-строками 031 нового пересечения нет: architect получает конкретные red-файлы, implementer — конкретный case. Права architect на каталог `fixtures/check_zones/` уже объявлены другими контрактами, в частности `023:353`; пояснение не вводит приоритет и не вычитает эти права. Совместный каталог с поимённой раздачей продолжает названную модель 029, а не создаёт вторую семантику зон.

`fixtures/check_zones/_repo.sh:24` действительно определяет `commit_as <корень> <автор> <сообщение>`: helper добавляет изменения и коммитит с явными `user.name`/`user.email`. Необходимость править общий helper из амендмента не следует. Практическая осуществимость локального использования `commit_as` проверена ниже, без копирования staged-helper.

Механизм границ остаётся прежним: точечный путь implementer и суд по замороженным ЗОНА-строкам. Новая оговорка прямо оставляет `_repo.sh` вне правки исполнителя. Дополнительного решения, необходимого именно для маршрутизации case и молча переложенного на исполнителя, не найдено; конкретная форма локального вызова helper — деталь реализации.

## 3. Приёмка не подменена существованием файла

Место предмета: `contracts/031-strazh-semejstvo-mint-i-bazlajn.md:275–278`.

Раннер связывает семейство с исполняемым барьером, учитывает вызовы через `$BARRIER`, требует зелёный контроль и самостоятельно повторяет отрицательный вызов. Поэтому смена семейства не позволяет зачесть прямой вспомогательный вызов вместо признания и не превращает критерий в «файл лежит рядом».

Проведён собственный изолированный эксперимент, не повтор исходного известного отказа 28/29:

- из клона предмета взяты неизменённые scripts и helper `fixtures/check_zones/_repo.sh`;
- настоящий `check_zones.sh`, его `lib_zones.sh` и исходный case взяты из закоммиченной реализации `b11943e124b2fd465be2dec24c408db967be18db`;
- case помещён в `fixtures/check_zones/`;
- только внутри экспериментального case прежний `commit_kak` переведён на `commit_as "$1" orchestrator "$2"`, удалены два вызова отсутствующего в целевом helper `set_author`; после каждого такого коммита проверен фактический `%an == orchestrator`;
- helper, барьер и раннер экспериментом не исправлялись.

Команда подготовки и запуска:

```text
python3 /tmp/dev-harness-verify/critic031-v2/probe.py
RC: 0
```

Фактически исполненная scoped-команда:

```text
bash /tmp/dev-harness-verify/critic031-v2/routing-tree/scripts/verify_antiplacebo.sh /tmp/dev-harness-verify/critic031-v2/routing-tree --scope check_zones/case_dver_minta_priznanie
RC: 0
```

Сырой результат:

```text
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_zones/case_dver_minta_priznanie.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «дверь минта 031: дельта манифеста не только-добавление»

барьеров: 1 · фикстур: 1 · предъявлено красным повторным прогоном: 1
```

Неизменность helper проверена побайтово против блоба предмета до и после запуска; SHA-256:

```text
b3f4a87b5fa852ded45f30be713eb7008f80348612cef1fb3a4ad7459c371362
```

Это доказательство осуществимости амендмента и живой зелёно-красной пары одного case. Это НЕ приёмка всей реализации, не полный прогон семей и не заявление, что перенос уже приземлён.

CI-проводка достаточна для переноса: `.github/workflows/ci.yml:64–65` включает `check_staged` и `check_zones` в ap3; шаг запуска передаёт ключи шарда в `--scope`. Изменять CI для нового адреса не требуется. Оба прежних red-критерия амендментом не изменены.

## 4. Упоминания пути

Поиск по контракту и отдельная проверка строк закоммиченного блоба дали:

```text
fixtures/check_staged/case_dver_minta_priznanie.sh: 0 вхождений
fixtures/check_zones/case_dver_minta_priznanie.sh: 1 вхождение
```

Единственное полное упоминание нового пути — ЗОНА implementer на строке 181. Неполное имя case в пояснении author-механизма не является вторым объявлением пути.

## 5. Совет по посадке истории

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:181 — замена пути в высшей заморозке имеет последствие для уже закоммиченной реализации: старый точечный путь перестаёт входить в зону implementer. При посадке нужно отдельно учитывать старые коммиты и удаление исходного файла, а не предполагать, что v1 навсегда сохраняет им разрешение. Это следствие изменённой строки, не пересмотр остальных зон.

Последствие измерено настоящим `check_zones` на изолированном toy: v1 разрешает старый путь; implementer создаёт case; v2 заменяет разрешение новым путём. Команда:

```text
bash /tmp/dev-harness-verify/critic031-v2/zones-migration.sh
RC: 0
v1 rc=0; v2 rc=1
```

Отказ после v2:

```text
FAIL коммит вне зоны: implementer da09aa3b fixtures/check_staged/case_dver_minta_priznanie.sh — зона автора (объединение всех замороженных): fixtures/check_zones/case_dver_minta_priznanie.sh
```

Проба доказывает семантику смены разрешения, а не результат будущей собранной ветки. Обхода, при котором из-за этого эффекта заявленная приёмка зелёная, а признание не сделано, не установлено: механизм здесь отказывает, а не даёт ложное зелёное. Поэтому находка помечена советом, не блокером. Вердикт не разрешает переписывание защищённой истории или расширение зоны помимо установленной процедуры.

## Решение и пределы

Пять вопросов гейта проверены в пределах трёх хунков: критерий не ослаблен относительно изменённого предмета; различающее исполнение названной команды продемонстрировано; механизм автора и адрес case определены; исполнитель и границы названы; нового противоречия норме AGENTS.md в этой дельте не выявлено.

Амендмент принимается для следующего шага установленного порядка: разрешающий коммит владельца/сессии по его прямому слову, затем freeze v2. Это не разрешение критику подписывать правку и не приёмка реализации.

Суд выполнен в отдельном клоне `tmp/critic031-v2/repo`; ветка и worktree автора не изменялись. Пробы и сырые логи сохранены в `/tmp/dev-harness-verify/critic031-v2/`. Вердикт передан полным текстом; приземление в `verdicts/critic/contracts-031-v2.md` выполняет оркестратор с author=critic. Клон сохранён до появления этого коммита в основном репозитории.
