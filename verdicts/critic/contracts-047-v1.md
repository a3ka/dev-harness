accept

Предмет: `contracts/047-blizhnec-priznanija-chernovika-check-zones.md` на HEAD `bb2115e`
(контракт закоммичен `69895f9`, фикс `c571f73`). Круг 1 контракта 047, гейт ПЕРЕД заморозкой.
Набор артефактов полон: предмет назван, критерий готовности — десять проб с кодами возврата,
исполнитель назван (`ЗОНА architect:` строка 129 + `РАБОТА НЕ РАЗДАЁТСЯ:` строка 157 +
объявление «РАЗДАЧА IMPLEMENTER'У НЕ ОТКРЫВАЕТСЯ» строка 145).

## ПОРОГ: блокирующих находок — 0

Ни одной находки с названным ОБХОДОМ не нашлось; всё найденное едет советом. Попытки обхода,
которые я предъявлял тексту и которые НЕ прошли (пишу их, потому что «блокеров нет» без
названных попыток — это вкус, а не порог):

1. **Плацебо-фикс через отрицание множества.** Замерено тремя мутантами в disposable-клоне:
   `[ "${base:3:1}" != "_" ]` → Р1 краснеет на двух клетках из трёх
   (`case_grammatika_bukva_posle_nomera`, `case_grammatika_bez_suffiksa`);
   `[[ ! "${base:3:1}" =~ [a-zA-Z_] ]]` → краснеет `case_grammatika_bez_suffiksa`;
   `if false` (признание убито целиком) → все три клетки И существующий страж
   `case_draft_priznanie` валятся на «нет положительного контроля». Критерий НЕ слабее
   предмета: ни один известный суррогат его не удовлетворяет. Обхода нет.
2. **Разведённая новая фикстура при неисправленном барьере.** Р5 (строка 177) действительно
   НЕ пинит три НОВЫЕ клетки — от тега минта `045c556` они видны как `A` при любой
   последующей правке. Но обход умирает на колонке ДО пробы Р1: фикстура, красная БЕЗ живого
   `id/CONTRACT/123`, была бы красной и на до-фикс барьере, то есть дала бы «32 из 32» вместо
   объявленного «29», а порядок предъявления («КРАСНОЕ отдельным коммитом, судья
   воспроизводит красноту на первом коммите», строки 151-155) — часть критерия. Бисект я
   прогнал: на `69895f9` ровно 29 из 32, зелёными остались ровно три новые. Обхода нет,
   но колонка ДО несущая — см. СОВЕТ 4.
3. **Над-сужение предиката** (например требование суффикса `.md`): закрывало бы предмет ценой
   расхождения с замороженной грамматикой 023, но §Инварианты п.1 (строка 82) называет
   предикат дословно и побайтово, то есть такая реализация нарушает названный инвариант,
   а не проходит мимо него. Обхода нет.
4. **Противоречие норме `AGENTS.md`** — не найдено. Правило 2 (нет печати PASS): фикс не
   добавляет ни одной печати. Правило 4 (счётное — ДРУГОЙ мерой): счёт 32/29 меряет раннер,
   счёт 18 пинов текста отказа — `grep -l` (я перемерил: 18); мои утверждения о вложенном
   пути ниже тоже сняты второй мерой (убитое признание). Правило 6 (гейт не зеленее CI): CI
   несёт `check:zones` (ci.yml:365) и `check:antiplacebo --scope` (ci.yml:87) — покрыты Р7 и
   Р1, новых `run:`-команд контракт не вводит. Правило 14: пачка не тронула ни одного файла
   `roles/` (полный список правок пачки — `NABLIUDENIA_ARCHITECT.md`, контракт, три фикстуры,
   `scripts/check_zones.sh`).

## Живые пробы критика (disposable-клоны, `/tmp/dev-harness-verify/scratch/critic047-*`)

| проба | результат |
|---|---|
| Р1 ПОСЛЕ `bb2115e`: `verify_antiplacebo.sh --scope check_zones` | rc=0, «фикстур: 32 · предъявлено красным повторным прогоном: 32» — сошлось с текстом |
| Р1 ДО `69895f9` (бисект): то же | rc=1, «фикстур: 32 · … 29», зелёными ровно `case_grammatika_{bukva_posle_nomera,podcherkivanie_posle_nomera,bez_suffiksa}` — сошлось |
| Р5 | rc=0 |
| Р6 ПОСЛЕ / ДО (`repro-n149.sh`) | rc=0 «расхождений: 0» / rc=1 «расхождений: 3» (A·B·C) — сошлось |
| Р7 `check_zones.sh .` | rc=0 (46-й контракт при симуляции заморозки — тоже rc=0, см. ниже) |
| Р8 `bash -n` | rc=0 |
| Р9 `check_precision_gate.sh` (норма роли 043) | rc=0, «OK»; печатает «семья zones — барьер правится этим же черновиком, задача (б) для неё пропущена (прецедент 036 В4-3)» |
| Р10 `check_threat_model` · `check_provodka` · `check_consumers` | rc=0 · rc=0 · rc=0 |
| В-4 (§Риски 1) перепроверен ЖИВЬЁМ: пост-freeze проверка вынесена ДО выбора руки | `case_draft_priznanie.sh` → «нет положительного контроля». Перенос ЛОМАЕТ живого стража — заявление контракта правдиво |
| В-5 (проба F) перепроверен ЖИВЬЁМ: подставные `awk`/`sort`/`grep` в PATH вызывающего | честный rc=1 · под тенью rc=1, строка отказа одна и та же — герметизация `env -i` держит, второй пин не нужен |
| Зоны читателем зон (`zones_load`/`zones_match_path`), НЕ прозой | `scripts/check_zones.sh` — В ЗОНЕ architect; `fixtures/check_zones/` — В ЗОНЕ; `scripts/check_no_leak.sh`, `scripts/lib_zones.sh` — ВНЕ (сошлось с «НЕ РАЗДАЁТСЯ») |
| Симуляция ЗАМОРОЗКИ 047 в клоне (тег `frozen/contracts/047/1` + вердикт) | заявление разбирается: три пути зоны architect, `zones_violations` пуст, `check_zones.sh .` rc=0 — грамматика `ЗОНА`-строки после заморозки не краснит барьер |
| §Инварианты п.4 (тег — не индульгенция на весь коммит) | смешанный коммит: rc=1, назван `plans/za-zonoj.md`, НЕ назван `contracts/123-chernovik.md` |
| §Инварианты п.5/п.6 (текст отказа и прочие строки не тронуты) | в диффе `69895f9..HEAD` по барьеру ни одной правки строк печати; хунки только 847-904 |
| §Риски 2 (якорь) | `grep -n "И-3 признание (iv)"` находит оба места; в новом якоре номеров строк — 0; устаревшая ссылка близнеца `check_no_leak.sh:1421` жива (чужая зона) |
| §Риски 3 (`_schet_fixtur.sh`) | rc=1 («32 против 19», «15 против 14»); в `.github/workflows/`/`package.json` не подключён — сошлось |
| Н-39 (клетки кодовые, не проза) | доказано мутационно, см. ПОРОГ п.1: каждая из трёх клеток убивает СВОЙ суррогат, `bez_suffiksa` — единственная, что убивает отрицание класса |

Делегированное мне гейтами исполнено, а не унаследовано молчанием:
`check_spec_ready.sh` печатает «В4 — семья zones в правке этим же черновиком — В4 для неё
судит критик» → сужу: у каждой из трёх новых клеток живой зелёный контроль (раннер печатает
«зелёный контроль есть» на всех трёх) и argv-грамматика барьера соблюдена (`"$BARRIER" "$R"`,
тот же вызов, что у 29 существующих клеток, барьер на реальном дереве rc=0). `check_precision_gate.sh`
пропускает задачу (б) для своей же семьи → полярность каждой новой клетки я снял бисектом и
тремя мутациями выше.

## Находки

СОВЕТ contracts/047-blizhnec-priznanija-chernovika-check-zones.md:222-225 — §Риски 4 называет
остаточную грамматику НЕВЕРНО, и ошибка измерима. Пример «включая каталог `contracts/123-x/y.md`»
неверен: у такого пути basename `y.md`, три первых байта не цифры, признание не выдаётся — путь
СУДИТСЯ (живая проба: rc=1, путь назван в строке отказа). А настоящий остаток контракт не
называет: признание выдаётся на конформный basename НА ЛЮБОЙ ГЛУБИНЕ — `contracts/sub/123-x.md`
выводится из суда зон (rc=0 с живым предикатом; при убитом предикате rc=1 и
«коммит вне зоны: agent-x … contracts/sub/123-x.md» — вторая мера, норма AGENTS п.4). Блокером
это не считаю: предмет 047 — четвёртый байт basename, и §Модель угроз формулирует защиту именно
по basename, то есть правдиво. Но замороженный текст становится нормой, и ложно названный остаток
дороже ненайденного: следующий круг, «починив» его по букве §Риски 4, расширит код до вложенных
путей вместо сужения. Правка — одна строка до заморозки.

СОВЕТ contracts/047-blizhnec-priznanija-chernovika-check-zones.md:53,82,146 — слово «побайтово»
в §Инвариантах неточно для реализации: `${base:3:1}` в bash под `LC_ALL=C.UTF-8` (барьер сам
ставит эту локаль при `env -i` re-exec) режет СИМВОЛЫ, не байты. На исход это не влияет ни на
одном входе (любой символ, кроме дефиса, даёт отказ — сторона безопасная), поэтому не блокер; но
инвариант, названный «побайтово», при следующей правке приглашает сверку байтов (`cut -b`,
`${base:3:1}` в C-локали) как «точное соблюдение», а это уже другая семантика. Точнее: «четвёртый
СИМВОЛ basename равен дефису; локаль барьера канонизирована re-exec'ом».

СОВЕТ contracts/047-blizhnec-priznanija-chernovika-check-zones.md:178,184-186 — Р6 указывает на
`/tmp/dev-harness-verify/scratch/arch047-a1b2/repro-n149.sh`, то есть на одноразовый скратч
АВТОРА. Мне он ещё был доступен и я прогнал обе колонки; после нормальной уборки скратча ячейка
критерия становится неисполнимой (`rc=127`). Fail-closed, поэтому не блокер и не обход. Но
«приёмочная проба, которую следующий круг не сможет повторить» — это цена, которую стоит назвать
прямо в самой ячейке: либо словом «снято кругом 1, дальше несут Р1-Р4», либо переносом пробы в
дерево (`red_*.sh` probe-only, если признавать её постоянной мерой).

СОВЕТ contracts/047-blizhnec-priznanija-chernovika-check-zones.md:177 — Р5 пинит только 29
СТАРЫХ клеток, и это в тексте сказано; но из этого следует неназванное: три НОВЫЕ клетки не
пинит НИЧТО, кроме колонки ДО пробы Р1 (см. ПОРОГ п.2). Пока контракт не заморожен, это
достаточно; после заморозки единственной защитой новых клеток остаётся память о том, что «29»
в колонке ДО — несущее число, а не украшение. Предложение: назвать это одной фразой рядом с Р5
(«новые три пинятся колонкой ДО Р1: 29, не 32») либо добавить ячейку
`git diff --exit-code c571f73 HEAD -- fixtures/check_zones/case_grammatika_*`.

СОВЕТ contracts/047-blizhnec-priznanija-chernovika-check-zones.md:203-214 — §Риски 1 назван
ДОБРОСОВЕСТНО (проверил живьём: перенос симметрии действительно валит `case_draft_priznanie.sh`,
и валит его не капризом фикстуры, а конструкцией `_repo.sh`, где `frozen/contracts/001/1` лежит
на коммите-основании, то есть ЛЮБОЙ draft-коммит toy-дерева пост-freeze по построению). Вопрос
владельцу стоит сделать ещё острее: у замороженного 023 внутреннее расхождение, а не просто
«рука А безусловна». Текст рук (023 (iv), строки 179-187 блоба заморозки) даёт руке А
безусловность, а И-3 того же контракта (строки 223-226) перечисляет «пост-freeze — не признаётся»
БЕЗ оговорки про руку. Владелец решает не «менять ли норму 021/023», а «какая из двух строк 023
и есть норма» — это дешевле и честнее формулируется.

## Что я НЕ судил

Реализацию за пределами предмета (адверсарий и ревьюер судят её своим чином); прозу §Frontier
и §Предмета сверх трёх вопросов гейта (что делаем · что значит готово · кто в каких границах);
устаревшую ссылку `check_zones.sh:846-888` в `scripts/check_no_leak.sh` (чужая зона, названа
§Риски 2); `_schet_fixtur.sh` (красен до пачки, назван §Риски 3, в CI не подключён).
