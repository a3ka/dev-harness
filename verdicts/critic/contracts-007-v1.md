FAIL

# Вердикт критика — контракт 007, круг 2

Предмет, критерий, исполнители и обе `ЗОНА`-строки находятся в закоммиченном HEAD
`419bc2df5a008d1eaa8853bfb953a5b490f00680`. Проверены усиленные пробы и пины протокола.

## Фактические прогоны текущего дерева

Код возврата читался непосредственно после каждого вызова.

- `bash scripts/check_scoped_run.sh . ц2; echo RC=$?` → `RC=1`: внутренний вызов с пустым base дал 2.
- `bash scripts/check_scoped_run.sh . ц3; echo RC=$?` → `RC=1`: внутренний вызов с нерезолвимым ненулевым SHA дал 2.
- `bash scripts/check_scoped_run.sh . ч2; echo RC=$?` → `RC=1`: нулевая выборка по `notes.txt` дала 2.
- `bash scripts/check_scoped_run.sh . ci; echo RC=$?` → `RC=1`: у анти-плацебо-шага нет `--changed github.event.before`.
- `bash scripts/check_metering.sh --port0; echo RC=$?` → `RC=1`, и все три под-пробы фактически напечатали `FAIL`:
  `p0.gen` увидела `port:28875`, `p0.up` получила внутренний `rc=1` при healthz на порту 0,
  `p0.conc` не получила оба `.actual_port` (`r1=''`, `r2=''`).
- `bash scripts/check_metering.sh; echo RC=$?` → `RC=0`, итог: `барьер зелёный: 15 ветвей пройдены`.

До прогонов процессы были сняты. После прогонов заключительный `pkill` вернул 1: совпадений уже не было.

## Пять обходов круга 1

1. **F1 закрыт.** При прежнем стабе «исправить только игрушечный `verify_antiplacebo`, а реальный
   `ci.yml` оставить без `--changed`» новая ветвь `ci` красна: она читает реальный workflow и
   привязана к строке с `antiplacebo`, поэтому строка `check:no-rewrite` не помогает.
2. **F2 закрыт в предъявленной прежней форме.** Спецкейс только сорока нулей оставляет красными
   `ц2` (пустой base) и `ц3` (ненулевой нерезолвимый SHA); обе требуют полный прогон a и b.
3. **F3 закрыт в предъявленной прежней форме.** Спецкейс только `README.md` оставляет красной
   `ч2`, где нулевая выборка создаётся путём `notes.txt`.
4. **F4 закрыт.** Правка только прокси, который пишет `.actual_port`, оставляет красными как
   минимум `p0.gen` (обычный `gen_config` всё ещё сканирует), и `p0.up` (штатный `proxy_up` не
   читает репорт и не переписывает конфиг).
5. **F5 закрыт в предъявленной прежней форме.** Один фиксированный порт 18777 не проходит
   `p0.conc`: два одновременных прокси либо сталкиваются, либо репортят одинаковое значение;
   условие разных портов и двух healthz не выполняется.

## Новые блокирующие находки

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:51 — ветвь `ci` проверяет строку простым
регэкспом и не отличает исполняемую `run:`-команду от YAML-комментария.
ОБХОД: [INFERENCE] оставить рабочую строку `run: npm run check:antiplacebo` без `--changed`, а
рядом добавить комментарий `# antiplacebo --changed ${{ github.event.before }}`. Ветка `ci`
зелёная, `check:ci-parity` продолжает видеть прежнюю исполняемую npm-команду, но CI по-прежнему
гонит полный анти-плацебо на каждом push — предмет среза 1 не сделан.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:45 — фиксированные значения `ц`, `ц2` и
`ц3` не доказывают заявленное «нерезолвимый base ВО ВСЕХ формах».
ОБХОД: [INFERENCE] распознать только три предъявленных литерала — сорок нулей, пустую строку и
сорок единиц — и на них сделать полный прогон, сохранив exit 2, например, для нерезолвимого SHA
из сорока двоек. Все `ц/ц2/ц3` зелёные, но общий fail-safe force-push не реализован.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:48 — две фиксированные дорожки
`README.md` и `notes.txt` не доказывают общий случай резолвимого base с нулевой выборкой.
ОБХОД: [INFERENCE] возвращать 0 без прогонов только для диффов, состоящих из этих двух имён,
оставив прежний exit 2 для другого незадевающего пути, например `verdicts/x.md`. Ветки `ч/ч2`
зелёные, а обещанный общий случай нулевой выборки не реализован.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:82 — пин требует наблюдаемые
`MODE: full` и `MODE: none` одновременно от `scope_select`/`verify_antiplacebo`, но ни одна новая
ветвь не утверждает эти маркеры; более того, существующий зелёный регресс
`check_scope_select` на docs-only пока требует старые `MODE: needs-full` и RC=2.
ОБХОД: [INFERENCE] изменить только потребитель `verify_antiplacebo`: для трёх известных
нерезолвимых значений самому запускать full, а для `README.md`/`notes.txt` возвращать 0 без
прогонов; `scope_select` оставить со старым `MODE: needs-full`, и не печатать `MODE: full/none`
из потребителя. Усиленные `check_scoped_run` и прежний `check_scope_select` зелёные, но
закреплённый протокол различения не реализован.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:60 — `p0.gen` вызывается внутри особого
глобального режима `MODE=port0`, поэтому она не доказывает миграцию default-пути.
ОБХОД: [INFERENCE] в `gen_config` задать `proxy_port=0` только при `[ "$MODE" = port0 ]`, а в
режиме default сохранить `proxy_port="$(free_port)"`; поддержку репорта добавить в прокси и
`proxy_up`. `--port0` и default 15 ветвей зелёные, но штатный default по-прежнему делает
глобальный scan+TOCTOU — предмет Н-45 не выполнен.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:64 — разные ненулевые репортные порты
не доказывают, что их назначила ОС через `listen(0)`; они отсекают одну константу, но не
самовыбор разных портов.
ОБХОД: [INFERENCE] при конфигурационном `port:0` самостоятельно выбрать разные свободные порты
(например, 18777 для каталога `c1`, 18778 для `c2`, 18779 для `up`), вызвать `listen` на выбранном
числе и записать его в `.actual_port`. На машине, где эти порты свободны, `p0.gen/p0.up/p0.conc`
зелёные, но OS-назначения через bind `:0` нет, а самовыбор и TOCTOU сохранены.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:87 — точный состав яруса B записан в
пине, но ветви `ц/ч` работают на inline-селекторе игрушки, а `ci` проверяет только текст аргумента;
ни одна из них не прогоняет реальный `scope_select` на изменениях из каждого яруса.
ОБХОД: [INFERENCE] провести `--changed` в CI, но оставить реальный селектор общим для любого
файла-барьера, как сейчас. Все названные новые ветви зелёные; при правке всегдашнего барьера
яруса A, например `scripts/check_ids.sh`, анти-плацебо повторно выбирает его как scoped-код,
поэтому заявленная граница «ярус B ровно три барьера» не реализована.

АРБИТР: `arbiter` — предмет спора: усиления круга 2 закрыли пять дословных стабов, но повторно
оставили те же классы обходов (конечные литералы вместо общего класса, синтаксическая строка
вместо исполняемой CI-проводки, различие портов вместо доказательства bind `:0`) и добавили
непроверенные пины протокола. Это второй `FAIL` по этим причинам; следующий переписывающий круг
без арбитража не начинать.

## Итог

Все пять дословных обходов круга 1 стали красными хотя бы на одной новой ветви. Заморозку всё
равно блокируют перечисленные новые состояния дерева: усиленный критерий остаётся выполнимым
без нескольких заявленных частей предмета.
