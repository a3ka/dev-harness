FAIL

# Вердикт критика — контракт 007, круг 1

Предмет на HEAD закоммичен; исполнители названы, обе раздачи объявлены `ЗОНА`-строками.
Красные тесты также находятся на HEAD: срез 1 — `c414ef2`, срез 2 — `54e15cd`.

## Прогоны текущего дерева

Порядок аргументов `check_scoped_run.sh` соблюдён; код читался непосредственно после каждого
прогона.

- `bash scripts/check_scoped_run.sh . ц; echo RC=$?` → фактически `RC=1`: «нерезолвимый base дал RC=2».
- `bash scripts/check_scoped_run.sh . ч; echo RC=$?` → фактически `RC=1`: «doc-only дифф дал RC=2».
- `bash scripts/check_metering.sh --port0; echo RC=$?` → фактически `RC=1`: прокси не записал
  ненулевой фактический порт в `.actual_port`.
- `bash scripts/check_metering.sh; echo RC=$?` → фактически `RC=0`: «барьер зелёный: 15 ветвей
  пройдены».

До и после прогонов процессы `verify_antiplacebo|check_scoped_run|check_metering|metering_proxy`
были сняты; после прогонов совпадений уже не было (`pkill` вернул 1).

## Находки

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:13 — красные ветви среза 1 вызывают
`verify_antiplacebo` в игрушечном дереве, но ни одна из них не утверждает проводку
`ci.yml` с `${{ github.event.before }}`. Указанный на строках 55–58 `check:ci-parity` сверяет
наличие CI-команды и npm-пункта, а не обязательный аргумент `--changed`.
ОБХОД: исправить `verify_antiplacebo.sh` ровно настолько, чтобы все ветви
`check_scoped_run.sh` стали зелёными, но оставить `.github/workflows/ci.yml` в текущем состоянии
с `run: npm run check:antiplacebo` без `--changed`. `check:scoped-run` и структурный
`check:ci-parity` зелёные, полный CI тоже может быть зелёным, но он по-прежнему гонит полный
анти-плацебо на каждом push — предмет scoped CI-гейта не сделан.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:20 — предмет требует полный прогон для
класса «нерезолвимый/пустой base», а ветвь (ц) на строках 47–49 предъявляет только одно значение:
сорок нулей.
ОБХОД: в `verify_antiplacebo.sh` распознать буквально
`0000000000000000000000000000000000000000` и на нём запустить полный прогон, сохранив текущий
exit 2 для любого другого отсутствующего commit id и для пустого base. Ветвь (ц) и остальные
ветви барьера зелёные, но force-push с нерезолвимым ненулевым SHA и пустой base остаются без
обещанного fail-safe.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:21 — предмет задаёт общий случай
«резолвимый base и пустая выборка яруса B», а ветвь (ч) на строках 50–52 создаёт только правку
корневого `README.md`.
ОБХОД: вернуть 0 без прогонов только когда diff состоит из `README.md`, оставив needs-full/exit 2
для другого пути с нулём задетых барьеров, например `verdicts/x.md`. Ветвь (ч) зелёная и
существующая ветвь выбора barrier-кода не затронута, но заявленный общий случай нулевой выборки
не реализован.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:27 — предмет среза 2 состоит в том, что
сам обычный `check_metering` перестаёт пользоваться глобальным `free_port`-сканом. Красная ветвь
на строках 61–66 отдельно переписывает сгенерированный конфиг на `port: 0` и запускает прокси
напрямую; она не утверждает, что основной пятнадцативетвевой путь `check_metering` перешёл на
этот протокол. Фактический регресс-прогон выше уже зелёный при нынешнем скане.
ОБХОД: изменить только `scripts/proxy/metering_proxy.ts`, чтобы после существующего `listen(0)`
он записывал `server.address().port` в `.actual_port`, не меняя `gen_config`, `proxy_up` и
обычный режим `check_metering.sh`. `--port0` станет зелёным, нынешний default останется зелёным,
но рабочий путь продолжит делать глобальный scan+TOCTOU — корень Н-45 не устранён.

БЛОКИРУЕТ contracts/007-scoped-ci-i-deflake-metering.md:61 — ветвь порт-0 проверяет только, что
репорт — ненулевое число и что на нём отвечает healthz; она не различает OS-назначение через
`bind :0` и самостоятельно выбранный порт.
ОБХОД: при конфигурационном `port: 0` выбрать в прокси константу (например, 18777), вызвать
`listen(18777)`, записать `18777` в `.actual_port` и отвечать на healthz. На свободной машине
ветвь `--port0` зелёная, хотя OS-эфемерного назначения нет и коллизия/TOCTOU, ради которых введён
срез, остаются.

СОВЕТ contracts/007-scoped-ci-i-deflake-metering.md:89 — точный список яруса B оставлен ссылкой
на недоступный из HEAD `agent://GateScout` и одновременно назван «незаполненным требованием».
Перед заморозкой лучше записать классификацию в контракт или в названный коммитный артефакт;
иначе исполнитель сам определит границу «читает данные».

## Зоны и итог по срезам

Границы строк 33–34 связны: предмет и CI-проводка среза 1 входят в зону `implementer`, его
проверка — в зону `architect`; предмет, прокси и проверка среза 2 — в зоне `architect`.
Файла, необходимого заявленному предмету и не имеющего исполнителя/зоны, не найдено; обхода зоны
нет.

- **Срез 1: FAIL** — найдены обходы CI-проводки и двух обобщённых классов входа.
- **Срез 2: FAIL** — найдены обходы миграции обычного пути и механизма OS-назначения порта.
