FAIL

# Критик 035, круг 1

Предмет: закоммиченные `contracts/035-zhnets-tmp.md` и `fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh` на HEAD `93ccf261f2fb472577715c429aee08ddf92eeb79`. Суд выполнен в отдельном клоне. Набор артефактов полный: предмет, команды приёмки, исполнители и ЗОНА-строки есть.

Предмет НЕ пустой. Фикстура-страж, живое предъявление существующего механизма при close-out и закрытие наблюдения — законная работа без изменения кода жнеца. Требовать реализации В2 вместо выбранного В1 оснований нет. Отказ ниже — из-за недоказуемости части объявленного завершения и прямого противоречия норме, не из-за выбора В1.

## Блокирующие находки

БЛОКИРУЕТ contracts/035-zhnets-tmp.md:87–89,104–111 — обязательное закрытие Н-97 в NABLIUDENIA.md отсутствует в проверяемом приёмочном критерии. Для перехода ОТКРЫТО → ЗАКРЫТО и ссылки на прогон не названа команда, которая возвращает ненулевой код при отсутствии результата. Строка census в HANDOFF тоже остаётся комментарием к вызову GC: сам GC её не проверяет.
ОБХОД: оставить раздел Н-97 в NABLIUDENIA.md побайтно как на HEAD — со статусом ОТКРЫТО и без ссылки на предъявление 035; честно выполнить все три проверки фикстур, живые dry-run/apply, удалить ровно два draft020-файла и даже записать требуемый census в HANDOFF. Все перечисленные команды возвращают 0, все перечисленные при live apply файловые постусловия выполнены, но пункт 3 предмета — закрытие наблюдения — не сделан. Три команды фикстур уже прошли на дереве с открытым Н-97; команды GC и новая фикстура не обращаются к NABLIUDENIA.md/HANDOFF.

Требуется до заморозки назвать исполняемую проверку результата close-out: она должна отвергать незакрытый Н-97, отсутствие ссылки на предъявление и отсутствие требуемой записи HANDOFF. Выполнять сам close-out до заморозки не требуется: речь о полноте будущего критерия, а не о преждевременной реализации. Новый постоянный барьер ради этого не обязателен.

БЛОКИРУЕТ contracts/035-zhnets-tmp.md:84–85 — §Предмет закрепляет в прозе пару «стаб 358b12a~1 ↔ слепота на done-записях». Это именно привязка стаба к входу, где объявлен его дефект; AGENTS.md:130–133 прямо запрещает такую привязку в контракте и относит её в код проб. То, что привязка также присутствует в коде фикстуры, не отменяет запрета дублировать её в контракте.

Основание блокировки — прямое противоречие AGENTS.md; по порогу роли отдельный ОБХОД для этого исключения не требуется. Достаточно оставить в §Предмет требование стаб-предъявления посредством названной фикстуры, а конкретную привязку блоба к различающему входу оставить в её коде. Прочую frontier-прозу этим замечанием не блокирую.

## Советы, не условия приёмки

СОВЕТ contracts/035-zhnets-tmp.md:84–85 — различать два результата исторического предъявления. Нынешняя фикстура вызывает пред-026 скрипт с неизвестным ему `--tmp-reap-apply`: мой прогон получил rc 1 с диагнозом «неизвестный аргумент», после чего всё сохранилось. Дополнительный независимый вызов старого скрипта без этого флага получил rc 0 и также сохранил всё. Следовательно, сам frontier-вывод верен, но первая процедура доказывает отказ старого интерфейса, а не исполнение старой ветви GC. Это не отдельный блокер: положительное поведение текущего механизма предъявлено живыми прогонами.

СОВЕТ contracts/035-zhnets-tmp.md:94 — пояснение в скобках лучше вынести со строки ЗОНА на отдельную строку. `lib_zones.sh` разбивает весь хвост после двоеточия на пути, не выделяя комментарий в скобках. Нужные NABLIUDENIA.md и HANDOFF.md объявлены корректно; недостатка зоны для заявленной работы нет, но пояснение дополнительно становится набором фиктивных путей.

## Независимые прогоны

1. `bash fixtures/gc_agent_branches/case_zhnets_ostatok_035.sh` → rc 0.
2. `bash scripts/verify_antiplacebo.sh --scope gc_agent_branches/case_zhnets_ostatok_035` → rc 0; одна фикстура, зелёный контроль и повторное красное rc 1 «не удалось удалить».
3. `bash scripts/verify_antiplacebo.sh --scope gc_agent_branches` → rc 0; восемь фикстур, восемь повторных красных предъявлений. Полный проектный прогон не запускался.
4. Независимая Python-проба, без использования каркаса новой фикстуры, создала две игрушки с пятью непустыми записями и всеми семью пустыми каталогами. В отличие от каркаса архитектора, в пробе 026 помечен done, как в предъявленном состоянии проекта; 020 done, 032 активен.
   - Текущий dry-run → rc 0; ровно `tmp/draft020-task.md done 020` и `tmp/fix5-draft020-task.md done 020`; содержимое файлов неизменно.
   - Текущий apply → rc 0; удалены только эти две записи; SHA-256 трёх выживших файлов совпадают; семь пустых каталогов сохранены: 7 → 7.
   - Пред-026 блоб с новым флагом → rc 1 «неизвестный аргумент»; все пять файлов и семь пустых каталогов сохранены.
   - Тот же пред-026 блоб с поддерживаемым вызовом `--root` → rc 0; все пять файлов и семь пустых каталогов сохранены.
5. Независимый read-only census основного чекаута обнаружил ровно семь пустых верхнеуровневых каталогов: barrier005, check_approval, ci_parity, coldstart005, critic-tmp, fix005limits, scratch. Вложенных файлов в них нет: 0 байт файлового содержимого. Оба draft020-файла пока присутствуют. Это не утверждение о нулевой стоимости метаданных каталогов на диске.

Историческая точка «7» прочитана из замороженного `frozen/contracts/026/4`; сегодняшняя точка «7» измерена независимо. Непрерывную неизменность между датами такой замер не доказывает. Переход 7 → 7 при apply непосредственно воспроизведён на независимой игрушке. Живой apply в основном чекауте не выполнялся: это последующий close-out оркестратора, а не действие критика до заморозки.

## Ответы по границам и нормам

- Все файлы заявленной работы имеют названных исполнителей: архитектор — контракт и новая фикстура; оркестратор — NABLIUDENIA/HANDOFF; судьи — свои каталоги вердиктов. Границы держит существующий механизм зон. Правка GC для В1 не нужна и прямо исключена.
- В2 явно оставлен развилкой владельца и не раздаётся исполнителю. В3 также вне предмета. Молчаливой реализации жнеца пустых каталогов в 035 не требуется.
- С замороженным 026 выбор В1 совместим: источник 026 не видит пустые каталоги, и они прямо объявлены его не-целью с отдельной развилкой по боли. Закрытие остаточной боли не отменяет Б5(i), правила квалификации, защиту tracked или REGRESS-024.
- `git diff 14fa1af^ 93ccf26 -- scripts/gc_agent_branches.sh scripts/verify_antiplacebo.sh AGENTS.md` пуст: эти защищённые поверхности предъявленная пачка не меняет.
- Противоречие AGENTS.md в пределах судимого §Предмет названо выше; утверждений о печати PASS либо об изменении правила 16 контракт не вводит.

Итог: В1 инженерно обоснован и прогонно подтверждён. Заморозку блокируют только два указанных дефекта текста контракта. Реализацию В2, новый жнец или изменение замороженного 026 для их устранения не требую.
