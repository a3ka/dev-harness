FAIL

# Контракт 040 v3 — incorrect: неполное исполнение решения арбитража

Предмет: `contracts/040-batching-git-vyzovov.md`, коммит текста
`f5e04f28ee10b59a89dde1b631d22850c5fcd090`, проверенный HEAD
`1838c5f99120d91313db0a71fdb7cd32721c8b54`. Пин красной пачки:
`f0b6de662d4826ed1707f2b97fef1c8aeeb06435`.

Набор полон: предмет, исполнимая приёмка, названные исполнители и ЗОНА-строки
закоммичены. Целиком прочитаны арбитраж
`verdicts/arbitration/040-batching-kriterii-i-tehnika.md` (`5c24017`), контракт
v3 и все три переписанных исполняемых файла приёмки. Прочитана норма
`AGENTS.md` проверенного дерева. Работа выполнена в собственных полных клонах.

**Граница вердикта:** инженерные решения Б1–Б4 принимаются без пересмотра.
Ни нового порога S/Δ, ни нового способа пинования, ни обещания «каждый будущий
CI-прогон», ни другой техники извлечения путей этот вердикт не требует.
Текст v3 переносит решения арбитра; однако запиненная исполняемая Р9 не
выполняет два уже обязательных требования Б3. Это **несоответствие исполнению
решения**, а не новое основание спора о самом решении. Новых блокеров вне
Б1–Б4 не установлено.

## 1. Сверка с обязательным текстом арбитража

| Решение | Место v3 | Результат |
|---|---|---|
| Б1: `C_high − C_low <= S`, `Δ >= 2·S` для ОБОИХ Р1/Р2; одинаковое увеличение нагрузки во всех окнах, контроль на обеих нагрузках; абсолютный предел вторичен | строки 112–145, 369–370 | Перенесено. Оба файла реально строят LOW/HIGH. Р1: K=12, M=60, L=5/35, S=15; Р2: M=60/360, S=15. Право менять грубые константы не распространено на свободное ослабление S/Δ. |
| Б2: восемь namespace-glob, включая `red_*`, `probe_*`, `stab_*`, `_*` обеих семей; MD/no-renames для case; заморозка SHA пина; замыкание приёмки | строки 282–313, 371–372, 376 | Все восемь литералов совпали с арбитражем. Пин охватывает правку и добавление; собственные отрицательные опыты ниже. Ограничение замыкания остаётся читаемой нормой ровно по решению арбитра. |
| Б3, часть 1: точная цитата ограниченного обещания | строки 213–221 | Совпала пословно; при машинной сверке нормализованы только переносы строк цитаты. |
| Б3, часть 2: пять требований fail-closed предиката и отдельный пункт о локальном разборщике | строки 238–260 | В тексте все требования присутствуют. В запиненной Р9 пункты 2 и 3 исполнены неполно: находки И1/И2 ниже. |
| Б3, часть 3: одноразовое наблюдение SHA/CI-run в HANDOFF как условие done, НЕ фикстура/постоянный сетевой страж | строки 610–643 | Перенесено; исполнитель — orchestrator, HANDOFF входит в его зону. Согласованную немеханизированную границу не переоткрываю. |
| Б4: только `rev-list HEAD | diff-tree -r --root -m --no-renames {--raw|--name-only} --stdin`; обязательные литералы; снятие старой поправки 1; постоянный вход дубликат-блоба | строки 154–193, 266–281, 370 | Перенесено. Старой действующей нормы о `rev-list --objects --full-history` больше нет; упоминания описывают отвергнутую технику. Все три подтверждения техники отдельно исполнены, rc=0. |

## 2. Два точных несоответствия исполнению Б3

### И1 — не исполнен отказ по continue-on-error на джобе

БЛОКИРУЕТ contracts/040-batching-git-vyzovov.md:250 — Р9 строки 377 принимает `continue-on-error: true` на джобе, хотя уже принятое решение `verdicts/arbitration/040-batching-kriterii-i-tehnika.md:269` буквально требует «на шаге или джобе → rc=1».
ОБХОД: добавить оба настоящих npm-ключа и оба отдельных канонических шага в джобу с `continue-on-error: true`. Не менять ни один из трёх оракулов и ни один барьер. Полная команда Р9 даёт **rc=0**, включая настоящий `verify_ci_parity.sh` rc=0, вместо обязательного отказа. Это позволяет принятой проводке терпеть отказ джобы с call-budget шагами, хотя согласованный предикат обязан такую проводку отвергать.

Причина точно локализована в запиненной приёмке:
`fixtures/check_zones/probe_ci_dostizhimost_predelov.sh:258` сохраняет у джобы
только `if`, `needs`, `steps`; проверка строк 299–302 читает
`continue-on-error` только у шага. Требование не оспаривается и не усиливается:
его джобовая половина просто не реализована.

### И2 — не исполнен отказ по if в needs-замыкании при блочном списке

БЛОКИРУЕТ contracts/040-batching-git-vyzovov.md:244 — Р9 строки 377 теряет зависимости из блочного `needs:` и принимает джобу, чьё замыкание содержит `if: false`, вопреки уже принятому `verdicts/arbitration/040-batching-kriterii-i-tehnika.md:261–268`.
ОБХОД: подключить два канонических шага к джобе с `needs:` в виде блочного списка `- critic040_upstream`, а у `critic040_upstream` поставить `if: false`. Полная неизменённая Р9 даёт **rc=0**, хотя обязана отказать по самому наличию `if:` в замыкании. Замена только записи зависимости на `needs: [critic040_upstream]` приводит к ожидаемому отказу структурного предиката. Нового выражения `if:` вычислять не требуется.

Причина: `fixtures/check_zones/probe_ci_dostizhimost_predelov.sh:240–241`
разбирает скалярные элементы `needs` функцией последовательности отображений,
затем заменяет нестроковые элементы на `''`; строки 265–267 считают такую
несуществующую зависимость не несущей `if:`. Это молчаливое сужение уже
объявленного транзитивного замыкания, не спор о достаточности замыкания.

### Полное воспроизведение обоих несоответствий

В отдельном клоне проверенного HEAD изменены ТОЛЬКО `package.json` и
`.github/workflows/ci.yml`. Добавлены настоящие значения scripts:

```json
"check:zones-call-budget": "bash fixtures/check_zones/red_predel_git_vyzovov.sh",
"check:protected-call-budget": "bash fixtures/check_protected/red_predel_git_vyzovov_ours.sh"
```

К существующему `jobs:` добавлялся один из двух вариантов; существующие
джобы, три запиненные пробы, `check_zones.sh`, `check_protected.sh` и
`verify_ci_parity.sh` оставались исходными.

Вариант И1:

```yaml
  critic040_budget:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - run: npm run check:zones-call-budget
      - run: npm run check:protected-call-budget
```

Вариант И2:

```yaml
  critic040_upstream:
    runs-on: ubuntu-latest
    if: false
    steps:
      - run: npm run check:ci-parity
  critic040_budget:
    runs-on: ubuntu-latest
    needs:
      - critic040_upstream
    steps:
      - run: npm run check:zones-call-budget
      - run: npm run check:protected-call-budget
```

В ОБОИХ вариантах выполнено:

```
bash fixtures/check_zones/probe_ci_dostizhimost_predelov.sh .
```

В ОБОИХ случаях rc=0 и настоящий вывод:

```
CI-ДОСТИЖИМОСТЬ ДЕРЖИТСЯ (СТРУКТУРНО, fail-closed): check:zones-call-budget -> джоба critic040_budget, rc(npm)=1=rc(прямой)=1; check:protected-call-budget -> джоба critic040_budget, rc(npm)=1=rc(прямой)=1; безусловный триггер push/pull_request; verify_ci_parity rc=0
```

Длительности полных воспроизведений: 230.63 с и 274.91 с. Равенство двух
единиц само по себе **не находка**: Р1/Р2 требуют нули отдельно. Здесь не
утверждается, что ещё не исправленный HEAD прошёл всю после-реализационную
приёмку. Проверено ровно нарушение обязательного предиката Р9; будущая
реализация батчинга для этого опыта не подменялась диагностическим кодом.

Отдельно исполнен извлечённый БЕЗ ИЗМЕНЕНИЙ Python-блок структурного
предиката: канон → `OK`; `if:` шага, `if:` джобы, `if:` в flow-списке needs,
step-level continue-on-error, `|| true`, `; true`, flow-steps и YAML-якорь →
`FAIL`. Только два предъявленных варианта выше дали ошибочное `OK`.
Это не повтор отвергнутого старого регулярного выражения: проверялся новый
запиненный структурный разборщик.

## 3. Собственный прогон всей текущей приёмки

| Пункт | Наблюдение на неисправленном HEAD |
|---|---|
| Р1 | rc=1; LOW=1696, HIGH=6376, грубая граница 452. Нарушение agent03 поймано на ОБОИХ деревьях. Разность наблюдённых счётчиков 4680 > 15. |
| Р2 | rc=1; LOW=151, HIGH=751, грубая граница 30. Ours-пропажа и дубликат-блоб-пропажа пойманы на ОБОИХ деревьях. Разность 600 > 15. |
| Р3 | `verify_antiplacebo --scope check_zones` rc=0: 21 фикстура, 21 предъявлена красной повторным прогоном. Дословный MD/no-renames diff к пину rc=0. |
| Р4 | `verify_antiplacebo --scope check_protected` rc=0: 20 фикстур, 20 предъявлены красными повторным прогоном. Дословный MD/no-renames diff к пину rc=0. |
| Р5 | `bash scripts/check_zones.sh .` rc=0, 59.89 с; 37 замороженных контрактов, 1567 коммитов в диапазонах, 1034 проверено по зонам. |
| Р6 | `bash scripts/check_protected.sh .` rc=0, 38.63 с; пройдено 1964 коммита, необъяснённых исчезновений 0. |
| Р7 | `timeout 60 bash scripts/check_zones.sh .` rc=124, 60.10 с. Это ДО реализации, не новый блокер и не пересмотр закрытой сноски о зависимости wall-clock от окружения. |
| Р8 | Дословная команда с восемью glob к HEAD rc=0. |
| Р9 | На исходной проводке rc=2, `NOT_IMPLEMENTED`: оба npm-ключа ещё отсутствуют. |
| ПРОВОДКА | `bash scripts/check_provodka.sh . contracts/040-batching-git-vyzovov.md` rc=0. |

Р1/Р2 штатно выходят на грубом пределе до исполнения решающего assert;
указанные разности вычислены из реально напечатанных LOW/HIGH, а не выданы
за выполненный после раннего выхода участок. Три живых подтверждения Б4
исполнены отдельно дословным хвостом Р2 на его отдельном toy: старая техника
теряет дубликат-блоб, отсутствие `--root` теряет корневой путь, отсутствие
`-m` теряет путь, рождённый в merge; итог rc=0. Порогов в исходных файлах
для этого не менял.

Не-ослабление независимо проверено обходом файлов: 21/20 `case_*.sh`;
совпало с независимым счётом scoped-раннера. Прямой diff пяти затронутых/
связанных барьеров к пину красной пачки пуст, rc=0. Полный проектный прогон
не запускался.

Для точности журнала: первоначальный параллельный scoped-прогон был
недостоверен из-за моих соседних запусков реальных барьеров, создающих
`tmp/zones.*`/`tmp/protected.*`; конкурентный второй scope отказал lock-ом.
Эти результаты не засчитаны. Два последующих запуска застряли на FIFO
обёртки раннера и были остановлены. Итоговые зелёные scoped-прогоны
выполнены отдельно без конкурирующих писателей; для protected задан свежий
`VERIFY_ANTIPLACEBO_SCRATCH=/tmp/dev-harness-verify/critic040v3/scoped`.
Файлы барьеров/фикстур не менялись; причина зависания не устанавливалась
и контракту 040 не приписывается.

### Пин испытан изменениями, а не чужим свидетельством

Из исходного HEAD через отдельный Git-индекс построены изменённые tree-объекты
без правки checkout. Применена та же команда diff с тем же architect-пином
и теми же восемью glob; вторым аргументом вместо исходного HEAD служил
соответствующий изменённый tree:

- добавление нового файла поочерёдно в КАЖДОЕ из восьми пространств → rc=1
  во всех восьми опытах;
- замена КАЖДОГО из трёх переписанных оракулов на `exit 0` → rc=1;
- для КАЖДОЙ из двух case-семей: добавление нового case → MD/no-renames
  rc=0; правка, удаление, переименование существующего → rc=1.

Таким образом, Б2 не переоткрывается: механизм реально ловит и правку,
и добавление оракулов, сохраняя согласованное право добавлять case-файлы.

## 4. Итог по пяти вопросам роли и выход

1. Достаточность согласованных дифференциальных критериев Б1 не
   пересматривается. Оракул Р9 слабее уже согласованного предиката в двух
   конкретных местах И1/И2.
2. Команды приёмки названы и исполнены; текст Б3 не доказан своей Р9 именно
   в этих двух местах. Одноразовое CI-наблюдение оставлено отдельным
   условием done согласно арбитру, а не превращено в новую фикстуру.
3. Нового блокирующего решения, молча оставленного исполнителю, не найдено;
   выбор npm-имён явно отдан implementer, техника и обязательные флаги названы.
4. Исполнители и границы названы; пересечение fixture-зон удерживается
   namespace-пином и отдельным не-ослаблением case, оба испытаны собственным
   прогоном. Документирование done-наблюдения входит в зону orchestrator.
5. Нового самостоятельного противоречия AGENTS.md вне уже разрешённых
   арбитражем вопросов не установлено.

**Выход — исправить исполнение Б3, часть 2, пункты 2 и 3, в architect-owned
приёмке и обновить её пин установленным порядком.** Содержание решения
арбитра не меняется. Несогласия с арбитром и нового основания арбитража нет;
второй спор о достаточности критерия этим вердиктом НЕ созывается.
Порог завершения этой проверки — обе предъявленные формы дают требуемый
rc=1, каноническая форма остаётся допустимой; расширения предмета сверх
буквально принятого Б3 не требуется.
