FAIL

Судимый предмет: `contracts/004-potolki-dokumentov.md` на HEAD `4593b07525e6eab43c0f604b2fb9e8175fb7d223`, первая заморозка контракта 004.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:13,29-31 — предмет разрешает файл на самом потолке (`≤`), но красная матрица предъявляет только превышение и не проверяет зелёную границу. Названные прогоны не отличают `>` от `≥` в реализации барьера.
ОБХОД: `check_ceilings.sh` отвергает размер `-ge 30720` для правила и `-ge 51200` для персоны; текущие файлы зелёные, фикстуры размером 30721 и 51201 байт красные, все объявленные команды проходят, но файлы ровно 30720 и 51200 байт запрещены вопреки `≤`.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:14,17,29-31 — предмет включает две разные ветви файлов-правил (`AGENTS.md` и будущие `.omp/rules/`), а критерий требует одно безымянное красное «правило >30КБ». Он не доказывает обход всех подходящих путей.
ОБХОД: барьер проверяет только `AGENTS.md`, а красная фикстура раздувает только его; текущий прогон и все три обязательные красные фикстуры проходят, но появившийся `.omp/rules/x.md` любого размера игнорируется, хотя предмет относит его к файлам-правилам.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:22-24,29-31 — предмет запрещает пустой раздел и требует список либо слово «нет», но критерий краснеет только при полном отсутствии заголовка. Формальная честность формы поэтому не доказана названной командой.
ОБХОД: барьер делает только `grep -q '^## Незаполненные требования:$'`; красная фикстура без заголовка возвращает 1, текущий контракт возвращает 0, `check:antiplacebo` проходит, однако контракт с заголовком и сразу следующим разделом принимается с пустым списком.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:22-24,29-31 — область «контрактов, идущих к заморозке» не определена механически, а команда не получает путь замораживаемого контракта. Старые 001–003 не имеют требуемого раздела и заморожены, поэтому исполнитель должен сам решить, какие файлы исключать; критерий не отличает общее правило для будущих контрактов от проверки только 004.
ОБХОД: барьер зашивает `contracts/004-potolki-dokumentov.md`, а фикстура удаляет заголовок именно из копии 004; все названные команды и красное проходят, но следующий `contracts/005-x.md` без раздела игнорируется и может идти к первой заморозке.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:38-48 — `check:skills` требует полного равенства `skills/` и `.agents/skills/`, поэтому строка в шапке требует второй правки `.agents/skills/writing-for-agents/SKILL.md`. Этого файла нет ни в одной зоне 004. Ветка (е) замороженного контракта 003 совместима с новой строкой: она сравнивает тело от первого `---`, а шапку оставляет адаптации; блокирует не (е), а ветка (д), требующая точного зеркала. После `done/contracts/003/1` прежняя зона implementer закрыта. Это противоречит `AGENTS.md:109-123`: исполнитель коммитит только в свою зону.
ОБХОД: обязательную зеркальную правку коммитит автор с необъявленным `user.name` (например, `mirror-sync`); `check:skills` и grep зелёные, а действующий `check:zones` судит только авторов из `ЗОНА`-строк и также возвращает 0. Все критерии выполнены, но необходимый файл изменён вне границ любого названного исполнителя.

СОВЕТ contracts/004-potolki-dokumentov.md:13,29-31 — зафиксировать потолки в байтах: сейчас «КБ» оставляет исполнителю выбор 30000/50000 против 30720/51200. После обязательных зелёных и красных граничных случаев это решение всё равно придётся принять; совет сам по себе вердикт не блокирует.

Проверены пять обязательных вопросов. (1) Критерий слабее предмета: названы обходы по границе, составу файлов-правил, непустоте и области контрактов. (2) Недоказуемые названными командами части — те же четыре предиката; остальные пункты имеют команды с кодами возврата. (3) Исполнителю молча оставлены единица байта, область контрактов и грамматика непустого списка. (4) Машинные `ЗОНА`-строки есть и внутри 004 не пересекаются; открытый 001 работу не раздаёт, 002 и 003 закрыты done-тегами, но обязательный файл зеркала не покрыт. (5) Печати `PASS` и счётной самопроверки в контракте нет; противоречие норме границ указано выше.

Независимые замеры: `wc -c roles/*.md | sort -nr` подтверждает крупнейшую персону `roles/critic.md` — 16919 байт. `bash scripts/check_zones.sh` на судимом HEAD возвращает 0 и подтверждает закрытие 002/003; реестр не содержит `frozen/contracts/004/*`. Теги не создавались.
