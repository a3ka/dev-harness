FAIL

Судимый предмет: `contracts/004-potolki-dokumentov.md` на HEAD `a4d310a9988a441e480da998b7c073d4ee8414f4`, шестая итерация до первой заморозки. Круг сверх капа разрешён владельцем 2026-08-19. Тегов `frozen/contracts/004/*` нет; предмет закоммичен, содержит предмет (`contracts/004-potolki-dokumentov.md:7-30`), критерий готовности и команды с кодами возврата (`:33-62,76-79`), двух названных исполнителей и две `ЗОНА`-строки (`:65-73`). Набор артефактов полный.

История кругов: первый вердикт `c6b8fa3` судил ранний черновик `4593b07`; второй `ca17e59` судил вторую итерацию `0f49a3b`; обязательный арбитраж `verdicts/arbitration/potolki-granica-pokrytie.md` (`eae8cf3`) разрешил повтор причин первых двух кругов; третий вердикт `00a8b65` судил третью итерацию `57ed01b` и предъявил неполную грамматическую матрицу и недоказанное исключение замороженных контрактов; четвёртый `3e00517` судил итерацию `b362177` и предъявил потерянную независимость от имён; пятый `9858c69` судил итерацию `a9bb6c8` и предъявил не связанную с входом диагностику отказа.

Правка шестой итерации закрывает находку пятого круга по существу. `git diff a9bb6c8..a4d310a -- contracts/004-potolki-dokumentov.md` содержит только три добавленные строки `contracts/004-potolki-dokumentov.md:50-52`: свойство матрицы требует от каждого красного исхода назвать имя входного файла и его фактический размер в байтах. Поэтому прежний обход — постоянный вывод `нарушение` и такая же `# ПРИЧИНА` — больше не удовлетворяет критерию. Так как правка только добавляет это свойство, прежние строки матрицы ею не потеряны.

Обязательный минимум арбитража `potolki-granica-pokrytie` повторно сверён по всем семи пунктам: (1) байты, запрет символьного счёта и различающая кириллическая проба — `contracts/004-potolki-dokumentov.md:13-17,42-43`; (2) 51200/30720 байт — `:13-15`; (3) потолок и ровно потолок+1 — `:40-41`; (4) пара во всех ветвях `roles/`, `AGENTS.md`, `.omp/rules/` — `:40-41`; (5) порождаемые при каждом прогоне имена красных проб каждого класса и запрет фиксированных имён — `:44-46`; (6) механическая грамматика, полная матрица пяти исходов и собственное каноническое `нет` — `:25-30,47-49,81-82`; (7) самодостаточный `test "$(grep -c …)" -eq 1` — `:78`. Все семь пунктов сохранены.

Полная заявленная матрица также повторно пройдена по тексту: шесть граничных исходов находятся в `contracts/004-potolki-dokumentov.md:40-41`; различающая кириллица — в `:42-43`; три класса красных проб со случайными именами — в `:44-46`; пять исходов грамматики — в `:47-49`; проверка диагностики каждого красного исхода — в `:50-52`; зелёная замороженная проба с подставным тегом — в `:53-55`; красные отсутствие раздела и иное тело продублированы в `:56-57`. При этой повторной проверке обнаружен один сохраняющийся обход замороженной ветви.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:25-30,44-46,53-55 — предмет исключает из проверки КАЖДЫЙ контракт, для номера которого существует тег `frozen/contracts/<NNN>/*`, но зелёная замороженная проба требует лишь «подставной» тег, не имя/номер, порождаемый при каждом прогоне. Случайность `:44-46` относится только к красной пробе незамороженного контракта. Поэтому названные команды не доказывают универсальность чтения тегов: номер зелёной пробы известен из фикстуры и может быть добавлен к фиксированному списку исключений. Это тот же класс плацебо, который `AGENTS.md:162-163` запрещает принимать без красного предъявления против обманной заглушки.
ОБХОД: фикстура всегда создаёт, например, `contracts/999-proba.md` с телом вне грамматики и тег `frozen/contracts/999/1`. Барьер не использует теги для общего решения: он исключает известные замороженные 001–003 и номер 999, а любой другой контракт проверяет по новой грамматике. Он проходит пять исходов грамматики, случайно именованную красную незамороженную пробу, зелёную пробу 999, новое требование к имени и фактическому размеру всех красных исходов и остальные названные команды. Но `contracts/888-novyj.md` с таким же невалидным телом после постановки тега `frozen/contracts/888/1` остаётся красным, хотя предмет прямо выводит его из суда.

Пять обязательных вопросов проверены. (1) Критерий слабее предмета в конкретном состоянии дерева из `ОБХОД`. (2) Универсальное исключение по тегам недоказуемо названными командами без порождаемого номера зелёной замороженной пробы; остальные пункты предмета связаны с кодами полной матрицы и командами `bash scripts/check_ceilings.sh`, `npm run check:antiplacebo`, `check:ci-parity`, `check:skills`, `check:gen`, `check:zones`, `check:contract-frozen` и самодостаточной grep-командой. (3) Исполнителю молча оставлен выбор фиксированного номера подставного замороженного контракта; иных новых молчаливых решений в предмете, критерии и раздаче не найдено. (4) `ЗОНА`-строки `:67-68` называют architect и implementer и покрывают все прямо требуемые файлы; `bash scripts/check_zones.sh` на судимом HEAD вернул 0. (5) Печати `PASS`, порога без области и счётного утверждения, проверяемого той же мерой, не найдено; фиксированная зелёная проба допускает противоречие норме красного предъявления `AGENTS.md:162-163`, как указано в находке.

АРБИТР: `arbiter` — это второй `FAIL` по причине «критерий не доказывает исключение каждого замороженного контракта»: первый был в вердикте `00a8b65`, а его заявленное закрытие подставным тегом не устраняет фиксированный известный номер. Предмет спора: достаточно ли одного подставного, но фиксируемого фикстурой номера для доказательства чтения `frozen/contracts/<NNN>/*`, либо зелёная замороженная проба обязана порождать номер при каждом прогоне так же, как красная незамороженная проба.

СОВЕТОВ нет.
