FAIL

Судимый предмет: `contracts/004-potolki-dokumentov.md` на HEAD `0f49a3b61ab0109bf4a1a94adc2c768ad94e9f95`, вторая итерация текста до первой заморозки. Этот файл заменяет вердикт по раннему черновику `4593b07` из коммита `c6b8fa3`; реестровая версия ещё не выдана.

Пять прежних находок сверены с исправлением. Прямой обход через `≥` частично закрыт заявлением порога и зелёными границами (`contracts/004-potolki-dokumentov.md:31-37`), обе ветви файлов-правил теперь названы отдельно (`:35-36`), пустой раздел получил отдельную фикстуру (`:38-39`), область незамороженных контрактов определена тегами (`:22-26`), а зеркало одновременно названо вечной обязанностью и включено в зону architect (`:40-42,49,52-55`). Последняя находка закрыта полностью. Первые четыре исправления устраняют дословные старые стабы, но критерий всё ещё пропускает состояния ниже.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:13,18,31-37 — потолки заданы как размер в КБ и контрольный замер дан в байтах, однако граничные фикстуры не закрепляют байтовый счётчик и многобайтовый вход. Они не отличают `wc -c` от счёта символов.
ОБХОД: `check_ceilings.sh` считает `wc -m`; все граничные и сверхграничные фикстуры построены из ASCII, поэтому дают объявленные коды, текущие русскоязычные файлы остаются зелёными и все названные команды проходят. Новый файл `roles/x.md` из 30000 кириллических символов занимает около 60000 байт, но принимается как меньший 50 КБ — байтовый потолок не удержан.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:17,31-37 — описание сверхграничных фикстур говорит лишь `>50КБ`/`>30КБ`, а единственная зелёная граница правила не привязана к обеим ветвям. Поэтому названная матрица не обязана проверять первый запрещённый байт и отдельное сравнение на границе `.omp/rules/`.
ОБХОД: фикстуры «сверх» создают файлы на два байта выше потолка, `case_pravilo_na_granice.sh` проверяет только `AGENTS.md`; барьер ошибочно разрешает потолок+1 для персон и использует правильное `>` для `AGENTS.md`, но `≥` для `.omp/rules/`. Ровно-на-потолке персона и `AGENTS.md` зелёные, обе далёкие сверхграницы красные, все перечисленные исходы и команды выполнены, однако персона на потолке+1 принята, а `.omp/rules/y.md` ровно на потолке отвергнут.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:14,17,22-25,31-39 — предмет квантифицирован по каждому подходящему пути, но критерий фиксирует `.omp/rules/x.md` и не требует ни одного случая с иным именем; внутренние имена персон и контрактов в фикстурах вообще оставлены их автору. Критерий не отличает обход ветви от барьера, зашитого на известные пути проб.
ОБХОД: барьер проверяет только `AGENTS.md`, `.omp/rules/x.md`, выбранный фикстурами `roles/x.md` и контракт 004; две контрактные фикстуры портят копию 004. Все семь поимённых фикстур, текущий чистый прогон, antiplacebo и CI проходят. Следующие `.omp/rules/y.md`, `roles/new.md` и незамороженный `contracts/005-x.md` игнорируются, хотя предмет включает каждый из них.

БЛОКИРУЕТ contracts/004-potolki-dokumentov.md:24-25,38-39 — предмет допускает только непустой список либо ровно `нет`, но красные случаи различают лишь отсутствие раздела и совершенно пустой раздел. Невалидное непустое тело командой не отвергается, а грамматика списка исполнителю не задана.
ОБХОД: барьер принимает любую непустую строку после заголовка; обе названные контрактные фикстуры красные, текущий контракт зелёный и antiplacebo проходит. Черновик с телом `пока не решили` или `нет.` принимается, хотя это не список и не ровно слово `нет`.

СОВЕТ contracts/004-potolki-dokumentov.md:13,33-37 — записать численные потолки в байтах (30000/50000 либо 30720/51200), а не оставлять исполнителю выбор значения «КБ». Это отдельная неоднозначность предмета; она не добавляет к вердикту ещё одного обхода сверх блокирующей разницы байты/символы.

СОВЕТ contracts/004-potolki-dokumentov.md:60 — `grep -c` кодом возврата различает только ноль и неноль, а точное значение `1` остаётся в stdout. Сравнение `test "$(grep -c ...)" -eq 1` сделало бы этот пункт самодостаточным кодовым критерием.

Пять обязательных вопросов проверены. (1) Критерий слабее предмета: конкретные обходы названы для единицы измерения, обеих границ, универсальной области путей и грамматики раздела. (2) Эти же свойства недоказуемы объявленной матрицей; остальные части имеют именованные команды с наблюдаемым кодом, а точный grep-счёт отмечен советом. (3) Исполнителю молча оставлены счётчик и численное значение КБ, величина превышения, ветвь единственной граничной фикстуры правила, внутренние имена проб и грамматика списка. (4) Машинные `ЗОНА`-строки есть; все прямо требуемые файлы, включая зеркало, покрыты названными исполнителями, новых файлов вне зон не найдено. (5) Печати `PASS`, порога без области и правила без механизма нет; противоречия старшей норме, отдельного от указанных слабостей кодового доказательства, не найдено.

Повтор причины: неполное доказательство границы и возможность зашить область на один известный путь уже блокировали вердикт `c6b8fa3`; это второй `FAIL` по той же инженерной причине.
АРБИТР: `arbiter`; предмет спора — достаточны ли одна безветочная граничная фикстура правила и по одному заранее известному пути на класс для доказательства порога `≤` и квантора «каждый подходящий файл», либо контракт обязан закрепить первый запрещённый байт и независимое от имени покрытие каждой ветви.

Независимые замеры: `wc -c roles/critic.md` → 16919, `wc -m roles/critic.md` → 9864, то есть различие счётчиков реально уже на HEAD. `bash scripts/check_zones.sh` → 0; тегов `frozen/contracts/004/*` нет. Теги не создавались.
