accept

# Контракт 024 v6 — круг 3, правка-3 против РЕШЕНИЯ арбитра

Судимый HEAD при входе: `c147971576d40c98232d2463bc13412800f0694a`. Источник — собственный свежий `git clone --branch main --single-branch ssh://git@github.com/a3ka/dev-harness.git /tmp/critic024v6r3/repo`; SHA получен собственным `git rev-parse HEAD`, не из чужого чекаута.

Окончательный судимый HEAD: `692220521dc1b654aa92cd22763f8288b41fbf1b`, получен собственным `git fetch origin main` и fast-forward того же клона. Между ними изменён только арбитражный вердикт: добавлена атрибутированная строка церемонии. Контракт, обе пробы, NABLIUDENIA_ARCHITECT.md и субъект побайтово прежние: `git diff --exit-code c147971 origin/main --` с этими пятью путями дал rc 0. Результаты oracle-экспериментов на c147971 относятся к той же правке-3; всю сохраняемую прямую семью дополнительно исполнил на 6922205.

Предмет — исполнение `verdicts/arbitration/orakul-podsmotrennaya-konstanta-024.md` (РЕШЕНИЕ `7a91070`) правкой `c73a8bf3b79ded3694b5c16a938257468dbf01ed` и land `c147971`: энтропия входов, двухсостоянный оракул, Н-39 и устранение cm3 круга 2. РЕШЕНИЕ прочитано целиком; обе пробы, контракт :279-308 и А-149 прочитаны. Это НЕ суд реализации корневого среза и НЕ done 024.

Набор артефактов полон: закоммиченный предмет — `contracts/024-detektor-utechek-osnovnogo-chekauta.md:209-266`, rc-приёмка — `:268-336`, именованные исполнители и зоны — `:401-403`. Арбитраж находится в истории; кап кругов удовлетворён. Заморозка v6 этим вердиктом РАЗРЕШЕНА. Сам тег не ставил.

## Пять обязательных вопросов

1. **Критерий слабее судимого предмета?** Для этой дельты обхода не найдено. `contracts/024-detektor-utechek-osnovnogo-chekauta.md:282-301` требует свежие входы и различение A/B. cm1, cm2 и прежний проходивший cm3 теперь отвергнуты; эхо A и накопление A+B отвергнуты фазой B. Положительный эксперимент с вычислением текущих байтов проходит обе пробы дважды. Это проверка различающей силы критерия, не признание экспериментального producer-а реализацией всего контракта.
2. **«Готово» доказуемо названными командами?** Да: две пробы :272-273 несут rc и проверяют значения/переход; сохраняемая семья, scoped и мера названы :309-336. На окончательном HEAD все 13 сохраняемых прямых red-проб и canary дали rc 0. На исходном HEAD scoped и probe_slabyh_detektora дали rc 0; frozen-barrier diff с --exit-code пуст. Новые rootcut-пробы на текущем субъекте закономерно красны — это предъявленный респек, не требование починить реализацию до заморозки.
3. **Решение молча оставлено исполнителю?** Нет новой развилки: источник энтропии, момент вычисления ожиданий, B≠A, появление sha(B), исчезновение sha(A), именованные отказы заданы :282-301. Конструкция реализует точную форму арбитра. Переякоривание red_excludes_target остаётся за architect (:322-325), код среза — за implementer; эта будущая работа не выдаётся за уже исполненную.
4. **Названы границы исполнения?** Да, :401-403: architect — контракт, fixtures/check_judge_gate/ и NABLIUDENIA_ARCHITECT.md; implementer — scripts/check_no_leak.sh; нераздаваемые поверхности объявлены. Механизм — check_zones.sh. Судья изменяет только собственный verdicts/critic/contracts-024-v6.md. Экспериментальные субъекты существуют отдельно, не расширяют чью-либо зону.
5. **Противоречие AGENTS.md?** В судимой дельте не найдено. Прежнее Н-39-противоречие снято; независимая исходная истина остаётся в памяти пробы и вычисляется до каждого вызова субъекта. Коды возврата и область меры названы, новая печать PASS не введена.

Н-39: операционные пары «контрмодель X ловится/краснеет/зеленеет на пробе Y» из :279-308 удалены; инвариант и rc-команды остались. :302-306 именует классы и первоисточники без распределения моделей по ветвям, :307-308 прямо оставляет привязку коду. Сопоставление «контроль 8 / контроль 4» в :292-293 локализует инвариант, а не назначает обманный стаб. Иллюстрации не имеют операционной силы. А-149 — наблюдение об инструментах вне контрактной страницы; новых критериев или зон оно не вводит и заморозку не блокирует.

Действительных блокирующих находок нет. Новых советов по судимому контракту нет.

## Энтропия и две фазы — исполнено

`fixtures/check_judge_gate/red_rootcut_tracked_bytes_024.sh:110-138` и `red_rootcut_untracked_024.sh:109-135` резолвят od только из доверенных системных путей, читают 16 байтов /dev/urandom и требуют 32 hex-символа. Имя и байты получают отдельные выборки: 128 бит на выборку, выше требуемых 64. Untracked-вход отдельно рандомизирован в :235-236. Префиксы `original ` и `SEED UNDER RULE ` не являются полным словарным содержимым: за каждым следует свежая энтропия; это согласуется с сохранением sanity-префикса в З3 РЕШЕНИЯ.

Tracked :289-348 и untracked :230-285: ожидание A вычислено до снимка; сверяется токен в строке нужного пути; затем свежие B, явные проверки B≠A и sha(B)≠sha(A), повторный снимок, обязательный sha(B), запрещённый sha(A). Ни снимок, ни ответ субъекта не используются как источник ожидаемого значения.

Обе полные пробы на c147971 исполнены по ТРИ раза. Tracked каждый раз заканчивается в фазе A «нога tracked-байтов отсутствует»; untracked — раньше, на контроле 1 «ложное чисто». Чтобы не приписывать недостигнутой фазе результат полной пробы, отдельно исполнена scratch-копия untracked-пробы без контролей 1-3: неизменённый субъект трижды даёт на контроле 4 фазу A «нога untracked-байтов отсутствует». Ассерты контроля 4 не менялись.

Независимый разбор xtrace: 30 имён входов, 30 уникальных; 27 значений энтропии mk_main, 27 уникальных. Это наблюдение изменчивости, не статистическое доказательство криптостойкости. Последние tracked-имена трёх полных прогонов:

```text
tracked_3721bc5a086cfde23863c2d734f3e57b.txt
tracked_36dc10fd43f500cc9216f5e54d7e2202.txt
tracked_c80750df58e465ea55923048fc075a13.txt
```

SEED трёх изолированных прогонов:

```text
hid_seed_17767_42547db4fedd9a4a67935a39635dfd10.txt
hid_seed_17069_7093bd5222ce2533b00f2d5b8bf9328c.txt
hid_seed_13057_f2c830e632be6e474bfac2af882950ee.txt
```

## Контрмодели и попытка cm4

Модели построены заново по конструкции собственных улик круга 2 и З2 арбитра: сохранены ВСЕ producer-ы v5, добавлен contract_countermodel; меняется только добавочная нога. Побайтовая сверка scripts/fixtures каждой экспериментальной копии подтверждает единственное отличие — scripts/check_no_leak.sh. Пробы не подменяются. Каждая пара ниже исполнена дважды:

| модель | tracked rc | untracked rc | наблюдаемый отказ дефектной ноги |
|---|---:|---:|---|
| cm1: нулевая tracked-декорация, честная untracked | 1 | 0 | контроль 8, несовпадение отпечатка, фаза A |
| cm2: честная tracked, нулевая untracked-декорация | 0 | 1 | контроль 4, несовпадение отпечатка, фаза A |
| cm3: tracked-константа sha256(original\\n), честная untracked | 1 | 0 | контроль 8, несовпадение отпечатка, фаза A |
| эхо первого отпечатка tracked | 1 | 0 | контроль 8, представление не следит за байтами, фаза B |
| эхо первого отпечатка untracked | 0 | 1 | контроль 4, представление не следит за байтами, фаза B |
| строка с ОБОИМИ sha(A):sha(B), tracked | 1 | 0 | контроль 8, всё ещё несёт отпечаток A, фаза B |
| строка с ОБОИМИ sha(A):sha(B), untracked | 0 | 1 | контроль 4, всё ещё несёт отпечаток A, фаза B |
| положительный контроль: обе добавочные ноги вычисляют текущие байты | 0 | 0 | обе фазы проходят |

Зелёность честной ноги не скрывает дефект другой ноги; это принятое арбитром по-стабное толкование. Прежний блокер cm3 закрыт именно обновлённым оракулом, а не ослаблением его соседей.

Попытки cm4:

- **Чтение SEED/байтов/ожидания из среды субъекта.** Вызовы реально записали `SEED=<unset> BYTES_MAIN=<unset> BYTES_A4=<unset> WANT_A8=<unset>`. Проба не экспортирует эти значения. Предвычисленная подстановка вместо недоступного значения дважды отвергнута обеими пробами, «несовпадение отпечатка».
- **Собственное чтение /dev/urandom вместо чтения поданного файла.** Первая версия выбирала новое значение на каждом чтении манифеста и умерла раньше, на контроле 1 из-за несовпадения двух чтений. Этот результат НЕ выдаётся за срабатывание оракула. Уточнённая версия кэширует независимо выбранный кандидат по пути: дважды tracked rc 1 именно на контроле 8 «несовпадение отпечатка», untracked rc 0. Чтение того же устройства не воспроизводит выборку пробы.
- **Инъекция entropy-хелпера od через PATH.** В начало PATH поставлен исполняемый od, возвращающий фиксированные 32 нуля и записывающий вызов; субъект несёт заранее вычисленный sha256 такого предполагаемого содержимого. Два прогона: tracked rc 1 «несовпадение отпечатка», untracked rc 0; число вызовов фальшивого od — 0. Пин обходит этот helper, входы остаются случайными.

Проходящей cm4 не получено. Это результат конкретных атак, не заявление о переборе всех программ или защите от владельца процесса, меняющего сам проверяющий код/доверенные системные утилиты. Эхо и A+B дополнительно проверяют обе отдельные обязанности фазы B, а не только одну красную ветвь.

## Сырые ключевые выводы

```text
ОТКАЗ контроль-8 (нога tracked-байтов отсутствует): 024-v6: чистый tracked-путь tracked_3721bc5a086cfde23863c2d734f3e57b.txt НЕ представлен в манифесте снимка (цель: git ls-files -z -> sha256 байтов КАЖДОГО tracked-пути; текущее: porcelain-формы v5 представляют только изменённые пути) — корневой срез не реализован
ОТКАЗ контроль-1 (утечка под стабильным committed .gitignore-правилом): rc=0 — ложное «чисто» при живой утечке под стабильным правилом (committed .gitignore): текущий код ослеплён porcelain-формами, цель v6 — нога-2 (ls-files --others -z БЕЗ --exclude-standard) обязана видеть утечку МИМО любых ignore-правил. Вывод: основной чекаут чист
```

Оба вызова полной пробы → rc 1. Ключевая строка cm3, tracked → rc 1:

```text
ОТКАЗ контроль-8 (несовпадение отпечатка): 024-v6 (фаза A): строка tracked_48b57e28e7c7aaee12a8cb11b3b3839e.txt не несёт ВЫЧИСЛЕННОГО пробой отпечатка 7ffe4eaa7e0fba11721b48509e86f6905e5078f85bbec12f02b54b2add4594a6 (форма без значения: 64-hex отсутствует либо не равен sha256 байтов файла; строка: DECORATION:25718360e05d3c2d0963d1381e9dd4dae5fca789244ee4b9f861adcc0cc96218	tracked_48b57e28e7c7aaee12a8cb11b3b3839e.txt) — корневой срез не реализован
```

Финальный HEAD 6922205:

```text
=== final HEAD red_stenogrammy_sudej_024.sh rc=0 expected=0
стенограммы судей 024: все вердикты после границы 17583d000ba58eeaa4e74741693e36e28e509c72 (самое раннее внесение норма-строки) несут строку вызова детектора с rc
=== final HEAD canary rc=0 expected=0
canary_vremya_024: t_raw(git status)=5 мс, t_snapshot=232 мс, t_check=439 мс, порог=3015 мс (3xt_raw+3000)
canary_vremya_024: ок — чистый toy, rc 0 «основной чекаут чист», t_check=439 мс в пороге 3015 мс
```

Все 13 прямых red_*.sh без rootcut на этом HEAD дали rc 0; число независимо получено перечислением путей, не строкой их общего раннера. Scoped на c147971:

```text
барьеров: 1 · фикстур: 3 · предъявлено красным повторным прогоном: 3
```

`bash scripts/verify_antiplacebo.sh . --scope check_judge_gate` → rc 0. `bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` → rc 0, честная форма прошла 18 ворот, плацебо exit-1 отвергнуто. `git diff --exit-code frozen/contracts/024/5 HEAD -- scripts/check_charter.sh scripts/check_zones.sh scripts/check_judge_gate.sh scripts/judge_gate.sh scripts/verify_antiplacebo.sh scripts/freeze_contract.sh scripts/check_contract_frozen.sh` на c147971 → rc 0, пустой вывод. Общепроектный CI/полная проверка не запускались и не заявляются.

Промежуточный отказ НЕ скрыт: на c147971 red_stenogrammy_sudej_024.sh дал rc 1 «вердикт без стенограммы детектора: verdicts/arbitration/orakul-podsmotrennaya-konstanta-024.md». Остановку доложил Main, чужой вердикт не правил. Коммит 6922205 исправил именно недостающую строку, явно атрибутировав новый вызов оркестратору, не арбитру. После fetch и чтения дельты вся прямая семья исполнена заново; окончательный результат выше. Первые драйверы прекращались на этом отказе и на неточном ожидании диагноза первоначальной urandom-модели; оставшиеся команды исполнены отдельным драйвером, отказы сохранены в сыром журнале.

## Церемония и воспроизводимость

Собственный `bash scripts/check_no_leak.sh --snapshot /tmp/critic024v6r3/repo` → rc=0, выполнен на входе и после fast-forward.
Собственный `bash scripts/check_no_leak.sh --check /tmp/critic024v6r3/repo` → rc=0, `основной чекаут чист`.
Это собственная сверка свежего клона, не выдуманная предспавновая стенограмма оркестратора и не утверждение о чистоте чужого main.

Вердикт подготовлен вне стерегомого дерева и проводится в основной репозиторий через write в scratch → hash-object -w → update-index --add --cacheinfo → commit с user.name=critic / user.email=critic@dev-harness.local → checkout HEAD -- только своего вердикт-файла. Клон получает тот же закоммиченный вердикт fast-forward после его появления в основном репозитории. Контракт, пробы и реализация мной не изменяются.

Полные сырые выводы с cwd, командами и rc: `/tmp/dev-harness-verify/critic024v6r3/raw.txt`. Драйверы и экспериментальные субъекты в том же каталоге: `verify.py`, `finish.py`, `final_head.py`, `audit.py`, cm1/cm2/cm3 и остальные именованные копии. `audit.py` независимо сверил уникальность входов и побайтовую неизменность проб/барьеров моделей. Артефакты сохранены как улики эксперимента, не добавлены в продукт.

Итог: оба блокера круга 2 закрыты по РЕШЕНИЮ арбитра; предъявленные и новые контрмодели оракул не обходят; текущая респек-краснота сохранена; окончательная сохраняемая семья зелена. `accept` разрешает заморозку текста v6, НЕ объявляет реализацию корневого среза готовой.
