FAIL

# Контракт 032 — критик, круг 1

Предмет: contracts/032-spawn-pinning-worktree.md и red_pin_spawn_zadanie.sh на c5fe23d97fb1d99197aba3545b281657f051ac1b; отдельно — корректность записи А-178. Судился закоммиченный текст, не последующая реализация. Набор артефактов полон: предмет, критерий, исполнители и зоны названы.

## Проверенные факты

Все прогоны выполнены в одноразовом клоне и производных экспериментальных деревьях. Worktree предмета, его ветка и main не изменялись; коммитов судья не создавал. Приземление этого текста — оркестратором с author=critic.

1. `bash fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh .` на исходном HEAD дважды вернул rc 1 именно на «п1-пин-из-задания-pass»: ожидалось pass, получено block с Н-85/А-122. Toy-пути действительно различались: `/tmp/pg032toy.rP2uDJ` и `/tmp/pg032toy.DLcGCj`.
2. `bash scripts/verify_antiplacebo.sh . --scope check_runner_hygiene` на неподвижном клоне завершился успешно: 1 барьер, 40 фикстур, 40 предъявлений красного повторным прогоном. Первый запуск был прерван внешним тайм-аутом 240 секунд; полный повтор занял 270 секунд. Первый запуск не засчитан как результат.
3. Три исходные регресс-фикстуры 025 — red_pin_allowlist.sh, red_granica_nepin_pipe_tee.sh, red_strazh_vectora_utechki.sh — дали rc 0. Использован отдельный TMPDIR=/tmp/critic032-runtime.fu4waL, чтобы сам клон не попадал в scratch-allowlist при проверке границы чекаута.
4. `git diff --exit-code frozen/contracts/025/5 HEAD -- .omp/extensions/path-guard.ts fixtures/check_runner_hygiene/red_pin_allowlist.sh fixtures/check_runner_hygiene/red_granica_nepin_pipe_tee.sh fixtures/check_runner_hygiene/red_strazh_vectora_utechki.sh` дал rc 0: перечисленные исходники не изменены.
5. Обходы ниже проверены исполнением независимых контрмоделей. Контрольная модель проходит предъявленную приёмку и дополнительные различающие входы. Каждая отдельная дефектная модель также проходит новую фикстуру дважды и все три регресс-фикстуры 025, но нарушает свой различающий вход. Это доказательство недостаточности критерия, не предложенная реализация.

## Блокирующие находки

### Б1. Канарейка п2 не проверяет фактический namespace

БЛОКИРУЕТ contracts/032-spawn-pinning-worktree.md:118-119 — обещание «извлечение ≠ namespace-открытие» не обеспечено предъявленным входом. Фикстура создаёт worktree под `/tmp/pg032toy.XXXXXX`, а не под namespace `/tmp/dev-harness-worktrees/` (fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh:28-42).

ОБХОД: реализация корректно извлекает пин, но дополнительно разрешает unpinned write/edit любого пути с префиксом `/tmp/dev-harness-worktrees/`. Новая фикстура ×2 и три регресс-фикстуры дают rc 0; отдельный фабричный вызов без пары в задании для `/tmp/dev-harness-worktrees/unrelated/wip-999-other/f` получает pass. Тем самым запрещённое namespace-открытие проходит приёмку.

Измерение: `node /tmp/dev-harness-verify/critic-032/probe-namespace.mjs`, rc 0; контрмодель `/tmp/critic032-namespace-ulWxF6`. Вывод: `unpinned writes real namespace => pass expected block`.

Нужен различающий вход именно для реального класса namespace-путей, а не только для toy-каталога произвольного имени.

### Б2. Per-session изоляция и восстановление на branch/tree не проверяются

БЛОКИРУЕТ contracts/032-spawn-pinning-worktree.md:65-69 — М2 требует ключ session_id и три lifecycle-события, но фабричный драйвер всегда создаёт отдельный Node-процесс, один session_id `pg032-s1` и вызывает только session_start (fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh:92-108).

ОБХОД: вместо таблицы по session_id хранить один общий пин. Все команды приёмки зелёные. В одном экземпляре расширения выполнить start(A), start(B), затем tool_call от A на путь B: получается pass вместо block. Собственный пин сессии отсутствует; первая сессия получает чужое разрешение.

Второй независимо проверенный обход того же М2: оставить таблицу по session_id, но зарегистрировать восстановление только на session_start. Все команды приёмки снова зелёные; session_branch/session_tree без предшествующего start для соответствующего session_id не восстанавливают пин, и запись в собственный worktree блокируется.

Измерение: независимые варианты `global` и `lifecycle` в `/tmp/critic032-isolated-J6Ll0J`. Контрольный вариант на тех же различающих входах даёт соответственно block и pass/pass. Нужны межсессионное чередование в одном экземпляре и наблюдаемые переходы branch/tree; увеличение числа запусков нынешнего односеансового драйвера этого не заменяет.

### Б3. Приоритеты источников пина и фабричная сверка actual не различаются

БЛОКИРУЕТ contracts/032-spawn-pinning-worktree.md:70-74,136-137 — все новые фабричные вызовы удаляют WORKTREE из окружения и не передают call.worktree/call.actual. П10 проверяет judge-CLI, а не выбор источника внутри register(). Поэтому фраза о сохранности событийного/env-каналов сильнее доказательства.

ОБХОД: в остальном корректной фабрике поставить пин задания выше call.worktree и env.WORKTREE, применяя к выбранному пину задания исключение из actual-сверки. Новая фикстура ×2 и три регресс-фикстуры дают rc 0. На входе task=A, event=B, actual=B запись в A получает pass вместо block; на входе task=A, env=B, cwd=B запись в A также получает pass вместо block.

Измерение: независимый вариант `priority` в `/tmp/critic032-isolated-J6Ll0J`. Контрольный вариант на обоих входах возвращает block. Требуются фабричные конфликты источников, включая event против env, а не дополнительный вызов judge-CLI.

### Б4. «Живой linked worktree» сводится в приёмке к наличию файла .git

БЛОКИРУЕТ contracts/032-spawn-pinning-worktree.md:58-59,122-123 — М1 требует файл формата `gitdir: …` и существующую цель gitdir. Единственный отрицательный вход п4 создаёт каталог вообще без .git. Он не отличает полную проверку от проверки одного наличия файла.

ОБХОД: проверять только `stat(<путь>/.git).isFile()`, не читать содержимое и не проверять цель gitdir. Новая фикстура ×2 и три регресс-фикстуры дают rc 0. Каталог правильного basename с `.git`, содержащим `gitdir: /nonexistent-critic032-target`, извлекается как полноценный пин, хотя по М1 должен дать null.

Измерение: независимый вариант `deadgit` в `/tmp/critic032-isolated-J6Ll0J`; контрольный вариант на том же классе входа возвращает `{"worktree":null}`. Нужны различающие входы для содержимого .git и отсутствующей цели gitdir. Привязку к нашему репозиторию не требую: она сознательно исключена из предмета.

### Б5. Н-39 нарушена непосредственно в критерии

БЛОКИРУЕТ contracts/032-spawn-pinning-worktree.md:116-125,131,134-135 — контракт содержит прямые пары «стаб ↔ ветвь»: «judge всегда pass» умирает на п0; «открыть весь префикс worktrees» умирает на п2; остальные аналогичные прогнозы на п3–п5, п7/п7б и п9. Это запрещено AGENTS.md:130-133 и дословной Н-39 из задания. Шапка «привязка к коду по Н-39» запрет не отменяет; Б1 показывает, что один из прогнозов уже неверен.

Основание блокирования — прямое противоречие AGENTS.md, предусмотренное ролью исключение из требования отдельного обхода. Оставить в контракте наблюдаемые инварианты и rc-команды; привязку обманок предъявлять по коду и исполнению, вне прозы контракта.

## Ответы по остальным предметам суда

- **П6, чужой worktree:** прямой случай «своя пара A → цель B» предъявлен. Он не заменяет межсессионную изоляцию: обход Б2 открывает B для A при зелёном п6.
- **П7/п7б, `..`:** это предмет реализации 032, а не только запись о сегодняшней дыре. М4 на строках 83-88 прямо требует нормализации обеих allowlist-веток, а строки 129-131 включают оба отказа в приёмку. Выносить исправление за предмет молча нельзя. Symlink-остаток объявлен отдельно и этим требованием не расширяется.
- **П9, steering:** предъявленная форма «первая user-запись A, последующая B» проверяет выбор первой записи. Из этого не следует доказательство изоляции разных сессий или восстановления lifecycle.
- **П10, грамматика 025:** judge-CLI действительно охраняется и исходные регрессы зелёные. Приоритеты фабрики эта проверка не доказывает — Б3.
- **Границы исполнения:** изменяемые файлы названы и распределены; новые файлы вне зон для самого фикса не необходимы. Для живой проверки назван orchestrator. Требовать новую CI-проводку, изменение spawn/land или присвоение зоны 033 оснований нет.

СОВЕТ contracts/032-spawn-pinning-worktree.md:150-152 — живой пост-фриз прогон назван, но не дан его исполнимый протокол с кодом возврата и сохраняемым результатом. До заморозки полезно определить способ предъявления результата. Дополнительные входы Б1–Б4 не требуют настоящего omp-сеанса и потому не могут быть отложены под эту оговорку. Живой task-спавн в данном суде не запускался; фабричные эксперименты не выдаются за него.

## Три развилки: классификация без выбора решения

СОВЕТ contracts/032-spawn-pinning-worktree.md:168-170 — **развилка 1: инженерия.** appendEntry против восстановления из исходного задания — способ хранения и восстановления состояния. Корректность и достаточность носителя разбирает консультант; техническое несогласие — арбитр. Если требуется подпись на изменение замороженной нормы, владельцу предъявляется подготовленное решение-подпись, а не вопрос о выборе структуры хранения.

СОВЕТ contracts/032-spawn-pinning-worktree.md:171-172 — **развилка 2: воля в части границы предмета.** Включать ли доставку судейских вердиктов в эту пачку — решение об объёме и цене. Выбор land_verdict.sh против нормы оркестратора внутри выбранного объёма — уже инженерия. Маршрут по норме-5: консультант с tree-верификацией и рекомендацией → владельцу решение-подпись. Объём судья не меняет.

СОВЕТ contracts/032-spawn-pinning-worktree.md:173-174 — **развилка 3: инженерия в выборе грамматики/маркера; воля в принятии остаточного риска ошибочного пина main-сессии и изменении обязательного формата раздачи.** Эти два вопроса следует разделить. Консультант сравнивает технические варианты; владельцу по норме-5 выносится только подготовленное принятие риска/изменение нормы, если оно требуется. Ни текущую грамматику, ни PIN-маркер этим вердиктом не выбираю.

Инженерные блокеры достаточны для FAIL независимо от решений по развилкам; ESCALATE не требуется.

## А-178

СОВЕТ NABLIUDENIA_ARCHITECT.md:3967 — запись 032 зафиксирована некорректно по идентичности: на этом же HEAD уже существует другая А-178 на строке 3941, про секундный seed случайных входов в 031. Теперь один номер обозначает два разных наблюдения. Содержательная запись о stale line-edit содержит причину, цену, источник и явно названный cognitive-only остаток; наличие механического закрытия она не доказывает. Нужен отдельный корректно выданный идентификатор и однозначные ссылки. Саму историю двух ошибочных edit по стенограмме этот суд не перепроверял. Замечание к наблюдениям не используется для расширения блокирующего предмета контракта.

## Воспроизводимость обходов

`node /tmp/dev-harness-verify/critic-032/probe-isolated.mjs` → rc 0: контрольная модель и четыре независимые контрмодели, новая фикстура ×2 плюс три регресса для каждой; затем различающие входы с указанными выше результатами. Экспериментальные деревья: `/tmp/critic032-isolated-J6Ll0J/{control,global,lifecycle,priority,deadgit}`.

`node /tmp/dev-harness-verify/critic-032/probe-namespace.mjs` → rc 0: отдельный namespace-обход при зелёных пяти запусках приёмки; дерево `/tmp/critic032-namespace-ulWxF6`.

Результат: красное исходного дефекта живо, зелёный базлайн сохранён, но критерий допускает конкретные реализации с открытым namespace, переносом пина между сессиями, отсутствующим восстановлением branch/tree, неверным приоритетом источников и мёртвым gitdir. Заморозка предъявленного текста не разрешается.
