FAIL

# Контракт 033, круг 3 — исполнение решения a74ba04

Предмет: закоммиченный `c9d2688`, `contracts/033-union-semantika-zon-check-zones.md:86,104`. Проверка ограничена пунктом 3 арбитража `verdicts/arbitration/ostatok-b1-033.md` и соответствием добавленной А-181 контракту. Заморозка-проверка :85, Б2, А-179, форма кодификации и СПАСЕНО не переоткрывались.

БЛОКИРУЕТ contracts/033-union-semantika-zon-check-zones.md:86 — форма-ERE не эквивалентна побайтовому сравнению последней бэктик-группы с токеном :102. Состояние `token_in_nonlast_group`: команда контракта возвращает 0, дословная команда арбитра — 1. `.*` пересекает границы бэктик-групп, а заключительная точка ERE допускает любой символ вместо закрывающего бэктика. Поэтому первое предложение :104 описывает свойство, которого команда не обеспечивает.
ОБХОД: у честно закрытого заголовка Н-100 с дословным токеном :102 дописать `; ` и отдельную бэктик-группу `ожидает приёмки прогонами)`. Дословный токен становится непоследней группой; последняя группа не равна :102. Несмотря на невыполненное условие done :98–102, команда :86 возвращает 0. Команда арбитра возвращает 1.

То же расхождение обнаружено без дополнительной группы: в последней группе заменить только `frozen/contracts/033/1` на `frozen/contracts/999/1` (`wrong_033_freeze`). Контракт возвращает 0, арбитр — 1. Следовательно, одного запрета ОТКРЫТО и привязки к концу строки недостаточно ни для выбора последней группы, ни для дословного равенства.

## Собственный прогон

Клон создан из указанного репозитория, checkout — `c9d2688`. Команда контракта извлечена из закоммиченной строки :86; эталонная команда — дословно из блоба арбитража `a74ba04`. Обе исполнены через Bash на одинаковых состояниях полного NABLIUDENIA.md в изолированных каталогах. Базовый заголовок взят из HEAD, положительный токен — из :102.

| Состояние | :86, rc | Арбитр, rc |
|---|---:|---:|
| Реально ОТКРЫТО | 1 | 1 |
| К2: токен процитирован перед нетронутым ОТКРЫТО | 1 | 1 |
| Честное закрытие: последняя группа = дословный токен | 0 | 0 |
| Двойной статус: ОТКРЫТО, затем дословный закрывающий токен | 1 | 0 |
| Двойной статус: закрывающий токен, затем ОТКРЫТО | 1 | 1 |
| Токен в непоследней группе; последняя — `ожидает приёмки прогонами)` | **0** | **1** |
| Заголовок Н-100 отсутствует | 1 | 1 |
| Токен только в теле при открытом заголовке | 1 | 1 |
| Порча `ЗАКРЫТО (033:` | 1 | 1 |
| Порча `cbbf6b1` | 1 | 1 |
| Порча `frozen/contracts/026/4` | 1 | 1 |
| Порча `frozen/contracts/027/3` | 1 | 1 |
| Замена только `frozen/contracts/033/1` на `frozen/contracts/999/1` | **0** | **1** |
| Парафраз внутри последней группы вместо дословного текста | **0** | **1** |
| Токен без бэктиков, с точкой после закрывающей скобки | **0** | **1** |

Три обязательных базовых состояния и четыре поякорные порчи проходят ожидаемо, но этого недостаточно: матрица содержит различающие состояния. Двойной статус с последним честным закрытием также показывает неэквивалентность в обратную сторону: команда арбитра судит последнюю группу, а :86 запрещает ОТКРЫТО во всём заголовке. Это дополнительное наблюдение, не самостоятельное основание блокирования.

СОВЕТ NABLIUDENIA_ARCHITECT.md:4008 — А-181 повторяет заявление о привязке к последней группе. Оно согласовано с намерением :104, но опровергается тем же `token_in_nonlast_group`; при исправлении команды следует синхронизировать описание. Других вопросов к добавленным семи строкам в этом круге не ставлю.

Арбитр: arbiter / Arbiter033Closure — уведомлён о повторе причины Б1. Предмет: исполнение уже вынесенного решения a74ba04, пункт 1; форма-ERE не обладает предписанной различительной силой. Новый пересмотр закрытых частей контракта не требуется.

Достаточная правка в установленном scope: поставить в :86 дословную команду пункта 1 арбитража, с двойным бэктик-ограждением либо fenced-блоком; первое предложение :104 должно описывать побайтовое сравнение последней группы с :102. Это применение существующего решения, не новый критерий.

Воспроизведение: `python /tmp/dev-harness-verify/critic-033-k3/probe.py` — выполнено, rc 0; это код завершения эксперимента, не приёмка контракта. Коды судимых команд приведены в таблице.
Протокол: `/tmp/dev-harness-verify/critic-033-k3/results.json` — обе команды, заголовки состояний, stdout/stderr и коды возврата.

Вердикт передан полным текстом для приземления оркестратором с author=critic. Основной репозиторий, worktree автора и их ветки не изменены; клон сохранён до приземления вердикта.
