accept

# Контракт 040 — критик v2-амендмента пина Р8

Предмет суда: **закоммиченный** `contracts/040-batching-git-vyzovov.md` на
`506d03f77e4db32f04eaf73dfe56fbfa75838fc5`, родитель
`9bcb8468d6c518b28802aae58fb80e9ae4a72a33`.
Судится только перенос пина Р8 с `1b67f790fd922b2248622a289ec29ad7889826de`
на `b38e78ac1e353be132cda2abbe6152b03fa9c275` и сопутствующее описание дрейфа.
Реализация 040, закрытие Б1 и готовность всего контракта к done этим вердиктом
не пересуживаются и не удостоверяются.

Проверка выполнена в собственном клоне из `https://github.com/a3ka/dev-harness`:
`/tmp/dev-harness-verify/critic040v2-pin/repo`. Коммит предмета получен fetch'ем
из architect-клона и выставлен HEAD; чужой клон и основной чекаут не менялись.
Вердикт коммитится под `critic <critic@dev-harness.local>` в этом же отдельном
клоне для механического переноса оркестратором (подтверждённый запрет Н-85/А-122).

**Результат: блокирующих находок нет.** Это восстановление действующего пина
на актуальном базисе, а не разрешение последующих незаметных правок оракулов.
Два совета ниже не требуют нового круга критика.

## Полнота артефактов и пять обязательных вопросов

Набор полный: предмет закоммичен, §Предмет существует, §Приёмка содержит команды
и ожидаемые rc, исполнители и границы названы в §Зоны. Для самой поправки
исполнитель — architect, файл контракта входит в его зону.

1. **Критерий не слабее предмета поправки.** В строке Р8
   (`contracts/040-batching-git-vyzovov.md:404`) изменён только SHA.
   Все восемь glob-паттернов, `--exit-code`, сравнение с HEAD, ожидаемое `rc=0`
   и запрет implementer менять/добавлять артефакты сохранены буквально.
   §Инварианты п.11 и §Зоны побайтово прежние: следующий дрейф пространства
   по-прежнему требует architect-круга с обновлением пина; сам пин защищён
   заморозкой текста. Ни фильтра, скрывающего изменения, ни нового исключения
   амедмент не добавляет. Конкретного обхода, создаваемого поправкой, не найдено.
2. **Готовность доказуема названной командой.** Дословная команда Р8 выполнена
   на HEAD предмета: `rc=0`, stdout/stderr пусты. Дословный контрфакт из
   `contracts/040-batching-git-vyzovov.md:480` выполнен: `rc=1`, вывод совпал
   с записанным побайтово. Цифры независимо сверены формой `--numstat`.
3. **Решение не оставлено исполнителю.** Новый SHA задан полностью и одинаково
   в преамбуле, строке Р8 и командах живого прогона. Выбор пространства или
   базиса implementer'у не передаётся. `fac8fe6` нигде не остался исполняемым
   пином Р8; старый пин упоминается только как предшествующий базис.
4. **Границы удержаны.** Единственный изменённый файл — сам контракт.
   Автоматическое побайтовое сравнение подтвердило: вне добавленного v2-абзаца,
   преамбулы §Приёмка, строки Р8 и раздела «Живой прогон Р8» изменений нет.
   §Предмет, §Инварианты, §Зоны, §ПРОВОДКА, §Риски и условие done совпадают
   с родителем; контракт родителя совпадает с `frozen/contracts/040/1`.
   Зоны и механизм их проверки не расширены.
5. **Противоречия AGENTS.md в предмете поправки нет.** Новый текст прямо
   фиксирует нарушение старого порядка, не объявляет его задним числом
   отсутствовавшим. Маршрут — новая версия текста с критиком, заморозкой и
   подписью владельца; этот accept не заменяет `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` и не
   разрешает приземлить незамороженный текст как действующий. Секции
   `## Модель угроз` нет ни в старом, ни в новом блобе: она не удалена,
   а рассматриваемая поправка не вводит гард для чужого ввода. Расширять
   предмет этого круга до нового суда над всей реализацией 040 оснований нет.

## Независимое подтверждение истории дрейфа

Выполнен `git log --format='%h %an %s' 1b67f790..HEAD -- <восемь glob Р8>`.
В диапазоне ровно две записи, обе с автором architect:

```text
b38e78a architect architect: контракт 040 Б1 — red_predel_git_vyzovov.sh переведён на внешнюю strace-трассировку, красная полярность восстановлена
fac8fe6 architect 040 v4: исполнение Границы v4 арбитража 040-II (доверенный PATH, --always+сверка paths_raw, fail-closed :306, реворк 2+1 спай-фикстур)
```

Это подтверждает рассказ `contracts/040-batching-git-vyzovov.md:32-47`:
новый пин не произвольно выбранный HEAD, а последний architect-коммит,
затронувший запиненное пространство. Прежняя краснота Р8 установлена
ревьюером в Б2; повторно выдавать её за новое открытие этого круга не требуется.
`git show --format='%H%n%P%n%an%n%s' --numstat b38e78a` подтверждает родителя
`d23086fc48e6c55b9bc3ec5a39b38d3d0a57be8a`; команда
`git merge-base --is-ancestor fac8fe6 b38e78a~1` дала `rc=0`.

## Живые команды и точный вывод

Обе команды выполнены на HEAD `506d03f` без обёртки, маскирующей их код возврата:

```sh
git diff --exit-code b38e78ac1e353be132cda2abbe6152b03fa9c275 HEAD -- ':(glob)fixtures/check_zones/red_*' ':(glob)fixtures/check_zones/probe_*' ':(glob)fixtures/check_zones/stab_*' ':(glob)fixtures/check_zones/_*' ':(glob)fixtures/check_protected/red_*' ':(glob)fixtures/check_protected/probe_*' ':(glob)fixtures/check_protected/stab_*' ':(glob)fixtures/check_protected/_*'
```

Код возврата **0**, вывод пуст.

```sh
git diff --exit-code b38e78ac1e353be132cda2abbe6152b03fa9c275~1 HEAD --stat -- ':(glob)fixtures/check_zones/red_*' ':(glob)fixtures/check_zones/probe_*' ':(glob)fixtures/check_zones/stab_*' ':(glob)fixtures/check_zones/_*' ':(glob)fixtures/check_protected/red_*' ':(glob)fixtures/check_protected/probe_*' ':(glob)fixtures/check_protected/stab_*' ':(glob)fixtures/check_protected/_*'
```

Код возврата **1**, stdout:

```text
 fixtures/check_zones/red_predel_git_vyzovov.sh | 48 ++++++++++++++++++++------
 1 file changed, 37 insertions(+), 11 deletions(-)
```

Временная проверочная программа извлекла команды и ожидаемые строки непосредственно
из committed-блоба и сравнила stdout как bytes: совпадение полное, включая пробелы
и переводы строк. `git diff --numstat b38e78a~1 HEAD -- <те же восемь glob>`
независимо дал `37\t11\tfixtures/check_zones/red_predel_git_vyzovov.sh\n`:
37 добавлений + 11 удалений = 48 изменённых строк, один файл. Сырой patch
`b38e78a~1..HEAD` в этих пространствах побайтово равен patch
`b38e78a~1..b38e78a`: контрфакт не смешивает два исторических изменения.

Все пять упоминаний `fac8fe6` — проза, строки 34, 43, 464, 488, 490.
Табличная команда Р8 и обе команды в живом Р8 используют исключительно
`b38e78a`/`b38e78a~1`. В прежних Р3/Р4 остаётся их собственный `f0b6de6`-пин:
это другой критерий, поправкой не затронутый.

Изменённая строка Markdown-таблицы сохранила пять колонок, fenced-блоки парные,
команды в блоках согласованы с таблицей. Полного рендеринга документа не проводилось.
Проверочная программа завершилась `rc=0`; временный файл удалён, постоянных
тестов и правок scripts/fixtures этот суд не добавляет. Полный регресс и CI
не запускались: предмет — только поправка пина, не новая реализация барьеров.

## Советы, не блокирующие accept

СОВЕТ contracts/040-batching-git-vyzovov.md:393 — после «подтверждено ниже» осталась
лишняя закрывающая скобка: скобка описания нового пина уже закрыта на строке 391,
а `(круг v3-fix)` закрыта на строке 392. На Markdown-структуру и команду Р8
это не влияет; можно удалить только последнюю лишнюю `)`.

СОВЕТ contracts/040-batching-git-vyzovov.md:39-41 — слова «ТЕМ ЖЕ коммитом» сильнее
приведённой вслед за ними дословной нормы §Зоны: та требует нового
architect-круга, обновляющего пин, но не формулирует атомарность одним коммитом.
Для честного описания нарушения достаточно «без обновления пина в соответствующем
architect-круге». Это неточность пересказа, а не ослабление действующего критерия:
ни одна из двух исторических правок вообще не обновила пин, и сам §Зоны не изменён.
