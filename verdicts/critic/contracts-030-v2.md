FAIL

# Контракт 030 — критик, круг 2, до заморозки

Предмет: `contracts/030-inventar-i-gonki-spavna.md` на `bd8dbfd3a88de61d29b99d7e04155bb7444504ea`, ветка `wip/030/architect`.
Судья: critic; модель `openai-codex/gpt-6-astra`.

Набор достаточен. Предмет закоммичен, инварианты, rc-команды, исполнители и зоны названы. Пробы 1/3 и 2/3 рассмотрены в предъявленной скратч-раскладке А-174, как в круге 1; они НЕ выданы за файлы HEAD. Проба 3/3 — из HEAD. Разрешение владельца, зоны и границы, уже проверенные кругом 1, повторно не судились.

Доставка этого вердикта — оркестратору полным текстом для коммита с author=critic. Судья не менял исходный worktree, ветку предмета или main. Все экспериментальные изменения сделаны в одноразовом клоне.

## Итог

Все четыре ТОЧНЫХ исполненных обхода круга 1 теперь отвергаются. Положительные контроли проходят: пробы не стали всегда-красными. Б2, Б3 и Б4 закрыты предъявлениями ниже; совет о merge-base предметно урегулирован.

Однако позиционная проверка Б1 пропускает другой исполненный вариант того же дефекта: она обнаруживает `hub` в одном из базовых присваиваний, а не доказывает его наличие в эффективном инвентаре. На таком дереве ВСЕ ТРИ объявленные команды возвращают 0, но ведущая сессия остаётся без `hub`. Поэтому итог — FAIL по одной находке, не по повторному непринятию четырёх прежних исправлений.

## Единственная блокирующая находка

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:129–138 — позиционная проверка не отличает действующее базовое присваивание от затёртого; критерий принимает дерево, нарушающее инвариант :21–22.
ОБХОД: вместо исходной базовой строки `TOOLS` поставить ДВЕ последовательные строки до блока FABLE:

```bash
TOOLS="read,edit,write,bash,grep,glob,lsp,web_search,task,todo,hub"
TOOLS="read,edit,write,bash,grep,glob,lsp,web_search,task,todo"
```

FABLE-присваивание оставить исходным, без `hub`. В остальных двух частях дерева поставить экспериментальные положительные варианты, на которых соответствующие пробы проходят.

Результат исполнения:

```text
bash fixtures/workshop_inventory/red_inventar_hub.sh                 rc=0
bash fixtures/qa_kanal_xd/red_kanal_qa_report_issue.sh                 rc=0
bash fixtures/gc_agent_branches/red_gonka_svezhij_spavn_vyzhivaet.sh   rc=0
```

Отдельное исполнение последовательности присваиваний и FABLE-блока через bash при `FABLE=0` напечатало:

```text
read,edit,write,bash,grep,glob,lsp,web_search,task,todo
```

Токена `hub` нет. При `FABLE=1` напечатан исходный FABLE-список без `hub`. Это исполнение блока определения инвентаря, НЕ запуск полной интерактивной сессии workshop.

Причина в предъявленной пробе, не в догадке о будущей реализации: `fixtures/workshop_inventory/red_inventar_hub.sh:81–83` накапливает `base_ok=1`, если `hub` найден ХОТЯ БЫ В ОДНОМ присваивании до FABLE; последующее присваивание без `hub` его не сбрасывает. Проверки :94–96 принимают любое положительное число базовых присваиваний и накопленный флаг. Эти ссылки относятся к скратч-пробе, разложенной в клоне, а не к отсутствующему на HEAD файлу.

Это закрытая форма прежней проблемы «проверен не тот список, который питает режим», а не требование общего интерпретатора shell. Нужна различимость предъявленного состояния. Конкретный механизм и привязка стаба к проверяемой ветви остаются за architect по коду; дописывать в прозу контракта карту стабов не требуется, Н-39 сохранена.

АРБИТР: роль `arbiter`; созыв запрошен судьёй у оркестратора Main в этом круге. Предмет — повторный FAIL по достаточности проверки эффективного базового инвентаря: пропуск последовательного перезаписывания TOOLS после фикса классификации режимов. Автор уведомлён об исполненном обходе и ПОСЛЕ составления текста ответил: СОГЛАСЕН — сам воспроизвёл rc 0 на двух присваиваниях, признал повтор корня Б1; против FAIL и созыва арбитра возражений нет (спора второй стороны нет; созыв — по правилу 15 и капу кругов заморозки). Третий круг критика автоматически не назначается; маршрутизация — по AGENTS.md.

## Различающие предъявления закрытий круга 1

### Б1: прежний обход закрыт, найденный выше вариант — нет

Связь с предметом: `contracts/030-inventar-i-gonki-spavna.md:129–138`.

- Прежний обход ДОСЛОВНО: базовый список без `edit` и без `hub`, FABLE-список с `edit,hub` → rc 1; живой отказ на FABLE-присваивании с `hub`.
- Базовый список с `hub`, FABLE без `hub` → rc 0.
- FABLE с `edit`, но без `hub` → rc 0.
- Базовый список без `edit`, но с `hub` → rc 0.

Следовательно, прежняя эвристика по наличию `edit` действительно устранена, а свобода этого токена сохранена. Новый обход двух присваиваний исполнен отдельно и не подменяет признание этого закрытия.

### Б2: отрицательные границы bash-зеркала закрыты

Связь с предметом: `contracts/030-inventar-i-gonki-spavna.md:139–145`.

На реальном CLI стража воспроизведён вариант круга 1: в edit/write — точное равенство `path === 'xd://report_issue'`, в bash — широкое `op.startsWith('xd://')`.

Полная новая проба 2/3 → rc 1 на входе 5: `tee xd://report_issue/x`, ожидалось block, получено pass. Положительный вариант с точным равенством в ОБОИХ потребителях → rc 0. Это одновременно выполняет положительные write/tee-входы, отрицательные суффикс/чужой xd/local-входы и пиннованный контроль. Прежний широкий второй потребитель больше не проходит.

### Б3: 48 часов, игнорирование N и снос без reflog различаются

Связь с предметом: `contracts/030-inventar-i-gonki-spavna.md:146–158`.

Эксперимент построен на настоящем `scripts/gc_agent_branches.sh`: в клоне добавлены только разбор флага и решение о грации перед исходным удалением слитой ветки; исходные операции удаления ref/worktree, OID-сверки и TMP-реапа сохранены. Это стенд для суда критерия, НЕ предлагаемая реализация.

Полная проба 3/3 дала:

| Вариант стенда | rc | Наблюдаемый различитель |
|---|---:|---|
| 24 часа, применение N, fail-closed без reflog | 0 | Проходят оба прогона A/B и хвост валидации |
| Совмещённый обход круга 1: 48 часов, N игнорируется, без reflog сносить | 1 | `no-log` снесена |
| Только 48 часов вместо 24 | 1 | `aged25` пережила gc |
| Только игнорирование положительного N | 1 | `young23` пережила прогон с N=3 |
| Только снос при отсутствии reflog | 1 | `no-log` снесена |

Каждый дефект предъявлен отдельно на фоне положительного стенда; ранний отказ совмещённого варианта не выдан за проверку остальных его дефектов.

### Б4: канал и связь проверяются раздельно

Связь с предметом: `contracts/030-inventar-i-gonki-spavna.md:87–90,149–155`.

- Положительные сообщения «ветка: причина» в stderr → полная проба 3/3 rc 0.
- Те же сообщения ТОЛЬКО в stdout → rc 1: stderr не содержит связки для `wip/031/architect`.
- Имя ветки и причина в stderr, но РАЗНЫМИ строками → rc 1 на той же проверке связки.

Тем самым новая проба отличает как потерю канала, так и потерю однострочной связи; полная формулировка сообщения не пиннится.

## Merge-base и остальные вопросы гейта

`contracts/030-inventar-i-gonki-spavna.md:76–106`: прежняя тавтологичная формула пустоты удалена из определения. Достижимость теперь отвечает только за слитость; пустота определена одним событием reflog, наличие работы — двумя и более, отсутствие источника — сохранением ветки. Решение исполнителю молча не оставлено. Положительный прогон 3/3 различил молодую пустую и молодую слитую с работой: первая выжила, вторая удалена; ветка без reflog выжила. Прежний совет урегулирован предметно, не косметической заменой объяснения.

Пять вопросов гейта рассмотрены в пределах предмета/приёмки/исполнителей. Слабость критерия названа единственным исполненным обходом выше. Rc-команды существуют и исполнены. Нового блокирующего решения, оставленного исполнителю, либо противоречия AGENTS.md вне этой слабости не установлено. Ранее проверенные зоны, разрешение владельца и границы не переоткрывались.

## Протокол и ограничения доказательства

1. Изолированный клон: `/tmp/dev-harness-verify/critic-030-k2/repo`, создан из указанного worktree; checkout `wip/030/architect`; HEAD = `bd8dbfd3a88de61d29b99d7e04155bb7444504ea`.
2. На неизменённых механизмах HEAD, после раскладки двух скратч-проб в клон, все три красных предъявления → rc 1: отсутствие `hub`; точный QA-путь блокируется; свежая пустая ветка снесена.
3. `python3 /tmp/dev-harness-verify/critic-030-k2/prove_closures.py` → rc 0. Скрипт исполнил перечисленные положительные и отрицательные варианты, проверил ожидаемые коды и восстановил исходные workshop/страж/жнец в клоне. Логи отдельных запусков: `/tmp/dev-harness-verify/critic-030-k2/logs/`. Снимок нового обхода: `/tmp/dev-harness-verify/critic-030-k2/B1-overwrite-workshop`.
4. Отдельный baseline-клон: `/tmp/dev-harness-verify/critic-030-k2/baseline`. `bash scripts/drill_path_guard.sh` → rc 0: 25 предъявлений judge и проверка фабрики handler. `bash scripts/verify_antiplacebo.sh --scope gc_agent_branches` в окончательном изолированном прогоне → rc 0: 1 барьер, 7 фикстур, 7 красных повторных предъявлений.
5. Неуспешные подготовительные запуски не скрыты: объединённый scope с `drill_path_guard` → rc 1, поскольку дрилл объявлен НЕ БАРЬЕРОМ и такого scope-ключа нет. Первый запуск правильного gc-scope параллельно дриллу → rc 1 из-за созданного дриллом `fake-pi.mjs`; все семь фикстур уже предъявились, но снимок дерева сделал прогон недостоверным. После завершения дрилла его временный файл удалён, gc-scope повторён без конкурентных действий и вернул 0. Именно последний результат принят как baseline.
6. `git diff --exit-code b31d0b5f1e19c69b22f5c5d66db987c301658d39 bd8dbfd3a88de61d29b99d7e04155bb7444504ea -- scripts workshop .omp/extensions` → rc 0: эти механизмы между кругами не изменены.
7. Стенограмма спавна: по переданному оркестратором состоянию последний `--check` → rc 0 после аудированного переснятия, класс Н-112. Это входное свидетельство церемонии, не приписанный судье собственный прогон детектора.

Пины SHA-256 реально исполненных скратч-проб:

```text
cd05196111a8fc84a477e4a9374b1ca891ec19940572dcdfe3bd4cb3ae3062fc  red_inventar_hub.sh
e632a4fd0b25cacce2efeb580a36b532f0966be2691cacff117cd23d3f2c0efc  red_kanal_qa_report_issue.sh
```

Full-suite, форматтеры, линтеры и project-wide build не запускались. Реализация не судилась и для слияния не создавалась. Клоны и логи сохранены до приземления вердикта оркестратором.

Итог: FAIL только по исполненному обходу эффективного базового TOOLS. Четыре прежних конкретных обхода стали красными; положительные контроли зелёные; исправления Б2–Б4 и решение по merge-base признаны.
