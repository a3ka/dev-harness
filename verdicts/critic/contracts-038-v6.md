FAIL

# Контракт 038: суд v6-правки сигнатуры

Предмет: только закоммиченная дельта `8182723..0723138417a67530d10e9100f5a1e8c19d1bf206`
в `contracts/038-provodka-done-gejt.md`. Судимый HEAD клона — `0723138`.
Предмет, критерий с командами, исполнители и ЗОНА-строки присутствуют
(:77–98, :323–364, :366–494). Набор полный. Реализация целиком заново не судится.

## Классификация — ПОДГОНКА СПЕЦИФИКАЦИИ ПОД СУЩЕСТВУЮЩУЮ РЕАЛИЗАЦИЮ

Это НЕ доказанная коррекция ошибочной записи уже согласованного двухаргументного API.
Первая реализация действительно требовала два аргумента, но исходная архитекторская
приёмка согласованно с замороженной прозой вызывала ОДНОАРГУМЕНТНЫЙ API — не только
D4, но и общий `_toy.sh` всей исходной R/G-семьи. Обе формы переписал implementer
одним `e448f4e`. Длительность существования противоположного поведения не превращает
его в исходный контракт. Замороженный 041 и поздние вызовы показывают зависимость
потребителей от фактического API, но не доказывают первоначальную опечатку 038.

При этом два разных свойства нельзя склеивать:

- **Арность API:** меняется нормативно с одного аргумента на два; это изменение
  контракта, а не восстановление формы его исходных исполняемых свидетелей.
- **Граница доверия:** вместе с арностью удалено требование «корень = HEAD-дерево».
  Удаление не необходимо для двухаргументной сигнатуры и допускает трактовку
  рабочего дерева как истины. Конкретный различающий свидетель приведён ниже.

Не установлено ослабление самих механизмов symlink/traversal/TOCTOU этой дельтой:
код и fixtures побайтово неизменны, соответствующие scoped-предъявления зелены.
Однако из этого НЕ следует неизменность нормативной модели угроз: ограничение
источника данных HEAD удалено из текста. Защита каноничности пути относительно
переданного корня и доверенность содержимого этого корня — разные свойства.

## Находки

БЛОКИРУЕТ contracts/038-provodka-done-gejt.md:151 — поправка не ограничена арностью: одновременно удалено «корень = HEAD-дерево», без равноценного определения нового параметра и без различающей команды приёмки. Это ослабление нормативного источника истины под фактическое чтение рабочего дерева, а не необходимая часть исправления сигнатуры. Противоречит AGENTS.md:149–150 (не ослаблять и не переписывать критерий под код) и :230–232 (истина — закоммиченный исходник).
ОБХОД: в HEAD игрушечного репозитория закоммичены контракт с `- role=roles/fixer.md «Норма роли существует только в рабочем дереве.»` и роль БЕЗ этой нормы. Барьер даёт rc1. Добавление строки ТОЛЬКО в незакоммиченный role-файл даёт rc0 при неизменных контракте и HEAD. Действующие 28 scoped-предъявлений и 37 прямых red/green-проб также дают итоговый rc0 на судимом коде. Такой читатель удовлетворяет новой неограниченной формулировке `<корень>` и этим проверкам, но не проверяет наличие нормы в объявленном прежде HEAD-дереве. Сохранённый :160 HEAD-предикат относится к guard-файлу и не восстанавливает общий источник для role/charter.

СОВЕТ contracts/038-provodka-done-gejt.md:13 — утверждение «единственный исполняемый свидетель» неверно. В `811f49f:fixtures/check_provodka/_toy.sh:56` также стоит одноаргументный вызов; его заменяет `e448f4e`. Это важная поправка провенанса, но prose-changelog сам по себе не используется как дополнительный блокер.

Двухаргументная форма сама по себе НЕ запрещена этим вердиктом. Узкая правка должна
сохранить смысл источника данных и отличить его исполнением от приведённого обхода;
решение заменить HEAD-истину рабочим деревом нельзя пронести как исправление числа
аргументов. Выбор владельцем иного предмета — отдельное явное решение, не полномочие
критика. Отсутствие будущей owner-подписи на черновике здесь отдельным блокером не
считается: предъявлен именно предусмотренный маршрут суда ДО owner-скрипта.

## Прямые проверки пяти заявлений

### 1. Реализация с первого коммита: подтверждено

`git log --oneline 9f460e9 -1 -- scripts/check_provodka.sh` → rc0, первый коммит
`9f460e9`. Из `git show 9f460e9:scripts/check_provodka.sh` непосредственно прочитано:

```sh
ROOT="${1:?использование: $0 <корень-дерева> <отн-путь-контракта>}"
CONTRACT_PATH="${2:?использование: $0 <корень-дерева> <отн-путь-контракта>}"
```

Отдельная программа `python3 /tmp/dev-harness-verify/critic038-v6/audit.py` → rc0
извлекла все версии файла из `git log --reverse --format=%H 0723138 --
scripts/check_provodka.sh` и проверила обе обязательные строки в КАЖДОМ blob:

```
9f460e9 cd2bb98 55760d2 f6b2e59 614230a d4919e4 b8529a2 f4f076f
f048251 313382e c9bfe36 b321298 eaa04d2 aaece7e aa0471c af9a13d b40f984
```

Проверены все 17 записей истории изменений файла, не только текущая версия.
Коммиты без изменения blob не образуют ещё одну реализацию API.

### 2. `_toy.sh` с момента landing: ОПРОВЕРГНУТО

`git log --follow --reverse --oneline 0723138 -- fixtures/check_provodka/_toy.sh`
→ rc0, ровно `811f49f`, затем `e448f4e`. Прямые `git show`:

```diff
# 811f49f -> e448f4e, fixtures/check_provodka/_toy.sh:56
-  LAST_OUT="$(cd "$1" && bash "$SUBJ" contracts/001-x.md 2>&1)"; LAST_RC=$?
+  LAST_OUT="$(cd "$1" && bash "$SUBJ" "$1" contracts/001-x.md 2>&1)"; LAST_RC=$?
```

Это не два аргумента при landing. Само `cd "$1"` не передаёт `$1` вызываемому
скрипту. Исходный `_toy.sh`, извлечённый без правок, исполнен против нынешнего
барьера в отдельной игрушке: `original toy barrier rc=1`, причина — обязательный
второй аргумент на строке 146, а не проверка проводки. Программа свидетельства
отличает этот отказ от успешного запуска собственным rc0.

### 3. D4 переписан `e448f4e`: подтверждено

`git show e448f4e -- fixtures/done_contract/red_D4_provodka_krasna.sh` → rc0:

```diff
-rc=0; out="$(cd "$T" && bash "$PROVODKA_BARRIER" contracts/404-ghost.md 2>&1)" || rc=$?
+rc=0; out="$(cd "$T" && bash "$PROVODKA_BARRIER" "$T" contracts/404-ghost.md 2>&1)" || rc=$?
```

`git show 811f49f:fixtures/done_contract/red_D4_provodka_krasna.sh` независимо
подтверждает исходную одноаргументную форму. Но D4 не был единственным свидетелем.

### 4. Новый текст исполняется: подтверждено

В чистом клоне судимого коммита:

```
bash scripts/check_provodka.sh . contracts/038-provodka-done-gejt.md
# stdout/stderr пусты; rc=0
```

### 5. Область диффа и сохранность защит: разделённый вывод

`git diff --stat 8182723 0723138` → rc0:

```
 contracts/038-provodka-done-gejt.md | 16 +++++++++++++++-
 1 file changed, 15 insertions(+), 1 deletion(-)
```

Полный diff прочитан: два хунка, changelog и §Инварианты 3. Независимая проверка
идентичности объектов на обоих концах, `audit.py` → rc0:

```
scripts/check_provodka.sh  1c206ffb66aedd61650c4f42f8dbe16fa3a71d74
fixtures                  4f33184078af9857954b297157239f134ad2cb30
```

`bash scripts/verify_antiplacebo.sh --scope check_provodka` → rc0; раннер сообщил
28 фикстур с зелёным контролем и повторным красным предъявлением. В том числе
case_02/03 traversal, case_04/08/09 symlink, case_14/16/19 TOCTOU и case_18
charter-external-symlink. Это scoped-регресс, НЕ полная проектная приёмка.

`EXPECT_RC=0 bash fixtures/_krasnye_038.sh` → rc0: «37 файлов, расхождений 0».
Это наблюдённый вывод раннера, не независимая перепись файлов.

Различающая проба HEAD/рабочее дерево (`audit.py`, общий rc0), сырой результат:

```
committed missing norm rc= 1 проводка: норма-строка не найдена в role-файле: roles/fixer.md
uncommitted added norm rc= 0
HEAD still lacks required norm: '# Роль\n\nНормы в HEAD нет.\n'
```

Программой дополнительно проверены неизменность HEAD-содержимого и точные коды
1/0. Проба демонстрирует значение удалённого ограничения, а не заявляет новую
регрессию реализации: тот же код читал рабочее дерево и ДО этого амендмента.

## Пять вопросов роли

1. **Критерий слабее предмета:** да, для удалённой HEAD-границы назван и исполнен
   конкретный обход; зелёная старая батарея его не отличает.
2. **Доказуемость командой:** команды объявлены и релевантные выполнены; само
   количество аргументов не доказывает сохранность источника данных. Не исполнены
   freeze/done и полная приёмка проекта; их успех не утверждается.
3. **Молчаливое решение исполнителю:** после удаления определения `<корень>`
   исполнитель фактически выбирает HEAD-снимок либо изменяемое рабочее дерево.
4. **Границы:** architect назван для текста, implementer для скрипта и семейств,
   critic для вердикта; ЗОНА-строки сохранены. Новые пути эта дельта не раздаёт.
5. **Старшая норма:** блокирующее противоречие названо выше с точным местом и
   обходом. Норму 041 о секции модели угроз не применяю ретроактивно к этой правке
   существующего гарда (041 §Инварианты п.1 прямо выводит такой случай из области).

Это первый отказ критика по данной дельте/HEAD-границе; прежний accept исходного
контракта разбирал C6 и зоны, а не эту причину. Повторного спора критик–автор по
ней пока нет. Freeze/re-freeze этим вердиктом НЕ разрешены. Чужие контракты,
исходники, фикстуры и нормы критик не менял; push и постановка тегов не выполнялись.
