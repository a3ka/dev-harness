accept

# Контракт 027, v2 — суд только зона-дельты

Предмет: закоммиченная строка `ЗОНА implementer` в `contracts/027-doc-priemka.md:225`, коммит `75f5ffeda43e90983335790c16de0c6690856e23`; база — `frozen/contracts/027/1` = `e2734f9437fdce2d9af6027ea54b308ccc6db029`. Суд выполнен в одноразовом клоне origin/main с HEAD `75f5ffe`. Слот вердикта — версия будущей заморозки, `verdicts/critic/contracts-027-v2.md` (механизм `scripts/freeze_contract.sh:119`).

Стенограмма спавна: «check_no_leak --check /home/aka/Documents/dev-harness → rc=1 (детерминированный самотрип Н-98: composer job-учёта вне carve-out); слово владельца 2026-09-16: раскрытый отказ для кругов 027 до структурного фикса; Н-98 = NABLIUDENIA.md 405c246».

Набор артефактов полный: предмет, исполнители, зоны и rc-команды приёмки присутствуют на HEAD. Вариант а и модель исполнителя — решения владельца, не предмет повторного выбора. Реализация обёрток, связь commit:path → blob и границы среды исполнения этим вердиктом не принимаются.

## Проверка пяти вопросов

1. **Критерий не стал слабее предмета.** В самом контракте относительно v1 заменена только строка 225: добавлены два пути `.sh` и аннотация. Раздел приёмки `contracts/027-doc-priemka.md:241-263`, включая прямые red_doc_* и четыре итоговые команды, не изменён. Разрешение создать файл не заменяет обязанность пройти эти команды.
2. **Названная проверка остаётся исполнимой мерой.** `scripts/scope_select.sh:55-56,103,114` выбирает `scripts/<ключ>.sh` и требует роль `b`. Шапка с `Коды возврата:` делает оболочку барьером; сочетание с `НЕ БАРЬЕР:` даёт `bp` и НЕ допускается явным `--scope`. Таким образом, новые пути не исключены из суда. `scripts/verify_antiplacebo.sh:9-14,54-72` требует положительный контроль и наблюдаемый отказ, а не одно наличие шапки или фикстуры.
3. **Решение не переложено молча на исполнителя.** Строка 225 задаёт настоящие оболочки-барьеры: шапка с кодами, передача вычисления в node, `.ts` как библиотека. Конкретные шапки, коды и поведение будут предъявлены после freeze v2. Форма `exec node ... "$@"` сама по себе не доказывает готовность и не получает здесь одобрения реализации.
4. **Исполнитель и механизм границ названы.** Два необходимых `.sh`-пути отданы implementer на строке 225; семьи постоянных фикстур уже названы на строке 234. Границы держит чтение блоба высшей заморозки через `lib_zones.sh` и проверка авторства/путей `check_zones.sh`. Аннотация в машинной строке имеет отдельный неблокирующий недостаток, указанный ниже.
5. **Противоречия норме в судимой дельте не найдено.** Приёмка не переписана; разрешение владельца находится в изменяющем коммите, в первой колонке тела, в форме `РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/027-doc-priemka.md <непустая причина>` (`AGENTS.md`, правило 11; разбор `scripts/check_charter.sh:134-163`). Подлинность воли не переоценивается: прямое слово владельца задано предметом суда.

## Исполненные проверки

- `git diff --numstat 75f5ffe^ 75f5ffe` → rc=0, единственная строка: `1  1  contracts/027-doc-priemka.md`. Дифф контракта от v1 показывает ту же единственную замену строки 225.
- `git diff --stat --patch e2734f9 75f5ffe` → rc=0: **3 файла, 37 добавлений, 1 удаление**, а не один файл во всём диапазоне. Причина установлена `git log --format='%h %an %s' --name-only e2734f9..75f5ffe` → rc=0: между базой и зона-дельтой лежит `184437f`, добавляющий разделы в `roles/implementer.md` и `roles/reviewer.md`. Сам `75f5ffe` этих файлов не меняет.
- `git diff --exit-code e2734f9 75f5ffe -- scripts fixtures package.json .github/workflows/ci.yml AGENTS.md` → rc=0, вывод пуст: механизмы, фикстуры, проводка и норма в этом диапазоне не изменены.
- `bash scripts/verify_antiplacebo.sh --scope check_document render_document freeze_contract check_check_contract_ready` → rc=1, вывод: `FAIL scope_select отказал (код 1):`. Это ожидаемая недостижимость приёмки до создания обёрток, не успешная приёмка реализации.
- `bash scripts/scope_select.sh . --scope check_document render_document freeze_contract check_check_contract_ready` → rc=1: `cannot open file './scripts/check_document.sh'` и `неизвестный ключ check_document`. Конкретный кандидат обхода «пути разрешены, обёрток нет» не проходит критерий.
- Одноразовая проба реального `zones_load` с локальным временным тегом `frozen/contracts/027/2` на `75f5ffe` → rc=0: вывод содержит оба новых `.sh`-пути, но также слова аннотации как отдельные пути. Временный тег удалён той же пробой; в основном репозитории заморозка не создавалась.

## Неблокирующие замечания

СОВЕТ contracts/027-doc-priemka.md:225 — аннотация после списка путей не является комментарием для действующей грамматики зон. `scripts/lib_zones.sh:224-236` разбивает весь хвост по пробелам; проба действительно выдала дополнительные буквальные пути `(v2:`, `обёртки`, `.sh`, `node;`, `.ts` и другие. Для точного расширения только на два пути пояснение лучше вынести на отдельную строку, не начинающуюся с `ЗОНА`. Обхода приёмки предмета через эти дополнительные буквальные пути не предъявлено; по порогу роли замечание не блокирует заморозку.

СОВЕТ contracts/027-doc-priemka.md:225 — утверждение «e2734f9..75f5ffe меняет ровно один файл» следует ограничить диффом контракта либо самим коммитом `75f5ffe^..75f5ffe`. Полный диапазон включает ранее внесённые разделы двух ролей из `184437f`; их содержание вне этого малого круга. Это уточнение доказательства границ, не основание расширять суд на роли.

## Решение

Действительного обхода, внесённого зона-дельтой и позволяющего выполнить неизменённый критерий без предмета, не найдено. Блокирующих находок нет. Заморозка v2 разрешена; это не вердикт по будущей реализации и не утверждение зелёности её приёмки.
