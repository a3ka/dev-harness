FAIL

## Круг 2 — предмет суда

Судился закоммиченный текст `53a74a590b2d9105987375dd776d36f128443c09` из
`wip/022/architect`; фикстуры находятся в его истории пачкой
`61bd422`, мандат явного cwd — `bc342ad`. Набор артефактов полный:
предмет, критерий готовности, исполнители, две `ЗОНА`-строки и
`РАБОТА НЕ РАЗДАЁТСЯ` присутствуют (`contracts/022-pre-push-charter-i-bez-avtopusha.md:20-193`).

## Закрытие блокеров вердикта `2f11b53`

1. **Закрыто.** Н-39-проза вынесена из контракта: «Привязка обманных стабов к
   входам, где их дефект наблюдаем, — КОДОМ шапок red/stab-файлов (Н-39), не
   прозой контракта» (`contracts/022-pre-push-charter-i-bez-avtopusha.md:150-151`).
   Точный поиск `Стаб-вход` в новом тексте дал 0 совпадений.
2. **Закрыто.** Новый текст требует три функции «ТОЛЬКО импортом» и прямо задаёт
   перемещение `razreshil` выше возврата `CHARTER_LIB`
   (`contracts/022-pre-push-charter-i-bez-avtopusha.md:42-49`); файл теперь входит
   в `ЗОНА implementer`, а разрешённая правка ограничена только этим перемещением
   и приёмкой `declare -F` (`:172,175-178`).
3. **Закрыто.** Предмет требует «ПОВЕДЕНЧЕСКУЮ ДВУХФАЗНУЮ push-пробу» с чистой и
   красной фазами, проверкой диагноза и неподвижности ref (`:74-77`), а И-10 даёт
   отдельную red-команду и именует `case_push_proba` (`:143-148`). Живой red дал
   rc 1 именно потому, что текущий `check_hooks` принял no-op pre-push.
4. **Не закрыто полностью.** Добавление получило обе стороны «ДОБАВЛЯЮЩИЙ ...
   ПРОПУСКАЕТ» / «дельта M ... отвергает» (`:137-142`). Для тега появился только
   красный вход «тег на красный коммит ... отвергнут» (`:134-136`), хотя предмет
   требует scoped-суд разыменованного тега (`:34-36`); чистого тега нет. Общий
   «ПОЛНЫЙ диагноз: ref, коммит, путь» (`:53-55`) различается только merge-входом.
   Два оставшихся обхода перечислены ниже.
5. **Закрыто.** Нулевой ref теперь определён как `$local_sha --not --remotes`,
   уже публичное повторно не судится (`:39-41`), а И-2 предъявляет уже публичный
   коммит на другом remote и отдельный `red_push_nulevoj_ref_scoped.sh` (`:92-97`).
6. **Закрыто.** И-3 добавил красный force-push после лечения и прямо запрещает
   fail-open любому non-fast-forward (`:98-107`).
7. **Закрыто.** И-5 называет обновление существующего ref на втором remote,
   отдельный `red_push_vtoroj_remote_sushhij_ref.sh` и семью
   `case_push_vtoroj_remote` (`:116-120`).
8. **Закрыто.** И-6 теперь строит НЕпустой origin и прямо различает полный
   демонтаж от условного авто-пуша (`:121-128`).
9. **Закрыто.** Рамка владельца записана дословно: «общий журнал наблюдений обязан
   быть в зонах обоих контрактов пары ... hand-merge без потери записи + общий
   счётчик ... не пересечение зон»; затем названы ручное сведение без потери записей
   и общий счётчик (`:188-193`).

## Генуинность девяти red

Свежий клон ветки: `/tmp/critic022r2.NW8Bul/repo`, HEAD
`53a74a590b2d9105987375dd776d36f128443c09`. Перед каждым замером команда напечатала
один и тот же явный cwd: `pwd => /tmp/critic022r2.NW8Bul/repo`.

- `fixtures/check_hooks/red_push_merge_gran_bez_stroki.sh` — rc 1,
  «механизм pre-push отсутствует»;
- `fixtures/check_hooks/red_push_vetochnaja_gran_bez_merge.sh` — rc 1,
  «механизм pre-push отсутствует»;
- `fixtures/check_hooks/red_push_nulevoj_ref_scoped.sh` — rc 1,
  «НОВЫЙ красный диапазон уехал бы ... механизм pre-push отсутствует»;
- `fixtures/check_hooks/red_push_lechenie_ne_blokiruetsja.sh` — rc 1,
  «механизм pre-push отсутствует ... НОВЫЙ красный диапазон прошёл бы»;
- `fixtures/check_hooks/red_push_vtoroj_remote_sushhij_ref.sh` — rc 1,
  «обновление существующего ref на втором remote ... уехало бы свободно»;
- `fixtures/check_hooks/red_push_teg_annotirovannyj_suditsja.sh` — rc 1,
  «аннотированный тег на красный коммит уехал бы»;
- `fixtures/check_hooks/red_push_dobavlenie_ustavnogo_svobodno.sh` — rc 1,
  «красная дельта M добавленного уставного файла уехала бы»;
- `fixtures/check_hooks/red_check_hooks_bez_push_faz.sh` — rc 1,
  «push-фазы отсутствуют ... барьер зелёеет на no-op pre-push»;
- `fixtures/land_agent/red_push_net_avtopusha.sh` — rc 1,
  «авто-пуш жив — origin/main двинулся» при rc 0 и `LANDED` на НЕпустом origin.

Независимое выведение по трём выбранным файлам, не из описания контракта:

- `red_push_nulevoj_ref_scoped.sh`: фаза стаба полного обхода засчитывается только
  при `p1_rc != 0` и неподвижном пустом `origin2` (`:125-129`); затем уже публичный
  диапазон проходит (`:132-149`); достигнутый фактический отказ находится лишь в
  проверке отсутствующего `HOOK_SRC` перед новой красной фазой (`:151-154`).
- `red_push_lechenie_ne_blokiruetsja.sh`: выполнение прошло стаб полной истории
  (`:89-131`) и отдельный стаб fail-open non-FF, успех которого требует, чтобы
  красный force-push действительно сдвинул origin (`:133-178`); затем лечение
  прошло, и только проверка отсутствующего `HOOK_SRC` перед новым красным дала
  наблюдённый rc 1 (`:182-205`).
- `red_push_teg_annotirovannyj_suditsja.sh`: стаб «теги не судятся» засчитывается
  только когда тег реально приехал на origin (`:81-119`), ref затем удаляется;
  наблюдённый отказ следует из отдельной проверки отсутствующего `HOOK_SRC`
  перед честной фазой (`:123-126`).

Следовательно, у этих трёх стаб-фазы прошли до именованной красной точки, а rc 1
вызван отсутствующим предметом, не поломкой подготовки toy.

## Семьи и форма

При явном `pwd => /tmp/critic022r2.NW8Bul/repo`:

- `bash scripts/verify_antiplacebo.sh . --scope check_hooks` — rc 0,
  7/7 фикстур предъявлены красным повторным прогоном;
- `bash scripts/verify_antiplacebo.sh . --scope land_agent` — rc 0,
  9/9 фикстур предъявлены красным повторным прогоном;
- `bash scripts/check_hooks.sh .` — rc 0;
- `bash scripts/check_charter.sh .` — rc 0;
- `bash scripts/check_ceilings.sh .` — rc 0, «потолки в порядке».

`Незаполненные требования: нет` (`:278-279`). Точный поиск `Стаб-вход` — 0.

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Да: не различает безусловный отказ чистого тега и
   не держит полный диагноз на non-merge входах.
2. **Есть недоказуемое названной командой «готово»?** Да: общий scoped-вердикт для
   тега и общий полный диагноз заявлены, но названные команды не различают два
   состояния из блокирующих обходов.
3. **Решение молча оставлено исполнителю?** Да: пропускается ли чистый
   аннотированный тег и одинаков ли формат диагноза на всех типах коммитов/ref.
4. **Границы исполнения не названы?** Нет. Пути предмета покрыты зонами
   (`:171-183`), ограничение `scripts/check_charter.sh` механически принято И-4.
5. **Противоречие `AGENTS.md`?** Не найдено: запрещённых Н-39 пар в тексте нет,
   результаты заданы rc-командами, зоны и область названы.

## Блокирующие находки

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:34-36,134-136 —
разыменование аннотированного тега предъявлено только красным тегом. Приёмка не
содержит чистого аннотированного тега, который scoped-суд обязан пропустить; позднее
требование абстрактного «зелёного контроля» не называет, что контроль является
именно чистым тегом.
ОБХОД: [INFERENCE] `.githooks/pre-push` может для любого `refs/tags/*` найти путь и
напечатать требуемый диагноз, но всегда вернуть отказ независимо от наличия красной
дельты/РАЗРЕШИЛ; для веток реализовать договор честно. Все девять `red_*`, обе
scoped-семьи, реальный `check_hooks` и его веточная push-проба пройдут, но чистый
аннотированный тег никогда не будет пропущен и scoped-суда тега из предмета нет.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:53-55,84-87 — предмет
требует полный диагноз `ref, коммит, путь` для красного вообще, но только И-1
требует все три поля; push-проба требует путь и sha (`:74-77`), остальные red
проверяют только путь. Поэтому одно merge-предъявление не держит общий квантор.
ОБХОД: [INFERENCE] реализация может печатать полный `ref, коммит, путь` только для
merge-коммита, а для каждого non-merge отказа печатать лишь sha и путь. Merge-red,
двухфазная push-проба и все остальные названные команды пройдут, но полный ref
будет отсутствовать в диагнозах ветки, тега, нового ref и второго remote вопреки
предмету.

АРБИТР: `arbiter` — созыв передан `Main`; предмет спора: достаточно ли одного
красного tag-входа без чистого tag-контроля для договора scoped-суда тегов и
достаточно ли единственного merge-входа с тройкой `ref, коммит, путь` для общего
требования полного диагноза. Это второй `FAIL` по причине блокера 4 вердикта
`2f11b53`: «аннотированный push-тег / полный диагноз не различены критерием».

## Советы

Советов нет.
