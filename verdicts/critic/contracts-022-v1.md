FAIL

## Круг 4 — узкий предмет и сохранённые срезы

По санкции владельца rule-12 от 2026-09-08 судилась **ТОЛЬКО** закоммиченная
дельта закрытия блокера `e91c99a421d6038aa535b5fd8c195079fcf5eebf`, пункт 2
РЕШЕНИЯ `b43d7a06ab05fbc9620697a85435860ba5b5fdcd`: код фикстуры
`aa67706` и текст И-10 `7fb23fb7eb6da0165c33541f5bad20cdd3ec2141` на tip
`wip/022/architect`. Круги 1–3 и прочие уже закрытые обязанности не
пересматривались: срез круга 1 — `2f11b53005257b941b533901125338d9352995a3`,
круга 2 — `0838862d5b207899ab639492476c6c285bb3a7aa`, круга 3 —
`e91c99a421d6038aa535b5fd8c195079fcf5eebf`.

Набор артефактов полный. Предмет находится в
`contracts/022-pre-push-charter-i-bez-avtopusha.md:21-85`, критерий и
rc-команды — `:87-178,217-285`, исполнители и границы — две `ЗОНА`-строки и
`РАБОТА НЕ РАЗДАЁТСЯ` в `:193-195`; незаполненных требований нет (`:302-303`).

## Что дельта действительно закрыла

Точная слабая форма из ОБХОДА круга 3 теперь мертва. Новый вход-2 устанавливает
pre-push, который пропускает чистый диапазон и отвергает красный без любого из
трёх полей (`fixtures/check_hooks/red_check_hooks_bez_push_faz.sh:69-91`), а
фаза 3 требует, чтобы `check_hooks` отверг этот корень (`:173-188`). В
одноразовом клоне на базе `7fb23fb` модель push-пробы, проверяющая только rc
пуша и неподвижность origin-ref и не читающая вывод, была закоммичена как
`9a0895474df70f5cad6e5b3ec700b76ea80b58b3`; команда
`bash fixtures/check_hooks/red_check_hooks_bez_push_faz.sh` дала rc 1 с
именованной причиной «push-фаза 2 не судит тройку диагноза».

Достижимость зелёного по А-96 также доказана. В том же одноразовом клоне модель,
где фаза 2 отдельно проверяет `refs/heads/main`, полный sha красного коммита и
`contracts/001-x.md`, была закоммичена как
`6ae298b7d2c18aa58a183bdad5760d2a6aff9aaf`; та же red-команда дала rc 0.

Но заявленный «закрытый конечный набор форм» не закрыт: один вход, в котором
одновременно отсутствуют все три поля, отличает ноль проверок от хотя бы одной,
но не доказывает конъюнкцию трёх отдельных проверок.

## Живые прогоны в одноразовом клоне

Клон: `/home/aka/Documents/dev-harness/tmp/critic/repo`; исходный HEAD
`7fb23fb7eb6da0165c33541f5bad20cdd3ec2141`. Перед каждым прогоном напечатан
явный cwd, rc снят отдельным оператором.

Красное текущего дерева — все девять rc 1 с именованными причинами:

- `red_push_merge_gran_bez_stroki.sh` — rc 1, «механизм pre-push отсутствует»;
- `red_push_vetochnaja_gran_bez_merge.sh` — rc 1, «механизм pre-push отсутствует»;
- `red_push_nulevoj_ref_scoped.sh` — rc 1, «НОВЫЙ красный диапазон … механизм pre-push отсутствует»;
- `red_push_lechenie_ne_blokiruetsja.sh` — rc 1, «при живом damage … НОВЫЙ красный диапазон»;
- `red_push_vtoroj_remote_sushhij_ref.sh` — rc 1, «обновление существующего ref на втором remote»;
- `red_push_teg_annotirovannyj_suditsja.sh` — rc 1, «аннотированный тег на красный коммит»;
- `red_push_dobavlenie_ustavnogo_svobodno.sh` — rc 1, «красная дельта M добавленного уставного файла»;
- `red_check_hooks_bez_push_faz.sh` — rc 1, «push-фазы отсутствуют … no-op pre-push»;
- `red_push_net_avtopusha.sh` — rc 1, «авто-пуш жив — origin/main двинулся» при rc 0 и `LANDED`.

Регрессионные семьи:

- `bash scripts/verify_antiplacebo.sh . --scope check_hooks` — rc 0, 7/7;
- `bash scripts/verify_antiplacebo.sh . --scope land_agent` — rc 0, 9/9.

Контроль дельты тремя моделями:

- полное отсутствие проверки вывода (`9a08954`) — red rc 1, именованно;
- честная проверка каждого из трёх полей (`6ae298b`) — red rc 0;
- частичная проверка только полного ref, без sha и пути (`6c3cb6cf59d419422128f76100fbdf14913bd3c2`) — red rc 0: обход наблюдён.

После экспериментов клон возвращён на чистый `wip/022/architect` @ `7fb23fb`.

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Да. И-10 требует три отдельных ассерта
   (`contracts/022-pre-push-charter-i-bez-avtopusha.md:162-166`), а вход-2
   отсутствием сразу всех полей доказывает лишь наличие хотя бы одного ассерта.
2. **Есть «готово», недоказуемое названной командой?** Да. Названная red-команда
   зелёна на модели, которая проверяет полный ref, но не проверяет полный sha и путь.
3. **Решение молча оставлено исполнителю?** Да. Исполнитель всё ещё может выбрать
   любое непустое подмножество `{ref, sha, путь}`; текущая команда не различает
   проверку одного поля и проверку всех трёх.
4. **Границы исполнения названы?** Да. Дельта architect покрыта зоной
   `fixtures/check_hooks/` и файлом контракта; будущий `scripts/check_hooks.sh`
   назван в зоне implementer (`contracts/022-pre-push-charter-i-bez-avtopusha.md:193-195`).
5. **Противоречие `AGENTS.md`?** Да: кодовая готовность обязана краснеть на
   обманном состоянии (`AGENTS.md:121-124,208-209`), а наблюдённая частичная
   модель зелёна. Н-39-гигиена соблюдена только для названной формы «ноль полей»:
   её стаб действительно привязан кодом (`red_check_hooks_bez_push_faz.sh:93-153`),
   но формы «одно/два поля» кодом не предъявлены.

## Блокер

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:162-169 — И-10
объявляет проверку полного ref **и** полного sha **и** пути и утверждает, что
формы «нет фаз / фазы без тройки / честные» исчерпаны двумя входами. Однако
`red_check_hooks_bez_push_faz.sh:69-91,173-188` подставляет только один немой
отказ, в котором отсутствуют сразу все три поля, и принимает любое именованное
отвержение корня. Поэтому частичная проверка одного поля неотличима от проверки
всей тройки; утверждение «закрытый конечный набор» ложно.
ОБХОД: реализовать настоящий pre-push и остальные ветви честно, а в
`scripts/check_hooks.sh` в красной push-фазе проверять rc, неподвижность origin-ref
и **только** полный ref `refs/heads/main`, не проверяя полный sha и путь.
Немой hook входа-2 не печатает и ref, поэтому такой `check_hooks` его отвергает;
no-op вход-1 тоже отвергается. Наблюдённая модель именно этого состояния
`6c3cb6cf59d419422128f76100fbdf14913bd3c2` дала red-команде rc 0, хотя пункт 2
РЕШЕНИЯ `b43d7a0` не выполнен. Остальные независимые обязанности можно выполнить
честно, не закрывая эту дыру.

АРБИТР: `arbiter` созван повторно. Предмет спора: «достаточно ли единственного
входа, где отсутствуют сразу все 3 поля, для требования проверить каждое из 3».
Арбитр привяжет отдельный вердикт после этого коммита; основание требования
по-прежнему — пункт 2 закоммиченного РЕШЕНИЯ `b43d7a0`.

## Советы

Советов нет.
