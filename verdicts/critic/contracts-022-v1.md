FAIL

## Предмет суда

Судился закоммиченный блоб `03eab152953d44161039167d07c99982479880cb` из
`wip/022/architect`; аннотированный тег `id/CONTRACT/022` разыменовывается в тот же
коммит. Набор артефактов полный: предмет, критерий готовности, исполнители, `ЗОНА` и
`РАБОТА НЕ РАЗДАЁТСЯ` присутствуют.

## Сверка по осям и живые rc

1. **Полнота предмета.** В тексте названы merge-грань, веточная грань, новый ref,
   лечение, все remote, ветвь C и непрерывная push-проба. Merge/веточная боль и
   демонтаж авто-пуша имеют прямые red-команды. Однако критерий не различает ряд
   прямо объявленных свойств A и Пробы; обходы перечислены ниже.
2. **Красные генуинны.** В свежем клоне ветки получены ровно ожидаемые rc 1:
   - `red_push_merge_gran_bez_stroki.sh` — отсутствует `.githooks/pre-push`,
     merge-дельта прошла бы на origin;
   - `red_push_vetochnaja_gran_bez_merge.sh` — отсутствует `.githooks/pre-push`,
     веточная дельта ушла бы backup-пушем;
   - `red_push_lechenie_ne_blokiruetsja.sh` — отсутствует `.githooks/pre-push`,
     новый красный диапазон при damage на origin проходит;
   - `red_push_net_avtopusha.sh` — rc land равен 0 и есть `LANDED`, но
     `origin/main` сдвинулся из `ПУСТО` в посаженный merge.
   Во всех трёх hook-red стаб-фаза расположена и исполняется до проверки честного
   хука; достижение именованного отказа второй/третьей фазы подтверждает, что
   no-op, only-merge и full-history стабы на своих входах были различены. В land-red
   обе стороны наблюдаются одним вызовом без флагов.
3. **Не-ослабление и конфликт.** Смена смысла rc 0 на «посажено локально» объявлена,
   прежние семейства сейчас зелёные: scope `check_hooks` — rc 0, 7/7; scope
   `land_agent` — rc 0, 9/9. `check:hooks` уже подключён в `.github/workflows/ci.yml:145`.
   Заявленное непересечение с 023 неверно — общий путь указан ниже.
4. **Форма.** Боль дана числами; зоны и нераздаваемая работа объявлены; раздел
   `Незаполненные требования` имеет тело `нет`; `bash scripts/check_ceilings.sh .`
   → rc 0. Норма Н-39 нарушена самим текстом контракта.
5. **Исполнимость.** Все названные пути и команды, кроме создаваемого хука,
   разрешаются. Текущие `bash scripts/check_hooks.sh .` и
   `bash scripts/check_charter.sh .` дают rc 0. Но обещанный ключ библиотечного
   импорта неполон: `CHARTER_LIB=1 source scripts/check_charter.sh; declare -F
   is_charter_path charter_diff_paths razreshil` печатает только первые две функции
   и даёт rc 1.

## Блокирующие находки

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:78-110 — контракт
пять раз утверждает пары «стаб-вход ↔ ветвь/инвариант» прозой (`no-op`,
`только merge`, `полная история`, `только origin`, `флаг наоборот`), хотя
`AGENTS.md:121-127` прямо запрещает контракту утверждать такую пару. Строки 120-123,
гласящие «НЕ прозой», не отменяют уже написанных пар. Это прямое противоречие норме
`AGENTS.md`.
ОБХОД: [INFERENCE] оставить пары в строках 78-110: все четыре red-команды сохранят
свои ожидаемые rc, `check_ceilings.sh` останется зелёным, но замороженный контракт
будет продолжать делать запрещённые Н-39 утверждения.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:40-46 — обязательный
`razreshil` невозможно получить объявленным импортом: текущий
`scripts/check_charter.sh` возвращается из `CHARTER_LIB=1` до определения
`razreshil`. При этом строки 97-102 запрещают менять эту грамматику, а строки
145-146 оставляют `scripts/check_charter.sh` вне всех зон 022. Граница исполнения
не содержит файла, без правки которого объявленный API не существует.
ОБХОД: [INFERENCE] implementer может импортировать две существующие функции,
скопировать упрощённый `razreshil` в `.githooks/pre-push` и оставить
некомментарную ссылку на `scripts/check_charter.sh`; текущие red-входы и обе rc-команды
И-4 можно провести, но кольцо и грамматика будут реализованы второй копией вопреки
предмету.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:62-70 — ветвь Проба не
предъявлена красной против отсутствующей push-пробы. Прямо наблюдено:
`.githooks/pre-push` отсутствует, а текущий `bash scripts/check_hooks.sh .` уже даёт
rc 0. Строки 158-160 дополнительно отказываются морозить наличие новых case, а
четыре `red_*` лежат вне case-глоба.
ОБХОД: [INFERENCE] реализовать хук ровно настолько, чтобы четыре прямых red стали
зелёными, но оставить `scripts/check_hooks.sh` без push-фаз и не конвертировать red;
`check_hooks.sh`, оба нынешних scope и CI дадут rc 0, а непрерывной push-пробы из
предмета не будет.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:33-49 — для
аннотированного push-тега, свободного добавления уставного файла и полного диагноза
`ref, коммит, путь` нет предъявленного различающего rc-входа. Все три hook-red пушат
ветки, меняют существующий уставной файл и в коде требуют от сообщения только путь.
ОБХОД: [INFERENCE] хук может не разыменовывать push-теги, запрещать добавление
уставного файла и при отказе печатать только путь; четыре предъявленных red и
нынешние семейства этого не отличат, хотя три свойства ветви A не сделаны.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:35-37 — вход нового ref
в И-2 доказывает лишь, что красная история отвергнута; он не различает требуемое
`$local_sha --not --remotes` и полный обход всей достижимой истории от `$local_sha`.
ОБХОД: [INFERENCE] для нулевого `remote_sha` судить полный `git rev-list
$local_sha`: новый ref из red И-2 всё равно красный и будет отвергнут, остальные
red используют обновление существующего ref, но первая экспозиция уже публичной на
другом remote истории не исключается по объявленной семантике.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:87-96 — дискриминатор
лечения не отличает scoped-суд `remote..local` от безусловного fail-open для любого
non-fast-forward/force push: лечение force-push, а следующий красный в этой фикстуре
пушится без `--force`.
ОБХОД: [INFERENCE] хук может пропускать любой non-fast-forward push без суда и
судить только fast-forward/new-ref; лечение пройдёт, последующий красный и обе грани
И-1/И-2 будут отвергнуты, все три hook-red станут зелёными, но произвольный красный
force-push обойдёт гейт.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:103-106 — И-5 отложен
на ещё не существующую «конверсионную case-семью» без имени и отдельной rc-команды.
И-2 действительно использует второй remote, но только с нулевым `remote_sha`, то
есть не держит обновление существующего ref во втором remote.
ОБХОД: [INFERENCE] хук может судить origin и любой новый ref, но пропускать обновление
существующего ref в не-origin remote; текущий origin2-вход И-2 и все прочие red
пройдут, а договор «все remote» останется невыполненным.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:51-60 — живая проба И-6
строит пустой bare-origin (`origin/main = ПУСТО` до land), поэтому она не отличает
полный демонтаж авто-пуша от авто-пуша, условно оставленного только для уже
существующего `origin/main` — именно обычного производственного состояния.
ОБХОД: [INFERENCE] сохранить `git push origin main 2>/dev/null || true` под условием
существования `refs/heads/main` на remote; red И-6 с пустым origin станет зелёным,
старые 9 case останутся зелёными, но авто-пуш и `|| true` из ветви C останутся живы.

БЛОКИРУЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:144-146 — зона architect
пересекается с параллельным контрактом 023 по `NABLIUDENIA_ARCHITECT.md`
(`contracts/023-rezervacija-nomera-dver-po-tegu.md:279`), вопреки утверждению
строк 217-218, что зоны не пересекаются. Перечень `РАБОТА НЕ РАЗДАЁТСЯ` разводит
implementer-файлы, но общий architect-файл не разводит.
ОБХОД: [INFERENCE] оба architect-коммита могут законно менять общий файл в своих
зонах и пройти каждый зональный барьер по отдельности; при сведении параллельных
веток потребуется молчаливое разрешение конфликта/расширение чужой зоны, хотя
контракт объявил непересечение.

## Советы

СОВЕТ contracts/022-pre-push-charter-i-bez-avtopusha.md:45-46 — ссылка
`check_charter.sh:23-35` на грамматику устарела относительно текущего расположения
`razreshil` (определение начинается около строки 178); после исправления блокера
импорта лучше ссылаться на имя функции, а не на текучий диапазон строк.
