FAIL

## Семь фактов проверки

1. `contracts/021-sud-okna-svoej-vetki.md:12,29-51` — предмет закрывает названную цену Н-66: открытое окно выбирает merge-дельты только своего `wip/<N>/…`, поэтому чужой merge не входит в судимое множество; признание draft по `id/CONTRACT/<NNN>` включено в ту же функцию «что судится» и поглощает Н-77(б).

2. БЛОКИРУЕТ `contracts/021-sud-okna-svoej-vetki.md:59-67,112-115,179-181` — усиление владельца «А-кандидат „два done одновременно" — НЕ в наблюдения, а МЕХАНИЗМ приёмки §10, определить ДО обкатки 022∥023, с анти-плацебо (последовательный прогон ПРОВАЛИВАЕТ „параллельно")» не предъявлено анти-плацебо для названной меры: единственный scope адресован барьеру `check_zones`, тогда как предметом является новый барьер `scripts/measure_parallel_windows.sh`; команда реальной обкатки проверяет только зелёный rc 0. На закоммиченном дереве `bash scripts/verify_antiplacebo.sh . --scope check_zones/case_mera_parallelnosti_okon` возвращает 1: case отсутствует.
ОБХОД: добавить `scripts/measure_parallel_windows.sh`, который всегда возвращает 0, и case семейства `check_zones`, дающий требуемые зелёный и красный вызовы именно `check_zones`; все названные scope-команды и команда на реальной перекрывшейся паре вернут 0, хотя последовательная пара также будет принята как «параллельно».

3. БЛОКИРУЕТ `contracts/021-sud-okna-svoej-vetki.md:69-77,106-111,151-161` — усиление владельца «021 обязан доказать регресс: оконная модель = строгое обобщение линейного диапазона, суд 013–020 неизменен, красная фикстура на расхождение» формулирует равенство, но красную не предъявляет: таблица прямо откладывает слабые реализации на время после реализации, а названный scope на закоммиченном дереве возвращает 1 из-за отсутствующего case. Это противоречит `AGENTS.md:121-124,208-209`: красные тесты кодового контракта должны существовать и быть предъявлены до круга критика.
ОБХОД: сначала реализовать оконную модель, затем написать case, в котором «линейная» и «оконная» стороны вызывают один и тот же новый вычислитель; scope вернёт 0 на тождественном сравнении, хотя изменение суда 013–020 относительно прежней линейной модели останется необнаруженным.

4. БЛОКИРУЕТ `contracts/021-sud-okna-svoej-vetki.md:79-118,128-129,143-148` — демаркация R1 задаёт конформную грамматику, но Н-39 нарушена дословно: контракт сам объявляет, что стабы «привязаны к ветвям кода», и перечисляет пары «слабая реализация ↔ ветвь», одновременно оставляя их будущим незакоммиченным case. `AGENTS.md:124-127` прямо запрещает контракту утверждать такие пары: architect обязан закрепить привязку кодом красных фикстур, а контракт — только инварианты и rc-команды.
ОБХОД: заморозить этот текст, реализовать выбранную структуру, а затем написать перечисленные стабы под уже увиденные ветви реализации; все rc-команды смогут пройти, но независимая от реализации привязка входа к стабу до начала работ отсутствовала.

5. `contracts/021-sud-okna-svoej-vetki.md:134-140` — границы исполнения названы: зоны architect и implementer не пересекаются и состоят из литеральных путей без глобов; для раннера, hooks, roles и судейских verdict-каталогов явно записано `РАБОТА НЕ РАЗДАЁТСЯ` с причинами и каналами.

6. `contracts/021-sud-okna-svoej-vetki.md:197-200` — замороженная область демаркирована: меняется только семантика судимого множества открытого окна `check_zones`; СПАСЕНО 003 v3, charter И-7, раннер и done-диапазоны Н-14 объявлены неизменными, а И-3, И-5 и полный scope семейства названы охранами.

7. БЛОКИРУЕТ `contracts/021-sud-okna-svoej-vetki.md:179-181,201-206` — порядок 021 → 022∥023 и сериализация peek через явный `--nnn` одного актора с ловцом `check_ids` названы, но решение «парная заморозка не требуется, потому что контракты пары независимы» в контракте отсутствует; приёмка требует лишь перекрытия времён двух окон.
ОБХОД: заморозить 022 и 023 одним парным актом с взаимными исключениями, довести оба до done и получить rc 0 меры перекрытия; объявленная приёмка выполнена, хотя независимые заморозки — требуемое следствие устранения рекурсии — не доказаны.

## Советы

Нет.
