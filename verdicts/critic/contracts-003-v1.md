FAIL

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:10-12 — предмет требует и `grilling`, и обёртку `grill-me`, но критерий требует ровно четыре каталога и не задаёт, где живёт и как обнаруживается отдельное имя обёртки. Исполнителю оставлено решить, какой из двух исходных скилов исчезает либо каким нестандартным размещением обходится discovery.
ОБХОД: положить только четыре каталога `grilling`, `writing-for-agents`, `tdd`, `diagnosing-bugs`; все четыре объявленные ветви `check_skills.sh` и число каталогов проходят, но `grill-me` из предмета отсутствует.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:8-9 — требуемый снимок назван как «коммит клона на момент разбора», но сам hash не зафиксирован; строки 26-30 требуют от барьера только наличие источника, а не совпадение тела с этим снимком.
ОБХОД: положить четыре минимальных `SKILL.md` с совпадающими именами фронтматтера и произвольной непустой строкой источника; подставной omp объявляет их обнаруженными, четыре красные ветви и вся приёмка проходят, хотя английских оригиналов требуемого снимка в дереве нет.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:31-37 — содержательные указатели заданы условиями, leading words и конкретными скилами, но единственная мера в строках 65-66 считает подстроку `skills/`. Она не проверяет ни условие открытия, ни словарь тела, ни соответствие пути скилу; кроме того, `grep -c` не возвращает ошибку при недостижении порога 3/2.
ОБХОД: добавить в `roles/architect.md` три произвольные строки `skills/x`, а в `roles/implementer.md` две строки `skills/x`; счётчики и их коды возврата зелёные, но роли не знают, когда открывать ни один из четырёх скилов.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:38-41 — решение Q8-C требует коммиты фикстур до старта исполнителя, однако приёмка в строках 67-69 проверяет только конечное дерево и авторские зоны, не порядок коммитов.
ОБХОД: implementer сначала коммитит `skills/` и `scripts/check_skills.sh`, затем architect коммитит четыре красные фикстуры. `check:antiplacebo` видит красные фикстуры в конечном дереве, `check:zones` принимает оба пути, все команды приёмки зелёные, но фикстуры написаны после раздачи.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:40 — найденные при реализации ветви поручены implementer, но `fixtures/check_skills/` в строках 47-48 принадлежит только architect. Строки 57-58 так же поручают implementer файл `decisions/008`, тогда как `decisions/` объявлен только в зоне architect. Это прямое противоречие норме `AGENTS.md` о работе исполнителя только в своей зоне; обход для противоречия норме не требуется.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:42-43 — предмет требует реально разложить `skills/` в профиль omp, но строки 67-70 наблюдают только ok-строку `overlay`, независимый `check_skills` и наличие шага CI; постусловие профиля не проверяется.
ОБХОД: `overlay.sh` не копирует скилы и печатает требуемую ok-строку, а `check_skills.sh` отдельно проводит захват подставного omp над репозиторным `skills/`; `overlay`, `check:skills`, `check:antiplacebo` и CI зелёные, профиль остаётся без скилов.
