FAIL

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:35-39,72-74,106-108 — предписание 4 арбитража 5 не исполнено: приёмка по-прежнему наблюдает реальный профиль только по ok-строке, а `cmp` выполняет отдельная ветвь (д) в созданном барьером пустом временном `HOME`. Арбитраж уже признал ветвь (д) достаточной на уровне барьера, но потребовал после отдельного реального `npm run overlay` сверить каждый `SKILL.md` именно в реальном профиле; такой сверки в приёмке нет.
ОБХОД: `overlay.sh` копирует файлы только когда `$HOME` указывает на пустой временный каталог, а в обычном профиле делает no-op и печатает ok-строку. Ветвь (д) запускает настоящий overlay в своём пустом `HOME` и получает зелёный `cmp`; отдельный `npm run overlay` в приёмке даёт требуемую строку, остальные команды зелёные, но скилы в реальный профиль не встроены. Это ровно заглушка замера 4 арбитража 5.

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:30-31,48-51,64-69 — предписание 5 арбитража 5 исполнено не полностью: заголовок объявляет СЕМЬ ветвей, перечень содержит ВОСЕМЬ — (а), (б), (в), (г), (д), (е), (ж), (з), а критерий 3 всё ещё требует «фикстуры пяти ветвей» без поимённого множества. Арбитраж дословно потребовал убрать «пяти» и назвать ветви поимённо; область меры полноты остаётся противоречивой.
ОБХОД: до первого коммита реализации положить красные фикстуры любых пяти ветвей и реализовать ветвь (з) как проверку именно этих пяти согласно критерию 3. Обе команды порядка и `check_skills.sh` зелёные, тогда как для трёх перечисленных ветвей красных фикстур до реализации нет; предмет Q8-C не сделан.

Проверено без отдельной находки по остальным предписаниям арбитража 5:

- contracts/003-skills-metta-adaptacija.md:40-44 задаёт категории upstream поимённо. Замер на HEAD: четыре URL с категориями вернули 200; URL `skills/grilling/SKILL.md` без категории вернул 404.
- contracts/003-skills-metta-adaptacija.md:33,40-44 задаёт для ветвей (в) и (е) одно значение hash из блоба высшей заморозки контракта.
- contracts/003-skills-metta-adaptacija.md:44-46 задаёт байтовую границу тела первой строкой upstream-фронтматтера `---` и включает её в сравниваемое тело.
- contracts/003-skills-metta-adaptacija.md:48-51 переносит меру полноты на родителя первого коммита по `skills/` и `scripts/check_skills.sh`; блокировка выше относится к неустранённому противоречию области этой меры, а не переоткрывает решение арбитража.
