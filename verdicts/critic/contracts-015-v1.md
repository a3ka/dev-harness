FAIL

Закоммиченный предмет: `7901e2975b1279448ed87001659c05e60902c25a`; дельта круга 4: `bd76f5d..7901e29`. Набор полон: предмет, команды готовности, названные исполнители и `ЗОНА`-строки находятся на HEAD.

## Блокирующая находка

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:59-80 — анкорирование считает границей токена любую букву вне перечисленных ASCII/кириллицы. Поэтому греческая буква, вплотную приклеенная к якорю слева или справа, проходит и форму, и адресатный ключ: на полном подменённом корне из 36 строк §Материал, где только Н-36 заменена на `ОТКРЫТО — адрес: αконтракт 015`, миграционная проба дала rc=0; вариант `ОТКРЫТО — адрес: контракт 015β` также дал rc=0. Между тем в первом маркере категория — слитная лексема `αконтракт`, а во втором идентификатор — слитная лексема `015β`: требуемой пары «категория контракт + номер 0NN» нет. Обычная mixed-script подмена внутри стема `контpакт 015` красна, но класс Unicode-буквы на объявленной границе остаётся зелёным.
ОБХОД: implementer дословно переносит пинованный ERE во все три потребителя и мигрирует Н-36 маркером `ОТКРЫТО — адрес: αконтракт 015` (либо `контракт 015β`), остальные 35 строк и три механизма исполняет честно. Миграционная проба на полном корне печатает `ok` и rc=0; барьер и дайджест обязаны нести тот же ERE, а существующие case такого алфавитного склеивания не предъявляют. Все названные команды готовности могут быть зелёными, хотя адрес Н-36 не содержит отдельной категории `контракт` с отдельным номером `0NN`.

Это новая причина, не повтор блокеров круга 3; новый созыв арбитра по правилу повторной причины не требуется.

## Закрытие трёх блокеров круга 3

1. `неконтракт 015` — дословная команда contracts/015-jadro-avtonomnosti-nabljudenij.md:297 на отдельном корне: rc=1, именованная ветвь `Н-14: адрес не называет цель` (до дельты была rc=0).
2. Чужой адрес `контракт 014` у Н-14 — команда :298: rc=1, именованная ветвь `назначение изменено — таблица ждёт Q, маркер несёт K:014`. Повтор на полном 36-строчном корне, переданном аргументом пробе, дал тот же единственный отказ и rc=1.
3. Слепой барьер, красящий только `адрес: .`, — команда :299: rc=1; 4 case, предъявлена красной только точка, расхождений 3: оба новых независимых case голых категорий и `case_adres_pustoj.sh` названы `красное не предъявлено`. Тем самым стабы объединённого case круга 3 мертвы: `очередь` и `правило роли` теперь имеют отдельные повторные прогоны.

Побайтовая сверка анкоренного ERE контракт ↔ миграционная проба: оба SHA-256 `cf4868b3177b328cdbc695c6f95a6e38a7cf76b44ca72ed98194ba00b1d587f4`. Арбитражная цитата verdicts/arbitration/contracts-015-povtornye-spory.md:59 не изменена; обёртка границ объявлена как усиление меры.

## Собственные кандидаты

- Полный корень §Материал с Н-36=`адрес: 015/fix` → rc=1, ровно `Н-36: адрес не называет цель`.
- Полный корень с mixed-script-гомоглифом Н-36=`адрес: контpакт 015` (латинская `p`) → rc=1, ровно `Н-36: адрес не называет цель`.
- Полный корень с Н-36=`адрес: αконтракт 015` → rc=0; с Н-36=`адрес: контракт 015β` → rc=0 — обход выше.
- Подмена строки §Материал Н-14 с ожидания `Q` на два несовпадающих с маркером ключа `K:014 Q` → rc=1, `таблица ждёт K:014 Q, маркер несёт Q`: ожидание действительно извлекается из таблицы, не зашито в пробу.
- Подмена корня-аргумента полным деревом с чужим адресатом Н-14=`контракт 014` → rc=1, единственный отказ назначения. Канал аргумента не отключает таблицу из дерева пробы.

## Обязательные прогоны и счёты на 7901e29

1. Пять красных проб:
   - `bash fixtures/check_nabludenia/probe_nabludenia_krasnoe.sh` → rc=1, `барьера нет`;
   - `bash fixtures/drill_gate_draft/probe_gate_draft_krasnyj.sh` → rc=1, `дрилла нет`;
   - `bash fixtures/drill_startup_digest/probe_digest_krasnyj.sh` → rc=1, `дрилла нет`;
   - `bash fixtures/drill_nabludenia_nechitaemo/probe_nechitaemo_krasnyj.sh` → rc=1, `дрилла нет`;
   - `bash fixtures/check_nabludenia/probe_migracija_adresov.sh` → rc=1, ровно 35 поимённых отказов: 32 без адреса, Н-50 без якоря и А-16/А-17 без статуса.
2. `bash scripts/verify_antiplacebo.sh --scope check_charter` → rc=1, ровно 4 именованных `фикстура без барьера`; собственный scope: 1 барьер, 7/7 красных, только эти 4 расхождения.
3. Собственная ready-сборка: `contract.md` из контракта HEAD + четыре каталога красных фикстур в отдельном `/tmp`-корне; `bash scripts/check_contract_ready.sh /tmp/critic015-ready` → rc=0, `OK`.
4. `bash scripts/check_contract_frozen.sh` → rc=0: 18 планов/контрактов, 4 черновика, 14 заморожено; 015 — черновик.
5. `npm run check:ci-parity` → rc=0: 26 workflow-команд, 38 npm-скриптов приёмки, 12 исключений, 0 расхождений.
6. Две Н-39-меры контракта → обе пусты, rc=1 у каждой.
7. Счёты по отслеживаемому дереву: `fixtures/check_nabludenia/` — 10 case; четыре новых каталога — `10 + 7 + 12 + 1 = 30` предъявлений; всего 285 `case_*.sh`; 31 различный fixture-каталог с case, то есть объявленные 31 барьер после реализации.
