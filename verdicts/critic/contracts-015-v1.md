accept

Закоммиченный предмет: `1067c1a07106e18ab1cd18cb53a896397d47a899`; дельты после вердикта круга 4: `55f721a` (структурный анкор и `build_root.py`) и `1067c1a` (постоянный `case_unicode_sklejka.sh`). Набор артефактов полон: предмет — contracts/015-jadro-avtonomnosti-nabljudenij.md:30-246, критерий с rc-командами — :262-354, исполнители и их границы — :248-260.

## СТОП по границе владельца

СТОП — НОВЫЙ ОБХОД ГРАММАТИКИ. СОЗВАТЬ АРБИТРА, НЕ ПЕРЕНОСИТЬ ЭТИ НАХОДКИ В БЛОКЕР КРУГА 6.

АРБИТР: роль `arbiter`; предмет спора — принципиальная граница «robust ли regex-якорь адреса»: допустим ли после одного структурного якоря произвольный, в том числе пустой или похожий на второй адрес, хвост. Первая строка остаётся `accept`: по прямой границе владельца новые обходы этого unicode-круга требуют СТОП и решения арбитра, но не являются блокером следующего авторского круга.

СОВЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:59-86 — полный 36-строчный корень §Материал с единственной подменой Н-36 на `ОТКРЫТО — адрес: контракт 015 — примечание` прошёл миграционную пробу, rc=0. Явный разделитель открывает произвольный `.*`; механической границы между допустимым пояснением и злоупотреблением хвостом критерий не задаёт. По границе владельца это новый проходящий кандидат грамматики и причина СТОП, а не блокер круга 6.

СОВЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:47-74,185-193 — полный корень с Н-36=`ОТКРЫТО — адрес: контракт 015 контракт 016` прошёл, rc=0. Значение несёт два адресоподобных якоря, хотя §Материал требует сохранить НАБОР якорей строки; проба проверяет только `want - have` и не запрещает лишнее назначение, а при одном пробеле потребляющая границу мера ключей дополнительно не извлекает второй ключ. По границе владельца это новый обход и причина СТОП, не блокер круга 6.

СОВЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:59-86 — полный корень с Н-36=`ОТКРЫТО — адрес: контракт 015 —` прошёл, rc=0. В пинованном `(разделитель.*)?` разделитель присутствует, а собственно хвост пуст; критерий не отличает это от содержательного хвоста. По границе владельца это новый обход и причина СТОП, не блокер круга 6.

## Реестр стабов: 9/9 красны

Для одноадресных входов использована одна и та же полная мера без подмены контракта: `python3 fixtures/check_nabludenia/build_root.py "$T" '<значение Н-36>'`, затем `bash fixtures/check_nabludenia/probe_migracija_adresov.sh "$T"`.

1. Точка, `.` → rc=1, `Н-36: адрес не называет цель`.
2. Голая очередь, `очередь` → rc=1, `Н-36: адрес не называет цель`.
3. Голое правило, `правило роли` → rc=1, `Н-36: адрес не называет цель`.
4. Некатегория, `неконтракт 015` → rc=1, `Н-36: адрес не называет цель`.
5. Чужой адрес, `контракт 014` → rc=1, `Н-36: назначение изменено — таблица ждёт K:015, маркер несёт K:014`.
6. Слепой case: изолированный корень с четырьмя `case_adres*.sh` и подставным барьером, который красит только `адрес: .`; `bash scripts/verify_antiplacebo.sh <корень>` → rc=1, 3 расхождения: голая очередь, голое правило и пустой адрес названы `красное не предъявлено`; только точка предъявлена красной.
7. Unicode-склейка слева, `αконтракт 015` → rc=1, `Н-36: адрес не называет цель`.
8. Unicode-склейка справа, `контракт 015β` → rc=1, `Н-36: адрес не называет цель`.
9. Латинская гомоглифа номера, `контракт 01p` → rc=1, `Н-36: адрес не называет цель`.

`case_unicode_sklejka.sh` также прогнан напрямую с `WORK` и адаптером `BARRIER`, который для каждого записанного case-входа собирает полный корень `build_root.py` и вызывает неизменённую миграционную пробу. Сам case завершился rc=0, как и обязан из-за `|| true` на красных ветвях; журнал четырёх вызовов барьера: `контракт 015` rc=0; `αконтракт 015` rc=1; `контракт 015β` rc=1; `контракт 01p` rc=1. Тем самым постоянный case действительно содержит три независимо красных вызова, а не одно красное прикрытие трёх входов.

Побайтовый ERE contracts/015-jadro-avtonomnosti-nabljudenij.md:86 и `ANCHOR` fixtures/check_nabludenia/probe_migracija_adresov.sh:38 имеют один SHA-256: `f8e5cb23db5e71e08f88064ddab7203dc03be51eecd0d92725069b258f2700d0`. Контракт :72-74 пинует этот же ERE трём потребителям — барьеру, дайджесту и миграционной пробе; до реализации первые два закономерно отсутствуют. Внутри пробы форма использует `ANCHOR_RE`, адресатная мера строится из тех же `STEMS` и `SEP`, а рассинхронизация стемов/разделителя является ранним rc=1.

Остаток из доклада дельты закрыт адресатной мерой: полный корень с Н-36=`очередь автономности контракт 015β` прошёл форму по валидной голове, но проба дала rc=1: `назначение изменено — таблица ждёт K:015, маркер несёт Q`. Склеенный β-ключ не извлечён; одной чужой валидной головы недостаточно для сохранения назначения Н-36.

## Обязательные прогоны на 1067c1a

1. Пять красных проб:
   - `bash fixtures/check_nabludenia/probe_nabludenia_krasnoe.sh` → rc=1, `барьера нет`;
   - `bash fixtures/drill_gate_draft/probe_gate_draft_krasnyj.sh` → rc=1, `дрилла нет`;
   - `bash fixtures/drill_startup_digest/probe_digest_krasnyj.sh` → rc=1, `дрилла нет`;
   - `bash fixtures/drill_nabludenia_nechitaemo/probe_nechitaemo_krasnyj.sh` → rc=1, `дрилла нет`;
   - `bash fixtures/check_nabludenia/probe_migracija_adresov.sh` → rc=1, ровно 35 строк отказа: 32 `открыта без адреса`, Н-50 `адрес не называет цель`, А-16/А-17 `статус не объявлен`.
2. Полный корень §Материал без подмены: `python3 fixtures/check_nabludenia/build_root.py "$T"`, затем миграционная проба на `"$T"` → rc=0; собрано 36 строк, 34 адресуемые цели зелёные.
3. `bash scripts/verify_antiplacebo.sh --scope check_charter` → rc=1, ровно 4 именованных `фикстура без барьера` (`check_nabludenia`, `drill_gate_draft`, `drill_nabludenia_nechitaemo`, `drill_startup_digest`); выбранный scope: 1 барьер, 7/7 красных, 4 расхождения.
4. Собственная ready-сборка: полный контракт как `contract.md` плюс четыре каталога фикстур в отдельном `tmp/`-корне; `bash scripts/check_contract_ready.sh <корень>` → rc=0, `OK`.
5. `bash scripts/check_contract_frozen.sh` → rc=0: 18 планов/контрактов, 4 черновика, 14 заморожено; 015 — черновик.
6. `npm run check:ci-parity` → rc=0: 26 workflow-команд, 38 npm-скриптов приёмки, 12 исключений, 0 расхождений.
7. Счёты отслеживаемого дерева: `fixtures/check_nabludenia/` — 11 case; пачка 015 — `11 + 7 + 12 + 1 = 31` case; всего 286 `case_*.sh`; 31 различный fixture-каталог с case. Итого требуемые `11/31/286/31` совпали.

Предыдущий unicode-обход закрыт структурным анкором и постоянным case; действительных блокирующих находок в пределах санкционированного круга нет. Три новых проходящих кандидата зафиксированы как СОВЕТЫ и СТОП-маркер для арбитра ровно по объявленной владельцем границе.
