accept

Судимый предмет: `cb87a5ba401f62e4a3c63070d9330f8828ac0019`, дельта поверх `357095e9f7a0e4fddd5e181ebb8b33172ab0623d`. Текущий HEAD проверки — `e8ee026f2f60d41352efc7c024430530c38b4e70`; следующий коммит менял только передачу контекста, поэтому предметом этого вердикта остаётся именно `cb87a5b`, не прежний `1067c1a`.

Набор артефактов полон: предмет — `contracts/015-jadro-avtonomnosti-nabljudenij.md:30-252`, критерий с rc-командами — `contracts/015-jadro-avtonomnosti-nabljudenij.md:319-434`, исполнители и границы — `contracts/015-jadro-avtonomnosti-nabljudenij.md:305-317`. Тегов `frozen/contracts/015/*` нет, поэтому по грамматике имени перезаписан v1.

## Фидельность РЕШЕНИЮ `contracts-015-hvost-adresa`, q1–q3

1. **Мера пробы исполнена.** `fixtures/check_nabludenia/probe_migracija_adresov.sh:72-80` строит несъедающие lookaround-границы стемов; `:179-186` на ветви «ОТКРЫТО с валидной формой» извлекает `have` только из значения после `адрес:` и требует `have != want` как отказ.
   - `build_root.py <root> 'контракт 015 контракт 016'` собрал 36 строк; миграционная проба → rc=1 ровно нужной ветвью: `Н-36: назначение изменено — таблица ждёт K:015, маркер несёт K:015 K:016`.
   - `build_root.py <root>` без подмены собрал 36 строк; проба → rc=0.
   - Подмены Н-36 `контракт 015 — примечание` и `контракт 015 —` каждая собирали полный 36-строчный корень; обе пробы → rc=0. Это В-ЯЗЫКЕ, как решил арбитр, а не красные кандидаты.

2. **Постоянный стаб добавлен.** `git cat-file -e cb87a5b:fixtures/check_nabludenia/case_lishnee_naznachenie.sh` → rc=0. Файл `fixtures/check_nabludenia/case_lishnee_naznachenie.sh:1-25` содержит отдельные зелёный и красный вызовы для равенства наборов; пойманный обход увеличивает реестр 9→10. В каталоге механизма теперь 12 отслеживаемых `case_*.sh`.

3. **Хвостовые кандидаты закреплены зелёными контролями.** Их две команды и ожидаемый rc=0 записаны в `contracts/015-jadro-avtonomnosti-nabljudenij.md:401-410`; обе самостоятельно прогнаны на полных корнях и дали rc=0. В реестр красных case они не добавлены.

4. **Закрытая форма перенесена слово-в-слово.** Словесная последовательность РЕШЕНИЯ q3 `verdicts/arbitration/contracts-015-hvost-adresa.md:121-160` воспроизведена в `contracts/015-jadro-avtonomnosti-nabljudenij.md:93-135`: форма, назначение, исчерпывающие классы (a)–(f), классы в языке, пост-миграционная граница и фраза «судится исполнение». Различаются только Markdown-отступ и переносы строк, не слова и не смысл.

## Гейты и собственные замеры

- **ERE-SHA:** строка ERE в `357095e:...md:86`, `cb87a5b:...md:87` и живом файле имеет один SHA-256: `f8e5cb23db5e71e08f88064ddab7203dc03be51eecd0d92725069b258f2700d0`. В нулевом контексте дельты нет хунка изменения самой пинованной ERE-строки.
- **Счёты:** `git ls-files` дал 12 case в `fixtures/check_nabludenia/`; по четырём каталогам пачки — `12 + 7 + 12 + 1 = 32`; во всём дереве — 287 `case_*.sh` в 31 различном fixture-каталоге. Искомая четвёрка: **12/32/287/31**.
- **Пять красных проб, поимённо:**
  1. `bash fixtures/check_nabludenia/probe_nabludenia_krasnoe.sh` → rc=1, `барьера нет`;
  2. `bash fixtures/drill_gate_draft/probe_gate_draft_krasnyj.sh` → rc=1, `дрилла нет`;
  3. `bash fixtures/drill_startup_digest/probe_digest_krasnyj.sh` → rc=1, `дрилла нет`;
  4. `bash fixtures/drill_nabludenia_nechitaemo/probe_nechitaemo_krasnyj.sh` → rc=1, `дрилла нет`;
  5. `bash fixtures/check_nabludenia/probe_migracija_adresov.sh` → rc=1.
- **Миграционная мера:** пятая проба напечатала ровно 35 отказов: 32 `открыта без адреса`, Н-50 `адрес не называет цель`, А-16/А-17 `статус не объявлен`.
- **Scoped:** `bash scripts/verify_antiplacebo.sh --scope check_charter` → rc=1, ровно 4 именованные `фикстура без барьера`; выбранный барьер предъявил 7/7 красных, итог `расхождений: 4`.
- **Ready собственной сборкой:** из полного контракта как `<root>/contract.md` и четырёх каталогов фикстур собрана отдельная песочница; `bash scripts/check_contract_ready.sh <root>` → rc=0, `OK`.
- **Frozen:** `bash scripts/check_contract_frozen.sh` → rc=0: 18 планов/контрактов, 4 черновика, 14 заморожено; контракт 015 назван черновиком.
- **Н-39:** обе меры из `contracts/015-jadro-avtonomnosti-nabljudenij.md:412-421` пусты: 0 заголовков `§ Стабы и их входы`, 0 прозаических связок `стаб.*(→|↔).*case_`. Стабы привязаны к ветвям кодом и шапками case, а контракт несёт инварианты и rc-команды.

## Советы: новые входы вне классов (a)–(f)

Нет. Новые кандидаты не изобретались: этот круг судит исполнение закрытого РЕШЕНИЯ. Если позднее найдётся проходящий при верной мере вход вне исчерпывающих классов `contracts/015-jadro-avtonomnosti-nabljudenij.md:107-121`, по прямому решению арбитра это будет новый спор и только СОВЕТ данного гейта, не блокер фидельности.

Действительных блокирующих находок в исполнении q1–q3 нет.
