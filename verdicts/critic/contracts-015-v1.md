FAIL

Закоммиченный предмет: `8c238add8b136158af6e8f019f451b3d89d2c7f1`. Тегов `frozen/contracts/015/*`: 0, поэтому имя этого круга — `contracts-015-v1.md`.

## Блокирующие находки

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:213-232 — контракт прямо утверждает десять пар «стаб → вход, где его дефект наблюдаем». Это дословно запрещено старшей нормой `AGENTS.md:124-127`: такую привязку делает автор кода, а контракту утверждать пару запрещено. Это самостоятельное противоречие `AGENTS.md`; по правилу роли обход для него не требуется.
ВЫХОД: удалить из контракта §«Стабы и их входы»; оставить в контракте инварианты и rc-команды, а пары — в шапках закоммиченных фикстур, где они уже находятся.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:43-49 — «непустой хвост» не отличает назначенный адрес от адреса-заглушки, хотя следующая фраза определяет адрес как место назначенной починки, а §Материал требует сохранить назначение каждой строки.
ОБХОД: миграция пишет всем 32 строкам ``ОТКРЫТО — адрес: .``. Точка — непустой хвост, поэтому `check_nabludenia.sh` и дайджест зелены, семь объявленных фикстур механизма 1 тоже не предъявляют этот случай, но ни контракт, ни очередь, ни ROADMAP-шаг, ни правило роли, ни обоснованное «историческое, не чинить» не названы.
ВЫХОД: назвать механически различимый порог содержания адреса и предъявить красную фикстуру адреса-заглушки; миграционную сверку назначения из §Материал также включить в команду с кодом возврата.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:153-154 — две из десяти строк, объявленных «историческое, не чинить», не являются реально мёртвыми по самому закоммиченному материалу. `NABLIUDENIA_ARCHITECT.md:627-644` говорит про А-30: «Сам слой — ОТКРЫТО», с живым риском ложного красного/зелёного. `NABLIUDENIA_ARCHITECT.md:646-660` говорит про А-31: кандидат «требует чистого прогона» и «не доказан». «Вне дерева», «процедурная заметка» и «механизма не планируется» не доказывают мёртвость, требуемую развилкой 1.
ОБХОД: implementer переносит из таблицы обе непустые строки «историческое, не чинить» в заголовки А-30/А-31. Синтаксический барьер и вся объявленная приёмка проходят, но открытый слой и недоказанный кандидат списаны именно в запрещённую владельцем свалку.
ВЫХОД: либо дать проверяемое основание фактической мёртвости, либо вывести А-30/А-31 из исторических в очередь/спорные с честным адресом.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:54-55 — ветвь «существующий нечитаемый файл → rc=2» входит в предмет, но среди семи фикстур механизма 1 нет входа с нечитаемым файлом; после реализации критерий требует только эти семь предъявлений и живой rc=0 (`:203-210`).
ОБХОД: `check_nabludenia.sh` считает существующий нечитаемый файл отсутствующим, печатает «неприменимо» и возвращает 0. На всех семи данных входах файл либо читаем, либо отсутствует, поэтому scoped-приёмка зелена, а объявленный rc=2 не реализован.
ВЫХОД: добавить красную фикстуру, которая различает rc=2 от «неприменимо», и назвать её scoped-команду приёмки.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:83-92 — fail-open скрипта и handler механизма 2 объявлен частью предмета, но пять красных фикстур предъявляют только запись, дедуп, не-гейт, TMPDIR внутри дерева и отсутствие подписки. Ни одна не создаёт внутреннюю ошибку во внешней базе и не отличает проглоченный отказ дочернего скрипта от проброшенного.
ОБХОД: `draft_nabludenia.sh` на ошибке `mkdir`/записи возвращает 1, а зарегистрированный `tool_result` handler пробрасывает этот отказ; на всех пяти предъявленных входах внешняя база доступна для записи либо проверяется отдельный именованный отказ внутреннего TMPDIR. Пять стабов красны, scoped-команда проходит, но отказ гейта способен сломать общую работу вместо fail-open.
ВЫХОД: предъявить отдельными красными входами внутреннюю ошибку скрипта и отказ вызываемого скрипта внутри handler, с ожидаемым живым процессом/rc=0.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:103-116 — критерий механизма 3 предъявляет только непушенный тег из всей секции дерева и только отсутствие подписки из контракта расширения. Нет красного входа для ложных ahead/behind, ложного «чисто» при аномалии `git status`, недоступного remote, указателя HANDOFF, ровно одного `sendMessage(nextTurn)` и fallback при падении скрипта.
ОБХОД: дайджест правильно перечисляет непушенные теги, записи и черновики, но всегда печатает `ahead 0, behind 0`, `чисто` и постоянный указатель; extension регистрирует один handler, но отправляет два сообщения с `deliverAs` по умолчанию и пробрасывает падение скрипта. Шесть имеющихся стабов (грамматика, grep тела, потолок, пустые черновики, отсутствие подписки, теги=0) остаются красными, а непредъявленные инварианты предмета не сделаны.
ВЫХОД: добавить красные входы для каждой названной ветви либо сузить предмет; каждая оставшаяся ветвь должна быть доказуема названной scoped-командой с кодом возврата.

## Собственный пересчёт §Материал

Мера независима от таблицы контракта: из обоих файлов выбраны только заголовки `^### Н-NN.`/`^### А-NN.`, затем статус взят из последней группы в бэктиках той же строки.

- `NABLIUDENIA.md`: 17 заголовков с последним маркером `ОТКРЫТО…`: Н-45, Н-41, Н-49, Н-50, Н-61, Н-13, Н-14, Н-33, Н-35, Н-36, Н-38, Н-57, Н-60, Н-62, Н-63, Н-64, Н-65. Из них адрес уже несут ровно 2: Н-50, Н-64; без адреса — 15.
- `NABLIUDENIA_ARCHITECT.md`: 17 заголовков с последним маркером `ОТКРЫТО…`: А-7, А-11, А-12, А-15, А-24, А-25, А-19, А-20, А-26, А-30, А-31, А-32, А-33, А-41, А-42, А-49, А-50. Адресов в этих маркерах: 0. Ещё ровно 2 заголовка без статусного маркера: А-16, А-17.
- Итого frontier таблицы: 17 + 17 + 2 = 36 строк. По идентификаторам таблица совпадает построчно: пропусков, лишних строк и дублей нет.
- Числовые категории таблицы также сходятся: «историческое» 10 = Н-13, Н-33, Н-35, Н-38, А-11, А-12, А-20, А-24, А-30, А-31; спорные 4 = Н-57, А-15, А-26, А-41; очередные/назначаемые 18 = Н-14, Н-36, Н-41, Н-45, Н-49, Н-60, Н-61, Н-62, Н-63, Н-65, А-7, А-19, А-25, А-32, А-33, А-42, А-49, А-50; уже с адресом 2 = Н-50, Н-64; без маркера 2 = А-16, А-17.
- Расхождение не в арифметике, а в истинности категории: А-30 и А-31 не обоснованы как реально мёртвые; это вынесено блокером выше. Четыре спорных названы поимённо.

## Прямые прогоны на 8c238ad

1. `bash fixtures/check_nabludenia/probe_nabludenia_krasnoe.sh` → rc=1; поимённо: `ОТКАЗ: барьера нет — .../scripts/check_nabludenia.sh`.
2. `bash fixtures/drill_gate_draft/probe_gate_draft_krasnyj.sh` → rc=1; поимённо: `ОТКАЗ: дрилла нет — .../scripts/drill_gate_draft.sh`.
3. `bash fixtures/drill_startup_digest/probe_digest_krasnyj.sh` → rc=1; поимённо: `ОТКАЗ: дрилла нет — .../scripts/drill_startup_digest.sh`.
4. `bash scripts/verify_antiplacebo.sh --scope check_charter` → rc=1, ровно 3 расхождения: `fixtures/check_nabludenia`, `fixtures/drill_gate_draft`, `fixtures/drill_startup_digest`, каждое с причиной `фикстура без барьера — она ничего не держит`; сам scoped-барьер: 1, фикстур: 7, предъявлено красным: 7. Это ожидаемое красное А-52, не находка.
5. `bash scripts/check_contract_frozen.sh` → rc=0: планов и контрактов 18, черновиков 4, заморожено 14; контракт 015 распознан черновиком.
6. `npm run check:ci-parity` → rc=0: workflow-команд 25, npm-скриптов приёмки 37, исключений 12, расхождений 0.

## Н-39 и границы

Поиск `(?i)стаб|симуляц|обманк` по контракту дал 5 мест: строки 20, 51, 207, 213, 234. Строки 20/51 — инвариант заголовка, 207 — rc-команда. Строка 213 открывает запрещённый § пар стабов и входов; строка 234 возвращает стаб-классификацию в контракт. Поэтому требуемая Н-39-чистота не выполнена.

§Зоны покрывает все 25 файлов пачки и все явно названные будущие файлы трёх механизмов: architect — контракт, 18 фикстур, package/CI и А-файл; implementer — 3 рабочих скрипта, 2 дрилла, 2 extension-файла и миграция обоих NABLIUDENIA-файлов; critic/adversary/reviewer/orchestrator названы отдельно. Развилки 4 и 5 в §Не-цели перенесены дословно; развилки 1-3 и 6 также объявлены. Блокера границ не найдено.
