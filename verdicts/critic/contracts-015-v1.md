FAIL

Закоммиченный предмет: `331f49528bf9b362e15f1e1c0e4a55af4a6f7881`. Круг 3 судит исполнение РЕШЕНИЯ `verdicts/arbitration/contracts-015-povtornye-spory.md` дельтой `85f1869..331f495`; контракт не заморожен, поэтому по грамматике имени перезаписан `contracts-015-v1.md`.

## Блокирующие находки

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:265-267 — два стаба круга 2 записаны в `case_adres_zaglushka.sh`, но не предъявлены красными: в одном case сначала идёт уже-красная точка, затем «очередь» и «правило роли», а раннеру достаточно ОДНОГО красного вызова с объявленной причиной. Прямой прогон case с подставным барьером, который красит только точку, а обе голые категории оставляет зелёными, завершился rc=0; трасса содержит все четыре вызова. Раннер выбирает первый красный кандидат и не требует красноты каждого последующего вызова (`scripts/verify_antiplacebo.sh:648-660,680-700`). Следовательно, обязательная форма решения «стабы … красны» исполнена текстом входов, но не работающим красным предъявлением.
ОБХОД: implementer кладёт в `# ГРАММАТИКА:` пинованный ERE, чтобы пройти сверку источников, но исполняемый разбор оставляет на regex круга 2: точка даёт «адрес не называет цель», а `адрес: очередь` и `адрес: правило роли` дают rc=0. `case_adres_zaglushka.sh` всё равно принимается на красной точке; миграционная проба после миграции видит только целевые адреса и тоже зелена. Объявленный предмет «голая категория — ветвь адрес не называет цель» не сделан.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:50-69 — пинованный ERE применяется как неякорный поиск подстроки и потому принимает лексему категории внутри другого слова. На собранном tmp-корне точный вход `ОТКРЫТО — адрес: неконтракт 015` дал миграционной пробе rc=0: «все … несут адрес с якорем цели». Ни отдельной красной фикстуры, ни границы токена в критерии нет.
ОБХОД: все 32 мигрируемых маркера получают хвост `адрес: неконтракт 015`. Пять проб после реализации, scoped-предъявления и живой барьер с дословным ERE зелёны, хотя обязательная категория «контракт» отсутствует и цель не названа.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:165-166,258-264 — §Материал требует сохранить назначение каждой из 32 записей, но названная миграционная команда проверяет только форму якоря, не соответствие записи целевому адресу таблицы. Проверенный tmp-вход для Н-1 с чужим, но синтаксически допустимым `адрес: контракт 014` прошёл пробу с rc=0.
ОБХОД: implementer меняет назначения всех 32 строк на один валидный чужой адрес `контракт 014`. Миграционная проба, живой барьер, все fixture/scoped-команды и счёт 28/28 зелёны, однако назначения §Материал изменены. Фраза «Ревьюер сверяет список» — чтение, а не команда с кодом возврата, и заявленную на строке 264 сходимость не доказывает.

## Буквальная сверка РЕШЕНИЯ

1. **Спор 1.** Фраза о месте пар сохранена (`:234-236`). Обе обязательные меры пусты: `§ *Стабы и их входы` → 0 совпадений; `стаб.*(→|↔).*case_` → 0 совпадений. Лексическое упоминание «стаб» отдельно не блокировалось.
2. **Спор 2 / ERE.** Строки арбитража `:59`, контракта `:69` (без отступа) и `anchor=` пробы `:14` (без оболочки присваивания) побайтово одинаковы: SHA-256 каждой — `22a958c925db7857f524adc3f87f4573a5a42ad014afea8fd59e4534ff8cf463`. По §Материал ровно 32/32 миграционных целевых формулировок (строки 170–205 без Н-50, Н-64, А-16, А-17) совпали с этим ERE. А-26 = `закрыто 014`, А-41 = `закрыто 013`, Н-65 = `контракт 015`. Входы `очередь` и `правило роли` добавлены, но их предъявление дефектно — блокер 1.
3. **Спор 3 / дрилл.** `case_nechitaemyj_fajl.sh` отсутствует; в `fixtures/check_nabludenia/` осталось 8 case-файлов. `scripts/drill_nabludenia_nechitaemo.sh` объявлен в предмете и зоне implementer после заморозки; новый architect-каталог содержит одну case-фикстуру и отдельную красную пробу «дрилла нет». Инвариант 4 (нечитаемость → rc=2 + именованная строка) сохранён. Счёты дерева: `8 + 7 + 12 + 1 = 28`; контракт объявляет радиус `27 → 31` и четыре шага CI. `package.json` и CI несут все четыре команды; `npm run check:ci-parity` → rc=0, workflow-команд 26, npm-скриптов приёмки 38, исключений 12, расхождений 0.
4. **А-54.** Рецидив ложного «grep пуст» признан отдельной А-54 в `NABLIUDENIA_ARCHITECT.md:1069`; контракт пересчитал файл как 54 записи.

## Прямые прогоны на 331f495

1. `bash fixtures/check_nabludenia/probe_nabludenia_krasnoe.sh` → rc=1, «барьера нет».
2. `bash fixtures/drill_gate_draft/probe_gate_draft_krasnyj.sh` → rc=1, «дрилла нет».
3. `bash fixtures/drill_startup_digest/probe_digest_krasnyj.sh` → rc=1, «дрилла нет».
4. `bash fixtures/check_nabludenia/probe_migracija_adresov.sh` → rc=1, 33 именованных отказа: 32 без адреса (15 Н + 17 А) и Н-50 с «адрес не называет цель».
5. `bash fixtures/drill_nabludenia_nechitaemo/probe_nechitaemo_krasnyj.sh` → rc=1, «дрилла нет».
6. `bash scripts/verify_antiplacebo.sh --scope check_charter` → rc=1: ровно 4 именованных А-52 «фикстура без барьера» (`check_nabludenia`, `drill_gate_draft`, `drill_nabludenia_nechitaemo`, `drill_startup_digest`); scoped-барьеров 1, фикстур 7, красных 7, расхождений 4.
7. `bash scripts/verify_antiplacebo.sh --scope drill_nabludenia_nechitaemo` → rc=1: те же ровно 4 отсутствующих барьера, затем `scope_select` rc=1. После реализации критерий явно требует этот scope с rc=0, поэтому простое изъятие дрилла приёмку не обходит.
8. `bash scripts/check_contract_frozen.sh` → rc=0: 18 планов/контрактов, 4 черновика, 14 заморожено; 015 — черновик.
9. Собственный tmp-корень с `contract.md`, четырьмя probe-файлами и каталогами case-файлов: `bash scripts/check_contract_ready.sh /tmp/tmp.YkY0b3RYG9` → rc=0, `OK`. Чистые счёт-строки 331f495 парсер принимает.

## Собственные кандидаты против грамматики

- `адрес: 015` → миграционная проба rc=1, одна ветвь «адрес не называет цель».
- Запрошенный чужой идентификатор `адрес: контракт 999` → rc=1: форма не удовлетворяет обязательному `0NN`.
- Чужой, но формально допустимый `адрес: контракт 014` → rc=0; это обход назначения из блокера 3.
- Пять регистровых/Unicode-вариантов одним прогоном — `КОНТРАКТ 015`, `контракт ０１５` (fullwidth digits), `контракт ٠١٥` (Arabic-Indic digits), `очередь АВТОНОМНОСТИ`, `contracts／015-x.md` (fullwidth slash) — rc=1, ровно 5 именованных отказов. Регистрового/Unicode-обхода на этих кандидатах нет.
- Неякорная подстрока `адрес: неконтракт 015` → rc=0; это обход границы категории из блокера 2.
- Проба «дрилла нет» сейчас красна rc=1; после реализации её собственный код требует существующий дрилл и его rc=0, а scoped `drill_nabludenia_nechitaemo` отдельно закреплён в критерии. Оmission-дрилла как самостоятельного обхода не найден.
