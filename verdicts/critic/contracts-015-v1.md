FAIL

Закоммиченный предмет: `2cc4e5fc5e08a8ac187cb7a9dc7dc3866874c505`. Тегов `frozen/contracts/015/*`: 0, поэтому по грамматике имени круг 2 перезаписывает `contracts-015-v1.md`.

АРБИТР: роль `arbiter` — повторный спор по причинам блокеров 1, 2 и 4 круга 1: допустима ли фраза о стаб-парах в контракте, какой якорь действительно называет назначение и как предъявлять предметный rc=2 через раннер, который rc=2 безусловно считает NOT_IMPLEMENTED.

## Блокирующие находки

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:201-202 — механическое закрытие Н-39 не состоялось: контракт всё ещё утверждает «Пары „стаб → вход“… по одному стабу на вход». Прямой требуемый поиск `Стабы и их входы|стаб →|симуляц` не пуст, а находит строку 201. Это повторное противоречие старшей норме `AGENTS.md:124-127`; по исключению роли отдельный обход не требуется.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:46-50 — закрытый список закрепляет только лексические якоря, но не механически различимое имя назначения. Закоммиченная фикстура красит только точку, а миграционная проба использует regex `контракт|contracts/|очеред|ROADMAP|правил|историческ|cognitive-only|закрыто|0[0-9][0-9]`: бессмысленные хвосты `адрес: очередь` и `адрес: правило роли` проходят его с rc=0.
ОБХОД: implementer реализует дословно этот regex и мигрирует все 32 записи в `ОТКРЫТО — адрес: очередь`. `case_adres_zaglushka.sh` остаётся красной на своей точке, миграционная проба и живой барьер зелены, scoped-предъявления проходят, но ни очередь/пачка, ни файл правила, где назначена починка, не названы — предмет «хвост НАЗЫВАЕТ ЦЕЛЬ» не сделан.

БЛОКИРУЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:59-62,227-228 — честная реализация обязана вернуть rc=2 на красном входе `case_nechitaemyj_fajl.sh`, тогда как обязательная scoped-команда обязана принять 28/28. Действующий `scripts/verify_antiplacebo.sh:664-665,677-678,694-695` любой rc=2 безусловно классифицирует как «нечем проверить / NOT_IMPLEMENTED» и валит фикстуру, в том числе повторный прогон. Поэтому объявленная команда не может доказать объявленную ветвь на честной реализации. Это противоречит норме `AGENTS.md:120-125`, по которой красный тест кодовой задачи должен пройти на честном дереве; по исключению роли обход не требуется.

## Механическая сверка шести блокеров круга 1

1. **Н-39 — НЕ ЗАКРЫТ.** Раздел с десятью конкретными парами удалён, но требуемый grep не пуст: одно совпадение в строке 201. Блокирует выше.
2. **Якорь цели + миграционная сверка — НЕ ЗАКРЫТ.** `case_adres_zaglushka.sh` и `probe_migracija_adresov.sh` добавлены; проба сейчас поимённо красит ровно 32 записи = 15 Н + 17 А. Однако якорь-заглушка из самого закрытого списка проходит обе объявленные меры. Блокирует выше.
3. **А-30/А-31 — ЗАКРЫТ.** Обе возвращены в живую очередь: А-30 → пачка Н-60, А-31 → пачка Н-61. Пересчёт даёт 8/4/20/2/2 = 36.
4. **Нечитаемый файл rc=2 — НЕ ЗАКРЫТ.** `case_nechitaemyj_fajl.sh` добавлена и действительно оставляет `chmod 000` для повторного прогона, но её требуемый предметный rc=2 несовместим с обязательным раннером. Блокирует выше.
5. **Fail-open двумя входами — ЗАКРЫТ.** Добавлены отдельные `case_vnutrennjaja_oshibka.sh` (внутренняя ошибка скрипта) и `case_otkaz_dochernego.sh` (отказ вызываемого скрипта внутри handler); всего в дрилле 7 case-файлов.
6. **Шесть digest-ветвей — ЗАКРЫТЫ поимённо.** Добавлены `case_ahead_behind_vrut.sh`, `case_status_anomalija.sh`, `case_remote_nedostupen.sh`, `case_ukazatel_handoff.sh`, `case_odno_soobshchenie_nextturn.sh`, `case_fallback_rasshirenija.sh`; всего в дрилле 12 case-файлов.

## Собственные кандидаты

- Якорь-заглушка испытан точным regex миграционной пробы: `ОТКРЫТО — адрес: очередь` и `ОТКРЫТО — адрес: правило роли` оба напечатаны как совпадения, rc=0. Это действительный обход и вынесено блокером.
- Подменённая запись с бессмысленным адресом из списка испытана той же мерой: последний маркер `ОТКРЫТО — адрес: очередь` удовлетворяет probe-regex; ни фикстуры с таким входом, ни сверки конкретного назначения таблицы нет. Это тот же обход блокера якоря, не отдельная причина.
- Каталог вместо `NABLIUDENIA.md` не принят как новый блокер: предмет строки 60-61 — «файл существует, но не читается»; каталог не является файлом. Красная фикстура предъявляет именно существующий обычный файл с mode 000. Дефект здесь другой и уже блокирует: честный rc=2 раннер не принимает.
- Ретрай `sendMessage(nextTurn)` проверен против состава входов. `case_odno_soobshchenie_nextturn.sh` исполняет fake-pi и ловит два обычных вызова; `case_fallback_rasshirenija.sh` форсирует падение дочернего скрипта. Отдельного входа, где первый `sendMessage` бросает после регистрации отправки и retry вызывает второй, нет. СОВЕТ contracts/015-jadro-avtonomnosti-nabljudenij.md:124-126 — если implementer добавит retry вокруг `sendMessage`, дрилл должен форсировать его; сейчас такой retry контрактом не заказан, а честно проходящий обход без реализованного дрилла механически не предъявлен, поэтому совет не добавляет причину FAIL.

## Свой пересчёт §Материал

Мера та же, что в круге 1: только заголовки `^### Н-NN.` / `^### А-NN.`, статус — последняя бэктик-группа заголовка.

- Н-файл: 17 ОТКРЫТО; адрес уже несут Н-50 и Н-64; без адреса ровно 15: Н-45, Н-41, Н-49, Н-61, Н-13, Н-14, Н-33, Н-35, Н-36, Н-38, Н-57, Н-60, Н-62, Н-63, Н-65.
- А-файл: 17 ОТКРЫТО и все без адреса: А-7, А-11, А-12, А-15, А-24, А-25, А-19, А-20, А-26, А-30, А-31, А-32, А-33, А-41, А-42, А-49, А-50. Без статусного маркера ровно А-16 и А-17.
- Категории таблицы: 8 мёртвых = Н-13, Н-33, Н-35, Н-38, А-11, А-12, А-20, А-24; 4 спорных = Н-57, А-15, А-26, А-41; 20 очередных/назначаемых = Н-14, Н-36, Н-41, Н-45, Н-49, Н-60, Н-61, Н-62, Н-63, Н-65, А-7, А-19, А-25, А-30, А-31, А-32, А-33, А-42, А-49, А-50; уже с адресом 2 = Н-50, Н-64; без маркера 2 = А-16, А-17. Арифметика: 8+4+20=32; 32+2+2=36.

## Прямые прогоны на 2cc4e5f

1. `bash fixtures/check_nabludenia/probe_nabludenia_krasnoe.sh` → rc=1, «барьера нет».
2. `bash fixtures/drill_gate_draft/probe_gate_draft_krasnyj.sh` → rc=1, «дрилла нет».
3. `bash fixtures/drill_startup_digest/probe_digest_krasnyj.sh` → rc=1, «дрилла нет».
4. `bash fixtures/check_nabludenia/probe_migracija_adresov.sh` → rc=1, ровно 32 поимённых отказа: 15 Н + 17 А; множества совпадают с независимым пересчётом выше.
5. `bash scripts/verify_antiplacebo.sh --scope check_charter` → rc=1, ровно 3 А-52-расхождения «фикстура без барьера»: `fixtures/check_nabludenia`, `fixtures/drill_gate_draft`, `fixtures/drill_startup_digest`; scoped-барьер 1, фикстур 7, красных 7.
6. `bash scripts/check_contract_frozen.sh` → rc=0: 18 планов/контрактов, 4 черновика, 14 заморожено; 015 распознан черновиком.
7. `npm run check:ci-parity` → rc=0: workflow-команд 25, npm-скриптов приёмки 37, исключений 12, расхождений 0.
8. Фактические case-счёты по дереву: 9 + 7 + 12 = 28.
9. Поиск `Стабы и их входы|стаб →|симуляц` по контракту → 1 совпадение (`:201`), а не требуемая пустота.
