FAIL

Предмет на HEAD `46f6f01464e0cba6b8974ef483959389ee07b682` закоммичен и комплектен:
предмет, исполняемый критерий, исполнители и две `ЗОНА`-строки есть. Scoped-регресс
`bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` завершился кодом 0 и
подтвердил 17/17 фикстур с зелёным контролем и повторным красным прогоном.
`bash scripts/check_contract_ready.sh /tmp/r011r2` на внешнем корне с симлинками завершился
кодом 0 (`OK`). На текущем дереве общий барьер завершился кодом 1 на `lock`; восемь отдельных
прогонов дали ожидаемое: `lock`, `scratch`, `scratchdef`, `chistka`, `norma`, `a010` — код 1
с именованной причиной, `pgid`, `carveout` — код 0.

По пяти пунктам круга 1:

1. Прежний обход `norma` с одной только противоположной формулировкой и двумя маркерами теперь
красный: `case_norma_protivopolozhnaya.sh` предъявлен красным с причиной `НЕ дословно`.
Однако требование поместить норму именно в раздел «Воркфлоу майлстоуна» всё ещё не наблюдается.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:20-29 — ветвь `norma` ищет
дословный блок по всему `AGENTS.md`, поэтому наличие букв не доказывает внесение нормы в
названный раздел. Это не вопрос исполнения нормы судьями, оставленный процессу в строках 34-37,
а проверяемая контрактом структура уставного файла.
ОБХОД: положить дословный трёхстрочный блок в раздел «Архив отклонённых предложений» внутри
code fence с пояснением «не является нормой», а в разделе «Воркфлоу майлстоуна» установить
противоположное правило «судья обязан гонять полный прогон». Проба
`bash scripts/check_runner_hygiene.sh /tmp/r011-norm-bypass norma` завершилась кодом 0 на таком
дереве, хотя предмет А.1 не сделан.

2. Прежний обход `scratchdef`, оставлявший постоянный `./tmp`, теперь красный:
`case_scratchdef_v_dereve.sh` названно падает. Но ветвь замечает любое дитя `TMPDIR`, а
стерегомое дерево в default-режиме осматривает только после завершения раннера.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:61-64 — предмет запрещает
run-каталоги, логи и артефакты в стерегомом дереве и требует его неизменности и во время, и
после прогона. Описание `scratchdef` в строках 125-127 слабее: оно не связывает найденный под
`TMPDIR` путь с фактическим scratch и не наблюдает дерево во время default-прогона.
ОБХОД: при пустой `VERIFY_ANTIPLACEBO_SCRATCH` создать под `TMPDIR` пустой каталог-приманку,
реальную работу вести в `<проверяемый-корень>/tmp/antiplacebo`, а trap удалить in-tree scratch
перед выходом. Ветка `scratchdef` и затем весь восьмиветочный барьер завершились кодом 0 на
`/tmp/r011-scratch-bypass`, хотя во время прогона scratch, run-каталог и лог находились в
стерегомом дереве.

3. Формат `<pid> <pgid> <epoch>`, pid/pgid владельца, ровно rc=3 и сохранность заполненного
чужого lock теперь наблюдаются; соответствующие новые фикстуры красны. Атомарность одновременного
захвата по-прежнему заменена последовательной пробой после заполнения lock.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:65-72 — предмет требует lock
против параллельных прогонов и атомарный захват. Строки 118-124 прямо исключают одновременную
гонку, но «отказ без перезаписи» после заполнения не является следствием, достаточным для
атомарного захвата: оно не наблюдает окно между созданием и заполнением файла.
ОБХОД: выполнить `(set -C; : > "$LOCK")`, выдержать паузу, затем записать три поля обычным
`printf > "$LOCK"`; увидев существующий пустой lock, другой старт считает владельца
неназванным, удаляет файл и также захватывает его. Весь восьмиветочный барьер завершился кодом
0 на `/tmp/r011-lock-bypass`, а два одновременно запущенных экземпляра над одним корнем и одним
scratch оба завершились кодом 0 вместо одного rc=3. Последовательная ветвь дожидается заполнения
и поэтому принимает этот неатомарный раннер.

4. Добавленные `check:charter` и `check_contract_frozen.sh` закрывают части прежнего обхода:
финальная зелень требует разрешение в изменяющем устав коммите, тег `frozen/contracts/010/2`
и файл `verdicts/critic/contracts-010-v2.md` с первой строкой `accept`. Но отдельный круг после
ранее закоммиченного блоба не доказан ни одной командой.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:46-50 — предмет требует три
последовательных акта: commit implementer, затем отдельный круг critic по уже закоммиченному
блобу, затем перезаморозку implementer. Конечные проверки в строках 146-153 проверяют наличие
разрешения, вердикта и тега, но не порядок коммитов и не раздельность актов.
ОБХОД: одним коммитом с автором `critic` внести раннер, `AGENTS.md`, аннотацию 010 и заранее
написанный `verdicts/critic/contracts-010-v2.md` с `accept`, после чего поставить
`frozen/contracts/010/2`. `check_charter` принимает разрешающие строки того же коммита,
`check_contract_frozen` видит совпавший блоб, тег и `accept`, а `check_zones` не проверяет
необъявленного автора. Все названные конечные проверки могут быть зелёными, хотя critic не
судил ранее закоммиченный блоб отдельным кругом и шаги 3-5 не состоялись.

5. Совет круга 1 по последовательности закрыт: строки 90-106 явно называют architect,
implementer и critic, закоммиченный вход critic, отдельный круг и последующую перезаморозку.

СОВЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:84-106 — мягко рекомендую добавить
`ЗОНА critic: verdicts/critic/contracts-010-v2.md`: сейчас исполнитель и файл шага 4 названы
в последовательности, но автор `critic` не объявлен `ЗОНА`-строкой, поэтому `check_zones.sh`
его вообще не проверяет. Это одновременно сделает границу шага явной и уберёт часть обхода
процедуры выше; сам по себе этот совет вердикт не определяет.

Итого: 4 блокирующие находки, 1 совет. Нужны правки критерия и круг 3; по норме третьего круга
после следующего вердикта предмет передаётся арбитру, а не в четвёртый круг критика.
