FAIL

Предмет на HEAD `81291c5e8a228732ac6914a2df2b79e6059dd095` закоммичен и комплектен:
предмет, исполняемый критерий, исполнители и три `ЗОНА`-строки есть.

Обязательные замеры выполнены на отдельном клоне. `bash scripts/verify_antiplacebo.sh
--scope check_runner_hygiene` завершился кодом 0 и предъявил 25/25 фикстур красными
после зелёного контроля; `--scope check_zones` — кодом 0 и 12/12. Внешний симлинк-корень
`bash scripts/check_contract_ready.sh /tmp/r011v3` дал `OK`, код 0. На текущем дереве
`check_runner_hygiene.sh` дал код 1 на `lock`; отдельные `lock`, `race`, `scratch`,
`scratchdef`, `chistka`, `norma`, `a010`, `porjadok` дали код 1 с именованными причинами,
а `pgid` и `carveout` — код 0. `bash scripts/check_zones.sh` завершился кодом 0.

По четырём обходам круга 2:

1. `norma`: единственность маркеров предъявлена, но обход размещения закрыт не полностью.
Настоящий Markdown-раздел всё ещё не отличён от строки заголовка внутри code fence.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:31-45 — объявленная
механика МЕСТА считает разделом любой текст после строки `## Воркфлоу майлстоуна`,
даже если эта строка и дословный блок находятся внутри архивного code fence и нормой
Markdown не являются. Это повтор причины круга 2: критерий по-прежнему не доказывает
помещение нормы в названный раздел.
ОБХОД: в настоящем разделе «Воркфлоу майлстоуна» оставить правило «судья обязан
гонять полный прогон», а в «Архиве отклонённых предложений» открыть code fence,
положить внутрь строку `## Воркфлоу майлстоуна` и дословный трёхстрочный блок, затем
закрыть fence. Оба маркера встречаются ровно по разу. Проба
`bash scripts/check_runner_hygiene.sh /tmp/r011-norma-codefence norma` завершилась
кодом 0, хотя предмет А.1 не сделан.

2. `scratchdef`: точный обход круга 2 с новым верхнеуровневым `<корень>/tmp`, пустой
приманкой и уборкой до выхода теперь красный; зонд и требование lock предъявлены.
Однако наличие правильно названного lock не доказывает, что найденный каталог —
фактический scratch, а неглубокий зонд дерева не доказывает байтовую неизменность
существующих поддеревьев.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:79-87 — предмет требует,
чтобы ВСЕ run-каталоги, логи и артефакты при пустой переменной находились под
`${TMPDIR:-/tmp}`, и чтобы стерегомое дерево было байт-в-байт неизменно во время
прогона. Критерий в строках 164-169 наблюдает лишь правильно названный lock под
`TMPDIR` и список путей первого уровня дерева; ни связь lock с реальными артефактами,
ни содержимое уже существующих поддеревьев этим не доказаны. Заявленный в контексте
остаток «третье место + фальшивый lock» в тексте контракта не объявлен
`cognitive-only`, вопреки правилу 1 `AGENTS.md`.
ОБХОД: два предъявленных состояния проходят ветвь `scratchdef` кодом 0. В
`/tmp/r011-scratch-third` обманка создаёт под подставным `TMPDIR` приманку с lock
верного hash8, а настоящий run-каталог и лог держит в `/dev/shm`; команда
`bash scripts/check_runner_hygiene.sh /tmp/r011-scratch-third scratchdef` зелёная.
В `/tmp/r011-scratch-nested` тот же фальшивый lock лежит под `TMPDIR`, а настоящий
run-каталог и лог живут во время прогона под уже существующим `$ROOT/scripts/` и
удаляются перед выходом; команда
`bash scripts/check_runner_hygiene.sh /tmp/r011-scratch-nested scratchdef` также
зелёная. В первом состоянии нарушено место scratch, во втором дополнительно нарушена
неизменность стерегомого дерева.

3. `race`: обход `(set -C; : > "$LOCK")`, пауза, затем `printf > "$LOCK"` закрыт
ветвью реальной одновременной гонки и `case_lock_real_race.sh`: фикстура предъявлена
красной с причиной `БЕЗ трёх полей`, зелёный контроль даёт ровно один rc=3 и один rc=0.
Нового блокирующего обхода этой части не найдено.

4. `porjadok`: точный обход круга 2, где critic одним коммитом вносит и реализацию,
и вердикт, закрыт совокупностью `ЗОНА critic`, красной фикстуры `check_zones` и
исторической ветви. Но критерий проверяет порядок только коммитов 010 и вердикта,
оставляя необязательной объявленную «одну пачку» до круга critic.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:53-70 — предмет требует,
чтобы акт (i) одним коммитом внёс раннер, AGENTS.md и аннотацию 010, а critic судил
СЛЕДУЮЩИМ кругом. `porjadok` доказывает только `commit(010) < commit(add verdict)`;
история раннера и AGENTS.md в эту меру не входит.
ОБХОД: implementer первым коммитом вносит только аннотацию 010; critic вторым
коммитом добавляет `accept`-вердикт; implementer третьим коммитом, уже ПОСЛЕ
вердикта, вносит раннер и норму AGENTS.md, каждый в своей объявленной зоне, после
чего ставится v2-тег. Внешняя история `/tmp/r011-order-late` принята командой
`bash scripts/check_runner_hygiene.sh /tmp/r011-order-late porjadok` с кодом 0,
хотя объявленной в строках 53-57 и 129-134 одной пачки до круга critic не было;
остальные конечные команды видят только итоговое состояние и этот порядок не
отвергают.

Совет круга 2 закрыт: `ЗОНА critic: verdicts/critic/contracts-010-v2.md` есть, а
`fixtures/check_zones/case_zones_critic_v_others.sh` предъявлена красной в составе
12/12. Объявленный cognitive-only остаток про противоположную формулировку БЕЗ
маркеров внутри настоящего раздела явно записан в строках 43-45 и сам по себе не
блокирует. Остаток про третье место scratch с фальшивым lock не записан — он вошёл
во вторую блокирующую находку выше.

Итого: 3 блокирующие находки, 0 советов.

АРБИТР: третий круг завершился FAIL; отдельно повторена причина «размещение нормы в
реальном разделе, а не архивном представлении». Предмет спора — достаточны ли
текстовые awk-границы для Markdown-раздела, а также обязаны ли механики `scratchdef`
и `porjadok` доказывать заявленные связь scratch с артефактами и состав акта (i).
Четвёртый круг критика не созывать.
