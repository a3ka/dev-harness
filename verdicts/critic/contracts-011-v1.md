FAIL

Предмет на HEAD `83245746d58bfa15e937cd2005f7002e27686b83` закоммичен и комплектен:
предмет, приёмочный критерий и две механически разбираемые `ЗОНА`-строки есть. Боль названа
по открытой Н-48 с ценой 3+ часа; граница V3=(а) и не-цели конечны. Красное предъявлено:
`bash scripts/check_runner_hygiene.sh` завершился кодом 1 на ветви `lock`,
`bash scripts/check_runner_hygiene.sh . norma` — кодом 1 на ветви `norma`, а
`bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` — кодом 0 и подтвердил
12/12 фикстур с зелёным контролем и повторным красным прогоном. Проводка и зоны также зелёные:
`npm run check:ci-parity` и `bash scripts/check_zones.sh` завершились кодом 0.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:21-26 — предмет требует
содержательную норму: каждому судье назначен ТОЛЬКО scoped-регресс и проверка неизменности
frozen-барьеров, полный прогон от судьи не требуется. Приёмка `norma` пинит лишь две подстроки,
а полный нормативный текст оставлен этому кругу критика в строках 128-129. Это одновременно
критерий слабее предмета, решение, молча переданное судье вместо автора контракта, и
противоречие правилам 1 и 8 `AGENTS.md`: смысл объявлен механически доказанным простым маркером,
без `cognitive-only` и названного ловца.
ОБХОД: внести в `AGENTS.md` строку
`ПРИЁМКА-СУДЬИ (v2, Н-48): утверждение «полный прогон — только CI» неверно; судья обязан гонять
полный прогон`. Обе искомые подстроки присутствуют, ветвь `norma` зелёная, но установленная
норма прямо противоположна предмету и не назначает ни scoped-регресс, ни git-diff.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:44-47 — пинованный API требует
при пустой `VERIFY_ANTIPLACEBO_SCRATCH` создавать scratch через `mktemp -d` под
`${TMPDIR:-/tmp}`, но команды приёмки этого режима не наблюдают. Ветви барьера, запускающие
раннер, всегда передают непустую `VERIFY_ANTIPLACEBO_SCRATCH`; регресс
`check_scoped_run.sh` проверяет поведение scoped-прогона, а не место default-scratch.
ОБХОД: после реализации поведения для явно заданной переменной оставить при пустой переменной
нынешний in-tree `./tmp`. Все ветви `check_runner_hygiene` получают переменную и зеленеют,
`check_scoped_run.sh` продолжает проходить на функциональном раннере, но обязательный default
через `${TMPDIR:-/tmp}` не реализован.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:48-54 — пинованный lock-API
требует атомарный захват, содержимое `<pid> <pgid> <epoch>` и ровно код 3 при живом владельце.
Ветвь `lock` проверяет только существование ожидаемого файла, сохранность первого прогона,
любой ненулевой код второго и подстроку `занят`; формат нового lock и атомарность захвата она
не проверяет. Двенадцать фикстур доказывают красноту известных обманок барьера, но не добавляют
наблюдение этих трёх требований на предмете.
ОБХОД: раннер пишет в ожидаемый путь обычным неатомарным `printf '%s\n' "$$" > "$LOCK"`,
то есть только pid, а при найденном живом pid печатает `занят` и выходит кодом 1. Последовательный
барьер дожидается появления файла перед вторым запуском, принимает любой `rc!=0` и не читает
трёхпольное содержимое; критерий зелёный, а три явно пинованных свойства отсутствуют.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:26-35 — обязательная процедура
двух уставных правок включает строки `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`, отдельный вердикт 010-v2 и
перезаморозку `frozen/contracts/010/2`, однако исполняемый критерий в строках 88-97 не запускает
ни `npm run check:charter`, ни `npm run check:contract-frozen`. Более того, строка 33 требует
вердикт 010-v2 «в тот же круг», когда будущая правка 010 ещё не закоммичена; это противоречит
правилам 9-11 `AGENTS.md` и норме роли критика судить только закоммиченный предмет. Такой
предварительный `accept` не может быть вердиктом по будущему блобу.
ОБХОД: реализовать раннер и два маркера, поместить фразу `отменено грилингом 2026-08-24` в
`contracts/010-…md`, но не создавать законный пост-правочный вердикт 010-v2, не ставить
`frozen/contracts/010/2` и закоммитить уставные правки без `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`. Барьер `a010`
ищет только фразу по всему файлу, `norma` — две подстроки, а остальные четыре названные команды
эту процедуру не проверяют: весь объявленный критерий выполнен, но предмет А.1-А.2 не сделан.

СОВЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:69-76 — при исправлении процедуры
явно назовите, кто и после какого закоммиченного блоба 010 создаёт вердикт 010-v2 и кто выполняет
перезаморозку. Сейчас зоны точно ограничивают пачку architect и реализацию implementer, но эти
два обязательных шага описаны вне последовательности исполнителей.

Итого: 4 блокирующие находки, 1 совет. По остальным пунктам круга блокеров нет: формат зон
механический и без шаблонов; Н-48 открыта и цена дана; предмет ограничен; счёт 12 подтверждён
независимым прогоном; явной пары «конкретный обманный стаб ↔ ветвь» в контракте нет; carve-out
охраняется; решение не убивать осиротевшие группы явно вынесено в 013, а не оставлено
исполнителю.
