accept

# Контракт 045 — узкий суд добавления fc7c536, редакция v3

Предмет: `contracts/045-pre-exchange-guard-gitw.md:251`, коммит
`d95c7b1d4071a238928e9332f64858df6104fa92`, на проверенном HEAD
`3a83a5f01dc0c101b1471024403f206585b64967`.
Блоб предмета и HEAD одинаков: `9962203c5794b590e473d7641150864e453e24e9`.

Область — только добавление второго SHA и его причины в существующую
СПАСЕНО-строку. Прежний контракт, реализация, решение владельца (А) и
предыдущий accept не переоткрываются. Набор полный: на HEAD есть предмет,
приёмка с командами/rc и именованные architect/implementer с ЗОНА-строками.
Имя файла v4 отличает сегодняшний суд от сохранённого исторического
`contracts-045-v3.md`; это не назначение версии frozen-тега.

## Четыре проверки

1. **Грамматика (:251) — соответствует.** Ровно два полных lowercase 40-hex
   SHA через одиночный пробел, автор `implementer` объявлен зоной; литеральный
   разделитель ` — `, непустая причина. Прежний SHA сохранён:

   ```text
   9c372335b2755540b15e569f47120472646a8335
   fc7c536adbb523e5e6ca415e8a218560332ed20b
   ```

   Для КАЖДОГО SHA исполнены `git cat-file -e <SHA>^{commit}`,
   `git show -s --format=%an <SHA>`,
   `git merge-base --is-ancestor frozen/contracts/045/1 <SHA>` и
   `git merge-base --is-ancestor <SHA> 3a83a5f01dc0c101b1471024403f206585b64967`.
   Все rc 0; оба автора — `implementer`. Это живые вызовы Git, не доверие
   проверке, описанной автором коммита.

2. **Область изменения (:251) — одна строка.**
   `git show --format=fuller --stat --patch d95c7b1` прочитан полностью
   (длинные строки дополнительно выведены с визуальным переносом без усечения).
   `git show --format= --numstat d95c7b1` → rc 0:

   ```text
   1	1	contracts/045-pre-exchange-guard-gitw.md
   ```

   Независимо сопоставлены строки двух Git-блобов `d95c7b1^:<путь>` и
   `HEAD:<путь>`: число строк одинаково; единственное различие — строка 251.
   Блоб `d95c7b1:<путь>` равен HEAD. Другие разделы и строки не менялись.

3. **Разрешение — синтаксически валидно.**
   `git show d95c7b1 -s --format=%B` → rc 0. Первая строка тела:

   ```text
   РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/045-pre-exchange-guard-gitw.md v3-черновик — списание fc7c536 (check_ids.sh ARBITRATION-класс, Н-150 третье повторение) СПАСЕНО-строкой, санкция (А) 2026-09-25
   ```

   Маркер в первой колонке, путь точно совпадает с единственным изменённым
   файлом, причина непуста. Прямое одобрение владельцем принято из задания;
   Git доказывает наличие текста, не личность произнёсшего разрешение.

4. **Основание (:251) — корень назван, не замаскирован.** Добавленный текст
   прямо называет fc7c536, implementer, оба затронутых пути,
   architect-зоны, «РАБОТА НЕ РАЗДАЁТСЯ», 023:355/031:187,
   ошибочный брифинг оркестратора, Н-150 и третье повторение.
   `git show --format=fuller --stat --patch fc7c536` → rc 0: автор implementer,
   реальные пути — `scripts/check_ids.sh` и
   `fixtures/check_ids/case_arbitration_contract_ref_ok.sh`; предмет коммита —
   ARBITRATION как link-class. В сообщении записана раздача через орк-канал.
   Обе названные строки 023:355/031:187 на HEAD действительно исключают
   `scripts/check_ids.sh` из раздачи. `git merge-base --is-ancestor 3cf248c
   fc7c536adbb523e5e6ca415e8a218560332ed20b` → rc 0: превенция предшествовала
   ошибочному касанию, поэтому основание не выдаёт её за будущую меру.

## Пять вопросов в пределах дельты

1. Критерий не сводится к существованию СПАСЕНО: проверены точные SHA,
   ancestry, авторы, разрешение и единственность изменённой строки.
   Состояния-обхода в назначенной области не найдено.
2. Все четыре условия проверены командами с rc; побайтовые и структурные
   утверждения проверены одноразовой Python-пробой над Git-блобами, rc 0.
3. Спасаемый коммит и причина заданы буквально, решения исполнителю
   молча не переданы. Прежнее основание сохранено без переписывания.
4. ЗОНА-строки и границы не менялись. Исторический SHA спасён точечно;
   будущие касания architect-путей implementer этим не разрешаются.
5. Противоречия AGENTS.md в этой дельте нет: разрешение присутствует,
   приёмка не ослаблена, правило Н-39 не затронуто, суд зафиксирован артефактом.

## Предел результата

Все проверки выполнены main-direct. Одноразовая проба
`python /tmp/dev-harness-verify/scratch/critic-043045-v3-probe.py` → rc 0;
она читала именно Git-блобы и исполняла перечисленные merge-base-команды.
Новых ЗОНА-строк/семей/гардов дельта не вводит: обязательный триггер
precision-гейта не возникает. Precision, полный suite, check_zones и
проверка реализации заново не запускались. Нулевой check_zones после
будущей заморозки и готовность done этим accept не утверждаются.

Блокирующих находок и новых советов по назначенной дельте нет.
Контракты, теги и прежние вердикты не изменены; push не выполнялся.
