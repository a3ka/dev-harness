FAIL

# Контракт 043: СПАСЕНО-строка черновика v2

Предмет — только `contracts/043-precizionnyj-prefriz-gejt.md:305` на
`96dcb449b95db2c212b81e57e3084008c8a9e111`, а не повторный суд реализации 043.
База: `frozen/contracts/043/1^{}` =
`f2259f8f243440b6dbe4a286b35246f313efe606`.
Сам аннотированный тег имеет другой объект:
`e2a12bca1c2173a18ee2a8196f41cb5ac534424a`.

Набор полный: контракт закоммичен, предмет и команды приёмки существуют,
исполнители и их зоны названы. Дельта не вводит новых ЗОНА-строк, семей
фикстур или гардов. Критерии исходной реализации и её прежние споры не
переоткрывались. Единственный блокер — ложная атрибуция доказательства в
самой строке, которую предлагается заморозить как честное основание исключения.

## Находки

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:305 — строка приписывает уже состоявшийся негативный/позитивный контроль ревьюеру круга 2 и ссылается на `verdicts/review/contracts-043-v1.md`, но этот закоммиченный файл является вердиктом круга 1 ДО исправления. Его Н2 описывает ещё дефектный вход с повторным 991 и предлагает исправление, а не подтверждает исправленный 993.
ОБХОД: дерево `96dcb449` уже удовлетворяет структурным проверкам предлагаемого исключения — одна добавленная строка, ровно два нужных SHA, правильный автор, правильная грамматика, тривиальный дифф профиля. При этом оно выдаёт проверку ДО исправления за уже проведённый независимый контроль ПОСЛЕ исправления. Если принять эти команды за достаточное доказательство всей СПАСЕНО-строки, ложное свидетельство второго судейского круга заморозится вместе с действительным зонным исключением; требуемое честное обоснование не получено.

Это не требование нового круга реализации и не обвинение в небезопасности
замены. Для снятия достаточно исправить атрибуцию в этой же строке:
убрать неподтверждённое утверждение либо сослаться на действительно
закоммиченный контроль исправленного состояния с его настоящим автором и
предметом. Параллельный будущий вердикт не делает старую ссылку верной.

Живая проверка хронологии:

```text
$ git log -1 --format='%H%n%an%n%aI%n%s' HEAD -- verdicts/review/contracts-043-v1.md
3af3e06b455c27c9d6ae7f9a4e5f8d1f30cdc001
reviewer
2026-09-24T11:32:20+02:00
reviewer: контракт 043 — FAIL, 6 находок (1 блокирует done, 1 владельцу, 4 совета)
[rc=0]
```

В конце самого файла: «Ревьюер, круг 1, 2026-09-24».
`git log --no-walk --format=fuller 80e1ea84 8766e769 4de64aa` дал rc 0:
исправление `80e1ea84` имеет AuthorDate `2026-09-24 11:47:03 +0200`;
его собственное сообщение заявляет контроль от implementer, не от reviewer.

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:305 — «однострочная замена» неточно описывает профиль: заменены ДВЕ строки, обе только `991→993` — вызов создания заморозки и согласованная строка ПЕРЕСЕЧЕНИЕ. Скрытой дополнительной логики в диффе этого пути нет. Лучше назвать именно две согласованные замены, а «мёртвой» — прежнюю часть входа, не всю исполняемую батарею. Эта неточность сама по себе не блокирует: семантически это один тривиальный фикс идентификатора, обхода защиты им не показано.

## Проверки по заданию

Все команды выполнены в отдельном SSH-клоне
`/tmp/dev-harness-verify/critic043v2-spaseno-20260924`.
Сразу после клонирования `git remote -v` вернул rc 0:

```text
origin  ssh://git@github.com/a3ka/dev-harness.git (fetch)
origin  ssh://git@github.com/a3ka/dev-harness.git (push)
```

### 1. Дельта действительно ровно одна строка

`git diff frozen/contracts/043/1 -- contracts/043-precizionnyj-prefriz-gejt.md`
дал rc 0. Полный патч прочитан: единственная вставка — СПАСЕНО на строке 305;
удалений и посторонних изменений нет. Независимое представление объёма:

```text
$ git diff --numstat frozen/contracts/043/1 HEAD -- contracts/043-precizionnyj-prefriz-gejt.md
1	0	contracts/043-precizionnyj-prefriz-gejt.md
[rc=0]
```

### 2. Набор исключений точный

`bash scripts/check_zones.sh .` на исходном HEAD дал rc 1. Во всём выводе
ровно две строки FAIL; ниже их дословные начала до списка объединённой зоны:

```text
  FAIL коммит вне зоны: implementer 80e1ea84 fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh
  FAIL коммит вне зоны: implementer 8766e769 contracts/043-precizionnyj-prefriz-gejt.md
```

Последняя строка вывода:

```text
замороженных контрактов: 42 · объявленных авторов: 2 · коммитов в диапазонах: 1926 · проверено по зонам: 1290
```

СПАСЕНО перечисляет ровно эти два полных SHA и ровно автора `implementer`:

- `80e1ea842f17332fa11d91cba35aade740f00515`;
- `8766e76946658b548bb9d4be870dfcf0b97ef288`.

Фактические авторы обоих коммитов проверены через git, не выведены из текста
контракта. Красный код сейчас ожидаем: барьер читает замороженный v1, не
этот черновик. Никакого третьего спасаемого коммита добавлять не требуется.

### 3. Грамматика и реальная причина

`git log --all --oneline -- 'contracts/031-*.md'` дал rc 0 и показал
`645aa89` — добавление единой СПАСЕНО-секции 031 v4. Сверен сам прецедент
`contracts/031-strazh-semejstvo-mint-i-bazlajn.md:332`, а также найденные
СПАСЕНО-строки в 013/016/024/025/026/027/028/036. В 032 такой строки поиск
не обнаружил; существования прецедента там не утверждаю.

Формат совпадает: `СПАСЕНО <роль>: <полные SHA через пробел> — <причина>`.
Корневая причина в 043 названа прямо: оркестратор ошибочно назвал
architect-зонный путь implementer-зоной в задании Н2-фикса. Это согласуется
с сообщениями коммитов 80e1ea84 и 8766e769; второй выход за зону также не
скрыт — implementer сам добавил исключение в architect-зонный контракт.

Живой `git show 80e1ea84 -- fixtures/parsing_hygiene_battery/profiles/check_precision_gate.sh`
дал rc 0. Весь предметный дифф профиля:

```diff
-  mk_foreign_frozen "$w" 991 alice one/first.txt
+  mk_foreign_frozen "$w" 993 alice one/first.txt
-ПЕРЕСЕЧЕНИЕ architect: one/first.txt — 991 объявлен для второго пути'
+ПЕРЕСЕЧЕНИЕ architect: one/first.txt — 993 объявлен для второго пути'
```

Иных изменений этого файла в коммите нет. Это узкая проверка профиля:
остальные, законно зонированные изменения коммита 80e1ea84 здесь не судятся.

### 4. Консолидация архитектора не требует спасения

`git show frozen/contracts/043/1:contracts/043-precizionnyj-prefriz-gejt.md`
дал rc 0; исходная строка 275 уже включает под `ЗОНА architect` дословный
путь `contracts/043-precizionnyj-prefriz-gejt.md` и путь батарейного профиля.
`git show --format=short 4de64aa -- contracts/043-precizionnyj-prefriz-gejt.md`
дал rc 0: автор `architect`, изменена только СПАСЕНО-строка. В живом выводе
check_zones нет FAIL для `4de64aa195acc309dd6d4cee876515ebeed28725`.

### 5. Дополнительный ограниченный прогон

```text
$ bash fixtures/parsing_hygiene_battery/run_battery.sh check_precision_gate
БАТАРЕЯ check_precision_gate: класс delimiter-collision      — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс regex-injection          — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс silent-drop              — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: класс self-application-green   — закрыт (rc 0)
БАТАРЕЯ check_precision_gate: итог 4/4 классов закрыто
[rc=0]

$ bash scripts/check_precision_gate.sh . contracts/043-precizionnyj-prefriz-gejt.md
precision-гейт 043: семья precision_gate — барьер правится этим же черновиком, задача (б) для неё пропущена (прецедент 036 В4-3)
OK
[rc=0]
```

Последний вывод — отдельный последовательный запуск. Первый запуск гейта
одновременно с батареей вернул rc 0 и OK, но сопровождался ошибками
`lib_zones.sh: ... zones_scoped: No such file or directory`; его чистым
доказательством не считаю. Причина этой конкурентной аномалии здесь не
установлена, реализацию библиотеки этим вердиктом не судил. Негативные
мутации реализации не выполнял: это отдельный ревьюерский трек. Мой
зелёный прогон не подменяет приписанный строкой чужой негативный контроль.

## Граница решения и следующий шаг

Это первый отказ именно по СПАСЕНО-черновику, а не очередной круг о логике
precision-гейта. Контракт, зоны, реализация и прежние вердикты мной не
изменены. Заморозку v2 и теги не создавал.

После исправления основания и получения accept следующий шаг ВНЕ моих
полномочий: оркестратор запрашивает у владельца подпись
`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` для фактической заморозки v2. Сам эту строку не пишу
(правило 11 AGENTS.md). Файл `verdicts/critic/contracts-043-v2.md` не создаю:
он зарезервирован для отдельной консолидации в момент фактической заморозки.
