FAIL

БЛОКИРУЕТ contracts/012-izoljacija-progonov.md:36-39,58-61 — ветвь `techka`
не доказывает объявленную неприкосновенность default-скратча, существовавшего до
прогона; более того, названный «честным» эталон сам нарушает эту часть предмета.
ОБХОД: при пустой `VERIFY_ANTIPLACEBO_SCRATCH` реализация безусловно ставит признак
владения лишь по факту пустой переменной, даже если детерминированный каталог уже
существовал, а в `EXIT` делает `rm -rf "$SCRATCH"`. Все шесть приёмочных ветвей это
пропускают: `techka` кладёт `pre-marker` только в родительский `$TMPDIR`, но ничего
не кладёт в существующий детерминированный default-каталог. Именно это состояние уже
реализовано в `fixtures/check_runner_hygiene/_ref_runner.sh:44-48,153-158`: прямой
прогон с заранее созданным
`${TMPDIR}/verify_antiplacebo-<hash8>/preexisting` завершился rc=0 и удалил
`preexisting`, тогда как зелёный контроль ветви `techka` принимает тот же эталон.

СОВЕТ contracts/012-izoljacija-progonov.md:48 — сузить зоны implementer с `roles/`
и `.omp/` до трёх реально названных файлов: `roles/orchestrator.md`,
`roles/architect.md`, `.omp/config.yml`. Текущие каталожные зоны механически разрешают
этому майлстоуну менять любые роли и любой проектный конфиг, хотя предмет таких правок
не называет.

Проверка остальных обязательных граней:

- §Не-цели (`contracts/012-izoljacija-progonov.md:91-98`) явно оставляет
  `models_actual`/fallback отдельному контракту и запрещает изоляцию кодом раннера;
  смешения класса с топологией нет.
- Райдер (i) не декларативный: на закоммиченном HEAD
  `bash scripts/check_runner_hygiene.sh . lockdef` завершился rc=1 с точной причиной
  «второй default-прогон ... кодом 0, а не 3». Контрольный корень с общим
  default-скратчем завершил ту же ветвь rc=0, причём второй живой прогон отказал
  rc=3 «занят».
- Нормативная часть А входит в приёмку, а не потеряна за райдерами Б: ветви
  `izolcfg`, `klon`, `izolnorm` и их три отдельные красные фикстуры присутствуют.
  На текущем до-предметном HEAD все три ветви ожидаемо завершились rc=1 с
  именованными причинами отсутствия `task.isolation.mode: btrfs`, внешнего клона
  architect и `isolated: true`; их внесение назначено implementer в строке 48.
- `bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` завершился
  rc=0: 39/39 фикстур имеют зелёный контроль и предъявлены красными повторным
  прогоном. Все шесть новых ветвей на текущем HEAD завершились ожидаемым rc=1.
  Внешний `bash scripts/check_contract_ready.sh /tmp/critic012-ready` завершился
  `OK`, rc=0.
