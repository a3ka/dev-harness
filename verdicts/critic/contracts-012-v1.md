accept

Блокер круга 1 закрыт. Контракт теперь прямо требует неприкосновенности самого
существовавшего default-скратча и его содержимого
(`contracts/012-izoljacija-progonov.md:38-43`); исправленный эталон ставит
`OWN_SCRATCH` только при отсутствии каталога и не метёт безымянное содержимое
чужого default-скратча
(`fixtures/check_runner_hygiene/_ref_runner.sh:47-58,114-117,164-171`). Ветвь
`techka` создаёт `preexisting` непосредственно в детерминированном
default-скратче и требует его сохранности
(`scripts/check_runner_hygiene.sh:775-790`).

Прямой зонд прежнего обхода повторён на новом эталоне: для пустого корня
`/tmp/critic012-r2-probe-root` заранее создан файл
`/tmp/critic012-r2-probe-tmp/verify_antiplacebo-f749e858/preexisting`, затем при
пустой `VERIFY_ANTIPLACEBO_SCRATCH` запущен
`fixtures/check_runner_hygiene/_ref_runner.sh`. Прогон завершился rc=0; после
него `stat` подтвердил существование файла (`exists size=0`). Следовательно,
состояние дерева из ОБХОДА круга 1 больше не удовлетворяет приёмке за счёт
дефектного «честного» эталона.

Новая фикстура
`fixtures/check_runner_hygiene/case_techka_udaljaet_chuzhoj.sh:1-36` предъявляет
ровно прежний дефект: зелёный контроль на исправленном эталоне проходит, а
обманка с безусловным `rm -rf "$D"` краснеет rc=1 с именной причиной «удалила
существовавший до прогона default-скратч». Самостоятельный
`bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` завершился
rc=0: 40/40 фикстур предъявлены красным повторным прогоном; строка этой фикстуры
отдельно подтверждает зелёный контроль и названную красную причину.

Совет круга 1 применён дословно: `ЗОНА implementer` содержит раннер и ровно три
названных файла нормы — `roles/orchestrator.md`, `roles/architect.md`,
`.omp/config.yml` (`contracts/012-izoljacija-progonov.md:53`). Счёт контракта —
40 (`contracts/012-izoljacija-progonov.md:75,77-79`) — совпадает с фактическими
40 `case_*.sh` и итогом scoped-прогона.

Новых классов дефектов в изменённых коммитом `268ad56` частях предмета,
эталона, ветви `techka` и новой фикстуры не найдено. Остальные пять ветвей
круга 1 повторно не пересуждались по норме узкого круга 2. Блокирующих находок
и советов нет.
