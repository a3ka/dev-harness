FAIL

# Контракт 037 v2 — первый круг критика текста

Предмет: `contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` на HEAD
`7bff6166252d14f9ad73f0486e7a314d9b45a1ab`. Блоб
`d78ba7ed4f7a6143e5bbff0ab32872c47ae23124` совпадает с блобом коммита
архитектора `19678a0034fba8906a4103965516494de2ae58d2`.
Последний тег — `frozen/contracts/037/1`; это первый круг именно v2,
не повтор прежнего критик-круга и не повтор суда реализации.

Рабочее место — отдельный SSH-клон
`/tmp/dev-harness-verify/critic037-v2-20260924`. Сразу после клонирования
`git remote -v` показал `ssh://git@github.com/a3ka/dev-harness.git` для fetch
и push. HEAD и origin/main совпали на `7bff616`, то есть сдвинулись относительно
переданного при раздаче `5edeab8`. Главный рабочий каталог не читался и не менялся.

Набор артефактов полный: предмет закоммичен; критерий с командами находится
в :577–641; исполнители и ЗОНА-строки — :527–535. Код path-guard и приёмника
не разбирался, их текущая корректность этим вердиктом не оценивается.
Названные контрактом фикстуры прочитаны только для вопроса критика:
доказывает ли объявленная команда новый пункт критерия готовности.

## Блокирующая находка

БЛОКИРУЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:637 — Р2 обещает доказать «именованный отказ 4а с sha», но названная команда этого не доказывает. В `fixtures/accept_task_commit/red_accept_task_commit.sh:198–211` ветвь п7 проверяет лишь ненулевой rc, неизменность целевой ветки и наличие подстроки «мерж». SHA созданного мержа не извлекается и с диагностикой не сравнивается; точный rc 1 из М2 п.4а (:408) тоже не проверяется. Это дефект доказуемости нового критерия, а не утверждение о дефекте нынешнего `accept_task_commit.sh`.
ОБХОД: реализация честно выполняет остальные пункты М2, обнаруживает мерж до cherry-pick, сохраняет ветку и не оставляет worktree, но печатает только `ОТКАЗ: мерж не поддерживается` без SHA и возвращает 2. Названная ветвь п7 принимает такой результат: rc ненулевой, слово «мерж» есть, ветка прежняя; остальные ветви и структурные проверки от этой локальной разницы не меняются. Командный критерий зелёный, но новый обязательный результат М2 п.4а — rc 1 и SHA отклонённого мержа — не предъявлен.

Закрытие: усилить названную архитекторскую фикстуру проверкой точного rc 1
и SHA реально созданного ею мержа. Ослаблять текст п.4а или убирать SHA из Р2
не требуется. Это один дефект соответствия команды критерию, не два круга
и не требование повторно судить реализацию. Обход выше выведен из условий
проверки; подмена субъекта и адверсарий-прогон в этом текстовом круге не делались.

## М1: п.4, п.5 и п.6 не заменяют друг друга

На вопрос задания ответ однозначный: **текст требует самостоятельного
HOME-условия п.4; п.6 не разрешает его удалить**.

- :127 объявляет «⟺ ОДНОВРЕМЕННО», то есть конъюнкцию всех условий.
- :142–144 отдельно требует `isWithin(realpath(process.env.HOME), canonicalActual)`
  и отказ при пустом/отсутствующем HOME. Отсутствие `.git` у предков не доказывает
  принадлежность HOME и не подменяет это требование.
- :315–317 повторяет применение п.2–6 целиком. Норма ПРОВОДКА :729 также
  перечисляет HOME, владельца и анцестор-проверку отдельными условиями.
- :261–274 требует отказа при `.git`-предке до чтения owner.json, включая
  gitfile с мёртвым gitdir. П.5 задаёт необходимое условие разрешения,
  а не обязательный ранний побочный эффект: порядок «проверить п.6, затем
  читать owner для п.5» удовлетворяет обоим пунктам.
- Отличие вложенного cwd от вложенной цели сформулировано в :300–306
  и :647–654. Отказ нового cwd не запрещает владельцу клона писать
  в его собственный вложенный каталог как цель.

Сообщённое выпадение п.4 из реализации — нарушение этого ясного текста,
а не его допустимое прочтение. Я его не воспроизводил и не использую
основанием настоящего FAIL.

СОВЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:267 — фразу «canonicalActual === HOME никогда не допускается по п.5» точнее связать с описанной раскладкой артефактов харнеса, а не с одним предикатом п.5. Сам п.4 равенство допускает, п.5 проверяет наличие и содержимое owner, но не сравнивает actual с HOME. В штатной описанной топологии нужного owner над HOME нет; отдельного достижимого обхода внутри заявленной среды эта текстовая проверка не предъявляет, поэтому это совет, не дополнительный блокер.

## М2: решение и его основание названы

П.4а (:407–418) определяет класс мержа числом родителей > 1, именованный
отказ с SHA, rc 1, неизменность ветки и обнаружение до cherry-pick.
Он не оставляет исполнителю выбора между отказом и `-m N`.
Основание отказа содержательное: identity не определяет, какую родительскую
линию считать mainline; это решение автора источника, не приёмника.
Линеаризацию выполняет агент в скретч-клоне. Граница отражена в модели угроз
и role-канале. Блокер выше касается только обещания доказать весь этот
диагностический результат названной командой.

## Собственные проверки

Все команды ниже выполнены на указанном HEAD в отдельном клоне.
Вместо shell-glob указан его единственный соответствующий файл 037.

| Команда | rc | Наблюдение |
|---|---:|---|
| `bash scripts/check_threat_model.sh . contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` | 0 | `ЗАЩИЩАЕТ 6 буллет(ов), НЕ ЗАЩИЩАЕТ 5 буллет(ов)` |
| `bash scripts/check_provodka.sh . contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` | 0 | Вывода нет |
| `bash scripts/check_precision_gate.sh . contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` | 0 | `OK` |
| `bash fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh .` | 0 | `ИТОГ 032: ветвей 31, красных 0, зелёных 31` |
| `bash fixtures/check_runner_hygiene/red_pin_allowlist.sh .` | 0 | Вывода нет |
| `bash fixtures/check_runner_hygiene/red_granica_nepin_pipe_tee.sh .` | 0 | Вывода нет |
| `bash fixtures/check_runner_hygiene/red_strazh_vectora_utechki.sh .` | 0 | `ЗЕЛЁНОЕ 025-дыра-B`, различены восемь названных сценариев |
| `node tmp/critic037-check-text.cjs` | 0 | `Три полные norm-строки совпадают побайтово; независимый счёт модели угроз: 6/5.` |
| `git diff frozen/contracts/037/1 -- contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` | 0 | Непустой diff: 157 добавлений, 53 удаления |

Временная Node-проверка читала блобы контракта и ролей через `git show HEAD:…`,
выделяла ровно три `role=`-объявления и сравнивала каждую целую норму с целой
строкой целевого файла через `Buffer.equals`. Тем самым проверены и обе
изменённые нормы архитектора, а не только наличие имён файлов.
Независимый счёт списков модели угроз — строки с префиксом `- ` в её секции;
он не использовал вывод `check_threat_model.sh`. Временный скрипт удалён.

Полный diff прочитан: изменения объясняются добавлением п.6 и 4а,
обновлением модели угроз/двух норм-строк, переносом нового вложенного cwd
из остаточного риска в обязательный отказ и усилением описанных Р1/Р2.
Учёт argv-багa относится к прежнему М2 п.5, не вводит новой политики.
Изменения текста v+1 сами по себе не объявляются регрессией и не обязаны
давать пустой diff с v1. Исторические «красное сейчас» — снимок пачки
архитектора, не утверждение о результате параллельного implementer-круга.
Р1/Р2 реализации и общепроектный прогон в этом круге не запускались.

## Пять обязательных вопросов

1. Критерий слабее предмета: один конкретный обход доказательного Р2 указан выше.
2. Каждый новый обязательный результат доказуем командой: SHA и точный rc
   нового отказа пока не доказуемы названной командой; это тот же блокер.
3. Решение молча оставлено исполнителю: в п.4/п.5/п.6 и политике мержа — нет;
   способ обхода предков и получения числа родителей остаётся деталью реализации.
4. Границы исполнения: контракт/фикстуры/нормы принадлежат architect,
   guard/приёмник/профиль батареи — implementer; зоны судей отделены,
   precision-гейт зелёный. Исправление критерия помещается в объявленную зону.
5. Противоречие AGENTS.md: дополнительного нормативного блокера в изменениях
   v2 не найдено. Предмет имеет модель угроз и проводку; вывод регрессий —
   коды возврата. Суд реализации и полномочия владельца не подменяются.

Итог: **FAIL — один блокер доказуемости нового приёмочного результата,
один совет**. Это первый критик-круг v2; арбитраж повторной причины не возникает.
Контракт, роли, фикстуры и реализация критиком не изменены.
