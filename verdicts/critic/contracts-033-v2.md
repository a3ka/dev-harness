accept

# Критик — контракт 033 v2, поправка B с двумя гвардами владельца

Предмет: закоммиченная дельта `478b8143789b3c81a7566056c8b7ebffb2452797 → 76fcd00cf097fbf88727f52641811350a6502b4d`. Проверен HEAD отдельного клона `/tmp/dev-harness-verify/critic-033-v3/repo`. Решённое в к1–к4 не переоткрывалось.

Набор артефактов полный: предмет, исполнимые критерии, именованный исполнитель и границы присутствуют. Блокирующих находок и советов в заданной дельте нет.

## ПОСТАНОВЛЕНИЕ — явный affirm по гварду-2

**Постановляю: признание `empty→green` у общего барьера `check_zones` в контракте 033 v2 — легитимная КОРРЕКЦИЯ ложного утверждения v1 о механике, а не ослабление приёмки. Это содержательное решение о допустимости коррекции, не просто accept редакции текста.**

Основание — анализ консультанта `verdicts/consultant/pustaja-vyborka-033-lechenie-v1.md`, §2, сверенный с исходниками и собственными scoped-прогонами:

1. **Пустые окна легальны и возникают конструктивно.** `scripts/freeze_contract.sh:301–305` ставит аннотированный тег на текущем HEAD, не создавая следующего коммита. Диапазон первой заморозки до HEAD в таком состоянии пуст. Правило диапазонов закреплено в `scripts/lib_zones.sh:244–250`. Отсутствие последующей работы само по себе не является нарушением зон.
2. **«Зон не объявлено» согласовано с CI.** Ветка `scripts/check_zones.sh:245–249` возвращает 0 при отсутствии зон и иных нарушений. `.github/workflows/ci.yml:242–246` прямо называет отсутствие контрактов с зонами законным состоянием, не `NOT_IMPLEMENTED`. Коррекция не придумывает новую уступку барьеру: она честно описывает действующее соглашение с его потребителем.
3. **Четыре действующие фикстуры имеют зелёные контроли на пустоте.** `case_kommit_vne_zony.sh:12–13` и `case_zona_vne_grammatiki.sh:12–13` вызывают барьер непосредственно после `make_repo`, до судимого коммита. `case_ni_zon_ni_otkaza.sh:11–12` и `case_otkaz_bez_prichiny.sh:11–12` начинают с законного объявления «РАБОТА НЕ РАЗДАЁТСЯ», без зон. Собственный scoped-прогон подтвердил зелёные контроли и целевые красные повторы всех четырёх. Следовательно, вариант A с безусловным отказом на этих пустых состояниях ломает эти четыре контроля до достижения их целевых красных ветвей. Это вывод из исполненных контролей и последовательности команд с `set -e`; отдельная реализация A не создавалась и не испытывалась.
4. **Пустая ПРИЁМКА остаётся красной.** В `contracts/033-union-semantika-zon-check-zones.md:85–95` общий `rc 0` означает только «нарушений не выявлено». Достаточность доказательства вынесена в именованную `Непустота-выборки-033-проверку`, требующую `N>0`. Обе записанные пустые сводки адверсария эта команда отвергает собственным прогоном, rc 1. Union-семантика по-прежнему защищена различающей фикстурой, а не наличием счётчика.

Таким образом, норма «пустая выборка — красное» сохранена на уровне приёмочного доказательства. Удаление ложного обещания из v1 сопровождается действующим гвардом, а не разрешением принимать пустой зелёный запуск. **Без гварда-1 подобное удаление было бы ослаблением; в рассматриваемом HEAD гвард присутствует и проверен.**

## Гвард-1 — собственные исполнения

Команда извлечена из закоммиченной строки `contracts/033-union-semantika-zon-check-zones.md:95`, без переписывания предиката. Для пустых случаев отдельные подставные источники возвращали rc 0 и дословно печатали в stderr сводки из закоммиченного вердикта адверсария к1. Тем самым проверялся новый приёмочный предикат на уже установленном входе, а не повторно доказывался известный дефект v1.

| Вход | Результат Непустота-выборки-033-проверки |
|---|---:|
| `замороженных контрактов: 1 · объявленных авторов: 1 · коммитов в диапазонах: 0 · проверено по зонам: 0` | rc 1 |
| `замороженных контрактов: 0 · зон не объявлено — проверять нечего` | rc 1 |
| Живой check_zones на HEAD 76fcd00, `проверено по зонам: 739` | rc 0 |

Отдельный живой `bash scripts/check_zones.sh .` вернул **rc 0**. Сводка из stderr:

```text
замороженных контрактов: 31 · объявленных авторов: 1 · коммитов в диапазонах: 1141 · проверено по зонам: 739
```

Это именно определённая контрактом мера — число «проверено по зонам», не число уникальных коммитов или проверенных путей. Именованный предикат не заменяет самостоятельного требования rc 0 прогона 2: приёмка требует оба условия.

Прогон 1 в `contracts/033-union-semantika-zon-check-zones.md:81–83` побайтово сохранён. Собственный запуск:

```text
VERIFY_ANTIPLACEBO_SCRATCH=/tmp/dev-harness-verify/critic-033-v3/antiplacebo
bash scripts/verify_antiplacebo.sh --scope check_zones
```

Результат: **rc 0**, один барьер, **20 фикстур, 20 предъявлены красным повторным прогоном**. В частности, `case_union_zon_vseh_zamorozok.sh` имеет зелёный контроль и красный повтор rc 1 с причиной «коммит вне зоны». Это scoped-доказательство, не заявление о полном CI.

## Синхрон /1→/2 и сохранность истории

В `contracts/033-union-semantika-zon-check-zones.md:103,104,120` переведены все **пять литеральных ссылок**: три в команде проверки заморозки, одна в команде закрытия Н-100 и одна в дословном закрывающем токене. Полных старых ссылок `frozen/contracts/033/1` в новом тексте нет; упоминания `/1` как истории оставлены явно.

Собственные исполнения:

- старая Заморозка-033-проверка, извлечённая из `478b814`, на новом дереве → **rc 1**;
- новая Заморозка-033-проверка до создания `/2` → **rc 128**, отказ на отсутствующем ref; это ожидаемое ненулевое состояние до freeze v2;
- исторический `/1` существует и остаётся аннотированным объектом `tag`.

Объекты:

```text
frozen/contracts/033/1: 1815e49c38614254d255cb664c0b494c836e6629
blob контракта в /1:  4e60c383de6f64e17d3ca95f44e89123fd399948
blob контракта в v2:  a3448b94e13e285022a45c5b31b02472301ad0a8
```

Следовательно, историческая заморозка не может удостоверить исправленный текст. Логика закрывающей команды, разрешённая арбитражем и закрытая к4, не изменена: синхронизированы версия тега и ссылки на строки.

## А-182 и границы дельты

`NABLIUDENIA_ARCHITECT.md:4103–4109` корректно фиксирует установленный класс ошибки: нормативное требование к достаточности замера было объявлено свойством rc инструмента. Локальное лечение отмечено как частичное, общая проблема оставлена открытой; ложного объявления общего исправления нет.

Сравнение закоммиченных деревьев показало ровно два изменённых файла:

- `contracts/033-union-semantika-zon-check-zones.md`;
- `NABLIUDENIA_ARCHITECT.md` — только добавление А-182, предыдущий текст сохранён.

Предмет, frontier, инварианты 1–5 и раздел зон контракта побайтово неизменны. Код, фикстуры, CI, СПАСЕНО-заморозки и реестр не менялись. Исполнитель `architect` и его два пути названы в строке :73; запрет кодовой раздачи и отдельные каналы остальных работ сохранены в :74.

Пять обязательных вопросов критика в пределах дельты закрыты: критерий не подменяет предмет пустым запуском; новые проверяемые требования имеют команды с rc; неоговорённой реализации не раздаётся; границы сохранены; противоречия норме AGENTS.md не выявлено — пустое доказательство отвергается, а изменение frozen-текста требует отдельной разрешающей процедуры v2.

## Доставка и предел решения

**Итог: accept с явным affirm коррекции B.** Это разрешение продолжить маршрут к подписи владельца через РАЗРЕШИЛ-owner-скрипт, затем freeze v2 и повторному суду адверсария; не подпись владельца, не совершённая заморозка и не done Н-100.

Эксперимент `python3 /tmp/dev-harness-verify/critic-033-v3/probe.py` завершился **rc 0**. Команды, исходные сводки, stdout/stderr, коды возврата и проверки границ сохранены в `/tmp/dev-harness-verify/critic-033-v3/results.json`.

Полный текст передан оркестратору для приземления в `verdicts/critic/contracts-033-v2.md` с author=critic. Основной репозиторий, worktree автора и его ветки не изменялись. Клон и материалы сохранены до приземления. Полный CI не запускался.
