FAIL

# Контракт 033 — критик critic, круг 2: scoped-проверка closures Б1/Б2

Предмет: дельта `41040cc→cdf13c68679dea5b668954d1e5d281b561a7f3e7`, контракт `contracts/033-union-semantika-zon-check-zones.md`; основания первого круга — вердикт `5a4f1cf:verdicts/critic/contracts-033-v1.md`. Судья: critic / Critic033K2.

Набор артефактов полон. Проверен закоммиченный предмет; предмет, rc-команды, исполнители и границы названы. Исследование проведено в одноразовом клоне `/tmp/dev-harness-verify/critic-033-k2/repo`, положительные и отрицательные симуляции — в отдельном Git-дереве `/tmp/dev-harness-verify/critic-033-k2/simulation`. Исходный worktree, ветки и main не изменялись. Полный текст вердикта передаётся оркестратору для приземления с author=critic.

Итог: **Б2 закрыт, советы по А-179 применены; Б1 закрыт не полностью — воспроизведены обе зелёные команды при фактически открытом Н-100.** Форма пост-фактум кодификации, СПАСЕНО-прецеденты, фактическое ядро А-179 и отказ от новых фикстур повторно не судились.

## Единственная блокирующая находка

БЛОКИРУЕТ contracts/033-union-semantika-zon-check-zones.md:86 — `Закрытие-Н-100-проверка` ищет четыре подстроки во всём заголовке, но не проверяет статусный токен. Между тем строки 98–102 прямо определяют закрытие как замену ПОСЛЕДНЕЙ бэктик-группы с ОТКРЫТО на названный закрывающий токен. Наличие закрывающего текста в другой части заголовка не выполняет этого условия.

ОБХОД: в отдельном Git-дереве лежит точный блоб контракта cdf13c6 и аннотированный тег `frozen/contracts/033/1`, несущий этот блоб. В исходный заголовок Н-100 ПЕРЕД последней бэктик-группой вставлено `план закрытия: `, затем полный точный закрывающий токен из строки 102 в собственной бэктик-группе и разделитель `; `. Исходная последняя группа `ОТКРЫТО — адрес: очередь автономности, предмет «union-семантика check_zones — фикс фильтра :335 к заявленному шапкой ОБЪЕДИНЕНИЮ + красная фикстура union-vs-per-contract; канал приземления — развилка владельца 2026-09-17 (owner-channel-фикс против двойного waiver мини-контракта)»` сохранена без изменений. Это состояние закоммичено в игрушечном дереве. Обе команды контракта возвращают **rc 0**, хотя Н-100 по объявленной грамматике остаётся **ОТКРЫТО**. Дополнительно существующий `check_nabludenia.sh` на этом дереве возвращает **rc 0**: контрпример не опирается на синтаксически недопустимую запись.

Это структурный обход, а не спор о смысле русского текста: уже существующий читатель в `scripts/check_nabludenia.sh:122–127` извлекает бэктик-группы и выбирает последнюю. Новая команда контракта эту границу не соблюдает. Следовательно, общий зелёный критерий всё ещё допускает невыполненную часть предмета — закрытие Н-100.

Для закрытия находки приёмка должна проверять именно статусную группу, определённую собственным условием done, и отвергать предъявленный вариант с закрывающей цитатой перед последним ОТКРЫТО. Новый механизм union или новая фикстура его поведения не требуются.

## Собственные прогоны

Обе команды извлечены непосредственно из закоммиченного блоба HEAD и исполнены без изменения их текста. Проба: `python3 /tmp/dev-harness-verify/critic-033-k2/probe.py` — rc 0; этот код означает, что все перечисленные ниже ожидаемые результаты, включая обход, воспроизведены.

| Состояние | Заморозка-033-проверка | Закрытие-Н-100-проверка |
|---|---:|---:|
| Неизменённый клон cdf13c6 | 128 | 1 |
| Изолированное дерево: аннотированный 033/1, равный блоб, точный токен в последней группе | 0 | 0 |
| Изолированное дерево: закрывающий токен процитирован перед последней группой ОТКРЫТО | 0 | 0 |

Дополнительные различающие контроли:

- Изменение файла контракта при сохранённом замороженном блобе → проверка заморозки **rc 1**.
- Замена аннотированного тега на lightweight с тем же именем → проверка заморозки **rc 1**.
- Удаление по очереди каждого якоря `ЗАКРЫТО (033:`, `cbbf6b1`, `frozen/contracts/026/4`, `frozen/contracts/027/3` из положительного заголовка → проверка закрытия каждый раз **rc 1**.
- Полный закрывающий токен только в теле записи, при исходном открытом заголовке → проверка закрытия **rc 1**.
- Существующий `bash scripts/check_nabludenia.sh` фактически выполнен с каталогом **`simulation`**, и на дереве обхода её результат — **rc 0**.

Форма проверки заморозки в строке 85 соответствует заявке: точная ссылка `refs/tags/frozen/contracts/033/1`, `objecttype=tag`, сравнение идентификатора блоба из тега с `git hash-object` файла. Претензия относится не к этой проверке, а к позиции статусного токена во второй команде.

Сырой структурированный протокол, извлечённые команды, точный заголовок обхода и его фактический статус сохранены в `/tmp/dev-harness-verify/critic-033-k2/results.json`.

## Б2: closure применён

В закоммиченном блобе контракта для каждого из маркеров `подмена`, `вход`, `ровно на`, `обманный`, `$1 ==`, `$3 ==`, `путь зоны 002`, `диапазона 001`, `наблюдаем`, `умирает именем` получено **0 вхождений**. Отдельный поиск по файлу клона также не нашёл совпадений.

В `contracts/033-union-semantika-zon-check-zones.md:53–56` остались инвариант, имя ловца и описание протокола предъявления. Прежняя конкретная пара «порча ↔ различающий вход» удалена; повтор из приёмки также удален. Основание Б2 первого круга устранено.

## А-179: оба совета применены

- `NABLIUDENIA_ARCHITECT.md:3971–3972`: cbbf6b1 атрибутирован только union-фильтру и двум фикстурам; СПАСЕНО 026/4+027/3 явно отнесено к последующим отдельным изменениям и заморозкам.
- `NABLIUDENIA_ARCHITECT.md:3977–3983`: установленное расхождение задания с деревом отделено от контрфактической гипотезы; прямо сказано, что причинная роль цены модели данными не доказана.

Новых блокирующих требований к А-179 нет. А-180 и иная неприменимая к closures проза не использовались для расширения предмета суда.

## Пять вопросов критика — только в пределах дельты

1. **Критерий слабее предмета?** Да: обе итоговые команды зелены при открытом Н-100; обход предъявлен выше.
2. **Каждое «готово» доказуемо названной командой?** Для существования аннотированной заморозки и равенства блоба — да. Для замены последнего статусного токена — нет: строка 86 проверяет другую, более слабую меру.
3. **Оставлено ли решение исполнителю?** Требуемая позиция статуса и закрывающий текст уже названы. Дефект не в недостающем решении, а в несоответствии команды этому решению; это та же находка, не отдельное основание отказа.
4. **Названы ли исполнители и границы?** Да. Дельта затрагивает только контракт и NABLIUDENIA_ARCHITECT.md; закрывающая запись отнесена к оркестратору. Нового дефекта границ в closures не обнаружено.
5. **Противоречие AGENTS.md?** Прежнее противоречие Н-39 устранено. Отказ настоящего круга основан на конкретном обходе Б1, а не на переоткрытии формы кодификации.

## Повтор основания и арбитраж

АРБИТР: роль `arbiter`; предмет — остаток Б1: достаточно ли совпадений во всём заголовке при явно объявленном статусе в последней бэктик-группе и воспроизведённых двух rc 0 при ОТКРЫТО. Маршрутизация арбитража запрошена у Main; автору переданы контрпример и запрос согласия/несогласия. На момент доклада ответ не получен. При согласии автора применяется исключение AGENTS.md для согласованного повторного исправления; при несогласии повтор Б1 требует арбитра, а не третьего обычного круга критика.

Полные CI-прогоны и регресс неизменённой реализации union в этом scoped-круге не повторялись. Заморозка редакции cdf13c6 не разрешается: требуется устранить только доказанный разрыв между статусным условием done и его rc-проверкой.
