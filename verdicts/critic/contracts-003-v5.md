FAIL

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:43-46,193-209 — при правке реестровой v5 из требования исчез предикат, связывающий запускаемый бинарь с `config/harness_pin.json`: после слов «Ответу можно верить только от бинаря» осталось только имя файла и неназванное «расхождение». Не определено, какие поля с чем сверяются, а среди команд приёмки нет `check:overlay`. Поэтому исполнитель вправе выбрать совпадение одной лишь версии и получить зелёную живую пробу от бинаря, чья контрольная сумма пину не соответствует. Это новая причина, не повтор спора «оракул не доказывает `read(skill://…)`».
ОБХОД: оставить честный `config/harness_pin.json` и все четыре дерева скилов, но первым в `PATH` поставить изменённый `omp`, который печатает запиненную версию при `--version`, имеет другую `sha256` и выдаёт для четырёх имён требуемые пары `tool_execution_start`/`tool_execution_end` с правильными `toolCallId`, `args.path`, строкой шапки и проектным `resolvedPath`. Реализовать `check_skills.sh --live` со сверкой только `.version` — это допустимое разрешение неназванного предиката — и не вызывать из него `check:overlay`. Тогда `check:skills-live` и все перечисленные команды приёмки возвращают требуемые коды, хотя коррелированные события созданы не запиненным бинарём и живое обнаружение доверенным omp не доказано.

Обязательный минимум `verdicts/arbitration/orakul-korreljacija.md:96-126` в части корреляции перенесён: критерий требует одну пару по `toolCallId`, равенство `args.path = skill://<имя>`, строку шапки в `result` того же вызова, нормализованный проектный `resolvedPath` и код 1 при отсутствии полей (`contracts/003-skills-metta-adaptacija.md:34-43,149-157,200-205`). Повторная находка по закрытой арбитражем причине не открывается.

Проверены пять обязательных вопросов на HEAD `08044da98ad9da0a6e54e0f1dc95ed12d2d4aa51`:

1. Названный выше обход делает команды зелёными без доверенного живого доказательства предмета.
2. Утверждение о запиненном бинаре не доказуемо перечисленными командами, пока соответствие пину не определено либо `check:overlay` не включён в обязательный прогон.
3. Выбор полей пина молча оставлен исполнителю; остальные решения обязательного минимума корреляции названы.
4. Исполнители названы, а требуемые пути входят в непересекающиеся `ЗОНА`-строки (`contracts/003-skills-metta-adaptacija.md:99-118`).
5. Других противоречий нормам `AGENTS.md` не найдено; эта же находка реализует запрещённое правило без механизма и имеет конкретный обход.

Советов нет.
