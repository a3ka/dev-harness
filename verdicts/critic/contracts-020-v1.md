FAIL

# Контракт 020 — критик, круг v1

Предмет: `contracts/020-shardirovanie-ci-antiplatsebo.md` на HEAD
`cc4e03ce3055dc059078d442c94aeebe25740d0c`; блоб
`5d7f09c40b6b763aba5199a90f3d824ce4d05d0c`, введён коммитом `6f2a3c2`.
Судимы предмет, инварианты, приёмка и раздача; решения Р1–Р4 не переоткрываются.

Набор комплектен: предмет закоммичен, приёмка и rc-команды названы, исполнители
implementer/architect названы, обе ЗОНА-строки стоят в первой колонке и читаются
грамматикой `lib_zones.sh`. Незакоммиченные пробы рассмотрены как предъявленные
эксперименты, не как новый текст контракта. Раскрытие А-155 принято: отказ
pre-commit до freeze не является находкой этого круга.

## Блокирующие находки

БЛОКИРУЕТ contracts/020-shardirovanie-ci-antiplatsebo.md:86 — обе зеркальные ветви полноты объявлены, но предъявленная приёмка их не различает.
ОБХОД: расширить существующий барьер только тремя проверками, которые предъявлены новыми красными пробами: на объявленной matrix проверять выпавшие ключи, дубли известных ключей и отсутствие `--scope`; при отсутствии matrix не проверять полноту, незнакомые ключи не проверять. Все 19 предъявленных фикстур проходят, `bash scripts/verify_ci_parity.sh` на исходной, вообще НЕ шардированной проводке возвращает 0. Предмет можно не внести в CI вовсе. На отдельном полном шардированном входе добавление `check_zzz` без каталога также даёт 0 — вторая объявленная зеркальная ветвь отсутствует.

Это не гипотеза о будущей реализации: такой неполный вариант исполнен в одноразовом
клоне. Он не подбирает имена `check_aaa/check_ccc`, а сравнивает множества и строит
диагноз из входных ключей. Контрольные эксперименты дали:

- исходные 15 + четыре предъявленные пробы: **19/19, rc=0**;
- исходный CI без matrix: **29 workflow-команд, 46 скриптов, 17 исключений,
  расхождений 0, rc=0**;
- полная игрушечная matrix: **rc=0**;
- та же matrix с мёртвым ключом: **rc=0**;
- те же каталоги фикстур, matrix удалена, возвращён обычный `--changed`-шаг:
  **rc=0**.

Критерий готовности должен предъявить отказ на этих двух состояниях, а не только
записать его в инварианте. Привязку дополнительных проб к коду делает architect
по Н-39; готовая реализация здесь не предписывается.

БЛОКИРУЕТ contracts/020-shardirovanie-ci-antiplatsebo.md:66 — чистый cutover не различается названной приёмкой на строках 111–130.
ОБХОД: добавить корректную matrix из пяти шардов, покрывающих все каталоги ровно один раз, с требуемыми concurrency, timeout 20, Node 26 и fetch-depth 0, но сохранить старый `npm run check:antiplacebo -- --changed ${{ github.event.before }}` в главной джобе. Неполный барьер выше и все 19 проб зелёные; команда паритета возвращает 0. Сумма самих шардов полна, но запуск вне matrix остаётся, то есть прямо запрещённое предметом состояние принимается.

Этот контрпример также исполнен на проводке настоящего клона, не только на
игрушечном YAML: пять шардов, 38 ключей, веса 71/71/71/71/70 после посадки проб;
старый шаг оставлен. Дополнительный запуск использует существующий пункт приёмки,
поэтому правило 6 само по себе его не запрещает. Нужен rc-критерий исключительности
шардного запуска; проба замены `--scope` внутри matrix проверяет другой переход и
сохранённого внешнего шага не видит.

## Советы, не дополнительные блокеры

СОВЕТ contracts/020-shardirovanie-ci-antiplatsebo.md:58 — отличить грамматику боевой проводки от грамматики подставного дерева. Здесь N=5 и checkout с fetch-depth 0; общий `_shard_tree.sh` предъявляет два шарда и checkout без fetch-depth. Уточнение избавит исполнителя от решения, обязан ли общий барьер отвергать положительный контроль. Р1 не переоткрывается: в боевом CI остаются пять шардов.

СОВЕТ contracts/020-shardirovanie-ci-antiplatsebo.md:64 — для параметров Р1–Р4 и порядка первого коммита полезно назвать отдельную rc-проверку проводки/истории. Предъявленные пробы проверяют полноту и паритет, а не наличие concurrency, timeout, N=5 или расположение двух scope-шагов. На игрушечной форме удаление concurrency и timeout оставалось зелёным. Это не предложение переносить всю проверку истории в `verify_ci_parity.sh`.

СОВЕТ contracts/020-shardirovanie-ci-antiplatsebo.md:53 — пересборка референса действительно нужна на прилёте, как уже разрешено текстом: в судимом HEAD 38 каталогов и 350 case-файлов, с четырьмя предъявленными пробами — 354. Числа 36/343 и веса 69/69/69/68/68 относятся к прежнему замеру, не к текущему составу. Само наличие явно датированного референса не блокирует.

## Обязательные пять вопросов

1. **Критерий слабее предмета?** Да: два исполненных обхода выше. Первый допускает
   отсутствие самого шардинга, второй — незавершённый cutover даже при полной matrix.
2. **Каждое «готово» доказуемо названной командой?** Нет для зеркальных ветвей и
   исключительности запуска. Команды присутствуют, но их предъявленные проверки
   принимают запрещённые состояния; это не требование к автору добавить больше прозы.
3. **Решение оставлено исполнителю?** Р1–Р4 в тексте соответствуют данному слову
   владельца. Неясность общей/боевой грамматики отмечена советом; выбор размещения
   проверок остаётся за автором, Н-39 не подменяется привязкой стаба в прозе.
4. **Названы исполнители и границы?** Да. Изменения CI, паритета и его фикстур
   покрыты implementer; контракт, фикстуры и наблюдения — architect. Раннер/CLI,
   package.json, исключения, роли и генератор не меняются по предмету, выходов из
   зон для заявленной работы не требуется. Общая зона фикстур требует обычной
   координации, а не расширения списка путей.
5. **Противоречие AGENTS.md?** Отдельного блокера по норме не предъявляю. Фактический
   дефицит кодовой приёмки назван точными обходами, а не общей ссылкой на правило.
   Правило 6 сохранено статичным именем команды; scoped Н-48 и отсутствие правок
   раннера соблюдены в конструкции. Н-43/44/45/63/87/92 прочитаны; CI-прогоны и
   детектор утечек на живом дереве не запускались.

## Исполнение и граница доказательства

Клон создан критиком командой `git clone /home/aka/Documents/dev-harness
/tmp/dev-harness-verify/critic-020-v1/repo`. Прочитаны контракт целиком, актуальный
workflow, шапка паритета, обход барьеров/фикстур и scoped-фильтр раннера, пять
предъявленных файлов проб и `_fake_root.sh`.

До изменения предмета: `bash scripts/verify_antiplacebo.sh --scope verify_ci_parity`
дал **rc=1, 19 фикстур, 16 предъявлений красного, ровно три отказа**:
`case_shard_scope_sloman`, `case_shard_vypavshaja`, `case_shard_zadublirovannaja`
не предъявили красного; все старые пробы и проба правила 6 прошли. Из кода проб
видно, что меняются именно keys/run после положительного контроля. Дубль сохраняет
полноту объединения; выпадение сохраняет каталог; шаблон имени действительно
попадает в существующий отказ правила 6. Эти четыре пробы не фиктивны — они просто
не закрывают весь заявленный критерий.

После экспериментального неполного расширения барьера: тот же scoped-прогон
**rc=0, 19/19**. После добавления полной matrix с сохранённым прежним шагом:
паритет **rc=0**, scoped-прогон **rc=0, 19/19**. Это проверка различающей силы
приёмки, НЕ суд реализации и НЕ утверждение о зелёном полном CI. Неполный барьер
и изменённый workflow существуют только в выбрасываемом клоне и не приземляются.
Скрипты воспроизведения и журналы контрольных входов оставлены вне клона в
`/tmp/dev-harness-verify/critic-020-v1/`; существенные команды, состояния и результаты
зафиксированы здесь, чтобы вердикт не зависел от сохранности временного каталога.

Круг первый; основания для арбитража повтора нет. Заморозка по этому тексту
приёмки запрещена до устранения двух названных обходов.
