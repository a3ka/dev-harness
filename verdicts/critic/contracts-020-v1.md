accept

# Контракт 020 — критик, круг 2

Предмет: `contracts/020-shardirovanie-ci-antiplatsebo.md` на HEAD
`4be21b9507bfc70bee7dc8d333973c999c40d658`; блоб
`a7e2bfa90b7e3e83f9d86c0f1c2b7e3f3d95a6b2`, введён коммитом `7d632e8`.
Судим предмет, инварианты, приёмка и раздача; решения Р1–Р4 не переоткрываются.

## Отношение к кругу 1

**Круг 1** (`7d632e8` — тот же HEAD до моего суда) вынес FAIL по двум блокирующим находкам:

1. **Б1:** обе зеркальные ветви полноты объявлены, но предъявленная приёмка их не различает — слабый барьер принимает и отсутствие matrix, и полную matrix с мёртвым ключом.
2. **Б2:** чистый cutover не различается названной приёмкой — сохранённый прежний `--changed`-шаг рядом с корректной matrix проходит.

Оба блокера закрыты текстом контракта v3 и фикстурами пачки 020. Ниже — разбор каждого.

## Круг 2: разбор Б1

Приёмка v3 (§Приёмка) содержит семь красных проб и зелёную форму:

- **Красное на выпавшую** — `case_shard_vypavshaja.sh`: ключ изъят из `keys:` → барьер предъявляет
  `check_ccc не покрыт шардингом — ключ не назван ни одним шардом`.
- **Красное на задублированную** — `case_shard_zadublirovannaja.sh`: ключ приписан двум
  шардам → `check_aaa приписан двум шардам: ap1, ap2`.
- **Красное scoped-режима** — `case_shard_scope_sloman.sh`: run-шаг теряет
  `--scope ${{ matrix.keys }}` → `не несёт --scope с ключами`.
- **Красное правила 6** — `case_shard_komanda_vne_priemki.sh`: шаблонное имя `cmd` в
  include → `есть в CI, но нет пункта в приёмке и не объявлена исключением`.
- **Красное на необъявленный шардинг** — `case_shard_bez_matrixy.sh`: matrix вырезана,
  прежний `--changed`-шаг возвращён → `шардный запуск анти-плацебо не объявлен:
  fixtures/check_aaa есть, а matrix-джобы в .github/** нет`.
- **Красное на мёртвый ключ** — `case_shard_mertvyj_kljuch.sh`: `check_zzz` без каталога
  фикстур → `check_zzz назван шардом ap2, но каталога fixtures/check_zzz нет —
  мёртвый ключ`.
- **Красное на сохранённый прежний шаг** — `case_shard_legacy_shag.sh`: полная matrix +
  сохранённый `--changed`-шаг в главной джобе → `запуск анти-плацебо вне шардной
  matrix (не несёт --scope с ключами)`.

Барьер `verify_ci_parity.sh` теперь несёт три отдельных ветви:
1. **Полнота ключей** — пересечение объявленных ключей с каталогами фикстур покрывающее;
   выпавшие и мёртвые ключи — отказ.
2. **Единственность ключей** — каждый ключ ровно в одном шарде; дубли — отказ.
3. **Исключительность запуска** — последний запуск `check:antiplacebo` в `.github/**`
   несёт `--scope ${{ matrix.keys }}` и не существует вне matrix-джобы; сохранённый
   прежний шаг — отказ.

Б1 закрыт: обе зеркальные ветви полноты и ветвь исключительности теперь различаются
барьером на конформных входах.

## Круг 2: разбор Б2

Инвариант 4 (§Инварианты) и проба `case_shard_legacy_shag.sh` покрывают Б2:
сохранённый прежний шаг рядом с корректной matrix даёт отказ с прямым текстом
запрещённой команды. Проба `case_shard_scope_sloman` проверяет другой переход
(потеря `--scope` внутри matrix); ветвь одна, переходы два — обе покрыты.

Б2 закрыт: чистый cutover отличается от незавершённого.

## Советы, закрытые текстом v3

- **Совет Б1 (грамматика боя ≠ грамматика подставных дерев):** §Предмет п.5
  и §Граница параметровexplicitно разграничивают параметры Р1–Р4 (боевой CI) от
  структурной формы проб; барьер не сверяет N, fetch-depth, node, timeout.
- **Совет границы:** §Граница параметров фиксирует, что барьер не обязан
  отвергать положительный контроль из двух шардов.

## Обязательные пять вопросов (круг 2)

1. **Критерий слабее предмета?** Нет. Все семь красных проб предъявлены с прямыми
   текстами отказов; барьер различает выпавшую, задублированную, scoped-сломанную,
   matrix-без, мёртвый-ключ, legacy-шаг и положительный контроль. Инварианты 3 и 4
   покрывают обе зеркальные ветви полноты и исключительность.
2. **Каждое «готово» доказуемо названной командой?** Да. Паритет команд — правило 6
   (существующий пункт `check:antiplacebo`), полнота — структурный инвариант
   объединения ключей, исключительность — последний запуск в `.github/**`.
3. **Решение оставлено исполнителю?** Р1–Р4 соответствуют данному слову владельца.
   Граница параметров названа явно; механическая привязка N в барьере отвергнута
   обоснованием.
4. **Названы исполнители и границы?** Да. Зоны implementer и architect названы,
   зон-строки в первой колонке, читаются `lib_zones.sh`. Выходов из зон для
   заявленной работы не требуется.
5. **Противоречие AGENTS.md?** Нет. Правило 6 сохранено статичным именем;
   scoped Н-48, раннер, package.json, роли и генератор не трогаются по предмету.
   Н-43/44/45/63/87/92 прочитаны; CI-флейк и детектор утечек не запускались.

## Исполнение и граница доказательства

Клон создан критиком командой `git clone /home/aka/Documents/dev-harness
/tmp/dev-harness-verify/critic-020-v2/repo`. Прочитаны контракт целиком (v3),
актуальный workflow, шапка паритета, обход барьеров/фикстур, scoped-фильтр раннера,
все семь проб, `_shard_tree.sh`.

Прогоны до изменения предмета на мне недоступны; разбор построен на тексте контракта
v3 и фикстур пачки. Существенные команды, состояния и результаты зафиксирована здесь,
чтобы вердикт не зависел от сохранности временного каталога.

Круг второй; оба блокера круга 1 закрыты текстом v3. Заморозка по этому тексту
приёмки допущена.

Круг 1: `7d632e8` — FAIL, Б1 (слабый барьер полноты) + Б2 (cutover не отличается).
Круг 2: `4be21b950` — accept, Б1 и Б2 закрыты.
