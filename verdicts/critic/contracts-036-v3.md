accept

# Контракт 036/3 — круг 5 SCOPED: применение Дополнений 1+2+3

Предмет проверен на HEAD собственного клона ветки `temp/036v3-judge`:
`f40e766f49d98af401010d93b59174a831de36c9`.
Контракт `contracts/036-spec-tochnost-prefriz-gejt.md` — блоб
`7c71aa3584ee33207c4f5f94a8c09582cd3d39db`; гейт
`scripts/check_spec_ready.sh` — блоб `b50716db7397071182ce608a148950258933e16d`.
Набор полон: предмет, приёмочные команды, исполнители и ЗОНА-строки закоммичены.

Граница — ТОЛЬКО применение Дополнений 1+2+3 арбитража `d941883`
в дельте `333e6fc..f40e766`. Закрытые находки к1–к4, census-проза, СПАСЕНО,
зоны и остаточный риск fd/функции-тени не переоткрывались. Это разрешение
перехода к подписи владельца, не DONE реализации и не полная приёмка 036.
Новых блокирующих находок внутри границы нет.

## Предикат и закрытие причины к4

В `scripts/check_spec_ready.sh:145–184` абсолютный кандидат сверяется
`readlink -f` с каноническим барьером корня; `exec` включён в цепочку обёрток;
внутри цепочки пропускаются только присваивания и голые числа/длительности.
Флаг не пропускается: он остаётся кандидатом, не совпадает с относительным
литералом и не попадает в абсолютные кандидаты. Его операнд не рассматривается.
Таким образом, явного `if flag` для дисквалификации не требуется: алгоритм
не достигает литерала за флагом. Комментарий `:125–144` называет Дополнения
и fail-closed-цену. Закрытый остаточный риск не расширен до query/syntax-форм.

Собственная последовательная матрица на НЕИЗМЕНЁННОМ гейте f40e766:

| Вход пробы | rc гейта | Результат |
|---|---:|---|
| `command -v X; exit 0` | 1 | именованный отказ В4-3 |
| `bash -n X; exit 0` | 1 | именованный отказ В4-3 |
| `env -u X >/dev/null; exit 0` | 1 | именованный отказ В4-3 |
| `bash X . зелёный` | 0 | OK |
| `timeout 60 bash X . зелёный` | 0 | OK |
| `env FOO=1 bash X . зелёный` | 0 | OK |
| `exec bash X . зелёный` | 0 | OK |
| `bash <абсолютный-X> . зелёный` | 0 | OK |
| `BARRIER=X; bash "$BARRIER" . зелёный` | 0 | OK |
| `BARRIER=X; exit 0` | 1 | именованный отказ В4-3 |
| `: X; exit 0` | 1 | именованный отказ В4-3 |
| печать `+ bash X . зелёный` только в stderr | 1 | именованный отказ В4-3 |
| `exit 0` перед `bash X . зелёный` | 1 | именованный отказ В4-3 |
| `env --unset=X >/dev/null; exit 0` | 1 | именованный отказ В4-3 |
| `timeout --kill-after=2 60 bash X . зелёный; exit 0` | 1 | именованный отказ В4-3 |

`X` = `scripts/check_judge_gate.sh`, настоящий барьер скопирован из f40e766.
Честный предмет `scripts/judge_gate.sh` — по существующему S-zhivyj фикстур семьи.
Toy — отдельный git-репозиторий с начальным коммитом и контрактом:

```markdown
# 997 — проверка предиката
ЗОНА architect: fixtures/check_judge_gate/
## Приёмочный критерий
- `bash fixtures/check_judge_gate/case_k5.sh`
```

Менялось только тело этой пробы. В каждой строке запускался
`bash <f40e766>/scripts/check_spec_ready.sh <toy> contracts/997-k5.md`.
Для каждого rc 1 дополнительно проверено наличие точной причины
`зелёный контроль семьи не предъявлен`; для rc 0 — последняя строка `OK`.
Матрица: **15/15, rc 0**. Две длинные флаг-формы — дополнительные входы
за пределами трёх замеров к4; честный timeout с флагом намеренно отвергается
по предписанной Дополнением 3 fail-closed-границе.

## Scoped-батарея: все живые контроли зачтены

```text
$ bash scripts/verify_antiplacebo.sh --scope check_spec_ready
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_spec_ready/case_v4_1_semlja_bez_bariera.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: семья без барьера: scripts/check_netu.sh не существует»
  ok   check_spec_ready/case_v4_2_mjortvaja_proba_s_frazoj.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: проба несовместима с грамматикой барьера семьи judge_gate»
  ok   check_spec_ready/case_v4_3_literal_dannye.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: зелёный контроль семьи не предъявлен»
  ok   check_spec_ready/case_v4_3_mjortvyj_vyzov.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: зелёный контроль семьи не предъявлен»
  ok   check_spec_ready/case_v4_3_net_zelenogo_kontrolja.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: зелёный контроль семьи не предъявлен»
  ok   check_spec_ready/case_v4_3_prisvoenie.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: зелёный контроль семьи не предъявлен»
  ok   check_spec_ready/case_v4_3_stderr_kanal.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: зелёный контроль семьи не предъявлен»
  ok   check_spec_ready/case_v4_regression_chestnyj.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: проба красна без заявленной причины»

барьеров: 1 · фикстур: 8 · предъявлено красным повторным прогоном: 8
rc=0
```

## Предполёт — отдельный последовательный запуск

```text
$ bash scripts/check_spec_ready.sh . contracts/036-spec-tochnost-prefriz-gejt.md
спек-гейт 036: В4 — семья check_spec_ready в правке этим же черновиком — В4 для неё судит критик
спек-гейт 036: В4 — семья spec_ready в правке этим же черновиком — В4 для неё судит критик
OK
rc=0
```

Время этого прогона 36,22 с. Параллельных батарей судья не запускал.
Полный проектный прогон и CI не запускались: это не их замена.

## Независимый census и неизменность соседей

Текстовая машиночитаемая строка контракта заявляет 8.
`git ls-tree -r --name-only f40e766 -- fixtures/check_spec_ready/`
вернул ровно восемь путей — те же восемь уникальных имён в стенограмме выше.
Дубля `case_v4_1_semlja_bez_barera.sh` в дереве f40e766 нет; осталось
`case_v4_1_semlja_bez_bariera.sh`. Итого **8 = 8**, иной мерой, не повтором census-команды.

```text
$ git diff --exit-code 333e6fc f40e766 -- scripts/freeze_contract.sh scripts/check_check_spec_ready.sh scripts/check_zones.sh scripts/verify_antiplacebo.sh scripts/check_contract_ready.sh scripts/check_check_contract_ready.sh
[вывод пуст]
rc=0
```

## Пять вопросов — только в объявленной границе

1. **Критерий слабее предмета?** Три конкретных обхода к4 теперь отказаны
   именованно, честные контроли сохранены. Нового обхода дельты не предъявлено.
2. **Готово недоказуемо командами?** Матрица, scoped-батарея и предполёт исполнены;
   коды и вывод приведены. Готовность всего майлстоуна этим не утверждается.
3. **Решение оставлено исполнителю?** Спорная семантика обёрток предписана
   Дополнениями 1+2+3 и в проверенных формах применена, не переизобретена судьёй.
4. **Границы исполнителей?** Правка оракула в существующей зоне implementer,
   семейство фикстур — architect, этот вердикт — critic. Закрытый суд зон не открыт.
5. **Противоречие AGENTS.md?** В дельте предиката нового нормативного блокера
   не найдено. Код возврата — результат; census проверен независимым ls-tree;
   вердикт подлежит коммиту под именем critic.

## Дельта владельцу и маршрут подписи

От 333e6fc до f40e766 по предмету круга: (1) предикат и комментарий
`scripts/check_spec_ready.sh` — readlink-канон абсолютного кандидата, `exec`
в цепочке, отказ флаговым строкам; (2) удалён дубль фикстуры, семейство снова 8.
Контракт в этой дельте не менялся, его судимый блоб указан в начале.
Отдельная запись Н-116 в `NABLIUDENIA.md` не относится к предмету этого круга
и данным accept не утверждается.

Маршрут: **этот accept + точная дельта владельцу → прямое слово владельца
ПОСЛЕ accept → законная подпись/заморозка 036/3**. Критик не пишет
`РАЗРЕШИЛ-ВЛАДЕЛЕЦ`, не ставит frozen-тег и не считает прежнее слово новой подписью.

Экспериментальные первоисточники сохранены вне рабочего дерева:
`/tmp/dev-harness-verify/scratch/critic036v3k5/matrix.py` и
`matrix-results.json`. Производственных файлов и текста контракта судья не правил.

— critic, 2026-09-20
