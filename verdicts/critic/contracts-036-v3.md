FAIL

# Контракт 036/3 — круг 3: литерал в трассе не равен вызову

Предмет: `contracts/036-spec-tochnost-prefriz-gejt.md`, блоб `6fe98203d47aaa6de9bd2552b2ccc50ff9486794`, на HEAD собственного клона `ef5979aa48871155cb0b29ab5c35eddea2ce2fc2` ветки `temp/036v3-judge`. Дополнение предмета — фикс В4-3 и пятая фикстура `case_v4_3_mjortvyj_vyzov.sh`, блоб `3ea69214825749f2e4736156c7e8211e2f77a226`. Судится различающая сила приёмки В4, не DONE всей реализации.

Набор полон: предмет, приёмочные команды, исполнители и зоны закоммичены. Точное воспроизведение К2 закрыто, но класс обхода остался. Прямое слово владельца и заморозка этим вердиктом не заменяются.

## Блокирующая находка В4-3-ТРАССА-ДАННЫХ

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:312 — г7, даже усиленный новой дифференциальной фикстурой, не различает исполненный вызов барьера и исполненное присваивание его пути. Обещание предмета `:66–69` — минимум один живой вызов; литерал в stderr/xtrace этого не доказывает. Демаркация `:354–358` допускает приведённую пробу: файл исполним, содержит `$BARRIER` и литерал, семья существует и не заявлена к правке.
ОБХОД: оставить барьер и пять фикстур из ef5979a без изменений. В конформном toy-контракте объявить пробу `fixtures/check_judge_gate/case_control.sh` с присваиванием `BARRIER=scripts/check_judge_gate.sh`, затем `exit 0`, затем недостижимым `bash "$BARRIER" . зелёный`. Гейт возвращает 0/OK, хотя вызовов барьера ноль. В отдельной копии ef5979a выполнена только CI-проводка г8 в объявленной зоне implementer: добавлен ключ check_spec_ready и npm-шаг check:spec-ready. Новая приёмка г7 = rc 0, 5/5; г8 = rc 0, расхождений 0; тот же обман по-прежнему даёт rc 0. Никакой правки гейта, фикстур или текста контракта для этого не потребовалось.

Это повтор причины К2 fe5e4f73 — наличие имени принято за состоявшийся вызов; изменился носитель имени, а не свойство, которое отличает действие от имитации. Находка не требует проверять все case или закрывать иной предмет. Критерий должен различать хотя бы один настоящий вызов и ноль вызовов. Способ реализации судья не назначает.

АРБИТР: Arbiter036N39 (роль arbiter). Предмет: достаточность поиска литерала в stderr/xtrace для обещания ≥1 живого вызова; различающий критерий должен закрывать класс, а не только `exit 0` перед литералом. Созван критиком напрямую через hub; созыв принят, Main уведомлён о необходимости формального задания. Автор конструкции — сторона, не судья. Новый автоматический круг критика вместо арбитража не предлагается.

## Минимальное воспроизведение

Toy-корень — git-репозиторий с начальным коммитом. `scripts/check_judge_gate.sh` скопирован побайтово из ef5979a; он не находится в ЗОНА-путях toy-контракта. Для положительных контролей `scripts/judge_gate.sh` взят из честного S-zhivyj новой фикстуры.

`contracts/998-control.md`:

```markdown
# Проба достижимости

## Приёмочный критерий
- `bash fixtures/check_judge_gate/case_control.sh`

## Зоны
ЗОНА architect: fixtures/check_judge_gate/case_future.sh fixtures/check_judge_gate/case_control.sh
```

Проба:

```bash
#!/usr/bin/env bash
BARRIER=scripts/check_judge_gate.sh
exit 0
bash "$BARRIER" . зелёный
```

Собственный прогон неизменённого гейта ef5979a:

```text
$ bash <ef5979a>/scripts/check_spec_ready.sh <toy> contracts/998-control.md
OK
rc=0
$ env SHELLOPTS=xtrace bash fixtures/check_judge_gate/case_control.sh
+ BARRIER=scripts/check_judge_gate.sh
+ exit 0
rc=0
```

Независимая минимальная форма без переменной и без мёртвой строки:

```bash
#!/usr/bin/env bash
: scripts/check_judge_gate.sh
exit 0
```

```text
gate: OK; rc=0
trace:
+ : scripts/check_judge_gate.sh
+ exit 0
```

Обе пробы ничего не подделывают в файле трассы и не мутируют дерево: bash честно трассирует данные, а гейт принимает их за вызов. Третья проверенная форма — `printf '%s\n' scripts/check_judge_gate.sh >&2; exit 0` — также даёт gate rc 0 без вызова. Это один класс, не три независимых блокера.

## Запрошенная матрица

Каждая строка — собственный прогон одной и той же toy-приёмки; барьер и его предмет неизменны. Трасса получена отдельно через `env SHELLOPTS=xtrace`.

| Проба | rc гейта | Литерал пути в трассе | Результат |
|---|---:|---|---|
| Живой `bash scripts/check_judge_gate.sh . зелёный` | 0 | да | честный контроль сохранён |
| К2: `exit 0` перед буквальным вызовом | 1 | нет | старый экземпляр обхода закрыт |
| Вызов внутри `if false; then …; fi` | 1 | нет | отказ по В4-3 |
| Вызов только в `#`-комментарии, затем `exit 0` | 1 | нет | отказ по В4-3 |
| `out=$(bash scripts/check_judge_gate.sh . зелёный)` | 0 | да | вызов внутри подстановки виден |
| `BARRIER=scripts/check_judge_gate.sh; bash "$BARRIER" . зелёный` | 0 | да | живой переменный вызов проходит |
| `PATH="$PWD/scripts:$PATH" bash check_judge_gate.sh . зелёный` | 0 | да | живой PATH-вызов проходит |
| Присваивание BARRIER, затем exit, затем мёртвый вызов | 0 | да | ОБХОД: вызовов нет |
| `: scripts/check_judge_gate.sh; exit 0` | 0 | да | ОБХОД: только данные |
| Печать пути в stderr, затем exit | 0 | да | ОБХОД: только данные |

У PATH-вызова внешняя команда содержит лишь `check_judge_gate.sh`; полный литерал появляется во внутренней трассе `dirname …/scripts/check_judge_gate.sh`. Этот эксперимент доказывает конкретный путь вызова bash через PATH, не поддержку любого интерпретатора или всякой формы разрешения имени.

Все три ожидаемых отказа несут прежнюю именованную причину `спек-гейт 036: зелёный контроль семьи не предъявлен`.

## Дифференциал новой фикстуры

Фикстура 3ea69214 запущена напрямую с `REPO=<клон ef5979a>`, `BARRIER=<клон>/scripts/check_spec_ready.sh`, отдельным `WORK`:

```text
OK
свидетельство: ДО фикса мёртвый вызов проходит — rc 0, «OK»
красное предъявление: мёртвый вызов отказан именованным
калибровка: старый класс красен и ДО фикса — стаб различает три входа
красное предъявление: старый класс отказан именованным
rc=0
```

Исполнен S-mjortvyj: исторический субъект fe5e4f73 → 0, ef5979a → 1. S-bez-vyzova → 1 на обоих. Живой контроль ef5979a → 0. Дифференциал правдив, но защищает один экземпляр класса и пропускает присваивание перед выходом.

## Scoped-батарея и честный контракт

Изолированный прогон ef5979a:

```text
$ bash scripts/verify_antiplacebo.sh --scope check_spec_ready
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_spec_ready/case_v4_1_semlja_bez_bariera.sh: зелёный контроль есть, повторный прогон красный кодом 1
  ok   check_spec_ready/case_v4_2_mjortvaja_proba_s_frazoj.sh: зелёный контроль есть, повторный прогон красный кодом 1
  ok   check_spec_ready/case_v4_3_mjortvyj_vyzov.sh: зелёный контроль есть, повторный прогон красный кодом 1
  ok   check_spec_ready/case_v4_3_net_zelenogo_kontrolja.sh: зелёный контроль есть, повторный прогон красный кодом 1
  ok   check_spec_ready/case_v4_regression_chestnyj.sh: зелёный контроль есть, повторный прогон красный кодом 1
барьеров: 1 · фикстур: 5 · предъявлено красным повторным прогоном: 5
rc=0
```

Строки выше сокращены только удалением хвостов с причинами: причины соответственно «семья без барьера», «проба несовместима с грамматикой барьера семьи judge_gate», дважды «зелёный контроль семьи не предъявлен», «проба красна без заявленной причины».

Первый параллельный прогон батареи с предполётом признан недостоверным: снимок дерева заметил временные файлы spec_ready_trace/zones второго процесса. Он не используется как доказательство дефекта. Батарея и предполёт повторены раздельно; результаты здесь — изолированные.

Предполёт самого закоммиченного v3 НЕ зелёный:

```text
$ bash scripts/check_spec_ready.sh . contracts/036-spec-tochnost-prefriz-gejt.md
спек-гейт 036: В4 — семья check_spec_ready в правке этим же черновиком — В4 для неё судит критик
спек-гейт 036: В4 — семья spec_ready в правке этим же черновиком — В4 для неё судит критик
спек-гейт 036: замер расходится: заявлено 4, команда даёт 5
rc=1
```

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:245 — машинный census остался 4 после добавления пятой фикстуры; строки `:300` и `:334` также остались на четырёх. Это отдельная фактическая причина красного предполёта предъявленного v3, не новый обход и не самостоятельная блокирующая находка по порогу роли. Заявление «честный контракт 036 → rc 0» для ef5979a собственным прогоном не подтверждено. Критик не правил контракт, чтобы получить желаемый результат.

На отдельной экспериментальной копии с одной CI-правкой, описанной в ОБХОДЕ:

```text
$ bash scripts/verify_ci_parity.sh
workflow-команд: 37 · скриптов в приёмке: 47 · объявленных исключений: 24 · matrix-ключей: 39 · анти-плацебо-запусков: 2 · расхождений: 0
rc=0
$ bash scripts/verify_antiplacebo.sh --scope check_spec_ready
барьеров: 1 · фикстур: 5 · предъявлено красным повторным прогоном: 5
rc=0
$ bash scripts/check_spec_ready.sh <toy с печатью литерала> contracts/998-control.md
OK
rc=0
```

Это зелёность новых поведенческих ворот г7/г8 при живом обходе, а НЕ заявление о зелёности всей приёмки или исправленном census. Полный CI, полный анти-плацебо и заморозка 037+ не запускались.

## Исполнители, зоны, неизменность соседних барьеров

Зона-дельта v3 `:208` — каталог `fixtures/check_spec_ready/` целиком у architect; он покрывает все пять наличных case-файлов, дополнительная поимённая зона для пятого не нужна. `scripts/check_spec_ready.sh` остаётся в зоне implementer `:209`, существующей с v1; производственный фикс 41f76716006e37f0ad5c9f3ecde444e7b79d3e10 имеет автора implementer и меняет только этот файл. CI-проводка также в зоне implementer. Предъявленный temp-коммит ef5979a от architect переносит код для суда: это не свидетельство законности приземления этого транспортного коммита целиком как architect в main.

Новых выпадений путей относительно v2 нет; прежнее СПАСЕНО `:210` сохранено. Механизм границ назван — check_zones. Полная зелёность check_zones после будущей заморозки этим судом не объявляется.

```text
$ git diff --exit-code frozen/contracts/036/2 HEAD -- scripts/freeze_contract.sh scripts/check_check_spec_ready.sh scripts/check_zones.sh scripts/verify_antiplacebo.sh scripts/check_contract_ready.sh scripts/check_check_contract_ready.sh
[вывод пуст]
rc=0
```

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:198 — закоммиченный текст всё ещё описывает grep по коду пробы, тогда как предъявленный фикс добавляет второй запуск под xtrace. Контракт и реализацию следует согласовать по решению арбитра; это не отдельный блокер сверх предъявленного обхода.

## Пять вопросов гейта

1. Критерий слабее предмета: да, ноль вызовов проходит при г7 = 5/5 и г8 = 0; конкретный обход приведён.
2. Команды с кодами возврата названы. Различающая сила г7 недостаточна; собственный предполёт v3 дополнительно красен на census.
3. Свидетельство именно вызова, а не наличия пути среди исполненных данных, оставлено недоопределённым. Это та же блокирующая причина, не новая.
4. Исполнители и границы названы; каталог покрывает пять фикстур, код и CI — implementer, судейский артефакт — critic. Необъявленных необходимых путей в этой дельте не найдено.
5. AGENTS.md требует отличать проверку от имитации и после третьего круга перейти к арбитру. Обход предъявлен, арбитр созван; отдельный нормативный FAIL без контрпримера не используется.

Вердикт запрещает заморозку v3 в предъявленном состоянии. Текст контракта, реализация и фикстуры основного репозитория судьёй не правились. В основной репозиторий вносится только этот вердикт под именем critic.

— critic, 2026-09-20
