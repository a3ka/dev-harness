FAIL

# Контракт 036/3 — круг 2, содержательный

Предмет: `contracts/036-spec-tochnost-prefriz-gejt.md` на HEAD собственного клона `014149890fcdfe97c39b2bcf909aedafa4540078`, ветка `temp/036v3-judge`, блоб `6fe98203d47aaa6de9bd2552b2ccc50ff9486794`. Судилась дельта к `frozen/contracts/036/2`: В4, зоны и приёмка г7/г8. Это не суд DONE и не повторный аудит всей реализации 036.

Набор полон: текст и четыре фикстуры закоммичены; предмет, команды готовности, исполнители и ЗОНА-строки названы. Процессная причина К1 устранена. Подпись владельца для суда черновика не подменяется этим вердиктом; заморозка требует отдельного законного действия владельца.

## Блокирующая находка

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:312 — г7-3 различает отсутствие файла живого контроля и наличие честного контроля, но не отличает исполненную пробу от исполненного вызова барьера. Обещание предмета `:66–69` — минимум один ЖИВОЙ вызов барьера — сильнее критерия. Условие `:197–200` и демаркация `:354–358` требуют лишь наличия литерала в коде пробы; достижимость этого вызова приёмкой не защищена.
ОБХОД: оставить гейт и все четыре фикстуры ровно из `0141498`, закончить CI-проводку г7/г8 в объявленной зоне implementer; г7 возвращает 0 и 4/4, г8 возвращает 0. При этом конформная проба семьи `fixtures/check_judge_gate/case_control.sh` из трёх строк `#!/usr/bin/env bash`, `exit 0`, `bash scripts/check_judge_gate.sh . --retake` засчитывается как живой контроль семьи. Барьер существует, его путь НЕ объявлен к правке, будущий case объявлен в зоне; ни одного вызова барьера нет, трасса пробы — только `+ exit 0`. Гейт принимает такой черновик кодом 0 с `OK`. Следовательно, объявленная новая приёмка зелена на реализации, не обеспечивающей предмет В4-3.

Это дефект различающей силы КРИТЕРИЯ, а не требование расширить полноту до всех case: обход не даёт даже одного вызова, прямо обещанного предметом. Он не использует иную фразу диспетчера, правку барьера самим черновиком, мутацию дерева пробой или отсутствующую историю. Требуется различающий критерий для живого вызова либо явное изменение обещаемого предмета автором; способ реализации судья не назначает.

### Воспроизведение обхода

В отдельном toy-корне создан существующий `scripts/check_judge_gate.sh` — побайтовая копия из судимого HEAD. Файл пробы:

```bash
#!/usr/bin/env bash
exit 0
bash scripts/check_judge_gate.sh . --retake
```

Черновик `contracts/998-control.md`:

```markdown
# Проба достижимости

## Приёмочный критерий
- `bash fixtures/check_judge_gate/case_control.sh`

## Зоны
ЗОНА architect: fixtures/check_judge_gate/case_future.sh fixtures/check_judge_gate/case_control.sh
```

Корень — git-репозиторий с начальным коммитом. Собственный прогон неизменённого субъекта из `0141498`:

```text
$ bash scripts/check_spec_ready.sh <toy> contracts/998-control.md
OK
rc=0
$ bash -x fixtures/check_judge_gate/case_control.sh
+ exit 0
rc=0
```

Отдельная экспериментальная копия `0141498`: изменён ТОЛЬКО `.github/workflows/ci.yml`, добавлены `check_spec_ready` в `keys:` существующего шарда и шаг `npm run check:spec-ready -- . contracts/036-spec-tochnost-prefriz-gejt.md`. Контракт, гейт, package.json и фикстуры не менялись.

```text
$ bash scripts/verify_antiplacebo.sh --scope check_spec_ready
SCOPED: барьеров 1 из выборки — не для приёмки
барьеров: 1 · фикстур: 4 · предъявлено красным повторным прогоном: 4
rc=0
$ bash scripts/verify_ci_parity.sh
workflow-команд: 37 · скриптов в приёмке: 47 · объявленных исключений: 24 · matrix-ключей: 39 · анти-плацебо-запусков: 2 · расхождений: 0
rc=0
$ bash scripts/check_spec_ready.sh <toy> contracts/998-control.md
OK
rc=0
```

Здесь доказана зелёность НОВОЙ приёмки v3 г7/г8 при живом обходе В4-3. Полный CI, старые scoped-семьи и санкционированная заморозка 037+ не запускались и зелёными этим вердиктом не объявляются.

## Остальные собственные проверки

### Предполёт судимого черновика и В3

На неизменённом `0141498`:

```text
$ bash scripts/check_spec_ready.sh . contracts/036-spec-tochnost-prefriz-gejt.md
спек-гейт 036: В4 — семья check_spec_ready в правке этим же черновиком — В4 для неё судит критик
спек-гейт 036: В4 — семья spec_ready в правке этим же черновиком — В4 для неё судит критик
OK
rc=0
```

Прогон дошёл до В3. Зоны прочитаны отдельно из обоих frozen-блобов и v3: по сравнению с union v1∪v2 добавлен только каталог `fixtures/check_spec_ready/` роли architect. Новых выпадений относительно v2 нет. Буквальное токенное включение union в v3 НЕ выполняется: три старых поимённых пути `fixtures/check_check_spec_ready/case_prichina_predmeta.sh`, `case_zamer.sh`, `case_perenos_zony.sh` с v2 заменены каталогом той же роли implementer. Это уже объявленный перенос, сохранённый строкой СПАСЕНО `:210`, а не новая потеря v3.

```text
$ git log --format='%H %an %s' frozen/contracts/036/1..HEAD -- fixtures/check_check_spec_ready/case_prichina_predmeta.sh fixtures/check_check_spec_ready/case_zamer.sh fixtures/check_check_spec_ready/case_perenos_zony.sh
b98896acabd9d810c7bc746c2dd6d0839b4fbc0a implementer 036 — мета-семья check_check_spec_ready: wrapper + 3 case_* (причина-предмета, замер, перенос зоны) + freeze-contract case_spec_preflight_gejt
rc=0
```

Единственный коммит показанного окна по этим путям покрыт сохранённым полным хешем. В3 не ослаблена.

### Г7 и контрпример 6096e3d6-класса

На неизменённом HEAD выполнена scoped-батарея:

```text
$ bash scripts/verify_antiplacebo.sh --scope check_spec_ready
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_spec_ready/case_v4_1_semlja_bez_bariera.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: семья без барьера: scripts/check_netu.sh не существует»
  ok   check_spec_ready/case_v4_2_mjortvaja_proba_s_frazoj.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: проба несовместима с грамматикой барьера семьи judge_gate»
  ok   check_spec_ready/case_v4_3_net_zelenogo_kontrolja.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: зелёный контроль семьи не предъявлен»
  ok   check_spec_ready/case_v4_regression_chestnyj.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «спек-гейт 036: проба красна без заявленной причины»

барьеров: 1 · фикстур: 4 · предъявлено красным повторным прогоном: 4
rc=0
```

Дополнительно A2-фикстура запущена напрямую, с `BARRIER=<клон>/scripts/check_spec_ready.sh`, `REPO=<клон>`, отдельным `WORK`:

```text
OK
свидетельство: ДО В4 дыра A2 жива — rc 0, «OK»
спек-гейт 036: проба несовместима с грамматикой барьера семьи judge_gate (argv-совместимость): fixtures/check_judge_gate/case_peresnjatie_n113.sh
rc=1
```

Старая дыра действительно закрыта. Дифференциал `ef5ff3a` исполнен внутри фикстур, включая извлечение соседней библиотеки; для A2 сырой результат приведён выше. Это не доказательство отсутствия другого обхода.

### Код, литералы, порядок, зоны

Три шаблона именованных отказов В4 из `:180–182` сравнены с printf-шаблонами кода с подстановкой `<key>/<путь>` вместо `%s`: совпадение 3/3. Оба литерала грамматического отказа совпадают: `ОТКАЗ диспетчер`, `использование:`. Порядок `:203–204` соответствует коду: В1 с В4-2 после запуска пробы → В4-1/В4-3 → В2 → В3. Исключение для семьи в правке объявлено и наблюдалось в stderr собственного прогона.

Названный в задании диапазон `7e11c9e..24057a1` по `scripts/check_spec_ready.sh` ПУСТ: `7e11c9e` уже несёт этот код. Настоящий diff `24057a1^..24057a1` — один файл, +119/-23. Этот файл в зоне implementer с v1 и остаётся там в v3; единственная новая зона — каталог фикстур architect. Проводка CI также в существующей зоне implementer.

`git diff --exit-code frozen/contracts/036/2 HEAD -- scripts/freeze_contract.sh scripts/check_check_spec_ready.sh scripts/check_zones.sh scripts/verify_antiplacebo.sh scripts/check_contract_ready.sh scripts/check_check_contract_ready.sh` — пустой вывод, rc 0. Эти смежные барьеры не изменены; повторный полный проектный прогон не выполнялся.

## Совет по приёмке

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:328 — утверждение «приземление ключа в keys: зелёнит пробу» неверно на судимом дереве. В отдельной копии добавление ТОЛЬКО ключа даёт parity rc 1 с `скрипт «check:spec-ready» есть в приёмке, но отсутствует в CI и не объявлен исключением`; matrix-ключей становится 39, расхождений остаётся 1. Добавленный в эксперименте отдельный npm-шаг даёт rc 0. Оба изменения укладываются в уже названную зону ci.yml; это совет уточнить проводку, не самостоятельный блокер без обхода.

## Пять вопросов гейта

1. Критерий слабее предмета — да, назван и исполнен обход В4-3 при зелёных г7/г8.
2. Команды с кодом возврата названы; проблема не в отсутствии команды, а в её различающей силе. Обещанный простой маршрут г8 отдельно помечен советом.
3. Проверяемое свидетельство фактического вызова барьера не определено; лексическое наличие пути не доказывает живой вызов. Это тот же блокер, не новая находка.
4. Исполнители и границы названы; дельта и старое СПАСЕНО сохранены, механизм — check_zones. Нового выхода за зоны не найдено.
5. Зелёность декларации вместо действия не устраняет требование AGENTS.md о красном предъявлении проверки против имитации. Отдельный нормативный блокер без обхода не используется: основание FAIL — конкретный эксперимент выше.

К1 был отказом из-за отсутствия закоммиченного предмета; текущая причина иная. Повторного FAIL по одной причине нет. Это второй круг, не разрешение на третий: следующий маршрут определяется капом и нормой арбитража.

Вердикт не разрешает заморозку v3. Файл контракта и реализация в основном репозитории судьёй не правились; изменяется только судейский артефакт.

— critic, 2026-09-20
