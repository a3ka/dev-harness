FAIL

# Контракт 036/3 — круг 4 SCOPED: операнд обёртки принят за команду

Предмет — `contracts/036-spec-tochnost-prefriz-gejt.md`, блоб
`7c71aa3584ee33207c4f5f94a8c09582cd3d39db`, HEAD собственного клона
`333e6fccf457be1efe5c17ee6ad49094912e44f0` ветки `temp/036v3-judge`.
Набор полон: предмет, команды готовности, исполнители и ЗОНА-строки закоммичены.

Граница суда — п.4 арбитража `036d1f1`, совместно с `8e6ec26`: применение
критерия «командная позиция в изолированной трассе» и новые дефекты дельты
`ef5979a..333e6fc`. Закрытые находки к1/к2/к3, census-проза, СПАСЕНО и зоны
не переоткрываются. Это не DONE реализации и не повторный суд всего 036.

## Единственная блокирующая находка: В4-3-ОПЕРАНД-ОБЁРТКИ

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:74 — новый оракул не применяет требование КОМАНДНОЙ позиции к обёрткам с операндами опций. В `scripts/check_spec_ready.sh:151–155` awk удаляет любое слово-флаг, но не разбирает его операнд; аргумент `env -u` после такого удаления принимается за исполняемую команду. Это новый дефект дельты оракула, а не повтор закрытых строк к3.
ОБХОД: в конформном toy-контракте объявить семью `fixtures/check_judge_gate/`, оставить настоящий `scripts/check_judge_gate.sh` вне зон правки и предъявить единственную пробу `env -u scripts/check_judge_gate.sh >/dev/null; exit 0`. Неизменённый гейт 333e6fc возвращает rc 0 и `OK`: критерий наличия живого зелёного контроля В4-3 засчитан, хотя env только удаляет переменную окружения с таким именем, а барьер не вызывается. Литерал находится в позиции операнда `-u`, НЕ в командной позиции. Трасса подлинная; проба не пишет fd9, не меняет PS4, не подменяет функцию, PATH, барьер или файл трассы.

Это неприменение арбитражного критерия, не остаточный риск same-UID. Не требуется
расширять модель угроз или добавлять xargs/nice в белый список: нельзя кредитовать
операнд опции как команду. Зелёность всей батареи в этом обходе НЕ заявляется:
ниже отдельно зафиксированы ошибки предъявленного набора; ложный зачёт В4-3
измерен прямым вызовом неизменённого гейта.

Дополнительные формы того же дефекта: `command -v scripts/check_judge_gate.sh;
exit 0` и `bash -n scripts/check_judge_gate.sh; exit 0` также дают rc 0/OK.
Первая только ищет команду, вторая проверяет синтаксис файла; тело барьера
не исполняется. Для FAIL достаточно уже формы `env -u`.

АРБИТР: ArbV43Live (роль arbiter, автор 036d1f1). Критик сам направил новые измеренные входы и вопрос границы: операнд опции/неисполняющий режим против принятой командной позиции. Повтор общей причины «данные приняты за вызов» при новом парсере не оставлен спором автора с судьёй.

Арбитр ответил закоммиченным **Дополнением 3, d941883** к
`verdicts/arbitration/036-v4-3-zhivoj-vyzov.md`: находка признана легитимным
НОВЫМ ДЕФЕКТОМ ДЕЛЬТЫ в скоупе к4, не остаточным риском. Его прежняя классификация
query/syntax-форм как риска в Дополнении 2 прямо исправлена. Маршрут правки
предиката и красного входа назван арбитром; критик его не переизобретает.
Это подтверждение границы, не новый круг всего 036.

## Минимальное воспроизведение

Toy — git-репозиторий с начальным коммитом; `scripts/check_judge_gate.sh` скопирован
побайтово из 333e6fc. Для положительных контролей использован честный
`scripts/judge_gate.sh` по существующему S-zhivyj фикстур семьи.

`contracts/998-control.md`:

```markdown
# Контроль командной позиции
## Приёмочный критерий
- `bash fixtures/check_judge_gate/case_control.sh`
## Зоны
ЗОНА architect: fixtures/check_judge_gate/case_control.sh fixtures/check_judge_gate/case_future.sh
```

`fixtures/check_judge_gate/case_control.sh`:

```bash
#!/usr/bin/env bash
env -u scripts/check_judge_gate.sh >/dev/null
exit 0
```

Собственный прогон:

```text
$ bash <333e6fc>/scripts/check_spec_ready.sh <toy> contracts/998-control.md
OK
rc=0
$ env SHELLOPTS=xtrace bash fixtures/check_judge_gate/case_control.sh
+ env -u scripts/check_judge_gate.sh
+ exit 0
rc=0
```

## Четыре красных и честные контроли

Все строки — собственные прогоны неизменённого гейта 333e6fc над одним
контрактом с заменой только тела пробы. Для матрицы отдельно снимались трассы
с `BASH_XTRACEFD=9`; stderr не принимался за доказательство вызова.

| Вход | rc гейта | Наблюдение |
|---|---:|---|
| `exit 0` перед буквальным вызовом | 1 | «зелёный контроль семьи не предъявлен» |
| `B=…; exit 0; bash "$B" …` | 1 | тот же именованный отказ |
| `true scripts/check_judge_gate.sh; exit 0` | 1 | тот же именованный отказ |
| печать `+ scripts/check_judge_gate.sh …` в stderr, затем exit | 1 | тот же именованный отказ |
| честный `bash scripts/check_judge_gate.sh . зелёный` | 0 | OK |
| честный `BARRIER=…; bash "$BARRIER" . зелёный` | 0 | OK |
| честный вызов со stderr-шумом | 0 | OK |
| `env -u scripts/check_judge_gate.sh >/dev/null; exit 0` | 0 | новый ложный зачёт |
| `command -v scripts/check_judge_gate.sh; exit 0` | 0 | ложный зачёт поиска |
| `bash -n scripts/check_judge_gate.sh; exit 0` | 0 | ложный зачёт синтаксиса |

Четыре закрытых входа к2/к3 действительно закрыты; их результаты не превращаются
в новые находки. Изоляция stdout/stderr реализована и наблюдаемо работает.

## Обёртки вне списка и остаточный риск

Проверены `printf '<литерал>\n' | xargs true`, `nice true <литерал>`,
`nohup true <литерал>`, `(true <литерал>)`: все rc 1 с именованным отказом В4-3,
ложного зачёта данных не найдено. Живые `nice bash <литерал> . зелёный` и
`nohup bash <литерал> . зелёный` также отвергнуты rc 1 — допустимая арбитражем
fail-closed-граница экзотических форм. `(bash <литерал> . зелёный)` проходит rc 0.
Многострочный литерал-данные отвергнут rc 1.

Прямая запись поддельной строки в fd9 и функция с именем-путём барьера дают rc 0.
Это принятый остаточный риск d2ffb8e, явно записанный в контракте `:77–79` и
комментарии гейта `:133–136`. Он не блокер и не предлагается к исправлению.

## Батарея и канонизация

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:345 — заявленный зелёный контроль после канонизации не подтверждён на предъявленном коммите. Три старые пробы содержат `exec bash scripts/check_judge_gate.sh . зелёный` (`case_v4_3_mjortvyj_vyzov.sh:57`, `case_v4_3_net_zelenogo_kontrolja.sh:52`, `case_v4_regression_chestnyj.sh:64`), но `exec` отсутствует в белом списке. Прямые прогоны трёх фикстур отказали уже на живом контроле: rc 1, «зелёный контроль семьи не предъявлен». Четвёртая канонизированная фикстура, `case_v4_2_mjortvaja_proba_s_frazoj.sh`, батареей принята. Это фактический провал запрошенной проверки, не самостоятельный блокер без обхода. Дополнение 2 арбитра в d941883 уже называет механизм для exec; его наличие в другом коммите не меняет результат суда 333e6fc.

Изолированный scoped-прогон, без параллельного предполёта в том же клоне:

```text
$ bash scripts/verify_antiplacebo.sh --scope check_spec_ready
SCOPED: барьеров 1 из выборки — не для приёмки
  ok   check_spec_ready/case_v4_1_semlja_bez_bariera.sh
  ok   check_spec_ready/case_v4_1_semlja_bez_bariera.sh
  ok   check_spec_ready/case_v4_2_mjortvaja_proba_s_frazoj.sh
  ok   check_spec_ready/case_v4_3_literal_dannye.sh
  FAIL check_spec_ready/case_v4_3_mjortvyj_vyzov.sh: нет положительного контроля
  FAIL check_spec_ready/case_v4_3_net_zelenogo_kontrolja.sh: нет положительного контроля
  ok   check_spec_ready/case_v4_3_prisvoenie.sh
  ok   check_spec_ready/case_v4_3_stderr_kanal.sh
  FAIL check_spec_ready/case_v4_regression_chestnyj.sh: нет положительного контроля
барьеров: 1 · фикстур: 9 · предъявлено красным повторным прогоном: 6
расхождений: 3
rc=1
```

Хвосты строк фикстур сокращены. Раннер у первых двух файлов печатает одинаковое
имя из одинаковых метаданных. Независимое `git ls-tree --name-only HEAD
fixtures/check_spec_ready/` показывает два реальных пути `...bez_barera.sh` и
`...bez_bariera.sh`: первый добавлен дельтой 333e6fc. Это объяснение фактических
9 вместо заявленных 8, НЕ переоткрытие census-прозы.

Затем, последовательно после батареи:

```text
$ bash scripts/check_spec_ready.sh . contracts/036-spec-tochnost-prefriz-gejt.md
спек-гейт 036: проба красна не по предмету: вывод не несёт заявленной причины
rc=1
```

Зелёного предполёта именно 333e6fc нет; `3/1/8 сходится` собственным прогоном
не доказано: предполёт отказал раньше В2. Судья не правил предмет ради зелёного.

## Пять вопросов гейта в границе круга

1. **Критерий слабее предмета?** В4-3 кредитует операнд `env -u` без вызова;
   конкретное принятое состояние и rc предъявлены выше.
2. **Готово доказуемо командами?** Команды и коды названы; батарея и предполёт
   этого HEAD красны. Полная приёмка и CI не запускались.
3. **Решение оставлено исполнителю?** Режимы и операнды обёрток ошибочно сведены
   к пропуску флагов; это одна находка, не требование расширять белый список.
4. **Границы исполнения?** Исполнители и зоны есть; оракул — существующая зона
   implementer, фикстуры — architect, вердикт — critic. Закрытый суд зон не открыт.
5. **AGENTS.md?** Требование отличать имитацию от исполнения применено к новому
   обходу; отдельный нормативный FAIL не добавлен. Арбитр созван самим критиком.

Неизменность соседей:

```text
$ git diff --exit-code ef5979a 333e6fc -- scripts/freeze_contract.sh scripts/check_check_spec_ready.sh scripts/check_zones.sh scripts/verify_antiplacebo.sh scripts/check_contract_ready.sh scripts/check_check_contract_ready.sh
[вывод пуст]
rc=0
```

Экспериментальные скрипты и результаты:
`/tmp/dev-harness-verify/scratch/critic036v3k4/` (`probe.py`, `results.json`,
`fixtures_probe.py`, `fixture-results.json`). Минимальный контрпример сохранён
в этом коммитном вердикте. Производственных изменений судья не вносил.

Заморозка не разрешена. Подпись владельца не подменена; маршрут
accept → дельта и вердикт владельцу на подпись пока не наступил.

— critic, 2026-09-20
