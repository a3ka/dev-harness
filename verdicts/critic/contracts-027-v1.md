FAIL

# Критик: контракт 027, круг 1, до заморозки

Судимый HEAD: `405c246564e94917481de66ec29945d16259fa48`.
Предмет: `contracts/027-doc-priemka.md`; авторская пачка
`a95be6bd9e18b20e4f506b7b83e3b870e41d740a`, формат-фикс
`7bb768af1702d76ba5438c1d320aef1ab00808ce`.
Суд выполнен в одноразовом клоне origin/main
`/tmp/dev-harness-verify/critic027-review-0916`.
Реализация не судилась и не создавалась; существующие пробы исполнены без правок.

## Стенограмма спавна

check_no_leak --check /home/aka/Documents/dev-harness → rc=1 ×6 „основной чекаут мутировал во время сверки" (Б4-ловец); соло-замер 20:04-20:17: единственный некарвнутый мутатор agent/cache/composer/<hash>/status.json — job-учёт самой сверки; слово владельца 2026-09-16: раскрытый отказ одноразово для 027 (путь б, прецедент dbeffd4); структурный фикс — предмет очереди Н-98 (NABLIUDENIA.md, 405c246), НЕ именной паттерн

## Блокирующие находки

БЛОКИРУЕТ contracts/027-doc-priemka.md:246 — названная мера проверяет согласие renderer с собственным --check, но не предъявляет генерацию фактов, статусов и источников, обещанную в строках 130–136.
ОБХОД: дерево реализует все JSON-проверки, preflight и проводку, но renderer всегда очищает содержимое formal-фрагмента; его --check возвращает 0 ровно для пустого фрагмента и 1 для непустого. Checker проверяет пакет и согласие с этим renderer. В `_doc027.py:217-230` вставка постороннего текста даёт требуемый отказ, повторная генерация очищает его, следующий --check зелёный; переключение на to-be также проходит, хотя ни один факт, статус или источник не попадает в Markdown. Остальные прямые пробы от содержимого formal-фрагмента не зависят. Будущие case_* с положительным контролем пустого фрагмента и отрицательным контролем ручной вставки удовлетворяют объявленным требованиям строк 188–190 и могут пройти scoped antiplacebo; npm/CI-проводка этого различия не добавляет.
Основание: `fixtures/check_check_contract_ready/_doc027.py:144-146,217-230`.
[INFERENCE] Это конкретное слабое состояние будущей реализации, выведенное из кода приёмки, НЕ отчёт о запуске отсутствующего renderer. Нужна независимая наблюдаемая мера зависимости формального результата от факта/статуса/источника; фиксировать весь формат Markdown для этого не требуется.

БЛОКИРУЕТ contracts/027-doc-priemka.md:247 — read-only и изоляция probe из строк 152–153 не доказаны на изменяющем вход запуске: сохранность снимается только после восстановления предмета и замены probe на безвредный.
ОБХОД: дерево реализует остальные обязанности, а checker снимает оракул в память, но запускает probe непосредственно в исходном проекте. Probe из `_doc027.py:238-243` меняет contract и evidence на диске и возвращает 8; checker честно отвергает 8 относительно исходного expected=7 с rc=1. Ассерт rewrite проходит. Затем сама проба переписывает probe, восстанавливает contract/evidence и рендерит пакет (строки 245–247); snapshot появляется лишь в строке 251. Последующий безвредный запуск ничего не меняет, поэтому read-only-control проходит. Исходный проект уже был изменён проверкой, но объявленная команда зелёная. Будущие case_* могут воспроизводить эти же контроли, а настоящая npm/CI-проводка не добавляет отсутствующего сравнения сохранности на отказном запуске.
Основание: `fixtures/check_check_contract_ready/_doc027.py:233-254`.
[INFERENCE] Это вывод по порядку операций существующей пробы, не исполненное воспроизведение будущего checker. Требуется снять состояние до изменяющего probe и сравнить его непосредственно после отказа, до любого восстановления; обязанности оракульной неизменности и сохранности проекта различны.

## Остальные обязательные вопросы гейта

- Набор полон: предмет закоммичен, критерий содержит конечный перечень rc-команд, исполнители и ЗОНА-строки названы. Отказ не из-за отсутствия артефактов и не из-за ожидаемого красного до реализации.
- Критерий слабее предмета в двух конкретных местах выше. Остальные требования не расширялись до полного аудита будущей реализации.
- Молча оставленные решения: формат строк renderer сознательно оставлен реализационной деталью; это допустимо. Блокирует не свобода формата, а отсутствие независимого различения результата. Принятые решения владельца Q1–Q7/D10–D18 не перевыбирались: сохранены два профиля, implementer как автор, ограниченный reviewer-суд, снимочная Q7-гарантия и отсутствие универсального TTL.
- Границы названы в строках 216–232: исходники и проводка — implementer; критерий и исходные пробы — architect; ролевые правки — orchestrator; вердикты — соответствующие судьи. Повторные ЗОНА-строки допустимы: действующий механизм объединяет пути автора. Перечисленные пути соответствуют объявленной грамматике. Реестр, project-агенты и Odelix исключены строками 273–278; реализация назначена после freeze.
- Н-39 соблюдена в судимом тексте: критерий несёт инварианты и rc-команды; пары слабых форм и ветвей находятся в коде `_doc027.py`, не в прозе контракта. Нового противоречия AGENTS.md, самостоятельного от двух обходов, не предъявлено.

СОВЕТ contracts/027-doc-priemka.md:203 — мандат reviewer пока является будущим обязательством orchestrator, не исполненным изменением роли на судимом HEAD. `git log -3 --format=fuller -p -- roles/reviewer.md` показывает последнюю правку роли `09cf51b9f10d9704053ea2b7ee9d4e55554a5af3` от 2026-08-17, а не правку 2026-09-16. Текст требует изменить роль ДО открытия именно её круга; текущий круг — critic, поэтому нарушения правила 14 этим фактом не установлено. Перед вызовом reviewer следует предъявить соответствующий orchestrator-коммит, не утверждать, что он уже существует.

## Лично исполненные команды

Каждая команда запущена из одноразового клона; Git-переменные контекста сняты.
Красное — результат повторного прогона критика, не запись фикстуры о себе.

| Команда | rc | Сырой вывод |
|---|---:|---|
| `bash fixtures/check_check_contract_ready/red_doc_obligations.sh` | 1 | `ОТКАЗ DOC-obligations/missing: поведение rc=0, требуется 1; OK` |
| `bash fixtures/check_check_contract_ready/red_doc_product.sh` | 1 | `ОТКАЗ DOC-product/outcomes: поведение rc=0, требуется 1; OK` |
| `bash fixtures/check_check_contract_ready/red_doc_architecture.sh` | 1 | `ОТКАЗ DOC-architecture/dangling: поведение rc=0, требуется 1; OK` |
| `bash fixtures/check_check_contract_ready/red_doc_evidence.sh` | 1 | `ОТКАЗ DOC-evidence/drift: поведение rc=0, требуется 1; OK` |
| `bash fixtures/check_check_contract_ready/red_doc_assertions.sh` | 1 | `ОТКАЗ DOC-assertions/value: поведение rc=0, требуется 1; OK` |
| `bash fixtures/check_check_contract_ready/red_doc_status_render.sh` | 1 | `ОТКАЗ DOC-status_render/proposal: поведение rc=0, требуется 1; OK` |
| `bash fixtures/check_check_contract_ready/red_doc_oracle.sh` | 1 | `ОТКАЗ DOC-oracle/rewrite: поведение rc=0, требуется 1; OK` |
| `bash fixtures/freeze_contract/red_doc_lifecycle.sh` | 1 | `ОТКАЗ DOC-lifecycle/ready-preflight: поведение rc=0, требуется 1; OK` |
| `python3 fixtures/check_check_contract_ready/_doc027.py regressions` | 0 | вывода нет |

Итого: восемь именованных отказов rc=1, регрессии rc=0. Код `subject()`
исполняет прежний `check_contract_ready.sh` при отсутствии doc-CLI; положительный
контроль предшествует отказу. Наблюдаемое rc=0 субъекта на отрицательном входе —
именно ожидаемая отсутствующая проверка, не rc=127 и не ложное заявление о
прохождении ещё недостигнутых последующих ассертов.

А-114 проверена историей: `git show --format=fuller --stat a95be6b` — новые
пробы только в `fixtures/check_check_contract_ready/` и `fixtures/freeze_contract/`;
`git ls-tree a95be6b^` для этих путей показывает существовавшие case-файлы.
Новых семей `fixtures/check_document/` и `fixtures/render_document/` в пачке нет;
их создание назначено после freeze, а не выполнено до него.

Полные проверки проекта, постреализационные npm-команды и будущие case_* не
запускались; зелёность этих команд не заявляется. Контрольные эксперименты с
новой слабой реализацией не создавались согласно границе задания.
Н-98 и перечисленные в задании известные детекторные случаи не пересуживались;
кириллические зонные пути контракт не обещает. Два блокера относятся только
к различимости собственного приёмочного критерия 027.
