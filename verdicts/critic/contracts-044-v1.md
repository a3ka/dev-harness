accept

# Контракт 044 — второй круг критика, закрытие Б1–Б3

Предмет: `contracts/044-bulk-peresnjatie-bazlajna.md` на HEAD
`fb200cd3dd2ab8dcf4c1484f844b101152525ffb`.
Прочитан дифф `952267f71226795e5bfeb188268cfbdf20bd1881`, не только самоотчёт.
Между этим коммитом и судимым HEAD контракт и `fixtures/check_no_leak/`
не изменены (`git diff --exit-code` → rc 0).
SSH-клон: `/tmp/dev-harness-verify/critic-044-r2`; `git remote -v` показал
`ssh://git@github.com/a3ka/dev-harness.git` для fetch и push.

Область — ровно Б1–Б3 из `verdicts/critic/contracts-044-v1.md`, по прямому
пределу владельца и варианту X, п.2 консультации
`verdicts/consultant/kaskadnaja-jama-037-044-triazh-v1.md:141`.
Набор артефактов полон: контракт закоммичен, предмет, команды приёмки,
исполнители architect/implementer и ЗОНА-строки присутствуют.
Контракт и реализацию критик не правил; нового аудита конструкции не открывал.

## Б1 — закрыт по существу

`contracts/044-bulk-peresnjatie-bazlajna.md:460-469`: есть структурное поле
`ПРОВОДКА:` и запись `role=roles/orchestrator.md` с полной нормой-строкой.
Прочитана сама строка роли (`roles/orchestrator.md:230`): bulk-дверь,
стенограмма и «отказ обеих дверей переснятия = СТОП и доклад» присутствуют.
Поведенческий канал не заменён декларацией чистого энфорсмента.
Живой `check_provodka.sh` → rc 0. Дополнительный красный факт №10 первого
круга также снят приземлением строки роли.

## Б2 — закрыт по существу

`contracts/044-bulk-peresnjatie-bazlajna.md:334-365,381-383`: фикстура
перенесена в `fixtures/check_no_leak/`, туда объявлена зона architect,
добавлен `.probe-only`; приёмочная команда использует новый адрес.
Дифф переноса меняет комментарии, а не исполняемую проверку. Фикстура
действительно вызывает `scripts/check_no_leak.sh` через bash
(`fixtures/check_no_leak/red_bulk_peresnjatie_bazlajna.sh:115-139`).
Адрес теперь соответствует субъекту, а не чужой семье check_judge_gate.

Живой precision-гейт → rc 0. Граница доказательства: задача (б) для no_leak
**пропущена**, а не предъявила полярность. Пропуск предусмотрен существующей
логикой `scripts/check_precision_gate.sh:267-282`: этот же черновик раздаёт
`scripts/check_no_leak.sh`. Это не изменение гейта под контракт и не действие
самого `.probe-only`. Прямое исполнение перенесённой фикстуры выполнено
отдельно два раза: оба rc 0, в каждом выводе б0–б10 и бб1–бб6.
Прежний family mismatch устранён согласованием адреса с субъектом.

## Б3 — закрыт по существу

`contracts/044-bulk-peresnjatie-bazlajna.md:415-427`: все три запрещённые
привязки «стаб … умирает здесь» удалены из критерия. Теперь названы входы
и ожидаемые отказы. Бб3 прямо описан как приоритет покрытия замороженной
зоной, с отказом из 7г, без упражнения fallback; бб5 требует ancestry.
Аналогичные привязки удалены из границы-5 (`:90-97`). Прежнее противоречие
AGENTS о паре «обманный стаб ↔ ветвь» в этих местах устранено. Неточность
нового объяснения бб4 ниже — совет, а не возобновление Б3.

## Живые проверки

Все команды исполнены из судимого SSH-клона.

```text
$ bash scripts/check_provodka.sh . contracts/044-bulk-peresnjatie-bazlajna.md
[вывод пуст]
rc=0

$ bash scripts/check_precision_gate.sh . contracts/044-bulk-peresnjatie-bazlajna.md
precision-гейт 043: семья no_leak — барьер правится этим же черновиком, задача (б) для неё пропущена (прецедент 036 В4-3)
OK
rc=0

$ bash fixtures/check_no_leak/red_bulk_peresnjatie_bazlajna.sh
[прямой прогон 1: строки б0–б10 и бб1–бб6]
ok: дверь bulk-переснятия 044 — все ворота пройдены (б0..б10, бб1..бб6)
rc=0

$ bash fixtures/check_no_leak/red_bulk_peresnjatie_bazlajna.sh
[прямой прогон 2: строки б0–б10 и бб1–бб6]
ok: дверь bulk-переснятия 044 — все ворота пройдены (б0..б10, бб1..бб6)
rc=0

$ bash scripts/check_spec_ready.sh . contracts/044-bulk-peresnjatie-bazlajna.md
спек-гейт 036: В4 — семья no_leak в правке этим же черновиком — В4 для неё судит критик
OK
rc=0
```

Квадратные скобки — сокращение вывода для отчёта, не строки команд.
CLI-буллеты `:452-453` теперь имеют ожидаемые фразы красного исхода;
попутная правка грамматики не является расширением предмета. Сам
`check_spec_ready.sh` коммитом `952267f` не изменён. Пропуск его В4 также
не выдаётся за проверенную полярность.

Дополнительно исполнено:

- `bash scripts/verify_antiplacebo.sh . --scope check_judge_gate` → rc 0.
  Вывод: зелёные контроли и красные повторные прогоны для
  `case_dispatcher_sha.sh`, `case_krasnyj_lax.sh`, `case_zelenyj_bypass_sha.sh`;
  итог механизма: «барьеров: 1 · фикстур: 3 · предъявлено красным повторным
  прогоном: 3». Это scoped, не полный регресс проекта.
- `git diff --exit-code frozen/contracts/031/4 -- fixtures/check_judge_gate/red_peresnjatie_bazlajna.sh contracts/031-strazh-semejstvo-mint-i-bazlajn.md`
  → rc 0, вывод пуст: оба замороженных регресс-пути 031 неизменны.

## Пять вопросов — в пределах повторного круга

1. **Критерий слабее предмета?** Б2 устранён без замены работающей фикстуры
   заглушкой; прямые прогоны проверяют последствия переснятия и отказы.
2. **Готовность недоказуема командой?** Четыре заказанные проверки исполнены
   с rc 0; пропуски precision/spec-ready явно раскрыты выше.
3. **Молчаливое решение исполнителя?** Адрес семьи и способ предъявления
   теперь согласованы с действующим механизмом явно.
4. **Границы исполнения?** Контракт и новая семья — architect, скрипт —
   implementer; роль приземлена оркестратором, повторной раздачи нет.
5. **Противоречие AGENTS?** Прежние Б1 и Б3 устранены: поле проводки есть,
   запрещённых трёх пар в критерии нет.

## Совет, не условие accept

СОВЕТ contracts/044-bulk-peresnjatie-bazlajna.md:422-425 — новое объяснение бб4
«путь не матчит грамматику contracts/<M>-*» неточно. В фикстуре
`fixtures/check_no_leak/red_bulk_peresnjatie_bazlajna.sh:495-505` путь имеет
вид `contracts/${NNN4}-rucnoj-${RANDOM}.md`, NNN4 — три цифры; отсутствует
резервный тег, а не соответствие имени грамматике. Именованный отказ и
проверка сохранены, прежняя запрещённая пара «стаб ↔ ветвь» удалена.
Это новая текстовая неточность вне трёх прежних блокеров, не критический
обход; по пределу владельца нового блокирующего круга она не открывает
и ответа автора не требует.

Итог: ровно Б1–Б3 закрыты. Незакрытых блокеров этого повторного круга нет.
Заморозка разрешена в пределах критик-гейта. Это не замена адверсария,
ревьюера или done-церемонии.
