FAIL

# Контракт 044 — первый круг критика

Предмет: `contracts/044-bulk-peresnjatie-bazlajna.md` на
`7e01ed220991b23af4fffe89d029693f84b4e5cb`.
SSH-клон: `/tmp/dev-harness-verify/critic044-20260924-a`.
При клонировании HEAD = origin/main = указанный SHA; origin —
`ssh://git@github.com/a3ka/dev-harness.git` (fetch/push).

Набор артефактов полон: контракт закоммичен, предмет и rc-критерии присутствуют,
исполнители и ЗОНА-строки названы. Статус «черновик» не означает отсутствие предмета
на HEAD. Это ПЕРВЫЙ круг; повторной причины и основания созывать арбитра нет.
Текст контракта и реализацию критик не менял.

## Блокирующие находки

### Б1 — отсутствует обязательное поле проводки

БЛОКИРУЕТ contracts/044-bulk-peresnjatie-bazlajna.md:424-429 — раздел имеет заголовок
`## ПРОВОДКА` и поле `ПРОВОДКА-ЭНФОРСМЕНТ:`, но не имеет самого структурного поля
`ПРОВОДКА:`. Проза про будущий role-канал не объявляет этот канал механизму.
Это прямое противоречие `AGENTS.md:170-171`: поле обязательно, поведенческая норма
идёт role-каналом. Исключение порога для противоречия AGENTS: обход не требуется.

Живое доказательство:

```text
$ bash scripts/check_provodka.sh . contracts/044-bulk-peresnjatie-bazlajna.md
проводка: поле ПРОВОДКА отсутствует
rc=1
```

### Б2 — объявленная новая фикстура не проходит обязательный precision-гейт

БЛОКИРУЕТ contracts/044-bulk-peresnjatie-bazlajna.md:327 — новая фикстура объявлена
в семье `check_judge_gate`, но вызывает детектор `check_no_leak`, а не барьер этой
семьи. Живой precision-гейт прошёл проверку зон, выполнил фикстуру двумя способами,
затем отказал на отсутствии вызова `judge_gate`. Исключение «инструмент недоступен»
неприменимо: инструмент существует и исполнился. По норме 043 rc≠0 блокирует accept.

ОБХОД:   ok   б0 (снимок/сверка 024 живы)

Предыдущая строка после `ОБХОД: ` воспроизводит дословно ПЕРВУЮ строку вывода команды,
как требует норма роли. Это начальный вывод вложенной фикстуры, не причина отказа.
Именованная причина в КОНЦЕ того же прогона:

```text
$ bash scripts/check_precision_gate.sh . contracts/044-bulk-peresnjatie-bazlajna.md
[оба вложенных прогона: б0..б10 и бб1..бб6 зелёные]
precision-гейт 043: fixtures/check_judge_gate/red_bulk_peresnjatie_bazlajna.sh: барьер judge_gate не вызван живьём (/proc-наблюдение не нашло exec)
rc=1
```

В середине второго вложенного прогона также напечатано:
`scripts/check_precision_gate.sh: line 405: /proc/1634263/status: No such file or directory`.
Это не финальная причина отказа. Гейт не отказывал на коллизии зон.
Прецедент прямой приёмки 031 не является объявленным исключением из обязательной
проверки 043; выбирать новый адрес фикстуры или нормативное решение должен автор,
не исполнитель молча и не критик правкой чужого предмета.

### Б3 — запрещённые уставом пары «стаб ↔ ветвь» в самом критерии

БЛОКИРУЕТ contracts/044-bulk-peresnjatie-bazlajna.md:388-394 — приёмочный критерий
привязывает конкретные обманные реализации к конкретным входам: «стаб, признавший
пост-freeze итерацию, умирает здесь» (бб3), «стаб, признающий по одному паттерну
пути, умирает здесь» (бб4), «стаб, признающий по существованию тега без ancestry,
умирает здесь» (бб5). `AGENTS.md:130-133` прямо запрещает контракту утверждать такую
пару. Это не замечание к факультативной прозе Frontier: находка находится в
приёмочном критерии. Исключение порога для противоречия AGENTS: обход не требуется.
Описание входа, требуемого результата и неизменности снимка допустимо; привязка
порчи к различающему входу остаётся делом фикстуры и отчёта прогона.

## Обязательная проверка №10 также красная

Условие `contracts/044-bulk-peresnjatie-bazlajna.md:420-422` не выполнено на судимом
HEAD: строка `отказ обеих дверей переснятия = СТОП и доклад` отсутствует в
`roles/orchestrator.md`. Поиск всей строки дал ноль совпадений; повторный поиск
`retake-bulk|отказ обеих` по `roles/` также пуст. Прямое чтение строки 230 показало
старую норму с «ЕДИНСТВЕННЫЙ разрешённый выход — ... --retake».

Независимый точный подсчёт строк с литеральным вхождением (эквивалент требуемого
`grep -cF`, выполнен временным Python-скриптом, поскольку shell-grep запрещён
инвентарём сессии):

```text
$ python3 tmp/critic044-proof.py count
0
rc=1
```

Это невыполненная обязательная проверка задания и факт состояния дерева, а не
утверждение, будто само по себе отложенное до done приземление роли запрещено.
Оно не заменяет отдельную текстовую находку Б1.

## Проверки и живое свидетельство

1. `bash fixtures/check_judge_gate/red_bulk_peresnjatie_bazlajna.sh` выполнена
   ДВА раза подряд: оба rc=0; в каждом выводе все б0..б10 и бб1..бб6, итог:
   `ok: дверь bulk-переснятия 044 — все ворота пройдены (б0..б10, бб1..бб6)`.
   Дополнительно два трассируемых прогона независимо посчитали воротa и показали
   различие входных корней (не счёт по одной итоговой фразе самой фикстуры):

   ```text
   Прогон 1: WORK=/tmp/red044-bulk.uxlhDl, rc=0, ворот=17
   Ворота: б0, б1, б2, б3, б4, б5, б6, б7, б8, б9, б10, бб1, бб2, бб3, бб4, бб5, бб6
   Прогон 2: WORK=/tmp/red044-bulk.fKcsfO, rc=0, ворот=17
   Ворота: б0, б1, б2, б3, б4, б5, б6, б7, б8, б9, б10, бб1, бб2, бб3, бб4, бб5, бб6
   Корни двух прогонов различны; набор ворот независимо посчитан по трассе.
   ```

2. `bash fixtures/check_judge_gate/red_peresnjatie_bazlajna.sh` → rc=0,
   все р0..р10; итог:
   `ok: дверь переснятия 031 — все ворота пройдены (р0..р10)`.
   Следующий вызов → rc=0, вывод пуст:

   ```text
   git diff --exit-code frozen/contracts/031/4 -- fixtures/check_judge_gate/red_peresnjatie_bazlajna.sh contracts/031-strazh-semejstvo-mint-i-bazlajn.md
   ```

   Проверены ОБА требуемых пути: неизменны и фикстура, и замороженный текст 031.

3. `bash scripts/verify_antiplacebo.sh . --scope check_judge_gate` → rc=0:

   ```text
   SCOPED: барьеров 1 из выборки — не для приёмки
     ok   check_judge_gate/case_dispatcher_sha.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «а judge_gate дал RC»
     ok   check_judge_gate/case_krasnyj_lax.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «судья пропустил»
     ok   check_judge_gate/case_zelenyj_bypass_sha.sh: зелёный контроль есть, повторный прогон красный кодом 1 — «НЕ передал свой»

   барьеров: 1 · фикстур: 3 · предъявлено красным повторным прогоном: 3
   ```

4. Граница-1 проверена чтением живого файла: `red_peresnjatie_bazlajna.sh:6-8`
   действительно содержит «Переснятие базлайна ПРОИСХОДИТ ⟺ ШЕСТЬ условий ВМЕСТЕ».
   `git show frozen/contracts/031/4:contracts/031-strazh-semejstvo-mint-i-bazlajn.md`
   подтвердил, что заголовок механизма-2 ограничивает это режимом `--retake`.
   Новое имя двери само по себе этой эквивалентности не противоречит; требовать
   переписи 031-v5 оснований не найдено.

5. Граница-7 сверена с `contracts/033-union-semantika-zon-check-zones.md:74`:
   строка действительно перечисляет `scripts/check_zones.sh scripts/lib_zones.sh`
   вне раздачи и говорит «правка этих путей — отдельный контракт со своей болью».
   Цитата 044 с обозначенными пропусками точна. `git diff --exit-code
   frozen/contracts/033/2 -- contracts/033-union-semantika-zon-check-zones.md`
   → rc=0, пусто. Это ограничение раздачи 033, а не вечный запрет новому контракту
   владеть этими путями; выбранный 044 отказ от их правки самосогласован.

6. Живой `git show c7de0ef -- scripts/check_no_leak.sh` → rc=0; добавленный якорь:

   ```text
   +# И-3 признание (iv) (контракт 023; draft-признание (контракт 023, ветвь iv;
   +# брат check_zones.sh:846-888) — будущая правка И-3 обязана найти оба места
   ```

   Прямое чтение текущего файла подтвердило сохранение якоря. Обе руки сопоставлены
   с 023:179-185; `git diff --exit-code frozen/contracts/023/1 --
   contracts/023-rezervacija-nomera-dver-po-tegu.md` → rc=0, пусто.

7. Через СУЩЕСТВУЮЩИЙ `zones_load`, без второго читателя заморозок, получено:

   ```text
   implementer	scripts/check_no_leak.sh	024
   implementer	scripts/check_no_leak.sh	031
   ```

   033:58,66-67 задаёт union по автору. Для того же implementer выдача того же пути
   044 не создаёт чужого владельца; сама эта пара НЕ требует ПЕРЕСЕЧЕНИЕ-строки.
   Живой precision-прогон прошёл фазу зон и дошёл до фикстуры. Architect получает
   только контракт и новую red-фикстуру; внутри 044 раздачи по путям различны.

8. Проверка угроз:

   ```text
   $ bash scripts/check_threat_model.sh . contracts/044-bulk-peresnjatie-bazlajna.md
   модель угроз: секция валидна (ЗАЩИЩАЕТ 1 буллет(ов), НЕ ЗАЩИЩАЕТ 1 буллет(ов))
   rc=0
   ```

   Числа выше — дословная диагностика механизма, не подсчёт всех буллетов критиком.
   Содержательно названы защищаемые свойства и остатки identity, честно слитой
   утечки, удалений, локального git/PATH, происхождения тега и дрейфа копий И-3.

9. CLI проверен дополнительно:

   ```text
   $ bash scripts/check_no_leak.sh --retake-bulk
   ОТКАЗ диспетчер: использование: check_no_leak.sh --snapshot|--check|--retake|--retake-bulk <абс-корень>
   rc=1
   $ bash scripts/check_no_leak.sh --retake-bulk .
   ОТКАЗ: корень обязан быть абсолютным: . (CLI судит абсолютный корень основного чекаута — относительный путь резолвится от случайного cwd, Н-85-класс)
   rc=1
   ```

## Пять вопросов критика

1. **Критерий слабее предмета?** Основная боль и обе руки признания предъявлены
   действующими воротами, проверяются последствия переснятия и сохранность снимка,
   не одно существование файлов. Это не заменяет обязательный precision-гейт: Б2.
2. **Готовность доказуема командами?** Именованные основные прогоны существуют,
   но обязательная проводка и заданная проверка строки роли красны: Б1 и замер №10.
3. **Решение молча оставлено исполнителю?** Девять шагов, порядок, авторство,
   literal-покрытие, обе руки и остаточные угрозы описаны. Неразрешённая совместимость
   выбранного адреса фикстуры с гейтом 043 требует решения автора, не скрытого обхода.
4. **Названы границы исполнения?** Architect/implementer названы; роль оркестратора
   и существующая его зона подтверждены 031. Повторное владение implementer законно
   по union; запрет менять старые режимы удерживается регрессией и областью ревью,
   не построчной механикой ЗОНА.
5. **Противоречие AGENTS?** Найдены два прямых: обязательное поле проводки отсутствует
   (Б1), пары «стаб ↔ ветвь» помещены в критерий (Б3).

## Советы, не основания отказа

СОВЕТ contracts/044-bulk-peresnjatie-bazlajna.md:299-313 — демаркация одновременно
требует конформность всем ограничениям и нарушение одного инварианта, затем
исключает из контрпримеров именно незонированность/чужого автора/неслитость.
Лучше разделить положительную область и отрицательные входы: сейчас при буквальном
чтении это затрудняет последующий суд обходов. Самостоятельного блокера из этой
формулировки здесь не делаю.

СОВЕТ contracts/044-bulk-peresnjatie-bazlajna.md:386-390 — бб3 выдаёт пути собственную
замороженную зону, поэтому упражняет приоритет зонного суда над признанием. Не стоит
считать этот вход самостоятельным доказательством проверки frozen-предка внутри
fallback-признания: до fallback на данном пути дело не доходит.

Полный регресс проекта не запускался: по AGENTS выполнен только заданный scoped.
Временные скрипты замеров удалены перед коммитом; постоянная дельта критика —
только этот вердикт. accept не выдан; замораживать текущий текст нельзя.
