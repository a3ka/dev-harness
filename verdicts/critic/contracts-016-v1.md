accept
(записано как v1 по конвенции v<следующая-версия-заморозки>; круги 1–2 FAIL — в git-истории 37e2dc4 и 688581d; арбитраж contracts-016-nabljudaemost-invariantov b1e890b; нумерация кругов отдельными файлами — ошибка оркестратора)

Судимый предмет: исполнение `РЕШЕНИЯ` из `verdicts/arbitration/contracts-016-nabljudaemost-invariantov.md` дельтой `51294abd0ff03fe717e824ed0b7ad60b3ab11526` (= HEAD на входе суда). Граница суда — только С-1/И-1, С-2/И-9, С-3/И-6 и дефекты, внесённые этой дельтой. Решение арбитра прочитано целиком. Набор артефактов полон: контракт закоммичен; предмет, критерий готовности с rc-командами, исполнители и механические `ЗОНА`-строки присутствуют.

## Исполнение решения арбитража

### С-1/И-1 — ИСПОЛНЕНО

`contracts/016-izoljacija-agenta.md:77-94` совпадает с (а)/(б) решения по существу. Текст требует наблюдать переход самого вызова `land_agent.sh`: зелёный case до вызова пинует `main_before` и `tip`, заранее построенного merge в фикстуре нет, а после rc=0 обязательны четыре отдельные сверки `main != main_before`, `main^1 == main_before`, `main^2 == tip`, `%cn(main) == orchestrator`. На :84-88 явно закрыта вилка реализации-валидатора чужого merge. На :88-92 записаны ровно пределы (г): исторический провенанс живого журнала cognitive-only, внутренний способ создания merge не судится. Rc-носитель — scoped `land_agent`; `case_kommit_mimo_merge` и `case_merzh_ne_orkestrator` сохранены на :92-94.

Названный в v2 обход теперь красный по тексту критерия: валидатор заранее созданного merge либо получает зелёную фикстуру без такого merge и отказывает, либо возвращает успех без сдвига main, после чего красна сверка `main != main_before`. Состояния, где все четыре постусловия выполнены без требуемого перехода вызова, решение не оставляет.

### С-2/И-9 — ИСПОЛНЕНО

`contracts/016-izoljacija-agenta.md:133-153` совпадает с (а)/(б) решения по существу. На :136-140 требуется отказ rc 1 ДО merge для любого коммита диапазона с `committer != author`, с поимённой причиной `identity расщеплена: <sha> author=<имя> committer=<имя>`; там же закреплено, что `--no-verify` не помогает, потому что проверка находится в `land_agent`. На :139-144 сохранён единый реестр committer и прямо установлено тождество поля `check_zones` (author) полю И-9 (committer), без второго реестра. `case_imja_vne_reestra` сохранён, `case_identity_rasscheplena` назван rc-носителем на :151-153.

Обход v2 `committer=critic`, `author=implementer`, `--no-verify` теперь красный по тексту инварианта до merge: пара полей расщеплена независимо от хука.

### С-3/И-6 — ИСПОЛНЕНО

`contracts/016-izoljacija-agenta.md:115-127` совпадает с (а)/(б) решения по существу. На :115-119 фикстура обязана пиновать `oid_before` каждой несбитой ветки ДО вызова GC, а после `git rev-parse` обязан вернуть ТОТ ЖЕ OID; это результат-форма, способы смены ref не перечисляются. Список владельцу и существование имени сохранены. `case_zavisshaja_snesena` сохранён, `case_zavisshaja_peredvinuta` назван отдельным красным носителем на :124-127. Пределы (г) про `update-ref` мимо GC и object pruning записаны cognitive-only на :121-124 и круг не открывают.

Обход v2 «напечатать имя и перевести ref на main» теперь красный: имя может сохраниться, но OID после GC не равен `oid_before`.

## Новые находки дельты

Новых находок нет. Sweep добавленной дельты не выявил дефекта в прозе, границах, счётных утверждениях или Н-39:

- дельта меняет только `contracts/016-izoljacija-agenta.md`, три предписанных хунка; новые rc-носители лежат в уже названных зонах `fixtures/land_agent/` и `fixtures/gc_agent_branches/`, а механизмы — в зонах `scripts/land_agent.sh` и `scripts/gc_agent_branches.sh` (`contracts/016-izoljacija-agenta.md:157-162`);
- «четыре сверки» — предписанная арбитром семантическая полнота перехода, не замороженный счёт фикстур/стабов/ветвей; счётных констант будущей приёмки дельта не добавляет;
- новые `case_identity_rasscheplena` и `case_zavisshaja_peredvinuta` — предписанные арбитром красные rc-носители с наблюдаемым результатом, а не запрещённая Н-39 пара «обманный стаб ↔ ветвь»;
- строки cognitive-only повторяют известные пределы разделов (г); они не переопределяют предмет и не заводят новый.

## Что прогнано дословно

Все shell-прогоны выполнены своими руками в отдельном скратч-клоне `/tmp/critic016-v3/repo` на `51294abd0ff03fe717e824ed0b7ad60b3ab11526`; скратч и toy размещены в literal `/tmp`.

1. `git rev-parse HEAD` → rc=0, `51294abd0ff03fe717e824ed0b7ad60b3ab11526`.
2. `bash fixtures/check_staged/probe_check_staged_krasnyj.sh` → rc=1; четыре `ok`, затем поимённо `ОТКАЗ: барьера нет — /tmp/critic016-v3/repo/scripts/check_staged.sh (реализация за implementer после заморозки)`.
3. `bash fixtures/check_hooks/probe_check_hooks_krasnyj.sh` → rc=1; три `ok`, затем поимённо `ОТКАЗ: барьера нет — /tmp/critic016-v3/repo/scripts/check_hooks.sh (реализация за implementer после заморозки)`.
4. `bash scripts/verify_antiplacebo.sh . --scope check_charter` → rc=1; поимённо названы ровно существующие каталоги без барьера `fixtures/check_hooks` и `fixtures/check_staged`; все 7 case `check_charter` предъявлены красным повторным прогоном, итог `расхождений: 2`.
5. Живое дерево, команда И-5 с `contracts/016-izoljacija-agenta.md:109` дословно:
   `! grep -rnE '\bgit\b[^|<>]*[[:space:]](commit|merge)[[:space:]]' scripts/ | grep -vE ':[0-9]+:[[:space:]]*#' | grep -v merge-base | grep -vE 'user\.(name|email)=|GIT_(AUTHOR|COMMITTER)_(NAME|EMAIL)' | grep .`
   → rc=0, stdout пуст.
6. Та же дословная команда в `/tmp/critic016-v3/canary`, где `scripts/bypass.sh:2` содержит `git commit -m 'без identity'`, → rc=1; stdout `scripts/bypass.sh:2:git commit -m 'без identity'`.
7. Выполнены текстовые sweeps контракта с паттернами `Q[1-8]`, `\(Q[1-8]\)`, `расхождений нет|tmp/frontier|Единственный источник`, `стаб`, `ровно|ДВУМЯ|двумя|оба case|обе пробы|три фазы|все три case|[0-9]+ (case|стаб|фаз|проб)`, `^ЗОНА `, `^ЗОНА .*roles/`. Источник решений остаётся контрактом; содержательные Q1–Q8 присутствуют; stale-утверждений об источнике нет; счёт будущих фикстур не заморожен; шесть зон сохранены, `roles/` ни в одну не попал.

## Таймлайн

- подтверждён HEAD `51294ab`, прочитан `git show 51294ab` и зафиксированы три хунка;
- целиком прочитаны решение арбитра и контракт 016; граница (г) принята как предел, а не новый предмет;
- создан отдельный скратч-клон в `/tmp`, выполнены обе обязательные красные пробы, scoped `check_charter`, живая и toy-канарейки И-5;
- С-1/С-2/С-3 сверены с (а)/(б) по существу; затем выполнен delta-sweep прозы, зон, счётов и Н-39;
- контрольная отметка завершения суда: `2026-08-30T00:28:06+02:00`.
