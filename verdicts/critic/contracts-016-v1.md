FAIL

Судимый предмет: `f4b66de2b5fbd1ccf8c113afa2da2631503f01ec`. Сам контракт закоммичен; предмет, приёмочный критерий, исполнители и `ЗОНА`-строки присутствуют. Автоматического отказа по неполному набору из грамматики роли нет. Ниже — суд только предмета, критерия и границ исполнения.

## Блокирующие находки

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:4 — объявленный «единственный источник решений» отсутствует в коммите предмета: `git ls-files --error-unmatch tmp/frontier-068.md` вернул rc=1, а чистый клон `f4b66de` этого пути не содержит. Поэтому Q1–Q8 нельзя сверить по HEAD; утверждение «расхождений нет» опирается на игнорируемый рабочий файл. Это противоречит норме `AGENTS.md`, что истина живёт в закоммиченном исходнике; для противоречия норме ОБХОД по регламенту не требуется.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:51 — контракт прозой связывает стаб «тихий спавн» с конкретной ветвью; то же повторено для «приёмка-OK-без-ветки» на :56, `case_bez_identichnosti` на :102–103 и поимённых входов стабов на :147–153. Это прямо противоречит Н-39 и норме `AGENTS.md`, хотя :142–143 утверждает обратное. Для противоречия норме ОБХОД по регламенту не требуется.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:147 — замороженный текст содержит волатильные счёты стабов/case («три фазы», «оба case»), а :155 фиксирует «ровно с ДВУМЯ» каталогами фикстур. Это расходится со словом владельца, записанным самим контрактом на :135–136, и со счётной нормой `AGENTS.md`: тот же scoped-прогон одновременно порождает и якобы проверяет число. Для противоречия норме ОБХОД по регламенту не требуется.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:88 — rc-команда И-5 заканчивается `|| true`, поэтому всегда зелёная даже при найденном вызове `git commit|merge` без identity. Собственный прогон на `/tmp/critic016-canary/scripts/bypass.sh` с буквальной строкой `git commit -m 'без identity'` напечатал нарушение и завершился rc=0. Условие «канарейка ПУСТА» на :168 требует чтения stdout, а не доказано кодом возврата.
ОБХОД: оставить в `scripts/` вызов `git commit -m ...` без `-c user.*`/`GIT_*`; команда :88 печатает строку, возвращает 0, остальные scoped-проверки могут её не исполнять, и приёмка зелёная при нарушенном И-5.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:71 — И-1 проверяет только прямой агентский коммит на main (`case_kommit_mimo_merge`), но предмет на :58 требует именно merge-коммит ОРКЕСТРАТОРА. Ни И-1, ни И-5 не требуют identity merge-коммиттера: И-5 различает лишь наличие любой явной identity.
ОБХОД: `land_agent.sh` делает `git -c user.name=implementer merge --no-ff ...`. First-parent содержит merge, прямого коммита на main нет, канарейка identity молчит, все названные критерии могут быть зелёными, но приземляет не оркестратор.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:77 — И-3 требует расширить `check_contract_frozen`, однако зона implementer на :108 не включает ни `scripts/check_contract_frozen.sh`, ни `fixtures/check_contract_frozen/`. Исполнитель обязан либо выйти из зоны, либо молча не выполнить заявленное расширение.
ОБХОД: оставить текущий `check_contract_frozen.sh` без проверки `merge-base --is-ancestor` (такой проверки в нём сейчас нет), а в будущих `land_agent`-фикстурах не строить недостижимый frozen-тег. Оба названных scope могут стать зелёными, хотя frozen-тег на боковом коммите с тем же блобом останется недостижим из HEAD и И-3 не удерживается общей охраной.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:97 — И-9 утверждает «каждый агент коммитит `user.name=<роль>`», но его критерий на :101–103 проверяет только отсутствие дефолтного local `user.name` и внутренний git-вызов spawn без identity. Сам контракт на :29–30 сохраняет семантику «неизвестный автор → не судится»; известный остаточный риск `check_zones.sh:34–41` здесь не объявлен cognitive-only и не закрыт механизмом.
ОБХОД: агент коммитит `git -c user.name=owner -c user.email=x@x commit ...` либо новым именем. Local `user.name` остаётся пустым, case отсутствующей identity не срабатывает, staged-судья возвращает 0 «не судится», scoped-команды зелёные, но агент подписан не своей ролью и может провести путь вне зоны.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:90 — И-6 запрещает только МОЛЧАЛИВО трогать/удалять неслитые ветки, тогда как предмет на :64–66 требует список владельцу и отсутствие удаления до его решения. Сохранность неслитой ветки после GC отдельным наблюдаемым условием не закреплена.
ОБХОД: GC печатает каждую неслитую ветку в «СПИСОК владельцу», затем удаляет её. Удаление не молчаливое и буквальный И-6 выполнен; scoped-гейт, реализованный по этому тексту, может стать зелёным, но checkpoint бюджет-аборта потерян без решения владельца.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:112 — механическая `ЗОНА orchestrator: ... roles/` выдаёт весь каталог ролей, а :127–129 одновременно объявляет `РАБОТА НЕ РАЗДАЁТСЯ: уставный класс` и утверждает, что зоны 016 этот путь никому не дают. `check_zones.sh` читает именно `ЗОНА`-строку, не последующее отрицание; граница исполнения противоречива.
ОБХОД: после заморозки закончить всю кодовую реализацию, не меняя `roles/`; все приёмочные команды зелёные, но замороженный блоб уже механически авторизует orchestrator на весь `roles/`, то есть заявленная граница «не раздаётся» не выполнена.

## Советы и неблокирующие результаты обход-анализа

СОВЕТ contracts/016-izoljacija-agenta.md:39 — ограничения И-2 (`--no-verify`, `git commit -- <пути>`, неустановленный hook и старый worktree) названы как cognitive-only остаток; при принятом владельцем уровне Q1 это честное сужение, а не молчаливое обещание жёсткого гейта.

СОВЕТ contracts/016-izoljacija-agenta.md:79 — для И-4 названо наблюдаемое отсутствие `wip/*` после land и отдельный GC-scope; отдельного проходящего состояния, где обе проверки честно зелёные, а ветка сохранилась, из текста не следует.

СОВЕТ contracts/016-izoljacija-agenta.md:92 — И-7 и И-8 имеют поимённые входы `case_gryaznyj_main` и `case_priyomka_bez_vetki`. Формулировку «предмет в HEAD» полезно свести к наблюдаемому отношению HEAD к базе ветки, но самостоятельного обхода сверх уже заявленной проверки приёмки не предъявлено.

СОВЕТ contracts/016-izoljacija-agenta.md:115 — А-60 честная: каталоги фикстур срезов 2–4 разведены по ключам барьеров раннера. Расширения `lib_zones/check_zones`, `check_hooks/.githooks/package.json/ci.yml`, `.gitignore` и каталоги по ключам имеют названную механическую причину; лишнего доверия сверх предмета не найдено. Исключение — отсутствующая зона frozen-охраны, уже блокирует выше.

СОВЕТ contracts/016-izoljacija-agenta.md:197 — А-61 объявлена честно: якорь назван cognitive-only, отсутствие пятой проверки не скрыто, заведение механизма оставлено отдельному слову владельца. Не-цели 3*, omp §6.7, Н-60/49/50, зачистка и правка ролей перечислены; молчаливого включения этих работ в реализацию не требуется.

## Что гонял

Все три требуемых красных прогона выполнены своими руками последовательно на чистом живом дереве HEAD `f4b66de`:

1. `bash fixtures/check_staged/probe_check_staged_krasnyj.sh` → rc=1. Дословный итог: четыре строки `ok` (честная форма; зона-слеп; грамматика-слеп; всё-красно), затем `ОТКАЗ: барьера нет — /home/aka/Documents/dev-harness/scripts/check_staged.sh (реализация за implementer после заморозки)`.
2. `bash fixtures/check_hooks/probe_check_hooks_krasnyj.sh` → rc=1. Дословный итог: три строки `ok` (честная форма; хук-декой; установщик-декой), затем `ОТКАЗ: барьера нет — /home/aka/Documents/dev-harness/scripts/check_hooks.sh (реализация за implementer после заморозки)`.
3. `bash scripts/verify_antiplacebo.sh . --scope check_charter` → rc=1. Ровно две строки отказа: `FAIL fixtures/check_hooks: фикстура без барьера — она ничего не держит` и `FAIL fixtures/check_staged: фикстура без барьера — она ничего не держит`; `check_charter` предъявил 7/7 case, итог `расхождений: 2`.
4. `git rev-parse HEAD && git status --porcelain=v1` → rc=0; SHA `f4b66de2b5fbd1ccf8c113afa2da2631503f01ec`, status пуст.
5. `git ls-files --error-unmatch contracts/016-izoljacija-agenta.md && git ls-files --error-unmatch tmp/frontier-068.md` → rc=1: контракт отслеживается, `tmp/frontier-068.md` не известен git.
6. Канарейка И-5 дословно на `/tmp/critic016-canary` с `scripts/bypass.sh` → rc=0 и stdout `scripts/bypass.sh:2:git commit -m 'без identity'`.
7. `git config --local user.name` → rc=1 и пустой stdout: желаемое отсутствие имени сейчас есть, но оно не доказывает соответствие имени агента роли.

Расхождения трёх обязательных красных прогонов с ожиданиями контракта нет.

## Таймлайн

- фиксация HEAD/чистоты и отдельный чистый клон — начало суда;
- чтение `AGENTS.md`, контракта, committed-состава, frontier §1–§6 и А-60/А-61;
- три обязательных красных прогона — 4.11 с + 4.21 с + 11.93 с;
- обход-анализ И-1…И-9, зон, не-целей и незаполненных требований; отдельное воспроизведение обхода И-5;
- запись и проверка вердикта — `2026-08-29T23:50:34+02:00` и далее.

## Вопросы владельцу

Нет. Находки требуют инженерно проверяемой правки контракта/границ, а не решения о цене или воле.
