FAIL

БЛОКИРУЕТ contracts/002-approval-mode-steward.md:54-55 — зоны синтаксически читаются, но в заявленном состоянии заморозки барьер уже красный. На локальном теге `frozen/contracts/002/1` команда `bash scripts/check_zones.sh` распечатала обе зоны, затем вернула 1: новая зона `architect` была применена к диапазону от заморозки контракта 001 и отвергла прежние коммиты, включая `c3c88d73 contracts/002-approval-mode-steward.md`, `feac8742 package.json` и `.github/workflows/ci.yml`. `scripts/check_zones.sh` при этом не входит ни в одну зону. Контракт не даёт исполнителю ни шага, ни права привести обязательный CI-барьер в зелёное состояние.
ОБХОД: заморозить контракт с этими строками и выполнить заявленный предмет только в названных путях. Команды раздела «Приёмка» не включают `npm run check:zones` и могут быть зелёными, тогда как обязательный шаг CI `npm run check:zones` остаётся красным на истории до этого контракта; объявленные границы фактически непригодны для первой раздачи.

БЛОКИРУЕТ contracts/002-approval-mode-steward.md:62-63 — контракт прямо поручает последующие правки `package.json` и `config/` «владельцу задания», но не называет его git-автора и не даёт этим путям зоны. Для проводки нового постоянного барьера правило 6 также требует шага в `.github/workflows/ci.yml`, которого нет ни в одной зоне. Это молчаливое расширение исполнения после заморозки, а не полное объявление границ.
ОБХОД: автор с именем, не объявленным в строках `ЗОНА`, коммитит `package.json`, `config/ci_parity_exceptions.txt` и/или `.github/workflows/ci.yml` на приёме пачки. `check_zones.sh` по своей объявленной области не проверяет необъявленных авторов, поэтому проводка проходит вне обеих зон, хотя критерий и CI могут стать зелёными.

БЛОКИРУЕТ contracts/002-approval-mode-steward.md:33-36 — команда для реестра доказывает только грамматику и заполненность полей. Контракт не фиксирует личности семи решений и не требует от барьера проверять, что ссылка разрешается в коммит-источник и что запись действительно извлечена из него. Счёт «семь» и содержательная полнота проверяются более слабой мерой.
ОБХОД: положить в `decisions/` семь синтаксически правильных записей с произвольными вопросами и решениями, а поле `основание` каждой заполнить одной и той же строкой, похожей на hash. По объявленному контракту `scripts/check_decisions.sh` возвращает 0 за грамматику и полноту полей, хотя семь решений владельца из прозы не извлечены.

БЛОКИРУЕТ contracts/002-approval-mode-steward.md:37-41 — для steward проверяются только отсутствующее основание и ссылка на несуществующую запись. Не задано, как доказать применимость прецедента к вопросу, как разрешать конфликт двух действующих записей и как учитывать `область` и `условие пересмотра`. Эти решения молча оставлены исполнителю, а критерий отличает «ссылка существует» от «ссылки нет», но не ответ по прецеденту от произвольного ответа.
ОБХОД: steward на любой вопрос отвечает ссылкой на одну существующую, но не относящуюся к вопросу запись. Ответ проходит обе названные отрицательные проверки — ссылка существует и основание присутствует, — хотя решение владельца по соответствующей области не применено.

СОВЕТ contracts/002-approval-mode-steward.md:67-74 — фраза «каждая обязана иметь пункт в CI» включает интерактивный `bash workshop <проект>`, тогда как действующее `config/ci_parity_exceptions.txt` обоснованно исключает `workshop` из CI как зависающий интерактивный вход. Отделить CI-команды от обязательного живого прогона, чтобы приёмка не обещала невозможную проводку.

Проверка зон: временный тег создан только в клоне и удалён после прогона. Наблюдаемый итог — `замороженных контрактов: 2 · объявленных авторов: 2 · коммитов в диапазонах: 12 · проверено по зонам: 10`, код возврата 1.
