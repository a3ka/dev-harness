accept

# Контракт 046 — критик, узкий круг 2

Предмет: `contracts/046-germetichnoe-okruzhenie-check-protected.md` на
`d5e9fa81f3ac731fea83ae7ba2e00d551e0c1905`, блоб
`2fa48a2944162c6f2a61863b6743b28178b6578f`.
Проверено закрытие Б1/Б2/Б3 и совета С1 из
`verdicts/critic/contracts-046-v1.md`, а также изменение Р5/Р10.
Новый полный суд контракта и суд реализации не проводились.

Набор артефактов полон: предмет закоммичен, приёмочные команды с rc есть;
исполнитель architect и границы названы, ЗОНА-строка — строка 144.
Прочитаны вердикт первого круга целиком, `AGENTS.md` и вся секция приёмки
нынешнего контракта (строки 193–263). Новых блокирующих находок и советов нет.

## Б1 — закрыта

Место исправления: `contracts/046-germetichnoe-okruzhenie-check-protected.md:214-215`.
Живые дословные команды в чистом SSH-клоне на указанном коммите:

```sh
bash scripts/verify_antiplacebo.sh --scope check_protected/case_ten_funkcii_comm
bash scripts/verify_antiplacebo.sh --scope check_protected/case_ten_funkcii_unset_comm
```

Обе дали **rc=0**, каждая — одну фикстуру и один красный повторный прогон.
Причины соответственно:
`существовал и на HEAD его нет: plans/001-p.md` и
`существовал и на HEAD его нет: verdicts/adversary/v-a.md`.
Поиск `--scope\s+[^`\s]*\.sh` по закоммиченному контракту не нашёл совпадений:
расширение убрано из ключей команд, упоминание прежней ошибки в пояснении
не является ключом. Отказ scope_select больше не подменяет предъявление.

## Б2 — закрыта

Место исправления: `contracts/046-germetichnoe-okruzhenie-check-protected.md:249-250`
(Р14/Р15).

`bash fixtures/check_protected/probe_mutant_bez_posix_rezhima.sh` дала **rc=0**:

```text
честный + тени exec,comm : rc=1 причина_напечатана=1
мутант  + тени exec,comm : rc=0 причина_напечатана=0
мутант  без теней        : rc=1 причина_напечатана=1
```

Независимая временная мутация в disposable-клоне удалила только строку
`  POSIXLY_CORRECT=1` из `scripts/check_protected.sh`. На этом дереве
`bash scripts/verify_antiplacebo.sh --scope check_protected` дала **rc=1**:

```text
FAIL check_protected/case_ten_funkcii_exec_comm.sh: барьер остался зелёным на обманном дереве — красное не предъявлено
барьеров: 1 · фикстур: 28 · предъявлено красным повторным прогоном: 27
```

Прочие 27 фикстур предъявили красное. Р15 на том же мутанте дала **rc=1**
с причиной «мутация не применилась» и требованием инварианта 4.
Следовательно, старый обход теперь не удовлетворяет приёмке: его ловит
не только отдельная проба, но и постоянная семья.

## Б3 — закрыта

Место исправления: `contracts/046-germetichnoe-okruzhenie-check-protected.md:251`
(Р16).

`bash fixtures/check_protected/probe_mera_mertva_predela.sh` дала **rc=0**.
Дополнительный запуск с `bash -x` предъявил содержимое захваченного вывода:

- Пустая трасса: два запуска настоящего субъекта, **rc стража=1**,
  `мера мертва: git-вызовов под трассировкой LOW=0 HIGH=0`.
- Синтетическая трасса: два запуска субъекта, **rc стража=0**,
  `LOW(M=60)=12, HIGH(M=360)=12` и `ПРЕДЕЛ ДЕРЖИТСЯ: LOW=12<=30`.

Затем только в disposable-клоне удалены две исполняемые строки проверки
`CALLS_LOW>0 && CALLS_HIGH>0` и её именованного отказа из стража
`red_predel_git_vyzovov_ours.sh`; остальной страж и барьер не менялись.
Та же команда Р16 дала **rc=1**:

```text
мёртвая трасса: запусков субъекта=2 rc=0
ОТКАЗ: страж на мёртвой мере вернул rc=0 вместо 1 — fail-closed живости отсутствует
```

В выводе мутированного стража действительно присутствует ложное
`ПРЕДЕЛ ДЕРЖИТСЯ: LOW=0<=30`, при LOW=0/HIGH=0. То есть старый обход
воспроизведён, а исполнимая приёмка его отвергла.
Это проверка живости канала, **не** измерение реального call-budget через strace
и **не** утверждение о выполненном CI.

## С1 и изменение Р5/Р10 — закрыты без новой неисполнимости

Места: `contracts/046-germetichnoe-okruzhenie-check-protected.md:223-234`.
Независимая команда `git rev-list --count 5baf803` дала **2322**, как теперь
указано в Р5. Счётчики обхода явно исключены из текущего критерия; числа
2322/477/437 и 2123/1374 относятся именно к замеру на `5baf803`.
Это не требование удержать исторический счёт после новых коммитов.

Живые Р5 и Р10 в клоне дали **rc=0** каждая. Р5: текущий счёт
2332/478/438, но обязательная строка решения сохранена:
`исчезло: 0 · с разрешения: 30 · перенесено: 10`.
Р10: 2122/1370; приёмку несёт rc, не это зависящее от истории и тегов число.

## Дополнительная обязательная проверка

- Честный `bash scripts/verify_antiplacebo.sh --scope check_protected`:
  **rc=0**, `барьеров: 1 · фикстур: 28 · предъявлено красным повторным прогоном: 28`.
  Счёт подкреплён именованными результатами всех 28 случаев; мутационная
  дифференциальная проверка выше исключила ложное 28/28 на старом обходе.
- Обязательный живой precision-гейт из корня клона:
  `bash scripts/check_precision_gate.sh . contracts/046-germetichnoe-okruzhenie-check-protected.md`
  — **rc=0**, именованный пропуск задачи (б) для изменяемого этим же черновиком
  барьера, затем `OK`.
- Команда Р6 с id-базлайном `90419deeca0849430da448c640ce30a0f80ed78b`
  — **rc=0**, существующие case-файлы не изменены/не удалены.

Один первоначальный запуск семьи был недостоверен: критик параллельно запустил
Р5/Р10 в том же клоне, и раннер закономерно отказал из-за файлов их scratch
`tmp/protected.*`/`tmp/zones.*` (несмотря на 28/28 случаев). Этот запуск
не засчитан. Приведённый выше rc=0 получен отдельным последовательным прогоном
без соседних писателей. Мутационные прогоны тоже исполнены последовательно.

Пять вопросов гейта в границе этого круга: слабость критерия Б2/Б3 устранена
исполняемыми контрпримерами; неисполнимость Б1 устранена рабочими ключами;
новых решений, молча переданных исполнителю, не появилось; добавленные файлы
остаются в объявленной зоне architect `fixtures/check_protected/`; прежнее
противоречие норме о честной/обманной стороне приёмки больше не воспроизводится.
Повторного FAIL нет, основания для арбитража нет. Заморозка разрешена.

Все мутации — в уникальном SSH-клоне
`tmp/critic/046-r2-d5e9fa8-20260925`, исходные байты восстановлены.
Основной код и контракт критиком не правились. Пробник и сырые логи оставлены
в `/tmp/dev-harness-verify/scratch/critic046r2/`; истинный результат гейта —
этот закоммиченный вердикт, а не доступность временного каталога.
