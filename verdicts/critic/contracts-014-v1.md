FAIL

РЕШЕНИЕ арбитража `verdicts/arbitration/kontrakt-014-koren-tmp.md` исполнено буквально по вопросам 1–3, но новый проверенный обход оставляет критерий слабее предмета.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:24-27,104-109; fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh:201-202,240-241 — предмет требует при недоступной запасной базе отказаться **ДО создания чего-либо**, а ветви 9–10 проверяют чистоту только после завершения раннера. Созданный внутри стерегомого корня путь, удалённый перед именованным отказом, не наблюдается ни пробой, ни остальными командами приёмки.
ОБХОД: `/tmp/dev-harness-critic/sim14/transient-create` отличается от честной симуляции одной строкой непосредственно перед именованным отказом: `: > "$_croot/.probe014-transient"; rm -f "$_croot/.probe014-transient"`. Кандидат создаёт путь внутри корня до отказа и тем нарушает инвариант 2, но затем удаляет его. На побайтово той же пробе он получил rc=0: зелены 10/10 ветвей и 12/12 печатных наблюдений, включая «подставной /tmp чист» в ветви 9 и чистый корень в ветви 10. Полный набор названных локальных команд тоже зелён: мета-цикл на сокращённом дереве rc=0, 23/23 фикстуры предъявлены красным повторным прогоном; `bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` в полном кандидатном дереве rc=0, 40/40. Значит объявленный критерий выполнен, а обязательное «ДО создания чего-либо» — нет.

## Буквальная сверка РЕШЕНИЯ

- **Q1, механическое предъявление:** закрыто. Ветвь 9 `корень-tmp` использует `unshare --map-root-user --mount` и приватный bind подставного каталога на `/tmp` (`probe_tmpdir_raven_koren.sh:187-194`); проверяет rc=2, именованный текст, отсутствие общего текста стража и итоговую чистоту подставного `/tmp` (`:195-206`). Ветвь 10 использует userns + chroot, `/tmp`-симлинк внутрь `/work/root` и rbind `/usr`/`/proc` (`:221-233`); проверяет тот же именованный отказ и итоговую чистоту корня (`:234-245`).
- **Q2, канонический `/tmp` и наблюдаемая граница литерала:** закрыто. Контракт называет канонизацию той же мерой и формулирует различие наблюдаемым именованным отказом (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:22-33`). Стаб `literal-fallback` красен только на ветви 10.
- **Q3, обязательные наблюдения:** rc=2, точный именованный текст, запрет общего текста и итоговая чистота есть в обеих новых ветвях; недоступность userns красна именованно и fail-closed (`probe_tmpdir_raven_koren.sh:210-213,247-248`). Корень `/` и иные предки `/tmp` оставлены прозой как представители того же класса (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:114-120`). Не-цель больше не утверждает «непостроимость».
- SHA-256 пробы на HEAD и в `honest`, `nfg`, `lf`, `transient-create` совпал побайтово: `91d0ddb9a454866a3641fb7cbf8abcbcb2acd039312958bb557b909bbc4aaf2a`.

## Прогоны судьи на закоммиченном HEAD `c0818095ef62b582bd8301b5c1b12fbfca3260ed`

- `bash fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh` → rc=1, расхождений 10. Из 10 поимённых ветвей красны по смыслу: `н63-зелёный`; `детерминизм` (два наблюдения); `вложенный-tmpdir`; `симлинк-внутрь`; `алиасы-хеша` (два наблюдения); `без-mkdir`; `корень-tmp`; `tmp-симлинк-внутрь-корня`. Зелены `охрана-011` и `внешний-tmpdir`. Обе новые ветви красны общим отказом стража вместо именованного отказа инварианта 2; userns на судной машине доступен.
- `/tmp/dev-harness-architect/sim14/honest`: проба → rc=0, зелены 10/10 ветвей и 12/12 печатных наблюдений. Ветвь 9 вернула rc=2 с именованным отказом и чистым подставным `/tmp`; ветвь 10 — rc=2 с тем же отказом и канонической запасной базой.
- `/tmp/dev-harness-architect/sim14/nfg`: проба → rc=1, расхождений 2; красны ровно ветви 9 `корень-tmp` и 10 `tmp-симлинк-внутрь-корня`, остальные 8/10 ветвей зелены.
- `/tmp/dev-harness-architect/sim14/lf`: проба → rc=1, расхождение 1; красна ровно ветвь 10, ветвь 9 и остальные 9/10 ветвей зелены.
- Новый кандидат `/tmp/dev-harness-critic/sim14/transient-create`: проба → rc=0, 10/10 ветвей; мета-цикл → rc=0, 23/23; scoped `check_runner_hygiene` в `/tmp/dev-harness-critic/sim14/transient-full` → rc=0, 40/40. Это блокирующий обход выше.
- `bash scripts/check_contract_frozen.sh` → rc=0: документов 17, черновиков 4, заморожено 13; контракт 014 распознан черновиком, все замороженные блобы совпали.
- Поиск regex `уникальный mktemp|симуляц` в контракте → 0 совпадений: Н-39-чистота соблюдена.
- Тегов `frozen/contracts/014/*` нет, поэтому по грамматике имени перезаписан `verdicts/critic/contracts-014-v1.md`, новый `v2` не создан.

## Пять обязательных вопросов

1. Критерий слабее предмета: да — на проверенном `transient-create`, который создаёт и удаляет путь до именованного отказа.
2. Недоказуемое названными командами «готово»: ровно временное создание до отказа; все названные локальные команды обход проходит.
3. Решения Q1–Q3 исполнителю молча не оставлены: рецепты, диагноз, каноническая запасная база, fail-closed и граница класса названы. Новый дефект — наблюдаемость порядка эффекта, а не оставленная политика.
4. Границы исполнения названы `ЗОНА`-строками; контракт и проба принадлежат `architect`, реализация после заморозки — `implementer`, вердикт — `critic`.
5. `AGENTS.md:121-124` требует, чтобы команда краснела на обманном дереве. `transient-create` проходит весь локальный критерий, поэтому для фразы «ДО создания чего-либо» эта норма пока не выполнена.

Советов нет.
