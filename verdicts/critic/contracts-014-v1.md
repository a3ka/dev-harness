FAIL

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:13-25,49-53,81-86 — обе приёмочные команды запускают раннер только с лексическим `TMPDIR`, РАВНЫМ корню. Они не отличают полный инвариант «равен ИЛИ лежит внутри после каноникализации» от частного сравнения строк.
ОБХОД: реализация `if [ "$TMPDIR" = "$ROOT" ]; then base=/tmp; else base=$TMPDIR; fi` с неизменным последующим стражем делает зелёными пробу и все 23 мета-фикстуры: во всех их вложенных запусках `TMPDIR=$WORK`, а корень тоже `$WORK`. Явная охрана 011 и scoped-гигиена также остаются зелёными. При `TMPDIR=$ROOT/podkatalog` либо при внешнем имени-симлинке, канонически ведущем внутрь корня, такая реализация сохраняет нынешний отказ код 2 и предмет не сделан.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:22-26,81-86 — критерий не предъявляет ветвь «TMPDIR канонически вне корня остаётся базой» и не предъявляет hash8 через два разных имени одного канонического корня.
ОБХОД: реализация всегда выбирает `/tmp/verify-antiplacebo-<hash8>` при пустой явной переменной. Она даёт ожидаемый `/tmp` в пробе, детерминированный живой lock в обоих прогонах, зелёный мета-цикл и не затрагивает scoped-гигиену, но игнорирует внешний TMPDIR вопреки инварианту 3. Независимо от этого hash8 можно считать от лексического аргумента корня: все названные команды используют корни без симлинк-алиаса и пройдут, а два алиаса одного канонического корня дадут разные пути вопреки инварианту 2.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:18-21,30-31,81-86 — критерий не наблюдает каноникализацию отсутствующей базы и не доказывает запрет `mkdir` до проверки.
ОБХОД: реализация сначала делает `mkdir -p "$TMPDIR"` только для отсутствующего TMPDIR, затем каноникализирует и правильно выбирает запасную базу. В пробе и мета-цикле TMPDIR уже является существующим корнем, поэтому все команды станут зелёными; на непредъявленном `TMPDIR=$ROOT/novyj` реализация сначала загрязнит стерегомое дерево, то есть нарушит инвариант 5.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:13-21 — безусловный запасной `/tmp` противоречит безусловному требованию пути вне корня для допустимого корня `/tmp`; критерий использует только корни, являющиеся потомками `/tmp`, и этой границы не предъявляет.
ОБХОД: буквальная реализация запасной базы `/tmp` проходит пробу и мета-цикл на их корнях `/tmp/probe014.*` и `/tmp/meta014.*`, но при запуске раннера на корне `/tmp` создаёт `/tmp/verify-antiplacebo-<hash8>` внутри стерегомого корня. Контракт не называет ни альтернативную базу, ни именованный отказ для этого случая.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:60-63 — текст контракта сам утверждает запрещённую нормой Н-39 пару «симуляция уникального mktemp красна ровно ветвью детерминизм». Это прямое противоречие `AGENTS.md` («контракту ЗАПРЕЩЕНО утверждать пару обманный стаб ↔ ветвь»); для противоречия норме обход не требуется.

## Прогоны судьи на HEAD 02a78dc

- `bash fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh` → rc=1, расхождений 3: `н63-зелёный` получил rc=2; `охрана-011` зелёная с ожидаемым rc=2 и именованным отказом; `детерминизм` получил rc=2/2 и `seen=0/0`.
- Прямой вложенный запуск `case_tmpdir_raven_koren.sh` на `WORK=TMPDIR=<честный fake_root>` с `BARRIER=scripts/verify_antiplacebo.sh` → rc=2, «явный scratch внутри стерегомого дерева».
- Мета-цикл из критерия на сокращённом дереве → rc=1: барьеров 1, фикстур 23, предъявлено красным повторным прогоном 0, расхождений 23; все 23 строки — «нечем проверить» код 2, включая `case_tmpdir_raven_koren.sh`.
- `bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` → rc=0: барьеров 1, фикстур 40, предъявлено красным повторным прогоном 40.
- `bash scripts/check_contract_frozen.sh` → rc=0: документов 17, черновиков 4, заморожено 13; контракт 014 распознан черновиком.
- Аудит Н-39 поиском `стаб|обман|симуляц|заглуш` нашёл совпадения контракта на строках 49, 60, 61 и 62; запрещённая привязка находится на строках 60–63.

## Именной анализ обходов

- ПОКРЫТО: простой молчаливый rc=0 вместо работы не проходит — ветвь детерминизма требует живой lock по предсказанному пути, а мета-цикл требует 23 предъявления.
- ПОКРЫТО: потеря детерминизма через уникальный mktemp в сценарии `TMPDIR=ROOT` не проходит — проба наблюдает один предсказанный путь в двух запусках.
- ПОКРЫТО: ослабление обычного явного in-tree отказа не проходит — `охрана-011` требует rc=2 и именованный текст; scoped-гигиена предъявила 40/40, включая `scratchexpl` и `tocou`.
- НЕ ПОКРЫТО: вложенный и симлинк-канонизируемый внутрь TMPDIR — первый блокер.
- НЕ ПОКРЫТО: сохранение внешнего TMPDIR и hash8 канонического, а не лексического корня — второй блокер.
- НЕ ПОКРЫТО: отсутствие создания при каноникализации несуществующей базы — третий блокер.
- НЕ ПОКРЫТО: корень, равный `/tmp` или содержащий `/tmp`, — четвёртый блокер.
