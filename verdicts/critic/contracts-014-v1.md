FAIL

АРБИТР: `arbiter` — созван по второму `FAIL` одной причины после круга 3. Предмет спора: достаточно ли стартового `MON-READY` для требования fail-closed на всём интервале вызова, если раннер может прекратить наблюдателя после handshake, создать и удалить путь, а проба всё равно печатает оба транс-наблюдения зелёными.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:24-32; fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh:119-153 — предмет требует наблюдать создание/перенос за ВЕСЬ вызов и fail-closed при недоступности наблюдателя, но проба проверяет доступность процесса только до `MON-READY`. После раннера `mon_stop` безусловно посылает сигнал, игнорирует результат и код завершения, а `mon_check` знает лишь сохранённый rc старта и отсутствие напечатанного `MON-EVENT`; смерть наблюдателя между handshake и событием принимается за отсутствие события.
ОБХОД: собственный стаб `/tmp/dev-harness-critic/sim14/observer-kill` на специальных входах 9–10 выполняет `pkill -f '[m]on014.py'`, затем создаёт и удаляет `$ROOT/.probe014-observer-kill` и возвращает rc=2 с точным именованным отказом. На побайтово той же пробе (`sha256 c2b1f497…`) команда завершилась rc=0: зелены все 14/14 печатных наблюдений, включая оба транс-наблюдения «ядро не доставило ни одного события». Критерий выполнен, но инвариант 2 нарушен созданием пути во время вызова, а недоступность наблюдателя принята открыто, не fail-closed.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:93-96 — контракт снова утверждает пару «стаб `transient-create` ↔ оба транс-наблюдения» и результат «честной симуляции 14/14». Это прямо противоречит старшей норме `AGENTS.md:121-127`, которая запрещает контракту утверждать пару «обманный стаб ↔ ветвь» и требует оставлять привязку тому, у кого перед глазами код. Для противоречия `AGENTS.md` отдельный обход по правилу роли не требуется. Поиск regex `уникальный mktemp|симуляц` не пуст: совпадение `симуляции` на строке 95.

## Матрица судьи на закоммиченном HEAD `376025f`

Все прогоны использовали `bash fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh`; userns и оба наблюдателя доступны.

- HEAD → rc=1, расхождений 10: красны поимённо `н63-зелёный`; `детерминизм` ×2; `вложенный-tmpdir`; `симлинк-внутрь`; `алиасы-хеша` ×2; `без-mkdir`; `корень-tmp`; `tmp-симлинк-внутрь-корня`. Зелены `охрана-011`, `внешний-tmpdir` и ОБА транс-наблюдения — корректное сейчас-зелёное, текущий раннер до отказа путей не создаёт.
- `/tmp/dev-harness-architect/sim14/honest` → rc=0, 14/14 печатных наблюдений зелены.
- `/tmp/dev-harness-critic/sim14/transient-create` → rc=1, ровно 2 расхождения: `корень-tmp-транс` и `tmp-симлинк-транс`, в обоих назван путь `.probe014-transient`; остальные 12/14 зелены.
- `/tmp/dev-harness-architect/sim14/nfg` → rc=1, ровно ветви 9 `корень-tmp` и 10 `tmp-симлинк-внутрь-корня` красны; 12/14 печатных наблюдений зелены.
- `/tmp/dev-harness-architect/sim14/lf` → rc=1, ровно ветвь 10 `tmp-симлинк-внутрь-корня` красна; 13/14 печатных наблюдений зелены.

## Собственные кандидаты против транс-наблюдения

Проба во всех кандидатах побайтово совпадает с HEAD: SHA-256 `c2b1f4975c2a4e9866107a65b5876c7f5dc80063ac42e3345005f9e4091a10d1`.

- `/tmp/dev-harness-critic/sim14/postprint-race`: точный именованный отказ печатается ДО немедленных create+rm и exit. Проба → rc=1, ровно оба транс-наблюдения красны и называют `.probe014-postprint-race`; простой хвостовой race после печати не прошёл.
- `/tmp/dev-harness-critic/sim14/moved-to`: путь создаётся вне стерегомого корня, переносится внутрь и удаляется. Проба → rc=1, ровно оба транс-наблюдения красны: ветвь 9 получила `MON-EVENT создание путь .probe014-moved-to` (межфайловый перенос материализован созданием), ветвь 10 — `MON-EVENT перенос путь .probe014-moved-to`. Матрица не сузилась по `MOVED_TO`.
- `/tmp/dev-harness-critic/sim14/observer-kill`: наблюдатель прекращается после `MON-READY`, затем путь создаётся и удаляется. Проба → rc=0, 14/14; это блокирующий обход выше.
- Создание ДО старта монитора через запуск раннера не построено: обе специальные ветви запускают раннер только после возврата `mon_start`, который ждёт `MON-READY`. Создание через заранее существующую ссылку наружу не создаёт канонического объекта в стерегомом корне; создание самой ссылки во время вызова покрыто `IN_CREATE`. Отдельный overflow-стаб не нужен для порога после зелёного `observer-kill`; обработка `IN_Q_OVERFLOW` в коде наблюдателя присутствует, но не закрывает установленную потерю процесса после handshake.

## Остальные механические проверки

- `bash scripts/check_contract_frozen.sh` → rc=0: документов 17, черновиков 4, заморожено 13; контракт 014 распознан черновиком.
- Счёт контракта и проба синхронны: после дельты заявлено 14 печатных наблюдений, честная симуляция дала 14/14; прежние 12 не удалены, добавлены два транс-наблюдения.
- Тегов `frozen/contracts/014/*` нет; поэтому перезаписан `verdicts/critic/contracts-014-v1.md`, `v2.md` не создан.
- Предмет, критерий, исполнители и `ЗОНА`-строки присутствуют. Зоны покрывают контракт/пробу (`architect`), реализацию после заморозки (`implementer`) и этот вердикт (`critic`).

## Пять обязательных вопросов

1. Критерий слабее предмета: да — `observer-kill` выполняет весь критерий, создав и удалив путь при уже недоступном наблюдателе.
2. Недоказуемое названной командой «готово»: непрерывная доступность наблюдателя и fail-closed после `MON-READY` не доказаны; сохранён только rc старта.
3. Политика предмета исполнителю не оставлена: инвариант события во времени и fail-closed сформулированы. Дефект находится в механизме доказательства, а не в молчаливом выборе реализации.
4. Границы исполнения названы `ЗОНА`-строками и покрывают все затрагиваемые контрактом файлы.
5. Есть прямое противоречие `AGENTS.md:121-127`: запрещённая пара «стаб ↔ ветвь» возвращена в приёмочный раздел контракта.

Советов нет.
