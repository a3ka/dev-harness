accept

Закоммиченный предмет: `743d35b24762939530979188415e3af15e9c0e28`. Тегов `frozen/contracts/014/*` нет, поэтому по заданной грамматике перезаписан `contracts-014-v1.md`. Блокирующих находок в объявленной модели угроз нет.

## Последовательная матрица судьи (А-50)

Все шесть прогонов выполнены строго последовательно одной командой `bash fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh`; SHA-256 пробы во всех пяти стаб-деревьях и на HEAD одинаков: `ab55927d63edafca4b84820312aeafcaec24ff69b944dad125f54f9ac2aa5448`.

1. HEAD `743d35b` → rc=1, расхождений 10. Красны `н63-зелёный`, `детерминизм` ×2, `вложенный-tmpdir`, `симлинк-внутрь`, `алиасы-хеша` ×2, `без-mkdir`, `корень-tmp`, `tmp-симлинк-внутрь-корня`; обе транс-ветви зелены с доставленным терминальным маркером. Матрица исходного красного не сузилась.
2. `/tmp/dev-harness-architect/sim14/honest` → rc=0, 14/14 печатных наблюдений зелены; на обоих транс-входах терминальный маркер доставлен.
3. `/tmp/dev-harness-critic/sim14/observer-kill` → rc=1, ровно 2 расхождения: `корень-tmp-транс` и `tmp-симлинк-транс`; обе причины поимённо: `терминальный маркер .probe014-mark-m9|m10 не доставлен — наблюдатель мёртв или канал не доставляет … (fail-closed)`. Остальные 12 наблюдений зелены. Обход круга 4 закрыт механически на обоих входах.
4. `/tmp/dev-harness-critic/sim14/transient-create` → rc=1, ровно 2 расхождения: обе транс-ветви называют чужой `MON-EVENT создание путь .probe014-transient`; остальные 12 наблюдений зелены.
5. `/tmp/dev-harness-architect/sim14/nfg` → rc=1, ровно ветви 9 `корень-tmp` и 10 `tmp-симлинк-внутрь-корня` красны; обе транс-ветви с терминальным маркером зелены, остальные 12/14 наблюдений зелены.
6. `/tmp/dev-harness-architect/sim14/lf` → rc=1, ровно ветвь 10 `tmp-симлинк-внутрь-корня` красна; 13/14 наблюдений зелены.

## Собственные стаб-кандидаты

Проба в каждом дереве — тот же SHA-256 `ab55927d…`; прогоны также выполнены последовательно.

- `/tmp/dev-harness-critic-v5/sim14/preexit-move`: на входе 9 стаб создаёт путь снаружи, переносит его внутрь стерегомого корня и удаляет до именованного отказа; на входе 10 делает то же внутри одной ФС. Проба → rc=1, ровно 2 расхождения: ветвь 9 называет `MON-EVENT создание путь .probe014-preexit-move` (межфайловый перенос материализован созданием), ветвь 10 — `MON-EVENT перенос путь .probe014-preexit-move`. Остальные 12 наблюдений зелены. Срезание угла не прошло.
- `/tmp/dev-harness-critic-v5/sim14/living-child`: раннер оставляет фонового потомка; тот ждёт смерти PID родителя, затем создаёт и удаляет `.probe014-living-child`, то есть после возврата раннера, но перед терминальным маркером. Проба → rc=1, ровно 2 расхождения; обе транс-ветви называют `MON-EVENT создание путь .probe014-living-child`, остальные 12 наблюдений зелены. Живой потомок не открыл окно между возвратом раннера и маркером.
- `/tmp/dev-harness-critic-v5/sim14/marker-collision`: раннер намеренно создаёт и удаляет зарезервированный `.probe014-mark-m9|m10`, тем самым порождая поддельный ожидаемый `MON-EVENT`, затем проба создаёт настоящий маркер. Проба → rc=0, 14/14. С сохранением `PROBE014_KEEP=1` файлы `m9.events` и `m10.events` содержат по ДВЕ одинаковые строки `MON-EVENT создание путь .probe014-mark-m9|m10`. Это проходящий кандидат, но он прямо относится к исключённому владельцем классу «раннер, подделывающий MON-EVENT», поэтому за моделью угроз и не блокирует.

СОВЕТ fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh:160-194 — за моделью угроз: код проверяет наличие ожидаемого маркера и отсутствие отличающихся событий, но не проверяет обещанное контрактом «ровно один MON-EVENT»; две одинаковые строки проходят.
ОБХОД (за моделью угроз, поэтому не блокирует): стаб `marker-collision` выше создаёт и удаляет зарезервированный маркер сам; затем настоящий маркер пробы даёт вторую одинаковую строку, и проба завершается rc=0. Это именно объявленная не-цель «раннер, подделывающий MON-EVENT» (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:144-147`), не срезание угла.

## Буквальная сверка РЕШЕНИЯ арбитража 6069143

1. **Q1, терминальный маркер и порядок:** контракт требует событие после раннера и до остановки (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:33-40`). Проба создаёт маркер в `mon_check` (`fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh:160-175`), а на обоих входах вызывает `mon_check` до `mon_stop` (`:341-342`, `:385-386`). `observer-kill` доказывает недоставку красным на обоих входах.
2. **Q2, оба входа:** маркер предъявлен на ветви 9 и ветви 10 тем же общим механизмом (`probe_tmpdir_raven_koren.sh:341-342,385-386`); отдельные прогоны `observer-kill`, `transient-create`, `preexit-move` и `living-child` краснят ровно обе транс-ветви.
3. **Четыре поимённые причины `mon_check`:** rc старта ≠0, `MON-ERR`, любой чужой `MON-EVENT`, недоставка маркера перечислены и имеют отдельные красные сообщения (`probe_tmpdir_raven_koren.sh:160-194`). Терминальный маркер ждётся той же 50×0.05с петлёй, что и `MON-READY`.
4. **Q3, Н-39-рецидив:** запрещённая пара «стаб ↔ ветвь» и поисходные стаб-счёты удалены из контракта; `diff 6069143..743d35b` удаляет прежние строки про `transient-create` и `14/14`. Контракт оставляет инвариант и rc-команду (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:80-101`), а имена и привязка стабов находятся в шапке пробы (`probe_tmpdir_raven_koren.sh:55-59`), как предписано решением. Счётная матрица не потеряла ни одной прежней ветви — это отдельно подтверждено шестью прогонами выше.
5. **Модель угроз дословно:** фраза владельца находится в контракте (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:144-147`) и в шапке пробы (`probe_tmpdir_raven_koren.sh:64-67`): «раннер, подделывающий MON-EVENT, и охота за монитором не покрыты; уровень угроз = срезание углов, как везде в репо; расширение — отдельный контракт по воле владельца».

## Полнота и механические проверки

- Предмет и исполняемый критерий присутствуют; исполнители и границы названы `ЗОНА`-строками (`contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:66-71`).
- `bash scripts/check_contract_frozen.sh` на `743d35b` → rc=0: документов 17, черновиков 4, заморожено 13; контракт 014 распознан черновиком.
- `diff 6069143..743d35b` сверён: изменения предмета ограничены контрактом, пробой и архитектурным журналом; арбитражное решение проведено в контракт и общий механизм обоих входов.
- В объявленной cognitive-only модели угроз новых проходящих стабов не найдено. Единственный проходящий собственный кандидат намеренно подделывает ожидаемый `MON-EVENT` и потому записан советом «за моделью угроз», как прямо велел владелец.
