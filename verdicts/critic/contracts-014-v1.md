FAIL

АРБИТР: `arbiter` — созван через `Main` по повтору причины блокера 4 круга 1. Предмет спора: допустимо ли оставить инвариант именованного отказа при `корень ⊇ /tmp` только прозой как герметично непостроимый живой вход, когда приёмочная проба пропускает реализацию без специального отказа; дополнительно та же граница не различает каноникализацию запасного `/tmp` и литерал `/tmp`.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:24-28,90-98,102-108 — инвариант 2 требует при корне `/tmp`, `/` или ином предке `/tmp` отдельный именованный отказ код 2 «default scratch без запасной базы вне стерегомого дерева», но все команды приёмки исключают этот вход. Объявление входа герметично непостроимым объясняет остаток А-44, но не превращает требуемый диагноз в доказанное состояние: живая `охрана-011` доказывает лишь общий отказ in-tree scratch и допускает другой диагноз. Это прямой повтор блокера 4 круга 1.
ОБХОД: взять честную симуляцию и удалить только второй `case`, который после выбора `_base` сравнивает запасную базу с каноническим корнем и печатает требуемый именованный отказ. Все остальные инварианты и общий страж оставить. Собственный кандидат `/tmp/dev-harness-critic/sim14/no-fallback-guard` прошёл `bash fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh` с rc=0: зелены все 8 ветвей, 10/10 наблюдений. При `ROOT=/tmp` он выберет `/tmp/verify_antiplacebo-<hash8>`, после чего лишь общий страж вернёт код 2 «явный scratch внутри стерегомого дерева» вместо требуемого именованного отказа; критерий выполнен, инвариант 2 — нет.

БЛОКИРУЕТ contracts/014-default-skratch-vne-dereva-pri-tmpdir.md:17-23,90-98 — предмет требует канонизировать запасной системный `/tmp` той же мерой до выбора, однако приёмка не различает эту операцию от литерала `/tmp` на машине, где `/tmp` уже каноничен.
ОБХОД: в честной симуляции заменить только `_fb="$(cd /tmp ... && pwd -P ... || ...)"` на `_fb=/tmp`. Собственный кандидат `/tmp/dev-harness-critic/sim14/literal-fallback` прошёл ту же пробу с rc=0: зелены все 8 ветвей, 10/10 наблюдений. На системе, где `/tmp` — симлинк, такая реализация не выполняет предписанную каноникализацию до выбора; последующий общий страж может дать иной общий отказ, но названные команды на судимом дереве этого не отличают.

## Адресация пяти блокеров круга 1

1. **Лексическое сравнение вместо канонического отношения `TMPDIR` к корню — закрыт механически.** Архитекторский `s1` с частным лексическим выбором завершил пробу rc=1, расхождений 4: красны `вложенный-tmpdir`, `симлинк-внутрь`, второй прогон `алиасы-хеша` и `без-mkdir`; остальные наблюдения зелены. Тем самым непосредственно предъявлены вложенный и симлинк-разрешающийся внутрь входы первого обхода.
2. **Безусловный `/tmp` и hash8 лексического корня — оба обхода закрыты механически.** `s2a` завершился rc=1 с ровно 1 расхождением, `внешний-tmpdir`; `s2b` — rc=1 с ровно 1 расхождением, второй прогон `алиасы-хеша`. Остальные 9/10 наблюдений каждого кандидата зелены.
3. **`mkdir` отсутствующей базы до проверки — закрыт механически.** `s3` завершился rc=1 с ровно 1 расхождением: дополнительная проверка `без-mkdir` увидела материализованный `TMPDIR`; предшествующее наблюдение этой ветви и остальные 9 наблюдений зелены.
4. **Корень, равный `/tmp` или содержащий `/tmp`, — не закрыт механически.** Политика предмета теперь определена разумно: именованный отказ без введения запасных баз. Но живой вход исключён из всех команд, а собственный `no-fallback-guard` выше проходит пробу rc=0 без этой части предмета. Как не-цель допустима герметичная живая конструкция входа; как не-доказанный обязательный инвариант — нет.
5. **Запрещённая Н-39 пара — закрыта.** Поиск regex `уникальный mktemp|симуляц` в контракте дал 0 совпадений. Контракт оставляет инварианты, команды и ссылку на код пробы, не утверждая пару «стаб ↔ ветвь» прозой.

## Прогоны судьи на закоммиченном HEAD `0f192635126c9054ba1cdafc79c4dd296f5f7923`

- `bash fixtures/verify_antiplacebo/probe_tmpdir_raven_koren.sh` → rc=1, расхождений 8. Красны по смыслу: `н63-зелёный` (1), `детерминизм` (2 прогона), `вложенный-tmpdir` (1), `симлинк-внутрь` (1), `алиасы-хеша` (2 прогона), `без-mkdir` (1), все текущим отказом код 2. Зелены `охрана-011` с ожидаемыми кодом 2 и именованным общим отказом, а также `внешний-tmpdir` с rc=0 и живым scratch во внешней базе.
- Прямой вложенный репликат `case_tmpdir_raven_koren.sh` при `WORK=TMPDIR=<подставной корень>` и `BARRIER=scripts/verify_antiplacebo.sh` → rc=2, «явный scratch внутри стерегомого дерева».
- Архитекторский `/tmp/dev-harness-architect/sim14/honest` → проба rc=0, 8/8 ветвей и 10/10 наблюдений зелены.
- Архитекторские стабы: `s1` → rc=1, 4 расхождения; `s2a` → rc=1, 1; `s2b` → rc=1, 1; `s3` → rc=1, 1. Каждый красен только на перечисленных выше входах своего дефекта; хеш пробы во всех пяти деревьях совпал с HEAD: `fa7dc04c0aec980d3a95bd342697b0be84a158f558a9b6f078459fb929f3c231`.
- Собственные стабы из честной симуляции: `no-fallback-guard` → rc=0, 8/8 ветвей, 10/10 наблюдений; `literal-fallback` → rc=0, 8/8, 10/10. Оба проходят критерий без соответствующей части инвариантов 1–2.
- `bash scripts/check_contract_frozen.sh` → rc=0: документов 17, черновиков 4, заморожено 13; 014 распознан черновиком, замороженный 013 v2 совпал побайтово.

## Пять обязательных вопросов

1. Критерий слабее предмета: да, ровно на двух названных собственных обходах запасной базы.
2. Недоказуемое «готово»: да, каноникализация запасного `/tmp` и специальный именованный отказ инварианта 2 не доказаны ни одной командой.
3. Молчаливого выбора новой политики исполнителю не оставлено: отказ без `/var/tmp` определён; дефект — не решение, а отсутствие различающего входа.
4. Границы исполнения названы: все требуемые файлы покрыты `ЗОНА`-строками; implementer ограничен `scripts/verify_antiplacebo.sh` после заморозки.
5. Н-39 исправлена, но `AGENTS.md:121-124` требует для кодового контракта красную пробу против обманной реализации. Два собственных кандидата с rc=0 показывают, что это требование для инвариантов 1–2 пока не выполнено.

Советов нет.
