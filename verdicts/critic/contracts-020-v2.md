accept

# Контракт 020, заморозка v2 — суд только авторизованной дельты

Судимый HEAD: `f91b8d56e2cdffe71f66b131fd0a064df87e1344`.
Предмет: `contracts/020-shardirovanie-ci-antiplatsebo.md`, блоб
`bf8dc6825c02123c10e733415177590273a46625`; база `frozen/contracts/020/1`,
блоб `7311f32b69e59319ec91ee964ee7be41e3feca39`.
Исследование и проба выполнены только в одноразовом клоне
`/tmp/dev-harness-verify/critik-020-v2/repo`. Реализация параллельного implementer
не судилась; Р1–Р4, прежняя приёмка и окончательные решения арбитров не переоткрывались.

Набор артефактов полон: текст закоммичен; предмет — строки 59–81;
критерий и rc-команды — раздел «Приёмка», дополненный окончательной приёмкой
шага в `verdicts/arbitration/contracts-020-samtesty.md`; исполнители и границы —
строки 111–112. Прочитаны `AGENTS.md`, оба арбитража 020, действующие грамматики
`lib_zones.sh` и `verify_ci_parity.sh`.

Основание — прямая пред-авторизация владельца 2026-09-18 из задания:
«РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/020-shardirovanie-ci-antiplatsebo.md v2 — зона-дельта:
+config/ci_parity_exceptions.txt в зону implementer (арбитраж c8aaa67 предписал запись
команда:-исключения сам-тестов раннера); кроме строки зоны и записи команда: ничего
не меняется. Сверх (а) этой строки зоны и (б) записи команда: … = … c8aaa67 — НЕ расширять.»
В телах `01780c0` (architect) и `f91b8d5` (merge, orchestrator) независимо прочитана
строка `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` первой колонки с тем же разрешённым предметом.
Арбитраж `c8aaa67eb58cf939f106293ecfc4c1a32c89a8e6` имеет первую строку `РЕШЕНИЕ`.

## Пять предметов суда

### 1. Дельта ровно из двух изменений

`git diff --no-ext-diff frozen/contracts/020/1..main -- contracts/020-shardirovanie-ci-antiplatsebo.md`
вернул rc=0: два hunk, 5 добавленных строк и 1 удалённая. В первом hunk —
четыре строки записи под инвариантом 1 (89–92, включая указание файла), во втором —
замена строки зоны (111) добавлением единственного пути.

Дополнительно одноразовая Python-проба прочитала ОБА закоммиченных блоба через
`git show`, построила ожидаемый результат ровно этими двумя заменами и сравнила
байты целиком: равны, rc=0. Иных изменений текста, включая скрытые пробелы или
переносы в остальных местах, нет. Форматирование записи в Markdown — часть
разрешённой записи, не третья смысловая правка.

### 2. Коллизия с v1 и приоритет окончательного решения

СОВЕТ contracts/020-shardirovanie-ci-antiplatsebo.md:73–74,104–107 — при чтении
только страницы остаётся буквальное напряжение: старый запрет новых исключений
и «ТОЛЬКО шардный шаг» соседствуют с конкретной новой записью. Аналогичная старая
абсолютная формулировка есть в строках 61 и 69–72. Это не основание расширять
разрешённую дельту: запись прямо ссылается на `c8aaa67`, а п.2 этого окончательного
решения предписывает именно отдельный шаг сам-тестов в `ci` вне matrix и его оплату;
п.3 приёмки прямо объявляет эту статическую каноническую команду не красным флагом
residual. Владелец затем разрешил ровно эту запись и её зону, запретив прочие правки.

В этом суде применяется узкое исключение из окончательного решения, а не отмена
исключительности вообще: только названные сам-тесты на минимальном корне и ровно
названная команда. Ни `--changed`, ни скрытые запуски, ни иные исключения этим
не разрешены. Одной оплаченной записи без исполненного шага недостаточно:
приёмка арбитража требует зелёного честного исполнения и красного на саботаже,
видимом честному судье. Состояние «запись есть, сам-тесты не исполняются» этой
приёмке не удовлетворяет. Обхода, требующего правки сверх авторизации, не найдено;
`FAIL` или новый вопрос воли владельца из этой редакционной коллизии не следуют.
Совет не требует ответа, правки либо нового круга и не разрешает менять старые абзацы.

### 3. Форма записи

`contracts/020-shardirovanie-ci-antiplatsebo.md:89–92` задаёт единственную запись:

```
команда: npm run check:antiplacebo -- "$TMP_ROOT" = сам-тесты раннера анти-плацебо на минимальном mktemp-корне, аргумент — путь корня; оплата предписана арбитражем c8aaa67
```

Маркер точный; разделитель — первое ` = `; внутри команды `=` нет.
Команда побайтово совпадает с канонической командой в п.2 арбитража c8aaa67.
Имя `check:antiplacebo` статично; `$TMP_ROOT` находится в аргументе, а не в имени.
Markdown-переносы строки причины при внесении в построчный exceptions-файл
складываются в одну строку; переносить туда обратные кавычки или три физических
строки не предписано ни форматом файла, ни контрактом.

Одноразовая проба исполнила извлечённый без изменений Python-блок действующего
парсера исключений (`verify_ci_parity.sh:958–1015`) на этой однострочной записи:
rc=0, ровно одна TSV-запись вида `команда`, файл ошибок пуст. Причина содержит
13 слов по мере парсера и 100 буквенно-цифровых знаков: пороги ≥4 и ≥24 выполнены.
Это проверка синтаксиса записи, не заявление о зелёном полном `verify_ci_parity`
на ещё не приземлённой реализации.

### 4. Зона и исполнитель

`contracts/020-shardirovanie-ci-antiplatsebo.md:111`: `ЗОНА implementer:` —
литерал первой колонки. Четыре пути разделены пробелами, все ASCII, без глобов,
кавычек и аннотаций. Форма соответствует единственному читателю
`scripts/lib_zones.sh:204–239`.

Имя `implementer` совпадает с `%an` прочитанных коммитов реализации 020,
в частности `af6a8e7`, `eaf629e`, `3bd6cf6`, `6eee2a5`.
Добавленный файл — именно `config/ci_parity_exceptions.txt`, предписанный
арбитражем наряду с уже объявленными `ci.yml` и `verify_ci_parity.sh`.
Новых неназванных исполнителей или путей дельта не оставляет.
Границы держит `check_zones.sh` по автору и блобу высшей заморозки;
этот accept не подменяет сам тег v2.

### 5. Ретро-де-зонирование отсутствует

Проба сравнила множества путей implementer до и после: добавлен ровно
`config/ci_parity_exceptions.txt`, снято 0. Зона architect и весь остальной текст
побайтово неизменны. Класс 75f5ffe — снятие прежней зоны и исчезновение её суда —
данной дельтой не воспроизводится.

Сообщённый оркестратором прогон `check_zones.sh` с локальным тегом дал rc=0.
Повторно для подтверждения сообщённого результата не запускался; собственное
доказательство этого пункта — побайтовая дельта и разность множеств, не чужой
прогон, выданный за свой.

## Обязательные вопросы гейта

1. Критерий не ослаблен дельтой до «файл существует»: неизменённая приёмка и
   окончательная приёмка арбитража сохраняются; новая запись лишь оплачивает
   предписанную команду. Конкретного состояния, удовлетворяющего совокупной
   приёмке без выполнения предмета дельты, не найдено.
2. Новой недоказуемой обязанности вместо команды не добавлено: команда сам-тестов
   названа дословно, `verify_ci_parity.sh` с rc=0 уже назван в инварианте 1 и
   приёмке; исходы честного и саботированного исполнения закреплены арбитражем.
3. Существенное решение исполнителю молча не оставлено: место шага, минимальный
   корень, команда, оплата и запрет правки раннера/селектора определены c8aaa67.
   Привязку саботажа к наблюдаемой ветви этот вердикт не выдумывает (Н-39).
4. Исполнитель и весь добавленный путь названы; механизм границ существует.
5. Дельта не противоречит `AGENTS.md`: оплата явная, вывод результата — rc,
   граница меры причины названа; осмысленность предложения не объявлена
   автоматически доказанной. Текст меняется по прямой авторизации, до v2-freeze,
   с отдельным закоммиченным вердиктом. Правки за пределами двух разрешённых
   изменений не требуются.

## Стенограмма детектора и граница доказательств

Ниже — раскрытая в задании стенограмма оркестратора, не собственные измерения судьи:

- snapshot rc=0, 194 с, до пачки; check#1 rc=1 с `contracts/020-...` — легальный
  v2-land между снимком и сверкой, не утечка; снимок переснят;
- snapshot2 rc=0; check#2 rc=1 на
  `.zones/dev/.omp/profiles/dev/agent/cache/composer/413325046dbcc226/status.json` —
  Н-98, самотрип на composer-джобах живых субагентов;
- отказ раскрыт и благословлён владельцем 2026-09-18 до миграции Н-98;
  миграция — ручной шаг владельца после done×4; обходов детектора не было;
- CI пушенного `20c1d1c`: 6/6 success, `check_ci_gate` rc=0. Судимый `f91b8d5`
  ещё не пушен; заморозка v2 и push идут одним пакетом после accept.

Эти два rc=1 не превращены здесь в зелёный детектор. Результат CI `20c1d1c`
не перенесён на `f91b8d5`. Собственная проверка — только суд текста и описанная
проба дельты/грамматики (`python3 tmp/critic020_delta_probe.py`, rc=0);
полные suite, сам-тесты раннера, CI и реализация параллельной ветки не прогонялись
и этим вердиктом не принимаются. Весь одноразовый клон с пробой удаляется после
подтверждения коммита вердикта в основном репозитории.

Действительных блокирующих находок нет. Разрешена заморозка ровно судимого блоба
v2; совет выше едет дальше без условия его закрытия.

— critic, 2026-09-18
