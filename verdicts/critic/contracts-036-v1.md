FAIL

# Контракт 036 — критик, круг 1

Предмет: `contracts/036-spec-tochnost-prefriz-gejt.md` на HEAD `fce77c39dfac6a1531787030320b5bf7802bc663`. Суд выполнен в отдельном клоне `/tmp/dev-harness-verify/critic-036/repo`. Контракт, реализация и основной репозиторий не изменялись. Вердикт передаётся оркестратору для коммита с author=critic.

Минимальный набор для содержательного суда присутствует: закоммиченный предмет, приёмочный критерий с названными командами, исполнители и ЗОНА-строки. Отказ ниже — не формальный отказ за неполный набор, а три содержательные блокирующие находки. Проверены контрактные предмет, приёмка и границы исполнения; frontier-проза сама по себе заморозку не блокирует.

## Б1. Объявленное СПАСЕНО не спасает историю

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:83–84,153–160 — критерий г4б требует зелёного на `СПАСЕНО: <путь> — <причина>`, ошибочно называя это живой грамматикой 003-v3/031-v4. Настоящая грамматика — `СПАСЕНО <автор>: <полные 40-символьные хеши> — <причина>`. Это различие не косметическое: нынешний `check_zones.sh` не выводит исторический коммит из суда по объявлению 036.

ОБХОД: v1 разрешает implementer путь `scripts/foo.sh`; implementer законно коммитит этот файл; v2 заменяет разрешение на `scripts/bar.sh` и добавляет `СПАСЕНО: scripts/foo.sh — перенос в семейство Y`. Реализация, в точности удовлетворяющая г4б и В3 контракта 036, обязана пропустить этот черновик. После заморозки настоящий `check_zones` возвращает 1 за исторический коммит вне зоны. Причина owner-перезаморозки класса 031/v4 сохранена, хотя объявленный критерий 036 зелён.

Это измерено, а не выведено только чтением. Изолированный toy создан с настоящей git-историей и аннотированными тегами. Результат на грамматике 036:

```text
bash scripts/check_zones.sh <toy>
  FAIL коммит вне зоны: implementer d0caf715 scripts/foo.sh — зона автора (объединение всех замороженных): scripts/bar.sh
rc=1
```

Затем в следующей версии того же toy использована действующая грамматика с полным SHA `d0caf7154de8ad166012b6482a01825c7c4514af`:

```text
СПАСЕНО implementer: d0caf7154de8ad166012b6482a01825c7c4514af — перенос в семейство Y

bash scripts/check_zones.sh <toy>
  ok   контракт 001: коммит d0caf715 (implementer) — СПАСЕНО, из суда зон выведен
rc=0
```

Источники механики: `scripts/check_zones.sh:188–235,334–337`; историческое подтверждение — реальная строка `СПАСЕНО implementer: <три SHA> — …` в дельте frozen 031/3→031/4. Нового spec-гейта ещё нет: его зелёный исход здесь — обязательное следствие текста г4б, не приписанный несуществующему скрипту прогон.

Условие закрытия: приёмка переноса должна предъявлять не только разрешение нового гейта, но и сохранение законности исторических коммитов настоящим потребителем зон. Нужна одна согласованная грамматика и семантика, а не маркер, который понимает только предполёт.

## Б2. Компенсация пост-фриз красного противоречит действующей норме

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:139–179 — четыре будущих red-файла заменены утверждением о скретч-прогоне и прозаической таблицей «стаб ↔ ворота». `AGENTS.md:127–133` прямо требует предъявить кодовые тесты красными до круга критика и прямо запрещает контракту утверждать пары «обманный стаб ↔ ветвь». Строки 168–179 делают именно запрещённое. Основание блокирования — исключение порога для противоречия AGENTS; выдумывать дополнительный обход для этой находки не требуется.

На судимом HEAD все четыре названные команды действительно возвращают 127 — отсутствует файл, а не предъявлен именованный отказ предмета:

```text
bash fixtures/check_check_spec_ready/red_prichina_predmeta_036.sh → rc=127
bash fixtures/check_check_spec_ready/red_zamer_036.sh             → rc=127
bash fixtures/check_check_spec_ready/red_perenos_zony_036.sh      → rc=127
bash fixtures/freeze_contract/red_spec_preflight_036.sh           → rc=127
```

В теле контракта есть утверждение «все четыре rc 1 с именем», но нет воспроизводимого закоммиченного исходника этих скретч-проб и сырой стенограммы их ворот. Наблюдение А-192 объясняет организационную трудность, но не отменяет норму и не доказывает различающую силу будущих тестов. Сама строка `ПРЕДМЕТ 036 НЕ РЕАЛИЗОВАН` не доказывает выполнение г1–г5б.

Более того, обобщение А-192 о закрытости всех четырёх каталогов на предъявленном HEAD не воспроизводится. Собственный живой прогон `check_staged.sh` под identity architect, по одному новому staged-файлу:

| Каталог | rc | Наблюдение |
|---|---:|---|
| `fixtures/check_check_spec_ready/` | 1 | `ОТКАЗ: вне зоны` |
| `fixtures/freeze_contract/` | 0 | `staged в зоне автора architect` |
| `fixtures/check_check_contract_ready/` | 0 | `staged в зоне автора architect` |
| `fixtures/check_zones/` | 0 | `staged в зоне автора architect` |

Для четырёх конкретных red-путей 036 одновременно: до экспериментального тега три новых пути отвергнуты, а путь `fixtures/freeze_contract/red_spec_preflight_036.sh` уже разрешён; общий rc 1. При экспериментальном frozen/contracts/036/1 на неизменённом HEAD все четыре разрешены, rc 0. Тестовый тег после измерения удалён, staged-пробы сняты и удалены. Это эксперимент в клоне, не настоящая заморозка и не разрешение на работу.

Итак, узкое утверждение «черновик сам не открывает новую семью» подтверждено. Утверждение «до заморозки нет доступной поверхности для кодового предъявления» — нет. Конкретный исторический ворктри автора этим измерением не судится; результаты относятся к предъявленному HEAD.

Условие закрытия: предъявить воспроизводимое кодовое красное до повторного суда через законно доступную поверхность и вынести привязку стабов к наблюдаемым входам в код. Если избирается отступление от нормы red-first, требуется надлежащее решение об отступлении, а не молчаливая замена нормы контрактом.

## Б3. В2 проверяет согласие команды с константой, но не связь census с деревом

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:22–25,81–82,149–152 — предмет обещает закрыть 035-класс «число без связи с деревом», но объявленная приёмка требует лишь сравнить вывод произвольной команды с N. Независимого наблюдения census или иного различителя команды-константы нет. Это также касается правила 4 `AGENTS.md:216–217`: счётное утверждение проверяется другой мерой.

ОБХОД: автор объявляет census 999 строкой `замер: `printf '999\n'` = 999` при фактическом количестве 0. Реализация может честно пройти все г3/г3б/г3в/г3г и затем разрешить такую заморозку: команда существует, rc 0, последняя строка — целое 999, совпадающее с заявленным. Никакой связи с деревом не появилось; класс подделки 035 остаётся зелёным.

Малый прогон команды выполнен: `printf '999\n'` → stdout `999`, rc 0. Независимая проверка файлов на судимом HEAD дала 0 для трёх `fixtures/check_check_spec_ready/red_*.sh`, заявленных в строке 137. Зелёный исход будущего spec-гейта на таком входе — следствие его нормативно описанного алгоритма, не заявление о запуске отсутствующего гейта.

Не требуется универсально доказывать семантику произвольного shell-кода. Требуется до заморозки согласовать предмет с действительным различителем измеренного census-класса либо явно обозначить незакрытую связь «команда ↔ дерево» как cognitive-only и не объявлять её механически закрытой. В текущем тексте такой границы нет: остаточный риск на строках 200–202 описывает последующий дрейф, а не константную команду уже в момент заморозки.

## Проверка замеров 031 и А-192

Выполнены собственные git-прогоны, а не принято на веру наблюдение А-191:

```text
git for-each-ref --format='%(refname:short) %(objectname:short) %(contents)' refs/tags/frozen/contracts/031/
git diff frozen/contracts/031/1 frozen/contracts/031/2 -- contracts/031-strazh-semejstvo-mint-i-bazlajn.md
git diff frozen/contracts/031/2 frozen/contracts/031/3 -- contracts/031-strazh-semejstvo-mint-i-bazlajn.md
git diff frozen/contracts/031/3 frozen/contracts/031/4 -- contracts/031-strazh-semejstvo-mint-i-bazlajn.md
```

Все команды завершились rc 0. Четыре живых annotated-тега подтверждены: /1 `f66d4e6`, /2 `378b7d6`, /3 `d36ce3f`, /4 `43498ba` — здесь SHA объектов тегов.

Независимое извлечение четырёх контрактных блобов дало:

| Версия | Старый путь признания в check_staged | Новый путь в check_zones | Зонные пути пяти retake-case | Строк СПАСЕНО |
|---|---|---:|---|---:|
| v1 | да | нет | 5 | 0 |
| v2 | нет | да | 5 | 0 |
| v3 | нет | да | 5 | 0 |
| v4 | нет | да | 5 | 1 |

Из дельт: v2 исправляет назначение семейства признания и одновременно вычитает прежнее зонное разрешение; v3 исправляет недостижимую приёмку --retake; v4 добавляет ретро-признание трёх коммитов после вычитания зоны. Это дефекты исходной спецификации/её поправки, а не продемонстрированные случаи неизбежного дрейфа после корректной заморозки.

Для А-182-подобной проверки поведенческого утверждения извлечён и исполнен именно `scripts/check_judge_gate.sh` из frozen/contracts/031/1. Все три вызова с argv `--snapshot <корень>`, `--check <корень>`, `--retake <корень>` вернули rc 1 и `ОТКАЗ диспетчер: неизвестная ветвь «<корень>» (из: красный зелёный)`. Следовательно, несовместимость argv существовала уже на v1, а не появилась вследствие позднего дрейфа.

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:56–61 — точнее назвать меру «четыре версии, три корректирующие перезаморозки; среди трёх поправок нет предъявленного drift-класса». Начальная v1 — не четвёртая корректирующая заморозка. Археология поддерживает отказ от мягкой заморозки на этой выборке, но не делает 4/4 независимыми случаями исправления. Эта точность frontier-обоснования не добавлена к основаниям FAIL.

А-192 подтверждён в узкой части и опровергнут в расширенной части живой матрицей выше. После настоящей заморозки 036 точечные ЗОНА-строки действительно откроют red-пути architect — эта часть раздачи работоспособна.

## Остальные вопросы гейта

### Freeze действительно должен вызывать проверку

`contracts/036-spec-tochnost-prefriz-gejt.md:32–39,107–114,161–166` требует настоящего отказа freeze, отсутствия тега после отказа и положительного конца-в-конец. Поэтому отдельно объявлять блокером пример «гейт написан, но freeze его не вызывает» по одному тексту оснований нет: г5 должен его различать. Б2 означает, что исполнимое различение пока не предъявлено; наличие работающих ворот я не утверждаю.

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:161–166 — в предъявленном тексте отрицательный freeze-вход покрывает В1; полезно тем же концом-в-конец закрепить отрицательные В2 и В3, чтобы вызов только части spec-preflight не выглядел полным подключением. Это совет, а не дополнительный блокер.

### Недоказуемые пункты и решения, оставленные исполнителю

Основные rc-пробы перечислены. Б1 и Б3 показывают существенные решения, которые нельзя оставлять на догадку implementer: совместимость СПАСЕНО с настоящим потребителем и источник независимого census-наблюдения.

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:181–185 — для «мета-барьер зелёный в своей семье» и «freeze живого 037+ проходит» следует назвать точные команды/артефакт стенограммы и ожидаемые rc. Последнее — действие писателя реестра, не безусловно повторяемая проверка; важно отличать пробный toy-прогон от санкционированной заморозки следующего живого контракта.

### Зоны и замороженный 023

Все названные основные изменения имеют исполнителей на строках 118–123: spec-гейт, мета-барьер, freeze, case-файлы, package.json и CI — implementer; red-файлы — architect; роль critic — orchestrator. Для этих поимённых файлов молчаливого расширения зоны не требуется.

Прочитан именно блоб `frozen/contracts/023/1:contracts/023-rezervacija-nomera-dver-po-tegu.md`. Его строка 355 действительно содержит `scripts/freeze_contract.sh` в «РАБОТА НЕ РАЗДАЁТСЯ». Это ограничение раздачи 023, не вечный запрет последующим контрактам менять данный скрипт. AGENTS в шаге заморозки различает frozen-текст и код механизмов; 036 явно раздаёт этот путь заново. Само по себе добавление гейта freeze не требует РАЗРЕШИЛ за правку текста 023: текст 023 не меняется. При реализации остаётся обязанность не сломать прежнее поведение freeze и doc-preflight; настоящим вердиктом регресс реализации не принимается.

Правка `roles/critic.md` сейчас не выполнена и этим FAIL не разрешается. Проверка согласия судьи по правилу 14 нужна при реальной правке во время открытого круга; наличие зоны orchestrator не заменяет это согласие. Предлагаемое правило о стенограмме следует вводить после появления исполнимого гейта, без заднего применения отсутствующего инструмента к текущему кругу.

## Итог и воспроизводимость

Заморозка 036/1 не разрешается. Основания: Б1 — доказанная несовместимость спасения истории; Б2 — противоречие норме кодового red-first/Н-39, не компенсированное предъявленной стенограммой; Б3 — конкретный константный обход обещанного census-свойства. Остальные замечания не блокируют.

Два собственных проверочных скрипта завершились rc 0:

```text
python3 tmp/critic036_measure.py
python3 tmp/critic036_zones.py
```

Они сохранены в клоне для воспроизводимости. Первый исполняет приёмочные пути, staged-эксперимент до/после временного тега, исторический argv-диспетчер и красно-зелёный toy настоящего check_zones. Второй исполняет покаталожную staged-матрицу и независимо извлекает исторические ЗОНА/СПАСЕНО и границы 023. Полные CI, project-wide проверки и реализация 036 не запускались и не объявляются проверенными. Клон сохранён до приземления вердикта; основной репозиторий и текст контракта не изменены.
