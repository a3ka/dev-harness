FAIL

# Контракт 043 — первый круг критика

Предмет: `contracts/043-precizionnyj-prefriz-gejt.md` на HEAD
`b99e397e1c12a3cf43e49385107cacaf858111ea`; коммит текста — `4db81e5`.
Набор полный: предмет, исполняемая приёмка, architect/implementer и ЗОНА-строки
закоммичены. Контракт прочитан целиком; прочитаны AGENTS.md и roles/critic.md.
Проверки выполнены в отдельном SSH-клоне. Код использован как конкретное
состояние дерева, показывающее недостаточность приёмки, а не как повод
расширить предмет критика до общего ревью реализации.

## Исполнение объявленной приёмки

- `bash fixtures/_krasnye_043.sh` → **0**: все девять файлов Р1–Р9 имеют
  `rc=0`, итог `9 файлов, провалов 0`.
- `bash fixtures/parsing_hygiene_battery/run_battery.sh check_precision_gate`
  → **0**: `итог 4/4 классов закрыто`.
- Обязательная живая команда
  `bash scripts/check_precision_gate.sh /tmp/dev-harness-verify/critic043-review contracts/043-precizionnyj-prefriz-gejt.md`
  → **0**, последняя строка `OK`. Stderr:
  `precision-гейт 043: семья precision_gate — барьер правится этим же черновиком, задача (б) для неё пропущена (прецедент 036 В4-3)`.
- Норма-строка Р12 найдена точным полно-строчным поиском в `roles/critic.md:191`.
  Это проверка текста специализированным поиском, не заявление о выполнении
  отсутствующей в контракте полностью выписанной shell-команды Р12.

Один параллельный запуск self-gate вместе с Р10 столкнулся с общим scratch
`lib_zones`: stderr содержал `zones_scoped: No such file or directory`, хотя
rc остался 0. Такой результат не засчитан как чистая проверка union. Приведённый
выше итог обязательного гейта получен повторным ПОСЛЕДОВАТЕЛЬНЫМ запуском без
этих ошибок. Общую библиотеку и её конкурентность этим вердиктом не раздаю.

## Блокирующие находки

### Б1 — объявление одной коллизии скрывает другую

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:130 — правило «пара пропущена, зелёная» после одного ПЕРЕСЕЧЕНИЕ слабее предмета :27–34: согласовать столкновения с union всех остальных контрактов. Р2/Р3 и батарея не различают все коллизии одного пути и только первую.
ОБХОД: в живых заморозках `991` и `992` один `shared/file.txt` принадлежит соответственно `alice` и `bob`; черновик 043 объявляет этот путь за `architect` и только `ПЕРЕСЕЧЕНИЕ architect: shared/file.txt — 991 объявлена только коллизия с alice`. Гейт возвращает **0, OK**, хотя коллизия с 992 не объявлена. Текущее дерево одновременно проходит Р1–Р11 и Р13.

Живой опыт использовал настоящие git-коммиты и теги
`frozen/contracts/991/1`, `frozen/contracts/992/1`, построенные существующими
`mk_toy_repo`/`mk_foreign_frozen` из `_toy.sh`; барьер не подменялся.
Сырой результат: `MULTIPLE_COLLISIONS gate_rc=0 expected=1 output=OK`.
Причина видна в `scripts/check_precision_gate.sh:162-175`: поиск обрывается
на первой чужой строке, её объявление завершает разбор всей пары.
Нужен критерий, различающий объявление всех столкновений и одного из них;
это не требование нового вида коллизий или сравнения путей по префиксу.

### Б2 — канал трассы доступен самой пробе

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:171 — объявленный независимый оракул опирается на неверное свойство :174–175 «проба … не [владеет] каналом трассы». Унаследованный дескриптор доступен коду case-файла; Р7 проверяет отсутствие вызова, но не имитацию подтверждения вызова. Это также противоречит AGENTS.md, правило 8: данные, записываемые проверяемым, нельзя принимать за независимую истину.
ОБХОД: новый `red_forged_trace.sh` не вызывает барьер вообще, а при наличии `BASH_XTRACEFD` исполняет `printf '+ %s\n' '<абсолютный-путь>/scripts/check_dummy.sh' >&"$BASH_XTRACEFD"`, затем `exit 0`. На неизменённом гейте результат **0, OK**; Р7, весь Р10 и Р11 остаются зелёными.

Сырой результат: `FORGED_TRACE gate_rc=0 expected=1 output=OK`.
Внешнее окружение вызывающего гейт процесса не подменялось: дескриптор и
его номер пробе выдаёт сам гейт. Поэтому исключение ENV TRUST из :66 этот
обход не покрывает. Проверялся свежий toy с `id/CONTRACT/043`, существующим
`check_dummy.sh` и ЗОНА только `fixtures/check_dummy/`; исключение :138–147
не задействовано. Контрпример не использует stdout/stderr-совпадение,
обёртки команд или путь в аргументе постороннего вызова из residual :68.

### Б3 — прямой провал самопроверки не наблюдается

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:184 — предмет требует rc прямого, нетрейсового прогона, но Р8 различает лишь безусловный `exit 1`. Критерий принимает механизм, который наблюдает только изменённое xtrace-окружение, а не самопроверку при штатном исполнении.
ОБХОД: новый `red_trace_only.sh` реально вызывает абсолютный путь барьера, затем выполняет `case $- in *x*) exit 0 ;; *) exit 1 ;; esac`. `bash <case>` возвращает **1**, а precision-гейт — **0, OK**. Та же реализация проходит объявленные Р8, Р10, Р11 и Р13.

Сырой результат:
`UNTRACED_SELFCHECK plain_rc=1 gate_rc=0 expected=1 output=OK`.
`scripts/check_precision_gate.sh:267-269,294-295` сохраняет rc единственного
трейсового запуска; отдельного прямого запуска нет. Контрпример снова
исполнялся на настоящем toy, без изменений гарда. Проверка прямого rc и
подтверждение живости должны различаться приёмкой, а не только прозой.

### Б4 — приёмку можно закончить без freeze-time гейта и CI-проводки

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:313 — названный «E2E» Р13 запускает sibling напрямую, а не обещанный в :200–213 freeze-time путь. Ни одна команда Р1–Р13 не доказывает добавление этой проводки или подключение семейной приёмки к CI (:260–262).
ОБХОД: оставить `scripts/freeze_contract.sh`, `package.json` и `.github/workflows/ci.yml` без новой проводки 043, добавить sibling, его прямые пробы и строку роли. Все названные проверки зелёные, а обязательная половина предмета — freeze-time backstop вместе с CI-проводкой — отсутствует. Это не гипотетический мутант: именно такое состояние предъявлено на HEAD b99e397.

Проверено чтением `scripts/freeze_contract.sh:151-180`: после consumers-gate
сразу идёт doc-preflight; поиска `check_precision` в файле — нет.
Поиск `precision|043` в package.json/CI не нашёл вызова новой приёмки
(совпадение `:1043` в комментарии CI вызовом не является).
Живые положительные команды приведены выше. Факт отсутствия вызова
установлен по коду; саму заморозку без accept не выполнял и успешной
несанкционированной заморозки не заявляю. Дефект критерия в том, что такой
незаконченный путь нельзя отличить названными командами от законченного.

## Что проверено и не является блокером

- Self-application 043 исключает именно `precision_gate`: семейный ключ
  сопоставляется с литеральным `scripts/check_<key>.sh` среди ЗОНА-путей.
  В toy для задач (б) такого ЗОНА-пути нет, поэтому эти пробы реально доходят
  до задачи (б). Наличие корректного исключения самого 043 не объясняет Б2/Б3.
- Задача (а) действительно берёт `zones_scoped` из `zones_load`, определяет
  own-пару по автору/пути/NNN и сравнивает литералы. Б1 — не обвинение в
  подмене библиотеки: потеря возникает после нахождения первой коллизии.
- Задача (в) буквально вызывает существующий `verify_ci_parity.sh` (:301–303
  скрипта), не воспроизводит его парсер. Дополнительный toy с
  `package.json={"scripts":{"check:orphan":"true"}}` дал **rc1**:
  `паритет CI красен (verify_ci_parity.sh rc 1):   FAIL скрипт «check:orphan» есть в приёмке, но отсутствует в CI и не объявлен исключением`.
- Грамматика :162–167 действительно объявляет полярность именем, а не
  доказывает смысл красного сценария. Само переименование `red_`/`green_`
  не предъявляю отдельным блокером сверх обещанного контрактом свойства.
- Обычная попытка поместить `+ <барьер>` внутрь многострочного аргумента
  `printf`, не записывая в trace-fd, дала **rc1** «барьер … не вызван живьём».
  Её не выдаю за найденный обход; доказанный Б2 использует именно writable fd.

## Советы

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:195 — порядок назван `а → б → в`, но :197 требует исполнять (б) последней. Выбрать один порядок до заморозки; самостоятельного обхода предмета из этой цены/очерёдности не предъявляю.

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:304 — `red_*`/`green_*` оправданы собственной грамматикой :162–167, но не совместимы с case_*-инвентарём antiplacebo автоматически. Маркер `НЕ БАРЬЕР` уже явно предупреждает об исключении; повторный маркер сам по себе ничего не докажет. Не требую переименования вопреки контракту. Нужен честно названный отдельный канал CI-приёмки; его отсутствие входит в Б4, а не превращает имена файлов в самостоятельный запрет.

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:323 — ПРОВОДКА называет `roles/architect.md`, но ЗОНА :249 этот файл не содержит. Норма уже приземлена до заморозки, поэтому текущей необходимости повторно менять файл не утверждаю; при дальнейшей правке этой нормы надо явно удержать исполнителя и границу, не поручать незаявленный путь молчанием.

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Да: Б1–Б4 предъявляют конкретное зелёное
   состояние с невыполненным свойством; Б1–Б3 подтверждены живыми входами.
2. **Готовность доказуема названными командами?** Локальные пробы исполнимы
   и зелёные; полнота cross-контрактных столкновений, независимость трейса,
   прямой rc и freeze-time/CI-путь ими не доказаны.
3. **Решение оставлено исполнителю?** Предел одного ПЕРЕСЕЧЕНИЕ и источник
   доверия к trace-fd требуют явного исправления контракта/критерия, а не
   выбора исполнителем после заморозки. Противоречие порядка — совет.
4. **Границы?** Два исполнителя и ЗОНА-строки названы; механизм границ —
   существующий check_zones и union-семантика. Особенность роли architect
   отмечена советом, новые области реализации этим вердиктом не вводятся.
5. **Старшая норма?** Б2 противоречит правилу 8. Модель угроз есть; её
   перечисленные исключения не разрешают принимать запись самого case
   в выданный ему канал за независимое наблюдение исполнения барьера.

Полные тесты, форматтеры и линтеры не запускались. Код, контракт, роли и
фикстуры не правились. Единственная постоянная правка — этот вердикт.
Первый FAIL: повторной причины/спора и основания созывать арбитра пока нет.
