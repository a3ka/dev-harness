accept

# Контракт 040 v3 — И1/И2 закрыты, узкая проверка исполнения

Проверенный HEAD: `4c2bab8ff18b78c3b3f80e6c3a73fa56a2b47371`.
Предмет: `contracts/040-batching-git-vyzovov.md`, после обновления пина
коммитом `41555d9`; исправление исполнимой Р9 —
`1b67f790fd922b2248622a289ec29ad7889826de`.

Это НЕ новый полный круг и НЕ суд реализации батчинга. Целиком прочитан
предыдущий `verdicts/critic/contracts-040-v3.md` (`1934156`); проверено только
исполнение его И1/И2 и отсутствие регрессии прежних девяти структурных
сценариев. Б1/Б2/Б3ч1/Б4 не переоткрывались, новые классы находок не искались.
Основание — `verdicts/arbitration/040-batching-kriterii-i-tehnika.md:384–386`:
несоответствие уже принятому решению чинится правкой, а не новым спором.

Набор артефактов полон и закоммичен: предмет, исполнимая приёмка, исполнители
и ЗОНА-строки присутствуют. Опыты выполнены в собственном полном клоне
текущего HEAD; в основном checkout приёмка и контракт не менялись.

## 1. Два точных репро и честный контроль — полная команда Р9

Во всех трёх опытах запускалась именно:

```sh
bash fixtures/check_zones/probe_ci_dostizhimost_predelov.sh .
```

В клоне менялись ТОЛЬКО `package.json` и `.github/workflows/ci.yml`:
добавлены два настоящих npm-ключа со значениями из предыдущего вердикта;
к существующим джобам добавлялся соответствующий вариант. Существующая
проводка сохранялась. Три приёмочных оракула, оба производственных барьера
и настоящий `verify_ci_parity.sh` не подменялись.

| Опыт | Результат полной Р9 | Время |
|---|---|---:|
| И1 / ИЙ1: джоба `critic040_budget` с `continue-on-error: true`, два прямых канонических шага | **rc=1**, отказ именно по джобовому `continue-on-error` | 160.45 с |
| И2 / ИЙ2: блочный `needs:` с `- critic040_upstream`, у зависимости `if: false`, два прямых шага в зависимой джобе | **rc=1**, отказ именно по `if:` в needs-замыкании | 225.31 с |
| Канон: одна добавленная честная джоба без `if`, `continue-on-error`, `needs`; два отдельных прямых шага | **rc=0**, настоящий `verify_ci_parity` rc=0 | 190.38 с |

Точный вывод И1:

```text
ОТКАЗ: джоба critic040_budget (несёт шаг для check:zones-call-budget) сама имеет continue-on-error: true
```

Точный вывод И2:

```text
ОТКАЗ: needs-замыкание джобы critic040_budget (несёт шаг для check:zones-call-budget) содержит if:
```

Точный вывод честного контроля:

```text
CI-ДОСТИЖИМОСТЬ ДЕРЖИТСЯ (СТРУКТУРНО, fail-closed): check:zones-call-budget -> джоба critic040_budget, rc(npm)=1=rc(прямой)=1; check:protected-call-budget -> джоба critic040_budget, rc(npm)=1=rc(прямой)=1; безусловный триггер push/pull_request; verify_ci_parity rc=0
```

Равенство единиц у Р1/Р2 — ожидаемая предреализационная ситуация, уже
разобранная предыдущим вердиктом; здесь оно не выдано за завершение батчинга.
Проверен предмет Р9, включая его настоящую npm/прямую проводку и паритет.

**И1 закрыта** (`contracts/040-batching-git-vyzovov.md:250,377`):
джобовый флаг сохраняется и отвергается
(`fixtures/check_zones/probe_ci_dostizhimost_predelov.sh:242–246,318–319`).
**И2 закрыта** (`contracts/040-batching-git-vyzovov.md:244,377`):
скалярный элемент блочного списка больше не теряется, зависимость доходит
до проверки замыкания
(`fixtures/check_zones/probe_ci_dostizhimost_predelov.sh:202–204,250–254,320–321`).
Основание закрытия в обоих случаях — выполненный полный репро, не чтение кода.

## 2. Девять прежних синтетических сценариев — без регрессии

Как в предыдущем круге, извлечён и исполнен БЕЗ ИЗМЕНЕНИЙ Python-блок
структурного предиката из текущей Р9. Это отдельная проверка предиката,
не девять полных прогонов Р9: Python печатает статус, оболочка Р9
превращает `FAIL` в rc=1. Сам Python-блок завершает эти ответы кодом 0.

| Сценарий | Статус предиката |
|---|---|
| Канон | `OK` |
| `if:` на шаге | `FAIL` |
| `if:` на джобе | `FAIL` |
| `if:` у зависимости из flow-списка `needs` | `FAIL` |
| `continue-on-error: true` на шаге | `FAIL` |
| `run: npm run <ключ> || true` | `FAIL` |
| `run: npm run <ключ>; true` | `FAIL` |
| flow-стиль `steps: [...]` | `FAIL` |
| YAML-якорь `steps: &budget_steps` | `FAIL` |

Итого: **9/9 совпали с предыдущим кругом**. Дополнительно оба И1/И2 дали
`FAIL` и в изолированном предикате; полные rc=1 предъявлены выше.

## 3. Новый Р8-пин и быстрое не-ослабление

Исполнена команда Р8 из `contracts/040-batching-git-vyzovov.md:376`, с
новым пином `1b67f790fd922b2248622a289ec29ad7889826de` и всеми восемью
namespace-glob: **rc=0**, diff пуст.

Та же команда к прежнему пину
`f0b6de662d4826ed1707f2b97fef1c8aeeb06435` дала **rc=1** и непустой
живой diff именно `fixtures/check_zones/probe_ci_dostizhimost_predelov.sh`:
исправления джобового continue-on-error и скалярных элементов needs.
Значит обновление пина покрывает реальную правку исполнимого оракула.

В отдельном индексе клона построено дерево с заменой только этой пробы на
`#!/usr/bin/env bash` / `exit 0`; та же команда с НОВЫМ пином и восемью glob,
с изменённым tree вместо HEAD, дала **rc=1**. Новый пин действительно
отвергает дальнейшую подмену файла, а не просто совпадает с текущим HEAD.

Не-ослабление проверено быстро, без повторного полного scoped-раннера:

- независимый обход файлов: `check_zones` — **21** `case_*.sh`,
  `check_protected` — **20**;
- обе команды Р3/Р4 `git diff --exit-code --diff-filter=MD --no-renames`
  к прежнему пину, каждая по своей case-семье: **rc=0**, пусто;
- прямой diff к прежнему пину для `scripts/check_zones.sh`,
  `scripts/check_protected.sh`, `scripts/verify_ci_parity.sh`,
  `scripts/verify_antiplacebo.sh`, `scripts/check_contract_frozen.sh`:
  **rc=0**, пусто.

Повторный прогон 21/20 фикстур и полный проектный прогон здесь НЕ заявляются:
это узкая верификация патча Р9, существующие case-файлы и связанные барьеры
не изменены. Контракт и реализация этим вердиктом не правятся.

## Итог

Порог, заданный предыдущим вердиктом и текущим поручением, достигнут:
**И1 rc=1, И2 rc=1, честный канон rc=0, прежние 9/9 без регрессии**.
Пин обновлён и испытан живой мутацией; case-семьи 21/20 не ослаблены.
Открытых блокирующих находок в пределах этой проверки не осталось.
**Заморозка разрешена; новый арбитраж не требуется.**
