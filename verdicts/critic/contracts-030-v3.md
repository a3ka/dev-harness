FAIL

# Контракт 030 — критик, круг 3, scoped по РЕШЕНИЮ 0375aed

Предмет: `contracts/030-inventar-i-gonki-spavna.md` на `05d1ad6061a192fb42e6377ac24df7c22b1d7048`, ветка `wip/030/architect`. Судья: critic, модель `openai-codex/gpt-6-astra`.

Набор достаточен: предмет закоммичен, исполнители, зоны и команды готовности названы. Проба 1/3 предъявлена по А-174 в скратче, её SHA-256 совпадает с пином коммита:

```text
7c53034856e519753515a1017a772dfb0f3f74139c8184da6e41645d6c963fe0
```

Суд ограничен применением критериев арбитра и новыми дефектами дельты `bd8dbfd→05d1ad6`. Инварианты :21–24, Б2/Б3/Б4 круга 1, прежние границы и РАЗРЕШИЛ-абзац не переоткрыты. Проба 1/3 не выдана за файл HEAD: ссылки на её строки ниже относятся к исполненной скратч-копии с указанным пином.

## Итог

Живой перехват действительно появился. Внутренний суд пробы правильно различает все 14 заявленных контрольных входов, включая v5 именно проектным путём. На исходном workshop, симуляции добавления hub и восстановленном исходнике полная команда даёт `1 → 0 → 1`.

Однако этого недостаточно для accept:

1. Новый строковый транспорт наблюдения обрезает настоящий операнд `--tools` по LF. Исполнено дерево, на котором полная проба возвращает 0 при наличии токена `hub` в действительном FABLE-инвентаре.
2. Зелёный внутренний v4 не означает принятия v4 объявленной командой: при установке того же легального класса в проверяемое дерево полная проба возвращает 2 до живого запуска. Это прямо расходится с положительным критерием арбитра и нормой прохождения честного дерева.

Отдельно измерено: сломанный omp-стаб даёт внутренние rc 2, но общий rc 1. Это не скрыто в протоколе и не выдано за правильную дисциплину 0/1/2.

## Блокирующие находки

### Б1 — новый транспорт теряет часть наблюдаемого операнда

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:134–136 — утверждение, что судится полученный exec операнд и грамматика самого операнда, не выполнено: проба судит лишь его первую строку.

ОБХОД: в изолированном дереве добавить hub в базовый список, как в положительном контроле. Сразу после исходного FABLE-присваивания добавить:

```bash
    TOOLS="$TOOLS"$'\n,hub'
```

Исходные якорные строки остаются. Никакой подделки stdout или знания имени проверяющего в workshop нет: изменено только значение проверяемого списка.

Полная объявленная команда:

```text
bash fixtures/workshop_inventory/red_inventar_hub.sh
rc=0
```

Все 14 внутренних контролей также имеют ожидаемые коды и отметку `ok`.

Независимый стаб, сериализующий argv в JSON без потери LF, при живом `./workshop --fable` получил:

```json
{"runtime_rc":0,"fable_operand":"read,grep,glob,bash,web_search,task,todo\n,hub","hub_token_present":true}
```

После разделения по запятой последний токен — ровно `hub`. Следовательно, неизменённый инвариант запрета hub в FABLE нарушен при зелёной пробе. Новая трактовка инварианта не вводится.

Причина локализована в новой пробе: стаб печатает `EFFECTIVE_TOOLS=%s\n`, а `rt_tree`/`rt_mini` проводят stdout через `grep '^EFFECTIVE_TOOLS=' | sed ...` (:84–120). Продолжение одного аргумента после LF отбрасывается прежде, чем `sudit_op` увидит его. Грамматическая проверка уже не может обнаружить потерянные байты. Это новый дефект именно динамической дельты, scope (б), а не повторное требование общего интерпретатора shell.

Воспроизведение: `python3 /tmp/dev-harness-verify/critic-030-k3/operand_integrity.py` → rc 0; скрипт проверяет одновременно зелёную пробу и наличие hub в независимо перехваченном FABLE-операнде. Снимок входа: `operand-integrity-workshop`; результаты: `operand-integrity.json`, `operand-integrity.stdout` в том же каталоге.

Выход по находке: наблюдение должно сохранять целиком аргумент argv либо явно отвергать повреждённый/непредставимый транспортом аргумент до инвариантного суда. Описанный вход должен возвращать 1, положительный контроль — 0. Механизм выбирает architect.

### Б2 — внутренняя таблица не доказывает селективность объявленной команды

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:137–142 — легальный класс v4, которому прямо обещано зелёное, полная приёмочная команда не принимает. Противоречие норме `AGENTS.md:127–129`: критерием служит команда, проходящая на честном дереве, а не успешный вызов её внутренней функции в обход входных ограничений.

Это блокировка по исключению порога для противоречия AGENTS.md; ложное зелёное для этой отдельной находки не заявляется.

Исполненный v4: базовое присваивание заменить на:

```bash
. "$HERE/scripts/session_tools.sh"
```

В `scripts/session_tools.sh` поместить исходный базовый список с `,hub`. Независимые живые запуски подтвердили:

```text
тул FABLE=0: hub есть
тул FABLE=1: hub нет
проектный путь: hub есть
```

Однако полная команда вернула:

```text
rc=2
NOT_IMPLEMENTED: якорь базового TOOLS (:473) найден 0 раз — проба не судит молча
```

Причина — предусловия :72–78 скратч-пробы, требующие точного исходного присваивания до любого живого наблюдения. Внутренний `sudit_tree` вызывается на синтетическом v4 после этих предусловий и потому зеленеет; реальный вход v4 пройти через них не может. Это именно положительный класс арбитра, не новый класс и не расширение области.

Тот же внешний прогон выявил, что k1/k2/v5 дают 2, а не инвариантный 1: базовый якорь найден соответственно 0/2/2 раза. Код 2 нельзя засчитать как обещанное именованное красное этих классов. Полная таблица ниже различает внутренние и внешние результаты.

Выход по находке: предъявить требуемую классификацию всей командой на соответствующих входных деревьях; построение её самоконтролей не должно запрещать легальный v4 или превращать уже заданные отрицательные классы в ошибку подготовки. Способ организации контроля — за architect.

## Совет по дельте

СОВЕТ contracts/030-inventar-i-gonki-spavna.md:125,129–136 — заявленная rc-дисциплина теряется на внешнем уровне пробы. После ветви `--version` в локальную копию создаваемого omp-стаба добавлен `exit 73`. Получено: все 14 внутренних судов `rc=2 | СЛОМ`, живой суд также не дошёл до наблюдения, но вся команда вернула **1**, не 2.

Это не молчаливое зелёное: stderr честно называет отсутствие наблюдения, таблица печатает СЛОМ. Но автоматический потребитель rc не отличит данный результат от красного предмета. Причина: `vivod` сводит поломку к `FAIL=1` (:259–260), а хвост (:286–291) сводит любой ненулевой результат `sudit_tree` к `exit 1`. Рекомендуется сохранить различие на внешнем интерфейсе. Как самостоятельный блокер без отдельного обхода готового дерева эта находка не используется; FAIL уже обоснован выше.

## Собственные прогоны

### Внутренняя батарея и внешний вход — разные измерения

Первый столбец получен запуском предъявленной пробы, не переписан из доклада автора. Второй — установкой независимо построенного соответствующего workshop в проверяемое дерево и запуском всей объявленной команды.

| Вход | Внутренний суд | Полная команда на дереве этого класса |
|---|---:|---:|
| head | живой хвост 1 | 1 |
| pos | 0 | 0 |
| k1 | 1 | 2 — нет базового якоря |
| k2 | 1 | 2 — два базовых якоря |
| v1 | 1 | 1 |
| v2 | 1 | 1 |
| v3 | 1 | 1 |
| v4 | 0 | 2 — нет базового якоря |
| v5 | 1 | 2 — два базовых якоря |
| w1/w2/w3, мини-workshop | 1/1/1 | не подменялись полным деревом |
| w4/w5/w6, мини-workshop | 0/0/0 | не подменялись полным деревом |

Именованные внутренние отказы извлечены отдельным прогоном с единственным изменением диагностической копии — сохранением временных файлов вместо удаления:

- w1 — F=0 без hub;
- w2/k1 — F=0 без hub, FABLE с hub;
- w3 — завершающая запятая;
- k2/v3 — без hub в тул- и проектном путях;
- v1/v2 — без hub в тул-пути;
- v5 — **только проектный путь :597 без hub**;
- w4/w5/w6/pos/v4 — отказов нет.

Таким образом, внутренняя динамическая функция действительно различает предъявленные классы. Дефект внешнего v4 не отменяет признания этого результата.

Форма покрытия установлена точно: проба делает три запуска полного дерева — без аргументов, `--fable`, `<дерево>`. Это оба названных exec-пути и оба режима тул-пути, но не декартово произведение из четырёх запусков. Дополнительный собственный запуск положительного дерева `./workshop --fable <дерево>` вернул базовый список с hub и модель `zai/glm-5.3`: существующий проектный путь не применяет FABLE-блок. Неизменённая семантика этого сочетания не переоткрывается и дополнительным основанием FAIL не служит.

### Три предъявления и недеградация 2/3, 3/3

```text
Проба 1/3: исходный workshop                  rc=1
Проба 1/3: симуляция предмета — базовый hub    rc=0
Проба 1/3: восстановленный исходный workshop  rc=1
Проба 1/3: сломанный omp-стаб                 rc=1, внутри rc=2/СЛОМ

Проба 2/3: исходный path-guard                rc=1, точный QA-путь блокируется
Проба 2/3: точный carve-out в двух зеркалах   rc=0
Проба 2/3: восстановленный path-guard         rc=1

Проба 3/3: исходный gc                       rc=1, свежая ветка снесена
Проба 3/3: скратч-стенд zhnets-stand.sh      rc=0
```

Положительный прогон 3/3 — суд критерия на ранее предъявленном скратч-стенде, не приёмка реализации жнеца. Б2/Б3/Б4 круга 1 заново не судились.

Проба 3/3 из скратча `proba3-v2.sh` побайтово совпадает с файлом HEAD:

```text
0d58a6b1686a260f69c3d48249f929b5fd318a8a0daf76431b2a55c92037af31
```

Пин исполненной пробы 2/3 совпадает с кругом 2:

```text
e632a4fd0b25cacce2efeb580a36b532f0966be2691cacff117cd23d3f2c0efc
```

### Базлайны, дельта и границы доказательства

В отдельном неизменяемом baseline-клоне, изолированно и последовательно:

```text
bash scripts/verify_antiplacebo.sh --scope gc_agent_branches  rc=0
bash scripts/check_zones.sh .                              rc=0
```

Gc-scope: 1 барьер, 7 фикстур, 7 красных повторных предъявлений. Коды дополнительно записаны subprocess-драйвером в `baselines.json`, поскольку асинхронный текстовый отчёт инструментов не показывал их явно.

`git diff --exit-code bd8dbfd 05d1ad6 -- scripts workshop .omp/extensions fixtures` → rc 0. Коммит меняет только абзац приёмки контракта и добавляет А-178 в `NABLIUDENIA_ARCHITECT.md`; оба пути относятся к объявленной зоне architect. Нового выхода в чужую зону нет. Пин пробы синхронен коммиту, но фактические свойства пробы не полностью соответствуют абзацу, как показано выше.

Пять вопросов гейта рассмотрены только в scoped-области: слабость доказательства и расхождение команды с обещанной селективностью предъявлены; нового молча делегированного предметного решения не установлено; границы изменения сохранены; противоречие норме прохождения честного дерева названо отдельно. Неизменённая остальная проза контракта не использована для блокировки.

Стенограмма спавна: входное свидетельство оркестратора — последний `--check` rc 0 после аудированного переснятия, класс Н-112. Это не мой самостоятельный прогон детектора. Ветка реализации 031 не тронута.

## Артефакты и выход

Все действия выполнены в собственных клонах под `/tmp/dev-harness-verify/critic-030-k3/`:

- `repo/` — checkout 05d1ad6, baseline;
- `experiment/` — изолированные входы и симуляции;
- `verify.py`, `results.json` — предъявления 1/3–3/3, симуляции и слом стаба;
- `external_matrix.py`, `external-matrix.json` — вся команда на head/pos/k1/k2/v1–v5 и независимый JSON-перехват argv;
- `capture_internal.py`, `internal-diagnostics.txt` — именованные внутренние отказы;
- `operand_integrity.py`, `operand-integrity.json`, `operand-integrity-workshop` — исполненный обход Б1;
- `baselines.py`, `baselines.json` — явные коды scoped-базлайнов.

Full-suite, форматтеры, линтеры и project-wide build не запускались. Рабочий worktree, ветка предмета и основной репозиторий не изменялись. Вердикт передан полным текстом оркестратору для приземления с author=critic; клоны и доказательства сохранены до его коммита.

Выход: **FAIL только по scope (а)/(б)**. Консолидации в v1 и заморозки нет. Техническое исправление — сохранить целый наблюдаемый аргумент и доказать классификацию полной командой, не только её внутренним судом. После FAIL по тому же предмету вслед за РЕШЕНИЕМ дальнейший процесс направляется оркестратором владельцу согласно AGENTS.md; автоматический четвёртый круг этим вердиктом не назначается.
