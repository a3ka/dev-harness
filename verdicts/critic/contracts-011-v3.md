FAIL

Узкий круг проведён только по закоммиченному diff `689f09e` в §Зоны и §История
правок; остальной контракт 011 повторно не судился.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:153 — строка называет
`СПАСЕНО architect`, но все три названных коммита имеют фактический `%an=orchestrator`.
Механизм хранит исключение как тройку `<автор из СПАСЕНО, хеш, контракт>`
(`scripts/check_zones.sh:241`) и применяет её только при совпадении с фактическим `%an`
коммита (`scripts/check_zones.sh:310-312`). Поэтому новые записи сохраняются как
`architect + hash`, не совпадают ни с одним из трёх `orchestrator + hash` и фактически
не легализуют их. Более того, `orchestrator` не объявлен ни одной `ЗОНА`-строкой, а
строка 310 пропускает всех не объявленных авторов. Наблюдение на HEAD: `bash
scripts/check_zones.sh` вернул 0 и напечатал четыре старых СПАСЕНО-коммита 011, но не
напечатал ни `7b98b07`, ни `8e4b5d1`, ни `092314c`.
ОБХОД: оставить строку 153 неизменной и добавить четвёртый коммит с
`user.name=orchestrator`, меняющий `scripts/check_runner_hygiene.sh`. Он не назван в
СПАСЕНО, однако будет пропущен строкой 310 вместе с этими тремя, а `bash
scripts/check_zones.sh` по-прежнему завершится кодом 0. Тем самым объявленная конечность
«ИМЕННО и ТОЛЬКО эти 3 хеша» механически не удерживается.

БЛОКИРУЕТ contracts/011-prijomka-sudi-i-gigiiena-rannera.md:331-334 — история правок
утверждает, что три коммита «легализованы», хотя новая СПАСЕНО-строка механизмом к ним
не применяется. Это тот же дефект строки 153, не отдельная причина отказа.
ОБХОД: удалить строку 153 целиком; на текущем HEAD `check_zones.sh` всё равно пропускает
три `orchestrator`-коммита и остаётся зелёным, то есть заявленная легализация не является
условием результата.

Остальные предписанные проверки дефектов не нашли. Полные хеши в строке совпадают с
`git log`; каждый из трёх — потомок `frozen/contracts/011/1`
(`cb5f1c975056a71dbcf404b1ac4faebb02d7f491`). `git show --stat` подтверждает честное
описание: `7b98b07` меняет барьер, эталон и добавляет три fixture; `8e4b5d1` меняет две
строки причины ветви `sostav`; `092314c` меняет только `NABLIUDENIA_ARCHITECT.md` с
А-14/А-15. Текст прямо различает похожий Н-33-класс и этот предметный случай.
`bash scripts/check_zones.sh` и `npm run check:charter` завершились кодом 0; зелёный
`check_zones` здесь и предъявляет описанный обход, а не закрывает его.
