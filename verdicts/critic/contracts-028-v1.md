accept

## Предмет, три круга и основание третьего

Круг 3 (финальный) по контракту 028. Судимое:
`contracts/028-home-sessii-vne-dereva.md` на HEAD
`2facfe37ae7da74fd67aa769dda77b0fff844d3b`, блоб
`7e3f091f84fe9b95c02d1ae965a8db0d85798a0b`.
Идентичность с `5be0d04` и `015e9ea` проверена по блобам, не по `%an` (Н-101).

- Круг 1: `4be21b9`, FAIL, три блокера; блоб вердикта
  `20173d15a86680c10b4ddff91681d88fba40fbd8`.
- Круг 2: `626af24`, FAIL, два новых блокера и совет; блоб вердикта
  `2d15bceb51489e15c8a65c5b30f4d22e5169f1f6`. Закрытия круга 1 тогда подтверждены.
- Круг 3: этот файл заменяет тот же слот v1. Основание — закоммиченный
  `verdicts/arbitration/028-prijemka-tretego-kruga.md` на `2facfe3`:
  «РЕШЕНИЕ», приёмка достаточна, третий круг проводится как обычный под арбитражем.

Набор полный: предмет закоммичен, инварианты, rc-команды, исполнители и шесть
`ЗОНА`-строк присутствуют. Суд проведён в собственном клоне
`/tmp/dev-harness-verify/critic-028-v3/repo`. Прочитаны оба прежних вердикта,
арбитраж, контракт, исходники всех шести предъявлений, модели автора и
`agent://Arch028Fix2`. Судился контракт, не будущая реализация.
Контракт, авторские пробы и модели не изменялись.

## Собственная сверка закрытий круга 2

1. **Передача среды — закрыто** (`contracts/028-home-sessii-vne-dereva.md:58-71`,
   `:167-188`). В пробе 2 launcher запускается с удалённой из входной среды
   `HARNESS_SESSION_HOME`; подставной omp сам снимает унаследованное значение
   и вызывает настоящий `scripts/models_actual.sh` из той же среды.
   Проба не экспортирует ожидаемое значение вместо launcher. Прочитаны именно
   тело потомка и команда запуска, не только комментарии.
   Собственный прогон модели `bez-peredachi` дал пробе 2 rc=1,
   `peredacha-net`, «потомок увидел 'ПУСТО'»; честная форма дала rc=0.
   Модель автора проверена текстуально: исполняемый блок соответствует
   `chestnaja` без строки `export HARNESS_SESSION_HOME="$ZONE"`.
   Прежний зелёный обход круга 2 теперь различается.
2. **Буквальная Н-39 — закрыто** (`contracts/028-home-sessii-vne-dereva.md:150-158`,
   `:160-221`). Собственный grep по стабильным фразам
   `красен ИМЕННО|Красное предъявление против стаба|Стабы, красные`
   на проверенном блобе не дал совпадений. Дополнительно прочитаны все
   оставшиеся совпадения `стаб|обман|красн`: пять заблокированных утверждений
   убраны. Остались инварианты, rc-команды, разграничение ответственности
   и мера покрытия паттерна №5, не запрещённая пара «стаб ↔ ветвь».
   Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза
   контракта; контракт несёт инварианты + rc-команды».
3. **Совет sort/comm — закрыт** (`contracts/028-home-sessii-vne-dereva.md:215-217`,
   `:236-253`). Манифесты пробы 6 и церемонии сортируются полной строкой
   `LC_ALL=C sort`; оба сравнения содержимого в пробе отдельно проверяют rc
   и stderr `comm`. Церемония также требует rc=0 и пустых файлов ошибок/потерь.
   Честная миграция исполнена с rc=0; модель потери истории исполнена с rc=1
   и правильным именованным отказом, без прежней диагностики несортированности.

Это самостоятельная проверка, не принятие матрицы арбитра на доверии.
Арбитражная матрица прочитана для сравнения; полученная независимо совпала.

## Закрытия круга 1 не сломаны

- Канонический результат и наблюдение всего дерева
  (`contracts/028-home-sessii-vne-dereva.md:72-95`, `:189-195`): модель `m2`
  краснеет в пробе 3 с `otkaz-ne-predjavlen-prjamoj`. На честной форме
  исполнены оба входа — прямой XDG и внешний симлинк внутрь; оба отвергнуты
  с rc=1 без роста дерева. Это проверка предъявленных входов, не всех
  возможных способов записи пути.
- Код отказа (`contracts/028-home-sessii-vne-dereva.md:160-166`): модель `m1`
  краснеет в пробе 1 с `uspekh-vmesto-otkaza`; честная форма проходит.
- Сохранность и продолжение (`contracts/028-home-sessii-vne-dereva.md:100-116`,
  `:209-221`): проба 6 проходит на честной форме. Дополнительно независимо
  повторены объявленные дискриминаторы из круга 2: первый старт удаляет
  прежнюю игрушечную `.omp` → rc=1 `pervyj-zapisk-stjor-izmenil`;
  launcher не передаёт `RESUME_ARGS` → rc=1 `resume-ne-peredan`.
  Прежние три причины не переоткрываются.

## Независимые прогоны и границы доказательства

Порядок столбцов: предъявления **1 / 2 / 3 / 6 / 4**.

| Модель | 1 | 2 | 3 | 6 | 4 |
|---|---:|---:|---:|---:|---:|
| bez-modeli | 1 | 1 | 1 | 1 | 0 |
| m1 | 1 | 0 | 1 | 1 | 0 |
| m2 | 0 | 0 | 1 | 0 | 0 |
| bez-peredachi | 0 | 1 | 0 | 0 | 0 |
| chestnaja | 0 | 0 | 0 | 0 | 0 |

Выполнены собственные команды, обе rc=0:

    python3 /tmp/dev-harness-verify/critic-028-v3/proverka.py
    python3 /tmp/dev-harness-verify/critic-028-v3/migratsija.py

Клоны матрицы происходят от собственного клона `2facfe3`, не от рабочего
дерева автора. Патчи моделей и копии проб закоммичены внутри одноразовых
клонов, поэтому вложенный клон миграционной пробы получает нужную модель.
HARNESS_SCRATCH/HARNESS_SESSION_HOME/XDG_STATE_HOME из среды судьи удалены.
Исходники подставных omp и моделей объявлены; живая сессия не спавнилась.
Сырой вывод каждой пары сохранён в `/tmp/dev-harness-verify/critic-028-v3/`:
`matrix-results.json`, `*-red_*.log`, `stiraet-istoriju-migration.log`,
`bez-resume-migration.log`. Проверенные копии проб/патчера лежат в `evidence/`,
их sha256 — в `evidence-sha256.json`; runner-скрипты находятся вне удаляемого клона.

Регресс-пара прошла во всех пяти строках: оба evil-пути названы детектором,
пустая игрушка чиста, объявленная копия с пропуском `.zones/*` различается.
`git diff --exit-code 626af24 2facfe3 -- scripts/check_no_leak.sh
scripts/verify_antiplacebo.sh AGENTS.md` вернул 0. Замороженные барьеры и норма
не менялись; их изменение ради 028 контрактом запрещено.

Живую канарейку 5 не перегонял: раскрытый и благословлённый самотрип Н-98
принят. Две сверки на мигрированном живом дереве и реальное продолжение старой
сессии остаются приёмкой реализации/живой церемонии, не выполнены этим судьёй.
CI на `2facfe3` раскрыт как находящийся в полёте; его результат этим вердиктом
не утверждается. Общепроектные тесты и CI не запускались.

## Пять обязательных вопросов и решение

1. **Критерий слабее предмета?** Прежние конкретные обходы различаются
   предъявлениями. Нового состояния с выполненной приёмкой и невыполненным
   предметом в этом круге не предъявлено.
2. **Доказуемость командами?** Шесть rc-предъявлений разделяют launcher,
   отказ, миграцию, неизменный детектор и живой end-to-end. Пять неживых
   предъявлений исполнены самостоятельно; граница живого доказательства
   названа выше. Зелёная модель не выдаётся за готовую реализацию.
3. **Решения оставлены исполнителю?** Формула и hash8, приоритет источников,
   передача среды, канонический отказ, порядок миграции и отказ существующего
   назначения названы. Нового блокирующего пробела решения не установлено.
4. **Границы названы?** `contracts/028-home-sessii-vne-dereva.md:135-146`
   распределяет файлы между architect/implementer/orchestrator и судьями;
   механизм — `check_zones.sh` после freeze. Разовая живая миграция явно
   поручена владельцу/оркестратору вне git (:113-116, :232-263).
   Раннер-скратч, Н-89 и чистка чужих зон не включены в предмет (:265-276).
5. **Противоречие AGENTS.md?** Блокер Н-39 устранён; durable-дом сессии
   не отменяет правило 16 для временного scratch. Основание третьего круга
   закоммичено арбитром. Нового противоречия в судимых разделах не установлено.

Новых блокирующих находок нет; новых советов не добавлено. Оба блокера круга 2
и его совет закрыты, дискриминаторы круга 1 живы. Заморозка этого блоба
контракта разрешена; это не вердикт о готовности реализации 028.
