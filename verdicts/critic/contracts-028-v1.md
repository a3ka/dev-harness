FAIL

## Предмет и происхождение

Круг 2 по контракту 028. Судимое: `contracts/028-home-sessii-vne-dereva.md`
на HEAD `e05d6fcbdcce4cb0b0a5e2cf4f1800bf1033fb9f`, блоб
`85fd022d568c28599f60c2b059c829d4addd0698` (тот же блоб в `14eb812`).
Круг 1: FAIL в `4be21b9`, блоб прежнего вердикта
`20173d15a86680c10b4ddff91681d88fba40fbd8`. Этот файл — замена вердикта
в слоте заморозки v1; круг 1 остаётся в git-истории, новый слот v2 не заводится.
Идентичность установлена сравнением блобов, не доверием к `%an` (Н-101).

Набор полный: закоммиченный предмет, критерий с rc-командами, исполнители и
шесть `ЗОНА`-строк присутствуют. Суд проведён в собственном клоне
`/tmp/dev-harness-verify/critic-028-v2/repo`. Судился контракт, не реализация.
Прочитаны прежний вердикт, усиленный контракт, `agent://Arch028Fix`, код всех
шести предъявлений и объявленные модели автора. Контракт и авторские пробы
не правились.

## Блокирующие находки

БЛОКИРУЕТ contracts/028-home-sessii-vne-dereva.md:60 — обязательный экспорт разрешённого корня из workshop в сессионную среду не различается предъявлением 2 (:162-176). Оно проверяет потребителя с переменной, которую устанавливает сама проба, а не наследник workshop.
ОБХОД: оставить разрешение внешнего HOME, канонический отказ, миграцию и потребителя из честной модели, но убрать только `export HARNESS_SESSION_HOME="$ZONE"`. Пять неживых предъявлений возвращают 0. `red_zony_net_v_dereve.sh:107-108` самостоятельно задаёт `HOME` и `HARNESS_SESSION_HOME`, поэтому отсутствующую передачу лаунчера не замечает. Канарейка 5 сравнивает дерево, не окружение потребителя: отсутствие экспорта не возвращает писателей внутрь дерева. Реальный потребитель, запущенный потомком workshop при незаданных внешних ручках, вычисляет другой корень и не видит прежние трейсы — инварианты 1 и 7 не выполнены.

Доказательство: объявленная модель критика `bez-peredachi` — честная модель
автора с удалением ровно одной строки экспорта. Независимый прогон дал
`0, 0, 0, 0, 0` для предъявлений 1, 2, 3, 6, 4. Дополнительный PATH-подставной
omp вызывал настоящий `scripts/models_actual.sh` из унаследованного окружения,
не задавая за launcher переменную. На `bez-peredachi`: `SESSION_HOME=UNSET`,
`rc=2`, `NOT_IMPLEMENTED: трейсов в зоне нет — сессий ещё не было`.
На честной форме: `SESSION_HOME=<внешний дом>`, `сессий показано: 1`, `rc=1`
из-за намеренно отличающейся модели игрушечного трейса, а не отсутствия трейсов.
Живая канарейка на этой модели не запускалась; её нечувствительность к экспорту
обоснована прочитанным кодом, не выдана за шестой зелёный прогон.

Закрытие: предъявление должно наблюдать потребителя/переменную в реальном
окружении потомка workshop. Ручная установка ожидаемого значения за предмет
проверяет лишь половину договорённости. Новая модель без передачи должна
краснеть, честная — зеленеть. Отдельный постоянный `case_*` не требуется.

БЛОКИРУЕТ contracts/028-home-sessii-vne-dereva.md:159 — приёмочный критерий прямо утверждает соответствие стаба входу/ветви: «Стабы, красные ИМЕННО на этом входе». То же в :173-176, :184-187, :191-197, :216-218. Это противоречие буквальной норме `AGENTS.md:130-133`, а не замечание к остальной прозе документа.

Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза
контракта; контракт несёт инварианты + rc-команды». `AGENTS.md`:
«Контракту ЗАПРЕЩЕНО утверждать пару „обманный стаб ↔ ветвь“».
Заголовок :146-147 объявляет соблюдение этого разделения, но перечисленные
строки приёмочного критерия его нарушают. Код моделей и результаты матрицы
должны оставаться в предъявлениях/отчёте, не становиться такими утверждениями
замораживаемого критерия. Для противоречия `AGENTS.md` роль критика прямо
предусматривает блокирование без отдельного ОБХОДА. Круг 1 эту же норму
не применил; прежний пропуск судьи не является разрешением нормы.

## Закрытие трёх блокеров круга 1

1. **Канонический результат независимо от источника — закрыт прежний обход.**
   Инвариант 2 (:68-82) теперь охватывает явный HARNESS_SCRATCH, явный XDG,
   дефолтный `$HOME/.local/state` и симлинки; наблюдение :83-89 — всё дерево,
   не одно имя `.zones`. `m2` проходит предъявления 1–2, но
   `red_xdg_vnutri_dereva` даёт 1 с `otkaz-ne-predjavlen-prjamoj`.
   Честная форма даёт 0; внутри действительно исполнены прямой и симлинковый
   входы, оба отвергнуты с rc=1 без роста дерева. Это доказательство названных
   входов, не заявление о полном переборе всех возможных путей.
2. **Код отказа — закрыт.** `m1` на `red_scratch_vnutri_dereva` даёт 1 с
   `uspekh-vmesto-otkaza`; причиной служит именно rc=0 workshop при напечатанном
   отказе. Честная форма даёт 0, workshop отвергает вход с rc=1.
3. **Миграция и продолжение — закрыт прежний обход.** Предъявление 6 сравнивает
   sha256 до/после mv и после первого запуска, проверяет единственный корень,
   внешний HOME и `--resume 02802808`. На исходном дереве — 1, на честной форме
   — 0. Дополнительно независимо испытаны две объявленные модели: первый старт
   удаляет старую игрушечную `.omp` → 1, `pervyj-zapisk-stjor-izmenil`;
   launcher не передаёт RESUME_ARGS → 1, `resume-ne-peredan`.
   Это проверка launcher и игрушечной церемонии с подставным omp; реальный
   запуск прежней живой стенограммы остаётся шагом 8 живой миграции, не выполнен
   критиком вместо владельца.

## Советы круга 1 и остаточный совет

- Совет о родителе назначения и уже существующем `dev` закрыт текстом
  :235-240 и исполняемой церемонией пробы 6: создаётся именно `.../zones`,
  существующее назначение означает именованный отказ, не слияние. Честный
  миграционный вход исполнен; отдельную коллизию назначения критик не гонял.
- Совет о сессионном окружении принят конструктивно (:58-67), но доказательство
  передачи не закрыто: новый конкретный обход приведён выше. Это не повтор
  трёх прежних блокирующих причин, а обнаруженная неполнота нового предъявления.

СОВЕТ contracts/028-home-sessii-vne-dereva.md:243 — манифесты сортируются `sort -k2`, а `comm` требует сортировки всей строки. На модели утраты истории получено `comm: file 1 is not in sorted order`, после чего проба всё же правильно отказала по потерянным файлам. Согласовать порядок сортировки с командой сравнения и учитывать её код. Обход приёмки этой находкой не предъявлен, поэтому это совет, не основание FAIL.

## Независимая матрица и границы доказательства

Порядок столбцов: предъявления **1 / 2 / 3 / 6 / 4**.

| Модель | 1 | 2 | 3 | 6 | 4 |
|---|---:|---:|---:|---:|---:|
| bez-modeli | 1 | 1 | 1 | 1 | 0 |
| m1 | 1 | 0 | 1 | 1 | 0 |
| m2 | 0 | 0 | 1 | 0 | 0 |
| chestnaja | 0 | 0 | 0 | 0 | 0 |
| bez-peredachi, критик | 0 | 0 | 0 | 0 | 0 |

Первые четыре строки совпали с отчётом автора. Использован собственный
runner, клоны происходят от судимого e05d6fc, а не от рабочего дерева автора.
Патчи моделей и копии проб закоммичены внутри одноразовых клонов: вложенный
клон миграционной пробы получает именно модель, не исходный launcher.
HARNESS_SCRATCH/HARNESS_SESSION_HOME/XDG_STATE_HOME из среды судьи удалены.
Подставные omp и все дополнительные модели явно объявлены в исходниках.

Команды критика (обе rc=0):

    python3 /tmp/dev-harness-verify/critic-028-v2/repo/tmp/critic028_matrix.py
    python3 /tmp/dev-harness-verify/critic-028-v2/repo/tmp/critic028_migration.py

Сырой вывод и результаты сохранены вне клона:
`/tmp/dev-harness-verify/critic-028-v2/matrix-results.json`,
`migration-results.json`, `*-naslednik.log`, `*-migration.log` и пофайловые
логи всех строк матрицы. После коммита копии runner-скриптов сохраняются рядом.

Все пять обязательных вопросов рассмотрены:

- **Критерий слабее предмета:** конкретный зелёный обход передачи окружения
  воспроизведён; три прежних обхода различаются усиленными предъявлениями.
- **Доказуемость командами:** rc-команды и пробы предъявлены; существенное
  расхождение между обещанным наблюдением и командой — первая находка.
- **Решения, оставленные исполнителю:** источник пути, hash8, порядок передачи,
  отказ канонического результата и церемония назначения теперь названы.
  Проблема передачи — в доказательстве, не в отсутствии решения.
- **Границы:** `ЗОНА`-строки :131-136 неизменны относительно круга 1;
  заявленные файлы покрыты architect/implementer/orchestrator, судебные роли
  имеют свои каталоги. Разовая миграция явно отдана владельцу/оркестратору вне
  git. Расширения не-целей нет; раннер-скратч, Н-89 и чистка чужих зон не включены.
- **Норма:** буквальное противоречие Н-39 указано отдельно. Перенос durable
  сессионного состояния не отменяет правило 16 для временного scratch.

Carve-out :642-647 не расширен и не сужен. `git diff --exit-code 4be21b9
 e05d6fc -- scripts/check_no_leak.sh scripts/verify_antiplacebo.sh AGENTS.md`
вернул 0; блобы попарно совпали, включая весь check_no_leak
`436eca98c70b478d689588389da7073b43b7d5b6`. Не-цель :262-264 сохранена.
Регресс-пара независимо прошла во всех пяти строках: оба evil-пути ловимы
поимённо, пустая игрушка чиста, объявленная копия с `.zones/*` не видит утечки.

Живую канарейку повторно не запускал: раскрытие о самотрипе Н-98 и разрешении
владельца принято; текущий красный живой результат не перепроверял.
Общепроектные тесты и CI не запускались, критик не связан текущим CI-гейтом.

Три прежние блокирующие причины закрыты, но заморозка пока запрещена новыми
находками выше. Повторного FAIL по прежней блокирующей причине нет. Следующий
круг по этому предмету будет третьим: по AGENTS.md:134-145 перед ним требуется
арбитр (роль `arbiter`), а не ещё один обычный круг критика без арбитража.
