FAIL

Предмет: `contracts/028-home-sessii-vne-dereva.md` на HEAD `fbc172e`.
Суд проведён в собственном клоне `/tmp/dev-harness-verify/critic-028-v1/repo`.
Контракт закоммичен; предмет, rc-команды, исполнители и шесть буквальных
`ЗОНА`-строк присутствуют. Набор артефактов полный. Реализация не судилась.

## Блокирующие находки

БЛОКИРУЕТ contracts/028-home-sessii-vne-dereva.md:58 — утверждение о внешности дефолтного XDG-пути не обеспечено ни формулой :44, ни приёмкой :114-123. `XDG_STATE_HOME` может быть внутри чекаута или разрешаться туда через симлинк. Проверка только явного `HARNESS_SCRATCH` этого не исключает. Более того, буквальное ограничение :62 касается только `<корень>/.zones`, хотя предмет — отсутствие рантайм-писателей во всём судимом дереве.
ОБХОД: реализация правильно обслуживает внешний `HARNESS_SCRATCH`, отказывает явному внутреннему, но для дефолта дословно использует формулу :44 без проверки разрешённого пути. Все предъявленные входы проб 1–2 проходят; регресс-пара 3 не зависит от разрешения HOME; канарейка 4 на текущем внешнем дефолте проходит. При `env -u HARNESS_SCRATCH XDG_STATE_HOME=<корень>/state ./workshop` тот же лаунчер создаёт `<корень>/state/dev-harness-sessions/<hash>/zones/dev/session-prompt-*`, затем направляет туда рантайм. `<корень>/.zones` отсутствует, но причина Н-98 сохранена под другим именем.
Доказательство: в клоне заменён только блок ZONE одноразовой моделью этой реализации. Авторская проба 2 вернула 0. Следующий запуск того же лаунчера с внутренним `XDG_STATE_HOME` и объявленным PATH-подставным omp вернул 0 и создал `state-inside-028/dev-harness-sessions/4443133d/zones/dev/session-prompt-orchestrator.md` внутри клона. `.zones` в корне не возник. Это не предположение о поведении будущего omp: запись промпта уже совершил сам workshop.
Закрытие: инвариант и команда должны различать внешний и внутренний канонический результат разрешения HOME независимо от источника значения; не ограничивать наблюдение прежним именем `.zones`. Достаточно усилить предъявление, постоянный `case_*` для этого не обязателен.

БЛОКИРУЕТ contracts/028-home-sessii-vne-dereva.md:111 — заявленный `rc≠0` отказа не проверяется названной командой. В предъявленном `red_scratch_vnutri_dereva.sh` значение `RC` только включается в сообщения; среди проверок нет ненулевого кода workshop.
ОБХОД: ветка внутреннего `HARNESS_SCRATCH` печатает «внутри стерегомого дерева» и делает `exit 0`, ничего не создавая и не запуская omp; остальные ветки реализованы правильно. Проба 1 возвращает 0, остальные предъявления этой ветки не касаются. Вызывающая автоматизация получает успех вместо обязательного именованного отказа.
Доказательство: отдельная модель этой ветки из `printf` и `exit 0` дала авторской пробе 1 код 0 и дословный результат `отказ «внутри стерегомого дерева» предъявлен до создания/подмены/exec (workshop rc=0)`.
Закрытие: код предъявления обязан проверять код отказа, а не только его текст и отсутствие побочных эффектов.

БЛОКИРУЕТ contracts/028-home-sessii-vne-dereva.md:46 — непрерывность `--resume` и сохранность ВСЕХ старых трейсов/agent.db объявлены предметом (:69-71, :150-159), но ни одна команда приёмки не отличает перенесённую историю от заново созданной. `models_actual.sh` проверяет последнюю сессию; наличие новой корректной сессии не доказывает сохранность прежних.
ОБХОД: перенос выполняется через `mv`, однако первый старт нового HOME пересоздаёт каталог истории/БД и начинает новую сессию, теряя прежние трейсы. Пробы 1–2 используют клон без старой истории и проходят; проба 3 не затрагивает миграцию; живая канарейка видит внешних писателей и проходит; `omp config path` показывает внешний путь, `models_actual.sh` успешно проверяет новый трейс. Старый идентификатор `--resume` уже недоступен, улики утрачены, все названные проверки зелёные.
Закрытие: назвать rc-проверяемое сохранение существующего состояния до/после миграции и первого запуска, включая доступность прежней сессии для продолжения. Одного слова `mv` и успешного контроля новой сессии недостаточно. Это пробел критерия, не утверждение, что будущая реализация уже удаляет файлы.

## Советы

СОВЕТ contracts/028-home-sessii-vne-dereva.md:153 — последовательность `mkdir`/`mv` не создаёт родителя назначения `.../<hash8>/zones`. На свежем внешнем корне указанный `mv ... .../<hash8>/zones/dev` откажет. Указать создание именно родителя назначения и поведение при уже существующем `dev`; иначе церемонию придётся дописывать исполнителю. Сам по себе этот отказ не является обходом приёмки и вердикт не блокирует.

СОВЕТ contracts/028-home-sessii-vne-dereva.md:47 — «побайтово тот же блок» ещё не означает одинаковый результат у потребителей: `models_actual.sh`, запущенный из сессии, наследует уже подменённый HOME. При незаданном `XDG_STATE_HOME`/`HARNESS_SCRATCH` повторная формула от `$HOME/.local/state` вычислит другой корень. Назвать передачу разрешённого корня потребителю и предъявить запуск из сессионного окружения; одна проверка из внешней оболочки эту развилку не разрешает.

## Остальные обязательные вопросы и границы

- Критерий слабее предмета: конкретные обходы приведены выше; именно они запрещают заморозку. Требования к сохранности и дефолтному пути нельзя заменить проверкой одной внешней ручки.
- Доказуемость rc-командами: предъявления существуют и исполнимы; найдены конкретный непроконтролированный код отказа и отсутствие наблюдения прежней истории. Проба 4 не запускалась повторно на живом дереве: раскрытый самотрип Н-98 и свежий авторский результат 738 с / rc 1 приняты как входные данные, не переизмерялись.
- Оставленные исполнителю решения: внутренний дефолт, окружение потребителя и точная церемония назначения описаны в находках. Изменять замороженные именные исключения ради этих вопросов не требуется.
- Границы исполнения названы: implementer покрывает `workshop`, `scripts/models_actual.sh`, `config/ci_parity_exceptions.txt`, `fixtures/models_actual/`, `.gitignore`; architect — новые предъявления в `fixtures/workshop/` и `fixtures/check_judge_gate/`; orchestrator — только перечисленные уставные файлы церемонии. Исполнители суда имеют отдельные каталоги вердиктов. Разовая миграция явно отдана владельцу/оркестратору вне git. Пробела зон для названных правок не найдено.
- AGENTS.md: durable-состояние сессии не является одноразовым временным файлом правила 16; обоснование отказа от `/tmp` дано замером tmpfs и объёмом улик, не предпочтением. Carve-out 011 и замороженные `check_no_leak.sh`/`verify_antiplacebo.sh` менять не требуется. Н-89, жнец Н-97 и worktree-дефект Н-95 не втягиваются в предмет.

По коду предъявлений 1–3 проверены входы и наблюдаемые дефекты, а не только
подписи контрактной прозы: внутренний HARNESS_SCRATCH; внешний HOME у
PATH-подставного omp; два evil-пути против живого детектора и sed-копии с
расширенным пропуском. Подставной omp и sed-копия явно объявлены в исходниках.
Проба 3 сохраняет ловимость evil-класса поимённо; перенос не требует расширения
или сужения четырёх замороженных именных паттернов :642-647.

Различение «прямые red/canary-предъявления, не новые постоянные case_*»
приемлемо: предмет — лаунчер и живая церемония, не расширение барьера
мета-прогона. Наличие rc-команд и воспроизводимых входов важнее имени файла;
названные выше пробелы можно закрыть в том же формате. Перевод существующей
фикстуры `models_actual` на новую ручку отдельно предусмотрен инвариантом 7.

## Проверка этого вердикта

Выполнен одноразовый `python3 /tmp/dev-harness-verify/critic-028-v1/prove.py`:
код 0; внутри — проба 1 на модели успешного отказа: 0; проба 2 на модели
непроверенного дефолта: 0; запуск этой же модели с внутренним XDG: 0,
создание промпта внутри дерева наблюдалось. Модели меняли только одноразовый
клон; исходный workshop в нём восстановлен скриптом. Контракт и пробы автора
не редактировались. Полный набор приёмки на модели не запускался; обходы
остальных входов обоснованы их прочитанным кодом, не выданы за полный прогон.

Прочитаны frontier `agent://ArchN98Frontier`, отчёт `agent://Arch028Draft`,
Н-98/Н-97/Н-95, AGENTS.md, указанные участки workshop/models_actual/check_no_leak
и четыре staged-предъявления. Общепроектные проверки не запускались.
Первый круг по 028; повторной спорной причины для арбитража нет.
