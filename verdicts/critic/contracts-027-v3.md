accept

# Контракт 027, v3 — мини-круг СПАСЕНО

Предмет: только строка `СПАСЕНО orchestrator` в `contracts/027-doc-priemka.md:227`, закоммиченная в `f7c61830311d44bcf36bbc008ab4445fd9b0941d`. Судимый HEAD — `cc4e03ce3055dc059078d442c94aeebe25740d0c`; собственноручный одноразовый клон — `/tmp/dev-harness-verify/critic-027-v3/repo`. Относительно `frozen/contracts/027/2` дифф контракта содержит только добавление этой строки. Реализация doc-приёмки и весь прежний контракт заново не принимаются.

Набор артефактов полный: предмет (`:9–26`), именованные исполнители и зоны (`:224–235`), приёмочные инварианты и rc-команды (`:243–272`) присутствуют в закоммиченном контракте. Полный CI не запускался: это пред-адверсарный мини-круг. Раскрытия из задания приняты: check_no_leak на живом дереве не завершается из-за TOCTOU с composer-писателем; снимок до волны имел rc=0. Этот незавершающийся прогон не повторялся и успешным текущим измерением не объявляется.

## Пять вопросов в границе дельты

1. **Критерий не ослаблен.** `contracts/027-doc-priemka.md:227` не меняет инварианты либо rc-команды приёмки. Строка исключает точно один уже существующий административный коммит, а не разрешает будущую работу без реализации.
2. **Мера исполнима и предъявлена.** `bash scripts/check_zones.sh <клон>` действительно читает строку из блоба временной высшей заморозки. Ниже приведены фактические коды и именованные результаты контрольного эксперимента и контрпримеров; наличие строки само по себе не принято за доказательство.
3. **Решение не передано исполнителю.** На `:227` заданы имя `orchestrator`, единственный полный SHA `75f5ffeda43e90983335790c16de0c6690856e23` и непустая причина. Этот SHA — v2-расширение зоны implementer двумя оболочками, ранее принятое вердиктом `ad045ddc59b221efafc93fff928a44aab6151074`. Прецедент административного orchestrator-канала назван в `contracts/026-zhnec-tmp.md:72`; слово владельца 2026-09-17 задано поручением и отражено в изменяющем коммите. Строки СПАСЕНО в 026 и 027 побайтно совпадают.
4. **Границы остаются механическими.** Автор уже объявлен ЗОНА-строкой в заморозке 027 v2; в текущем тексте это `:226,234`. Оба `git merge-base --is-ancestor` — от `frozen/contracts/027/1` к названному SHA и от SHA к судимому HEAD — завершились rc=0. `git diff-tree --no-commit-id --name-only -r 75f5ffeda43e90983335790c16de0c6690856e23` вернул единственный путь `contracts/027-doc-priemka.md`. Новых путей либо будущих SHA строка не разрешает.
5. **Противоречия правилу 11 AGENTS.md в дельте нет.** Сообщение самого изменяющего коммита `f7c6183` содержит в первой колонке `РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/027-doc-priemka.md СПАСЕНО 75f5ffe — слово владельца 2026-09-17 (П1-развилка Н-100: разрешение остатка union-фикса)`. Формат соответствует существующему разбору `%B` в `check_charter.sh:134–165`. Подлинность прямого слова владельца при одном uid не выдаётся за механически доказанную; это заданное основание данного круга, а не самостоятельно выданное разрешение критика.

## Исполненные контрольные эксперименты

Команда во всех случаях — `bash scripts/check_zones.sh /tmp/dev-harness-verify/critic-027-v3/repo`. Скрипт барьера не изменялся; изменяемые блобы и теги существовали только в одноразовом клоне.

- **Только `frozen/contracts/027/3` на судимом HEAD:** rc=1, но строка распознана без грамматических отказов, с точным сообщением `ok   контракт 027: коммит 75f5ffed (orchestrator) — СПАСЕНО, из суда зон выведен`. В выводе остался один отказ для `75f5ffed`; диагностика union-зон не содержит номера окна, поэтому номер не приписывается самой строке отказа. Это не заявление о глобальной зелёности.
- **Парный положительный контроль:** временные теги `frozen/contracts/026/4` и `frozen/contracts/027/3` на том же HEAD → rc=0; вывод отдельно называет СПАСЕНО в окнах 026 и 027. Таким образом, оставшееся красное первого опыта закрывается второй предусмотренной заморозкой, а не подавлением диагностики.
- **Чужой существующий полный SHA вне диапазона:** в замороженном пробном блобе 027 единственный SHA заменён на `3782fddfbe5ccf1a90c4f873f9de1fa509463702` (`frozen/contracts/027/1^`). Rc=1 и точный диагноз: `строка СПАСЕНО в contracts/027-doc-priemka.md называет коммит «3782fddfbe5ccf1a90c4f873f9de1fa509463702» вне диапазона контракта (frozen/contracts/027/1..HEAD)`. Отказ установлен по этой диагностике, не по одному общему rc=1.
- **Неполный SHA:** замена на `75f5ffe` → rc=1 с диагнозом `«75f5ffe» — не полный 40-символьный hex`.
- **Другой коммит того же автора:** при исходной строке и теге 027/3 добавлен контрольный commit `02ab5ac4…` автора orchestrator с единственным новым путём `critic027-outside-zone.txt`. Rc=1, этот SHA и путь именованно отвергнуты как `коммит вне зоны`; старый `75f5ffed` по-прежнему отдельно распознан как СПАСЕНО для 027. Исключение не распространилось на нового соседа в истории.

После опытов HEAD клона восстановлен, временные frozen-теги удалены. Основной репозиторий опытами не изменялся; настоящая заморозка этим вердиктом не выполнялась.

## Неблокирующее уточнение границы

СОВЕТ contracts/027-doc-priemka.md:227 — формулу «не индульгенция на прочие пути коммита» следует понимать точно: `check_zones.sh:328–332` исключает **целый именованный коммит** в соответствующем окне, а не отдельный путь. У проверенного immutable SHA единственный путь — административная правка данного контракта; иных путей в нём нет, а добавить их без изменения SHA невозможно. Поэтому для предъявленной строки обхода не возникает. Более сильное общее обещание о попутевых исключениях для произвольного будущего СПАСЕНО этим механизмом не доказано и здесь не принимается.

Блокирующих находок и действительного обхода, внесённого судимой дельтой, не найдено. Заморозка 027 v3 разрешена; это не принятие реализации и не результат полного CI.
