FAIL

# Контракт 025 — пятый круг критика: СПАСЕНО и советы v4

Судимый HEAD: `fbb53d53b5b7bf19cf7765ec8f694ae000bee8c4`, установлен командой
`git rev-parse HEAD` в основном репозитории. Клон `tmp/critic/025-v5b/repo` снят с него.
Предмет — только дельта `frozen/contracts/025/4..HEAD -- contracts/025-sreda-cwd-rc.md`:
СПАСЕНО, §B-1 и Р3. Вердикт v4 прочитан целиком первым. Реализация не пересуживалась.

Набор артефактов полон: текст закоммичен (`eba56712`, merge `fbb53d53`), предмет и
rc-критерии И-1…И-6 присутствуют, исполнители и границы объявлены ЗОНА-строками
`:399-403`. Полнота набора не означает законность изменяющего устав коммита.

## Блокирующая находка

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:199-204,405,445-453 — правка замороженного
уставного документа внесена merge-коммитом `fbb53d53` без строки разрешения владельца.
`AGENTS.md:149-153,237-240` требует её в изменяющем коммите. Тело `eba56712` её содержит;
тело `fbb53d53` содержит только `land: wip/025/architect`. Команда
`git diff --stat fbb53d53^1 fbb53d53 -- contracts/025-sreda-cwd-rc.md` показывает
изменение файла этим merge: 16 добавлений, 9 удалений. Обязательный прогон
`bash scripts/check_charter.sh` в клоне вернул **rc 1**, не ожидаемый 0:

```
FAIL уставной документ изменён без разрешения владельца: contracts/025-sreda-cwd-rc.md в fbb53d53 — тело коммита обязано нести строку «РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/025-sreda-cwd-rc.md <причина>» в первой колонке
ok   уставной документ изменён с разрешения владельца: contracts/025-sreda-cwd-rc.md в eba56712
```

Это исключение порога — противоречие норме `AGENTS.md`; ОБХОД не требуется.
Вердикт не пишет разрешение за владельца и не предписывает переписывание истории.

## СПАСЕНО: грамматика верна, практическая цель не достигнута

Независимо проверено:

- `:405` имеет формат `СПАСЕНО architect: <40-hex> — <непустая причина>`.
- `32d580c720058175ffde95a9aa117f15b574a711` существует как commit; автор — `architect`.
- `architect` объявлен ЗОНА-строкой 025 (`:399`). Единственный изменённый `32d580c7`
  путь — `contracts/025-sreda-cwd-rc.md` — лежит в этой собственной зоне.
  Это не выход автора за границы его работы по 025.
- `git merge-base --is-ancestor frozen/contracts/025/1 32d580c7` и
  `git merge-base --is-ancestor 32d580c7 HEAD` дали rc 0; `git log` по диапазону
  `frozen/contracts/025/1..HEAD` также содержит полный хеш.
- Названный в причине accept `cb980b9` соответствует прочитанному вердикту v4.

СОВЕТ contracts/025-sreda-cwd-rc.md:405 — СПАСЕНО в 025 не снимает отказ в окне 024.
Для обязательного эксперимента **только в одноразовом клоне** создан пробный тег
`frozen/contracts/025/5` на `fbb53d53`, затем выполнен
`bash /home/aka/Documents/dev-harness/tmp/critic/025-v5b/repo/scripts/check_zones.sh`.
Получен **rc 1**, с двумя одновременными строками:

```
FAIL коммит вне зоны: architect 32d580c7 contracts/025-sreda-cwd-rc.md — зона контракта 024: contracts/024-detektor-utechek-osnovnogo-chekauta.md fixtures/check_judge_gate/ NABLIUDENIA_ARCHITECT.md
ok   контракт 025: коммит 32d580c7 (architect) — СПАСЕНО, из суда зон выведен
```

Читатель `scripts/check_zones.sh:236,330` сопоставляет тройку `(автор, хеш, контракт)`.
Грамматика новой строки принята механизмом, но отказ 024 остался. Заморозку v5 нельзя
выдавать за починку этого отказа. По порогу роли это СОВЕТ: предъявлен сохраняющийся
красный гейт, а не обход объявленных rc-критериев с ложным зелёным. Пробный тег не
является разрешённой заморозкой и в основной репозиторий не переносится.

## Советы v4: три закрыты, четвёртый остался

1. Сужение невозможности различения — закрыто (`:199-202`). Теперь речь о двух
   носителях 025; иной PIPESTATUS-aware носитель назван кандидатом, не реализованной
   гарантией. Источник обозначен честно: «СОВЕТ критика v4».
2. Синхронизация Р3 — закрыта (`:445-453`): РАСШИРЕН, охватывает любой `$?=141`,
   включая SIGPIPE вне пайпа, и отсылает к исправленной §B-1. Это тот же остаток,
   а не новая гарантия.
3. Детектор 024 снят из ловцов rc-остатка **в обоих местах** (`:203-204,450-451`).
   Ловцом остаётся маркер B-2; настоящая область детектора — утечка записи.
4. Метка редакции не исправлена: §B-1 по-прежнему `respec v4` (`:145`); новая
   шапка Р3 её повторяет (`:445`). Полный diff не меняет строку `:145`.
   Утверждение «четыре совета исправлены» деревом не подтверждается: закрыты три
   из четырёх текстовых советов, а не четыре за счёт двух мест одного совета.

СОВЕТ contracts/025-sreda-cwd-rc.md:145,445 — синхронизировать метку редакции либо
явно отделить историческую метку respec от номера новой заморозки. Это прежний
совет, не повтор блокирующей причины; сам по себе accept не отменяет.

СОВЕТ contracts/025-sreda-cwd-rc.md:200-202 — кандидат различает контекст
пайп/не-пайп, но не причины SIGPIPE внутри пайпа: `red_pipefail_prefiks.sh:54`
проверяет число элементов `PIPESTATUS`. Нынешнее упоминание не создаёт новой
обязанности реализации; ограничение стоит сохранить в следующем уточнении,
чтобы не приписать кандидату полное различение.

## Пять обязательных вопросов к дельте

1. **Критерий слабее предмета?** Гарантии `false | true` → ошибка у MAIN и CHILD,
   `yes | head -1` → успех, `grep -q` → 0, `exit 7` → 7 (`:187-192`) неизменны.
   И-2 и И-6 не тронуты. Нового ложнозелёного обхода дельтой не предъявлено.
   Ограниченный эффект отдельного объявления СПАСЕНО измерен выше.
2. **Готово недоказуемо командой?** Новый пункт «готово» не добавлен; rc-команды
   прежние (`:296-302,328-351`). Их живой прогон здесь не заявляется. Отдельно
   выполнены предписанные прогоны читателей зон и разрешений с фактическими rc 1.
3. **Решение оставлено исполнителю?** Новый носитель не раздаётся: кандидат прямо
   не реализован и не заявлен двумя носителями. Р3 синхронизирован. Ограничение
   кандидата и недостигнутая цель спасения отмечены советами, не новой раздачей.
4. **Границы исполнения?** ЗОНА-строки `:399-403` неизменны; текущая правка и путь
   спасаемого коммита принадлежат `architect`. Границы держит `check_zones.sh`,
   читающий замороженный блоб; поэтому эксперимент использовал пробный локальный
   тег, а не выдавал текст HEAD за уже действующее исключение.
5. **Противоречие AGENTS.md?** Есть: разрешение не записано в изменяющем merge;
   это подтверждено `check_charter.sh` и является единственной блокирующей находкой.

## Проверки и предел доказательства

- Полный diff v4..HEAD прочитан: три хунка — §B-1, СПАСЕНО, Р3.
- `git diff --exit-code frozen/contracts/025/4 HEAD -- .omp/extensions/rc-prefix.ts
  .omp/extensions/exit-marker.ts fixtures/check_runner_hygiene/red_pipefail_prefiks.sh
  fixtures/check_runner_hygiene/probe025_dochernij_vector.sh scripts/check_zones.sh
  scripts/check_charter.sh` → rc 0, пустой вывод. Доказана неизменность этих файлов,
  а не заново измерено поведение живых сессий.
- `check_charter.sh` → rc 1 на исходном клоне; `check_zones.sh` → rc 1 на том же
  HEAD с пробным локальным тегом v5. CI, полный раннер и живой И-6 не запускались:
  предмет — текст, не реализация; эти запуски не выдаются за сделанные.
- В основном реестре при снятии клона максимум заморозки 025 — 4;
  `freeze_contract.sh:108-121` поэтому требует `contracts-025-v5.md`.
- `verdicts/arbitration/kap-krugov-zamorozka-025.md` на HEAD имеет первую строку
  с `(контракт 025)`. Читатель капа (`freeze_contract.sh:207-220`) ищет этот маркер,
  не свежесть. Это факт механизма, не новое разрешение арбитра на v5.

Новая блокирующая причина не повторяет причины FAIL прошлого круга; нового спора
по повторной причине нет, арбитр этим вердиктом не созывается. Старые советы не
превращены в блокеры. До устранения уставного препятствия заморозка запрещена.
