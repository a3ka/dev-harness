accept

# Контракт 025 — v5: узкая перепроверка разрешения в merge

Судимый HEAD: `542c7bca847cf77af0d42a97898c3dddc87e5cf6`, установлен
`git -C /home/aka/Documents/dev-harness rev-parse HEAD`. Проверка выполнена в
собственном клоне `tmp/critic/repo-025-v5c`. Перезаписывается тот же вердикт v5;
теги заморозки этим кругом не меняются.

## Единственная причина FAIL устранена

Предыдущий анализ: `542c7bca:verdicts/critic/contracts-025-v5.md` (прочитан целиком).
Он судил `fbb53d53` и блокировал только отсутствие разрешения владельца в merge,
меняющем замороженный `contracts/025-sreda-cwd-rc.md:199-204,405,445-453`.

В текущей истории merge — `0fcf1817dfd1e2bbd0b9cdc0a7305f419675be04`.
`git merge-base --is-ancestor 0fcf1817dfd1e2bbd0b9cdc0a7305f419675be04 HEAD`
завершился rc 0. `git log -1 --format=full 0fcf1817dfd1e2bbd0b9cdc0a7305f419675be04`
показывает `land: wip/025/architect` и следующую строку тела коммита:

```
РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/025-sreda-cwd-rc.md merge несёт устав-дельту v5 (SPASENO+советы, eba5671) — тот же грант владельца 2026-09-13, что и в eba5671/32d580c/bbfadd0; строка нужна ЗДЕСЬ по прецеденту Н-79 (merge сам меняет уставной файл)
```

Повторный обязательный прогон в клоне:

```
bash /home/aka/Documents/dev-harness/tmp/critic/repo-025-v5c/scripts/check_charter.sh
```

Результат — rc 0; существенные строки вывода:

```
  ok   уставной документ изменён с разрешения владельца: contracts/025-sreda-cwd-rc.md в 0fcf1817
  ok   уставной документ изменён с разрешения владельца: contracts/025-sreda-cwd-rc.md в eba56712
  ok   contracts/025-sreda-cwd-rc.md — уставной с frozen/contracts/025/1, коммитов в диапазоне 119, изменений без разрешения нет

уставных документов: 26 · изменений в них: 74 · с разрешения: 74
```

Противоречие правилу 11 `AGENTS.md`/Н-79 снято: разрешение находится именно в
изменяющем устав merge, а не только в его родителе. Вердикт разрешения не добавлял.
Действительных блокирующих находок не осталось.

## Сохранённый анализ и границы перепроверки

Полнота набора, предмет, rc-критерии и исполнители с ЗОНА-строками
`contracts/025-sreda-cwd-rc.md:399-403` установлены предыдущим кругом.
Основание переноса анализа — пустой вывод и rc 0 команды:

```
git diff --exit-code eba56712 HEAD -- contracts/025-sreda-cwd-rc.md .omp/extensions/rc-prefix.ts .omp/extensions/exit-marker.ts fixtures/check_runner_hygiene/red_pipefail_prefiks.sh fixtures/check_runner_hygiene/probe025_dochernij_vector.sh scripts/check_zones.sh scripts/check_charter.sh
```

Это тот же закоммиченный текст и проверявшие его механизмы. Пять обязательных
вопросов закрываются прежним анализом, кроме заново проверенного пункта 5:

1. Критерий не ослаблен относительно предмета; нового ложнозелёного обхода дельтой
   не предъявлено — прежний раздел «Пять обязательных вопросов», пункт 1.
2. Нового недоказуемого пункта «готово» не добавлено; rc-команды неизменны — пункт 2.
3. Новый носитель исполнителю не оставлен; кандидат не заявлен реализацией — пункт 3.
4. Исполнители и зоны неизменны; границы держит `check_zones.sh` — пункт 4.
5. Единственное установленное противоречие `AGENTS.md` устранено указанным выше
   merge и подтверждено свежим прогоном `check_charter.sh`.

§B-1, грамматика СПАСЕНО и Р3 с нуля не пересуживались. Реализация, живой И-6,
CI и `check_zones.sh` повторно не запускались; старые результаты не выдаются за
свежие прогоны. Несвязанная дельта 024 не является предметом этого вердикта.

## Сохранённые неблокирующие советы

СОВЕТ contracts/025-sreda-cwd-rc.md:405 — СПАСЕНО в 025 само по себе не снимает
отказ суда зон 024. Известный пункт обрабатывается отдельно зеркальной строкой
в контракте 024 и кругом `verdicts/critic/contracts-024-v3.md` (по заданию).
Здесь его завершение не заявляется; оснований блокировать им 025 не добавилось.

СОВЕТ contracts/025-sreda-cwd-rc.md:145,445 — прежний совет о метке `respec v4`
сохраняется: отделить историческую редакцию носителя от номера новой заморозки
либо синхронизировать метку. Сам по себе accept не отменяет.

СОВЕТ contracts/025-sreda-cwd-rc.md:200-202 — прежнее ограничение PIPESTATUS-aware
кандидата сохраняется: различение контекста пайп/не-пайп не равно различению
причин SIGPIPE внутри пайпа. Новой обязанности реализации текст не создаёт.

Заморозка v5 разрешена этим вердиктом; остальные механические условия заморозки
он не подменяет. Повторного FAIL и нового спора нет, арбитр не созывается.
