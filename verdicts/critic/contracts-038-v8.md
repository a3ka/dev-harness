accept

# Контракт 038 — подтверждение исполнения арбитража, v8

Предмет: исполнение П1–П3 решения `78785ec` из
`verdicts/arbitration/contracts-038-head-granica-dokumentacija.md` в
`contracts/038-provodka-done-gejt.md` на закоммиченном
`0c6ebe298b7dc02513217925dc717a324e583bf7` основного репозитория.
Независимый клон: `/home/aka/Documents/dev-harness/tmp/critic/repo`.
Это суд текстовой дельты, НЕ повторная приёмка реализации 038.

Набор полный: предмет (:85–108), критерий готовности (:403–531), исполнимые
команды (:480–531), именованные исполнители и ЗОНА-строки (:361–400)
присутствуют. Судимый текст взят из клона закоммиченного HEAD, не из переписки.

## Соответствие обязательным пунктам

1. **П1 исполнен** — `contracts/038-provodka-done-gejt.md:160–164`.
   Указана двухаргументная форма
   `check_provodka.sh <корень> <отн-путь-контракта>`; источник — диск по
   канонизированному корню (`cd <корень> && pwd -P`), без сверки с HEAD и
   чтения git-объектов. В :190–194 граница повторена для всех каналов.
   Коррекция не выдаётся за изменение реализации.
2. **П2 исполнен** — `contracts/038-provodka-done-gejt.md:190–200`.
   Вместо ложной передачи гарантии вызывающему прямо сказано: гарантия
   закоммиченности и байтового совпадения role/charter-целей сейчас
   «НЕ ЗАКРЫТА НИКЕМ». Сверка самим done-писателем ограничена файлом
   контракта. Незакоммиченная норма не объявлена допустимым ожидаемым
   результатом пайплайна; разрыв назван остатком, не новой нормой (:255–260).
3. **П3 исполнен** — `contracts/038-provodka-done-gejt.md:246–260`.
   В §Инварианты 4 есть именованный implementer-остаток «ПРОВОДКА-цели на
   HEAD», явно «ЕЩЁ НЕ РЕАЛИЗОВАНО». Названы носитель `done_contract.sh`,
   расширение байтовой сверки шага 4 (`git hash-object` против `HEAD:<путь>`),
   обе категории целей — `roles/<роль>.md` и `AGENTS.md` — и отказ без тега
   при расхождении. Различающий вход будущей реализации определён:
   норма только в рабочем дереве → done-гейт rc1. Переписывание барьера на
   git-объекты и реализация остатка в этой текстовой правке не требуются.
   Исполнитель и пути уже покрыты :364 и :387.

Причина FAIL v6/v7 закрыта именно предписанной арбитром заменой. Новых
блокирующих причин в рассматриваемой дельте не выявлено; новых находок нет.
Нереализованный, но прямо предписанный арбитром остаток не используется как
повод для третьего FAIL по прежней HEAD-границе.

## Собственные проверки

Все команды ниже исполнены в указанном клоне на судимом `0c6ebe2`.

1. `git diff --stat 78785ec HEAD -- contracts/038-provodka-done-gejt.md`
   → rc0:

   ```text
    contracts/038-provodka-done-gejt.md | 53 ++++++++++++++++++++++++++++++++++++-
    1 file changed, 52 insertions(+), 1 deletion(-)
   ```

   Это область указанного pathspec. Независимое перечисление
   `git diff --name-only 78785ec HEAD` → rc0 показывает два пути:
   `HANDOFF.md` и сам контракт. Поэтому утверждение «во всём диапазоне
   изменён единственный файл» здесь НЕ делается; HANDOFF не предмет суда.

2. `bash scripts/check_provodka.sh . contracts/038-provodka-done-gejt.md`
   → rc0, stdout/stderr пусты.

3. `bash scripts/verify_antiplacebo.sh --scope check_provodka` → rc0.
   Итог раннера:

   ```text
   барьеров: 1 · фикстур: 28 · предъявлено красным повторным прогоном: 28
   ```

   Это отчёт scoped-раннера, не независимая перепись и не полная приёмка
   проекта. Раннер для каждого предъявления сообщил зелёный контроль и
   повторный красный кодом 1; в том числе traversal, symlink и TOCTOU.

4. `git diff --exit-code 78785ec HEAD -- scripts/ fixtures/` → rc0,
   вывод пуст. Реализация и фикстуры относительно арбитражной базы не
   изменены; выполнение будущей HEAD-сверки этим вердиктом не утверждается.

5. Число вхождений «вызывающий» проверено с явным учётом двух регистров:

   ```sh
   jq -Rs '{exact_word: ([match("вызывающий|ВЫЗЫВАЮЩИЙ"; "g")] | length), with_instrumental: ([match("вызывающий|ВЫЗЫВАЮЩИЙ|вызывающим"; "g")] | length)}' contracts/038-provodka-done-gejt.md
   ```

   → rc0, `exact_word = 2`, `with_instrumental = 3`.
   Независимый поиск строк и чтение контекста дают:
   - :21 — цитата «несёт ВЫЗЫВАЮЩИЙ» под явным отрицанием «НЕ вводится»;
   - :22–23 — «ни один вызывающий … никогда не сверял HEAD»;
   - :197 — склонённое «и не вызывающим».
   Все три употребления отрицают наличие гарантии; положительного
   утверждения «гарантию несёт вызывающий» нет.

## Пять вопросов роли в границах этой дельты

1. **Критерий слабее предмета?** Нет для предъявленного предмета — точного
   исполнения П1–П3 текстом. Отложенный done-предикат сохранён требованием,
   а не заменён разрешением принимать незакоммиченную норму.
2. **Доказуемость командами?** Команды критерия сохранены, порученные scoped
   проверки исполнены выше. Они доказывают работоспособность существующей
   проводки, НЕ реализацию будущего остатка. Его реализация сейчас не
   предмет приёмки — это прямо установлено арбитражем.
3. **Молчаливые решения?** Источник чтения, незакрытая гарантия, носитель
   будущего исправления, охват role/charter и отказ без тега названы явно.
4. **Границы исполнения?** Текст — architect (:363), будущий done-писатель
   и его семейство — implementer (:364, :387), вердикт — critic (:366).
   Механизм границ — существующие ЗОНА-строки и `check_zones.sh`.
5. **Старшая норма?** Решение арбитра исполнено; новый гард эта дельта не
   вводит, требование 041 о модели угроз ретроактивно не применяется.
   Маршрут изменения замороженного текста остаётся прежним: v+1 и
   `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`. Суд до owner-подписи разрешён самим арбитражем;
   этот accept не заменяет подпись и не разрешает обход pre-push-гейта.

Контракт, исходники, фикстуры и теги критиком не изменены. Freeze/done и
полная проектная приёмка не запускались. Push не выполнялся; клон сохраняется.
