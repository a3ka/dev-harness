accept

# Контракт 026 — критик, круг 2

Судимый HEAD: `a111848decef71fb25de1c81c5b6a6ae2bf9bf00` — собственный `git rev-parse HEAD` в свежем клоне origin `ssh://git@github.com/a3ka/dev-harness.git`. Предмет — правка `4328695`, приземлённая `a111848`, против пяти блокеров вердикта `e1e431636379fc9d8507904246c2418bfa9a284e`. Набор артефактов полон: закоммиченные предмет, rc-приёмка, именованные исполнители и ЗОНА-строки. Заморозка v1 разрешена; тег этим судом не ставился.

Диапазон задания `e1e416..a111848` дал `bad revision`. По истории восстановлено точное основание `e1e4316`; прочитан полный `git diff e1e4316..a111848 -- contracts/026-zhnec-tmp.md fixtures/gc_agent_branches/`. Судится контракт и исполняемое предъявление критериев, не будущая реализация gc и не проза слоя 027.

## Закрытие Б1–Б5

1. **Б1 закрыт** — `contracts/026-zhnec-tmp.md:89-103,138-150`. Четыре red и три новых case физически находятся на HEAD, все семь имеют git-mode `100755`. `_zhnets.sh` — каркас, не восьмая проба. Каждая точная команда исполнена и вернула 1 с именованным отказом проверки поведения, а не 127 из-за отсутствующего файла. Свидетельства ниже. Прежнее противоречие норме красного предъявления ДО критика снято.
2. **Б2 закрыт** — `contracts/026-zhnec-tmp.md:99-103,105-136`. Операционные пары «стаб ↔ вход/ветвь» удалены из замораживаемой прозы. Остались инварианты, входы и rc-команды; привязки находятся в шапках исполняемых проб. Перенос подтверждён диффом и чтением проб; комментарии не выдаются за уже выполненный прогон всех будущих стабов.
3. **Б3 закрыт** — `contracts/026-zhnec-tmp.md:47-51,105-109`. `red_zhnets_reap_stale.sh:27-62` создаёт две записи один раз, снимает sha256 ДО вызова gc, вызывает dry-run, проверяет существование и исходные байты, затем вызывает apply на ТОМ ЖЕ корне и требует исчезновения обеих записей при сохранном tmp/. Обход «dry-run печатает список и удаляет» отвергается сохранностью до apply. На судимом HEAD прогон останавливается раньше, на отсутствии списка; прохождение последующих ассертов будущей реализацией здесь не заявляется.
4. **Б4 закрыт** — `contracts/026-zhnec-tmp.md:42-45,110-114`. `red_zhnets_aktiv_vyzhivaet.sh:27-54` строит `tmp/arch026-from021/` возрастом 40 дней, живой контракт 026 без done и done-тег 021. После apply требуются исходные байты этого входа и ещё двух защищённых записей, одновременно done-контроль `critic021v3` обязан исчезнуть. Обход «любой done раньше активного» различим именно на этом входе; no-op не удовлетворяет положительному контролю. Постоянный case повторяет этот контроль перед своим красным смешанным входом.
5. **Б5 закрыт как дефект контракта** — `contracts/026-zhnec-tmp.md:75-79,151-156`. Исключение «не блокер приёмки» удалено. До done обязательна правка существующего close-out в `roles/orchestrator.md`, её владелец orchestrator и зона названы. После done при отсутствии живых спавнов обязательно живое предъявление `bash scripts/gc_agent_branches.sh --root <живой корень> --tmp-reap-apply`: rc 0, подходящие записи присутствуют до и отсутствуют после, результат записывается в HANDOFF. Дерево с правильным CLI, но прежней dry-run-проводкой больше не удовлетворяет объявленной приёмке. Это разрешение заморозить обязательство, НЕ свидетельство уже выполненного close-out: живое удаление на основном чекауте я не запускал.

Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза контракта; контракт несёт инварианты + rc-команды».

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Прежние обходы Б3/Б4 отвергаются файловыми ассертами, Б5 исключён обязательностью проводки и живого предъявления. Нового действительного блокера в судимой правке против Б1–Б5 не установлено.
2. **Готовность недоказуема командами?** Семь rc-команд существуют и исполнены; для dry-run есть не только вывод, но и проверка байтов. Будущая приёмка требует их rc 0, зелёный scoped-метапрогон и отдельно живой close-out. Текущее красное не подменяется утверждением о готовности реализации.
3. **Решение молча оставлено исполнителю?** Прежние неоднозначности нормализации NNN и порядка смешанности сняты в `:29-45`: контроль смешанности следует за квалификацией; активный номер имеет приоритет. Формат строки вывода явно оставлен деталью реализации в `:201-205`, а не молчаливым решением.
4. **Границы исполнения названы?** Да, `:70-85`: architect — контракт/фикстуры, implementer — gc, orchestrator — close-out-норма/HANDOFF, судьи — собственные каталоги. Механизм — `check_zones.sh` по авторству. Оракул 024, раннер и AGENTS.md остаются вне зон; вызов gc оркестратором не означает правку чужого файла.
5. **Противоречие AGENTS.md?** Прежние нарушения Б1/Б2 сняты: предъявления закоммичены и красны, стаб-пары не заморожены в прозе. Счёт семи предъявлений независимо сверен `git ls-tree`, а не переписан из вывода той же батареи. В судимом тексте дельты нового противоречия не установлено.

СОВЕТ contracts/026-zhnec-tmp.md:151-156 — живое предъявление следует сохранять с конкретными именами и проверкой существования до/отсутствия после, не только с rc gc: сам rc 0 не означает, что на данном корне были кандидаты. Это уточнение формы доказательства уже обязательного наблюдаемого удаления, не возврат Б5 и не дополнительное условие заморозки.

## Исполненное доказательство

Все семь команд исполнены в свежем origin-клоне на судимом HEAD:

| Команда | rc | Наблюдаемый именованный отказ |
|---|---:|---|
| `bash fixtures/gc_agent_branches/red_zhnets_reap_stale.sh` | 1 | `TMP-РЕАП список не называет кандидата tmp/anon10d — реап не реализован (инвариант 4)` |
| `bash fixtures/gc_agent_branches/red_zhnets_aktiv_vyzhivaet.sh` | 1 | `apply вернул rc 1, ожидался 0`; gc: `неизвестный аргумент: --tmp-reap-apply` |
| `bash fixtures/gc_agent_branches/red_zhnets_tracked_netrogat.sh` | 1 | `ветка (а) apply rc 1, ожидался 0`; gc: `неизвестный аргумент: --tmp-reap-apply` |
| `bash fixtures/gc_agent_branches/red_regress_024_noga2.sh` | 1 | `apply вернул rc 1, ожидался 0`; gc: `неизвестный аргумент: --tmp-reap-apply` |
| `bash fixtures/gc_agent_branches/case_zhnets_reapstale.sh` | 1 | `список не называет кандидата tmp/anon10d` |
| `bash fixtures/gc_agent_branches/case_zhnets_aktiv_vyzhivaet.sh` | 1 | `apply rc 1, ожидался 0`; gc: `неизвестный аргумент: --tmp-reap-apply` |
| `bash fixtures/gc_agent_branches/case_zhnets_tracked_netrogat.sh` | 1 | `отказ не назван «смешанный вход» (инвариант 2)`; gc: `неизвестный аргумент: --tmp-reap-apply` |

`bash scripts/verify_antiplacebo.sh --scope gc_agent_branches` → rc 1. Сырой итог: `барьеров: 1 · фикстур: 5 · предъявлено красным повторным прогоном: 2`, `расхождений: 3`. Обе прежние case зелены; новые case соответственно отказали отсутствием положительного контроля, отсутствием предъявленного красного и неназванной причиной «смешанный вход» на повторе. Это предреализационное красное семейства, не зелёная приёмка. Четыре red не входят в case-глоб, поэтому семь прямых прогонов приведены отдельно.

`git diff --exit-code e1e4316..HEAD -- scripts/check_no_leak.sh scripts/verify_antiplacebo.sh scripts/gc_agent_branches.sh AGENTS.md fixtures/gc_agent_branches/case_zavisshaja_peredvinuta.sh fixtures/gc_agent_branches/case_zavisshaja_snesena.sh` → rc 0, без вывода. Оракул, раннер, gc, норма и прежние case не изменены этой правкой. Полный прогон не запускался: он принадлежит CI. Нового повторного FAIL нет, арбитр не требуется.

Клон первоначально создан в `tmp/critic/repo-026-v2`, затем сохранён в `/tmp/dev-harness-verify/critic026v2/repo`: самостоятельный перенос был запрещён стражем null-allowlist, операция передана основной сессии. Вердикт подготовлен в `/tmp/dev-harness-verify/critic026v2/contracts-026-v1.md` для церемонии hash-object → update-index → commit под critic → checkout только своего файла. Ограничение проверки чистоты ниже относится к церемонии, не к семи исполненным пробам контракта; зелёная чистота основного чекаута не заявляется.

стенограмма: check_no_leak --check /home/aka/Documents/dev-harness → rc не получен: таймаут 120с; повтор после переноса клона → rc не получен: таймаут 600с; оба без итогового вывода, чистота основного чекаута не подтверждена.
