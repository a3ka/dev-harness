FAIL

# Контракт 026 — критик, круг 1

Судимый HEAD: `77d530942de6d069e33778ef3901c7a27fd5520f` — собственный `git rev-parse HEAD` в свежем клоне origin `ssh://git@github.com/a3ka/dev-harness.git`. Прочитан контракт `contracts/026-zhnec-tmp.md` целиком. По существу судятся предмет, приёмка, исполнители/границы; остальная проза не блокирует. Заморозка v1 НЕ разрешена.

Формальный вход есть: закоммиченный предмет, критерий с командами, исполнители и ЗОНА-строки. Отсутствие исполняемых красных тестов ниже — нарушение специальной нормы кодового контракта, не отсутствие текста критерия.

## Блокирующие находки

БЛОКИРУЕТ contracts/026-zhnec-tmp.md:78-86 — Б1: «все 7 красны на текущем HEAD» не соответствует дереву. На HEAD нет ни четырёх новых red, ни трёх новых case. Это прямое противоречие `AGENTS.md:126-130`: тесты кодовой задачи предъявляются красными ДО критика, не описываются как будущая работа. Исключение порога — противоречие AGENTS.md, ОБХОД не требуется. Все четыре приёмочные команды реально запущены: rc 127, `No such file or directory`, не заявленный rc 1 от проверки поведения gc. Нужны закоммиченные поведенческие предъявления; отсутствие файла не является красным тестом.

БЛОКИРУЕТ contracts/026-zhnec-tmp.md:85-108 — Б2: проза утверждает пары «стаб ↔ вход/ветвь»: S-exclude/S-ageonly для первой пробы, S-ageonly/S-sweepall для второй, S-find для обеих ветвей третьей, S-overreach для четвёртой. `AGENTS.md:130-133` прямо запрещает это контракту: architect привязывает стабы по коду. Упоминание Н-39 в строке 86 нарушения не отменяет. Исключение порога — противоречие AGENTS.md, ОБХОД не требуется. В замораживаемом тексте остаются инварианты и rc-команды, доказанные привязки живут в исполняемом предъявлении.

БЛОКИРУЕТ contracts/026-zhnec-tmp.md:42-44,112-116 — Б3: dry-run не доказан проверкой сохранности. Все описанные поведенческие пробы вызывают apply; единственная команда без него требует только rc 0 и список. Чтение stderr не доказывает отсутствие удаления.
ОБХОД: реализация без apply печатает правильные путь, причину и счёт в stderr, затем удаляет тех же кандидатов, что с apply, и возвращает 0. Все описанные apply-пробы, прежние case и команда «dry-run» удовлетворяют объявленной приёмке, но инвариант «удаление ТОЛЬКО явным флагом» нарушен. Нужен rc-ассерт перехода на одном входе: dry-run сохраняет запись/содержимое, apply удаляет.

БЛОКИРУЕТ contracts/026-zhnec-tmp.md:40,88-108 — Б4: защитный приоритет активного номера объявлен, но входа с активным и done одновременно нет. Три постоянных case названы повтором тех же инвариантов/входов, самостоятельного различающего предъявления приоритета не задано.
ОБХОД: реализация удаляет при наличии ЛЮБОГО done-номера до проверки активных. Она удаляет `critic021v3`, сохраняет `arch026task.md`, свежего анонима и tracked, отказывает смешанному входу, проходит показанный REGRESS-024; описанная приёмка зелена. Но `tmp/arch026-from021/` при живом контракте 026 без done и теге `done/contracts/021/1` удаляется вопреки строке 40. Нужен различающий вход для этого защитного приоритета, не дополнительные копии первой пробы.

БЛОКИРУЕТ contracts/026-zhnec-tmp.md:66-68 — Б5: вызов с apply после done объявлен «не блокер приёмки предмета», хотя предмет — механизм при close-out (строки 3-4), а default только печатает список. Н-97 требует закрыть отсутствие жнеца при close-out, не только предоставить ручную возможность удаления.
ОБХОД: CLI реализован правильно, все пробы проходят, но `roles/orchestrator.md:125` остаётся с прежним gc без `--tmp-reap-apply`. Каждый close-out выполняет dry-run, tmp не чистится. Приёмка разрешает это дерево, поскольку проводка необязательна. Нужны обязательная проводка в названной зоне и наблюдаемое предъявление close-out с удалением подходящего скратча; новая роль/демон не требуются.

## Советы и пять обязательных вопросов

СОВЕТ contracts/026-zhnec-tmp.md:135-156 — развилка 026=реап, 027=durable обоснована, не считаю её дроблением обязательного объёма. `NABLIUDENIA.md:34-40` прямо разделяет расширение gc сейчас и durable в очередь. Поиск по HEAD подтверждает перечисленные in-tree producer-места в 19 скриптах; две роли действительно требуют in-tree клон, `AGENTS.md:255-262` сохраняет норму и отдельный carve-out раннера. Для реапа её менять не нужно. Исторические 1.2 ГБ/12 584 не перемерялись в чистом клоне: это свидетельство Н-97/А-151, не новый замер критика.

СОВЕТ contracts/026-zhnec-tmp.md:61-74 — конфликта «gc правит implementer, вызывает orchestrator» нет. Зона ограничивает правку файла, не вызов команды. Скрипт, фикстуры, close-out-норма и вердикты имеют владельцев; границы удерживают `check_zones.sh` и страж staged по авторству (`AGENTS.md:154-155`). Дефект — необязательность проводки (Б5), не пересечение зон. Оракул 024 и раннер остаются вне правок.

СОВЕТ contracts/026-zhnec-tmp.md:32-41 — NNN в имени не доказывает принадлежность скратча контракту: роли допускают `tmp/critic/repo` и `tmp/<имя>/repo`, producer'ы используют анонимные mktemp-имена. Это не самостоятельный блокер: старые безномерные записи уже охватывает возрастная ветвь, переименовывать старый скратч норма не требует. Остаточный риск шире строк 45-47: случайный done-номер в чужом свежем имени даёт раннее удаление, старый безномерный активный контекст не получает защиты. Назвать эвристику и этот риск полезно; гарантии принадлежности из имени нет. Явно обещанный приоритет активного и done — отдельный Б4.

СОВЕТ contracts/026-zhnec-tmp.md:26-30,37,98-103 — молча исполнителю остаются `contracts/0NN-*.md` против NNN/примера `contracts/026-*.md` и порядок проверки смешанности относительно возраста: у `tmp/mix/` приёмка не задаёт mtime, но требует rc 1. Формат строки и точка вывода явно оставлены деталями реализации — это допустимо. Без отдельного зелёного обхода эти неоднозначности не блокируют.

СОВЕТ contracts/026-zhnec-tmp.md:53-57,104-120 — живой неизменённый `check_no_leak.sh` подходит как оракул: обе полярности названы, положительная требует имя утечки, отрицательная — rc 0 при исчезновении строк. Но правильный оракул не заменяет отсутствующую исполняемую композицию (Б1). В пробе следует наблюдать фактическое исчезновение стейла и не подменять свежего анонима защитой активного 026 в toy. Число «4 red + 3 case» не доказывает достаточность: повтор трёх входов не покрывает dry-run и приоритет активного номера (Б3/Б4).

Пять вопросов: критерий слабее предмета — Б3/Б4/Б5; готовность не доказана командами — Б1/Б3; молчаливые решения — совет о распознавании/смешанности; исполнители/границы названы, вызов gc не конфликтует с зоной правки; противоречия AGENTS.md — Б1/Б2. Основание FAIL — только блокеры, не советы и не проза будущего 027.

## Исполненное доказательство

В свежем клоне на судимом HEAD:

- `git rev-parse HEAD` → `77d530942de6d069e33778ef3901c7a27fd5520f`.
- `git ls-tree -r --name-only HEAD fixtures/gc_agent_branches/ contracts/026-zhnec-tmp.md verdicts/critic/contracts-026-v1.md` → контракт, `_repo.sh`, `case_zavisshaja_peredvinuta.sh`, `case_zavisshaja_snesena.sh`; семи новых проб и прежнего вердикта v1 нет.
- `bash fixtures/gc_agent_branches/red_zhnets_reap_stale.sh` → rc 127, `No such file or directory`.
- `bash fixtures/gc_agent_branches/red_zhnets_aktiv_vyzhivaet.sh` → rc 127, `No such file or directory`.
- `bash fixtures/gc_agent_branches/red_zhnets_tracked_netrogat.sh` → rc 127, `No such file or directory`.
- `bash fixtures/gc_agent_branches/red_regress_024_noga2.sh` → rc 127, `No such file or directory`.
- `bash scripts/verify_antiplacebo.sh --scope gc_agent_branches` → rc 0; сырой итог: `барьеров: 1 · фикстур: 2 · предъявлено красным повторным прогоном: 2`. Это только старая семья, не приёмка жнеца.

Прочитаны контракт 026 целиком, gc целиком, Н-97, обе записи под заголовком Н-60, А-151 и применимые нормы AGENTS.md. Код не менялся и реализация не судилась. Б3-Б5 — контрмодели текста приёмки, не результаты исполнения будущего gc. Полный прогон не запускался: он принадлежит CI. Арбитр не созывается: первый круг, повторного спора нет.
