FAIL

# Контракт 033 — круг 1: формализация Н-100 без кодовой дельты

Предмет: `contracts/033-union-semantika-zon-check-zones.md` и наблюдение А-179 в `NABLIUDENIA_ARCHITECT.md` на коммите `41040ccdab2df2414a067d791b9945dfb0b83700`. Проверка проведена в одноразовом клоне `/tmp/dev-harness-verify/critic-033/repo`, ветка `wip/033/architect`. Worktree предмета, его ветка в исходном репозитории и main не изменялись. Вердикт передаётся оркестратору для приземления с author=critic.

Набор артефактов полон: предмет закоммичен; предмет, приёмочные rc-команды, именованные исполнители и границы присутствуют. Отказ не связан с отсутствием новой реализации или новых фикстур. Два основания отказа: машинный критерий не различает выполненную формализацию и её отсутствие; текст приёмки нарушает прямой запрет Н-39.

## Блокирующие находки

### Б1. Приёмка реализации не доказывает предмет формализации

БЛОКИРУЕТ contracts/033-union-semantika-zon-check-zones.md:67–73 — обе команды проверяют уже существующую механику, но не результат, объявленный предметом в строках 23–25: закрепление union-семантики заморозкой и судейское закрытие Н-100. Условие done в строках 83–87 добавляет закрывающую запись, однако не содержит rc-команды, которая отличала бы выполненное условие от невыполненного.

ОБХОД: предъявленный HEAD 41040cc уже удовлетворяет обеим командам приёмки: scoped antiplacebo завершился успешно, живой check_zones — успешно. При этом отсутствуют все теги `frozen/contracts/033/*` и `done/contracts/033/*`, а Н-100 в `NABLIUDENIA.md:287` остаётся ОТКРЫТО. Можно остановиться на этом дереве, предъявить весь объявленный машинный критерий зелёным и не выполнить единственную новую работу 033 — формализацию. Этот контрпример наблюдался непосредственно, без изменения барьера или фикстур.

Это не требование заморозить черновик ДО критика. Требуется развести предзаморозочный регресс механики и итоговый проверяемый результат некодового контракта. Итоговая приёмка должна иметь rc-проверку именно заморозки 033 с соответствующим блобом и названного закрытия Н-100, включая связь с П1а-коммитом и двумя СПАСЕНО-заморозками. Новый механизм или новая поведенческая фикстура для этого сами по себе не требуются. Общий `check_contract_frozen.sh` без проверки наличия именно заморозки 033 недостаточен: его строки 129–132 законно принимают незамороженный документ как черновик.

### Б2. В контракт возвращена запрещённая привязка стаба к входу

БЛОКИРУЕТ contracts/033-union-semantika-zon-check-zones.md:52–54 — контракт описывает конкретную подмену `$1 == a && $3 == n`, конкретный вход «путь зоны 002 внутри диапазона 001» и утверждает: «дефект стаба наблюдаем ровно на union-входе». Строка 71 повторяет привязку в самом приёмочном критерии. Это прямо противоречит `AGENTS.md:130–133`: контракту запрещено утверждать пару «обманный стаб ↔ ветвь», привязку к входу выполняет архитектор по коду. Н-39 дословно повторён в задании текущего круга.

Основание блокировки — исключение для прямого противоречия AGENTS.md; отдельный ОБХОД не требуется. Успешный прогон существующей фикстуры не отменяет запрета на форму контракта. В контракте достаточно сохранить инвариант union и rc-команды; детали подмены и доказательство её наблюдаемости относятся к фикстуре и отчёту прогона, а не к замораживаемому обещанию.

## Ответы по предмету задания

### 1. Форма пост-фактум кодификации допустима

`AGENTS.md:127–130` отличает кодовую задачу от некодовой. `scripts/check_zones.sh:20–24` прямо предусматривает контракт, кодифицирующий уже действующее соглашение, с объявлением `РАБОТА НЕ РАЗДАЁТСЯ:`. Здесь названа узкая документальная зона architect, а отсутствие раздачи кодовых путей объявлено отдельно. Само отсутствие кодовой дельты не основание отказа.

История подтверждает исполненный бутстрап: `cbbf6b14968fe09ef19a10e048da19bf02502c35`, author=owner, изменил check_zones и две фикстуры; проверка его ancestry к судимому HEAD вернула 0. Дифф самого 41040cc содержит только контракт и наблюдение; дифф по `scripts/` и `fixtures/` пуст.

Прецеденты СПАСЕНО найдены и сверены: `verdicts/critic/contracts-026-v4.md` в `06792d3` и `verdicts/critic/contracts-027-v3.md` в `76b304c`, оба начинаются с `accept`. Реальные аннотированные заморозки `frozen/contracts/026/4` и `frozen/contracts/027/3` называют СПАСЕНО 75f5ffe, разрешение владельца и соответствующие вердикты. Предмет тех мини-кругов — точное административное исключение уже существующего SHA, не повторная реализация. Они подтверждают допустимость ограниченного пост-фактум суда, но не подменяют приёмку новой формализации 033: там изменение замороженного блоба имело отдельно предъявленный механический эффект.

Новой СПАСЕНО-строки для cbbf6b1 этот вердикт не требует. Разрешение owner-канала задано поручением и отражено в сообщении коммита; повторно открывать уже закрытую владельцем развилку незачем.

### 2. Union проверяется различающе; формализация — нет

Существующая `case_union_zon_vseh_zamorozok.sh` действительно содержит положительный контроль легального межконтрактного пути, отдельный запуск копии барьера с возвращённым per-contract фильтром и проверку отказа с именем union-пути. Затем предъявляется путь вне объединения. Scoped-прогон прошёл весь протокол этой фикстуры. Поэтому претензии «union подтверждён только общим зелёным rc» нет.

Живой check_zones отдельно распознал СПАСЕНО 75f5ffe в окнах 026 и 027. Это доказывает работоспособность уже существующего состояния, но не завершение нового документального предмета. Этот разрыв — Б1, а не повод повторно писать union-фикс.

### 3. Новая красная фикстура не обязательна

Нового поведения 033 не вводит. Существующая union-фикстура и её красное предъявление реально исполнены; правило о красном доказательстве проверки не обойдено. Обязательный новый вариант с N=3 не следует из предмета: для него не назван отдельный дефект, который не ловит нынешний различающий вход. Требовать такой вариант только ради нового файла было бы padding.

Недостающая мера относится к факту завершения формализации, а не к расширению семьи поведенческих тестов. Смысл русской нормы сам по себе нельзя выдать за доказанный antiplacebo-прогоном.

### 4. А-179: фактическое ядро подтверждено, обобщения следует отделить

Оригинал `verdicts/review/contracts-029-k1.md:182–192` действительно говорит, что check_zones зелён и прав по объявленной union-семантике; замечание З1 относится к необязывающей пораздельной раздаче. Кодовая дельта cbbf6b1 действительно предшествует заданию 033. Эта часть наблюдения корректна. Его предложение о гигиене будущих заданий оставлено открытым и cognitive-only, а не объявлено новым действующим механизмом.

СОВЕТ NABLIUDENIA_ARCHITECT.md:3976–3979 — фразы о «дешёвой модели» и о том, что более дешёвый исполнитель обязательно повторил бы работу, выходят за установленный факт расхождения задания с деревом и первоисточником. Последнее — контрфактическая гипотеза, не наблюдавшийся исход. Стоит явно отделить её от замера; вывод о причинной роли цены модели приведёнными данными не доказан. Исходную стенограмму первоначального задания этот круг независимо не восстанавливал.

СОВЕТ NABLIUDENIA_ARCHITECT.md:3971 — скобка «cbbf6b1 — union-фильтр + та самая фикстура + СПАСЕНО 026/4+027/3» сжимает несколько событий до одного SHA. Сам cbbf6b1 изменил только барьер и две фикстуры; СПАСЕНО оформлено отдельными последующими изменениями и заморозками. В контракте строки 16–21 разделяют их точнее; наблюдению полезна такая же атрибуция.

## Пять обязательных вопросов критика

1. **Критерий слабее предмета?** Да: Б1, измеренный зелёный критерий без завершённой формализации.
2. **Каждое «готово» доказуемо названной командой?** Поведение барьера — да; заморозка именно 033 и закрытие Н-100 — нет, также Б1.
3. **Что молча оставлено исполнителю?** Поведенческой реализации нет; семантика union, ретроактивность и остаток З1 названы. Не определена машинная мера документального close-out — это тот же Б1, не отдельная находка.
4. **Границы и исполнители названы?** Да: architect — два документальных пути; закрывающая запись — orchestrator; кодовые пути исключены. Новой раздачи реализации нет. Ограниченность per-contract распределения при union раскрыта как когнитивный остаток, а не обещана механически.
5. **Есть противоречие AGENTS.md?** Да: Б2, прямой запрет Н-39. Форма некодового контракта и отказ от добавления дублирующих фикстур сами по себе норме не противоречат.

## Выполненные проверки и пределы

- `bash scripts/verify_antiplacebo.sh --scope check_zones` — rc 0; вывод: «барьеров: 1 · фикстур: 20 · предъявлено красным повторным прогоном: 20», включая union-фикстуру. Это scoped-регресс, не заявление о полном CI.
- `bash scripts/check_zones.sh .` — rc 0; вывод: «замороженных контрактов: 29 · объявленных авторов: 7 · коммитов в диапазонах: 1114 · проверено по зонам: 723». Счёт приведён как вывод инструмента, не как независимо перепроверенное счётное утверждение.
- `git merge-base --is-ancestor cbbf6b14968fe09ef19a10e048da19bf02502c35 HEAD` — rc 0.
- `git for-each-ref --format='%(refname)' refs/tags/frozen/contracts/033/ refs/tags/done/contracts/033/` — rc 0, пустой вывод: обе серии тегов отсутствуют. Дополнительный `git show-ref --verify refs/tags/frozen/contracts/033/1` — rc 128, ссылка отсутствует.
- `git diff 41040cc^ 41040cc -- scripts fixtures` — rc 0, пустой вывод: кодовые барьеры и фикстуры предметным коммитом не изменены.
- Проверены сообщения cbbf6b1, вердиктов двух СПАСЕНО-кругов и аннотации соответствующих заморозок. Полная CI-батарея не запускалась; реализация других контрактов заново не принималась.

Заморозка текущей редакции 033 не разрешается. Исправление требуется в контракте приёмки, а не в уже работающем union-фильтре. Это первый круг; оснований созывать арбитра по повтору причины нет.
