FAIL

Судимый срез: HEAD `c1e871071ed1b25b1b8fd948c777d6b09f3327b2`; контрактный blob входит коммитом `d374f67`, красные фикстуры — коммитом `bd4938e`. Репродукции выполнены только в одноразовом клоне `/tmp/c018-critic-repo`; основной чекаут не переключался.

## Проверено исполнением

На исходном HEAD без стража:

- `VERIFY_ANTIPLACEBO_SCRATCH=/tmp/c018s-worker bash scripts/verify_antiplacebo.sh . --scope check_staged/case_vetka_rabochij_na_main` → rc 1, `красное не предъявлено`;
- `VERIFY_ANTIPLACEBO_SCRATCH=/tmp/c018s-judge bash scripts/verify_antiplacebo.sh . --scope check_staged/case_vetka_sudja_i_vladelec` → rc 1, `красное не предъявлено`.

Прототип ровно объявленного стража был наложен только в клоне: собственная живая `refs/heads/wip/<NNN>/<автор>` + любой текущий `wip/*` пропускается, не-`wip/*` отвергается. На нём:

- обе команды выше → rc 0 каждая;
- `... verify_antiplacebo.sh . --scope check_staged` → rc 0, все 7 case имеют зелёный контроль и именованное красное;
- `... --scope land_agent` → rc 0; `... --scope gc_agent_branches` → rc 0;
- после одноразового тега заморозки `bash scripts/check_zones.sh .` → rc 0; строки контракта 018 разобраны без `zones_violations`;
- после задания локальной identity `critic` команда `bash scripts/check_staged.sh .` → rc 0 (`staged пуст`).

Красные фикстуры не тавтологичны: на исходном барьере они краснят приёмочную пачку rc 1; на прототипе заявленного стража становятся зелёными rc 0; положительные контроли рабочего агента на `wip/*`, судьи на main без собственной wip-ветки и незонированного владельца сохраняются.

## Сверка решений владельца

- (Q1) есть: `git-worktree`; Н-60 названа ОБОЙДЁННОЙ, не закрытой; `omp-btrfs` явно оставлен upstream-не-целью.
- (Q2) есть: рабочие агенты идут через worktree/`wip/<NNN>/<автор>`, судьи — main-direct с репро в одноразовом клоне; причина сверена с `scripts/land_agent.sh:130-151` — И-9 требует committer из реестра авторов `ЗОНА`.
- (Q3) есть выбранная владельцем допустимая ветвь: единый NNN не объявлен механическим срезом 1, но остаточный риск назван `cognitive-only`, указан митигатор `--nnn <NNN>` и честное будущее место механизма — spawn-time.
- (Q4) есть: судьи исключаются отсутствием собственной живой wip-ветки, владелец — незонированностью/fail-open; ложная премиса «судья без зон» прямо отвергнута.
- Явный land/merge есть: `land_agent --no-ff` из основного чекаута, затем `gc_agent_branches`.
- Исполнители и границы есть; `ЗОНА`-строки и `РАБОТА НЕ РАЗДАЁТСЯ:` соответствуют грамматике.

## Находка

БЛОКИРУЕТ contracts/018-vetka-na-agenta.md:35 — объявленный страж и И-2 на `contracts/018-vetka-na-agenta.md:92` различают только «любой `wip/*`» против «не `wip/*`», но предмет требует собственную ветку (`contracts/018-vetka-na-agenta.md:12`, `:61-65`). Красного входа «рабочий агент на чужой wip-ветке» нет. Повтор требования в обкатке (`contracts/018-vetka-na-agenta.md:141-143`) не имеет rc-команды и потому не может сделать приёмку красной.
ОБХОД: реализовать страж буквально по объявленному правилу: при живой `wip/018/implementer` пропускать любой checkout `wip/*`. В репозитории одновременно оставить `wip/018/implementer`, переключить implementer на `wip/018/architect` и staged-ить `scripts/b.sh` внутри его зоны. Прототип прошёл обе новые scoped-фикстуры и весь `--scope check_staged` с rc 0, а отдельная проба этого состояния напечатала `ok: staged в зоне автора implementer`, `OBSERVED_BRANCH=wip/018/architect`, rc 0. Критерий зелёный, но implementer коммитит в ветку architect, поэтому режим «каждый агент в СВОЮ ветку» не достигнут.
