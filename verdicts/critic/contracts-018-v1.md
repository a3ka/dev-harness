FAIL

Судимый срез: HEAD `39416b4590792ac483bd2e9f2f845636a3de7881`; текст v2 входит этим коммитом, новая фикстура `case_vetka_chuzhaja_wip.sh` — закоммиченным деревом HEAD. Все репродукции выполнены в одноразовом клоне `$TMPDIR/c018v2`; основной чекаут не переключался.

## Проверено исполнением

На исходном HEAD без стража три требуемых красных scoped-прогона предъявлены:

- `bash scripts/verify_antiplacebo.sh . --scope check_staged/case_vetka_chuzhaja_wip` → rc 1, `красное не предъявлено`;
- `bash scripts/verify_antiplacebo.sh . --scope check_staged/case_vetka_rabochij_na_main` → rc 1, `красное не предъявлено`;
- `bash scripts/verify_antiplacebo.sh . --scope check_staged/case_vetka_sudja_i_vladelec` → rc 1, `красное не предъявлено`.

Контрольный прототип общего правила «жива своя `wip/*/<автор>` → текущая ветка обязана иметь последний компонент, равный committer» был наложен только в клоне. На нём:

- `bash scripts/verify_antiplacebo.sh . --scope check_staged` → rc 0: все 8 case получили зелёный контроль и именованное красное;
- прежний обход v1: implementer с живой `wip/018/implementer`, checkout `wip/018/architect`, staged `scripts/b.sh` → rc 1, `вне своей ветки wip/`;
- дополнительный вход detached HEAD при той же живой собственной ветке → rc 1, `вне своей ветки wip/`;
- дополнительный вход checkout `wip/018/implementerXyz` при авторе implementer и живой `wip/018/implementer` → rc 1, `вне своей ветки wip/`.

Прототип затем снят обратным patch; `git diff --exit-code -- scripts/check_staged.sh` → rc 0.

Для проверки силы критерия был отдельно наложен контрольный литеральный прототип: при живой собственной ветке он отвергает только два предъявленных красных checkout — `main` и точный `wip/018/architect`, — с требуемой причиной, остальные ветки оставляет прежнему суду зон. На нём:

- `bash scripts/verify_antiplacebo.sh . --scope check_staged` → rc 0, все 8 case зелёны для раннера;
- обход v1 на точном `wip/018/architect` → rc 1;
- detached HEAD → rc 0, `ok: staged в зоне автора implementer`;
- `wip/018/implementerXyz` → rc 0, `ok: staged в зоне автора implementer`.

Литеральный прототип также снят обратным patch; исходный `scripts/check_staged.sh` в клоне восстановлен.

После одноразового тега заморозки в клоне `bash scripts/check_zones.sh .` → rc 0: строки `contracts/018-vetka-na-agenta.md:113-118` разобраны как зоны architect/implementer/critic/adversary/reviewer и явное `РАБОТА НЕ РАЗДАЁТСЯ`, нарушений грамматики нет.

## Сверка текста v2

- Предмет и И-1/И-2 теперь требуют именно СВОЮ `wip/*/<автор>` по последнему компоненту committer (`contracts/018-vetka-na-agenta.md:29-45`, `:90-100`).
- (Q1)–(Q4), явные `land_agent`, merge `--no-ff` и `gc_agent_branches` остаются в тексте (`contracts/018-vetka-na-agenta.md:8-84`, `:105-109`).
- Счёт фикстур и ветвей явно не заморожен (`contracts/018-vetka-na-agenta.md:127-128`).
- Однако И-1 и приёмка по-стабно связывают команды только с входами main и точной чужой `wip/018/architect` (`contracts/018-vetka-na-agenta.md:90-100`, `:131-146`). Исполнением выше показано, что это не удерживает объявленный класс «всё, кроме своей ветки».

## История кругов

- v1: `FAIL` — критерий различал «любой `wip/*`» и не-wip; implementer на чужой `wip/018/architect` проходил.
- v2: точный обход v1 закрыт новой красной фикстурой и на контрольном прототипе теперь rc 1. Причина критерия повторяется на оставшейся части того же объявленного класса: достаточно реализовать два литерально предъявленных входа, не реализуя общий предикат собственной ветки.

## Находка

БЛОКИРУЕТ contracts/018-vetka-na-agenta.md:90-100 — И-1 объявляет весь класс «не своя ветка» (включая detached и любую чужую wip), но две названные rc-команды предъявляют только `main` и точную `wip/018/architect`. Критерий поэтому слабее предмета и не отличает общий предикат последнего компонента от реализации по двум литералам.
ОБХОД: в `check_staged.sh` при живой собственной ветке отвергать с причиной `вне своей ветки wip/` только checkout `main` и точный `wip/018/architect`, а остальные checkout передавать прежнему суду зон. В клоне такой прототип дал rc 0 на всём `--scope check_staged` и rc 1 на прежнем обходе v1, но пропустил detached HEAD и чужую похожую ветку `wip/018/implementerXyz` с rc 0. Объявленный режим «только СВОЯ ветка» не достигнут при зелёном критерии.

АРБИТР: `arbiter`; предмет спора — повторная недостаточность красного контура для класса «не своя ветка»: достаточно ли точек main + одна точная чужая wip, либо критерий обязан предъявить независимую границу общего предиката (detached/другая чужая wip).
