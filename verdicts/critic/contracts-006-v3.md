ESCALATE

АРБИТР: arbiter — предмет спора: можно ли считать обход 2 закрытым одним конструкционным требованием «фильтр над тем же циклом», когда названная команда тира 2 не различает сохранение кодов и часть обещанных ветвей вообще не исполняет; это повтор причины содержательного FAIL v2 на третьем круге того же контракта.

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:79-84 — формулировка требует, чтобы выбранный сломанный барьер краснил scoped «ТОЧНО как полный», и обещает сохранение кода 2, вырожденного и чужого кода, но `scripts/check_scoped_run.sh:123-132` проверяет только два предиката: полный код не равен 0 и scoped-код не равен 0. Ветви (н) в исполняемом барьере нет вовсе: аргумент `н` сразу печатает общий успех. Ограничение «тот же цикл/учёт» на строках 18-21 называет требуемую конструкцию, но названная команда не доказывает её соблюдение.
ОБХОД: реализация до существующего цикла честно фильтрует массив, на исправном выбранном `b` возвращает 0, а после цикла только в scoped отображает любой отказ выбранного барьера в код 2 (либо сохраняет код 1, но глотает исходный код 2). Ветка (л) зелена; в (м) полный и scoped оба ненулевые; полный гейт и npm-команды зелены. При этом scoped уже не сохраняет тот же код/учёт отказа, а в варианте с проглоченным кодом 2 становится зеленее полного — предмет не сделан.

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:22-28 — интерфейс объявляет `--scope <ключ>[/<case>]` с case-уровнем и обязывает `verify_antiplacebo.sh` потреблять селектор и фильтровать цикл, но тир 2 на строках 74-84 проверяет интеграцию только через `--changed` на барьерном уровне. Тир 1 (и) доказывает лишь выход селектора, не применение case-части раннером. Закоммиченный зелёный контроль `_ref_va.sh` сам отбрасывает суффикс `${w%%/*}` при фильтрации барьеров и затем запускает все case выбранного барьера.
ОБХОД: исполнитель переносит фильтр из `_ref_va.sh` без изменений. Все команды строк 101-109 проходят, включая (и), (л) и (м), однако `verify_antiplacebo.sh --scope check_scope_select/case_vetv_a_neizvestnyj_kljuch` запускает все 10 фикстур `check_scope_select`, а не один названный case. То есть объявленный case-уровень раннером не реализован; этот обход воспроизведён на зелёном референс-раннере (`барьеров: 1 · фикстур: 10`).

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:37-43 — предмет требует для `scoped|needs-full` совокупность `MODE:` и строки `SCOPED: …`, а критерий (к) на строках 69-72 повторяет обе моды. `scripts/check_scope_select.sh:195-202` проверяет `SCOPED:` только на успешном `MODE: scoped`; ветвь (г) для `needs-full` требует лишь `MODE: needs-full` и подстроку `0 задет`, без префикса `SCOPED:`.
ОБХОД: селектор на пустой выборке печатает `MODE: needs-full` и диагностическую строку `0 задетых — полный гейт в CI`, но не печатает машинный маркер `SCOPED:`; на непустой scoped-выборке маркер печатает. Ветви (г) и (к), оба тира и все команды приёмки зелены, хотя обещанный машинно-отличимый маркер режима `needs-full` отсутствует.

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:37-40 — предмет отдельно требует код 2, когда недоступен сам инструмент `git`. Критерий (д) на строках 63-64 и `scripts/check_scope_select.sh:133-143` проверяют нерезолвимую базу и не-git дерево, но оба сценария выполняются при доступном бинаре `git`; предварительная проверка барьера при отсутствии git возвращает 2 до запуска предмета.
ОБХОД: селектор при доступном `git` правильно возвращает 2 для нерезолвимой базы и не-git дерева, но веткой `command -v git || exit 0` ложно зеленеет, когда бинарь отсутствует. Все названные команды приёмки в обычном окружении с git проходят, а требуемый fail-closed для недоступного инструмента не сделан.
