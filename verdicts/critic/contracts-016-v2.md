FAIL

Судимый предмет: дельта `30b00d9b0e5f14f77dade70dc7baa71d58722279` против девяти блокеров `verdicts/critic/contracts-016-v1.md`; базовый предмет v1 — `f4b66de2b5fbd1ccf8c113afa2da2631503f01ec`. HEAD закоммичен и запушен по условию задания; отдельный чистый клон перед судом был на `30b00d9`. Набор артефактов полон: предмет, приёмочный критерий, исполнители и механические `ЗОНА`-строки есть. Ниже судятся только решения девяти находок v1 и дефекты, внесённые их дельтой.

## Итог по каждому из девяти блокеров v1

1. **ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:3-6,19,37,48,62-64,72,114,196,202,205-206`. Контракт теперь прямо объявляет источником решений собственный закоммиченный текст, а `tmp/` — только неисточниковым рабочим материалом. Sweep по `расхождений нет|tmp/frontier|Единственный источник` пуст. Sweep `Q[1-8]` находит содержательные решения для всех восьми: Q1 — барьер/пределы хука, Q2 — отсутствие push, Q3 — сохранение identity и И-9, Q4 — GC без таймера, Q5 — триггер 3*, Q6 — класс установки hooks, Q7 — checkout из объектов вместо walker, Q8 — разовая зачистка вне предмета. Источник больше не зависит от незакоммиченного файла.

   СОВЕТ contracts/016-izoljacija-agenta.md:5,48,202,205-206 — буквальный sweep `\(Q[1-8]\)` не подтверждает заявленную форму всех восьми отдельных тегов: отдельные `(Q7)` и `(Q8)` отсутствуют (Q7/Q8 стоят внутри более длинных скобок). Содержательные решения при более широком sweep `Q[1-8]` присутствуют, поэтому обхода предмета это не создаёт и закрытие находки 1 не отменяет.

2. **ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:156,163-164,170,172,183`. Sweep `стаб` оставляет только мета-правило о немороженном счёте, отсылку Н-39 к коду проб и слово «по-стабно». Поимённых связок «стаб ↔ вход» в прозе больше нет; прежние «тихий спавн», «приёмка-OK-без-ветки» как стабы и привязки стабов к case удалены.

3. **ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:156-175,180-191`. Sweep `ровно|ДВУМЯ|оба case|обе пробы|три фазы|все три case|[0-9]+ (case|стаб|фаз|проб)` не находит счётных констант приёмочных прогонов; единственное оставшееся «ровно» на :48 задаёт stdout-протокол, а не число фикстур. Исторические `2` CI-цикла и `305`-й запрос на :8-10 ссылаются на закоммиченную запись `NABLIUDENIA.md:1666-1701`; `14 worktree` на :121 и разовая зачистка `7 клонов` на :205-206 описаны как прежняя боль/исполненное действие, а не ожидаемое число будущего прогона. С границей согласен: это не константы приёмки.

4. **ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:90-102,187`. Канарейка :97 дословно дала на живом дереве rc=0 и пустой stdout; на toy-дереве с `scripts/bypass.sh:2:git commit -m 'без identity'` напечатала эту строку и дала rc=1. Без `set -e` и с `set -e` статусы одинаковы: пустой последний `grep .` даёт 1, `!` инвертирует его в 0; найденная строка даёт последнему grep 0 и после `!` — 1. Это настоящий rc-переход.

   СОВЕТ contracts/016-izoljacija-agenta.md:97 — в составном shell-скрипте `set -e` не прерывает выполнение непосредственно на команде, чей статус инвертирован `!`: эксперимент напечатал `CANARY_RC=1` и затем `AFTER_CANARY`. Как самостоятельная приёмочная команда она всё равно возвращает 1, поэтому заявленный контракт кода возврата выполнен; при будущей проводке её статус следует явно проверить либо оставить последней командой.

5. **НЕ ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:60-61,77-82`. Дельта закрепила нужное имя committer и добавила `case_merzh_ne_orkestrator`, поэтому прежний буквальный обход `git -c user.name=implementer merge` закрыт. Но наблюдаемое условие по-прежнему доказывает существование merge-коммита с committer `orchestrator`, а не заявленный на :60 факт, что именно `land_agent.sh` создал этот merge. Блокирующий остаток приведён ниже.

6. **ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:86-87,128`. Зона implementer теперь включает и `scripts/check_contract_frozen.sh`, и `fixtures/check_contract_frozen/`. `git ls-files --error-unmatch scripts/check_contract_frozen.sh fixtures/check_contract_frozen/` дал rc=0 и предъявил скрипт, `_repo.sh` и шесть `case_*.sh`. Расширение И-3 исполнимо без выхода из зоны.

7. **НЕ ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:114-123`. Дельта существенно сузила дыру: коммит, где и author, и committer названы известной чужой ролью `critic`, а путь лежит в `fixtures/land_agent/`, существующий `check_zones.sh` действительно ловит как «коммит вне зоны» (мой toy-прогон rc=1). Но И-9 судит **committer**, тогда как `check_zones.sh:324-327` выбирает зону по **author**. Разделение этих двух identity оставляет механический обход; он приведён ниже и воспроизведён на настоящем `check_zones.sh`.

8. **НЕ ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:68-72,103-108`. Дельта закрыла прежнее «напечатать, затем удалить»: имя зависшей ветки после GC теперь обязано остаться в `for-each-ref`, быть в списке владельцу, а force-входа нет. Но критерий наблюдает существование refname, не сохранность его object id; checkpoint всё ещё можно потерять без исчезновения имени. Блокирующий остаток приведён ниже.

9. **ЗАКРЫТ** — `contracts/016-izoljacija-agenta.md:127-132,147-149`. Sweep всех строк `^ЗОНА ` показывает `ЗОНА orchestrator: NABLIUDENIA.md HANDOFF.md`; sweep `^ЗОНА .*roles/` пуст. Ни одна из шести `ЗОНА`-строк 016 не содержит `roles/`, поэтому механическая граница и объявление «РАБОТА НЕ РАЗДАЁТСЯ: уставный класс» больше не противоречат друг другу.

## Блокирующие дефекты контура закрытия, внесённого дельтой

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:60-61,77-82 — `case_merzh_ne_orkestrator` проверяет identity результата, но не пинует переход «до вызова ветка не слита → вызов `land_agent.sh` → новый first-parent merge создан этим вызовом». Наличие подходящего merge-коммита в журнале не доказывает, что приземлил скрипт.
ОБХОД: реализовать `land_agent.sh` как валидатор уже существующего merge-коммита с committer `orchestrator`, который только удаляет worktree/ref и делает push, но сам `git merge --no-ff` не вызывает. В `case_merzh_ne_orkestrator` заранее строить merge: с `orchestrator` для зелёного контроля и с другим committer для красного. Оба именованных case И-1 и scoped `land_agent` могут стать зелёными, first-parent содержит правильный merge, однако требование :60 «merge исполняет сам скрипт» не сделано.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:114-123 — реестр отсекает только неизвестный committer; он не доказывает соответствие committer роли агента, а смежный `check_zones` судит другое поле — author. Это не теоретическая претензия: на scratch-клоне я поставил `frozen/contracts/016/1`, создал коммит пути `fixtures/land_agent/critic016_identity_probe.txt` с `author=implementer`, `committer=critic` и запустил настоящий `bash scripts/check_zones.sh .`; rc=0.
ОБХОД: агент вызывает `git -c user.name=critic -c user.email=critic@dev-harness.local commit --author='implementer <implementer@dev-harness.local>' --no-verify ...` по пути зоны implementer. Local `user.name` остаётся пустым; committer `critic` входит в реестр, поэтому И-9/`case_imja_vne_reestra` зелёный; `check_zones` читает author `implementer` и принимает путь зоны implementer; `--no-verify` уже объявлен cognitive-only остатком на :41-43. Все названные проверки могут пройти, хотя агент коммитит `user.name=critic`, а не своей ролью implementer.

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:68-72,103-108 — `for-each-ref` и `case_zavisshaja_snesena` различают «имя ref есть/нет», но критерий не сравнивает OID зависшей ветки до и после GC. Для сохранения checkpoint существенна именно цель ref, не одно имя.
ОБХОД: GC печатает `wip/016/implementer` в СПИСКЕ владельцу, затем принудительно переводит этот ref с несбитого checkpoint на текущий `main`. После прогона имя присутствует в `git for-each-ref`, `case_zavisshaja_snesena` не видит пропажи, force-флага у пользователя нет и все названные условия зелёные, но несбитый checkpoint уничтожен без слова владельца.

Новых блокеров вне этих трёх остаточных контуров решений v1 дельта не внесла.

АРБИТР: `arbiter` — следующий круг по повторившимся причинам И-1/И-9/И-6 должен решить спор о достаточной наблюдаемости: переход создания merge, сцепление author↔committer↔роль и неизменность OID зависшей ветки.

## Красное и обязательные прогоны

Все прогоны выполнены своими руками в отдельном чистом клоне `/tmp/critic/repo` на HEAD `30b00d9b0e5f14f77dade70dc7baa71d58722279`:

1. `bash fixtures/check_staged/probe_check_staged_krasnyj.sh` → rc=1. Напечатаны четыре `ok` (честная форма; зона-слеп; грамматика-слеп; всё-красно), затем `ОТКАЗ: барьера нет — /tmp/critic/repo/scripts/check_staged.sh (реализация за implementer после заморозки)`.
2. `bash fixtures/check_hooks/probe_check_hooks_krasnyj.sh` → rc=1. Напечатаны три `ok` (честная форма; хук-декой; установщик-декой), затем `ОТКАЗ: барьера нет — /tmp/critic/repo/scripts/check_hooks.sh (реализация за implementer после заморозки)`.
3. `bash scripts/verify_antiplacebo.sh . --scope check_charter` → rc=1. Именованные фикстуры-без-барьера ровно те, что фактически есть сейчас: `fixtures/check_hooks` и `fixtures/check_staged`; собственные 7/7 case `check_charter` предъявлены, итог `расхождений: 2`.
4. Живое дерево, дословная команда :97:
   `! grep -rnE '\bgit\b[^|<>]*[[:space:]](commit|merge)[[:space:]]' scripts/ | grep -vE ':[0-9]+:[[:space:]]*#' | grep -v merge-base | grep -vE 'user\.(name|email)=|GIT_(AUTHOR|COMMITTER)_(NAME|EMAIL)' | grep .` → rc=0, stdout пуст.
5. Та же команда в `/tmp/critic016-v2-canary` с `scripts/bypass.sh:2` = `git commit -m 'без identity'` → rc=1, stdout `scripts/bypass.sh:2:git commit -m 'без identity'`.
6. Те же live/toy команды после `set -e`, с сохранением `$?`, печатью `CANARY_RC` и финальным `exit "$rc"` → live rc=0 (`CANARY_RC=0`, `AFTER_CANARY`), toy rc=1 (`CANARY_RC=1`, `AFTER_CANARY`). Это подтверждает и rc-переход, и особую семантику errexit у `!`.
7. `git ls-files --error-unmatch scripts/check_contract_frozen.sh fixtures/check_contract_frozen/` → rc=0; скрипт и каталог с семью отслеживаемыми файлами существуют.
8. `git config --local user.name` → rc=1, stdout пуст: дефолта в clone нет.
9. Прямой чужой identity experiment: после scratch-тега 016 коммит `author=critic`, `committer=critic` в `fixtures/land_agent/` и `bash scripts/check_zones.sh .` → rc=1, поимённо `FAIL коммит вне зоны: critic ... fixtures/land_agent/critic016_foreign_probe.txt`.
10. Разделённый identity experiment: после scratch-тега 016 коммит `author=implementer`, `committer=critic` в том же каталоге и `bash scripts/check_zones.sh .` → rc=0; итог `проверено по зонам: 244`, отказа по probe-пути нет.
11. `git rev-parse HEAD && git status --porcelain=v1` в судейском клоне → rc=0; SHA `30b00d9b0e5f14f77dade70dc7baa71d58722279`, status пуст.

Grep-sweeps специализированным поиском по закоммиченному контракту: `Q[1-8]`; `\(Q[1-8]\)`; `расхождений нет|tmp/frontier|Единственный источник`; `стаб`; `ровно|ДВУМЯ|двумя|оба case|обе пробы|три фазы|все три case|[0-9]+ (case|стаб|фаз|проб)`; `^ЗОНА `; `^ЗОНА .*roles/`. Результаты приведены по пунктам 1–3 и 9.

## Таймлайн

- зафиксирован HEAD `30b00d9`, чистота основного дерева и создан отдельный клон в literal `/tmp`;
- целиком прочитаны v1 и контракт 016, затем проверена только дельта `f4b66de..30b00d9`;
- выполнены три обязательных красных прогона, sweep Q-тегов/стабов/счётов/зон и обе канарейки И-5;
- отдельно воспроизведены direct-foreign и split-author/committer контуры `check_zones`;
- вердикт записан после последней проверки клона; контрольная отметка — `2026-08-30T00:10:19+02:00`.
