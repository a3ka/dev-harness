FAIL

Судимый предмет — только закоммиченная дельта `frozen/contracts/016/1..94f82ec` по `contracts/016-izoljacija-agenta.md`: перегистрация `v2` означает следующую версию заморозки, а не номер прежнего судейского круга.

## Находка

БЛОКИРУЕТ contracts/016-izoljacija-agenta.md:157-162 — сама однострочная правка точна, но изменяющий её коммит `d55dd206` записан автором `orchestrator`, тогда как объявленная зона `orchestrator` содержит только `NABLIUDENIA.md HANDOFF.md`; файл контракта принадлежит `architect`. Строка `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` законно снимает защиту неизменности уставного текста по правилу 11, но не снимает отдельную защиту границ исполнителей: `AGENTS.md:58-59` объявляет выход за зону провалом, а `check_zones.sh` не имеет исключения по маркеру владельца.

ОБХОД: в изолированном клоне поставить пробный тег `frozen/contracts/016/2` на текущий `94f82ec` и запустить `bash scripts/check_zones.sh .`. Новая строка уже принимается парсером, пробой `bf7c456 config/ci_parity_exceptions.txt` исчезает, однако команда остаётся красной с rc=1: `FAIL коммит вне зоны: orchestrator d55dd206 contracts/016-izoljacija-agenta.md — зона контракта 016: HANDOFF.md NABLIUDENIA.md`. Таким состоянием дерева можно выполнить точную дельту, легализовать сам пробой и пройти `check_charter`, но всё равно оставить процесс заморозки с нарушением механической границы.

ВЫХОД: до заморозки v2 перепостроить незамороженную ветвь так, чтобы однострочное изменение контракта было коммитом автора `architect` (его зона на :157 содержит этот файл), сохранив в том же изменяющем коммите дословное разрешение владельца; затем повторить пробный `check_zones.sh` с тегом v2. Альтернатива требует отдельного явного решения владельца о другом механизме исключения и доказательства этим механизмом, а не одного маркера `check_charter`.

Других находок нет.

## Что прошло

1. `git diff --no-ext-diff --unified=3 frozen/contracts/016/1..HEAD -- contracts/016-izoljacija-agenta.md` и `--numstat` → один файл, `1` добавление / `1` удаление; обе версии отличаются только строкой `ЗОНА implementer` на :158.
2. Та же команда с `--word-diff=porcelain` → единственный добавленный токен `config/ci_parity_exceptions.txt`, удалённых токенов нет. `git diff --check ...` → rc=0, вывода нет.
3. `git show --stat --oneline --no-renames bf7c456` и `git show --format= --name-only --no-renames bf7c456` → только `config/ci_parity_exceptions.txt`, `19 insertions`; автор коммита — `implementer`. Расширение на :158 ровно совпадает с пробоем и не шире него.
4. `git show -s --format=fuller d55dd20` → в первой колонке тела есть `РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/016-izoljacija-agenta.md ...`; названы путь, причина, дата слова владельца и развилка A. `bash scripts/check_charter.sh .` → rc=0, по 016: `d55dd206` изменил уставной документ с разрешения владельца; общий итог 46/46 разрешённых изменений.
5. `git show --stat --oneline d55dd20` → изменён только файл контракта, `1 insertion(+), 1 deletion(-)`. `git tag --list 'frozen/contracts/016/*'` → существует только `frozen/contracts/016/1`: изменение действительно идёт как v+1 до v2.
6. Прочитаны `contracts/016-izoljacija-agenta.md:155-164` и `config/ci_parity_exceptions.txt:61-77`: путь — операционный конфиг пяти команд срезов 1–4, а на :158 он соседствует с уже зоновыми `package.json` и `.github/workflows/ci.yml`. Содержательно расширение не ослабляет границу и даёт implementer ровно файл его работы по CI-паритету.
7. `bash scripts/verify_antiplacebo.sh . --scope check_charter` → rc=0: один барьер, семь красных повторных предъявлений.
8. Контрольный негативный прогон до v2: `bash scripts/check_zones.sh .` на основном HEAD → rc=1 по ожидаемому старому пробою `bf7c456` и по `d55dd206`. Пробный тег v2 в изолированном клоне убрал первый отказ, но оставил второй — это и есть блокер выше.

## Таймлайн

- `2026-08-30T00:34:09+02:00` — `ab07aeb`, поставлена заморозка `frozen/contracts/016/1`;
- `2026-08-30T11:23:23+02:00` — `bf7c456`, implementer меняет единственный файл `config/ci_parity_exceptions.txt`;
- `2026-08-30T12:26:33+02:00` — `d55dd20`, внесена однострочная дельта с разрешением владельца;
- `2026-08-30T12:27:02+02:00` — `94f82ec`, HEAD фиксирует Н-71 и развилку A; контракт больше не меняется;
- `2026-08-30T12:31:08+02:00` — завершены чтение, прогоны основного дерева и пробная заморозка v2 в изолированном клоне; записан этот вердикт.
