FAIL

БЛОКИРУЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:85-91 — достижимость имени тега на `origin` не доказывает заявленное свойство «тег минтит тот, кого коммиттящий агент не контролирует»: проверка не различает пуш оркестратора и пуш того же агента, который создал тег.
ОБХОД: коммиттящий агент создаёт `id/CONTRACT/<NNN>` локально и сам пушит этот ref в настроенный `origin`; `ls-remote` становится непустым, гейт спавна и дверь пропускают номер, хотя внешней выдачи не было. Код предъявленных корней подтверждает отсутствие разделения полномочий: тот же процесс создаёт тег и вызывает `git push origin refs/tags/id/CONTRACT/<NNN>`.

БЛОКИРУЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:322-330 — готовность доказывается тремя прямыми `red_*` на фиксированных именах toy-корней и номерах, но критерий не требует исполнимых стаб-подстановок или инвариантности на независимом входе. Поэтому он отличает текущий код от ожидаемых ответов только на перечисленных примерах, а не реализацию предмета от подгонки.
ОБХОД: обёртки над тремя барьерами распознают basename корней `repo_mnogorazovo`, `repo_peek_bez_tega`, `repo_self_mint`, `repo_origin_nedostupen`, `repo_mimo_reestra`, `repo_chuzhoj_klass`, `repo_chuzhoe_okno`, `repo_rezerv_na_baze`, `repo_smeshannyj`, возвращают ожидаемые rc/строки только им и делегируют прежнему коду все прочие входы. В независимом клоне такие обёртки дали rc 0 всем трём `red_*` и всем трём scoped-семьям, хотя на любом иначе названном конформном корне предмет отсутствует. Косметическое переименование `next_id_peek` удовлетворяет отдельному `git grep`, не меняя peek-семантику.

БЛОКИРУЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:202-216 — таблица прямо связывает стабы С1–С10 с ветвями и входами контракта. Это противоречит старшей норме `AGENTS.md` (Н-39): контракту запрещено утверждать пару «обманный стаб ↔ ветвь»; привязка должна жить в исполнимом коде фикстуры. Для противоречия `AGENTS.md` ОБХОД не требуется.

БЛОКИРУЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:52-65 — ветвь (i) требует устойчивого порядка «резерв и push до spawn», но единственный rc-оракул лишь повторно доказывает уже существующий issue-режим `next_id`; изменение `roles/orchestrator.md` одновременно объявлено внешним владелец-каналом и исключено из раздачи. Дерево не получает ни нормы, ни механизма, удерживающего порядок.
ОБХОД: реализовать только ветви (ii)–(iv), оставить текущий `roles/orchestrator.md`, где `spawn_agent --nnn` обязателен, но предварительный резерв не назван. Ворота 0 и все команды после реализации зелёны, однако следующий оркестратор по действующей роли вызывает spawn без предварительно выданного тега и новый гейт его отклоняет; предмет (i) не сделан.

СОВЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:303-305 — команда `bash tmp/probe_cross_window_draft.sh` с HEAD ветви возвращает 127: файл прямо объявлен неживущим в дереве. Удалить её из набора команд либо предъявить закоммиченный исполнимый вход.

СОВЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:335 — «полный CI зелёный» не называет rc-команду. Для воспроизводимости назвать существующий гейт CI и его аргумент SHA.

СОВЕТ contracts/023-rezervacija-nomera-dver-po-tegu.md:279-280 — `fixtures/check_staged/` в зоне architect включает точечную implementer-зону `fixtures/check_staged/case_draft_sledujushhij_id.sh`; `check_zones.sh` разрешит правку файла обоим авторам. Если граница задумана исключительной, её нужно выразить неперекрывающимися путями.
