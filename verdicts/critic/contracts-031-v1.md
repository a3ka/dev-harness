FAIL

# Контракт 031 — критик, круг 1

Предмет: `contracts/031-strazh-semejstvo-mint-i-bazlajn.md` на HEAD
`4931c49` ветки `wip/031/architect` (текст контракта — `8de741e`).
Судятся контракт и приёмочные предъявления, НЕ отсутствующая реализация.
Набор артефактов полон: предмет, rc-команды, исполнители, семь ЗОНА-строк и
объявление о нераздаче присутствуют. Контракт и фикстуры критик не менял.

## Блокирующие находки

### Б1. Признание минта в истории не имеет различающего предъявления

БЛОКИРУЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:105 — предмет включает признание в `check_zones`, но приёмка :179–220 его нового поведения не упражняет.
ОБХОД: реализовать дверь в `check_staged` и `--retake`, оставить `check_zones.sh` без изменения. Оба red-файла и их повторы могут стать зелёными; существующие scoped-семьи зелены; проверка зон существующего дерева тоже зелена. Живой минт по :221–222 запрещён до done. Первый последующий честный минт orchestrator при открытом окне пройдёт commit-дверь, но будет отвергнут CI как коммит вне зоны. Половина механизма не сделана, до-done команды этого не отличают.

Основание: `red_dver_minta_orkestratora.sh:174–176` вызывает только
`check_staged.sh`; тело файла не вызывает `check_zones`. Формулировка «case_*
исполнителя зелёные» не задаёт наблюдаемого испытания признания. Достаточное
закрытие: rc-предъявление честного минта в открытом окне и отказа на
не-минтной дельте того же пути, с сохранением суда остальных путей коммита.
Привязку стабов к входам делает архитектор по коду, не текст контракта.

### Б2. Дверь может стать пропуском для всего staged-коммита

БЛОКИРУЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:72 — ограничение двери одним путём не различается перечисленными воротами.
ОБХОД: после успешной проверки валидной добавки в `registry/contracts.tsv` вернуть rc 0 из всего `check_staged`, вместо продолжения суда прочих staged-путей. Минтные ворота предъявляют манифест без второго запрещённого пути; существующие case_* не содержат `registry/`. Такая реализация проходит эти предъявления, но staged-набор «честная строка минта + запись в `registry/extra.tsv` вне зоны orchestrator» проходит целиком. НЕ-минтная дельта registry/ разрешена дверью, хотя :73–74 это запрещают.

Достаточное закрытие: смешанный staged-вход с честным минтом и отдельным
запрещённым путём; проверить rc 1 именно на втором пути. Общий старый тест
«второй путь вне зоны» без минта эту границу новой двери не упражняет.

### Б3. Чистый porcelain не доказывает закоммиченность переснимаемых байтов

БЛОКИРУЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:119 — условия :119–126 допускают незакоммиченную подмену вердикта под авторством старого легитимного коммита.
ОБХОД: закоммитить `verdicts/review/contracts-001-k1.md` автором reviewer, объявить его зону в frozen-контракте и снять снимок. Затем `git update-index --assume-unchanged verdicts/review/contracts-001-k1.md` и изменить рабочие байты этого файла без коммита. Снимок цел, porcelain пуст, дельта манифеста непуста и содержит ровно один покрытый вердиктный путь, последний автор пути reviewer. Все шесть объявленных проверок разрешают retake. Реализация ровно этих проверок проходит восемь предъявленных ворот, но переснимает НЕ закоммиченную судейскую дельту и скрывает утечку.

Это не утверждение о существующем `--retake`: его ещё нет. Состояние-противоречие
условий воспроизведено на действующем детекторе и читателе зон:

```text
porcelain=<>
последний автор: reviewer 2fa122dbd6eb407ca71c3d05a4f9855090ae6834
HEAD bytes: accept
рабочие bytes: незакоммиченная подмена вердикта
покрывающая зона: verdicts/review/
ОТКАЗ: основной чекаут загрязнён: verdicts/review/contracts-001-k1.md
check rc=1
```

Достаточное закрытие: доказательство соответствия переснимаемых байтов
закоммиченному вердикту, не только пустой status и `%an`; различающий красный
вход должен оставить снимок неизменным. Это продолжение корневой меры 024,
которую статус-флаги не ослепляют, а не новый запрет на конкретный git-флаг.

### Б4. Fail-closed читателя зон остаётся непроверяемым обещанием

БЛОКИРУЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:123 — обязательный отказ при недоступности реестра не различается ни одними из восьми ворот; :214 не называет проверку этого отказа.
ОБХОД: нормальная реализация шести условий, но при ненулевом `zones_load` разрешать переснятие для автора reviewer и пути `verdicts/review/*` по запасному префиксу/кэшу. Все предъявленные toy-реестры доступны: р7 имеет доступный реестр, но отсутствующую зону, это другой вход. Все восемь ворот проходят, а при реально недоступном авторитете базлайн меняется без доказанной зоны. Дерево зелёное по объявленным предъявлениям, fail-closed не сделан.

Отложить привязку стабов/проб до реализации законно; отложить определение,
каким rc-наблюдением доказывается уже названное обязательное условие, — нет.
Закрытие не требует реализации двери до freeze: нужен различающий вход отказа
читателя с требованием rc 1, именованного отказа и сохранения снимка. Его
постоянная реализация может быть раздана после freeze.

## Границы раздачи и советы

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:162 — два `case_*.sh` в ЗОНА-строке не предоставляют implementer права на конкретные новые файлы. Это измеренный дефект раздачи, но сам по себе он останавливает работу, а не даёт зелёный обход приёмки; отдельным основанием FAIL по порогу роли его не объявляю.

В одноразовом toy заморожен точный текст 031 и вызван существующий `zones_load`.
Оба `zones_match_path implementer` для
`fixtures/check_staged/case_dver_minta_probe.sh` и
`fixtures/check_judge_gate/case_peresnjatie_probe.sh` вернули **1**.
`lib_zones.sh:261–270` сопоставляет точный файл либо префикс каталога с `/`,
не shell-glob; `check_zones.sh:15–17` объявляет грамматику без шаблонов.
Загрузчик оставляет `*.sh` буквальным путём без записи нарушения грамматики:
отсутствие ошибки загрузки не доказывает выдачу прав. В 029 :199 названы
конкретные файлы, не glob. Нужны конкретные case-файлы либо другая раздача
в пределах уже существующей грамматики.

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:170 — «каждый путь разрешает ровно одного автора» не обеспечивается упомянутым прецедентом: зоны объединяются, а более узкая зона implementer не вычитает права architect на каталог из 016/024. Семь строк 031 между собой не пересекаются по замыслу, но глобальной исключительности из них не следует. Не вводить молча новую семантику приоритета зон.

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:89 — порядок отказов не определяет отдельного имени для доступного origin с другим tag-object-sha и для lightweight-тега. В8 предъявляет недоступный origin, но не расхождение двух доступных объектов; в4 предъявляет повтор только в HEAD, не дрейф живой origin/main. Различающие входы и диагностику стоит закрепить, не отдавая решение исполнителю молча.

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:217 — для дословной нормы-строки нет rc-команды; для запрета живого минта до done тоже нет механического наблюдения. `git grep retake` :218 доказывает присутствие слова, не свойство двери. Эти пункты не использованы как самостоятельное основание FAIL; границу чтения ревьюером и механической приёмки следует назвать явно.

## Ответы по обязательным границам задания

- **Граница-2 — инженерная необходимость, не новая воля владельца.** Проверен
  `check_zones.sh:288–401`: orchestrator в открытом окне судится по путям;
  манифест не его зона. `git show d28c688` подтверждает исторический минт
  orchestrator с двумя добавленными строками. Признание именно доказанного
  минтного пути нужно для сквозной работоспособности выбранной двери; оно не
  добавляет новый класс привилегированной дельты. ESCALATE и подпись владельца
  здесь не нужны. Если вместо узкого признания предлагается ослабить общий суд
  registry/, это другой предмет: анализ консультанту, владельцу только решение
  о воле/риске по норме-5, не просьба решить инженерную реализацию.
- **Граница-5 обоснована.** Consultant main-direct пишет свидетельства в
  `verdicts/consultant/`; при выбранном едином источнике зон эта строка делает
  его легитимную дельту доказуемой, без второго списка авторов внутри retake.
  По самой строке :167 нового предмета воли не обнаружено. Общий префикс
  `verdicts/` не должен подменять покрытие конкретной зоной.
- **Красное на скрытие не-вердиктной дельты есть.** Р2 создаёт вердикт плюс
  `scripts/musor-*`, р3 — единственный инженерный путь; `ozhid_otkaz` :174–195
  сравнивает SHA снимка до/после и требует красного последующего `--check`.
  Р7 проверяет отсутствие зоны, не отказ читателя. Эти входы не закрывают Б3/Б4.
- **Деление «инвариант сейчас, стабы/проба по коду после freeze» допустимо**
  по Н-39 и прецеденту 026. `known_gap` не автоматический блокер и не
  индульгенция: Б4 блокирует из-за конкретного зелёного обхода, а не из-за
  самого отсутствия готовой реализации case-файла.
- Новые предъявления не печатают PASS. Условие готовности слабее предмета
  в Б1–Б4; неназванные решения и границы отмечены выше. Остальная проза
  документа предмет суда не расширяла.

## Прогоны и ограничения доказательства

Клон: `/tmp/dev-harness-verify/critic-031/repo`, создан из предъявленного
worktree, ветка `wip/031/architect`. Все прогоны — в одноразовом клоне либо
его внешних toy-корнях; в ветке предмета — только публикация вердикта.

| Команда | Наблюдение |
|---|---|
| `bash fixtures/check_staged/red_dver_minta_orkestratora.sh`, прогон 1 | rc 1; честный минт 032: `ОТКАЗ: вне зоны: registry/contracts.tsv` |
| Та же команда, прогон 2 | rc 1; честный минт 028, та же именованная боль Н-81 |
| `bash fixtures/check_judge_gate/red_peresnjatie_bazlajna.sh`, прогон 1 | р0 зелёный; rc 1 на р1, именованный отказ диспетчера: режима retake нет |
| Та же команда, прогон 2 | р0 зелёный; rc 1 на р1 с тем же именованным отсутствием режима |
| `bash scripts/verify_antiplacebo.sh . --scope check_staged`, изолированный прогон | rc 0; 23/23 предъявлены красными повторным прогоном |
| `bash scripts/verify_antiplacebo.sh . --scope check_judge_gate`, последовательный прогон | rc 0; 3/3 предъявлены красными повторным прогоном |
| `bash scripts/check_zones.sh .` | rc 0; 28 замороженных контрактов, проверено 717 коммитов по зонам |
| `GIT_AUTHOR_NAME=critic GIT_AUTHOR_EMAIL=critic@dev-harness.local bash scripts/check_staged.sh .` | rc 0; staged пуст |
| Одноразовая `probe-contract.sh` (зоны + состояние Б3) | rc 0; оба concrete case-пути отвергнуты зоной implementer, незакоммиченная однопутная дельта видна при пустом porcelain |

Red-файлы останавливаются на первой отсутствующей возможности (в1/р1).
Это доказательство живой боли СЕГОДНЯ, не утверждение, что в этих запусках
исполнены все последующие 12/8 ворот. Минтные входы двух прогонов различны;
retake создаёт случайные toy-корни, имена которых штатный вывод не печатает.

Честность стенограммы: первый scoped `check_judge_gate` получил rc 3 из-за
общего lock; первый scoped `check_staged` получил rc 1 из-за моего параллельного
`check_zones`, создававшего `tmp/zones.*` в том же клоне. Это ошибки организации
прогона критиком, не дефекты предмета; результаты исключены из доказательства.
Оба scoped-прогона затем выполнены изолированно/последовательно с результатами
выше. Первый пустой staged-чек без identity дал rc 1 «identity автора пуста»;
повтор с явной identity critic дал rc 0.

Полный CI не запускался. Б1, Б2 и Б4 — конкретные контрреализации критерия
[INFERENCE], не утверждения о несуществующей реализации. Б3 дополнительно
подкреплён выполненным состоянием-противоречием. Нужен ремонт приёмочного
контракта; переходить к реализации по этому тексту нельзя.
