accept

# Контракт 031 — критик, круг 3, SCOPED

Предмет: `contracts/031-strazh-semejstvo-mint-i-bazlajn.md` на `b59a2c77f1036da54ab61085e753595112794b5b`, ветка `wip/031/architect`. Основной фикс — `c50289c`; просмотрена также совокупная дельта от `538c829`.

Основание и предел суда: РЕШЕНИЕ арбитра `9f86d1c`, `verdicts/arbitration/dostatochnost-zakrytija-b1-b4-031.md`, вопрос 3. Проверены применение критериев Б1/Б4 и новые дефекты дельты. Б2/Б3, границы 2/5 и советы первого круга не переоткрывались. Действительных блокирующих находок в этой области нет.

Предмет закоммичен; предмет работы, команды приёмки, исполнители и ЗОНА-строки присутствуют. Суд выполнен в одноразовом клоне `/tmp/dev-harness-verify/critic-031-k3/repo`, HEAD совпадает с предъявленным. Worktree автора и его ветка не изменялись.

## Б1: критерий арбитра применён

Места: `contracts/031-strazh-semejstvo-mint-i-bazlajn.md:217–225`; `fixtures/check_staged/red_dver_minta_orkestratora.sh:208–222,483–542`.

Независимые пробы извлекли из закоммиченной фикстуры её настоящие построения в11/в12, помощники и проверки. Субъект — неизменённый `check_zones.sh`, без подмены его поведения. Добавлена только печать наблюдений; ранний отказ остальных ворот исключён запуском каждого построения отдельно.

Получено:

```text
в11: %an=orchestrator; check_zones rc=1;
     «коммит вне зоны: orchestrator … registry/contracts.tsv»;
     проверено по зонам: 1.
в12: %an=orchestrator; check_zones rc=1;
     «коммит вне зоны: orchestrator … registry/contracts.tsv»;
     проверено по зонам: 1.
```

Это соответствует замеру 1 арбитра для настоящего orchestrator, а не прежнему плацебо с автором «Фикстура». `commit_kak` не вызывает `g` и не передаёт `-c user.name=Фикстура`. Оба вызова `assert_an_orchestrator` стоят непосредственно перед вызовом `check_zones`.

Проверен дополнительный путь подмены: запуск каждого построения с `GIT_AUTHOR_NAME=ПодменённыйАвтор`. В обоих случаях само предъявление завершилось rc 1 с «предъявление подменено» до вызова `check_zones`. Следовательно, оставшееся влияние внешнего авторского окружения не превращает эти ворота в молчаливый зелёный обход. Нового обхода Б1 в изменённых помощниках не найдено.

Постреализационная проверка в11 требует rc 0; в12 требует rc 1 одновременно с «вне зоны» и именем `registry/contracts.tsv`. Сегодня изолированная проверка в11 закономерно красная, а проверка ожидаемого отказа в12 проходит. Это разные коды предъявления и субъекта, они не смешивались.

## Б4: ненулевой отказ настоящего читателя предъявлен

Места: `contracts/031-strazh-semejstvo-mint-i-bazlajn.md:245–257`; `fixtures/check_judge_gate/red_peresnjatie_bazlajna.sh:197–219,304–316`.

Независимо исполнено построение р10 из фикстуры: frozen-тег запушен в toy-origin и удалён только локально. Библиотеки `lib_registry.sh` и `lib_zones.sh` настоящие, без мока.

```text
здоровый контроль: zones_load rc=0
после удаления локального тега: registry_state=missing-remote
zones_load rc=2
NOT_IMPLEMENTED: реестр заморозок недоступен: missing-remote
porcelain пуст; rev-parse HEAD и ls-files работают
--check до попытки переснятия: rc=1, назван вердиктный путь
--retake сегодня: rc=1, отказ диспетчера — режим ещё отсутствует
снимок до == после
--check после попытки переснятия: rc=1, назван тот же путь
```

Наблюдение воспроизводит класс замера 2-B арбитра: ненулевой отказ читателя при живой механике манифеста. Р10 вызывает субъект через общий каркас `ozhid_otkaz`, который требует одновременно rc 1, точную подстроку «реестр зон недоступен», неизменность SHA-256 снимка и последующий красный `--check`. Фикстура не подменяет `zones_load`.

Граница доказательства: именованный отказ будущего `--retake` сегодня не наблюдался и не объявляется реализованным. Проверены реальность входа и наличие различающих требований в предъявлении. Сегодняшний диспетчерский отказ эти требования не удовлетворяет — как и должно быть до реализации.

Текст исправлен по решению арбитра: р9 различает пустой успешный ответ, р10 — ненулевой отказ. Утверждение, будто р9 убивает запасной путь, включаемый только при rc≠0, снято.

## Дельта, закрытые области и регресс

- `git diff --exit-code 538c829 HEAD -- scripts fixtures/check_staged/_repo.sh` → rc 0: код барьеров, раннер и общий помощник не изменены.
- Секции в13 (Б2) и р8 (Б3) побайтово совпадают с `538c829`; это проверка неизменности, не пересмотр закрытых решений.
- `case_*.sh` выбирает 23 файла `check_staged` и 3 файла `check_judge_gate`; оба `red_*` остаются вне глоба. Нового дефекта проводки в дельте не найдено.
- Изменённые критерии сильнее прежних предъявлений; дополнительных решений, молча переданных исполнителю именно этой дельтой, не выявлено. Исполнители и границы не изменены. Нового противоречия норме в пределах разрешённого суда не установлено.

Повторены команды:

| Команда | Результат |
|---|---|
| `bash fixtures/check_staged/red_dver_minta_orkestratora.sh`, два запуска | rc 1 / rc 1, именованная боль Н-81, «вне зоны: registry/contracts.tsv»; NNN 022 и 032 |
| `bash fixtures/check_judge_gate/red_peresnjatie_bazlajna.sh`, два запуска | rc 1 / rc 1, р0 зелёный, р1 красный из-за отсутствующего `--retake` |
| `bash scripts/verify_antiplacebo.sh . --scope check_staged` | rc 0; 23 фикстуры, 23 повторных красных предъявления |
| `bash scripts/verify_antiplacebo.sh . --scope check_judge_gate` | rc 0; 3 фикстуры, 3 повторных красных предъявления |
| `bash scripts/check_zones.sh .` | rc 0 |
| `bash scripts/check_staged.sh .` с явной identity architect | rc 0, staged пуст |

Первый дополнительный запуск `check_staged` без identity в свежем клоне вернул rc 1 «identity автора пуста». После передачи `GIT_AUTHOR_NAME=architect`, `GIT_AUTHOR_EMAIL=architect@dev-harness.local` — rc 0. Этот отказ окружения не скрыт и не принят за регрессию фикса.

Между двумя сериями red-прогонов выдержан интервал 1,1 с, чтобы не повторять секундный seed известного `srand()`; генератор не правился, прежний совет не переоткрывался.

## Решение

Критерии закрытия Б1/Б4 из РЕШЕНИЯ `9f86d1c` применены. Нового блокирующего дефекта самой дельты не обнаружено. Заморозка предъявленного текста разрешается в границах этого scoped-вердикта; это не приёмка ещё не выполненной реализации.

Пробы и сырые логи сохранены в `/tmp/dev-harness-verify/critic-031-k3/`: `verify.py`, `probe-в11.sh`, `probe-в12.sh`, `probe-r10.sh`, `check_scope.py`, файлы `*.log`. Вердикт передан полным текстом; приземление выполняет оркестратор с author=critic согласно заданию.
