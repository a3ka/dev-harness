accept

# Контракт 024, v2 → v3: механическое СПАСЕНО

Судимый HEAD основного main: `542c7bca847cf77af0d42a97898c3dddc87e5cf6`, установлен
`git -C /home/aka/Documents/dev-harness rev-parse HEAD`. Клон:
`tmp/critic024v3/repo`. Судится только дельта контракта относительно
`frozen/contracts/024/2`, не реализация детектора и не повторная приёмка всего 024.
Полный файл контракта прочитан, включая критерии после реализации.

Набор артефактов полон: предмет — `contracts/024-detektor-utechek-osnovnogo-chekauta.md:41-169`,
rc-критерии — `:274-309`, исполнители architect и implementer, их зоны и исключения —
`:255-264`. Текст закоммичен в `2de9f512`, принесён merge `c13041d9`.
При снятии клона высшая заморозка 024 — v2; этот вердикт предназначен для v3.

## Что установлено

- Полный `git diff frozen/contracts/024/2..542c7bca -- contracts/024-detektor-utechek-osnovnogo-chekauta.md`
  содержит один добавленный абзац СПАСЕНО (`:266-272`) и пустую строку: восемь
  физических добавлений, ни одного удаления. Это одно объявление, не буквально
  одна физическая строка. Предмет, критерии и ЗОНА-строки не изменены.
- `git cat-file -e 32d580c720058175ffde95a9aa117f15b574a711^{commit}` → rc 0.
  `git show --format=fuller --stat` подтверждает автора architect и единственный
  изменённый путь `contracts/025-sreda-cwd-rc.md`; это собственная зона architect
  в 025 (`contracts/025-sreda-cwd-rc.md:399`), но не в 024 (`:257`). Обе проверки
  `git merge-base --is-ancestor` для границ `frozen/contracts/024/1 → 32d580c7 →
  542c7bca` дали rc 0. Тегов done для 024 и 025 в клоне нет.
- Прочитаны `git log --oneline -- 'verdicts/critic/contracts-025*'` и весь
  `verdicts/critic/contracts-025-v5.md`. Его опыт на старом HEAD `fbb53d53`
  показывает: спасение в 025 оставляло отказ в 024. Это цитируемое доказательство
  другого круга, а не выданный за собственный повтор того же опыта.
- Причина `:267-272` соответствует читателю: `scripts/check_zones.sh:302-316`
  исключает принесённые чужим `land: wip/<OTHER>/…` merge коммиты, но не прямой
  коммит общей истории; `:236,330` сохраняет и сопоставляет тройку
  `(автор, хеш, номер контракта)`. Поэтому исключение под 025 не действует под 024.

## Собственный прогон механизма

Только в одноразовом клоне создан пробный тег:

```
git tag frozen/contracts/024/3 542c7bca847cf77af0d42a97898c3dddc87e5cf6
bash scripts/check_zones.sh
```

Обе команды завершились с rc 0. Существенная строка фактического вывода:

```
ok   контракт 024: коммит 32d580c7 (architect) — СПАСЕНО, из суда зон выведен
```

Теги 025 при этом оставлены исходными, максимум v4. Значит именно новое объявление
024 читается из предложенного замороженного блоба и достигает заявленной цели.
Пробный тег не переносится в основной репозиторий и не является заморозкой.

`bash scripts/check_charter.sh` на исходном клоне → rc 0. Фактический вывод:

```
ok   уставной документ изменён с разрешения владельца: contracts/024-detektor-utechek-osnovnogo-chekauta.md в c13041d9
ok   уставной документ изменён с разрешения владельца: contracts/024-detektor-utechek-osnovnogo-chekauta.md в 2de9f512
```

`git show -s --format=fuller` и журнал родителей подтверждают собственную строку
`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` и в `2de9f512`, и в merge `c13041d9` с темой
`land: wip/024/architect`. На проверенном HEAD последний коммит — уже вердикт
025, а не merge. Отсутствие разрешения у старого `fbb53d53`, описанное вердиктом
025, не переносится на другую историю: здесь merge 025 — `0fcf1817`, также с
разрешением. Подлинность гранта сверх предусмотренного нормой механизма этим
прогоном не удостоверяется.

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Нет новой слабости от дельты: `:274-309` неизменны,
   а исключение `:266` ограничено существующим коммитом, который не меняет детектор
   или его критерии. Конкретного ложнозелёного обхода предмета дельтой не найдено.
2. **Готово недоказуемо командой?** Новых пунктов готовности нет. Действие объявления
   проверено самим `check_zones.sh`, разрешения — `check_charter.sh`, оба rc 0.
   Команды приёмки реализации `:278-309` здесь не выдаются за выполненные.
3. **Решение оставлено исполнителю?** `:266-272` называют автора, полный хеш и
   причину; выбирать дополнительные коммиты или менять механизм исполнителю
   не поручено. Новая раздача не возникает.
4. **Границы исполнения?** `:257-259` неизменны; путь этой правки принадлежит
   architect. СПАСЕНО — точечное историческое исключение, не расширение зоны на
   будущие изменения 025. Границы держит существующий читатель замороженных зон.
5. **Противоречие AGENTS.md?** В судимой дельте не найдено. Объявление разбирается,
   критерии не ослаблены; правило 11 о разрешении изменяющего устав коммита
   соблюдено также на merge, что важно по прецеденту Н-79.

## Совет и пределы проверки

СОВЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:266-272 — читатель СПАСЕНО
построчный: механически причиной является только «коммит правит» на строке 266;
полное объяснение находится в продолжении абзаца. При следующем необходимом
редактировании можно держать полную причину на одной физической строке. Сейчас
причина непуста, грамматика принята реальным механизмом, человеческое объяснение
точно; оснований блокировать этим заморозку нет.

`git diff --exit-code frozen/contracts/024/2 542c7bca -- scripts/check_zones.sh
scripts/check_charter.sh scripts/check_judge_gate.sh scripts/judge_gate.sh` → rc 0,
пустой вывод. Более широкий запрос с `fixtures/check_judge_gate/` дал rc 1:
`red_detektor_utechek.sh` и `red_path_fake_utilit_024.sh` отличаются от старой
заморозки; неизменность всей реализации не утверждается. Это не дельта текста
контракта и не предмет этого круга. Реализация, scoped-регресс раннера и CI здесь
не пересуживались; доказательство механической правки — прогоны читателей выше.

Блокирующих находок нет. Новый спор по повторной причине не возникает, арбитр
этим вердиктом не созывается. Разрешена заморозка проверенного текста 024 v3;
готовность реализации и done этим accept не объявляются.
