FAIL

## Пять вопросов

1. **Критерий готовности слабее предмета?** Да. Прежний обход через `contracts/` закрыт, но условие 3 по-прежнему обещает неизменность всего дерева вне `$WORK`, тогда как новая область слепка явно исключает `tmp/`, `.git/` и `.zones/`. Измеренный обход через `tmp/` — блокирующая находка ниже.

2. **Есть «готово», недоказуемое названной командой?** Да. Подставной прогон вернул 0 при сохраняющейся правке `$REPO/tmp/probe.txt` вне `$WORK`. Штатный `bash scripts/verify_antiplacebo.sh` при этом вернул 0: 16 барьеров, 98 фикстур, 98 повторных красных предъявлений.

3. **Решение молча оставлено исполнителю?** Да: контракт не говорит, входят ли объявленные реализацией исключения `tmp/`, `.git/` и `.zones/` в обещание «дерево вне `$WORK`». Реализация выбрала исключения, но критерий сохранил безусловную формулировку.

4. **Границы исполнения не названы?** Нет: работа явно не раздаётся. В изолированной пробе с локальным тегом `frozen/contracts/001/1` `bash scripts/check_zones.sh .` вернул 0 и прочитал причину целиком: `кодифицирует уже действующее соглашение, заданий по нему не раздаётся`.

5. **Есть противоречие норме `AGENTS.md`?** Да: для части объявленной области «дерево вне `$WORK`» правило остаётся без механизма. Это тот же блокирующий обход, а не отдельная причина отказа.

## Находки

БЛОКИРУЕТ contracts/001-fikstura-antiplacebo.md:61-62 — условие 3 без исключений обещает, что названный прогон доказывает неизменность дерева вне `$WORK`; `snapshot_of` исключает весь `$REPO/tmp`, поэтому команда этого обещания не доказывает.
ОБХОД: в том же изолированном подставном дереве фикстура сначала предъявила игрушечный барьер зелёным, затем записала `$REPO/tmp/probe.txt`, испортила состояние только внутри `$WORK` и предъявила барьер красным. `bash scripts/verify_antiplacebo.sh tmp/tree-snapshot-probe` вернул 0 (`1` барьер, `1` фикстура, `1` повторное красное), а `tmp/probe.txt` после прогона содержал `изменено фикстурой`. Объявленная команда выполнена, но файл вне `$WORK` изменён и условие 3 не соблюдено.

АРБИТР: созвать по повтору одной причины во втором `FAIL`. Предмет спора: обязан ли контракт сузить условие 3 до механически наблюдаемой области с явными исключениями либо проверяющий обязан наблюдать всё, что контракт называет «деревом вне `$WORK`».

## Замеры на HEAD `286a124e3d84a46108948b2262ce5c3fce18259f`

- Прежняя проба через `$REPO/contracts/probe.txt`: `bash scripts/verify_antiplacebo.sh tmp/tree-snapshot-probe` → 1; назван `./contracts/probe.txt`. Прежний обход закрыт.
- Новая проба через `$REPO/tmp/probe.txt`: та же команда → 0; `1` барьер, `1` фикстура, `1` повторное красное; файл сохранился вне `$WORK`.
- `bash scripts/verify_antiplacebo.sh` → 0; `16` барьеров, `98` фикстур, `98` повторных красных предъявлений.
- `bash scripts/check_zones.sh .` на локальном теге заморозки → 0; причина отказа от раздачи прочитана целиком.
