FAIL

## Пять вопросов

1. **Критерий готовности слабее предмета?** Да. Условие 3 обещает неизменность всего дерева вне `$WORK`, но названная команда не наблюдает всё это дерево. Это блокирующая находка ниже.

2. **Есть «готово», недоказуемое названной командой?** Да. `bash scripts/verify_antiplacebo.sh` вернул 0 при сохраняющейся правке `contracts/probe.txt` вне `$WORK`; следовательно, команда не доказывает условие 3. Условия 1 и 2 замером подтверждены: штатный прогон дал 16 барьеров, 98 фикстур и 98 повторно предъявленных красных состояний.

3. **Решение молча оставлено исполнителю?** Нет блокирующей неопределённости: новых заданий контракт не создаёт, формат фикстуры, канал вызова, окружение, коды и остаточный `cognitive-only`-риск названы.

4. **Границы исполнения не названы?** Нет: работа явно не раздаётся. На незамороженном клоне `bash scripts/check_zones.sh .` вернул 0, но сообщил `замороженных контрактов: 0`, поэтому сам по себе этот прогон объявления не читал. В изолированной копии с локальным пробным тегом `frozen/contracts/001/1` тот же барьер вернул 0 и напечатал `ok contracts/001-fikstura-antiplacebo.md — работа не раздаётся`, то есть грамматику строки разобрал.

5. **Есть противоречие норме `AGENTS.md`?** Да: заявленная область «дерево вне `$WORK`» шире механической области слепка. Для каталогов вне перечисления проверяющего получается правило без механизма. Это тот же блокирующий обход, а не отдельная причина отказа.

## Находки

БЛОКИРУЕТ contracts/001-fikstura-antiplacebo.md:62-63 — условие 3 утверждает, что команда доказывает неизменность всего дерева вне `$WORK`, однако `snapshot_of` в `scripts/verify_antiplacebo.sh:186-195` хеширует только `scripts`, `fixtures`, `roles`, `config`, `.omp`, `plans` и файлы в корне. В частности, `contracts/` и `verdicts/` не наблюдаются.
ОБХОД: в изолированном подставном дереве фикстура сначала предъявила игрушечный барьер зелёным, затем записала `$REPO/contracts/probe.txt`, испортила состояние только внутри `$WORK` и предъявила барьер красным. `bash scripts/verify_antiplacebo.sh tmp/tree-snapshot-probe` вернул 0 (`1` барьер, `1` фикстура, `1` повторное красное), а `contracts/probe.txt` после прогона содержал `изменено фикстурой`. Объявленная команда выполнена, но дерево вне `$WORK` изменено и условие 3 не соблюдено.

СОВЕТ contracts/001-fikstura-antiplacebo.md:5-6 — причина отказа от раздачи перенесена на следующую физическую строку. Барьер читает только строку, начинающуюся с `РАБОТА НЕ РАЗДАЁТСЯ:`, поэтому в измеренном выводе причина оборвалась на слове `ни`; разместить её целиком в строке объявления устранит расхождение между текстом и тем, что фиксирует механизм.

## Замеры приёмки на HEAD `1142580c77b42cd4a9fdc68fe11dec65f0420bf2`

- `rm -rf tmp/antiplacebo && bash scripts/verify_antiplacebo.sh` → 0; `16` барьеров, `98` фикстур, `98` повторных красных предъявлений.
- `npm run check:antiplacebo` → 0; тот же итог `16 / 98 / 98`.
- `npm run check:ci-parity` → 0; `8` workflow-команд, `13` скриптов приёмки, `5` исключений, `0` расхождений.
- `bash scripts/check_zones.sh .` → 0; до заморозки прочитано `0` контрактов. Изолированная проба после локального тега прочитала объявление и вернула 0.
