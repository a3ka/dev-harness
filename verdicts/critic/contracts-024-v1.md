FAIL

Судимый предмет: дельта замороженного v1 `cedaf6f` → кандидата v2 на `main` @
`28c69ce`; это круг 1 по дельте v1→v2, а не повторный разбор принятого v1.
Разрешение владельца присутствует в изменяющем коммите `c2b3b2e` и merge
`28c69ce`.

стенограмма: check_no_leak --check → rc=0

## Сверка заявленной дельты

`git diff cedaf6f..28c69ce -- contracts/024-detektor-utechek-osnovnogo-chekauta.md`
показал ровно заявленные три изменения: `ТРИ` → `ЧЕТЫРЕ` на строке 88,
добавление пункта (г) на строке 105 и зеркальной rc-строки приёмки на строке
299. Вне этих трёх изменений байтового дрейфа от v1 нет; критерии v1 не удалены.
`bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` завершился rc=0:
честная форма прошла 18 ворот, слабые формы ворот 15–18 погибли по именованным
причинам, плацебо `exit 1` отвергнуто. Понижения 14→18 не обнаружено.

Замороженные границы исполнения не противоречат добавлению: носитель rc-команды
`fixtures/check_judge_gate/red_stenogrammy_sudej_024.sh` входит в `ЗОНА
architect: fixtures/check_judge_gate/`; вердикты остаются собственными
артефактами названных судей, а не реализационной зоной implementer. Н-39
соблюдена: «стабы к ветвям привязывает architect по коду, НЕ проза контракта;
контракт несёт инварианты + rc-команды».

## Блокирующие находки

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:105 — обещание
«каждый вердикт-файл, закоммиченный после приземления» сильнее названной
rc-команды: фикстура перечисляет только впервые добавленные файлы через
`git log --diff-filter=A`. Изменение уже существующего вердикт-файла после
границы вообще не попадает в проверяемое множество. Это не гипотеза: в
одноразовом клоне точная норма была приземлена коммитом `26b3636`, затем
существующий `verdicts/critic/contracts-024-v1.md` был закоммичен без
стенограммы коммитом `fc5dcf3`; `red_stenogrammy_sudej_024.sh` вернул rc=0.
ОБХОД: после приземления нормы судья обновляет на месте любой существовавший до
границы файл в `verdicts/critic|adversary|reviewer|arbitration/`, не добавляет
строку вызова детектора с rc; объявленная команда остаётся зелёной, хотя
обязанный носитель (г) отсутствует.

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:299 — граница
названа как «коммит приземления норма-строки», но команда вычисляет последний
коммит, тронувший `roles/orchestrator.md`. Более поздняя правка роли сдвигает
границу и вычёркивает из проверки уже совершённые нарушения. В одноразовом
клоне новый `verdicts/reviewer/experiment-024.md` без стенограммы после границы
дал rc=1 с именем файла; после следующего коммита `3a74b19`, менявшего только
`roles/orchestrator.md`, та же фикстура вернула rc=0, хотя виновный вердикт
остался в дереве без стенограммы.
ОБХОД: приземлить норму, закоммитить новый вердикт без вызова детектора, затем
до done-коммита внести независимую правку в `roles/orchestrator.md`; на done
`red_stenogrammy_sudej_024.sh` возвращает rc=0, а контрпример ветви B —
вердикт без носителя вызова — остаётся в истории.

## Итог

Добавленный (г)-якорь действительно краснит первый **новый** файл без
стенограммы (в эксперименте — rc=1), но не держит заявленную тотальность по
всем закоммиченным после границы вердиктам и допускает сброс самой границы.
Поэтому дельта пока имеет два именованных состояния дерева, где её rc-критерий
выполнен, а обязательного носителя нет.
