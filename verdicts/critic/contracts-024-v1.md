accept

круг 1: FAIL — см. git-историю файла

Судимый блоб: `wip/024/architect` @ `94438c5` (`e27d259` — фикстуры,
`94438c5` — контракт и А-113/А-114). Набор закоммичен и полон: предмет,
приёмочный критерий, исполнители, `ЗОНА`-строки и явная
`РАБОТА НЕ РАЗДАЁТСЯ` присутствуют. Круг 2 проверяет закрытие пяти блокеров
вердикта `4d1d265` и не-регрессию его плотных мест.

## Закрытие блокеров 4d1d265

### 1. Мера status заменена мерой содержимого — ПЛОТНО

ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:49-68,135-140 —
предмет теперь задаёт `porcelain -uall -z --no-renames`, пофайловые sha256,
раскрытие untracked-каталогов и рекурсивный манифест gitlink с `@head`; ignored и
утечка, успевшая попасть в коммит, явно вынесены из предмета в
contracts/024-detektor-utechek-osnovnogo-chekauta.md:172-180,220-224.

Контрольная команда в одноразовом клоне (честная форма подставлена как живой
субъект):

```
cp fixtures/check_judge_gate/stab_detektor_chestnyj.sh scripts/check_no_leak.sh
bash fixtures/check_judge_gate/red_detektor_utechek.sh
```

Два независимых прогона: rc 0 и rc 0. В каждом именованно прошли три исходных
контрольных эксперимента: ворота 9 — допись в уже ` M` tracked-путь, ворота 10 —
новый файл под существовавшим до снимка untracked-каталогом, ворота 11 — допись
в уже грязном gitlink/submodule. Слабые формы старой семантики проверены командой
`bash fixtures/check_judge_gate/probe_slabyh_detektora.sh`, два прогона rc 0:
`porcelain-множество` умерло на воротах 9, `свёртка-каталогов` — на 10,
`без-gitlinkов` — на 11; в каждом случае проба сверила номер ворота и токен
причины, а не только ненулевой rc.

### 2. cwd-симметрия предъявлена в обе стороны — ПЛОТНО

ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:70-77,144-150 —
ворота 7 требуют грязную жертву из грязной cwd-приманки, имя жертвы и отсутствие
имени приманки; ворота 8 требуют чистую жертву из грязной cwd-приманки, rc 0,
маркер чистоты и отсутствие имени приманки.

Та же прямая команда red с честной формой: дважды rc 0, ворота 7 и 8 прошли
поимённо. Проба слабых форм: дважды rc 0; `обеих-cwd` умерла на воротах 7 с
причиной `имя приманки`, `тихий-cwd` — на воротах 8 с причиной `ожидался 0`.
Названного в v1 состояния «жертва чиста, приманка грязна, решение зависит от
cwd» при зелёном критерии больше нет.

### 3. Ветвь B сужена до явной нормы-запроса и имеет rc-сверку — ПЛОТНО

ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:80-104,157-160 —
контракт дословно задаёт одну норма-строку, владельческий канал её приземления и
rc-команду. Независимое сравнение строки 88 контракта с `NORM` в
fixtures/check_judge_gate/red_norma_stroka_024.sh:25 дало rc 0 при равной длине;
литералы совпадают целиком. Прецедент `7df7483` проверен по коммиту: он изменяет
только `roles/orchestrator.md`, помечен как исполнение оркестратором по
замороженному тексту 023 при done и с `REVIEWS: владелец`. То есть 024 использует
тот же владельческий канал, но добавляет собственную машиночитаемую fixed-string
сверку.

Прогоны:

- `bash fixtures/check_judge_gate/red_norma_stroka_024.sh` на `94438c5` — rc 1
  `норма-строка 024 не приземлена`, дважды;
- та же фикстура, скопированная в отдельный клон текущего `main` @ `4d1d265`, —
  rc 1 с той же именованной причиной;
- после добавления точного литерала в `roles/orchestrator.md` одноразового клона
  команда вернула rc 0 `приземлена дословно`.

Остаток «вовсе не вызвал» не замаскирован под механизм: он прямо назван
cognitive-only риском в
contracts/024-detektor-utechek-osnovnogo-chekauta.md:237-242. Норма требует
положительную улику — сам вызов и rc в задании каждого судьи; задание остаётся в
стенограмме спавна и наблюдается судьёй. Отсутствие вызова сверки механизмом не
ловится, что текст говорит дословно. Для объявленного узкого предмета ветви B —
приземлить норму владельческим каналом, а не построить новый замороженный гейт —
это честно названная граница с сетью аудита, а не молчаливый обход критерия.

### 4. Probe больше не принимает безымянный exit-1 — ПЛОТНО

ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:191-198,278-284 —
привязка слабой формы живёт в коде, а проба требует тройку `rc≠0 ∧ имя ворот ∧
причина`. `bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` выполнен
дважды: rc 0 и rc 0. В обоих прогонах внутренняя фаза-плацебо подставила
двухстрочный `exit 1` и напечатала `плацебо exit-1 отвергнуто: смерть без
диагностики фазы не удовлетворяет`. Все 13 слабых форм умерли каждая на своём
именованном вороте с ожидаемым токеном причины; честная форма прошла 14/14.

### 5. Относительный корень имеет красный вход — ПЛОТНО

ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:43-48,165-168 —
ворота 12 предъявляют относительный корень отдельно для `--snapshot` и
`--check`: оба обязаны вернуть rc 1 и `корень обязан быть абсолютным` до `cd`.
Прямая команда red с честной формой: дважды rc 0, ворота 12 прошли именованно.
Проба: дважды rc 0; форма `без-абс-корня` умерла на воротах 12 с причиной
`ожидался 1`. Обход v1 «относительный существующий путь молча канонизирован»
закрыт наблюдаемым входом.

## Не-регрессия dense_checks круга 1

- Прямая red-команда с честной формой: дважды rc 0, все 14 ворот. Повторный
  снимок держится воротами 6. Старое ворото 8 «снимок не загрязняет дерево»
  перенесено без потери свойства: `снимок-в-дереве` умирает на воротах 2, а
  байтовая само-чистота пары snapshot/check проверяется воротами 14.
- `bash fixtures/check_judge_gate/canary_zhivoj_024.sh <абс-корень-клона>` с
  честной формой: дважды rc 0; каждый запуск сделал snapshot, check и повторный
  check, все три rc 0; porcelain до/после двух запусков совпал (rc сравнения 0).
- `bash scripts/verify_antiplacebo.sh . --scope check_judge_gate` — rc 0:
  `барьеров: 1 · фикстур: 3 · предъявлено красным повторным прогоном: 3`.
- `bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` — rc 0 дважды:
  прежние `всегда-чисто`, `снимок-в-дереве`, `без-снимка-пропускает`,
  `первый-снимок-навсегда`, `своя-cwd` и все восемь добавленных слабых форм
  погибли на собственных именованных воротах; честная форма осталась зелёной.
- Красное исходного дерева не подменено зелёным: `red_detektor_utechek.sh` — rc 1
  дважды с `детектор отсутствует`; `canary_zhivoj_024.sh <абс-корень>` — rc 1
  дважды с той же причиной; `red_norma_stroka_024.sh` — rc 1 дважды с
  `норма-строка 024 не приземлена`.

## Советы

СОВЕТ fixtures/check_judge_gate/red_norma_stroka_024.sh:31-32 — `grep -qF`
доказывает наличие точного литерала как подстроки, но не равенство полной строки
и не единственность вхождения. Контрольный HTML-comment с точным литералом тоже
дал rc 0. Это не отменяет accept: объявленный предмет ветви B — fixed-string
норма-запрос в raw-файле роли, а исполнение явно оставлено cognitive-only и
аудируемым; если автор хочет, чтобы слово «строка» означало полнострочное
совпадение, `grep -Fx` и проверка единственности скажут это точнее.
