FAIL

Судимый блоб: `wip/024/architect` @ `081098a` (драфт `8e8f480`, красные `9a07b96`, А-112 `081098a`). Набор закоммичен и полон: Предмет, Приёмочный критерий, `ЗОНА` и названные исполнители присутствуют.

## Блокирующие находки

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:49-52,99-101 — снимок и сравнение заданы как множество строк обычного `git status --porcelain`, а ворота 1 предъявляют только две новые строки на прежде чистых путях. Это слабее предмета «ловить утечки правок»: строка porcelain описывает состояние пути, не его содержимое, а обычный untracked-режим ещё и сворачивает каталог. Честная форма из красных проходит все 8 ворот, но в трёх контрольных экспериментах вернула rc 0 «основной чекаут чист» после реальной новой записи: (1) tracked-файл уже имел ` M` в снимке, затем в него дописана утечка; (2) в существующий до снимка untracked-каталог добавлен новый файл; (3) tracked-файл уже был изменён внутри submodule, затем туда дописана утечка. Тем самым И-5 разрешает базе маскировать дальнейшую правку того же porcelain-имени; `--untracked-files=normal` маскирует новый файл за прежним `?? dir/`; submodule маскирует вложенную правку за прежним ` M sub`.
ОБХОД: дерево содержит реализацию, побайтово равную `fixtures/check_judge_gate/stab_detektor_chestnyj.sh`; red дважды, probe, scoped-family и канарейка на чистом main зелёные. Перед `--snapshot` файл `tracked.txt` уже изменён намеренно, после снимка субагент дописывает в него утечку; строка остаётся ` M tracked.txt`, `--check` даёт rc 0, утечка не поймана. Эквивалентный обход: до снимка есть `?? scratch/`, после снимка появляется `scratch/leak`; porcelain остаётся `?? scratch/`. Если намеренная правка вместе с утечкой затем попала в вердикт-коммит до ближайшей сверки, рабочее porcelain вообще исчезает и также считается «чисткой».

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:105-106; fixtures/check_judge_gate/red_detektor_utechek.sh:167-183 — ворота 7 доказывают лишь наличие имени жертвы. Хотя приманка намеренно загрязнена в строках 172-173, код не запрещает имя приманки и не предъявляет симметричный вход «жертва чиста, cwd-приманка грязна → rc 0». Поэтому заявленная cwd-независимость проверена не на полном различии честной и слабой форм.
ОБХОД: реализация честно проверяет аргумент-жертву, но дополнительно проверяет свой cwd и краснеет по объединению двух status. На текущих воротах 1-6 и 8 cwd совпадает с жертвой; на воротах 7 в выводе есть требуемое случайное имя жертвы, поэтому red rc 0, даже если туда же попало имя приманки. На входе с чистой жертвой и грязной cwd-приманкой такая реализация возвращает rc 1 вместо договорного rc 0, то есть cwd влияет на решение при всех зелёных командах критерия.

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:59-68,113-114,180,211 — ветвь B требует вызова перед каждым судьёй и land/freeze/done, но ни один исполняемый носитель вызова не входит в работу: `roles/orchestrator.md` объявлен `РАБОТА НЕ РАЗДАЁТСЯ`, а `check_judge_gate.sh`, `judge_gate.sh`, `land_agent.sh` и `freeze_contract.sh` не правятся. Поиск `check_no_leak` в блобе на HEAD находит только контракт, HANDOFF/NABLIUDENIA и новые red/stab/probe; действующего вызова из церемонии нет. У И-10 и соответствующего пункта приёмки нет rc-команды — только чтение будущей норма-строки. Это прямо противоречит `AGENTS.md:121-124` («что значит готово» для кодовой задачи — красная команда, не чтение) и `AGENTS.md:204-205` («правило без механизма не существует»); сам контракт одновременно измеряет дисциплину мёртвой и оставляет «вовсе не вызвал» на дисциплину.
ОБХОД: дерево содержит исправный `scripts/check_no_leak.sh` и дословную строку в `roles/orchestrator.md`; все названные rc-команды и CI зелёные, но церемониальные скрипты детектор не вызывают. Оркестратор пропускает ручной `--check`, после снимка в main появляется утечка, затем судья спавнится: критерий выполнен, заявленная связка перед судьёй отсутствует и утечка не остановила событие.

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:196-199; fixtures/check_judge_gate/probe_slabyh_detektora.sh:48-74 — контракт утверждает пары «слабый стаб ↔ своё ворото», что прямо запрещено `AGENTS.md:124-127`, а исполняемый probe проверяет для каждой слабой формы только `rc != 0`, не номер/диагноз ворота. Нынешние пять стабов действительно отличаются от честной формы названными ветвями и при прямом запуске краснеют на воротах 1/5/8/7/6, но команда приёмки этого свойства не удерживает.
ОБХОД: в копии набора `stab_detektor_vsegda_chisto.sh` заменён на файл из двух строк `#!/usr/bin/env bash` и `exit 1`; `bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` всё равно вернул rc 0 и напечатал, будто «всегда-чисто» пойман на воротах 1. Живой red проверяет другой файл — реализацию — а scoped-run намеренно не включает `stab_*`, поэтому остальные rc-критерии тоже не отличают этот плацебо от доказанной пары.

БЛОКИРУЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:41-42,124-129 — CLI требует абсолютный корень, но ни один red-вход не предъявляет относительный корень и честная форма не проверяет абсолютность: `cd "$ROOT_ARG" && pwd -P` молча принимает относительный путь. Неизвестный режим проверен кодом кандидата, абсолютность — только прозой.
ОБХОД: реализация, равная честной форме стабов, принимает `--snapshot relative/repo` и `--check relative/repo`; она проходит все 8 ворот, probe и чистую канарейку, хотя объявленный CLI «абсолютный корень» не реализован.

## Проверенные плотные места

- ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:44-48,107-108 — снимок внутри стерегомого дерева не проходит. Прямой запуск слабой формы дошёл до ворот 8 и вернул rc 1 на байтовом сравнении сохранённого в памяти porcelain: появился `?? .leak-snimok`.
- ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:102-104 — «первый снимок навсегда» пойман именно на воротах 6, rc 1; повторный снимок обязан обновить базу. Один снимок на пачку и повторные `--check` против него сами по себе не дают найденного обхода. Дыра ветви B — не в необходимости переснимать перед каждым судьёй, а в отсутствии обязательного вызова `--check` вообще.
- ПЛОТНО contracts/024-detektor-utechek-osnovnogo-chekauta.md:94-98 — формы «всегда чисто» и «без снимка пропускает» дали rc 1 именно на воротах 1 и 5. Именованные причины совпали с договором.
- НЕ ПЛОТНО для status-класса — ignored-файлы обычный porcelain не показывает вовсе; контракт ограничивает конформную «утечку» новой porcelain-записью, но это молча уже общего предмета «утечки правок в основной чекаут». Исправление главного блокера должно либо явно сузить Предмет/Не-цели, либо предъявить режимы porcelain отдельными входами; одной формулировки недостаточно.

## Советы

СОВЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:46-48 — восьмизначный префикс SHA пути создаёт общее имя снимка при коллизии двух канонических корней. Это не предъявленный практический обход текущей боли, но полный digest либо каталог с проверяемой записью исходного корня убрал бы ненужную 32-битную неоднозначность.

СОВЕТ contracts/024-detektor-utechek-osnovnogo-chekauta.md:109-112,206-209 — живая канарейка описана последовательностью ручных команд и чтением porcelain, а не одним rc-оракулом. Toy-ворота 8 уже доказывают само-чистоту кандидата; если main-канарейка остаётся отдельным критерием, ей полезен один исполняемый wrapper с финальным rc.

## Прогоны на одноразовых деревьях

- `bash fixtures/check_judge_gate/red_detektor_utechek.sh` — rc 1, два прогона; оба раза дословная причина: `детектор отсутствует — scripts/check_no_leak.sh`, совпадает с контрактом:191-193.
- `bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` — rc 0.
- `bash scripts/verify_antiplacebo.sh . --scope check_judge_gate` — rc 0; 1 барьер, 3 фикстуры, 3 красных повторных прогона.
- red с честной формой — rc 0, все 8 ворот.
- red со слабым `всегда-чисто` / `без-снимка-пропускает` / `снимок-в-дереве` / `своя-cwd` / `первый-снимок-навсегда` — rc 1 соответственно на воротах 1 / 5 / 8 / 7 / 6.
- Контрольные эксперименты честной формы: допись в уже ` M tracked.txt` → rc 0; новый файл под уже `?? untracked/` → rc 0; допись в уже грязном submodule → rc 0.
- Probe с двухстрочным стабом `exit 1` вместо `всегда-чисто` — rc 0 (плацебо принято).
