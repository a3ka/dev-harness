FAIL

Предмет: `contracts/049-dver-minta-zamena-na-frozen.md` на ветке `wip/049/architect`, HEAD
`5bc35d73ab446fa4c38ff7478a7ae654dbd3e1d8`. Круг 1, гейт перед заморозкой. Прочитан целиком.
Реализации (check_staged.sh/check_zones.sh) не судил — не тронуты (сверено диффом против
`a15d818`).

## БЛОКИРУЕТ `contracts/049-dver-minta-zamena-na-frozen.md:168` (§Приёмочный критерий)

Пробы Р1-Р16 НЕ ОТЛИЧАЮТ исправление, реально снимающее origin-проверку в `check_staged.sh`
(предмет строк 14-15 и инвариант §Инварианты п.3: «провенанс формы замены проверяется ТОЛЬКО
ЛОКАЛЬНО в обеих копиях»), от МУТАНТА, который эту origin-проверку СОХРАНЯЕТ.

**ОБХОД:** реализовать все проверки формы замены корректно, но в `check_staged.sh` ОСТАВИТЬ
(не удалить) требование `ls-remote origin` для frozen-тега — точно так же, как оно СЕЙЧАС
стоит для `id/CONTRACT/NNN`. Все ТРИ новые положительные клетки семьи `check_staged`
(`zamena_na_frozen`, `zamena_ne_maksimalnaja_versija`, `staraja_stroka_proizvolnaja`) в своих
зелёных входах СНАЧАЛА пушат frozen-тег на origin (это удобство построения toy-истории, не
требование теста) — значит мутант с сохранённой origin-проверкой пройдёт ВСЕ клетки
неотличимо от честной реализации. Ни одна клетка не строит вход «frozen-тег живой ТОЛЬКО
локально, не запушен» для `check_staged.sh`.

Это не формальность: ИМЕННО эта дыра — предмет §Риски 1 (порядок `freeze_contract.sh`: тег
создаётся локально шагом 7, самокоммит реестра — шагом 9, БЕЗ пуша между ними). Если мутант с
origin-проверкой пройдёт приёмку контракта, штатная заморозка ПРОДОЛЖИТ падать — ровно та боль
(Н-153/Н-156), которую контракт должен закрыть.

## Ответ по вопросу architect (ancestor-мера для check_zones) — подтверждён живьём, обхода нет

Построена независимая история (v1-target `ec0e3d8` → A=`cafecd81` mint→v1 → v2-target `edef6b8`
→ C2=`dc0614d6`): `git merge-base --is-ancestor v1^{commit} A` → 0 (v1 НЕ предок A — сам коммит
A это и есть замена на v1); для v2 → 1 (v2 предок A НЕ является, т.к. v2 создан позже A по
судимой истории — уточнить порядок). `git for-each-ref --merged=A` показал только v1. Мера
«anctor судимого коммита» действительно отличает предложенную клетку корректно: «максимум
живых тегов сейчас» дал бы v2 и НЕВЕРНО отверг бы легитимный исторический коммит A — это и есть
устраняемая ancestor-мерой ретроактивность. Обхода САМОЙ ancestor-меры не нашёл в отведённое
время; частично неисследованный остаток (боковые ветки с тем же предком, peeled-sha коллизии)
назван ниже как СОВЕТ, не блокер — требует отдельного построения toy-истории.

## Прочие живые проверки (не блокеры)

- `bash scripts/verify_antiplacebo.sh --scope check_staged check_zones` → rc=1, 71 клетка, 65
  предъявлены красным, ровно 6 FAIL «нет положительного контроля»: `zamena_na_frozen`,
  `zamena_ne_maksimalnaja_versija`, `staraja_stroka_proizvolnaja` в обеих семьях — соответствует
  заявленному красному предъявлению контракта (Р1,Р4,Р13-Р16).
- `check_precision_gate.sh` → rc=0, OK (самоприменение двух изменяемых барьеров пропущено
  гейтом явно, как положено).
- `check_threat_model`, `check_provodka`, `check_consumers`, `check_spec_ready` → rc=0 каждый.
- Р7 (`git diff --exit-code --diff-filter=MD --no-renames a557899...HEAD -- fixtures/
  check_staged fixtures/check_zones`) → rc=0: старые фикстуры не тронуты.
- `bash -n` обоих барьеров → rc=0.
- Живой `check_zones.sh .` на реальном дереве → rc=1, ровно один FAIL: `2db10bfe
  registry/contracts.tsv` (Н-156, ожидаемо — фикс ещё не реализован).
- Границы исполнителей, отсутствие `ЗОНА orchestrator` — сверены, замечаний нет.

## СОВЕТ (не блокер)

`contracts/049-dver-minta-zamena-na-frozen.md:171` — у Р11 колонка ПОСЛЕ описывает
«исчезновение строки», но не называет ожидаемый rc и команду, которая это проверяет явно
(строка «в выводе нет 2db10bfe» описательна, не исполняема буквально как отдельная проба).

СОВЕТ (не блокер, для следующего круга): ancestor-мера для check_zones — остаточный вопрос,
не полностью закрытый обходом: если v2 существовал на НЕ-предковой ветке ДО коммита A, либо
если peeled sha двух тегов совпадают, ancestor-проверка может дать неожиданный результат;
собственный контрпример не построен (запрет записи оборвал работу). Следующий круг должен
это исследовать явно, не как повтор этой находки.

## Что я НЕ судил

Реализацию (её ещё нет — только контракт+фикстуры); НАБЛЮДЕНИЯ architect (А-278…А-281) —
вне предмета этого круга.
