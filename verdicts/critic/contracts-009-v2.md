FAIL

## Проверка предмета и меры

- Предмет закоммичен на HEAD `4953943`; предмет, исполняемая приёмка, исполнители и `ЗОНА`-строки присутствуют.
- Независимый пересчёт исходников: в `scripts/check_judge_gate.sh` восемь конструкций `if want`; в `fixtures/check_judge_gate/` девять файлов `case_*.sh`. Заявленные числа 8/9 совпадают.
- `bash scripts/verify_antiplacebo.sh --scope check_judge_gate` завершился RC=0 и напечатал: `барьеров: 1 · фикстур: 9 · предъявлено красным повторным прогоном: 9`.
- Проверенные привязки входов совпадают с фактическим исполнением: `case_failfast_timeout.sh` зовёт fake напрямую с `--fail-fast=2` как первым аргументом, ждёт его и получает `rc=1`; `case_failfast_invalid_N.sh` и `case_failfast_no_timeout.sh` предъявляют реализацию 008 на точных входах `--fail-fast=0` и `--fail-fast=2`; PATH последней содержит `dirname` и `pwd`; `case_failfast_default_540.sh` подаёт bare-флаг, а ветвь запускает fake с `SLEEP_FOR=2` и ловит стаб с TBOX=1.
- Полные строки `die` различаются именем ветви в префиксе `ОТКАЗ ветвь (<имя>)`. Однако объявленная механизмом подстрока `# ПРИЧИНА:` не уникальна: три новые фикстуры используют одно `rc=1`; это отмечено советом ниже. Межветочное ложное совпадение сейчас предотвращено тем, что каждая из этих фикстур вызывает ровно свою именованную ветвь.

## Находки

БЛОКИРУЕТ contracts/009-sudja-fail-fast.md:13-24, scripts/check_judge_gate.sh:113-118, fixtures/check_judge_gate/case_failfast_timeout.sh:33-47 — ветвь по-прежнему отличает только конкретный стаб, который не разобрал флаг и потому передал fake значение `--fail-fast=2`, получив `rc=1`. Отсутствие `rc=1` не доказывает, что предмет вызвал `timeout --foreground` и отказал до завершения долгого CI.
ОБХОД: `judge_gate.sh` разбирает `--fail-fast=2`, синхронно зовёт fake с правильным `$SLOW_SHA` без `timeout`, ждёт оба его sleep, затем печатает `FAIL-FAST: превышен тайм-бокс 2 с` и выходит 1. Fake печатает `rc=slow`, а не `rc=1`; поэтому медленная ветвь зелёная. Остальные ветви проходят при обычном пробросе SHA, разборе bare/invalid и проверке наличия `timeout`, хотя главный предмет «судья НЕ ВИСИТ» и требование запуска через `timeout --foreground` не сделаны.

БЛОКИРУЕТ contracts/009-sudja-fail-fast.md:19-20, scripts/check_judge_gate.sh:147-164 — ветвь «дефолт-540» проверяет только, что default больше двух секунд, а защита N подаёт только `N=0`. Ни точное значение 540, ни поддержка произвольного натурального N не доказаны.
ОБХОД: реализация ставит для bare `TBOX=3`, принимает только единственное положительное значение `--fail-fast=2`, корректно отвергает `--fail-fast=0`, но отвергает обязательное `--fail-fast=7`. Fake с sleep 2 успевает при TBOX=3, а ни одна из восьми ветвей не подаёт 7; все объявленные команды приёмки остаются зелёными при невыполненных требованиях предмета.

АРБИТР: `arbiter` — это повтор обеих причин `FAIL` круга 1: медленная ветвь всё ещё допускает синхронное ожидание без timeout, а покрытие default/области N всё ещё слабее предмета. Предмет спора: является ли отсутствие конкретной подстроки `rc=1` доказательством fail-fast для любого корректно разбирающего флаг стаба и доказывает ли sleep 2 точный default 540 и весь домен натуральных N.

СОВЕТ fixtures/check_judge_gate/case_failfast_timeout.sh:2, fixtures/check_judge_gate/case_failfast_invalid_N.sh:2, fixtures/check_judge_gate/case_failfast_no_timeout.sh:2 — три фикстуры объявляют одинаковую причину `rc=1`. Текущий точечный вызов именованной ветви не даёт им зачесть красное другой ветви, но уникальная ветвеспецифичная подстрока сделала бы это свойство явным и устойчивым к переходу на общий прогон.

СОВЕТ contracts/009-sudja-fail-fast.md:41-63, contracts/009-sudja-fail-fast.md:97, fixtures/check_judge_gate/case_failfast_timeout.sh:5-6,34 — текст сохранил снятую проверку `ELAPSED_MS`, дважды описал `фailfast-медленный`, оборвал описание `фailfast-дефолт` и не описал `фailfast-дефолт-540`, тогда как барьер уже перешёл на маркер `rc=1`. Перед следующим решением текст критерия стоит привести к единственному фактическому механизму.
