accept

# Критик: контракт 042 v3 — исполнение решения арбитража

Предмет: `contracts/042-excuse-for-otstavshij-roditel.md` на HEAD `f9ae635`
(изменение v3 — `4a22764`, сохранение длинных строк — `f9ae635`). Суд выполнен
в собственном клоне. Арбитраж `verdicts/arbitration/042-dostatochnost-merge-base-semantiki.md`
(`fc4de39`) прочитан целиком, включая таблицу З1–З4 и §Граница третьего круга.
Выбор оракула и кандидата Б не переоткрывался. Сверены `AGENTS.md`, Н-127 в
`NABLIUDENIA.md`; перед вердиктом выполнен поиск по обоим названным в задании файлам.

Набор полон: предмет и команды приёмки закоммичены, исполнители и зоны названы.
Действительных блокирующих находок нет. Это обычный круг по решению арбитра,
не новый арбитраж и не разрешение на done без остальных гейтов.

## Независимые живые замеры

Git 2.55.0. Четыре toy-репозитория созданы критиком с нуля, без копирования
фикстур архитектора или репозиториев арбитра. Барьер — неизменённый файл из
судимого HEAD. Оракул запускался отдельно; проверялись его rc, наличие пути в
результирующем дереве и побайтовое присутствие пути в NUL-секции конфликтов.

| Проба | Отдельный оракул | Новый `check_protected.sh` |
|---|---|---:|
| З1: тот же OID, mode 100644→100755 на side; D1 с ALLOW; настоящее слияние предъявило `CONFLICT (modify/delete)`, разрешено удалением без ALLOW | rc=1, путь в конфликтах и в дереве результата | rc=1 |
| З2: O→A с ALLOW; B с деревом O; P1=A,B восстанавливает путь; P2=B,A без пути; M=P1,P2 без пути и без ALLOW | `merge-base --all` даёт 2 базы; оракул rc=0, путь присутствует, конфликтов по нему нет | rc=1 |
| З3, законная полярность: `verdicts/adversary/v a.md`, ALLOW в кавычках, отставшая ветка не трогала путь | rc=0, пути нет, конфликта нет | rc=0 |
| З3, повторное удаление: восстановление после законного слияния; бесконфликтный мерж независимой side2 и ручной `git rm` без ALLOW | rc=0, путь присутствует, конфликта нет | rc=1 |
| З4: законное удаление с ALLOW, отставшая ветка путь не трогала, тело мерж-коммита без ALLOW | rc=0, пути нет, конфликта нет | rc=0 |

Все четыре строки арбитра подтверждены, обе полярности З3 различены.
**rc оракула и rc барьера не смешиваются:** в З2 оракул возвращает 0, но
присутствующий в авто-мерже путь требует отказа барьера с rc=1.

Дополнительно:

- В **реальном** `/home/aka/Documents/dev-harness` команда
  `bash scripts/check_protected.sh .` завершилась **rc=0**. Пройдено 1998 коммитов;
  исчезло без основания 0, с разрешения 23, перенесено 4. Все три пути
  `verdicts/critic/contracts-040-v{2,3,4}.md` признаны исчезнувшими с явного разрешения.
- `bash fixtures/check_protected/repro_n127_otstavshij_roditel.sh` — **rc=0**,
  законное удаление за отставшим родителем распознано.
- Все **22 старых `case_*.sh`** сопоставлены именно **по байтам** с блобами `fc4de39`,
  включая `case_slijanie_otstavshego_roditelja.sh` и
  `case_merge_povtornogo_udalenija.sh`: **22/22 неизменны**. Независимый список файлов
  даёт 25 текущих `case_*.sh`, то есть добавлены ровно три.
- `bash scripts/verify_antiplacebo.sh --scope check_protected` предъявил
  **25/25 красных повторных прогонов**, каждый с зелёным контролем и ожидаемым
  диагнозом. **Общий rc команды — 1, не 0:** до scoped-прогона обнаружены три
  уже названных в контракте внешних расхождения: неклассифицированный
  `check_threat_model.sh`, `fixtures/check_threat_model` и
  `fixtures/parsing_hygiene_battery` без распознанного барьера. В семье
  `check_protected` расхождений нет. Этот результат не объявляется зелёным полным CI.
- Тело `excuse_for()` сверено побайтово с `fc4de39`: неизменно. Остановка на первом
  не-артефакте и разбор ALLOW не ослаблены.
- `bash -n scripts/check_protected.sh` — **rc=0**.
- Проба доступности действительно находится в начале барьера:
  `git merge-tree --write-tree --no-messages -z HEAD HEAD`.
  Отсутствие возможности смоделировано экспортированной Bash-функцией `git`,
  возвращающей 129 только на `merge-tree --write-tree` и делегирующей прочие
  команды `/usr/bin/git`. Сам файл барьера и его PATH не менялись. Получено
  **rc=2** и `NOT_IMPLEMENTED: git merge-tree --write-tree недоступен
  (нужен git ≥ 2.38) — оракул мерж-семантики Н-127/арбитраж 042 нечем вычислить`.
  Это проверка отказа при недоступной команде, не заявление о запуске старого Git.

Одноразовые независимые драйверы:
`/tmp/dev-harness-verify/critic042v3.py`,
`/tmp/dev-harness-verify/critic042v3-finish.py`,
`/tmp/dev-harness-verify/critic042v3-no-oracle.sh`.
Toy-деревья и сырые логи — `tmp/critic/independent042/`.
Первый драйвер остановился на создании восстановленного файла после удаления
Git пустого каталога; второй явно создал каталог и завершил оставшиеся пробы.
Это ошибка одноразового построителя, не отказ барьера; результаты выше —
завершённые прогоны соответствующих сценариев.

## Пять вопросов гейта

1. Приёмка отличает предмет от имитации: законная Н-127-топология зелёная,
   повторное удаление и оба мерж-класса красные, существующая семья не переписана.
2. Критерии предъявлены командами с rc; реальный rc scoped-команды и внешний
   остаток названы выше, а не спрятаны под счётчиком 25/25.
3. Существенное решение исполнителю не оставлено: три условия оракула,
   fail-closed для неподдерживаемых кандидатов, формат state и остановка
   `excuse_for` определены. Оставшаяся устаревшая проза — совет ниже.
4. Исполнители и файловые границы названы в `ЗОНА`-строках; механизм —
   `check_zones.sh`. Предметные файлы не выходят за объявленную зону architect.
5. Нового блокирующего противоречия `AGENTS.md` в предмете, приёмке и раздаче
   не найдено. Отсутствие общего зелёного CI не выдано за его наличие.

## Советы, не блокирующие заморозку

СОВЕТ contracts/042-excuse-for-otstavshij-roditel.md:87 — в §Семантика старая мера
действительно заменена тремя условиями оракула; буквальные формулы «блоб равен базе»
и «merge-base вычисляется успешно и непусто» отсутствуют. Однако **утверждать,
что старое объяснение ушло из всего документа, нельзя**: строки 87–104 всё ещё
называют правильным сравнение с одной базой, `blob_at`, прежнюю стоимость и отказ
на неоднозначной базе. Строки 261–266 также объясняют поведение старым предикатом.
Это устаревшее обоснование рядом с уже однозначной новой операционной семантикой,
а не основание пересудить принятое арбитром решение; приведённые новые пробы
старую конструкцию не пропускают. Рекомендация — пометить этот фрагмент
историческим либо согласовать с §Семантика. Остатки записи объектов и дрейфа
версии Git в §Риски, пп.5–6, перенесены по решению арбитра.

СОВЕТ contracts/042-excuse-for-otstavshij-roditel.md:220 — фраза «правка ограничена
функциями» слишком узка: обязательная по решению арбитра стартовая проба находится
вне них. Файловая зона `scripts/check_protected.sh` её покрывает; ограничения
`check_zones.sh` не обходятся. Явно добавить стартовую пробу к описанию внутренней
границы, не меняя саму пробу и не ослабляя проверку.
