FAIL

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:165-170 — решение арбитра исполнено не полностью: две точки отсекают глобальные счётчики коммитами/видами, но смешанная точка состоит из двух РАЗНЫХ видов (`HANDOFF.md` и `NABLIUDENIA_SUDJA.md`). Поэтому гибрид «файлы в чистой ветви, виды фильтра в смешанной» получает ровно N=4 и N=6 и проходит пробу. Утверждение строки 165, что гибридная единица обязательно краснеет, неверно для прямо названного арбитром комбинированного выбора единиц по ветвям.
ОБХОД: фильтровать все три пути честно; в чисто процессной ветви увеличивать N за каждый исключённый файл, а в смешанной — один раз за каждый встреченный вид фильтра. Чистая точка из двух `verdicts/critic/*` даёт +2 по файлам; смешанная точка `HANDOFF.md` + `NABLIUDENIA_SUDJA.md` даёт +2 по двум видам. Построенный стаб `/tmp/dev-harness-critic-hybrid/scripts/check_zones.sh` именно так считает; `cd /tmp/dev-harness-critic-hybrid && bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=0. Предмет не сделан: в смешанной ветви N считает виды, а не исключённые файлы.
ВЫХОД: в смешанной ΔN=2 использовать два процессных файла ОДНОГО вида (например, два разных корневых `NABLIUDENIA*.md`) либо добавить отдельную смешанную точку такого состава; ожидаемые rc=1 по предметной половине и точный прирост +2 сохранить. Тогда счёт видами/префиксами в смешанной ветви даст +1, а файловый — +2.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:167-174 — приёмочный критерий прямо утверждает пары «стаб коммитов/видов/префиксов ↔ точки N» и «стаб любое R ↔ ветвь R100». Это противоречит старшей норме `AGENTS.md:121-127`: контракту запрещено утверждать пару «обманный стаб ↔ ветвь»; привязку делает проверяющий, у которого перед глазами код. Противоречие норме блокирует само по себе, ОБХОД по исключению роли не требуется.
ВЫХОД: оставить в контракте наблюдаемый инвариант и команды с rc, а утверждения о том, какой именованный стаб на какой точке краснеет, вынести из текста контракта в доказательство сдачи/вердикт.

После решения арбитра снова получен FAIL по тому же предмету — достаточность дискриминации единицы счёта. По `AGENTS.md:128-135` это проводка к владельцу; инженерный предмет находки — смешанная точка не различает файлы и виды при по-ветвевом гибриде.

## Что закрыто из круга 2

1. Исходный обход «N один раз на коммит» механически закрыт в ОБЕИХ ветвях: `cd /tmp/dev-harness-architect/stubZ && bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1; чистая точка напечатала N=3 вместо 4, смешанная — N=4 вместо 6. Глобальный стаб счёта видами тоже красный: `cd /tmp/dev-harness-architect/stubK && bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1; N=3 и N=5. Но эти два глобальных стаба не покрывают по-ветвевой гибрид из первой находки.
2. Выход R100 закрыт. Ветка переносит неизменённый файл класса FAIL после хвоста accept; ложное событие назначения не сжимается. `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` на HEAD → rc=1 и поимённо показывает пятую ветвь R100; `cd /tmp/dev-harness-architect/stubF && bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → rc=1 ровно на R100 (ложный кап 3); честная симуляция этой грамматики → rc=0.

## Прогоны на HEAD 80e0684

- `bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1; поимённы чистая ΔN=2 (2→4) и смешанная ΔN=2 (4→6), смешанная даёт rc=1 по предметной половине.
- `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → rc=1; поимённа пятая ветвь R100 с ожидаемым после реализации rc=0.
- `bash fixtures/check_zones/probe_protsessnye_vne_suda.sh` → rc=1; все четыре `verdicts/*` и три остальные процессные ветви поимённы.
- `bash fixtures/freeze_contract/probe_012_dva_kruga.sh` → rc=1; обе точки истории 012 поимённы.
- Копии всех четырёх проб в `/tmp/dev-harness-architect/sim13` побайтово совпали с HEAD (`cmp` → rc=0). Честная симуляция: из `/tmp/dev-harness-architect/sim13` команды `bash fixtures/check_zones/probe_protsessnye_vne_suda.sh`, `bash fixtures/check_zones/probe_svod_protsessnyh.sh`, `bash fixtures/freeze_contract/probe_012_dva_kruga.sh`, `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → каждая rc=0.
- `bash scripts/verify_antiplacebo.sh --scope check_zones` → rc=0: 1 барьер, 13/13 фикстур предъявлены красным повторным прогоном.
- `bash scripts/verify_antiplacebo.sh --scope freeze_contract` → rc=0: 1 барьер, 14/14 фикстур предъявлены красным повторным прогоном.
- `bash scripts/check_zones.sh .` → rc=0: 11 замороженных контрактов, 6 авторов, 254 коммита в диапазонах, 176 проверено по зонам.
- `bash scripts/check_contract_frozen.sh .` → rc=0: 16 планов/контрактов на HEAD, 4 черновика, 12 заморожено; контракт 013 распознан черновиком.
