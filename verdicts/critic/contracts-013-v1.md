FAIL

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:99-120 — названные команды не отличают реализованный предмет от текущего нереализованного дерева. На HEAD `27fcbd6` обе scoped-команды уже вернули rc=0 (`check_zones`: 13/13; `freeze_contract`: 14/14), а `bash scripts/check_zones.sh .` уже вернул rc=0; после реализации строки 114–115 требуют от них ровно те же коды. Требуемые переходы спрятаны в чтение `invocations.log` (строки 117–118), а контроль `circles=2` на истории 012 (строки 119–120) вообще не имеет названной команды с кодом возврата. Это пожелания, не приёмочный гейт.
ОБХОД: оставить `scripts/check_zones.sh` и `scripts/freeze_contract.sh` без изменений. На проверенном HEAD все три названные локальные команды уже зелёные, хотя ручной прогон новых case показал нереализованный предмет: процессная фикстура дала коды `[0,0,1,1,1,1,1]` (вердикт, наблюдение и HANDOFF всё ещё «коммит вне зоны»), а фикстура кругов — `[0,1,1]` с ложными `circles=6` на шуме и `circles=8` на охранной ветви.
ВЫХОД: назвать команду, которая на текущем коде возвращает ненулевой код, а после реализации возвращает 0 только если все три процессные ветви и шумовая ветвь стали зелёными, охранные остались красными; отдельно дать команду с rc, проверяющую `circles=2` на обеих точках истории 012.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:31-33,99-102 — предмет исключает каталог `verdicts/*` целиком, включая critic/adversary/review/arbitration, но единственная новая процессная проба строит только `verdicts/critic/...`; остальные три подкаталога ни одна названная приёмочная команда не различает.
ОБХОД: реализовать исключение только для `verdicts/critic/*`, `NABLIUDENIA*.md` и `HANDOFF.md`. Все семь вызовов `case_protsessnye_vne_suda.sh`, 13/13 scoped-регресс и smoke живого дерева проходят с требуемыми кодами, но вердикт adversary/reviewer/arbiter снова попадает под суд зон — корень Н-53 для этих обязательных процессных ролей не закрыт.
ВЫХОД: механически предъявить остальные виды `verdicts/*` (либо одну параметризованную пробу полного каталога) так, чтобы частный фильтр `verdicts/critic/*` не мог пройти приёмку.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:42-44,99-102 — обязательный счётчик `процессных вне суда: N` не проверяется ни кодом возврата, ни ожидаемым выводом. Новая фикстура вызывает барьер, но не утверждает содержание сводной строки.
ОБХОД: корректно отфильтровать три процессных пути, но вообще не печатать счётчик (либо всегда печатать `N=0`). Процессные вызовы станут зелёными, охранные останутся красными, scoped-команда и smoke вернут 0 — весь объявленный критерий выполнен командами, а свойство предмета о наблюдаемом skip не сделано.
ВЫХОД: добавить исполняемую пробу сводки с проверкой точного счёта как минимум для чисто процессного и смешанного коммитов; её несовпадение обязано давать ненулевой код приёмочной команды.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:59-65,103-120 — предмет Б требует события для R<100, нормировку CR/trim и неизвестного класса, а также детерминированный порядок нескольких файлов одного коммита. Новая фикстура строит только A/M/D/R100 с уже нормальными `FAIL|accept`; живая история 012 также не делает исход чувствительным к R<100, неизвестному классу или порядку разных классов в одном коммите.
ОБХОД: реализовать сжатие только A/M-событий `accept|FAIL|ESCALATE`, полностью игнорировать R<100, не нормировать CR/пробелы и обходить несколько файлов в произвольном порядке. Шумовая ветвь даст `[FAIL,accept]`, охранная — кап, три переписанные kap-фикстуры сохранят свои исходы, scoped 14/14 и заявленный замер истории 012 будут зелёными; перечисленная грамматика таймлайна останется частично не реализована.
ВЫХОД: добавить исполняемые ветви, исход которых меняют R<100, CR/trim, неизвестный класс и порядок двух файлов с разными классами в одном коммите; частичная A/M-реализация обязана вернуть ненулевой код приёмки.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:93-104 — счётные утверждения 13/14 проверяются той же scoped-командой, которая перечисляет найденные ею же case. Это прямо противоречит `AGENTS.md:210-211`: «Счётное утверждение проверяется ДРУГОЙ мерой».
ОБХОД: удалить одну фикстуру и оставить остальные зелёными для мета-раннера: scoped-команда вернёт 0 и самосогласованно напечатает уменьшенный счёт, не обеспечивая зафиксированные контрактом 13/14. Собственного независимого гейта фиксированного состава в критерии нет.
ВЫХОД: назвать отдельную команду с ненулевым кодом при расхождении ожидаемых 13/14 с независимым инвентарём, либо убрать счётные утверждения из критерия, если фиксированный состав не является контрактом.

## Советы

СОВЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:78-87 — `scripts/check_zones.sh` и `scripts/freeze_contract.sh` одновременно входят в зоны architect и implementer, хотя проза говорит, что до заморозки их не правит никто, а после — implementer. `check_zones.sh` фазу не читает и механически разрешит эти пути обоим авторам. Если scripts действительно не тронуты в пачке, убрать их из `ЗОНА architect`, чтобы граница исполнения совпадала с решением владельца.

## Выполненные проверки на HEAD 27fcbd6

- `bash scripts/verify_antiplacebo.sh --scope check_zones` → rc=0: 1 барьер, 13 фикстур, 13 предъявлены красным повторным прогоном.
- Прямой прогон `case_protsessnye_vne_suda.sh` текущим барьером → rc=1; 7 вызовов: два зелёных, затем пять красных. Именованы `verdicts/critic/...`, `NABLIUDENIA_ARCHITECT.md`, `HANDOFF.md`, предметная половина смешанного коммита `scripts/c.sh` и охранный `scripts/b.sh`; диагноз — «коммит вне зоны».
- `bash scripts/verify_antiplacebo.sh --scope freeze_contract` → rc=0: 1 барьер, 14 фикстур, 14 предъявлены красным повторным прогоном; все три переписанные kap-фикстуры сохранили именованную причину «вердикта арбитра по этому предмету нет».
- Прямой прогон `case_kap_smena_pervoj_stroki.sh` текущим барьером → rc=1: зелёный контроль rc=0, шум rc=1 с «кап кругов» и `circles=6`, охранная ветвь rc=1 с той же причиной и `circles=8`.
- `bash scripts/check_contract_frozen.sh .` → rc=0: 12 замороженных блобов совпадают, контракт 013 распознан как незамороженный черновик.
- `bash scripts/check_zones.sh .` → rc=0 на нереализованном дереве, что подтверждает первый обход.
