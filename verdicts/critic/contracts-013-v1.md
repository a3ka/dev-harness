accept

Блокирующих находок нет. План Б на закоммиченном HEAD `8874d97` отличает заявленный список исключённых записей от проверенных имитаций; предмет, критерий с кодами возврата, исполнители и `ЗОНА`-границы названы.

## План Б и чистота контракта

- Шапка фиксирует решение владельца 2026-08-26, план Б и условие его срабатывания — четвёртый круг. Предмет А задаёт маркер `процессных вне суда:`, точную последовательность путей в порядке обхода (коммиты от старых к новым, внутри коммита по пути), повтор пути на каждую следующую исключённую запись и пустой хвост при нуле исключений.
- Поиск `(^|[^[:alnum:]_])N([=[:space:][:punct:]]|$)|ΔN|N=` в `contracts/013-processnye-artefakty-i-schet-krugov.md` не дал совпадений: чисел старого счётчика N в контракте нет.
- Поиск `(?i)стаб|stub[A-Za-z0-9_]*|seenL|hybridC|anyR|обманк` в контракте не дал совпадений: Н-39 соблюдена, именованных связок «стаб ↔ ветвь» в прозе нет; контракт оставляет инварианты и rc-команды.
- `cmp` обеих check_zones-проб между HEAD и `/tmp/dev-harness-architect/sim13` → rc=0. Контрольные копии проб у пересобранных стабов также совпали с HEAD: один SHA-256 на 7 копий каждой check_zones-пробы; `probe_grammatika_sobytij.sh` — один SHA-256 на HEAD, `sim13` и `anyR`.

## Прямые прогоны перехода

На HEAD `8874d97` (предмет ещё не реализован):

- `bash fixtures/check_zones/probe_protsessnye_vne_suda.sh` → rc=1; поимённо красны процессные виды и отсутствующий маркер в смешанной/охранной точках.
- `bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1; поимённо красны пустой хвост при нуле, ПОВТОР и обе пары одного вида.
- `bash fixtures/freeze_contract/probe_012_dva_kruga.sh` → rc=1; обе точки поимённо дают ложный кап 4/6 вместо двух событий.
- `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → rc=1; поимённо красны CR/trim, порядок и R100.

В честной симуляции `/tmp/dev-harness-architect/sim13` те же четыре пробы → rc=0; в частности, обе check_zones-пробы с побайтово совпадающими с HEAD файлами → rc=0.

Пересобранные стабы архитектора краснеют на дискриминирующих входах:

- `seenL` (дедупликация уникальных путей): обе check_zones-пробы → rc=1; первая точка расхождения списка — повторный `HANDOFF.md`, которого в хвосте только один вместо двух.
- `stubZ` (одна запись на коммит): `probe_protsessnye_vne_suda.sh` → rc=0, `probe_svod_protsessnyh.sh` → rc=1 на чистой и смешанной парах одного вида.
- `stubK` (одна запись на вид): обе check_zones-пробы → rc=1; хвост теряет повторные записи одного вида.
- `hybridC` (файлы в чистой ветви, виды в смешанной): `probe_protsessnye_vne_suda.sh` → rc=0, `probe_svod_protsessnyh.sh` → rc=1 ровно на смешанной паре: есть только `NABLIUDENIA_ARBITRA.md`, ожидаются также `NABLIUDENIA_SUDJA.md`.
- `stubF` (фильтр только `verdicts/critic/*`): обе check_zones-пробы → rc=1; первая предметная проба называет `verdicts/adversary/`, сводная — не исключённый `NABLIUDENIA_ARCHITECT.md`.
- `anyR`: `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → rc=1 ровно на R100 — стаб ложно начисляет третий круг и включает кап.

## Собственные стаб-кандидаты критика

Четыре независимых кандидата собраны поверх честной симуляции; каждый честно фильтрует три вида, но нарушает ровно проверяемое свойство списка. `bash fixtures/check_zones/probe_svod_protsessnyh.sh` дал:

- `/tmp/dev-harness-critic-own-last` — печать только последнего исключения → rc=1, первое расхождение на добавлении второго вида;
- `/tmp/dev-harness-critic-own-reverse` — правильные пути в обратном порядке → rc=1, первое расхождение на добавлении второго вида;
- `/tmp/dev-harness-critic-own-commits` — последовательность commit-id вместо путей → rc=1 на первом исключении;
- `/tmp/dev-harness-critic-own-silentzero` — честный список при ненулевом хвосте, но молчание при нуле → rc=1 на положительном контроле с пустым хвостом.

Ни один собственный кандидат критерий не обошёл.

## Приёмка на HEAD

- `bash scripts/verify_antiplacebo.sh --scope check_zones` → rc=0: 1 барьер, 13 фикстур, 13/13 предъявлены красным повторным прогоном.
- `bash scripts/verify_antiplacebo.sh --scope freeze_contract` → rc=0: 1 барьер, 14 фикстур, 14/14 предъявлены красным повторным прогоном.
- `bash fixtures/check_zones/_schet_fixtur.sh` → rc=0; независимый инвентарь подтверждает числа 13/14.
- `bash scripts/check_contract_frozen.sh` → rc=0: 16 планов/контрактов, 4 черновика, 12 заморожено; контракт 013 распознан черновиком.
- `bash scripts/check_ceilings.sh .` → rc=0: 8 файлов персон, 1 файл правил, 12 судимых черновиков; потолки в порядке.
- `bash scripts/check_zones.sh .` → rc=0: 11 замороженных контрактов, 6 объявленных авторов, 254 коммита в диапазонах, 176 проверено по зонам.
