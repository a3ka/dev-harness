FAIL

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:48-50,144-155 — проба сводки различает молчание и `N=0`, но не доказывает объявленную единицу счёта «процессные ФАЙЛЫ»: во всех трёх её точках каждый новый коммит содержит ровно один процессный файл. Поэтому ожидаемые N=1/2/3 одинаковы и для счёта файлов, и для ошибочного счёта коммитов, содержащих процессный путь.
ОБХОД: реализовать фильтр честно, но увеличивать `N` один раз на коммит с процессными файлами, а не один раз на исключённый файл. `probe_svod_protsessnyh.sh` получит свои 1/2/3, остальные пробы, обе scoped-команды, smoke и независимый счёт фикстур тоже пройдут; смешанный коммит с двумя процессными файлами и предметным файлом даст N на единицу меньше предмета.
ВЫХОД: добавить ветвь с минимум двумя процессными файлами в одном коммите и сверкой точного прироста на два; несовпадение обязано делать rc пробы ненулевым.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:70-71,145-156 — объявленное правило «R100 событий не даёт» остаётся неотличимым от реализации, которая считает R100 событием назначения. В обеих предъявленных R100-историях ложное событие имеет тот же класс, что непосредственно предыдущее: в реплее 012 это `accept` после `accept`, в `case_kap_smena_pervoj_stroki.sh` — `FAIL` после `FAIL`; сжатие скрывает ошибку.
ОБХОД: реализовать всю грамматику, но считать событием назначения любое R, включая R100. `probe_grammatika_sobytij.sh` требует событие для R<100; `probe_012_dva_kruga.sh` всё равно сожмёт лишний `accept`; scoped 14/14 всё равно сожмёт лишний `FAIL`. Все названные команды пройдут, хотя чистое переименование R100 файла класса, отличного от последнего глобального класса, ложно увеличит `circles`.
ВЫХОД: добавить ветвь, где R100 переносит файл с первой строкой, отличной от последнего события другого файла глоба; заморозка обязана показать, что R100 круг не добавил.

АРБИТР: arbiter. Предмет спора — повтор причины находки 3 круга 1: критерий сводки по-прежнему допускает неверную единицу счёта, теперь «коммиты с процессным путём» вместо объявленных файлов.

## Закрытие пяти находок круга 1

1. Дискриминация предмета больше не спрятана в `invocations.log`: четыре отдельные команды-пробы имеют собственный rc. На HEAD 4287a29 все четыре красные rc=1 с поимённым расхождением; `probe_012_dva_kruga.sh` отдельно предъявляет обе точки истории. Исходный обход «оставить оба скрипта без изменений» закрыт.
2. `probe_protsessnye_vne_suda.sh` поимённо прогнал `verdicts/critic/`, `adversary/`, `review/`, `arbitration/`; каждая из четырёх ветвей сейчас дала барьерный rc=1 вместо ожидаемого 0. Частный фильтр только critic не пройдёт пробу — обход закрыт.
3. Молчание и всегда-нулевой счётчик теперь краснеют на сверках N=1/2/3, но свойство «считаются файлы, не коммиты» не дискриминировано — первый блокер выше.
4. Исходный частичный A/M-обход закрыт исполняемыми ветвями CR/trim, порядка файлов, неизвестного класса и R<100. Сейчас проба rc=1: CR/trim ошибочно дал кап, а порядок двух файлов ошибочно пропустил кап. Новый соседний обход R100 описан вторым блокером.
5. `bash fixtures/check_zones/_schet_fixtur.sh` независимо перечисляет `case_*.sh` и сверяет литералы 13/14; на HEAD rc=0. Удаление одной case без изменения этой меры даст rc=1 — обход закрыт.

Совет 6 принят: `scripts/check_zones.sh` и `scripts/freeze_contract.sh` отсутствуют в `ЗОНА architect` и названы только в `ЗОНА implementer` (`contracts/013-processnye-artefakty-i-schet-krugov.md:86-94`).

## Выполненные проверки на HEAD 4287a29

- `bash scripts/verify_antiplacebo.sh --scope check_zones` → rc=0: 1 барьер, 13/13 фикстур предъявлены красным повторным прогоном.
- `bash scripts/verify_antiplacebo.sh --scope freeze_contract` → rc=0: 1 барьер, 14/14 фикстур предъявлены красным повторным прогоном.
- `bash fixtures/check_zones/probe_protsessnye_vne_suda.sh` → rc=1: именованы семь процессных ветвей; все четыре `verdicts/*` дали «коммит вне зоны».
- `bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1: именованы отсутствующие точные счёты 1, 2 и 3; первые две процессные точки также дали rc=1 вместо 0.
- `bash fixtures/freeze_contract/probe_012_dva_kruga.sh` → rc=1: обе точки именованы; текущий механизм сообщил ложные `circles=4` и `circles=6`.
- `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → rc=1: CR/trim дал ложный кап `circles=3`, порядок `[accept, FAIL, accept]` ложно дал rc=0.
- Прямой `case_protsessnye_vne_suda.sh` с отдельным WORK → rc=1: два зелёных контроля, затем процессные и охранные ветви с причиной «коммит вне зоны».
- Прямой `case_kap_smena_pervoj_stroki.sh` с отдельным WORK → rc=1: зелёный контроль, ложный кап `circles=6`, охранный кап `circles=8`.
- `bash fixtures/check_zones/_schet_fixtur.sh` → rc=0.
- `git log` на `frozen/contracts/012/1` подтвердил A v1, A v2, D v1, R100 v2→v1; на `/2` добавлены M v1 и один коммит M v1 + A v2. Реплей форм истории честен.
- `bash scripts/check_zones.sh .` → rc=0.
- `bash scripts/check_contract_frozen.sh .` → rc=0: 12 замороженных блобов совпадают, 4 черновика, контракт 013 распознан черновиком.
