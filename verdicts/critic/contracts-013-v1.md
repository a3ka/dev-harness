FAIL

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:59-67,166-180 — точки N=1/2/4/6 различают файлы, коммиты, виды фильтра и префиксы/подкаталоги, но все процессные пути в точках счёта уникальны. Поэтому критерий не различает «каждое исключение файла при обходе коммитов» и «каждый уникальный процессный путь за весь диапазон»; второе не является объявленным счётом исключённых файлов и занижает N при повторной правке того же пути.
ОБХОД: честно фильтровать все три процессных вида и судить предметную половину смешанного коммита, но хранить множество путей (`seen[$path]=1`) и печатать его размер. В текущей пробе встречаются шесть разных процессных путей, поэтому такой счётчик даёт точную траекторию 1/2/4/6 и `bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=0. При двух последовательных изменениях одного `HANDOFF.md` фильтр совершает два исключения, а счётчик увеличивается один раз. Стаб `/tmp/dev-harness-critic-headcount/scripts/check_zones.sh` воспроизвёл обход: `probe_svod_protsessnyh.sh` → rc=0; `probe_protsessnye_vne_suda.sh` → rc=0, включая уже существующие в ней две последовательные версии `HANDOFF.md`. Поиск точной строки `процессных вне суда:` по `fixtures/` и `scripts/` нашёл единственную проверку N — в `probe_svod_protsessnyh.sh`; scoped-фикстуры и smoke точное N не сверяют, поэтому добавление остальных честных частей реализации обход не закрывает.
ВЫХОД: добавить исполняемую точку, где один и тот же процессный путь исключается в двух разных проверяемых коммитах, и сверять прирост N на каждом исключении (либо эквивалентную точку M/D); несовпадение обязано давать rc=1. Инвариант контракта должен однозначно назвать, считается ли каждое появление пути в обходе, а числа текста и пробы — совпасть.

## Выходы круга 3

1. Гибрид «файлы в чистой / виды в смешанной» закрыт механически. Проба на HEAD побайтово совпадает с `/tmp/dev-harness-architect/hybridC/fixtures/check_zones/probe_svod_protsessnyh.sh` (`cmp` → rc=0). На HEAD `bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1 и поимённо проходит ожидания N=1/2/4/6; смешанная точка содержит `NABLIUDENIA_SUDJA.md` + `NABLIUDENIA_ARBITRA.md` одного вида и `scripts/c.sh`. В `/tmp/dev-harness-architect/hybridC` та же проба → rc=1 ровно на смешанной точке: напечатано N=5 при ожидаемом N=6.
2. Н-39 закрыта: поиск `стаб|напечатает|коммитосчётчик` в контракте не нашёл совпадений; именованных прогнозов обманок в критерии нет. R100-ветвь не ослаблена: `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` на HEAD → rc=1 и поимённо красна пятая ветвь R100 (ложный кап 4 вместо сжатых 2 кругов). Раздел «Незаполненные требования» не заглушка: он явно оставляет исполнителю только пунктуацию сводной строки за проверяемой подстрокой и внутренний носитель уже заданной грамматики таймлайна; `bash scripts/check_ceilings.sh .` → rc=0 (8 файлов персон, 1 файл правил, 12 незамороженных черновиков с валидным разделом).

## Прогоны на HEAD 1cb49e4

- `bash scripts/verify_antiplacebo.sh --scope check_zones` → rc=0: 1 барьер, 13/13 фикстур предъявлены красным повторным прогоном.
- `bash scripts/verify_antiplacebo.sh --scope freeze_contract` → rc=0: 1 барьер, 14/14 фикстур предъявлены красным повторным прогоном.
- `bash fixtures/check_zones/_schet_fixtur.sh` → rc=0; числа контракта 13/14 совпадают с независимым инвентарём.
- `bash fixtures/check_zones/probe_protsessnye_vne_suda.sh` → rc=1; четыре подкаталога `verdicts/`, два корневых `NABLIUDENIA*.md`, `HANDOFF.md`, смешанная и охранная ветви поимённы.
- `bash fixtures/check_zones/probe_svod_protsessnyh.sh` → rc=1; ожидания траектории N=1/2/4/6 поимённы, смешанная предметная половина остаётся rc=1.
- `bash fixtures/freeze_contract/probe_012_dva_kruga.sh` → rc=1; обе точки истории 012 поимённы (нереализованный барьер считает 4 и 6 коммитов вместо 2 событий).
- `bash fixtures/freeze_contract/probe_grammatika_sobytij.sh` → rc=1; поимённы CR/trim, порядок путей и R100, в том числе R100 `[FAIL, FAIL, accept] → [FAIL, accept] = 2`.
- `bash scripts/check_contract_frozen.sh .` → rc=0: 16 планов/контрактов, 4 черновика, 12 заморожено; контракт 013 распознан черновиком.
- `bash scripts/check_zones.sh .` → rc=0: 11 замороженных контрактов, 6 авторов, 254 коммита в диапазонах, 176 проверено по зонам.
