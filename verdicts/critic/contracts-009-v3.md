FAIL

## Проверка предмета и меры

- Предмет закоммичен на HEAD `e45e121`; предмет, исполняемая приёмка, названные исполнители и
  `ЗОНА`-строки присутствуют.
- Независимый пересчёт исходника дал **10**, а не 11 конструкций `if want` в
  `scripts/check_judge_gate.sh`: 2 унаследованные + 8 fail-fast. В
  `fixtures/check_judge_gate/` действительно 11 файлов `case_*.sh`: 3 унаследованных + 8
  fail-fast. Сам комментарий барьера также перечисляет 10 ветвей.
- Все 10 фактических ветвей имеют `die` с именем ветви; полный красный вывод различается
  префиксом `ОТКАЗ ветвь (<имя>)`. Одиннадцатой ветви, для которой можно было бы проверить
  уникальный отказ, нет.
- `bash scripts/verify_antiplacebo.sh --scope check_judge_gate` завершился RC=0 и напечатал:
  `барьеров: 1 · фикстур: 11 · предъявлено красным повторным прогоном: 11`. Тем самым каждая
  из 11 фикстур предъявила положительный контроль и повторяемое красное на своём сломанном
  предмете.
- Четыре обхода круга 2 проверены отдельными сломанными предметами. Стаб с разбором флага и
  `shift`, передающий `SLOW_SHA`, покраснел на `rc=1` после примерно 3 с; bare с `TBOX=3`
  покраснел на `timeout_args.log = --foreground 3`; стаб только с `N=2` покраснел на
  `--fail-fast=7`; стаб, отвергающий только `N=0`, покраснел на `--fail-fast=abc` без нужного
  сообщения.
- Регресс 008 жив: scoped-антиплацебо подтвердил зелёные контроли и красные предъявления
  `case_dispatcher_sha`, `case_krasnyj_lax`, `case_zelenyj_bypass_sha`; дополнительно
  `bash scripts/check_judge_gate.sh . зелёный && bash scripts/check_judge_gate.sh . красный`
  завершился RC=0.

## Находки

БЛОКИРУЕТ contracts/009-sudja-fail-fast.md:34-35, scripts/check_judge_gate.sh:34-36,61 —
критерий заявляет 11 ветвей (2+9), но именованный барьер содержит только 10 (2+8). Команда
`bash scripts/check_judge_gate.sh` не считает ветви, а verifier считает 11 фикстур, поэтому
обе меры могут быть зелёными при ложном счётном утверждении критерия.
ОБХОД: предмет, зелёный на всех десяти именованных `if want`, получает итог
`check_judge_gate: ветви «all» зелены`; 11 фикстур также предъявлены verifier, хотя
одиннадцатой ветви нет вообще.

БЛОКИРУЕТ contracts/009-sudja-fail-fast.md:19-20,73-78, scripts/check_judge_gate.sh:169-178 —
предмет требует принимать любое натуральное `N`, но критерий наблюдает только явные `N=2` и
`N=7`. Добавление одной точки закрывает стаб «только 2», но не домен, заявленный предметом.
ОБХОД: стаб принимает явные `N` только из множества `{2,7}`, корректно обрабатывает bare 540,
0 и `abc`, а `--fail-fast=3` отвергает как `unsupported`. Своим прогоном он дал зелёное всем
10 именованным ветвям барьера, хотя обязательное натуральное `N=3` не поддерживает.

БЛОКИРУЕТ contracts/009-sudja-fail-fast.md:92-103, scripts/check_judge_gate.sh:141-156,198-209 —
ветви `all` делят `$WORK/nopath`: `фailfast-дефолт-540` создаёт там исполняемый `timeout`, а
`фailfast-защита-timeout-отсутствует` его не удаляет. Поэтому заявленный PATH «БЕЗ timeout»
при общем приёмочном прогоне фактически содержит timeout, и честная реализация с
`command -v timeout` получает ложный отказ «предмет НЕ проверил наличие timeout». Это также
противоречит требованию правдивого отказа `AGENTS.md:207-211`.
ОБХОД: стаб распознаёт оставшийся путь `*/nopath/timeout` на явном `N=2` и ложно печатает
`FAIL-FAST: timeout не найден`, но на bare продолжает вызывать тот же fake-timeout для записи
540. Такой предмет дал зелёное всем ветвям `all`, хотя защита срабатывает при существующем
`timeout`, а не при пустом `command -v timeout`.

БЛОКИРУЕТ contracts/009-sudja-fail-fast.md:45-126, AGENTS.md:116-122 — раздел критерия
многократно утверждает пары «конкретный обманный стаб ↔ ветвь» и сводит их в таблицу «что
ловит». Норма прямо запрещает контракту такие утверждения; это прямое противоречие
`AGENTS.md`, блокирующее без предъявления обхода.
ОБХОД: не требуется по исключению для прямого противоречия норме `AGENTS.md`.

СОВЕТ fixtures/check_judge_gate/case_failfast_timeout.sh:3-7,34-35 — комментарии всё ещё
обещают проверку `elapsed < 2800мс`, которой в барьере больше нет; фактическое красное теперь
строится на завершившемся маркере `rc=1`.

СОВЕТ fixtures/check_judge_gate/case_failfast_timeout.sh:2, fixtures/check_judge_gate/case_failfast_arbitrary_N.sh:2, fixtures/check_judge_gate/case_failfast_invalid_N.sh:2, fixtures/check_judge_gate/case_failfast_non_integer_N.sh:2, fixtures/check_judge_gate/case_failfast_no_timeout.sh:2 — полные `die`-сообщения уникальны благодаря имени ветви, но пять фикстур по-прежнему объявляют verifier одну и ту же короткую причину `rc=1`. Точечные вызовы ветвей сейчас не дают им зачесть чужой отказ, поэтому это не блокирует.

АРБИТР: `arbiter` — третий круг и повтор причины о недоказанном полном домене натуральных `N`;
дополнительно на арбитраж выносятся расхождение 11/10, межветочная утечка fake-timeout и
прямое противоречие запрету `AGENTS.md` на пары «стаб ↔ ветвь».
