FAIL

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:61-70 — заявленные ветви шире закоммиченного барьера: (д) обещает также недоступный `git`, но `check_scope_select.sh:111-115` проверяет только нерезолвимую базу; (з) обещает add/delete/rename/смену роли, но строки 138-145 строят только add; (и) обещает также `--changed` самого барьера и весь набор его case, но строки 148-153 проверяют только явный `--scope`; (к) обещает `needs-full` и отличие от полного, но строки 156-163 проверяют только маркер `SCOPED:` у scoped. Поэтому «ветви (а)–(к) зелены» не доказывает перечисленный контракт.
ОБХОД: реализация обрабатывает ровно проверенный add, но на delete/rename/смене роли возвращает `MODE: scoped` с пустым набором. Все десять ветвей `bash scripts/check_scope_select.sh`, будущие (л)–(н), полный прогон и оба npm-гейта могут вернуть 0, потому что ни один из них не строит такой дифф; удалённый или переименованный барьер при этом пропущен, хотя предмет требует полной выборки.

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:18-19 — предмет сохраняет ВСЕ ветви отказа полного прогона, но тир 2 на строках 72-79 закрепляет лишь один неназванный вид отказа в (м) и ветвь слепка в (н). Коды/классификация/сверка фикстур и прочие отказы полного раннера в scoped-режиме командой не различаются.
ОБХОД: scoped-цикл сохраняет красный код 1 выбранного барьера (им удовлетворяется (м)) и контроль слепка (н), но трактует код 2 выбранного барьера как пропуск/успех. (л)–(н), полный прогон честного дерева и команды строк 99-106 зелены; на том же дереве с выбранным барьером, вернувшим 2, полный раннер красный, а scoped зелёный. Предмет «ВСЕ ветви отказа» не сделан.

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:51-53 — тир 2 и его красные фикстуры намеренно оставлены на время ПОСЛЕ раздачи, хотя `AGENTS.md` требует для кодовой задачи предъявить красные тесты до круга критика и запрещает замораживать будущую проверку прозой. Финальная команда не доказывает временной факт «эта проверка краснела до реализации», а исходников (л)–(н) на HEAD нет.
ОБХОД: после готовой интеграции добавить три фикстуры, подогнанные под её существующие ветви (например, в (м) предъявить только код 1), и сразу получить зелёный `verify_antiplacebo.sh`; все команды приёмки выполнены, хотя никогда не существовало замороженной красной пробы, способной поймать false-green кода 2 из предыдущей находки.

БЛОКИРУЕТ contracts/006-scoped-izolirovannyj-gejt.md:69-70 — отсутствие пути публикации CI-статуса заявлено как свойство и как проверка фикстурой, но `check_scope_select.sh:160-162` проверяет исходник лишь blacklist-регэкспом `gh |curl |api.github|check-runs|POST`. Это не доказывает отсутствие публикации.
ОБХОД: `scope_select.sh` публикует статус через `python3`/`wget` и собирает строку endpoint из частей, не содержащих ни одного токена blacklist. Ветвь (к) и все команды приёмки возвращают 0, но scoped получает запрещённый путь авторитетной публикации.

СОВЕТ contracts/006-scoped-izolirovannyj-gejt.md:83-93 — `fixtures/verify_antiplacebo/` находится в зоне architect, однако строки 89-92 включают новые интеграционные фикстуры в то, что «Исполнителю раздаётся». Уберите противоречие: зона и проза должны называть одного автора; сейчас `check_zones.sh` разрешает этот путь только architect.
