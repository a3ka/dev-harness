accept

## Предмет и граница

Контракт `contracts/028-home-sessii-vne-dereva.md` на HEAD
`704f4698f2713f5d1395512a5c397a21357af26b`, блоб
`232292d1a23a700002b68b0fbe7719a2fc16b8d2`; база —
`frozen/contracts/028/1`, блоб `7e3f091f84fe9b95c02d1ae965a8db0d85798a0b`.
Слот v2 — версия заморозки, не четвёртый пересуд приёмки.
Проверена ТОЛЬКО разрешённая дельта путей, ЗОНА-строки и фразы :146.
Инварианты, достаточность приёмки и закрытые предыдущими кругами причины
не пересуживались. Арбитраж `2facfe3:verdicts/arbitration/028-prijemka-tretego-kruga.md`
прочитан; строки `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` в `8662a66` и land-merge `704f469`
разрешают именно эту дельту. Предмет, rc-критерий и исполнители закоммичены.

## Проверка дельты

- `contracts/028-home-sessii-vne-dereva.md:135,146,160,167,187,209,225-230`:
  побайтовая сверка старого и нового блобов после явных разрешённых замен
  подтверждает: меняются только пути, снятие прежней зоны, фраза о доме проб
  и выравнивающие пробелы. Других изменений инвариантов, ветвей, входов,
  rc-команд и порядка миграции нет. `git diff` показывает 10 заменённых строк.
- `contracts/028-home-sessii-vne-dereva.md:135`: все четыре новых пути уже
  внутри `fixtures/check_judge_gate/` исполнителя architect; второй исполнитель
  или расширение зоны не нужны. Удалённая зона `fixtures/workshop/` больше
  не нужна. Механизм границ — существующий `check_zones.sh` по заморозке.
- `contracts/028-home-sessii-vne-dereva.md:146,150-152,225-230`: перенос не
  превращает прямые `red_*`-предъявления в `case_*` и не выдаёт их за покрытие
  мета-раннером. Прямые команды приёмки сохранены и выполнены на новых путях.
  Это соответствует существующему соглашению `scripts/check_no_leak.sh:3-7`.

Нового ОБХОДА, вызванного дельтой, не найдено. В пределах дельты критерий не
ослаблен, команды исполнимы, новых оставленных исполнителю решений нет,
границы названы, противоречия норме AGENTS.md не обнаружено. Блокирующих
находок и советов нет.

## Независимые прогоны

Собственный клон `/tmp/dev-harness-verify/critic-028-v4/repo`, checkout
строго на `704f469`. Проверялось содержимое блобов, не `%an` (Н-101).

`git log --follow -M100% --format='%h %s' --name-status -- <новый путь>`
выполнен отдельно для каждой из четырёх проб. Во всех случаях перенос
`8662a66` — R100, первоначальное добавление — `dad0117`.
`git diff-tree --no-commit-id --name-status -r -M100% 8662a66` содержит
РОВНО четыре переноса проб. Дополнительно сравнены байты и идентификаторы
старых блобов `8662a66^:fixtures/workshop/<имя>` с новыми на HEAD;
все равны, режим каждого нового файла — `100755`:

| Проба | Неизменный blob |
|---|---|
| red_scratch_vnutri_dereva.sh | a13b8856185be8a9d951bce966cf18ac57251794 |
| red_zony_net_v_dereve.sh | 5a5ecf47a7240c8a60a21f36ed1dd7801a489157 |
| red_xdg_vnutri_dereva.sh | f3b6073dc450ad9eafbd4b2cd4e5383d33833d0d |
| red_migratsija_sohrannost.sh | 297bc6ab1a8899db320ede6bf819e6cf3dcaa8b0 |

Команды в клоне, последовательно (не параллельно снимкам дерева):

    bash fixtures/check_judge_gate/red_scratch_vnutri_dereva.sh     # rc 0
    bash fixtures/check_judge_gate/red_zony_net_v_dereve.sh         # rc 0
    bash fixtures/check_judge_gate/red_xdg_vnutri_dereva.sh         # rc 0
    bash fixtures/check_judge_gate/red_migratsija_sohrannost.sh     # rc 0
    bash scripts/verify_antiplacebo.sh --scope check_judge_gate    # rc 0

Первые четыре предъявили соответственно: отказ до создания/exec;
внешний HOME и передачу переменной потомку; оба XDG-входа с именованным
отказом; сохранность трёх исходных файлов при mv и передачу `--resume 02802808`.
Расположение не меняет вычисление корня: во всех четырёх `REPO=$HERE/../..`.
Scoped-регресс: один барьер, три case-фикстуры, у всех зелёный контроль и
красный повтор. Это scoped-доказательство, не полный прогон CI.

    git diff --exit-code frozen/contracts/028/1 HEAD -- scripts/check_no_leak.sh scripts/verify_antiplacebo.sh scripts/check_judge_gate.sh AGENTS.md

Код 0: эти механизмы и норма неизменны.

    python3 /tmp/dev-harness-verify/critic-028-v4/proverka.py

Код 0. Независимый обход шапок scripts/ и каталогов fixtures/:
38 каталогов, 38 ключей барьеров, разность (не-барьерные каталоги) — 0.
`fixtures/workshop/` отсутствует и в чекауте, и в дереве HEAD.
Сверка дельты и байтов проб также входит в этот прогон.

## Раскрытия

Живая канарейка Н-98 не перегонялась: благословлённый самотрип принят как
раскрытие, не предмет этого круга. CI на `704f469` раскрыт как находящийся
в полёте; результат CI и сообщённый шард ап3 этим судьёй не подтверждаются.
Полная батарея не запускалась. Реализация и достаточность прежней приёмки
этим узким accept не переоцениваются.
