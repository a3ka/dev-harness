FAIL

## Узкая сверка v3

Предмет суда закоммичен: `HEAD = a84a93c7cdb873ea08dc6e33923c2ddaaf88c1c5`, автор коммита — `architect`. Предмет, критерий готовности, исполнители и `ЗОНА`-строки в контракте присутствуют.

### Единственность изменения

Команда

```text
git diff --no-ext-diff --unified=5 frozen/contracts/013/2 a84a93c -- contracts/013-processnye-artefakty-i-schet-krugov.md
```

дала **1 файл, 2 добавления, 4 удаления**. Полный diff содержит только замену строки `СПАСЕНО` (1 хеш → 6 хешей) и замену двух строк примечания v2 одной строкой примечания v3; иных расхождений с блобом `frozen/contracts/013/2` нет.

### Грамматика и хеши

`contracts/013-processnye-artefakty-i-schet-krugov.md:105` объявляет автора `architect`; строка `СПАСЕНО` на строке 111 содержит **6** lowercase-хешей по **40** символов, `—` и непустую причину. Команда

```text
git rev-parse bc12da4 0f19263 c081809 376025f 743d35b c91a6c8
```

вернула, в том же порядке, ровно записанные в контракте полные хеши:

```text
bc12da471b5180bc2d8de299720ef38b2fa305fb
0f192635126c9054ba1cdafc79c4dd296f5f7923
c0818095ef62b582bd8301b5c1b12fbfca3260ed
376025fcbd5ced9b697d57a7ee4a6d94c6431e12
743d35b24762939530979188415e3af15e9c0e28
c91a6c843b959aef70c6c82363a7de098c7a365c
```

Однако грамматика — не только форма строки. `scripts/check_zones.sh:242` сохраняет тройку `<автор строки СПАСЕНО, хеш, номер контракта>`, а `scripts/check_zones.sh:334-337` выводит коммит из суда только при точном совпадении этой тройки с фактическим `%an` коммита.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:111 — шестой хеш `c91a6c843b959aef70c6c82363a7de098c7a365c` записан под `СПАСЕНО architect`, но `git show -s --format='%H %an' c91a6c8` даёт автора `implementer`. Поэтому строка спасает только первые **5** коммитов, а шестой остаётся в суде и нарушает зону 013 (`scripts/verify_antiplacebo.sh` не входит в `ЗОНА implementer` контракта 013).
ОБХОД: оставить строку v3 как есть и довести неизменённые приёмочные команды контракта 013 до rc=0 — ни одна из них не требует пробного тега `frozen/contracts/013/3` и ровно шести строк `СПАСЕНО`; предмет 013 будет объявлен готовым, хотя `c91a6c8` по-прежнему не выведен из суда зон.

БЛОКИРУЕТ contracts/013-processnye-artefakty-i-schet-krugov.md:112 — примечание объявляет расширение `СПАСЕНО` единственным изменением v3, но этого недостаточно для заявленного пробного тега: сам коммит v3 `a84a93c7` попадает в открытый диапазон замороженного контракта 014 и меняет `contracts/013-processnye-artefakty-i-schet-krugov.md`, которого нет в `ЗОНА architect` контракта 014. Даже исправление автора шестого хеша само по себе не сделает пробный прогон зелёным.
ОБХОД: оставить диапазон 014 открытым и текст v3 без отдельного механизма для `a84a93c7`; неизменённые критерии готовности 013 могут пройти, но после тега `frozen/contracts/013/3` `check_zones` всё равно вернёт 1 на коммите v3 вне зоны 014.

### Пробный тег и charter

В чистом клоне `/tmp/critic-013-v3-2` выполнены:

```text
git tag frozen/contracts/013/3 a84a93c
bash scripts/check_zones.sh .
```

Результат: **rc=1**, строк `контракт 013 ... СПАСЕНО, из суда зон выведен` — **5**, не 6; отказов — **2**:

```text
FAIL коммит вне зоны: implementer c91a6c84 scripts/verify_antiplacebo.sh — зона контракта 013: scripts/check_zones.sh scripts/freeze_contract.sh
FAIL коммит вне зоны: architect a84a93c7 contracts/013-processnye-artefakty-i-schet-krugov.md — зона контракта 014: contracts/014-default-skratch-vne-dereva-pri-tmpdir.md fixtures/verify_antiplacebo/ NABLIUDENIA_ARCHITECT.md
```

`bash scripts/check_charter.sh .` завершился с **rc=0**: **16** уставных документов, **41** изменение, **41** с разрешением; для `a84a93c7` напечатано разрешённое изменение контракта 013.
