FAIL

# Контракт 040 v1 — один круг критики до заморозки

Предмет: `contracts/040-batching-git-vyzovov.md`, коммит
`01a9f252e0a1efc920c41db639aced0b81f08c6b`, проверенный HEAD
`f4c2cd5280b22bcd7783bf586694c135a06d0afd`.
Набор полон: закоммичены контракт, исполнители/зоны, приёмка и обе красные
фикстуры. Контракт, обе фикстуры и весь `/home/aka/.local/state/dev-harness-sessions/775e24e1/zones/dev/.omp/profiles/dev/agent/sessions/--home-aka-Documents-dev-harness--/2026-09-22T10-06-13-194Z_01a0c894-8aca-76d5-8a52-657ab3d689fd/Arch126Frontier.md` прочитаны.
Судится контракт, не будущая реализация. Полный проектный прогон не запускался.

## Блокирующие находки

БЛОКИРУЕТ contracts/040-batching-git-vyzovov.md:215 — Р1 не требует второй половины A2: батчинга путей совпавших по автору коммитов (§Инварианты 1, строки 82–87).
ОБХОД: батчить только авторов, оставив `g diff-tree ... "$c"` на КАЖДЫЙ совпавший коммит. Р1 становится зелёной: `130 <= 452`; Р3 — rc=0, 21/21; настоящий `timeout 60 bash scripts/check_zones.sh .` — rc=0. Всё это прожито на изолированном мутанте без изменения фикстур. Дополнение такой половины A2 честной реализацией check_protected закрывает независимые Р2/Р4/Р6, но обязательный батчинг diff-tree всё ещё отсутствует.

Причина: все M=60 фоновых коммитов принадлежат НЕОБЪЯВЛЕННОМУ автору «Фон»
(`fixtures/check_zones/red_predel_git_vyzovov.sh:96-98`). Они нагружают
`log -1`, но отсекаются до `diff-tree`; последний получает практически только
один коммит-нарушение. Это не гипотеза об асимптотике, а зелёная неполная
реализация. Нужна нагрузка также на второй заменяемый источник метаданных,
при сохранённой проверке нарушения; конкретную конструкцию выбирает architect.

БЛОКИРУЕТ contracts/040-batching-git-vyzovov.md:261 — двойная зона не обеспечивает обещанного «implementer только добавляет», а приёмка не пинит обе новые красные фикстуры и все прежние файлы.
ОБХОД: на дереве с готовыми A2 и check_protected implementer повышает `BOUND=30` до `BOUND=1000` в `fixtures/check_protected/red_predel_git_vyzovov_ours.sh`. Р2 остаётся rc=0 со всеми шагами, остальные шесть команд и даже оба предложенных diff по `case_*.sh` не меняются; путь входит в его ЗОНУ. Предмет «постоянный структурный страж 30, принадлежащий architect байт-в-байт» не выполнен: страж теперь разрешает исходные 141 вызов. Аналогично подмена прежнего `red_mera_parallelnosti_okon.sh` на `exit 0` не попадает ни в Р3, ни в его diff, хотя обещана неизменность прежних файлов.

Механическая область строки 247 ровно совпадает с запрошенным списком — лишних
путей, в частности `scripts/check_provodka.sh`, нет. Дыра не в ширине списка,
а в отсутствии различения изменения существующего файла и добавления нового.
`check_zones` проверяет принадлежность пути союзу зон, не владельца байтов.
Р3 реально запускает 21 `case_*.sh`, не объявленные 33 файла; список каталога
содержит также red/probe/stab. `verify_antiplacebo.sh:585` выбирает только
`case_*.sh`. Сравнения строк 217–218 содержат незаменённый `<до-A2-sha>` и
охватывают только `case_*.sh`; они не являются готовыми командами фиксации
обещанного полного базлайна. Нужен исполняемый пин существующих файлов с
конкретным исходным SHA/слепком, допускающий добавления, но не молчаливое
изменение/удаление; исключение для пересмотра порога не должно легализовывать
обычную правку implementer.

БЛОКИРУЕТ contracts/040-batching-git-vyzovov.md:286 — объявление ПРОВОДКА сейчас не проходит обязательный done-гейт; противоречие обязательной валидной проводке из AGENTS.md, раздел «Воркфлоу майлстоуна».
ОБХОД: закончить оба алгоритма и выполнить все семь строк таблицы, оставив объявление как на проверенном HEAD. Все семь критериев могут быть выполнены, но контракт НЕ может быть доведён до done: `bash scripts/check_provodka.sh . contracts/040-batching-git-vyzovov.md` реально возвращает 1 с `проводка: строка вне грамматики: существующих барьеров (call-count граница check_zones; -s ours-корректность и`.

Парсер пропускает саму строку `ПРОВОДКА-ЭНФОРСМЕНТ:`, но продолжение строки
287 остаётся внутри поля и не является ни каналом, ни заголовком. Отказ
происходит ДО проверки существования/подключения guard. `done_contract.sh:124-130`
вызывает этот барьер и отказывает при non-zero. Исправляется размещение текста
контракта, не чужой `check_provodka.sh` из предмета 038. Разместить пояснение за
границей поля, сохранив непустую маркерную строку, и предъявить живое rc=0.

БЛОКИРУЕТ contracts/040-batching-git-vyzovov.md:141 — требование постоянных CI-стражей (§Инварианты 8) не входит в исполняемую приёмку строк 215–221.
ОБХОД: честно реализовать оба батчинга, оставить `.github/workflows/ci.yml` и `package.json` без новых вызовов двух red-фикстур. Все семь ручных команд зелёные, но ни один будущий CI не обязан исполнять новые пределы. Даже после исправления синтаксиса ПРОВОДКА guard= указывает на сами check_zones/check_protected, уже подключённые к CI, а не на новые красные стражи.

На проверенном HEAD новых вызовов действительно нет; это нормально ДО
реализации, но критерий ПОСЛЕ обязан отличать отсутствие проводки от готовности.
Нужна исполняемая проверка фактической CI-достижимости обеих новых проб,
не только наличие рекомендуемых имён npm-команд в прозе §Зоны.

## Проверка всех семи команд на исходном HEAD

Прогоны — в собственном полном клоне `tmp/critic/repo`, без изменения
контракта/фикстур/барьеров. Каждая основная команда таблицы выполнена дословно.

| Пункт | Получено | Сверка с ДО |
|---|---|---|
| Р1 `bash fixtures/check_zones/red_predel_git_vyzovov.sh` | rc=1; `git-вызовов 916 > границы 452` | Совпало; отрицательный контроль rc=1 поймал agent03 |
| Р2 `bash fixtures/check_protected/red_predel_git_vyzovov_ours.sh` | rc=1; `git-вызовов 141 > границы 30`; перед этим `п.Б держится` | Совпало |
| Р3 `bash scripts/verify_antiplacebo.sh --scope check_zones` | rc=0; `фикстур: 21 · предъявлено красным повторным прогоном: 21` | rc совпал, заявленные 33 запуска не подтвердились |
| Р4 `bash scripts/verify_antiplacebo.sh --scope check_protected` | rc=0; `фикстур: 20 · предъявлено красным повторным прогоном: 20` | Совпало |
| Р5 `bash scripts/check_zones.sh .` | rc=0; 37 контрактов, 1554 коммита в диапазонах, 1022 проверено; 28.09 с | Не совпало с заявленным rc=1 |
| Р6 `bash scripts/check_protected.sh .` | rc=0; 1949 коммитов пройдено; 16.40 с | rc совпал |
| Р7 `timeout 60 bash scripts/check_zones.sh . ; echo "rc=$?"` | rc оболочки=0; stdout `rc=0`; 29.78 с | В этом клоне таймаута не было, в отличие от указанного ДО |

Первый параллельный запуск Р3 был недостоверен: одновременно Р6 создавал
`tmp/protected.*/roleblobs`, что поймал снимок дерева. Первая попытка Р4
вернула 3 из-за занятости раннера. Оба результата отброшены; приведённые
выше итоговые Р3/Р4 получены ПОСЛЕДОВАТЕЛЬНО, без других процессов, меняющих
дерево этого клона. Различие Р5/Р7 с более ранним замером владельца на main
не опровергает его: место/момент замера и окружение другие; это фактическая
сверка требуемой таблицы на названном HEAD, а не переписывание Н-126.

Дополнительное сравнение с конкретным исходным SHA выполнено:
`git diff --exit-code 01a9f252e0a1efc920c41db639aced0b81f08c6b HEAD -- 'fixtures/check_zones/case_*.sh' 'fixtures/check_protected/case_*.sh'`
→ rc=0, вывод пуст. Буквальные `<до-A2-sha>` из таблицы не подаются оболочке
как якобы действующие ревизии: это незаполненные параметры, не исполняемые
команды.

## Пределы и поправка 1

452 действительно ВЫЧИСЛЯЕТСЯ, а не записана результатом: в Р1
`A=6; B=20; bound=$((A * (K + M) + B))`, K=12, M=60. Это линейный бюджет
6(K+M)+20. 30 в Р2 — намеренная константа, независимая от M=60: для замены
двух per-commit циклов на два запроса это подходящая форма ограничения,
а не потерянная формула от M. Однако формула сама не защищает коэффициенты
или BOUND от правки, и одна нагрузка Р1 не предъявляет обе половины A2.
Никакого повышения 452/30 для приёмки в этом круге не принималось.

Отдельно прожиты п.В и обе допустимые формы Р2. В диагностической копии
ТОЛЬКО для достижения строк после сегодняшнего красного п.А временно
поставлено BOUND=1000; исходная фикстура затем восстановлена. Получено:

```
п.Б держится: check_protected.sh (rc=1) ловит -s ours-пропажу И на дереве с M=60 фоновыми коммитами
п.В (КРАСНОЕ подтверждено): наивная `rev-list --objects -- pathspec` (без --full-history) НЕ видит plans/002-vetka.md — запрещённая техника (поправка 1)
форма 1 корректна: `rev-list --objects --full-history -- pathspec` видит путь
форма 2 корректна: `rev-list --objects` без pathspec + awk-фильтр видит путь
```

Диагностический запуск rc=0 НЕ объявляется зелёной приёмкой Р2: исходная Р2
имеет rc=1. П.В — реально исполняемый отрицательный контроль самой техники
git, не проза. Связь с проверяемым барьером обеспечивает п.Б, который
требует реального отказа о пропаже `plans/002-vetka.md`. Один п.В сам по себе
не инспектирует выбор команды внутри реализации; это важно не перепутать.

Для воспроизведения неполного A2 заменён только `an="$(g log -1
--format='%an' "$c")"`: перед циклом по `$TMP/commits` один
`g log --no-walk --format='%H%x09%an' --stdin < "$TMP/commits"` наполняет
ассоциативный массив SHA→author, внутри цикла читается массив. Строка
`done < <(g diff-tree -r --no-commit-id --name-only --no-renames "$c")`
НЕ изменена. Измерения этой копии: Р1 rc=0, 130 вызовов; Р3 rc=0, 21/21;
`timeout 60 bash scripts/check_zones.sh .` rc=0. Постоянных изменений
реализации из этого эксперимента нет.

## Неблокирующие замечания

СОВЕТ contracts/040-batching-git-vyzovov.md:221 — Р7 маскирует код timeout последним echo. Контрольная проба с 0.001 вместо 60 реально дала stdout `rc=124`, но код всей команды 0. Если явно проверять объявленную подстроку `rc=0`, нарушение всё же видно, поэтому отдельным блокером это не объявляю. Для канона rc-команд оставить непосредственно `timeout 60 bash scripts/check_zones.sh .`, а печать кода делать с сохранением/возвратом этого кода.

СОВЕТ contracts/040-batching-git-vyzovov.md:219 — «CI красный» не означает rc=1 голого check_zones; таймаут внешнего вызывающего — другой исход. Обновить ДО с указанием места/даты, не смешивая rc субъекта и rc timeout.

## Итог пяти вопросов роли

1. Критерий слабее предмета: измеренный неполный A2 и отсутствие CI-проверки.
2. Недоказуемое названной командой: полный неизменяемый базлайн не проверяется;
   placeholders в diff не заполнены; особенности rc Р7 отмечены отдельно.
3. Решения, оставленные исполнителю: выбор двух допустимых форм rev-list и
   точных имён npm-шагов объявлен явно и сам по себе не блокирует. Механизм
   доказательства сохранности старых файлов не определён до исполнимой формы.
4. Исполнители и верхние границы названы; ограничение «только добавлять»
   при пересекающихся каталогах механически не выражено.
5. Обязательная валидная ПРОВОДКА из AGENTS.md нарушена живым rc=1.

Заморозка v1 не разрешена. Это первый круг по 040; повторного спора,
требующего арбитра, не установлено. Контракт и реализация в основном дереве
критиком не правились.

