FAIL

# Контракт 037, v1 — критик до заморозки

Предмет: `contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` на HEAD
`b99e397e1c12a3cf43e49385107cacaf858111ea`. Прочитан целиком в отдельном
SSH-клоне; прочитаны AGENTS.md, roles/critic.md, нормативные разделы 028/041,
приёмочные пробы и соответствующая часть workshop. Судится контракт, не
будущая реализация. Набор артефактов полный: предмет, команды приёмки,
исполнители и ЗОНА-строки закоммичены. Это первый круг; повторного отказа
по той же причине и основания созывать арбитра нет.

## Блокирующие находки

### Б1. Обязательный precision-гейт красный на реальной коллизии

БЛОКИРУЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:293 — architect получает NABLIUDENIA_ARCHITECT.md без объявления пересечения с implementer-зоной 015. Живой `bash scripts/check_precision_gate.sh /tmp/critic037-review contracts/037-samodostatochnyj-cwd-i-priemnik-task.md` вернул rc=1. По норме roles/critic.md это блокирует; историческое утверждение о rc=0 не подтверждено текущим HEAD.
ОБХОД: precision-гейт 043: зона-коллизия: NABLIUDENIA_ARCHITECT.md заявлен architect (этот контракт, NNN 037), уже в union под NNN 015 для implementer — нужна строка ПЕРЕСЕЧЕНИЕ architect: NABLIUDENIA_ARCHITECT.md — 015 <причина>

Это дословно первая и единственная строка вывода команды. Скрипт существует,
не выдаёт NOT_IMPLEMENTED и останавливается на первой коллизии. Его отказ
не является доказательством отсутствия других пересечений.

### Б2. Приёмка требует принять автора, запрещённого своим алфавитом

БЛОКИРУЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:248 — battery_regex_injection требует успешного приёма `--author 'a.*b'` при таком же `%an`, хотя М2:180 объявляет `^[A-Za-z0-9_-]+$`. Точки и звёздочки в этом алфавите нет. Нельзя одновременно исполнить оба требования; это не вопрос вкуса реализации.
ОБХОД: реализация расширяет алфавит author до `[A-Za-z0-9_.*-]+`, сохраняет буквальную проверку an/ae и остальное поведение. Р2 с implementer проходит; описанная Р8 принимает одинаковые `a.*b`, отвергает `a.*b` против `axxb`, отвергает угловые скобки и неверную ветку; остальные Р1–Р7 не различают расширение. Критерий зелёный, но объявленный замкнутый алфавит М2 нарушен и ввод вне области принят.

Закрыть нужно согласованием области и наблюдения, не исключением только
для конкретного имени. При сохранении нынешнего алфавита такой author-argv
обязан быть отвергнут ещё до сравнения git identity.

### Б3. Проверка одного коммита выдаётся за проверку всего диапазона

БЛОКИРУЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:354 — четыре ветви Р2 не доказывают М2:225–228 («КАЖДЫЙ», отказ до любого cherry-pick). В обоих источниках red_accept_task_commit.sh только по одному новому коммиту; негатив проверяет чужого автора единственного tip, не чужого автора внутри диапазона. В п2 наблюдаются сдвиг ref и an/cn tip, но не состав перенесённого диапазона; ae/ce также не наблюдаются.
ОБХОД: приёмник проверяет identity только FETCH_HEAD, затем cherry-pick-ает весь диапазон. Однокоммитные SRC_OK/SRC_BAD дают ровно ожидаемые Р2 результаты; остальные Р1–Р7 не проверяют диапазон, четыре описанных класса Р8 тоже не требуют смешанной истории. Источник `base -> commit(critic) -> commit(implementer)` принимается целиком при `--author implementer`: чужой внутренний коммит попадает на целевую ветку вместо атомарного отказа. М2 и первый буллет ЗАЩИЩАЕТ не выполнены.

Достаточное различающее предъявление — смешанный диапазон и наблюдение
неизменности целевой ветки при несовпадении любого его участника. Это
требование к наблюдаемому поведению, не предписание алгоритма реализации.

### Б4. HOME-принадлежность не доказывает отсутствие общего с родителем cwd

БЛОКИРУЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:148 — из нормы 028 «HOME вне чекаута» выведено обратное утверждение «чекаут никогда не внутри HOME». 028, инвариант 2, и workshop:90–95 запрещают HOME быть потомком чекаута; они не запрещают чекауту быть потомком HOME. Живой замер в тексте доказывает различие двух конкретных путей, а не заявленную невозможность общей директории внутри HOME.
ОБХОД: родитель уже работает в isolated-клоне `$HOME/.omp/profiles/dev/wt/x/m` с `.git`-каталогом и единственной worktree-записью и вызывает голый, не isolated, unpinned `_task` в том же cwd. У ребёнка те же HOME/actual, все четыре условия М1 истинны. Буквально соответствующая М1 реализация разрешит ребёнку прямую запись в ОБЩИЙ с родителем чекаут, хотя предмет :32–34 и ceremony это запрещают. Р1 проходит: его канарейка п4 моделирует общий чекаут только СНАРУЖИ FAKEHOME; общего cwd ВНУТРИ HOME там нет. Р2–Р8 такого различия не наблюдают.

Это не атака на стартовое окружение и не подмена HOME агентом: окружение
унаследовано штатно. Также допустима топология HOME=`/H`, основной чекаут
=`/H/project`: запрет 028 на расположение HOME внутри проекта её сам по себе
не исключает. Для измеренного основного пути вне HOME п4 действительно
закрывает п1–3-дыру; для всего заявленного класса голых `_task` — нет.
Нужно либо различающее основание и приёмка, либо явное согласованное
сужение предмета/модели угроз, а не объявление уже измеренной пары путей
универсальной гарантией 028.

## Советы и ответы по норме 041

СОВЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:176 — пункты о раздельных argv, литеральном сравнении и полном якоре author/branch соответствуют существу 041 §Инварианты п.2(ii–iv). Но перечень замкнутых алфавитов не полный относительно буквального п.2(i): `--source` и `--root` — структурные пути, их допустимый язык не объявлен. Не решено, являются ли source только локальными путями или также git transport-выражениями. Это решение сейчас оставлено исполнителю; отдельного исполненного обхода приёмки на этом канале здесь не предъявляю.

СОВЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:288 — три незаполненных требования честно перечисляют отсутствующие pathAllowed-ветвь, приёмник и профиль; Р1/Р2 и запуск Р8 это подтверждают. Однако срок батареи противоречив: :242–244 и 041 §Инварианты п.5 требуют прогон до заморозки любого нового гарда, а :288/:357 откладывают его на implementer после раздачи. Неприменимость буквального self-application markdown-текста из п.2(v) не отменяет самостоятельную обязательность батареи п.5. Нельзя называть это уже выполненной предпосылкой freeze; срок и исполнитель пред-фризового предъявления должны быть согласованы.

СОВЕТ contracts/037-samodostatochnyj-cwd-i-priemnik-task.md:238 — default для отсутствующего `--root`, конфликт cherry-pick/merge-коммиты, уже checkout-нутая целевая ветка и судьба грязного root не доведены до однозначного CLI-контракта. В механике сказано «грязный root не трогается», в кодах — отказ на грязном root. Последнее разумно читать как fail-closed, но явное решение избавит implementer от самостоятельного выбора.

Модель угроз сформулирована по свойствам, не списком частных атак. Исключение
враждебного окружения соответствует объявленной границе; проверка git
identity не равна криптографической аутентификации человека или качеству
патча. В границах М2 пункты содержательны, но обещание проверки каждого
коммита не доказано приёмкой (Б3). Для М1 объявленный остаточный риск
подмены HOME не покрывает честное наследование общего cwd (Б4).
М3 уже несёт в роли ссылку на приёмник, рекомендацию isolated:true и запрет
общего cwd; Р3/Р7 подтверждают наличие текста, не исполнение ceremony каждым
будущим вызовом. Последнее не выдаю за машинно доказанный факт.

## Живые команды и буквальные результаты

Все команды ниже исполнены на судимом SSH-клоне `/tmp/critic037-review`;
контракт, роли, механизм и фикстуры не правились.

* Р1: `bash fixtures/check_runner_hygiene/red_self_contained_cwd.sh .` → **1**.
  Первая строка начинается буквально `КРАСНОЕ 037: ветвь «п1» — ожидалось pass, получено block`;
  JSON-reason — `Н-85/А-122`, цель внутри FAKEHOME не в null-allowlist.
  Итог дословно: `ИТОГ 037 (self-contained-cwd): ветвей 6, красных 1, зелёных 5`.
  Заявленная причина красного подтверждена, не инфраструктурный отказ.
* Р2: `bash fixtures/accept_task_commit/red_accept_task_commit.sh .` → **1**.
  Первая строка дословно: `КРАСНОЕ 037: ветвь «п1» — отсутствует: ./scripts/accept_task_commit.sh (NOT_IMPLEMENTED — приёмник не существует)`.
  Далее: `СВЕДЕНИЕ 037: 3/4 ветвей не прогнаны (скрипт отсутствует — легально на п1-only red)`;
  `ИТОГ 037 (accept_task_commit): ветвей объявлено 4, прогнано 1, красных 1, зелёных 0`.
  Подстановка `<root>` в тексте равна `.`; причина совпадает буквально.
* Р3: специализированный поиск нашёл `accept_task_commit.sh` в
  `roles/orchestrator.md:314`. Это проверка наличия маркера, не shell-rc
  команды grep, которая отдельно не запускалась.
* Р4: `bash fixtures/check_runner_hygiene/red_pin_spawn_zadanie.sh .` → **0**;
  `ИТОГ 032: ветвей 31, красных 0, зелёных 31`.
* Р5: `bash fixtures/check_runner_hygiene/red_pin_allowlist.sh .`,
  `bash fixtures/check_runner_hygiene/red_granica_nepin_pipe_tee.sh .`,
  `bash fixtures/check_runner_hygiene/red_strazh_vectora_utechki.sh .` → **0** каждая.
* Р6: структурный rc=0 предоставлен оркестратором во входе задания;
  не повторялся ради подтверждения уже сообщённого результата.
  Содержательный суд модели угроз приведён выше.
* Р7: `bash scripts/check_provodka.sh . contracts/037-samodostatochnyj-cwd-i-priemnik-task.md`
  → **0**, вывода нет.
* Р8: `bash fixtures/parsing_hygiene_battery/run_battery.sh accept_task_commit`
  → **2**, `NOT_IMPLEMENTED: профиль не найден: /tmp/critic037-review/fixtures/parsing_hygiene_battery/profiles/accept_task_commit.sh`.
* Обязательный precision-гейт → **1**, дословная первая строка в Б1.

Б2–Б4 — конкретные логические контрпримеры к контракту и его наблюдениям
[INFERENCE], не заявления о запуске несуществующей реализации или зелёной
будущей Р8. Перманентные тесты/мутанты в рамках этого суда не создавались.
Полные тесты, форматтеры, линтеры, CI и gen:harness не запускались.

## Пять обязательных вопросов — итог

1. **Критерий слабее предмета?** Да: Б2–Б4 называют конкретные состояния,
   различаемые предметом, но не заявленной приёмкой.
2. **Готово доказуемо командами?** Р1/Р2 красны по назначению, регрессы зелены;
   это не доказательство всего М1/М2. Р8 ещё недоступна, сроки расходятся.
3. **Решение оставлено исполнителю?** Да: несовместимые author-требования
   (Б2), язык source/root и перечисленные CLI-переходы (советы).
4. **Границы названы?** Исполнители и пути названы, path-guard явно
   переобъявлен той же implementer-роли; независимая коллизия architect с
   implementer/015 обнаружена живым precision-гейтом (Б1).
5. **Противоречие старшей норме?** Fail-open для отсутствующего инструмента
   неприменим: precision-гейт существует и отказал. Проза о достаточности
   HOME и о полном выполнении М2 не заменяет различающую приёмку; норма
   AGENTS.md о кодовой приёмке требует именно её. Новых правил/зон этим
   вердиктом не вводится.

Единственная постоянная правка критика — этот файл. Заморозка запрещена
до устранения действительных блокирующих находок; советы сами по себе
следующего accept не блокируют.
