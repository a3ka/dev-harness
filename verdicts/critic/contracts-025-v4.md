FAIL

# Контракт 025 v4 — критик дельты §B-1

Судимый HEAD: `116d92550d1f431aff54096c98b029100b465ac6`, получен `git rev-parse HEAD` в основном репозитории. Проверка выполнена в собственном клоне `/tmp/dev-harness-verify/critic025/repo`. Предмет — только дельта `frozen/contracts/025/3..HEAD` файла `contracts/025-sreda-cwd-rc.md`, не повторное ревью реализации 025.

Набор артефактов полный: контракт закоммичен; предмет, rc-критерии И-2/И-6 и исполнители присутствуют. Строки `ЗОНА` на `contracts/025-sreda-cwd-rc.md:384-388` называют architect, implementer и судей; нормативный текст принадлежит architect, оба расширения — implementer, вердикт — critic. Механизм границ — `check_zones.sh`. Новых файлов реализации дельта не раздаёт.

## Блокирующая находка

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:161-175 — описание нового носителя объявляет эквивалентность старому, но приписывает коду отсутствующее условие и не доказывает сохранение исключения для голой SIGPIPE-смерти (`contracts/025-sreda-cwd-rc.md:186`). В строке 163 написано «мапится в 0, когда isError=false». В действительности `.omp/extensions/rc-prefix.ts:88-99`, как на HEAD, так и в указанном `2c30d54`, проверяет только инструмент bash и `details.exitCode === 141`; входной `isError` вообще не читается. `isError:false` — ВЫХОД патча, не его предусловие. Кроме того, предшествующий `exit-marker.ts:146-155` сам возвращает `isError:true` для 141. Следовательно, это не формулировка реального условного различения: новый носитель безусловно обнуляет полученный bash-код 141 независимо от причины его появления.

ОБХОД: дерево с точным историческим env-префиксом и действующим обработчиком «любой bash details.exitCode=141 → {exitCode:0,isError:false}» удовлетворяет И-2 и проверяемым И-6 исходам `false|true`, `yes|head` и grep; именно эти исходы живого И-6 уже зафиксированы в указанном самим respec вердикте адверсария v3. При этом событие bash о голой SIGPIPE-смерти с `details.exitCode:141,isError:true` тот же обработчик превращает в успех 0. И-2 вызывает фабрику без API и исполняет историческую trap-строку, а И-6 не предъявляет голую SIGPIPE-смерть: его rc-ветви — `probe025_dochernij_vector.sh:377-433`, задания — `:537-547`. Поэтому названные критерии допускают это дерево, хотя заявленная в новой дельте «ТА ЖЕ семантика» с исключением строки 186 не обеспечена. Видимый `[exit=141]` не восстанавливает требование к результирующему коду: текст уже различает маркер и занулённый `details.exitCode`.

Это находка о правдивости и полноте НОВОГО утверждения эквивалентности, не требование перепроектировать остальную пачку или снова судить прежние атаки на страж. Для закрытия нельзя ограничиться перестановкой слов «когда isError=false» в «с isError=false»: необходимо согласовать утверждение о сохранённой семантике и его rc-доказательство с реально безусловным маппингом. Молчаливо снять гарантию строки 186 означало бы ослабить предмет, что задание на respec прямо запрещает. Решение о том, как обеспечить сохранение гарантии, остаётся автору; критик контракт и код не менял.

## Исполненные проверки и предел доказательства

1. Оба расширения прочитаны целиком; `git show 2c30d54 -- .omp/extensions/rc-prefix.ts` подтвердил происхождение и точное тело обработчика.
2. Выполнен throwaway-прогон `node /tmp/dev-harness-verify/critic025/carrier-probe.mjs /tmp/dev-harness-verify/critic025/repo`, rc 0. Он импортирует реальные default-factory из клона и вызывает зарегистрированные обработчики. Получено:

   ```text
   вход exitCode=141, isError=true      → exitCode=0, isError=false
   вход exitCode=141, isError=false     → exitCode=0, isError=false
   вход exitCode=141, isError отсутствует → exitCode=0, isError=false
   коды 0, 1, 7 → rc-prefix не возвращает патч
   после exit-marker: [exit=141], exitCode=141, isError=true
   после rc-prefix:   [exit=141], exitCode=0,   isError=false
   ```

   Последние две строки — явная последовательная композиция реальных обработчиков, не выдаваемая за запуск загрузчика omp. Алфавит имён подтверждён: `exit-marker.ts < rc-prefix.ts`; сохранение content при патче подтверждено исполнением. Исторический префикс с `kill -s PIPE $$` в отдельном bash действительно заканчивается сигналом SIGPIPE (`spawnSync: status=null, signal=SIGPIPE`), а не выполнением EXIT-trap. Отдельная команда инструмента bash, запускающая scratch-файл с `kill -s PIPE "$$"`, показала `Command exited with code 141` и `[exit=141]`; скрытые поля её события этим выводом не измерялись.
3. `bash fixtures/check_runner_hygiene/red_pipefail_prefiks.sh .` — rc 0, вывод: «ИМЕНОВАННЫЙ ОСТАТОК Р3 ... замороженная простая форма маппит и честный явный exit 141 в 0». Явный `exit 141` НЕ подменён в находке голой SIGPIPE-смертью: старый trap обрабатывает первый случай, но не второй.
4. Живые MAIN/CHILD заново не запускались. Утверждение о прежнем живом И-6 rc 0 и сохранении маркера взято из закоммиченного `verdicts/adversary/contracts-025-v3.md`, на который ссылается сам §B-1. Собственное исполнение здесь доказывает точную функцию обработчиков, а не новый end-to-end исход MAIN/CHILD.

## Остальные обязательные вопросы

- **Остаточный риск назван честно.** `contracts/025-sreda-cwd-rc.md:168-173` явно отрицает version-pin и автоматический upgrade-guard, называет зависимость от встроенного pipefail и окно между адверсарийскими раундами; ловец — следующий живой И-6. Дельта не выдаёт риск за закрытый. Это не блокер.
- **Семантические требования не удалены текстом.** `false|true` — ошибка у главной и ребёнка, `yes|head` — успех, нетронутый код 7 и исключение для голой SIGPIPE-смерти остались в §B-1. Проблема — доказанность новой эквивалентности, описанная единственной находкой выше, а не удаление старого абзаца.
- **Критерий слабее предмета / доказуемость командой.** Конкретная непокрытая граница и допускаемое дерево названы в ОБХОДЕ; И-2 зелёный исполнением, состав И-6 проверен по его исходнику. Наличие этих команд не доказывает заявленную полную эквивалентность двух носителей.
- **Решение не должно оставаться исполнителю молча.** Граница маппинга 141 сейчас описана как уже решённая, хотя код не имеет заявленного условия и различения. Это та же находка, не дополнительный блокер. Остальная дельта новых развилок или неназванных зон не вводит.
- **Разрешение владельца и AGENTS.md.** `git show -s --format=fuller 116d925 bbfadd0` подтвердил отдельные строки `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` первой колонки в обоих коммитах. Обязательный `bash scripts/check_charter.sh` в клоне завершился rc 0. В выводе есть `ok уставной документ изменён с разрешения владельца: contracts/025-sreda-cwd-rc.md в 116d9255` и такая же строка для `bbfadd01`; итог по файлу — изменений без разрешения нет. Отдельного противоречия норме в этой дельте не найдено.
- **Лишних секций нет.** Полный вывод `git diff frozen/contracts/025/3..HEAD -- contracts/025-sreda-cwd-rc.md` содержит один hunk `@@ -141,18 +141,38 @@`: только §B-1. Иные секции, зоны и критерии не изменены.

Первый отказ по этой новой причине в дельте v3→v4; опубликованные critic v1/v2/v3 этой причины не содержат. Повторный FAIL по одной причине здесь не объявляется.
