FAIL

## Пересуд четырёх блокеров круга 1

1. БЛОКИРУЕТ `contracts/021-sud-okna-svoej-vetki.md:73-86,137-143` — блокер 2 закрыт для названных случаев «всегда 0» и «пустая выборка зелёная», но не закрыт для самого временного договора меры. Предмет требует сравнивать времена коммитов как моменты времени; все три toy-входа меры используют только `+0000` (`fixtures/check_zones/red_mera_parallelnosti_okon.sh:61-93`), а объявленная честной контрольная форма читает `%ci` и сравнивает строки лексикографически (`fixtures/check_zones/stab_mera_chestnyj.sh:7,20`). Она проходит исходные ворота и пробу, но на конформных коммитах с разными смещениями часового пояса принимает последовательные окна за перекрывшиеся. Контрольный прогон с абсолютными временами `1769904000 < 1769907600 < 1769911200 < 1769914800` и представлениями `00:00 +0000`, `11:00 +1000`, `00:00 -0200`, `13:00 +1000` дал `ОТКАЗ: последовательная пара принята за параллельную: rc=0`.
ОБХОД: положить `fixtures/check_zones/stab_mera_chestnyj.sh` как `scripts/measure_parallel_windows.sh`. На исходном дереве `red_mera_parallelnosti_okon.sh` и `probe_slabye_realizacii.sh` вместе дают rc 0, то есть объявленные ворота меры удовлетворены; на приведённой последовательной истории тот же скрипт возвращает 0, хотя предмет требует 1 «не параллельно».

2. СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:89-103,127-136` — блокер 3 закрыт. Линейная сторона закреплена ожиданиями в исходнике, оконная сторона вызывает барьер, поэтому тождественного сравнения одного вычислителя нет. Прямой прогон `red_regress_posledovatel_naja_istorija.sh` дал rc 1 с причиной про чужой `land`-merge; фаза 4 `probe_slabye_realizacii.sh` отдельно довела до входа 3 реализацию, теряющую merge-принесённый коммит, и проба в целом дала rc 0.

3. СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:149-166` — блокер 4 закрыт. Контракт оставляет инварианты и rc-команды, а назначение входов находится в исходниках `red_*`, `stab_*` и `probe_*`. Поиск `§ *Стабы и их входы|стаб.*(→|↔).*case_` по контракту пуст; запрещённой Н-39 пары в тексте нет.

4. СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:237-253` — блокер 7 закрыт. Парная заморозка прямо отвергнута; приёмка требует два отдельных вызова `freeze_contract.sh`, каждый со своей причиной, затем rc 0 меры и rc 0 `check_zones` при обоих открытых окнах. Один парный акт либо один коммит с взаимными исключениями критерию не соответствует.

АРБИТР: роль `arbiter` — спор о достаточности временного оракула блока И-7: считается ли мера доказанной воротами только с `+0000`, если объявленная честной контрольная форма удовлетворяет им, но сравнивает `%ci` не как абсолютное время. Это второй `FAIL` по причине силы анти-плацебо меры.

## Пункт 5 задания — форма красного предъявления

СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:15-24,83-86,192-223` — форма `red_*` вне `case_*-глоба` совместима с `AGENTS.md:121-127`. Норма требует красный тест и rc-команду до круга критика, но не требует членства в глобе раннера. Прямые запуски наблюдаемо дали rc 1 с именованными причинами, а `probe_slabye_realizacii.sh` дал rc 0 на положительном контроле и трёх слабых реализациях. Для прямого запуска ограничение А-73 не действует: учитывается rc самого файла; для будущей конверсии контракт отдельно требует различающие проверки до первой зелёно-красной пары. Вынос сохраняет зелёный CI: полный scope `check_zones` дал rc 0, 13/13.

## Ранее зелёные пункты 1, 5 и 6

СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:43-69` — пункт 1 не ослаблен: merge-дельты своего `wip/<N>/…`, прямые first-parent-коммиты и draft-признание по тегу остаются предметом.

СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:169-177` — пункт 5 не ослаблен: исполнители и литеральные зоны названы; для раннера, hooks, roles и судейских каталогов записан явный отказ от раздачи с причиной.

СОВЕТ `contracts/021-sud-okna-svoej-vetki.md:98-99,272-275` — пункт 6 не ослаблен: done-ветка, СПАСЕНО-грамматика, charter И-7 и раннер остаются вне правки; живой `check_zones.sh .` дал rc 0 со сводкой `18 · 5 · 456 · 337`.

## Воспроизведение

Предмет проверен на закоммиченном HEAD `fb92dbdd4120fa7ec4b654d24ac41752bf3d7a2d` в одноразовом клоне; блоб контракта совпадает с `c7a212e`. Наблюдённые коды: `red_mera` — 1, `red_regress` — 1, `probe_slabye_realizacii` — 0, полный scope `check_zones` — 0 (13/13), `_schet_fixtur.sh` — 0, живой `check_zones.sh .` — 0. Основное дерево не переключалось.
