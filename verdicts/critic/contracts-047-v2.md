accept

Предмет: `contracts/047-blizhnec-priznanija-chernovika-check-zones.md` v2 на HEAD `68a3a18`
(origin/main). Узкий круг (прецедент 043 v2/v3): судится ТОЛЬКО дифф v1→v2, не весь контракт.
Правка — по прямому слову владельца, коммит несёт строку `РАЗРЕШИЛ-ВЛАДЕЛЕЦ` (проверено:
`git show 68a3a18` содержит её дословно), закрывает блокер Б-1 ревьюера
(`verdicts/review/contracts-047-v1.md`, `14db368`).

## Область правки

`git diff --stat 72d25ce 68a3a18 -- contracts/047-blizhnec-priznanija-chernovika-check-zones.md`
→ один файл, +7/-5. Полный дифф — РОВНО две правки: строка 137 (ПЕРЕСЕЧЕНИЕ с 023 — снято ложное
заявление «код приведён в соответствие с 023», заменено на честное «047 чинит ТОЛЬКО грамматику
четвёртого байта basename… вопрос глубины пути 023 не решает и 047 не решает — остаток назван
§Риски 4») и §Риски 4 (строки 222-227, переписан на фактический остаток). `git diff --exit-code
72d25ce 68a3a18 -- scripts fixtures` → rc=0: код и фикстуры НЕ тронуты. §Инварианты, §Приёмочный
критерий, §Зоны, исполнители — не менялись (сверено построчно против v1).

## Проверка честности нового §Риски 4 — ЖИВАЯ ПРОБА (позитив + негатив)

Новый текст утверждает: «Признание выдаётся на конформный basename НА ЛЮБОЙ ГЛУБИНЕ пути под
`contracts/`… `contracts/sub/123-x.md` и `contracts/a/b/123-payload.sh` выводятся из суда зон
(rc=0, путь не назван)». Построен toy-репозиторий (автор `agent-x`, зона ТОЛЬКО `scripts/`
через честно замороженный `frozen/contracts/001/1`, живой тег `id/CONTRACT/123`):

- Контроль (top-level `contracts/123-chernovik.md`): `check_zones.sh` rc=0 — базовая
  зелёная линия держит.
- **Позитив 1** (`contracts/sub/123-x.md`, глубина 2): барьер rc=0, путь `contracts/
  sub/123-x.md` в выводе НЕ встречается вовсе — признан, как и утверждает текст.
- **Позитив 2** (`contracts/a/b/123-payload.sh`, глубина 3): тот же результат — rc=0,
  путь не назван.
- **Негатив** (`contracts/sub/999notaconformname.md` — тот же автор, та же глубина, но
  basename НЕ конформен грамматике `<три цифры>-*`): барьер rc=1, путь **назван**
  дословно в отказе («коммит вне зоны: agent-x … contracts/sub/999notaconformname.md»).

Негативный контроль доказывает, что зелёный результат позитивов — не общая дыра (`rc=0`
не потому, что барьер вообще не судит author agent-x), а именно СЛЕДСТВИЕ конформного
basename на признанной глубине — ровно то, что заявляет исправленный текст. Старый (v1)
пример `contracts/123-x/y.md` в новом тексте отсутствует (он был неверен: у такого пути
basename = `y.md`, три первых байта не цифры — путь СУДИТСЯ, а не признаётся; ревьюер уже
показал это в Б-1).

Код `scripts/check_zones.sh` (предикат признания, строки ~869-873: проверка первого
компонента пути `contracts` + basename `<три цифры>-*`, БЕЗ проверки глубины) НЕ менялся
этой правкой — сверено диффом выше и подтверждено чтением исходника.

## ПОРОГ

Блокирующих находок — 0. Текст v2 факт-точен, область правки строго ограничена
названным предметом (§Риски 4 + одна строка ПЕРЕСЕЧЕНИЕ), код/фикстуры/критерий не
затронуты, malicious-класс остатка назван прямо («эксплуатация требует НАМЕРЕННО создать
такой вложенный путь»), решение владельца принять остаток зафиксировано текстом.

Готово к заморозке v2.
