FAIL

# Контракт 036 — критик, круг 2

Предмет: v2-дельта `contracts/036-spec-tochnost-prefriz-gejt.md` на HEAD `d2faa65`. Суд выполнен в отдельном клоне `/tmp/dev-harness-verify/critic-036-k2/repo`. Основной репозиторий, контракт и реализация не изменялись. Полный текст вердикта передаётся оркестратору для приземления в `verdicts/critic/contracts-036-v2.md` с author=critic.

Набор артефактов полный: предмет закоммичен, приёмочные команды названы, исполнители и ЗОНА-строки присутствуют. Четыре red-файла предъявлены в дереве. Содержательный суд ограничен предметом, приёмкой и границами исполнения; frontier-проза и токен наблюдения А-193 не служат самостоятельными основаниями отказа.

**Итог:** Б1 закрыт; Б3 закрыт в явно объявленной границе file-census; Б2 закрыт в части наличия исполнимого красного, но НЕ закрыт в части прямого запрета Н-39. Единственное блокирующее основание этого круга — сохранённые в приёмке пары «стаб ↔ вход» вопреки AGENTS.

## Б2 — повтор: запрещённые привязки остались в приёмочном критерии

БЛОКИРУЕТ contracts/036-spec-tochnost-prefriz-gejt.md:188–191,199–200,211–212 — контракт продолжает утверждать конкретные пары «обманный стаб ↔ ветвь/вход»: с1 «любая-красная-годится» ↔ г2, с6 «перестарался-зелёную-валит» ↔ г1, с2 «замер-маркер-игнор» ↔ г3, с3 «зоны-только-синтаксис» ↔ г4, с4 «СПАСЕНО-пропускник» ↔ г4д. Это именно раздел «Приёмочный критерий», а не внепредметная пояснительная проза. `AGENTS.md:130–133` прямо запрещает контракту утверждать такие пары; наличие соответствующего кода в фикстурах не отменяет запрета на второе утверждение в тексте контракта.

Основание блокирования — предусмотренное ролью исключение для противоречия AGENTS; отдельный ОБХОД здесь не требуется. Это не новый критерий и не требование переписать предъявленные фикстуры: запрет уже был назван в Б2 первого круга. Изменение табличной формы на абзацы его не исполнило.

Утверждение строк 180–181 «стаб-привязки Н-39 живут В КОДЕ фикстур, не в прозе контракта» опровергается непосредственно строками 188–212. Поиск `стаб` в приёмочном разделе не пуст; существенен не сам токен, а сохранившиеся явные соответствия с1↔г2 и остальные перечисленные пары.

Условие закрытия: убрать из приёмочного текста привязки конкретных обманных стабов к воротам, оставив поведенческие требования и команды. Привязки и их доказательство остаются в коде предъявления. Red-first файловую часть Б2 повторно открывать не требуется.

АРБИТР: роль `arbiter`; созыв запрошен мной у оркестратора Main до выдачи вердикта. Предмет — повтор Б2/Н-39: допустимо ли считать запрет контрактных пар «стаб ↔ ветвь» исполненным, когда пары перенесены из таблицы в абзацы приёмки и продублированы исполнимым кодом. Это узкий инженерный вопрос нормы, не вопрос владельцу о цене или воле; третьим обычным кругом критика его подменять не следует.

## 1. Б1 — грамматика СПАСЕНО согласована с живым потребителем

`contracts/036-spec-tochnost-prefriz-gejt.md:103–108,114–121` задаёт грамматику `СПАСЕНО <автор>: <полные 40-символьные хеши через пробел> — <непустая причина>`, объявленного автора, существующие коммиты окна и хеш-привязку к выпавшему пути. Она согласована с прочитанным живым потребителем `scripts/check_zones.sh:188–235`.

Приёмка г4б на строках 204–208 теперь требует не только зелёного нового spec-гейта, но и настоящего `check_zones` на замороженном toy, а также отрицательного контроля без СПАСЕНО.

Обе стороны воспроизведены отдельно от отсутствующего spec-гейта. Toy: замороженная v1 разрешает implementer пути foo+bar; исторический коммит implementer меняет foo; замороженная v2 оставляет только bar.

```text
без СПАСЕНО:
  FAIL коммит вне зоны: implementer fbaa2207 scripts/foo.sh — зона автора (объединение всех замороженных): scripts/bar.sh
  rc=1

со строкой:
  СПАСЕНО implementer: fbaa22073cd62da1be9a6f44b093e767db717205 — перенос в семейство Y

  ok   контракт 001: коммит fbaa2207 (implementer) — СПАСЕНО, из суда зон выведен
  rc=0
```

Б1 первого круга закрыт. Это измерение настоящего потребителя зон, не приписанный будущему гейту результат.

## 2. Б2 — файловая red-first часть исправлена

Все четыре прямых запуска выполнены на предъявленном HEAD:

| Команда | rc | Именованный вывод |
|---|---:|---|
| `bash fixtures/freeze_contract/red_prichina_predmeta_036.sh` | 1 | `ПРЕДМЕТ 036 НЕ РЕАЛИЗОВАН: В1 причина-предмета` |
| `bash fixtures/freeze_contract/red_zamer_036.sh` | 1 | `ПРЕДМЕТ 036 НЕ РЕАЛИЗОВАН: В2 замер-грамматика` |
| `bash fixtures/check_zones/red_perenos_zony_036.sh` | 1 | `ПРЕДМЕТ 036 НЕ РЕАЛИЗОВАН: В3 перенос зоны` |
| `bash fixtures/freeze_contract/red_spec_preflight_036.sh` | 1 | `ПРЕДМЕТ 036 НЕ РЕАЛИЗОВАН: класс-гейт заморозки (spec-preflight)` |

Каждый вывод продолжает сообщение указанием на отсутствующий `scripts/check_spec_ready.sh`. Кодов 127 нет. В коммите a678965 контракт и эти четыре файла предъявлены вместе. Файлы содержат построение входов и проверки поведения за проверкой наличия субъекта; это не четыре файла из одного `exit 1`.

Независимое раскрытие файловых множеств дало 3 для `fixtures/freeze_contract/red_*036.sh` и 1 для `fixtures/check_zones/red_perenos_zony_036.sh`, как объявлено в строках 176–177.

Данный прогон доказывает именно именованное красное предъявление до реализации. Он не означает, что будущая реализация прошла внутренние ворота: сейчас каждый файл останавливается на отсутствии субъекта. Оставшаяся нормативная часть Б2 описана выше отдельно.

## 3. Б3 — константный обход различается другой мерой

На строках 93–102 теперь заданы две независимые проверки числа: исполнение команды автора и собственное раскрытие census-глоба гейтом. Для обхода первого круга результат следует из текста однозначно:

| Вход | Обязательный результат по v2 |
|---|---|
| `замер: `printf '999\n'` = 999` | Отказ: `нет census-глоба`. |
| Та же команда с `census probes/*.sh`, которого нет в команде | Отказ: `census-глоб не входит в команду`. |
| Команда-константа с литеральной вставкой фиктивного глоба, N=999, фактических файлов 0 | Структурную проверку можно пройти, но независимый пересчёт даёт 0: отказ `заявлено 999, дерево даёт 0`. |

Сама команда `printf` действительно даёт 999, а независимое раскрытие использованного пустого множества — 0. Выполнен малый эксперимент; spec-гейт ещё отсутствует и не запускался. Отказы в таблице — вывод из обязательного алгоритма контракта, не выдуманная стенограмма реализации.

Не всякая команда-константа синтаксически запрещена: при литеральной вставке глоба она может пройти грамматику. Существенно, что ложное N уже не проходит независимую меру. Если константа равна действительному количеству файлов, само file-census утверждение истинно.

Релевантность выбранного множества предмету числа явно оставлена cognitive-only на строках 253–260. Подбор другого глоба, который действительно содержит 999 нерелевантных файлов, относится к названному остаточному риску, а не к обещанной механической проверке смысла. Б3 первого круга закрыт в этой явно объявленной границе.

## 4. «Гейт написан, но freeze его не вызывает» — не разрешённый обход критерия

`contracts/036-spec-tochnost-prefriz-gejt.md:140–144` предписывает настоящий вызов spec-preflight до doc-preflight, капа и тега. Приёмка на строках 213–222 требует отрицательного конца-в-конец через настоящий freeze, отказа по spec-preflight и отсутствия нового тега; положительный контроль должен создавать тег. Добавлены отрицательные В2- и В3-входы.

Поэтому состояние «скрипт гейта есть, но freeze его игнорирует и замораживает красный черновик» не удовлетворяет г5. Не объявляю этот пример новым блокером и не утверждаю, что отсутствующая реализация уже проверена.

СОВЕТ contracts/036-spec-tochnost-prefriz-gejt.md:219–220 — приёмочный В3-вход г5г следует предъявить с историческим коммитом implementer по выпавшему пути, как это сделано в г4. В нынешнем кодовом предъявлении `fixtures/freeze_contract/red_spec_preflight_036.sh:68–83` между v1 и v2 такого коммита нет. Инвариант В3 на строках 117–119 требует покрывать именно затронувшие путь коммиты; пустое множество коммитов само по себе не доказывает требуемый отказ. Это совет о согласовании отрицательного входа с названной семантикой, не дополнительное основание FAIL.

## 5. Границы исполнения и доказуемость

Основные изменения поимённо разнесены на строках 152–157: четыре red-файла — architect; spec-гейт, мета-барьер, freeze, case-файлы, package.json и CI — implementer; роль critic — orchestrator. Действующие барьеры и общие помощники, не требующие изменения, отдельно оставлены вне этой раздачи. Механизм границ — замороженные ЗОНА-строки и настоящий `check_zones` по автору коммита. Для обязательного объёма реализации нового неназванного исполнителя не обнаружено.

Команды зелёной приёмки перечислены на строках 224–233; санкционированная заморозка следующего живого контракта отделена от повторяемого toy-прогона. Секция `Незаполненные требования: нет` присутствует на строках 242–243. Правка роли critic этим кругом не выполнялась; требования согласия судьи на её фактическое изменение не отменены.

В пределах v2-дельты существенные решения Б1 и Б3 больше не оставлены implementer на догадку. Единственное блокирующее противоречие старшей норме — перечисленные выше привязки Н-39.

## Scoped-проверки и сохранность барьеров

```text
npm run check:antiplacebo -- --scope freeze_contract
rc=0
вывод раннера: барьеров 1 · фикстур 14 · предъявлено красным повторным прогоном 14

npm run check:antiplacebo -- --scope check_zones
rc=0
вывод раннера: барьеров 1 · фикстур 21 · предъявлено красным повторным прогоном 21
```

Первоначальная параллельная попытка `check_zones` получила rc=3 от штатного lock раннера, занятого freeze_contract. После завершения первого прогона check_zones запущен последовательно и завершился rc=0; занятость не выдана за дефект проверяемого барьера.

Проверка сохранности существующих затронутых барьеров:

```text
git diff --exit-code a4fc8af HEAD -- \
  scripts/check_zones.sh scripts/freeze_contract.sh \
  scripts/check_contract_ready.sh scripts/check_check_contract_ready.sh \
  scripts/verify_antiplacebo.sh \
  fixtures/freeze_contract/_repo.sh fixtures/check_zones/_repo.sh \
  'fixtures/freeze_contract/case_*.sh' 'fixtures/check_zones/case_*.sh'
rc=0, вывод пуст
```

Собственная живая репродукция `bash tmp/critic036_k2.sh` завершилась rc=0. Она проверяет оба исхода СПАСЕНО, независимо считает четыре предъявленных red-файла и показывает расхождение константы 999 с пустым файловым множеством. Скрипт сохранён в клоне; контракт и реализация не правились. Клон оставлен до приземления вердикта.

Полный CI и project-wide проверки не запускались. Заморозка 036/1 этим вердиктом не разрешается. Отказ ровно один: незакрытая нормативная часть Б2/Н-39; Б1 и Б3 повторно открывать не требуется.
