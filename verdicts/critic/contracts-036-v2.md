accept

# Контракт 036/2 — критик: зонная дельта

Предмет: закоммиченный `contracts/036-spec-tochnost-prefriz-gejt.md` на HEAD `3eb0ca8d3a4d6854faab45efdfc31bd7db8f0571`; только дельта `2cb78be..3eb0ca8`, содержательная правка `4360258`. Проверка выполнена в собственном клоне локального репозитория: `/tmp/dev-harness-verify/critic-036-v2z/repo`.

Граница суда — закрытие Б1/Б2 ревьюера `verdicts/review/contracts-036-k1.md`: каталожная зона implementer, СПАСЕНО для трёх прежних поимённых путей, живой репозиторный check_zones в приёмке, снятие прежней пост-фриз-двери архитектора. Реализация и прежние критерии v1 заново не судились. В полном диапазоне коммитов изменён также `HANDOFF.md`; он не входит в предмет этого круга. Утверждение об одном файле относится к контрактной дельте, не ко всему диапазону git.

## Полнота предмета

Набор полон: предмет закоммичен; названы предмет, критерии с командами и кодами возврата, исполнители и ЗОНА-строки. Оснований для отказа без разбора нет.

## Собственные проверки

### 1. СПАСЕНО соответствует 003-v3 и покрывает исторические коммиты

`contracts/036-spec-tochnost-prefriz-gejt.md:163-164`: автор `implementer` объявлен ЗОНА-строкой; хеш `b98896acabd9d810c7bc746c2dd6d0839b4fbc0a` — полный 40-символьный hex существующего коммита; причина после ` — ` непуста. Грамматика сверена с блобом `frozen/contracts/003/3:contracts/003-skills-metta-adaptacija.md` и живым потребителем `scripts/check_zones.sh:188-235`.

Собственный исторический замер:

```text
$ git log --format='%H %an %s' --name-only frozen/contracts/036/1..HEAD -- fixtures/check_check_spec_ready/case_prichina_predmeta.sh fixtures/check_check_spec_ready/case_zamer.sh fixtures/check_check_spec_ready/case_perenos_zony.sh
b98896acabd9d810c7bc746c2dd6d0839b4fbc0a implementer 036 — мета-семья check_check_spec_ready: wrapper + 3 case_* (причина-предмета, замер, перенос зоны) + freeze-contract case_spec_preflight_gejt

fixtures/check_check_spec_ready/case_perenos_zony.sh
fixtures/check_check_spec_ready/case_prichina_predmeta.sh
fixtures/check_check_spec_ready/case_zamer.sh
```

Это единственный показанный историей коммит по трём выпадающим поимённым путям в объявленном окне; автор — implementer. Оба вызова `git merge-base --is-ancestor` — от `frozen/contracts/036/1` до этого хеша и от хеша до HEAD — завершились rc 0. Таким образом, покрытие не опирается на короткий хеш, чужого автора или коммит вне окна.

СПАСЕНО называет создателя трёх прежних файлов, а не проблемный `0a4336ac`: symlink-фикстуру легализует именно каталожная ЗОНА-строка. Это разные механизмы, и текст их не смешивает.

### 2. Г6 до заморозки действительно красна заявленной причиной

На HEAD без `frozen/contracts/036/2` выполнено:

```text
$ bash scripts/check_zones.sh .
rc=1
```

Единственная строка отказа начинается дословно:

```text
  FAIL коммит вне зоны: implementer 0a4336ac fixtures/check_check_spec_ready/case_symlink_vne_kornja.sh
```

Здесь приведён префикс строки; далее механизм перечисляет зону автора. Итоговая строка вывода:

```text
замороженных контрактов: 35 · объявленных авторов: 6 · коммитов в диапазонах: 1408 · проверено по зонам: 912
```

Следовательно, `contracts/036-spec-tochnost-prefriz-gejt.md:234-239` предъявляет не произвольный rc 1, а существующий отказ по исправляемому пути с объявленной фразой `коммит вне зоны`.

### 3. Та же команда зеленеет от кандидатной заморозки

Только в собственном клоне поставлен пробный тег на проверенный HEAD:

```text
$ git tag frozen/contracts/036/2 HEAD
rc=0
$ bash scripts/check_zones.sh .
rc=0
```

В выводе присутствует:

```text
  ok   контракт 036: коммит b98896ac (implementer) — СПАСЕНО, из суда зон выведен
```

Отказов нет. Итоговая строка:

```text
замороженных контрактов: 35 · объявленных авторов: 6 · коммитов в диапазонах: 1408 · проверено по зонам: 911
```

Изменялся только набор локальных тегов, не код барьера и не проверяемые файлы. После эксперимента тег удалён:

```text
$ git tag -d frozen/contracts/036/2
Deleted tag frozen/contracts/036/2 (was 3eb0ca8)
rc=0
```

Это проба потребителя зон, а не санкционированная заморозка писателем реестра. Она не заменяет слово владельца, `freeze_contract.sh` и подтверждение ревьюера после настоящего ре-фриза.

### 4. Зелёный spec-preflight предъявлен своим прогоном

До постановки пробного тега:

```text
$ bash scripts/check_spec_ready.sh . contracts/036-spec-tochnost-prefriz-gejt.md
OK
rc=0
```

Требование стенограммы из `contracts/036-spec-tochnost-prefriz-gejt.md:156-158` выполнено. Красная г6 не маскирует посторонний отказ: её действительная причина независимо проверена выше.

### 5. Не-ослабление и scoped-регресс

Полный контрактный diff прочитан. Изменены преамбула, зона implementer и СПАСЕНО, размещение red-файлов/снятие пост-фриз-двери, добавление г6 и зелёного репозиторного check_zones. Раздел инвариантов не изменён; прежние приёмочные команды и требуемые исходы сохранены, включая живую заморозку следующего контракта.

```text
$ git diff --exit-code frozen/contracts/036/1 2cb78be -- contracts/036-spec-tochnost-prefriz-gejt.md
[пустой вывод]
rc=0
$ git diff --exit-code 2cb78be..3eb0ca8 -- scripts fixtures roles .github/workflows/ci.yml package.json
[пустой вывод]
rc=0
```

Scoped-регресс затронутого потребителя:

```text
$ npm run check:antiplacebo -- --scope check_zones
SCOPED: барьеров 1 из выборки — не для приёмки
[строки результатов отдельных фикстур опущены]
барьеров: 1 · фикстур: 21 · предъявлено красным повторным прогоном: 21
rc=0
```

Среди реально исполненных — конечность СПАСЕНО, отказ на невалидной грамматике, union зон заморозок и коммит вне зоны. Полная проектная приёмка не заявляется и не выполнялась.

## Пять обязательных вопросов — только к дельте

1. **Критерий слабее предмета?** Нет обнаруженного обхода. Зелёный пункт `:244-247` требует настоящий check_zones по репозиторию, а не наличие фикстуры или зелёный toy. Его различающая способность по исправляемой боли показана состояниями без/с кандидатным тегом.
2. **Готовность недоказуема названной командой?** Новый пункт содержит исполняемую команду и rc 0. Отрицательный контроль г6 содержит команду, ожидаемый rc 1 и предметную фразу; оба состояния воспроизведены.
3. **Решение молча оставлено исполнителю?** Нет нового такого решения: `:163-164` задаёт каталог и точный исторический хеш; `:175-181` прямо снимает прежнюю дверь архитектора и называет маршрут дальнейшего расширения его зоны.
4. **Границы исполнения не названы?** Названы и механизированы: каталог принадлежит implementer; четыре red-файла остаются в поимённой зоне architect. Границу читает check_zones из заморозки, а не из задания или переписки.
5. **Противоречие AGENTS.md?** В судимой дельте не обнаружено. Не-ослабление подтверждено diff; красный контроль предъявлен; изменение замороженного текста требует слова владельца согласно преамбуле `:12-18`. Этот accept не даёт права обойти правило 11.

## Итог

Новых блокирующих дефектов дельты не обнаружено. Находок и советов нет.

Б1 закрывается объявленным каталожным маршрутом и валидным СПАСЕНО; Б2 закрывается добавленной репозиторной мерой, для которой своим прогоном предъявлены красное до и зелёное при кандидатной заморозке.

**accept** — зонная дельта допускается к дальнейшему маршруту: прямое слово владельца → санкционированный freeze 036/2 → подтверждение ревьюера с check_zones rc 0 на фактическом репозитории. Это не вердикт done/036 и не замена оставшейся приёмки v1.

— critic, 2026-09-20
