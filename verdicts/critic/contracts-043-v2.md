FAIL

# Контракт 043 — второй круг критика

Предмет: `contracts/043-precizionnyj-prefriz-gejt.md` на закоммиченном HEAD
`0ca481acc1776b13e897f532c98485512ac72cda`. SSH-клон получен с
`ssh://git@github.com/a3ka/dev-harness.git`. Контракт и собственный вердикт
`verdicts/critic/contracts-043-v1.md` прочитаны целиком. Набор полный:
предмет, команды приёмки, architect/implementer и ЗОНА-строки существуют.

Судится достаточность контракта и его приёмки. Реализация использована как
конкретное состояние дерева, удовлетворяющее критерию; общий аудит кода не
проводился. Полный CI не повторялся, зелёный CI на этом HEAD принят как
предъявленный факт. Все приведённые ниже scoped-прогоны выполнены живьём.

## Обязательная приёмка

- `bash fixtures/_krasnye_043.sh` → rc 0: **13 файлов, провалов 0**.
- `bash fixtures/parsing_hygiene_battery/run_battery.sh check_precision_gate`
  → rc 0: **4/4 классов закрыто**. В stderr дополнительно было
  `fatal: tag 'frozen/contracts/991/1' already exists`; это не замалчивается
  и не выдаётся за чистый stderr.
- `bash scripts/check_precision_gate.sh /tmp/dev-harness-verify/critic043-round2 contracts/043-precizionnyj-prefriz-gejt.md`
  → rc 0, `OK`; stderr честно сообщает, что семья `precision_gate`
  пропущена задачей (б), поскольку барьер правится этим же черновиком.
- `npm run check:precision-gate` → rc 0, `OK`. Реально вызвана команда
  `bash scripts/check_precision_gate.sh . contracts/043-precizionnyj-prefriz-gejt.md`:
  прежняя ошибка числа аргументов исправлена.
- Отдельный `bash fixtures/check_precision_gate/red_14_freeze_refuses_red_precision.sh`
  → rc 0: `case_14: freeze-time backstop refuses (rc 1, именованная причина, тег НЕ записан)`.

## Перепроверка каждого обхода первого круга

**Б1 — исходный обход закрыт.** В независимом toy созданы настоящие коммиты
и теги `frozen/contracts/991/1` и `frozen/contracts/992/1`: `shared/file.txt`
принадлежит alice и bob. Черновик объявляет путь за architect.

```
MULTIPLE_COLLISIONS gate_rc=1 expected=1
precision-гейт 043: зона-коллизия: shared/file.txt заявлен architect (этот контракт, NNN 043), уже в union под NNN 992 для bob — нужна строка ПЕРЕСЕЧЕНИЕ architect: shared/file.txt — 992 <причина>
ALL_COLLISIONS_DECLARED gate_rc=0 expected=0 output=OK
```

Первая строка ПЕРЕСЕЧЕНИЕ объявляла только 991; добавление отдельной строки
для 992 изменило результат на 0. Это проверка всех столкновений одного пути,
а не пересказ `red_13` или шапки реализации.

**Б2 — исходный forged_trace закрыт.** Новый case, не вызывающий барьер,
условно пишет `+ <абсолютный путь барьера>` в `BASH_XTRACEFD`, если переменная
задана, затем БЕЗУСЛОВНО завершает работу с 0. Поэтому результат не зависит
от ошибки перенаправления в отсутствующий fd:

```
FORGED_TRACE plain_rc=0
FORGED_TRACE gate_rc=1 expected=1 output=precision-гейт 043: fixtures/check_dummy/red_forged_trace.sh: барьер dummy не вызван живьём (/proc-наблюдение не нашло exec)
```

Гейт больше не создаёт и не передаёт собственный trace-fd case-файлу;
наблюдение потомков через `/proc` находится в процессе гейта
(`scripts/check_precision_gate.sh:376–409`). Это закрывает именно исходный
обход, но не доказывает достаточность новой положительной проверки: см. Б5.

**Б3 — исходный xtrace_only закрыт.** Case вызывает буквальный абсолютный
путь настоящего dummy-барьера, затем исполняет
`case $- in *x*) exit 0 ;; *) exit 1 ;; esac`.

```
UNTRACED_SELFCHECK plain_rc=1
UNTRACED_SELFCHECK gate_rc=1 expected=1 output=precision-гейт 043: fixtures/check_dummy/red_xtrace_only.sh: самопроверка провалена (rc 1, ожидался 0)
```

Отдельный прямой запуск с собственным `direct_rc` существует
(`scripts/check_precision_gate.sh:349–359`), отдельно сохраняется
`traced_rc` внешне наблюдаемого запуска (:384–410). Для обычного case оба
должны быть 0. Исключение для явно ожидаемо красного сейчас case названо
самим контрактом :184–212; оно не использовалось в этих опытах.

**Б4 — freeze-time половина закрыта, CI-семейная половина нет.** Хук
`scripts/freeze_contract.sh:195–203` вызывает precision-гейт до записи тега
и отказывает при любом ненулевом rc; живой `red_14` подтвердил отсутствие
тега после отказа. `npm run check:precision-gate` исправлен. Но это вызов
самого sibling на 043, а не CI-вызов семейной приёмки Р10/Р11.

## Блокирующие находки

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:332 — Р10/Р11 и Р13 не доказывают обещанную в :285–287 CI-проводку семейной приёмки; часть Б4 первого круга остаётся открытой.
ОБХОД: текущее дерево HEAD 0ca481a сохраняет всю семейную приёмку только для ручного запуска, а в CI вызывает единственно `npm run check:precision-gate` → self-application 043 с пропуском задачи (б). Все объявленные исполняемые пробы выше зелёные, хотя Р10/Р11 в CI не исполняются. Отсутствующая часть предмета не отличена критерием от законченной работы.

Это не обвинение в отсутствии npm-команды: она есть и успешно выполнена.
Именно семейный канал назван в Б4 v1. Проверены `.github/workflows/ci.yml`
и `package.json`: нет вызова `_krasnye_043.sh` или parsing-батареи 043.
Единственный precision-шаг CI (:202–213) вызывает self-gate.
Альтернативного пути через antiplacebo нет: семья имеет `.probe-only`,
содержит red_/green_ файлы, отсутствует в матрице ключей CI; раннер выбирает
`case_*.sh` (`scripts/verify_antiplacebo.sh:585`). Комментарий CI о Р10/Р11
не исполняет эти команды. Полный CI ради подтверждения уже данного факта
его зелёности не запускался.

БЛОКИРУЕТ contracts/043-precizionnyj-prefriz-gejt.md:324 — Р7/Р8 проверяют отказы, но критерий не отличает наблюдение честного живого вызова от механизма, отказывающего также честному новому case. Р1/Р3 не вводят такую семью, Р9/Р13 исключают собственную семью из задачи (б).
ОБХОД: на том же дереве, проходящем 13/13, 4/4 и self-gate, новый `green_live.sh` буквально вызывает абсолютный путь `scripts/check_dummy.sh` и возвращает его rc. Dummy печатает свой PID, исполняет `sleep 1`, возвращает 0. Есть тег минта 043; семья объявлена в ЗОНА, сам барьер в ЗОНА не объявлен, исключения не применимы. Прямой case зелёный; оба запуска внутри гейта действительно исполняют барьер, но гейт возвращает 1 с ложным диагнозом «барьер dummy не вызван живьём». Критерий готовности принимает это дерево, хотя предмет «подтверждённо исполнен ЖИВЬЁМ» :41–45 не обеспечен для честного входа.

Сырой вывод отдельного положительного контроля:

```
ACTUAL_BARRIER_EXEC pid=1322114
HONEST plain_rc=0
HONEST gate_rc=1 output=ACTUAL_BARRIER_EXEC pid=1322180
ACTUAL_BARRIER_EXEC pid=1322190
precision-гейт 043: fixtures/check_dummy/green_live.sh: барьер dummy не вызван живьём (/proc-наблюдение не нашло exec)
```

Контроль не подменял гард, не использовал BASH_XTRACEFD, не менял окружение
вызова гейта и не передавал путь барьера как аргумент посторонней команды.
Результат также противоречит правилу 7 AGENTS.md: красное с ложным диагнозом
не является честным отказом. Это не требование расширить список видов
вызова: проверен простой абсолютный shell-вызов из исходного предмета.
Не задаю автору конкретную замену /proc; нужен критерий, который отвергает
предъявленное дерево и сохраняет честный положительный вход.

АРБИТР: роль `arbiter`; созван через оркестратора. Предмет спора — повтор Б4: достаточно ли CI-self-gate с исключённой задачей (б) вместо семейной CI-приёмки Р10/Р11, прямо названной незакрытой в первом вердикте. Автор контракта не судья этого вопроса; третьего круга критика не назначаю.

## Советы, не дополнительные блокеры

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:171 — текст по-прежнему предписывает `BASH_XTRACEFD`/`PS4`, наследуемый fd и командную позицию bash-трассы, а текущая реализация перешла к /proc. Утверждение :174–175 о недоступности fd пробе осталось старым. Синхронизировать предмет/инвариант/модель угроз с выбранным механизмом до заморозки; закрытие конкретного runtime-обхода Б2 этим замечанием не отменяю.

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:131 — текст «пара пропущена, зелёная» остался в единственном числе; живая проверка уже требует отдельное объявление для каждого NNN. Явно закрепить достигнутое свойство в контракте, чтобы исполнитель не восстановил старую семантику.

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:221 — в закоммиченном контракте всё ещё написано а → б → в и одновременно «б последней». А → в → б документировано в шапке скрипта :39–43, но фактический вызов `verify_ci_parity.sh` расположен после задачи (б), :413–417. Самостоятельного обхода предмета из порядка не предъявляю.

СОВЕТ contracts/043-precizionnyj-prefriz-gejt.md:351 — ПРОВОДКА содержит roles/architect.md, а ЗОНА этого пути не называет. Как и в v1, существующая уже приземлённая норма не требует повторной правки; если её придётся менять, исполнитель и граница должны быть названы явно.

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Да: незакрытая CI-семейная часть Б4 и
   предъявленное в Б5 зелёное по приёмке дерево с отказом честному вызову.
2. **Готовность доказуема командами?** Локальные заявленные команды исполнимы
   и зелёные; freeze-refusal теперь доказан живьём. CI-семейный канал и
   честная положительная задача (б) ими не доказаны.
3. **Решение оставлено исполнителю?** Канал CI-семейной приёмки остаётся
   незафиксированным критерием; расхождение описания оракула и порядка
   отмечено отдельно, без навязывания нового дизайна.
4. **Границы?** Architect/implementer и зоны заданы, механизм — check_zones;
   прежний совет о roles/architect.md остаётся советом.
5. **Старшая норма?** Модель угроз присутствует. Ложный отказ честному вызову
   противоречит правилу 7. Норму fail-closed при любом ненулевом rc паритета,
   включая rc2, НЕ переоткрываю: Н-142 и починенные toy-потребители не
   основание ослаблять решение владельца и не отдельный блокер этого круга.

Единственная постоянная правка — этот вердикт. Контракт, код, роли, CI и
постоянные фикстуры не изменялись. Опыты исполнялись в разрешённом scratch
`/tmp/dev-harness-verify` (перенос внутрь ./tmp отклонён workstation-барьером
непиннованной сессии). Полные suites, линтеры и форматтеры не запускались.
