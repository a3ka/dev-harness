accept

# Контракт 031 v4 — scoped: СПАСЕНО-дельта

Предмет: только вставка contracts/031-strazh-semejstvo-mint-i-bazlajn.md:330–333 в HEAD 645aa89ff33f334ea67677992fab6215cf413e9b относительно frozen/contracts/031/3. Инварианты, приёмки, ЗОНА-строки и AFFIRM v3 не переоткрывались. Реализация механизмов 031 этим вердиктом не принимается.

Набор артефактов полон: предмет закоммичен; критерии и команды приёмки, именованные исполнители и объявления зон присутствуют в сохранённом контракте. Свидетельство консультанта прочитано из коммита 659627d; вставка побайтово совпадает с его точной рекомендацией. В теле 645aa89 присутствует строка РАЗРЕШИЛ-ВЛАДЕЛЕЦ: в первой колонке, кириллицей; владельческий коммит 9b2c7ca содержит указанное прямое слово. Подлинность владельческого слова сверх этой записи не аттестуется: действует cognitive-only граница правила 11.

## Результат пяти обязательных вопросов

1. **Критерий слабее предмета?** Для предъявленной дельты обход не найден. Исключение конечно: один объявленный автор, три конкретных полных хеша, непустая причина. Действующие зоны не расширяются; будущие коммиты строкой не названы. Исключение применяется к перечисленным коммитам целиком по существующей грамматике 003-v3, а не является новым разрешением на путь.
2. **Готовность доказуема командой?** Да, после появления v4 в реестре заморозок. Незамороженная вставка сама по себе не меняет результат check_zones. Проведены оба состояния: без локального v4 — rc 1 с тремя ожидаемыми отказами; с пробным локальным v4 на проверенном HEAD — rc 0, все три коммита явно признаны СПАСЕНО, иных FAIL нет. Это доказательство эффекта будущей заморозки, не заявление о выполненной официальной заморозке или зелёном удалённом CI.
3. **Решение оставлено исполнителю?** В дельте такого решения нет: автор, множество хешей, причина и место вставки заданы точно. Причина различает создание/исправление по v1 и авторизованный перенос v2, не утверждает недоказанное временное покрытие всех трёх коммитов одной зоной.
4. **Границы исполнения названы?** Да. В изменяющем коммите затронут только контракт 031; его исполнитель architect объявлен сохранённой ЗОНА-строкой. Все семь ЗОНА-строк побайтово прежние. Код, фикстуры и уставные документы вне контракта этим коммитом не менялись.
5. **Противоречие AGENTS.md?** В scoped-дельте не найдено. Используется существующий механизм поимённого исключения, а не необеспеченная проза; изменение замороженного текста снабжено требуемой строкой разрешения. Следующий шаг — новая версия заморозки с закоммиченным вердиктом, не подмена v3.

## Собственные проверки

Все прогоны — в изолированном клоне /tmp/dev-harness-verify/critic-031-v4/repo на указанном HEAD. Основной checkout, его ветки и теги не изменялись.

1. `git diff frozen/contracts/031/3 HEAD -- contracts/031-*.md` → rc 0: ровно четыре добавленные строки перед «## Остаточный риск (назван прямо)», удалений нет. Независимая проверка байтов скриптом `/tmp/dev-harness-verify/critic-031-v4/verify_delta.py` → rc 0: удаление только вставленной секции восстанавливает весь frozen-v3-блоб побайтово; ЗОНА-строки 7 = 7 и идентичны. Тем самым сохранность всех остальных приёмок и инвариантов проверена целиком, а не выборочно.

2. Грамматика сверена с закоммиченным frozen/contracts/003/3. Проверены существование commit-объектов, автор implementer и обе границы диапазона для каждого хеша:
   - 136ab1f0705b5b446aed18ad5d277cd95f1dd7dc;
   - 67f7437c5b835a19b4b7c359cbcb8dcffd68c26e;
   - b11943e124b2fd465be2dec24c408db967be18db.
   Для каждого выполнены `git merge-base --is-ancestor frozen/contracts/031/1 <hash>` и `git merge-base --is-ancestor <hash> HEAD`; все шесть команд → rc 0. Отдельная проверка строки подтверждает ровно три 40-hex, объявленного автора и непустую причину.

3. `bash scripts/check_zones.sh /tmp/dev-harness-verify/critic-031-v4/repo` до пробного тега → rc 1: ровно три «коммит вне зоны» на fixtures/check_staged/case_dver_minta_priznanie.sh для перечисленных хешей. Затем только в клоне создан аннотированный пробный frozen/contracts/031/4 на 645aa89; повтор той же команды → rc 0, с выводом:

   ```text
   ok   контракт 031: коммит 136ab1f0 (implementer) — СПАСЕНО, из суда зон выведен
   ok   контракт 031: коммит 67f7437c (implementer) — СПАСЕНО, из суда зон выведен
   ok   контракт 031: коммит b11943e1 (implementer) — СПАСЕНО, из суда зон выведен
   ```

   Итог обоих прогонов: 32 замороженных контракта, 1230 коммитов в диапазонах; проверено по зонам 786 до / 783 после. Пробный тег удалён после проверки, никуда не отправлялся. Официальный freeze_contract.sh не запускался.

4. `git show bd3853f` → rc 0: подтверждён прецедент 028 v3 — поимённая СПАСЕНО-вставка при снятии старого пути, с РАЗРЕШИЛ-ВЛАДЕЛЕЦ в теле коммита. В прецеденте также добавлялась зона арбитра; он подтверждает класс исключения, а не побайтовую тождественность всей правки, и не заменяет новое разрешение для 031.

5. `git diff --exit-code 645aa89^ 645aa89 -- . ':(exclude)contracts/031-strazh-semejstvo-mint-i-bazlajn.md'` → rc 0, пусто. `git diff --exit-code 9b2c7ca^ 9b2c7ca -- . ':(exclude)forks/spaseno-031-v4.md'` → rc 0, пусто. 9b2c7ca добавляет только форк-запись.

## Неблокирующие замечания к интерпретации доказательства

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:330 — не описывать исходный HEAD без заморозки v4 как уже зелёный по check_zones. Читатель берёт высшую заморозку, а не HEAD-текст. Зелёный результат предъявлен после локального моделирования v4; в основном репозитории для этого ещё нужны штатная заморозка и доставка тега.

СОВЕТ contracts/031-strazh-semejstvo-mint-i-bazlajn.md:330 — утверждение «дифф вне 031 пуст» верно для самой правки 645aa89^..645aa89, но неверно для всего исторического интервала frozen/contracts/031/3..645aa89: там есть изменения ещё 29 файлов. Эти промежуточные изменения не относятся к единственному предмету данного круга и не судились. Побайтовая неизменность остального текста самого контракта относительно v3 доказана отдельно.

Блокирующих находок в СПАСЕНО-дельте нет. Заморозка v4 разрешена после штатного приземления этого вердикта. Зелёность всего CI, готовность 033/030/031, done-теги и первый автономный минт настоящим вердиктом не утверждаются.
