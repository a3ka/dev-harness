FAIL

# Контракт 030 — критик, круг 1, до заморозки

Предмет: `contracts/030-inventar-i-gonki-spavna.md` на
`b31d0b5f1e19c69b22f5c5d66db987c301658d39`, ветка `wip/030/architect`.
Судья: critic; модель `openai-codex/gpt-6-astra`.

Набор достаточен: предмет закоммичен, инварианты, rc-команды и исполнители названы,
пять ЗОНА-строк присутствуют. Пробы 1/3 и 2/3 рассмотрены в явно предъявленной
скратч-раскладке А-174, а не выданы за файлы HEAD; проба 3/3 — из клона HEAD.
Реализация не судилась и в ветку не добавлялась. Ниже судится различающая способность
объявленного критерия. Намеренно неверные варианты сделаны ТОЛЬКО в одноразовом клоне.

## Блокирующие находки

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:116 — проверка инвентаря определяет
базовый/FABLE-режим наличием `edit`, а не тем, какое из двух присваиваний питает этот
режим. Поэтому инварианты :21–24 не защищены самим критерием.
ОБХОД: в первом присваивании удалить `edit`, НЕ добавляя `hub`; во втором, внутри
`if [ "$FABLE" = "1" ]`, добавить `edit,hub`. Реальные списки:

```text
TOOLS="read,write,bash,grep,glob,lsp,web_search,task,todo"
    TOOLS="read,edit,grep,glob,bash,web_search,task,todo,hub"
```

`bash fixtures/workshop_inventory/red_inventar_hub.sh` возвращает 0. Однако ведущая
сессия осталась без `hub`, а FABLE получил запрещённый `hub`. Встроенные негативные
мини-workshop'и при этом по-прежнему отвергаются. Обход исполнен, не предположен.
При закрытии нужна различимость именно двух режимов: признак, свободно меняемый внутри
проверяемого списка, нельзя принимать за источник идентичности режима.

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:120 — границы carve-out проверяются
только через write. Положительная tee-проба не доказывает отрицательные границы второго
зеркала, хотя :64–72 требуют точного пути и одинакового решения обоих потребителей.
ОБХОД: в `judgeEditWrite` добавить точное `path === 'xd://report_issue'`, а в
`judgeBash` — широкое `op.startsWith('xd://')`. Полная проба 2/3 возвращает 0,
но `tee xd://report_issue/x` и `tee xd://other` у непинна получают `pass`.
Те же пути в write-форме получают `block`. Исполнено на реальном CLI стража:

```text
bash fixtures/qa_kanal_xd/red_kanal_qa_report_issue.sh   rc=0
write xd://report_issue/x                           decision=block
write xd://other                                    decision=block
bash "tee xd://report_issue/x"                      decision=pass
bash "tee xd://other"                               decision=pass
```

Это не требование одинакового исходного текста: допустима унификация предикатом,
но отрицательные границы должны отличать её от широкого второго зеркала.
Неработающая правка одного `isUnpinnedInternalURI` не выдаётся за решение.

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:125 — одна проба возраста
«только что / −3 дня» и один невалидный аргумент `0` не доказывают объявленные :83–89
24 часа, применение положительного `N` и сохранение при недоступном reflog.
ОБХОД: жнец сохраняет свежие пустые по фиксированным 48 часам, принимает положительное
`--wip-grace-hours N`, но игнорирует его, отсутствие reflog трактует как разрешение
сноса; `0` отдельно отвергает с rc 1 и словом «положительное». В экспериментальном
варианте пустота различалась по одному событию reflog, ветка с работой — по нескольким;
все остальные операции исходного жнеца сохранены. Полная проба 3/3 возвращает 0:
свежая пустая ref+worktree живы, трёхдневная пустая и молодая слитая удалены, хвост
валидации `0` проходит. Отдельный живой вход обнаруживает несделанный предмет:

```text
bash scripts/gc_agent_branches.sh --root <игрушка> --wip-grace-hours 1   rc=0
refs/heads/wip/030/aged: моложе грации
Deleted branch wip/030/no-log (...).
git show-ref --verify refs/heads/wip/030/aged                         rc=0
git show-ref --verify --quiet refs/heads/wip/030/no-log                rc=1
```

У `aged` mtime reflog установлен на −30 часов: ветка должна быть удалена при явно
заданных 1 часе, но живёт. У `no-log` reflog удалён: ветка должна выжить, но снесена.
Фиксированные 48 часов также не равны обещанному дефолту 24; текущая проба их не
различает. Нужны наблюдаемые различители этих объявленных правил, а не батарея ради
количества строк.

БЛОКИРУЕТ contracts/030-inventar-i-gonki-spavna.md:129 — обещанный stderr не проверен:
проба сливает stdout и stderr в один файл, затем ищет имя ветки и «граци» независимо.
ОБХОД: жнец печатает сообщение `<ref>: моложе грации` ТОЛЬКО в stdout. Проба 3/3
возвращает 0: она принимает совпадение в объединённом выводе, хотя stderr не объясняет
выживание, требуемое :81–82. Это отдельно исполнено заменой единственного направления
`>&2` у сообщения в уже проходящем экспериментальном жнеце; результат снова rc 0.
Свобода формулировки и выбора секции stderr не является дырой; потеря самого канала
и связи «эта ветка — эта причина» является. Закрытие не требует пиннить полную фразу.

## Совет о решении, оставленном исполнителю

СОВЕТ contracts/030-inventar-i-gonki-spavna.md:79 — формула пустоты противоречит
объяснению «ноль коммитов сверх точки ветвления». Для ЛЮБОГО tip, достижимого из HEAD,
`merge-base(tip, HEAD) == tip`, включая слитую ветку с работой. Поэтому формула
классифицирует молодую слитую из :90 и :127 как пустую и требует сохранить её по :81,
а приёмка требует удалить. В отдельной игрушке выполнены base → рабочий коммит на
ветке → `merge --no-ff`; обе команды вернули один OID:

```text
git rev-parse wip/030/work             31ffb1036abb98e65b32b3879a3dd81615012aae
git merge-base wip/030/work HEAD       31ffb1036abb98e65b32b3879a3dd81615012aae
```

Это измеренная несогласованность определения, а не предложенная реализация. До
раздачи полезно выбрать источник сведений о работе на ветке и согласовать с ним
инвариант. Помечено советом: самостоятельного дополнительного обхода, отличного от
блокирующих выше, этой находкой не заявляю; противоречие инженерного текста само по
себе не подменяет установленный порог блокирования.

## Что проверено и не блокирует

- `contracts/030-inventar-i-gonki-spavna.md:53–57`: финальный маркер «утверждена
  ВЛАДЕЛЬЦЕМ» стоит, `7d44313` и путь tree-свидетельства названы. Прочитан owner-коммит
  b31d0b5 и blob свидетельства. Отдельная проверка извлекла полную цитату
  `«РАЗРЕШИЛ-ВЛАДЕЛЕЦ: null-allowlist … QA-канал, Н-106/030»` из 86df164 и b31d0b5:
  байты равны; rc 0. Свидетельство кандидата не принимается за постоянную батарею
  будущей реализации и не закрывает обход второго зеркала.
- `contracts/030-inventar-i-gonki-spavna.md:97–104`: исполнители названы для
  контракта, проб, workshop, обоих затронутых механизмов, допустимой проводки и
  вердиктов. Префиксы разных авторов не пересекаются; грамматика путей проверена
  отдельным скриптом, rc 0. `РАБОТА НЕ РАЗДАЁТСЯ` здесь не нужна: работа раздаётся,
  и объявлено другое допустимое плечо. Роли вне зоны явно исключены. Механизм
  будущего удержания границ — `check_zones.sh`, читающий замороженный blob и
  `user.name`; прогон этого механизма не выдаю за проверку ещё не замороженных зон.
- `contracts/030-inventar-i-gonki-spavna.md:177–182`: свобода точного текста сообщения,
  выбора секции stderr и способа объединить предикат допустима. Требовать конкретную
  точку исходника вместо наблюдаемой семантики не нужно. Обход stderr назван выше.
- Сверка с AGENTS.md выполнена в пределах предмета/приёмки/раздачи. Отдельного
  нарушения нормы сверх показанной слабости rc-критерия не заявляю. Привязки
  экспериментальных дефектов рассмотрены по коду проб, не навязаны прозе контракта
  как требование расписывать стабы (Н-39).

## Протокол исполнения

Изолированный клон: `/tmp/dev-harness-verify/critic-030/repo`, создан из указанного
worktree; `git checkout wip/030/architect` → 0. Чтение предмета — из worktree после
проверки наличия blob на HEAD и отсутствия его отличий от HEAD.

1. На неизменённом клоне:
   `bash fixtures/gc_agent_branches/red_gonka_svezhij_spavn_vyzhivaet.sh` → 1.
   Именованный отказ: «свежая пустая wip/031/architect снесена gc при нуле коммитов —
   гонка спавн/close-out (Н-107)». Жнец действительно удалил три игрушечные ветки.
2. `bash /tmp/dev-harness-verify/scratch/030/stend.sh` → 0 у оболочки;
   вложенные пробы → 1, 1, 1. Отказы: «hub отсутствует»; точный QA-путь в write
   получил block вместо pass; свежая пустая ветка снесена. Нулевой rc обёртки не
   выдан за зелёную приёмку. Стенд сам создаёт временную раскладку и только читает
   исходные файлы worktree; вызван из клона.
3. `bash /tmp/dev-harness-verify/scratch/030/stend2.sh` → 0:
   ДО `block/block/block`, ПОСЛЕ `pass/block/block`, две вставки, хвостовая проба → 0.
   SHA-256 blob стража b31d0b5 независимо вычислен:
   `976c07e8432f76f0fcbf4b2b06c0ed43c9aaf9ed0e51b387dfe4d60ef6fde49c` — совпал с пином
   одноразового стенда. Пин относится к стражу 98f0ab2, не к текущему tip целиком.
4. `python3 /tmp/dev-harness-verify/critic-030/prove.py` → 0.
   В клоне одновременно построены описанные неверные варианты трёх частей;
   ВСЕ ТРИ команды приёмки вернули 0. Независимыми входами затем получены пропуск
   суффикса/иного xd в bash, выживание 30-часовой ветки при N=1, удаление ветки
   без reflog и тождество merge-base для слитой ветки с работой. Скратч-пробы скопированы
   без изменения их проверок.
5. `python3 /tmp/dev-harness-verify/critic-030/final_checks.py` → 0:
   байты разрешения/маркер/свидетель, грамматика и непересечение зон; затем
   stdout-only сообщение грации снова пропущено полной пробой 3/3 с rc 0.

Никаких format/lint/build/full-suite прогонов не было. Экспериментальные варианты
механизмов не предназначены для слияния. Вердикт сохраняет состояния обходов и
наблюдаемые результаты независимо от срока жизни скратч-скриптов. Клон удаляется
только после подтверждения коммита вердикта в репозитории ветки предмета.

Итог: FAIL по четырём исполненным обходам. Разрешение владельца и краснота сегодняшнего
HEAD подтверждены, но приёмочный критерий пропустил дерево, в котором не сделаны все
три части предмета. Исполнитель не должен получать такой критерий замороженным.
