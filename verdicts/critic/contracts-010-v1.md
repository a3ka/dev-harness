FAIL

БЛОКИРУЕТ contracts/010-topologija-orkestrator-arhitektor.md:82 — исполняемый критерий 1
не доказывает тело новой роли. Файл из одной строки `role:` действительно даёт этой команде
код 0. `check:gen` отсекает именно такой файл как синтаксически негодный, но не закрывает
смысловой обход: парсер принимает пустое тело роли после корректного фронтматтера.
ОБХОД: `roles/orchestrator.md` с корректным фронтматтером `role: orchestrator`, любым непустым
`tools` (например, `[read]`) и пустым телом, записью `orchestrator: default` в таблице модели и
порождённым зеркалом удовлетворяет пунктам 1–2, хотя стартап-протокола, обязанностей и списка
«чего НЕ делает сам» из строк 23–29 и 56–57 нет.

БЛОКИРУЕТ contracts/010-topologija-orkestrator-arhitektor.md:58 — изменение содержания
`roles/architect.md` не доказано ни одной командой: `check:gen` сверяет лишь точность зеркала
с текущим источником, а не требуемые признаки субагента, frontier/ред-тестов и стартап-протокола.
ОБХОД: оставить нынешний `roles/architect.md` без изменений и породить из него такое же зеркало;
`npm run check:gen` возвращает 0, хотя пункт предмета 2 не сделан.

БЛОКИРУЕТ contracts/010-topologija-orkestrator-arhitektor.md:85 — `check:charter` доказывает
наличие разрешения у уже совершённых уставных изменений, но не требует, чтобы `AGENTS.md` вообще
был изменён этим майлстоуном и содержал новую топологию.
ОБХОД: оставить текущий `AGENTS.md` со строкой «сессию ведёт architect»; на HEAD 2266a80
`npm run check:charter` возвращает 0, поэтому пункт исполняемой приёмки выполнен, а пункт
предмета 6 — нет.

БЛОКИРУЕТ contracts/010-topologija-orkestrator-arhitektor.md:17 — решение об инвентаре одновременно
задано точным списком в §Предмет и снова оставлено исполнителю в строках 94–95. Исполнитель может
выбрать вторую формулировку, а приёмка проверяет лишь непустой `tools` через парсер генератора.
ОБХОД: объявить у orchestrator только `tools: [read]`; пункты 1–2 исполняемой приёмки зелёные,
но роль не может вызвать субагента, вести HANDOFF или коммитить, то есть объявленные обязанности
неисполнимы.

БЛОКИРУЕТ contracts/010-topologija-orkestrator-arhitektor.md:72 — режим «работа не раздаётся»
проходит грамматику, но не держит заявленную границу одного автора: точного `git user.name` и
перечня разрешённых путей нет, а `check_zones.sh` контракты без `ЗОНА`-строк по автору и путям
не судит. Кроме того, для прохождения `check:gen` потребуется правка таблицы в
`scripts/roles.ts`, которой нет в перечислении самостоятельной декларативной правки строк 74–76.
ОБХОД: коммит под любым именем может менять `scripts/roles.ts` и любые посторонние пути;
`РАБОТА НЕ РАЗДАЁТСЯ:` остаётся валидной, `check_zones.sh` эти коммиты не проверяет, а названные
команды приёмки ограничения области не содержат.

БЛОКИРУЕТ contracts/010-topologija-orkestrator-arhitektor.md:20 — контракт прямо говорит, что
правило «оркестратор не пишет сам» до предмета 011 механизма не имеет, но не требует пометить
временное правило `cognitive-only`. Это противоречит старшей норме `AGENTS.md:195-196`:
правило без механизма допустимо только с явной такой пометкой и записанным остаточным риском.
Отсутствие красных фикстур у некодового 010 здесь не основание находки; противоречие создаёт
именно немаркированное правило, принуждение которого сознательно отложено до 011.

СОВЕТ contracts/010-topologija-orkestrator-arhitektor.md:90 — выбор «единый источник либо
дословный дубль» назван явно, и оба варианта способны выполнить один наблюдаемый стартап-протокол;
сам по себе этот выбор заморозку не блокирует.

СОВЕТ contracts/010-topologija-orkestrator-arhitektor.md:50 — смена решения 2026-08-16 с
`architect` на `orchestrator` сама по себе не конфликтует с уставом: это законная эволюция
решением владельца, если изменяющий `AGENTS.md` коммит действительно несёт требуемую строку
`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`. Дефект выше — в том, что приёмка не доказывает саму правку, а не в запрете
такой правки.

Fallback явно вынесен в 011; красных фикстур для декларативного 010 не требую. Признание обхода
через `bash` честно записывает остаток и потому 010 не бессмысленен как декларативный шаг, но до
011 разделение остаётся только правилом; старшая норма требует на этот промежуток явной маркировки
`cognitive-only`.
