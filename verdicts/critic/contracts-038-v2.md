FAIL

Предмет: contracts/038-provodka-done-gejt.md v2 на HEAD e1c36e1d170d6a62057f4356dad31f83e5380b44, круг 2 до заморозки. Судится контракт и предъявленная приёмка, не будущая реализация. Репро — отдельный клон /tmp/dev-harness-verify/critic-038-k2; основной репозиторий не использовался для репро-правок.

К1 закрыт в части предъявления: команды исполнимы, закоммиченная freeze-проба действительно красна; scratch-пачка предъявлена и прогнана в оговорённом 034-порядке. Повторного отказа «набор артефактов неполон» НЕТ. Нынешний FAIL — по существу впервые. Отсутствие ещё не написанных барьеров само по себе находкой не считается.

## Блокирующие находки

### Б1. Зона потребителя не доказывает его верификацию

БЛОКИРУЕТ contracts/038-provodka-done-gejt.md:187 — п3 заменяет требование владельца «верифицирует ВСЕХ потребителей» (:159) альтернативой «потребитель перечислен в ЗОНА». Это назначение исполнителя, а не исполнение проверки. Само-применение дрилла (:235–237) прямо использует эту альтернативу.
ОБХОД: зарегистрированный writer меняет формат; consumer остаётся несовместимым, но его путь записан в ЗОНА implementer. ПОТРЕБИТЕЛЬ-пробы нет. По п3 consumer покрыт, гейт возвращает 0, хотя consumer ни разу не проверен. C1 испытывает отсутствие ОБОИХ объявлений, C2 — красную пробу, CG1 содержит одновременно зону и зелёную пробу; состояние «зона есть, проверки нет» ни один из них не отличает от верификации.

Доказательство: :187–191 задают ИЛИ; :295 допускает CG1 с обеими альтернативами сразу. Предъявленный check_consumers/green_G1_zona_plus_zelenaja_proba.sh:12–21 действительно несёт ЗОНА и `bash probe_green.sh`, причём тело probe_green.sh — `exit 0`. Это логический обход объявленного критерия, не утверждение о поведении отсутствующего check_consumers.sh. Прогон предъявленной C-семьи: 6 × rc 1 отсутствием предмета, независимо пересчитан; полный протокол ниже.

### Б2. Окно пропускает обычную пост-фриз правку писателя

БЛОКИРУЕТ contracts/038-provodka-done-gejt.md:184 — п2 смотрит только уже совершённые коммиты mint/frozen..HEAD, а обязательная проводка п2–п4 находится в freeze (:201–204). По объявленному циклу :10 и :327 реализация писателя появляется ПОСЛЕ заморозки. done_contract (:119–153) гейт потребителей не вызывает. Требование :205 «гейт обязан найти потребителей freeze» этой последовательностью не обеспечено.
ОБХОД: mint → коммит контракта с будущей правкой freeze → freeze с пустым writer-окном и rc 0 → разрешённая пост-фриз реализация меняет freeze и ломает зарегистрированного consumer → done без повторного freeze. Проверки п2 в предусмотренной точке выполнены и зелёные, но изменение writer не попало в проверяемое окно на момент вызова. Это не residual неполного маппинга: consumer зарегистрирован.

Проверка на судимом HEAD: `git log --format=%H 7cc4c720892801638298190834c4e8b4ee486337..HEAD -- scripts/freeze_contract.sh scripts/lib_registry.sh scripts/done_contract.sh` — rc 0, 0 коммитов. SHA взят из registry/contracts.tsv:40. Сам контракт обещает правку freeze в ЗОНА implementer (:213). CG2 (:296) делает пустое окно зелёным; проверки перехода «заморозка → реализация writer → завершение» в предъявленной пачке нет. Нужна согласованная точка обязательной верификации изменённого writer; её нельзя молча оставлять исполнителю.

### Б3. Census-грамматика считает другую сущность

БЛОКИРУЕТ contracts/038-provodka-done-gejt.md:182 — `wc -l < scripts/consumers.tsv = N census scripts/consumers.tsv` несовместимо с названным механизмом 036-В2. Команда считает строки/пары, census раскрывает файловый glob и получает один файл. При обязательном минимуме трёх разных writers (:172–173) честный N не меньше трёх, и до check_consumers заморозка не дойдёт. Это конфликт AGENTS.md:216–217: две меры должны проверять одно счётное утверждение, не разные единицы.
ОБХОД: оставить в consumers.tsv только одну пару, заявить N=1 — обе проверки случайно дают 1, предъявленные C-пробы (их _toy.sh использует ровно одну пару) этого не отличают от готовности, но обязательный минимум трёх writers не выполнен. Честное расширение до минимального состава, наоборот, ломает freeze. Требование «состав — часть приёмки» (:176) не предъявлено отдельной командой.

Живое доказательство: в клоне созданы временный scripts/consumers.tsv из трёх строк и contracts/998-critic-census.md со строкой:

    замер: `wc -l < scripts/consumers.tsv` = 3 census scripts/consumers.tsv

`bash scripts/check_spec_ready.sh /tmp/dev-harness-verify/critic-038-k2 contracts/998-critic-census.md` — rc 1:

    спек-гейт 036: замер расходится: заявлено 3, дерево даёт 1

Использован действующий неизменённый механизм. Временные файлы удалены. Репро: /tmp/dev-harness-verify/scratch/compat-038-k2.py, собственный rc 0 означает подтверждение ожидаемых результатов, не зелёную приёмку.

### Б4. Положительный контроль г5 требует принять отсутствующий заголовок

БЛОКИРУЕТ contracts/038-provodka-done-gejt.md:268 — предъявленный R/G1 не является «полным честным входом», а перенос байт-в-байт (:327–331) закрепляет ошибку. check_provodka/green_G1_polnyj_vhod.sh:15 требует `§Воркфлою майлстоуна`, check_provodka/_toy.sh:33 создаёт `## Воркфлоу майлстоуна`. Согласно :84–87 и :108 честный г5 обязан отказать, не дать rc 0.
ОБХОД: барьер допускает несуществующий полный заголовок `Воркфлою майлстоуна` (например, исправляет эту опечатку при поиске). G1 зеленеет, R9 с коротким `§Воркфлою` по-прежнему красен: предъявленная пара не защищает побайтовое равенство. Предмет exact charter-pinning не выполнен. Без такого ослабления положительный контроль недостижим.

Независимое извлечение строк из обоих файлов и сравнение bytes, `python /tmp/dev-harness-verify/scratch/compat-038-k2.py` — rc 0; внутри сравнение заголовка rc 1:

    R/G1 requested heading=Воркфлою майлстоуна
    R/G1 created heading=Воркфлоу майлстоуна
    Exact heading comparison rc=1

Это дефект критерия, не предложение подгонять будущий барьер. Исправление только строки REPO, разрешённое :329–330, его не устраняет.

### Б5. Direct-прогоны не образуют объявленные scoped-ключи CI

БЛОКИРУЕТ contracts/038-provodka-done-gejt.md:331 — :352–354 требуют три scoped-ключа по существующему прецеденту, но предъявленная пачка имеет 0 case_*.sh во всех трёх семьях. Действующий раннер требует case_*.sh (scripts/verify_antiplacebo.sh:573–576); direct red_*/green_* им не исполняются. Команды раздела приёмки не проверяют объявленную scoped-проводку. Это конфликт AGENTS.md:220–221 (гейт не зеленее CI).
ОБХОД: реализовать поведение, проверяемое прямыми R/D/C-прогонами, перенести все 29 файлов точно как предписано, добавить три ключа в CI. Прямые команды могут удовлетворить :332–334, но ни одно предъявление нового барьера не выполняется scoped-раннером, CI красен отсутствием case-файлов. Критерий прямых прогонов не отличает непроводку от готовой проводки. Само переименование не названо решением: прямые пробы не используют протокол $BARRIER существующего раннера.

Живое доказательство: в одноразовом клоне временно размещены три семьи без изменения имён и три минимальные декларации барьеров с канонической шапкой кодов. Тела не исполнялись: маркер исполнения отсутствует; проверяется только discovery существующего раннера, не будущая реализация. `bash scripts/verify_antiplacebo.sh --scope check_provodka done_contract check_consumers` — rc 1:

    FAIL check_consumers.sh: нет фикстур — ожидается fixtures/check_consumers/case_*.sh
    FAIL check_provodka.sh: нет фикстур — ожидается fixtures/check_provodka/case_*.sh
    FAIL done_contract.sh: нет фикстур — ожидается fixtures/done_contract/case_*.sh
    барьеров: 3 · фикстур: 0 · предъявлено красным повторным прогоном: 0

Репро: /tmp/dev-harness-verify/scratch/audit-038-k2.py, rc самого репро 0. Добавленные файлы удалены. scripts/verify_antiplacebo.sh не правился и в ЗОНА контракта не раздаётся. Нужен совместимый способ предъявления/проводки с названным исполнителем, не неявный выбор между нарушением byte-for-byte и выходом за зону.

## Советы, не условия снятия FAIL

СОВЕТ contracts/038-provodka-done-gejt.md:133 — D4 не предъявляет преобразование rc 2 барьера в rc 1 писателя: red_D4_provodka_krasna.sh:24–33 вызывает барьер на призраке отдельно, писатель отказывает раньше на незакоммиченности. Шапка пробы называет ветвь residual, контракт такого остатка не называет. Не считать D4 доказательством этой ветви. Doc-preflight :153 также не имеет отдельного D-входа. Здесь не объявляется наблюдённый дефект реализации.

СОВЕТ contracts/038-provodka-done-gejt.md:192 — прояснить допустимое состояние реестра у положительных C-контролей: make_crepo создаёт frozen-тег, но не registry/contracts.tsv, тогда как п4 требует отказ при нечитаемом реестре. Уточнить, считается ли это допустимым локальным состоянием или каркас должен создавать валидный реестр; не ослаблять fail-closed молча.

СОВЕТ contracts/038-provodka-done-gejt.md:99 — задать приоритет диагностики при нескольких нарушениях: R3/R4 и D4a одновременно guard-only без обоснования и ghost/orphan, но требуют г1/г2, не г0-доп. Порядок этих отказов не задан. Это оставленное исполнителю решение; самостоятельного блокирования за порядок нет.

## Протокол команд приёмки

Все команды §«Исполнимые команды приёмки» исполнены. Cwd относительных путей — клон судимого HEAD. В первом буквальном запуске scratch использовал штатный REPO=/home/aka/Documents/dev-harness: барьеры отсутствовали, ничего там не изменялось. Затем каждый из 29 файлов независимо запущен с HARNESS_ROOT=/tmp/dev-harness-verify/critic-038-k2: те же 29 × rc 1, у каждого причина «ПРЕДМЕТ 038 НЕ РЕАЛИЗОВАН». Это отличает красный от иной ошибки с тем же rc.

| Команда | Наблюдение | rc |
|---|---|---:|
| `bash fixtures/freeze_contract/red_consumers_gejt_038.sh` | Предмет 038 отсутствует, ожидаемый до реализации красный | 1 |
| `bash fixtures/freeze_contract/red_spec_preflight_036.sh` | г5, г5б, г5в, г5г исполнены, существующий зелёный контроль жив | 0 |
| `bash /tmp/dev-harness-verify/scratch/n116-red/_krasnye_038.sh` | 29 файлов, все rc 1; расхождений 0, EXPECT_RC=1 | 0 |
| `bash /tmp/dev-harness-verify/scratch/n116-red/check_provodka/red_R1_pole_net.sh` | Предмет 038 отсутствует | 1 |
| `EXPECT_RC=0 bash /tmp/dev-harness-verify/scratch/n116-red/_krasnye_038.sh` с HARNESS_ROOT клона | 29 файлов, все rc 1; расхождений 29 — ожидаемо до реализации, НЕ зелёная приёмка | 1 |
| `bash fixtures/check_provodka/red_R1_pole_net.sh` | Пост-фриз файл не приземлён: No such file or directory; это не предъявление барьера | 127 |
| `VERIFY_ANTIPLACEBO_SCRATCH=/tmp/dev-harness-verify/scratch/critic-038-ap bash scripts/verify_antiplacebo.sh --scope freeze_contract` | 15 case: зелёные контроли и повторные красные предъявления | 0 |
| `npm run check:contract-frozen` | 038 — черновик, frozen-блобы совпадают; 40 документов / 4 черновика / 36 frozen — вывод команды, не независимая перепись | 0 |
| `git diff --exit-code HEAD -- scripts .github/workflows .githooks contracts plans` после репро | Пустой diff: существующие frozen-барьеры и спеки не менялись | 0 |

Независимая перепись каталогов через Python Path.iterdir, не повтор итоговой строки раннера: R = 10 red + 2 green; D = 8 red + 3 green; C = 4 red + 2 green; сумма 29. Отдельный разбор таблиц дал 12, 11, 6 строк входов. В каждой семье case_*.sh = 0. Собственный subprocess-прогон дал {1: 29}. Для существующего freeze-набора отдельный glob дал 15 case-файлов. Полный прогон проекта не запускался.

## Решения владельца, само-применение и границы

- В1 перенесено в :20–22, :88–95 и role-строки само-применения без подмены role на charter. Семантический выбор природы нормы оставлен ревьюеру явно, как решено владельцем; механического доказательства смысла текста не требую.
- В2 назван в :380–382; fence-aware не требую. В3 сохранён: фраза о кругах осталась дословной, дубль не снимается.
- Решение о ВСЕХ потребителях перенесено как обещание, но не выполнено объявленным механизмом: Б1/Б2. Residual неполноты census не оправдывает пропуск зарегистрированного consumer.
- Charter-якорь само-применения и строка устав-дельты совпали побайтово. Независимая мера: 584 байта без LF; с LF — 585, md5 e1108d2f8c358359ea7dd7558214191a у обоих. Устав НЕ правился, санкции от имени владельца не создавались.
- Исполнители и зоны названы. Исключённые registry/lib/хуки и owner-канал AGENTS.md названы явно; роли здесь не правились. Для устранения несовместимости CI требуется решение внутри объявленных границ (Б5), не неявная раздача замороженного раннера. Точная пара 027-потребителя сознательно оставлена census при реализации (:175); само по себе это не блокирует.
- Пять вопросов роли рассмотрены: сила критерия — Б1/Б2, доказуемость командами — Б3–Б5, оставленные решения — советы, границы — ЗОНА и Б5, старшая норма — Б3/Б5. Красное к1 не засчитывается за достижимость зелёного к2.

Пин scratch-доказательств (sha256; не утверждение об их закоммиченности):

    _krasnye_038.sh                           aa0b3e1f69930f9a3963bb6102ed20a486951e51590291cee9f11d38a0554bfe
    check_provodka/green_G1_polnyj_vhod.sh     1082aa97ede8aeacde025b5c2cdecc0fdd73778f5d20f985d9c94cb4cdc785ca
    check_provodka/_toy.sh                    367f68b8d25bfd1d9a37d823b8a977327970a087bb43cb42e99d8d7e71515531
    check_consumers/_toy.sh                   d8dab3d7fe154bec4fa1ea300e88e4c9b025cbbf50e0fc94af943821f84b8b27

Итого: 5 блокирующих находок, 3 совета. Повторной причины к1 нет. Если потребуется третий формальный круг, маршрут AGENTS.md:137 — арбитр; этот вердикт не выдаёт разрешения на дополнительный круг и не пишет санкцию владельца.
