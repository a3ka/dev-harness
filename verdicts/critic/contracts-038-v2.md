accept

# Контракт 038 — вердикт для freeze-цикла v2 (переподача v8 под номер freeze-механизма)

Это НЕ новый круг суда и НЕ новое суждение. Полное судебное содержание, все живые
проверки, пять вопросов роли и полный evidence trail — в `verdicts/critic/contracts-038-v8.md`
(closed accept, автор critic, коммит `b962a13` на HEAD). Настоящий файл — техническое
переподание ТОГО ЖЕ уже вынесенного вердикта под именем, которое ожидает
`scripts/freeze_contract.sh` для СЛЕДУЮЩЕЙ по счёту заморозки (`frozen/contracts/038/2`):
номер `v` там считается по числу СУЩЕСТВУЮЩИХ freeze-тегов (`vmax+1`), а не по внутренней
changelog-нумерации контракта («v6/v7/v8» в прозе §Changelog — это версии ТЕКСТА контракта
между заморозками, независимый счётчик). У 038 один freeze-тег (`frozen/contracts/038/1`),
поэтому следующий freeze — `v2`, и барьер ищет вердикт ровно по этому имени.

Судимый предмет остаётся тем же, что и в v8: исполнение П1–П3 решения арбитра `78785ec`
(`verdicts/arbitration/contracts-038-head-granica-dokumentacija.md`) в
`contracts/038-provodka-done-gejt.md`, подписанное владельцем (`РАЗРЕШИЛ-ВЛАДЕЛЕЦ:`,
коммит `4e0db83`).
