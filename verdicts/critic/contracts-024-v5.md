accept

# Контракт 024, v4 → v5: путь вердиктов ревьюера в Ветви B (г)

Судимый HEAD main: `34c8dd936c538ea5724b040dd29ab8e3029016c0`.
Источник — свежий клон `https://github.com/a3ka/dev-harness.git` в
`/tmp/dev-harness-verify/critic024v5/repo`; SHA получен командой `git rev-parse HEAD`.
База сравнения — `frozen/contracts/024/4`. Коммит респека —
`7c6cb1b2be73e3d76fc7345143591b5c3250de8c`, автор architect.
Локальный чекаут оркестратора не читался и не изменялся.
Предмет суда — ТОЛЬКО однострочная дельта текста контракта, не реализация,
не повторное ревью всей спецификации и не готовность 024 к done.

Прочитаны контракт и `AGENTS.md`, предыдущий вердикт
`verdicts/critic/contracts-024-v4.md`, раздел Б2
`verdicts/review/contracts-024-v1.md:94-136`, объявление пути
`roles/reviewer.md:1-5`, текущий `red_stenogrammy_sudej_024.sh`.
Сверены закоммиченные пути вердиктов и сообщение коммита респека.
Набор артефактов полон: закоммиченный предмет —
`contracts/024-detektor-utechek-osnovnogo-chekauta.md:41-105`, инварианты с rc —
`:123-169`, критерии — `:283-318`, исполнители architect/implementer,
зоны и явные исключения — `:266-268`.

## Три проверки респека

1. **Дельта ровно заявленная.**
   `git diff frozen/contracts/024/4 HEAD -- contracts/024-detektor-utechek-osnovnogo-chekauta.md`
   → rc 0: одна удалённая и одна добавленная строка. Фактический номер строки —
   **105**. В перечне `verdicts/critic|adversary|reviewer|arbitration/` заменено
   только `reviewer` на `review`. Независимая байтовая проверка через `git show`
   установила: замена единственного такого фрагмента в замороженном v4 даёт
   весь HEAD-файл побайтово. Других изменений файла, включая переносы, нет.
2. **Основание соответствует исправлению.** Блокер в исходном вердикте назван
   **Б2**, не «Бс2»: `verdicts/review/contracts-024-v1.md:94-136` требует
   устранить слепоту к `verdicts/review/` в фикстуре и строке Ветви B.
   `roles/reviewer.md:4` действительно объявляет `verdict: verdicts/review/`.
   Закоммиченные файлы под этим путём существуют, под `verdicts/reviewer/` — нет.
   Исправляется каталог артефактов; роль по-прежнему называется reviewer.
3. **Ослабления или расширения предмета нет.** В `:105` сохранены обязанность
   каждого вердикта нести стенограмму, граница после приземления нормы,
   rc-команда, именованный отказ и остаточные риски. Убрана опечатка, исключавшая
   реально существующий каталог из заявленной области. Текст теперь совпадает
   с путём в исполняемом перечислении
   `fixtures/check_judge_gate/red_stenogrammy_sudej_024.sh:118`.
   Это согласование текста с уже исправленным якорем, а не утверждение, что
   изменение Markdown само меняет поведение shell-скрипта.

## Пять обязательных вопросов

1. **Критерий готовности слабее предмета?**
   `contracts/024-detektor-utechek-osnovnogo-chekauta.md:105,316`: нет нового
   ослабления. После замены критерий явно охватывает реальный каталог ревьюера;
   обязательность стенограммы не снята. Конкретного нового обхода, внесённого
   этой дельтой, не найдено. Повторный суд всех прежних критериев не проводился.
2. **«Готово» недоказуемо названной командой?** `:105,316` сохраняют
   `bash fixtures/check_judge_gate/red_stenogrammy_sudej_024.sh` с rc 0/1.
   Нового немеханизируемого условия не добавлено. Сам респек доказан точным
   сравнением закоммиченных блобов; текущий запуск якоря завершился rc 0.
   Этот запуск не заменяет отрицательный контроль и не удостоверяет done.
3. **Решение молча оставлено исполнителю?** `:105`: нет. Исправленный путь
   задан однозначно и совпадает с декларацией роли. Ни переименование роли
   reviewer, ни создание нового каталога, ни изменение грамматики стенограммы
   не требуются. Н-39 сохранена: стабы к ветвям привязывает architect по коду,
   НЕ проза контракта; контракт несёт инварианты + rc-команды.
4. **Границы исполнения названы?** `:266-268` не менялись. Контракт и
   `fixtures/check_judge_gate/` названы за architect, детектор — за implementer;
   норма роли остаётся владелец-каналом. Механизм границ — прежний
   `scripts/check_zones.sh`. Новых файлов или зон эта текстовая дельта не требует.
5. **Противоречие AGENTS.md; что сломается при наивной реализации?**
   В `:105` нового противоречия норме нет: rc остаются результатом, область
   названа, механизм указан. Коммит респека несёт строку
   `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` для данного контрактного пути; подлинность воли сверх
   этой записи здесь не удостоверяется. Наивное переименование самой роли
   reviewer в review или перенос вердиктов обратно в reviewer не следует
   из дельты и противоречило бы `roles/reviewer.md:4`. Изменение только текста
   при старом списке путей в фикстуре не закрывало бы Б2 реализации; на судимом
   HEAD исполняемый список уже содержит `verdicts/review`. Полнота исправления
   реализации остаётся предметом её судей, не этого accept.

## Собственные прогоны и границы доказательства

Все команды выполнены в свежем клоне:

- `python3 /tmp/dev-harness-verify/critic024v5/verify_v5.py` → rc 0.
  Проба читала закоммиченные блобы через git, проверяла исходный HEAD,
  точную единственную замену и объявление/существование пути. Сырой вывод:

  ```text
  Судимый HEAD: 34c8dd936c538ea5724b040dd29ab8e3029016c0
  Байтовая сверка: HEAD = frozen/contracts/024/4 с единственной заменой reviewer на review в перечне путей
  Изменённая физическая строка: 105; число строк и остальные байты неизменны
  roles/reviewer.md объявляет verdicts/review/; в HEAD этот каталог есть, verdicts/reviewer/ отсутствует
  ```

- `bash fixtures/check_judge_gate/red_stenogrammy_sudej_024.sh` → rc 0.
  Сырой вывод:

  ```text
  стенограммы судей 024: все вердикты после границы 17583d000ba58eeaa4e74741693e36e28e509c72 (самое раннее внесение норма-строки) несут строку вызова детектора с rc
  ```

  Это ветка после активации нормы, не сообщение о неприземлённой норме.
  Собственная инъекция вердикта без стенограммы не выполнялась; данный прогон
  не предъявляется как доказательство красного или полного закрытия Б2.

- `git diff --exit-code frozen/contracts/024/4 HEAD -- scripts/check_charter.sh scripts/check_zones.sh scripts/check_judge_gate.sh scripts/judge_gate.sh scripts/freeze_contract.sh scripts/check_contract_frozen.sh`
  → rc 0, пустой вывод: перечисленные замороженные барьеры неизменны.
- После собственного `--snapshot` того же изолированного клона → rc 0:
  `bash scripts/check_no_leak.sh --check /tmp/dev-harness-verify/critic024v5/repo` → rc 0, `основной чекаут чист`.
  Это сверка МОЕГО клона, не стенограмма предспавнового вызова оркестратора
  и не утверждение о чистоте его чекаута.

Полный CI, scoped-регресс детектора и проверка готовности к done не запускались:
предмет задания — однострочный респек, реализация не судится.

Блокирующих находок нет. Разрешена заморозка проверенного текста как v5.
Этот accept не отменяет остальные находки ревьюера и не объявляет контракт 024 done.
