FAIL

БЛОКИРУЕТ contracts/003-skills-metta-adaptacija.md:31-35,46-50,84-92,183-188 — новая мера проверяет только маркер в `result` события `tool_execution_end`, но не требует связать это событие по `toolCallId` с вызовом `read`, у которого `args.path` равен именно `skill://<имя>`, и не требует, чтобы `result.details.resolvedPath` указывал именно в `$PWD/.agents/skills/<имя>/SKILL.md`. Поэтому названная команда не отличает регистрацию проектного скила от уже измеренного в Н-34 чтения обычного файла из cwd и от разрешения одноимённого скила из `~/.agents/skills`; утверждение «чистый чекаут получает скилы без засева HOME» остаётся недоказуемым этой командой.
ОБХОД: оставить полные зеркальные деревья `skills/` и `.agents/skills/`, чтобы ветвь (д), `diff -r`, состав, upstream и все остальные статические меры были зелёными; `check_skills.sh --live` запускает настоящий запиненный `omp`, но модель вызывает `read` для обычного пути `.agents/skills/<имя>/SKILL.md` (либо `skill://<имя>` разрешается из заранее засеянного `~/.agents/skills`). В обоих случаях `tool_execution_end.result` содержит требуемую строку и команда возвращает 0, хотя проектная регистрация `skill://<имя>` не была предъявлена; при отключённой/сломавшейся регистрации или в чистой живой сессии без HOME-сидов проектные скилы остаются невидимы. Это тот же класс состояния, который уже измерен Н-34: инструмент вернул файл из cwd, а успех был приписан резолверу скилов.

АРБИТР: созван через `Arbiter003spaseno` с передачей созыва `Main`; предмет спора — достаточен ли маркер в `tool_execution_end.result` или контракт обязан требовать корреляцию `tool_execution_start.args.path = skill://<имя>` по `toolCallId` и проектный `resolvedPath`. Это второй `FAIL` по причине «оракул не доказывает `read(skill://…)`»: первый — `verdicts/critic/contracts-003-draft-r1.md:3-4`, текущий — эта находка против HEAD `3bd8390d91452690d4274ca6fb77b670aba970a2`.

Проверены все пять обязательных вопросов. Предмет и исполнители названы; новая `.agents/skills/` входит в `ЗОНА implementer`, а барьер и его фикстуры — в `ЗОНА architect`; остальные пункты готовности имеют команды с кодом возврата. Дополнительных блокирующих обходов и противоречий норме `AGENTS.md` не обнаружено.
