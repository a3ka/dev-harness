FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` на HEAD `7dbbd4ec0a43e9ccb4038cf4b4da5890c46306cd`, первый черновик до первой заморозки. Последний коммит предмета — `7dbbd4e`; тегов `frozen/contracts/005/*` нет, теги не создавались.

Набор артефактов полный. Предмет назван в `contracts/005-avtonomija-proksi.md:7-39`, критерий готовности и доказывающие команды — в `:41-60,71-77`, исполнители и границы — двумя `ЗОНА`-строками в `:62-69`. На судимом HEAD `bash scripts/check_zones.sh` вернул 0 по четырём ранее замороженным и уже закрытым диапазонам; 005 до заморозки в диапазон ещё не входит.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:19-23` — токены, которым доверено приписывание роли, помещены в два версионируемых носителя, но контракт не требует ни ссылок на секреты/хешей вместо открытых значений, ни изоляции токена одной роли от остальных, ни отзыва старого токена. Проверка только отличает известный токен от неизвестного; она не отличает владельца токена от читателя репозитория и не проверяет ротацию.
ОБХОД: закоммитить буквальные токены в `config/metering.json` и `.omp/models.yml`. Любой вызывающий читает токен `architect`, посылает его и получает честно реализованную строку журнала `role=architect`; неизвестный токен по-прежнему даёт 401, все объявленные ветви сходятся, но расход приписан не фактической роли. После замены файла прокси, загрузивший таблицу один раз при старте, может до перезапуска принимать и отозванный токен — критерий этого также не различает.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:29-34,51-53` — требование «модель без цены проходит, `usd: null`» несовместимо с объявленным денежным потолком и границей перерасхода «на один вызов»: для `null` нет суммы, которую можно списать ПОСЛЕ, а критерий не задаёт ни резерв до запроса, ни запрет следующего вызова, ни иной консервативный учёт.
ОБХОД: многократно вызывать модель без цены. Каждый вызов возвращает требуемые 200 и `usd: null`; свёртка пропускает `null`, бюджет остаётся ниже 50 USD и никогда не даёт 402. Ветвь (ж) и отдельный отказ проверки конфигурации ведут себя ровно по тексту, но прокси уже пропустил не один, а неограниченное число неучтённых платных вызовов, поэтому потолка расхода нет.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:24-28,46-52,54-55` — равенство текущей суммы журнала и `budget.json` не доказывает ни `append-only`, ни заявленное направление производности «бюджет пересобирается из журнала». Нет различающего исхода с сохранением ранее записанного маркера и нет прогона реальной команды свёртки после удаления/искажения производного файла.
ОБХОД: перед каждой записью обнулять `calls.jsonl`, оставлять только последний вызов и записывать в `budget.json` стоимость той же последней строки. Ветви (в) и (д) каждая видят требуемую строку, ветвь (е) видит точное равенство суммы одной строки бюджету, selftest функции свёртки проходит, но журнал не append-only и история расхода потеряна.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:43-58` — семь ветвей перечислены, но контракт не требует для (а)–(ж) семи самостоятельных красных вызовов. `npm run check:antiplacebo` требует красное от барьера в целом и не заменяет свойство Н-37 «одна ветвь — один красный вызов»; зонтичная поломка может замаскировать шесть мёртвых ветвей.
ОБХОД: единственная фикстура временно убирает `metering_proxy.ts`, получает код 2 по (а) и тем предъявляет `check_metering.sh` красным. Проверки (б)–(ж) при этом не исполняются либо недостижимы; на настоящем дереве барьер проверяет только наличие файла и возвращает 0. `check:antiplacebo` видит требуемый красный барьер, все названные приёмочные команды зелёные, а шесть поведений прокси не защищены.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:15-23,43-53` — матрица не закрепляет порождаемые при каждом прогоне значения там, где они являются областью предмета: валидный и невалидный токены, provider/model и произвольный upstream-путь. Также ни один исход не требует отличить произвольные POST/GET пути и ответ «как есть» от единственного `/v1/chat/completions`. Это повторяет разрешённый арбитражем класс обхода «реализация зашита на заранее известные имена».
ОБХОД: прокси распознаёт только фиксированные токены, provider/model и `/v1/chat/completions` из фикстуры, а для них возвращает подготовленные статусы и usage; любой другой токен из изменённого конфига и любой иной GET/POST путь отвергает. Поимённая матрица (б)–(ж) сходится полностью, но прокси не читает произвольную конфигурацию и не является заявленным OpenAI-совместимым ретранслятором произвольных путей. Порождаемые в каждом прогоне значения и порождённый путь подставного upstream этот обход различили бы.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:59-60,76-77` — живое end-to-end утверждение не доказано названной командой с кодом возврата. Ответ сессии доказывает только доступность модели, а записываемый в HANDOFF вывод `/healthz` — только наличие слушателя; ни один из них не доказывает, что именно этот вызов прошёл через прокси и получил роль по токену. Это противоречит `AGENTS.md:158-159`: правило без механизма не существует.
ОБХОД: оставить `.omp/models.yml` направленным прямо на провайдера, а рядом поднять процесс, отвечающий 200 на `/healthz`. `bash scripts/check_skills.sh --live` получает ответ напрямую, `workshop` и HANDOFF показывают зелёный healthz, остальные тесты гоняют отдельный подставной прокси; вся объявленная приёмка выполнена, но живая сессия обходит учёт. Различающий живой механизм обязан связать уникальный вызов с новой строкой `calls.jsonl` и ожидаемой ролью и завершаться ненулём при прямом маршруте.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:21-22,64-69` — объявленная проводка «провайдер-псевдоним на роль» требует файлов вне зон. На судимом HEAD `.omp/config.yml:27-90` ссылается на общие provider id (`minimax`, `zai`, `openai-codex`, `anthropic`), а `roles/adversary.md` и `roles/reviewer.md` делят один `@advisor`; одно добавление псевдонимов в `.omp/models.yml` не заставляет роли выбрать их. Но `.omp/config.yml`, исходники ролей и порождаемые `.omp/agents/` не входят ни в одну `ЗОНА`-строку.
ОБХОД: добавить в `.omp/models.yml` все требуемые псевдонимы и прогнать фикстуры, которые обращаются к ним напрямую, не меняя действующие modelRoles. Тестовый прокси и healthz зелёные, но реальные сессии продолжают выбирать старые общие provider id; разные роли делят токен либо идут прежним маршрутом, и `role=unknown`/неверное приписывание в живом трафике не закрыто.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:29-31,51-52` — период в `50 USD/мес` оставлен исполнителю: не заданы календарный месяц, UTC и вычисление периода на каждом вызове. Одна сумма «строк периода» на текущем времени не различает период, зафиксированный при старте процесса, от меняющегося календарного периода.
ОБХОД: вычислить `2026-08` один раз при запуске 31 августа и продолжать относить сентябрьские строки и бюджет к августу до перезапуска. Все фикстуры, выполненные внутри одного месяца, и равенство журнал→budget проходят; в долгоживущем процессе сентябрьский потолок не расходуется. Референс устраняет именно этот обход функцией UTC-месяца, вызываемой для каждого запроса, но контракт это свойство не унаследовал.

СОВЕТ `contracts/005-avtonomija-proksi.md:54-55,73` — Node/TypeScript-файл запускается через `bash`. На судимом окружении есть Node v26.5.1 с нативным запуском `.ts`; честная команда здесь — `node scripts/proxy/metering_proxy.ts --selftest`. Текущая запись либо падает на синтаксисе Node, либо поощряет shell-файл с расширением `.ts`; автору стоит исправить обе одинаковые команды.

СОВЕТ `contracts/005-avtonomija-proksi.md:24-34,51-53` — не заданы денежное представление и округление (`number` против десятичной строки/целых minor units), а для обрыва без usage не названо значение `tokens_*` и `usd`. На целых ценах реализация через binary float выглядит верной, на реальном дробном тарифе сумма и точка 402 могут разойтись. Это решение лучше закрепить вместе с форматом `budget.json`, но отдельного полностью проходящего обхода сверх уже блокирующих дыр `null` и свёртки здесь не предъявляю.

СОВЕТ `contracts/005-avtonomija-proksi.md:24-28,64-65` — живой прогон создаёт `data/metering/calls.jsonl` и `budget.json`, однако `data/metering/` на HEAD не игнорируется, а `.gitignore` не входит в зоны. Если журнал — локальное состояние, стоит дать исполнителя правилу игнора; если он намеренно версионируется, это надо сказать прямо.

Пять обязательных вопросов проверены:

1. **Критерий слабее предмета?** Да: публичные/неотзываемые токены, бесконечный `usd:null`, усечение append-only журнала, поимённо зашитый ретранслятор и прямой живой маршрут дают конкретные состояния, в которых объявленная приёмка проходит, а предмет не сделан.
2. **«Готово» недоказуемо названной командой?** Да: живой маршрут доказывается лишь ответом сессии и текстом HANDOFF/healthz, не механизмом с кодом; команда selftest дополнительно называет неверный интерпретатор.
3. **Решение молча оставлено исполнителю?** Да: хранение и отзыв токенов, судьба неоценённого вызова, UTC/граница месяца, денежная арифметика и поля неуспешного вызова меняют наблюдаемое поведение, но не закреплены.
4. **Границы исполнения не названы?** Для перечисленных файлов две зоны есть, но требуемая проводка псевдонимов не помещается в них: ссылки modelRoles и общая роль `@advisor` живут вне зоны implementer. Отдельно не распределено правило игнора нового runtime-состояния.
5. **Противоречие `AGENTS.md`?** Да: живой критерий оставлен без механизма; семь ветвей не имеют семи независимых красных предъявлений; свойства матрицы не запрещают зашивание на известные значения. Печать `PASS` не требуется, CI-исключение названо явно и само по себе находкой не является.

Вердикт `FAIL`: восемь действительных блокирующих находок имеют названные обходы. Это первый отказ по этим причинам, арбитр не созывается. Вопросов воли или цены, требующих `ESCALATE`, не найдено.
