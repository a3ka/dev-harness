FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` в коммите
`f9d822cf08423b2bfd1bd4d413ba767d273fbdd7`, восьмая итерация до первой
заморозки. Предмет закоммичен (blob
`6917041d64ebac83c5cae9cd1bdbb1e5ce7e3879`); тегов
`frozen/contracts/005/*` нет.

Набор артефактов полный. Предмет назван в
`contracts/005-avtonomija-proksi.md:7-76`, критерий готовности и команды — в
`:78-156,168-175`, два исполнителя и границы — двумя `ЗОНА`-строками в
`:158-166`. В отдельном клоне судимого HEAD `bash scripts/check_zones.sh`
вернул 0 по четырём действующим замороженным контрактам; контракт 005 до
заморозки в проверяемый диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта
`01b4dc1`, судимый текст `7dbbd4e`, восемь блокирующих находок. Круг 2:
`b67dd2f` по `a23a6b1`, семь находок; ротация, passthrough и live-коррелятор
решены арбитражем `proksi-rotaciya-passthrough-live.md` (`4c03aa7`). Круг 3:
`f61e535` по `4e6d3b8`, четыре находки; cold-start решён арбитражем
`proksi-cold-start.md` (`4cb0c7a`). Круг 4: `546e8e0` по `ac87a3d`, шесть
находок [К4-1]–[К4-6]. Круг 5: `f2cf684` по `9791d03`, три находки
[К5-1]–[К5-3]. Круг 6: `ce87685` по `a2130d8`, четыре находки
[К6-1]–[К6-4]. Настоящий файл — круг 7 критика по итерации 8 текста.

Проверка четырёх [К6-N] по существу:

1. **[К6-1] закрыта не по существу.** Положительная часть ветви (к) называет
   два разных запрета — не-node импорт и внешний процесс, — но одно красное
   предъявление нарушает их одновременно (`contracts/005-avtonomija-proksi.md:130-134`).
   Такое красное может быть поймано только `curl` и ничего не доказывает о
   проверке импортов. Конкретный обход дан первой находкой ниже.
2. **[К6-2] закрыта.** GET порождает тело, request content-type и request-id;
   upstream-счётчик сверяет дословно те же пять полей, что POST, после чего
   сверяется нетривиальная тройка ответа (`contracts/005-avtonomija-proksi.md:102-108`).
3. **[К6-3] закрыта в заявленной области.** Ветка обрыва требует строку со
   всеми одиннадцатью полями, отдельно называя role/provider/model/path,
   latency_ms и request_id, а также status и нулевые tokens/usd
   (`contracts/005-avtonomija-proksi.md:116-118`). Отдельный разрыв точного
   статуса 502 дан ниже и не отменяет закрытие именно полноты схемы.
4. **[К6-4] закрыта.** Составная ветвь начинает ниже потолка, пропускает
   пересекающий вызов upstream, возвращает ответ как есть, пишет полную
   стоимость, фиксирует бюджет свыше потолка и только следующий вызов
   отсекает до upstream; назван точный красный стаб подмены на 402 без записи
   (`contracts/005-avtonomija-proksi.md:109-115`).

Счётная поправка проверена другой мерой: перечисление независимо разобрано как
ровно двенадцать ветвей — (а), (б), (в), (в2), (г), (г2), (д), (е), (ж),
(з), (и), (к) (`contracts/005-avtonomija-proksi.md:83-134`). Противоречия
«ДВЕНАДЦАТЬ» перечню нет.

Полный обязательный перечень арбитража
`proksi-rotaciya-passthrough-live.md:80-151` прогнан:

- ротация: собственный secrets-файл через `--config`, один живой процесс с
  утверждаемым pid, T1/T2 одной роли R, строки с R после обоих успешных
  вызовов, финальный 401 без роста журнала и красное стартового кэша названы
  (`contracts/005-avtonomija-proksi.md:124-129`);
- passthrough: отдельные составные POST и GET имеют свои порождённые пути;
  upstream фиксирует метод/путь (и сверх минимума остальные три поля), ответ
  имеет порождённые статус ≠ 200, content-type ≠ JSON и не-JSON байты, клиент
  сверяет тройку; POST-only и пересборка ответа названы красными стабами
  (`contracts/005-avtonomija-proksi.md:91-108`);
- live: схема содержит `request_id`; положительная проба требует
  восстановимый коррелятор, ожидаемую роль и свежий `ts`, отрицательный
  прямой маршрут возвращает 1 (`contracts/005-avtonomija-proksi.md:36-40,
  69-76,135-137`);
- четыре неповторённых хвоста сохранены: grep всего репозитория
  (`:122-123`), известные usage/тариф и ненулевой целый usd (`:91-101`),
  полная успешная схема и отсутствие строк unknown/unpriced (`:36-45,
  90-101,121`), workshop вынесен в отдельный cold-start (`:60-68,138-154`).

Полный обязательный перечень арбитража
`proksi-cold-start.md:116-151` прогнан, и он исполнен НЕ дословно:

- (3а) использует фатальный invalid JSON, фиксирует ненулевой код погибшего
  ребёнка, код лаунчера 1, полный внешний замер и красное отсутствия надзора
  за гибелью (`contracts/005-avtonomija-proksi.md:141-146`), но обязательную
  проверку **причины** отказа не называет;
- (3б) держит ребёнка живым и healthz молчащим, требует код 1 и полное время
  между окнами, предъявляет лаунчер без общего колпака и дополняющее красное
  раннего возврата (`contracts/005-avtonomija-proksi.md:146-152`), но также
  не проверяет **причину** отказа;
- два числа названы раздельно: W_healthz = 5 с и W_probe = 15 с, внешнее
  строго больше внутреннего (`contracts/005-avtonomija-proksi.md:153-154`);
- недоступный upstream прямо оставлен пер-запросным 502 и не является
  причиной гибели старта (`contracts/005-avtonomija-proksi.md:64-67`).

Ниже — действительные разрывы предмета и критерия.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:9-11,130-134 — [К6-1] одно
красное предъявление одновременно добавляет vendor-импорт И внешний `curl`.
Оно не различает две независимые половины builtins-only: ветвь может краснеть
только на `curl`, пока проверка импортов остаётся плацебо. Это противоречит
`AGENTS.md:162-163`: каждая реально заявленная проверка обязана быть
предъявлена красной.
ОБХОД: рабочий `metering_proxy.ts` импортирует HTTP-библиотеку из `vendor/`,
но не запускает внешних процессов; ветвь (к) фактически проверяет только
exec/spawn и на рабочем дереве зелёная. Красная копия одновременно получает
тот же vendor-импорт и внешний `curl`, поэтому честно краснеет на `curl`.
`check_metering.sh`, общий antiplacebo и вся приёмка возвращают 0, хотя
проверка запрета стороннего импорта отсутствует и прокси не builtins-only.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:60-68,138-152 — обязательный
минимум cold-start требует для (3а) и (3б) «отказ с причиной, код 1»
(`proksi-cold-start.md:123-131`). Текст критерия пишет «с причиной» только у
подставного слушателя (2); у (3а) и (3б) проверяются код и время, но не
диагностика. Декларация причины в Предмете без различающего исхода не заменяет
критерий — это ровно класс причины, по которому был созван этот арбитраж.
ОБХОД: `workshop` для фатального child-exit и истечения W_healthz молча
возвращает 1, ничего не печатая и не сохраняя; для чужого слушателя печатает
причину. Все три cold-start исхода, временные границы и оба красных стаба
сходятся, но пользователь не получает обещанной причины двух отказов запуска.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:55-59,80-82 — предмет требует
вычислять UTC-период НА КАЖДОМ запросе, но selftest проверяет лишь чистую
`periodAt` на фиксированном стыке; ни один исход не делает два запроса одним
живым процессом по разные стороны месяца. Декларация вызова функции в
Предмете не является механизмом.
ОБХОД: реализовать `periodAt(ts)` правильно и пройти её selftest, но при
старте прокси один раз сохранить `periodAt(Date.now())` и использовать
закэшированный месяц во всех обработчиках. Все фикстуры, выполненные внутри
одного месяца, и вся приёмка возвращают 0; процесс, переживший 00:00 UTC
первого числа, продолжает списывать новый трафик в старый период.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:47-56,91-115 — конфигурация
объявляет цену по паре provider+model и отдельный потолок каждого provider,
но матрица не сталкивает два provider с одинаковым model и разными
ценами/потолками. Один порождённый provider/model и его потолок не доказывают
обе оси конфигурации.
ОБХОД: фикстуры порождают единственный provider P и model M; прокси ищет цену
только по M, а расход и потолок хранит в одном глобальном месячном счётчике,
игнорируя provider. Известная стоимость, переход и следующий 402 сходятся,
unpriced-модель по-прежнему даёт 503, все команды возвращают 0. В реальном
конфиге P1/M и P2/M с разными тарифами и потолками второй provider получает
цену/бюджет первого — заявленные per-provider+model и per-provider ceilings
не реализованы.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:36-41,47-53,80-82 — предмет
фиксирует int64 minor units, но критерий требует лишь «целый» ненулевой usd и
не называет значения у границы точности. Целочисленность малых результатов не
отличает int64 от IEEE-754 `number`.
ОБХОД: хранить тарифы, `usd` и бюджет в JavaScript `number`; фикстуры
порождают безопасные малые целые, формула и округление вверх сходятся, все
команды возвращают 0. При сумме или произведении выше 2^53 прокси теряет
единицы и неверно решает границу 402, хотя значение ещё лежит в объявленном
int64.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:36-45,55-59,109-118 — предмет
обещает для уже исчерпанного потолка 402 **с состоянием бюджета до запроса**,
а журнал — строку на каждый вызов с двумя явно названными исключениями
(unknown и unpriced). Ветви (г)/(г2) проверяют только 402 и неизменный
upstream-счётчик; ни тело/состояние ответа 402, ни строка этого отказа не
проверяются.
ОБХОД: после перехода потолка следующий вызов вернуть пустым 402 и не писать
его в `calls.jsonl`; upstream не вызывать. Составной переход, счётчик,
budget.json свыше потолка, 402, rebuild и все команды сходятся, но клиент не
получает обещанного состояния бюджета, а журнал теряет вызов, не входящий ни
в одно объявленное исключение.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:64-67,116-118 — предмет и
cold-start-арбитраж фиксируют недоступный upstream как пер-запросный **502**,
но ветвь (д) принимает неназванный «статус ошибки». Полная схема строки не
различает конкретный HTTP-статус.
ОБХОД: при обрыве upstream возвращать клиенту 500 и писать полную строку со
`status=500`, одиннадцатью полями и нулевыми tokens/usd. Ветка (д), rebuild,
cold-start здорового прокси с мёртвым upstream и вся приёмка проходят, но
закреплённый 502 не реализован.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:60-68,138-154 — предмет требует
читать порт и именованное W_healthz из конфига, однако cold-start-матрица
фиксирует только значения по умолчанию 8765 и 5 с; ни один исход не задаёт
порожденный неумолчательный порт или другое допустимое окно и не сверяет, что
launcher их прочитал.
ОБХОД: `workshop` всегда обращаться к 8765 и всегда ждать ровно 5 секунд,
игнорируя обе настройки; cold-start-фикстуры оставить на объявленных
умолчаниях, а proxy config передавать лишь дочернему процессу. Успешный старт,
чужой слушатель, (3а), (3б), W_probe=15 и все красные сходятся, но любой
проектный конфиг с другим портом либо W_healthz не действует.

Пять обязательных вопросов:

1. **Критерий слабее предмета?** Да. Vendor-импорт при красном только на curl,
   безмолвные cold-start-отказы, кэш месяца, одномерные тариф/бюджет,
   арифметика `number`, пустой незаписанный 402, status 500 и hardcode
   launcher дают восемь конкретных состояний, где команды сходятся, а предмет
   не сделан.
2. **«Готово» недоказуемо названной командой?** Да. Команды не различают
   независимую проверку imports, причины (3а)/(3б), смену UTC-месяца в одном
   процессе, обе provider-оси, int64-границу, payload/журнал 402, точный 502 и
   чтение неумолчательных launcher-настроек.
3. **Решение молча оставлено исполнителю?** Да: независимость двух
   builtins-предикатов, формат причины запуска, ключи тарифов/бюджетов и
   числовое представление, семантика отклонённого 402 и фактическое чтение
   launcher-конфига определят наблюдаемое поведение, но не закреплены
   различающими исходами.
4. **Границы исполнения не названы?** Названы. Architect владеет контрактом,
   прокси, барьером, fixture-деревом, metering-конфигом, рольной проводкой и
   `.gitignore`; implementer — package/CI, `workshop` и `.omp/models.yml`
   (`contracts/005-avtonomija-proksi.md:158-166`). Все файлы, нужные для
   закрытия находок, уже входят в зоны; необходимого пути вне зон не найдено.
5. **Противоречие `AGENTS.md`?** Да: составное красное ветви (к) не
   предъявляет красной каждую из двух проверок (`AGENTS.md:162-163`), а
   остальные названные правила оставлены без различающих механизмов
   (`AGENTS.md:158-159`). Печать PASS не требуется; двенадцать ветвей
   независимо пересчитаны; CI-исключение ограничено `--selftest` и имеет
   причину.

По Н-38 арбитр не созывается: автор не записал несогласия с находками; повтор
без несогласия закрывается правкой. Вопросов воли, риска или денег для
`ESCALATE` нет.

Вердикт `FAIL`: восемь действительных блокирующих находок, у каждой назван
конкретный обход; обязательный cold-start-минимум дополнительно не перенесён
дословно в части причины двух отказов.
