FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` на HEAD `a23a6b1ea9241801431c474490e65f0cc8b1051f`, вторая итерация до первой заморозки. Предмет закоммичен; тегов `frozen/contracts/005/*` нет.

Набор артефактов полный. Предмет назван в `contracts/005-avtonomija-proksi.md:7-57`, критерий готовности и доказывающие команды — в `:59-83,98-104`, исполнители и границы — двумя `ЗОНА`-строками в `:85-96`. На судимом HEAD `bash scripts/check_zones.sh` вернул 0 по четырём действующим замороженным контрактам; контракт 005 до заморозки в диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта `01b4dc11b5a037504097a15a12db898f2b9d005c`, судимый текст `7dbbd4ec0a43e9ccb4038cf4b4da5890c46306cd`, восемь блокирующих находок [К1-1]–[К1-8]. Круг 2: настоящий вердикт по `a23a6b1`.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:27-29,61-80` — [К1-1] не закрыта различающим исходом для отзыва. Предмет требует перечитывать карту токенов на каждом запросе и запрещает стартовый кэш, но selftest проверяет только приписывание одного токена, а ветви (б), (в), (з) запускаются с уже подготовленной конфигурацией. Ни одна названная команда не меняет файл секретов между двумя запросами к одному живому процессу и не требует, чтобы новый токен был принят, а старый получил 401 без строки журнала.
ОБХОД: прочитать `secrets.env` один раз при старте прокси и держать карту в памяти. Selftest приписывания, порождённые известный/неизвестный токены, grep конфигов и живой вызов проходят; после замены секрета тот же процесс продолжает принимать отозванный токен, хотя предмет прямо требует отзыв без перезапуска.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:23-29,75-77` — вторая часть [К1-1] сужена с «токены ТОЛЬКО в доме секретов» до grep буквальных токенов только по конфигам. Объявление «в репозитории буквальных нет» шире названной области команды: исходники прокси, launcher и фикстуры ею не проверяются.
ОБХОД: оставить `config/metering.json` без токенов и ссылки `${METERING_TOKEN_<ROLE>}` в `.omp/models.yml`, но закоммитить рабочий токен роли литералом в `scripts/proxy/metering_proxy.ts`. Порождаемые фикстурные токены обрабатывать из переданного `--config`; grep по конфигам пуст, все ветви и live-проба проходят, но любой читатель репозитория получает рабочий токен, а требование «ТОЛЬКО в доме секретов» не выполнено.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:30-48,61-76` — денежный учёт priced-модели оставлен реализации: не названы источник и схема тарифов, формула перевода `tokens_in/tokens_out` в микро-доллары и ни один ожидаемый ненулевой `usd` для известных usage и тарифа. Selftest сворачивает уже записанные суммы, ветвь (в) сверяет usage, ветвь (г) может начинать с заранее исчерпанного бюджета, а ветвь (ж) отличает лишь наличие цены от её отсутствия. Эти исходы не доказывают вычисление стоимости вызова.
ОБХОД: считать любую модель с записью `priced: true` оценённой, но для каждого её успешного вызова писать `usd = 0`; для ветви (г) подложить журнал/бюджет уже на потолке, а модель без `priced` отвергать 503 до upstream. Свёртка, целочисленность, 200/402/503 и все названные команды сходятся, однако реальные priced-вызовы никогда не расходуют потолок — предмет «прокси учёта с потолком расхода» не сделан.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:16-21,64-76` — [К1-5] закрыта для порождаемого пути, provider/model и `--config`, но не для заявленных GET/POST и ответа upstream «как есть». В матрице есть один валидный вызов с путём не `chat/completions`; она не требует по одному различающему запросу обоих методов и не сверяет байты тела, статус и `content-type` с подставным upstream.
ОБХОД: честно читать порождённые provider/model/path из переданного конфига, но ретранслировать только POST, а ответ собирать заново как JSON с теми же `usage` и кодом 200, всегда с `application/json`. Выбрать POST для единственной ветви (в). Все восемь ветвей, порождения и красные вызовы проходят; произвольный GET не работает, нестандартное тело/статус/content-type провайдера меняются, поэтому объявленный ретранслятор не реализован.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:30-32,54-57,78-80` — [К1-6] всё ещё не связывает уникальный uuid-вызов с найденной строкой. UUID помещён в prompt, но объявленная схема журнала не хранит ни prompt, ни UUID/request id; проверяемы только ожидаемая роль и `ts` после старта. Два события остаются некоррелированными.
ОБХОД: оставить живой omp направленным прямо к провайдеру, а во время положительной live-пробы сделать отдельный HTTP-вызов через прокси тем же токеном роли. Он создаст свежую строку нужной роли, и команда вернёт 0, хотя uuid-вызов omp прошёл мимо учёта. В отдельной отрицательной пробе вспомогательный вызов не делать — прямой маршрут даст требуемый код 1. Все объявленные исходы выполнены, но живая сессия не маршрутизирована через прокси.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:30-41,61-80` — матрица не доказывает полный контракт строки журнала и отсутствие строки для unpriced-модели. Ветвь (в) требует роль и usage, (д) — status и нулевые tokens/usd, live — роль и ts; поля `provider`, `model`, `path`, `latency_ms` целиком не сверяются. Ветвь (ж), в отличие от (б), требует 503 и отсутствие upstream-вызова, но не требует отсутствия строки журнала, хотя предмет это говорит дословно.
ОБХОД: писать строки только с `ts, role, tokens_in, tokens_out, usd, status`, опуская provider/model/path/latency, и перед ответом `503 model_unpriced` дописывать нулевую строку. Все явно перечисленные проверки строк, свёртка бюджета, маркер append-only и счётчик upstream проходят, но журнал не имеет объявленной схемы и содержит запрещённый вызов unpriced-модели.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:49-53,81-83,98-104` — запуск прокси лаунчером, холодный старт, пятисекундный отказ и различение чужого процесса на занятом порту не имеют названной проверяющей команды с ожидаемым кодом. «Живой прогон владельца» и запись healthz в HANDOFF — чтение результата, а `check_metering.sh --live` проверяет уже доступный маршрут; `check_skills.sh --live` проверяет вызов omp, не переход workshop из состояния «прокси отсутствует». Это противоречит `AGENTS.md:158-160`: правило без механизма не существует, проверка обязана иметь код возврата.
ОБХОД: не менять `workshop` вообще; перед приёмкой поднять прокси вручную и записать успешный healthz в HANDOFF. Selftest, подставной барьер, live-metering, antiplacebo, CI parity и live-skills возвращают 0 на уже поднятом процессе, но `workshop` не запускает прокси из холодного состояния и не реализует объявленные отказы по таймауту/чужому порту.

АРБИТР: `arbiter` — созвать по второму `FAIL` для повторённых причин [К1-1], [К1-5] и [К1-6]. Предмет спора: достаточно ли декларации поведения в разделе «Предмет» без различающего исхода в критерии, либо отзыв токена обязан менять секрет между запросами к одному процессу, ретранслятор обязан отдельно предъявить GET/POST и ответ как есть, а live-проба обязана нести коррелятор uuid в наблюдаемой строке журнала.

Закрытие восьми находок круга 1 проверено поимённо:

1. **[К1-1] не закрыта по существу**: место хранения и ссылки на секреты названы (`:23-26`), но grep сужен до конфигов, а отзыв без перезапуска не предъявлен (`:27-29,61-80`). Два обхода названы выше.
2. **[К1-2] закрыта в главной части**: unpriced-модель даёт 503 до upstream, отдельная ветвь требует неизменный счётчик (`:40-48,74-75`). Осталась отдельная дыра журнала: ветвь не проверяет обещанное отсутствие строки.
3. **[К1-3] закрыта**: маркер обязан пережить прогон, `budget.json` удаляется, пересобирается реальной `--rebuild-budget` и сравнивается со свёрткой (`:35-39,72-73`).
4. **[К1-4] закрыта**: восемь ветвей требуют восемь отдельных красных вызовов собственных фикстур (`:64-77`).
5. **[К1-5] закрыта частично**: значения и конфиг порождаются, путь отличен от chat/completions (`:16-21,64-76`), но один вызов не различает GET от POST и не сверяет ответ как есть. Обход назван выше.
6. **[К1-6] не закрыта по существу**: свежая строка и отрицательный прямой маршрут названы (`:54-57,78-80`), но uuid отсутствует в схеме журнала и причинная связь не наблюдаема. Обход назван выше.
7. **[К1-7] закрыта**: `roles/`, `.omp/config.yml`, `.omp/agents/` и `.gitignore` добавлены в зону architect; требуемые контрактом файлы покрыты двумя `ЗОНА`-строками (`:85-96`).
8. **[К1-8] закрыта**: UTC `YYYY-MM`, вычисление `periodAt(ts)` на каждый запрос и selftest на фиксированной границе месяцев названы (`:43-47,61-63`).

Пять обязательных вопросов проверены:

1. **Критерий слабее предмета?** Да. Стартовый кэш токенов, токен в исходнике, нулевая стоимость priced-вызовов, POST-only/пересобранный ответ, некоррелированный live-вызов, неполная строка журнала и неизменённый workshop дают конкретные состояния, где приёмка проходит, а предмет не сделан.
2. **«Готово» недоказуемо названной командой?** Да. Ротация в одном процессе, корректная тарификация, обе HTTP-ветви и байтовый passthrough, причинность uuid→строка, полная схема журнала и холодный запуск workshop не доказаны командами с различающими исходами. Ручной HANDOFF не является командой с кодом возврата.
3. **Решение молча оставлено исполнителю?** Да. Исполнитель выбирает схему и арифметику тарифов, наблюдаемый коррелятор live-вызова и способ доказательства состояний launcher; эти решения меняют сам заявленный учёт и автономный запуск.
4. **Границы исполнения не названы?** Названы. Два исполнителя и все прямо требуемые пути покрыты `ЗОНА`-строками `:87-88`; каталожные границы и связность разъяснены в `:90-96`. Нового обхода зон не найдено.
5. **Противоречие `AGENTS.md`?** Да. Пункт workshop выдан за критерий через ручную запись в HANDOFF без механизма и кода; несколько счётных/наблюдаемых свойств не имеют различающих исходов. Печать `PASS` не требуется, восемь красных вызовов названы, CI-исключение объявлено явно.

Вердикт `FAIL`: семь действительных блокирующих находок имеют названные обходы; одна из них дополнительно является прямым противоречием норме `AGENTS.md`. Повтор трёх причин круга 1 требует арбитража. Советов и вопросов воли/денег для `ESCALATE` нет.
