FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` на HEAD `4e6d3b824088348245a197134532290ed8eb09b5`, третья итерация до первой заморозки. Предмет закоммичен; тегов `frozen/contracts/005/*` нет.

Набор артефактов полный. Предмет назван в `contracts/005-avtonomija-proksi.md:7-76`, критерий готовности и доказывающие команды — в `:78-110,123-129`, исполнители и границы — двумя `ЗОНА`-строками в `:112-121`. В отдельном клоне на судимом HEAD `bash scripts/check_zones.sh` вернул 0 по четырём действующим замороженным контрактам; контракт 005 до заморозки в проверяемый диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта `01b4dc11b5a037504097a15a12db898f2b9d005c`, судимый текст `7dbbd4ec0a43e9ccb4038cf4b4da5890c46306cd`, восемь блокирующих находок [К1-1]–[К1-8]. Круг 2: коммит вердикта `b67dd2f`, судимый текст `a23a6b1ea9241801431c474490e65f0cc8b1051f`, семь блокирующих находок; повторённые [К1-1], [К1-5], [К1-6] переданы арбитру. Обязательный минимум зафиксирован в `verdicts/arbitration/proksi-rotaciya-passthrough-live.md` (`4c03aa7`); решённые причины заново не обсуждаются, ниже предъявлены только буквальные несоответствия этому минимуму.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:82-103` — критерий объявляет «ДЕСЯТЬ ветвей», но поимённо перечисляет одиннадцать: (а), (б), (в), (в2), (в3), (г), (д), (е), (ж), (з), (и), причём каждой обещан отдельный красный вызов собственной фикстуры. Это внутренне противоречивое счётное утверждение и прямое противоречие `AGENTS.md:164-166`: счёт проверяется другой мерой, а не той же зонтичной командой. Исключение роли для противоречия `AGENTS.md` применяется: отдельный `ОБХОД` не требуется. При одиннадцати фикстурах неверно зафиксированное число, при десяти невозможно дать отдельную фикстуру каждой из одиннадцати названных ветвей; однозначного замораживаемого критерия нет.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:87-94` — обязательный минимум passthrough из `verdicts/arbitration/proksi-rotaciya-passthrough-live.md:101-115` исполнен не дословно. Арбитр требует два отдельных вызова GET и POST, для КАЖДОГО свой порождённый путь, фиксацию и сверку принятых upstream метода/пути, нетривиальную тройку ответа и собственное красное предъявление. В контракте только (в2) велит upstream фиксировать метод/путь; (в) называет клиентский POST, но не требует, чтобы именно POST и путь дошли до upstream; у (в3) метод вообще не назван. Поэтому арбитражное требование «метод обязан дойти дословно» для POST не имеет различающего исхода.
ОБХОД: на клиентский POST из (в) отправлять к upstream GET по тому же пути, получить оттуда порождённые usage и вернуть ожидаемые 200/журнал; (в2) честно выполнить как GET со сверкой метода/пути, а не закреплённую методом (в3) также выполнить GET и вернуть её тройку байт-в-байт. Все названные исходы `check_metering.sh` сходятся, POST-only-заглушка падает на GET, но клиентский POST не ретранслируется upstream методом POST — предмет `:18-20` не сделан.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:30-39,102-103` — обязательный минимум ротации из `verdicts/arbitration/proksi-rotaciya-passthrough-live.md:86-97` требует после T1 и T2 не только статус «принят», но строку журнала с ожидаемой ролью для каждого. Ветка (и) проверяет принятие T1/T2 и отсутствие строки лишь после финального 401; она не требует роста журнала и правильной роли после двух успешных вызовов. Общая ветвь (в) проверяет роль другого токена и этот пробел не закрывает.
ОБХОД: перечитывать секрет на каждом запросе и корректно принимать T1/T2, но для токена, которого не было при старте процесса, писать неверную роль либо не писать строку; отдельный токен ветви (в) журналировать правильно. Ротация проходит, стартовый кэш падает на T2, финальный T1 даёт 401 без строки, однако успешный T2 не приписан роли и обязательная строка на вызов из `:34-39` отсутствует либо неверна.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:58-67,106-108` — [К2-7] по-прежнему не имеет различающего исхода для обещания «ждёт ≤ 5 с; не поднялся — отказ». `--cold-start` доказывает успешный быстрый подъём и отказ уже занятому порту, но не моделирует процесс прокси, который не становится healthy, и не ограничивает длительность возврата ошибки. Это оставляет непроверенной ту же часть перехода launcher, которая была названа в блокере круга 2.
ОБХОД: при свободном порту после неудачного запуска ждать 60 секунд или бесконечно; исправный прокси в положительной ветви успевает подняться сразу, а подставной слушатель распознаётся до запуска и даёт код 1. Обе ветви `--cold-start` и вся приёмка проходят, но `workshop` не выполняет заявленный пятисекундный отказ при реально не поднявшемся прокси.

АРБИТР: `arbiter` — созвать по второму `FAIL` для повторённой причины [К2-7]. Предмет спора: обязан ли различающий исход launcher отдельно предъявлять неуспешный запуск и возврат ошибки не позднее 5 секунд, либо успешного cold-start и немедленного отказа чужому слушателю достаточно для предметного обещания `contracts/005-avtonomija-proksi.md:58-67`.

Повторные причины [К1-1] и [К1-5] заново арбитру не передаются: решение уже вынесено, а находки выше фиксируют несоответствие его обязательному минимуму. [К1-6] исполнена по минимуму арбитража: `request_id` включён в схему (`:34-38`), live-команда ищет строку по коррелятору и роли, прямой маршрут обязан дать 1 (`:68-76,104-105`); роль+ts отдельно критерием не считаются.

Закрытие полного перечня восьми находок круга 1 проверено поимённо:

1. **[К1-1] закрыта не полностью**: дом секретов, отсутствие литералов и отзыв стартового кэша названы (`:25-33,100-103`), но обязательные строки с ролью для T1/T2 из арбитража не вошли в критерий. Обход назван выше.
2. **[К1-2] закрыта**: unpriced-модель даёт 503 до upstream и без строки (`:47-50,98-99`).
3. **[К1-3] закрыта**: маркер append-only сохраняется, `budget.json` удаляется, реально пересобирается и сверяется со свёрткой (`:42-43,97-98`).
4. **[К1-4] закрыта не однозначно**: самостоятельные красные вызовы названы, но число «десять» противоречит одиннадцати ветвям (`:82-103`).
5. **[К1-5] закрыта не полностью**: значения и пути порождаются, GET отделён, тройка passthrough сверяется (`:18-23,87-94`), но POST не обязан дойти до upstream методом POST вопреки арбитражу. Обход назван выше.
6. **[К1-6] закрыта**: коррелятор вошёл в схему и обе live-ветви имеют коды (`:34-38,68-76,104-105`).
7. **[К1-7] закрыта**: два исполнителя названы, прямо требуемые пути покрыты `ЗОНА`-строками (`:112-121`).
8. **[К1-8] закрыта**: UTC `YYYY-MM`, вызов `periodAt(ts)` на каждом запросе и selftest стыка месяцев закреплены (`:51-56,79-81`).

Закрытие полного перечня семи находок круга 2 проверено поимённо:

1. **Ротация [К1-1/арбитраж] закрыта не полностью**: тождество процесса и различение стартового кэша есть, строки с ролью для T1/T2 отсутствуют в исходе ветви (и).
2. **[К2-2] закрыта**: grep значения секрета расширен на все файлы репозитория (`:25-28,100-101`).
3. **[К2-3] закрыта**: источник и схема цены, формула, округление вверх и ненулевой целый ожидаемый `usd` названы (`:44-50,79-81,87-89`).
4. **Passthrough [К1-5/арбитраж] закрыта не полностью**: GET и тройка ответа выделены, но upstream-метод/путь сверяются только для GET, а метод ветви (в3) не закреплён.
5. **Live [К1-6/арбитраж] закрыта**: поле `request_id`, поиск по коррелятору+роли и отрицательный прямой маршрут названы; роль+ts отдельно исключены (`:34-38,68-76,104-105`).
6. **[К2-6] закрыта**: полная схема строки сверяется в (в), для unpriced и неизвестного токена явно требуется отсутствие строки (`:34-41,86-89,98-99`).
7. **[К2-7] закрыта не полностью**: успешный переход launcher и чужой слушатель доказуемы `--cold-start`, но отказ не поднявшегося процесса в пределах 5 секунд не предъявлен. Обход и созыв арбитра названы выше.

Пять обязательных вопросов проверены:

1. **Критерий слабее предмета?** Да. GET вместо upstream-POST, неверная/отсутствующая строка T2 и неограниченное ожидание не поднявшегося прокси дают конкретные состояния, где названные команды проходят, а предмет не сделан. Дополнительно число фикстур внутренне противоречиво.
2. **«Готово» недоказуемо названной командой?** Да. Ни одна ветвь не требует доставить POST upstream методом POST; ротация не проверяет строки T1/T2 с ролью; `--cold-start` не проверяет пятисекундную границу неуспешного запуска.
3. **Решение молча оставлено исполнителю?** Да. Текст оставляет автору фикстур метод (в3), допустимость подмены upstream-метода для (в), проверку строк успешной ротации и сценарий/меру таймаута launcher. Эти выборы меняют заявленное поведение, а не устройство реализации.
4. **Границы исполнения не названы?** Названы. Architect владеет контрактом, прокси, барьером, одиннадцатью по факту ветвями фикстур, конфигом и рольной проводкой; implementer — npm/CI, `workshop` и `.omp/models.yml` (`:112-121`). Прямо требуемого файла вне зон не найдено.
5. **Противоречие `AGENTS.md`?** Да. «ДЕСЯТЬ» против одиннадцати перечисленных ветвей — счётное противоречие без другой меры; три предметных свойства остаются без различающих исходов, что противоречит правилам 1 и 3. Печать `PASS` не требуется; CI-исключение объявлено с причиной.

Вердикт `FAIL`: четыре действительных блокирующих находки; три имеют конкретные обходы, четвёртая блокирует как прямое противоречие `AGENTS.md` по предусмотренному ролью исключению. Вопросов воли или денег для `ESCALATE` нет.
