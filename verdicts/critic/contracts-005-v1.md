accept

Судимый предмет: `contracts/005-avtonomija-proksi.md` в коммите
`0dd884f8374715aa2d03c66c62e5163e96829e40`, десятая итерация до первой
заморозки. Предмет закоммичен (blob
`dd311779cf18f7aae158d390cd3cae05547dfd4f`); тегов
`frozen/contracts/005/*` нет.

Набор артефактов полный. Предмет назван в
`contracts/005-avtonomija-proksi.md:7-76`, критерий готовности и команды — в
`:78-174,186-193`, два исполнителя и границы — двумя `ЗОНА`-строками в
`:176-184`. В отдельном клоне судимого HEAD `bash scripts/check_zones.sh`
вернул 0 по четырём действующим замороженным контрактам; контракт 005 до
заморозки в проверяемый диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта
`01b4dc1`, судимый текст `7dbbd4e`, восемь блокирующих находок. Круг 2:
`b67dd2f` по `a23a6b1`, семь находок; ротация, passthrough и live-коррелятор
решены арбитражем `proksi-rotaciya-passthrough-live.md` (`4c03aa7`). Круг 3:
`f61e535` по `4e6d3b8`, четыре находки; cold-start решён арбитражем
`proksi-cold-start.md` (`4cb0c7a`). Круг 4: `546e8e0` по `ac87a3d`, шесть
находок [К4-1]–[К4-6]. Круг 5: `f2cf684` по `9791d03`, три находки
[К5-1]–[К5-3]. Круг 6: `ce87685` по `a2130d8`, четыре находки
[К6-1]–[К6-4]. Круг 7: `22a1c3f` по `f9d822c`, восемь находок
[К7-1]–[К7-8]. Круг 8: `5cd7a2c` по итерации 9 `089d25c`, одна находка
[К8-1]. Настоящий файл — круг 9 критика по итерации 10 текста.

## Закрытие [К8-1]

Блокер закрыт по существу. Diff `089d25c..0dd884f` меняет только требование
красноты двух passthrough-ветвей. На POST-ветви больше нет требования краснить
POST-only стаб: там осталось обманное предъявление пересборки ответа из usage,
и его дефект наблюдаем сравнением нетривиальной тройки ответа
(`contracts/005-avtonomija-proksi.md:89-99`). POST-only стаб перенесён в
GET-ветвь: GET превращается в POST, а upstream-счётчик сверяет присланный метод
GET, поэтому именно заявленный дефект различим; пересборка ответа различима там
же тройкой ответа (`contracts/005-avtonomija-proksi.md:100-106`). Ложного
диагноза, из-за которого отказал круг 8, больше нет.

## Полный прогон красных предъявлений

Объявление над матрицей требует пятнадцать отдельных красных вызовов
собственных фикстур (`contracts/005-avtonomija-proksi.md:83-86`). Все пятнадцать
ветвей пересчитаны независимо от этого числа: (а), (б), (в), (в2), (г), (г2),
(д), (е), (ж), (з), (и), (к1), (к2), (л), (м). Для каждой проверено свойство:
заявленный дефект обязан проявляться на названном входе и измеряться названным
наблюдением.

1. **(а), (б)** (`:87-88`): отсутствие процесса наблюдается кодом 2;
   принятие неизвестного порождённого токена либо ложная запись наблюдаются
   парой 401 + отсутствие новой строки.
2. **(в)** (`:89-99`): пересборка ответа из usage искажает порождённые
   не-JSON байты, статус либо content-type; клиент сверяет все три. На этой
   POST-ветви не осталось красного, чей дефект относится только к GET.
3. **(в2)** (`:100-106`): POST-only ретранслятор меняет входной GET на POST,
   что наблюдает upstream-счётчик; стаб пересборки отдельно наблюдается
   сравнением тройки ответа. Оба диагноза относятся к проявившемуся дефекту.
4. **(г)** (`:107-112`): преждевременный 402 и пропуск списания наблюдаются
   одновременно ответом клиента, разрешённым вызовом upstream и итоговой
   суммой бюджета.
5. **(г2)** (`:113-116`): пустой 402 и отсутствие строки наблюдаются точной
   четвёркой состояния в теле, неизменным upstream-счётчиком и полной строкой
   журнала с `usd=0`.
6. **(д)** (`:117-119`): сокрытие обрыва либо неполная запись наблюдаются
   клиентским 502 и полной одиннадцатипольной строкой со `status=502` и
   нулевыми tokens/usd.
7. **(е)** (`:120-121`): перезапись append-only журнала и доверие производному
   budget вместо журнала различаются живым маркером, удалением `budget.json`,
   пересборкой и сравнением со свёрткой.
8. **(ж)** (`:122`): отправка unpriced-вызова upstream либо запись такого
   вызова наблюдаются тройкой 503, неизменный upstream-счётчик, отсутствие
   строки.
9. **(з)** (`:123-124`): буквальный секрет в любом файле заявленной области
   наблюдается grep по значению порождённого секрета по всему репозиторию.
10. **(и)** (`:125-130`): стартовый кэш наблюдается без перезапуска процесса:
    T2 не принимается либо удалённый T1 остаётся принят; pid, роль и рост
    журнала сверяются.
11. **(к1)** (`:131-133`): копия только с vendor-импортом наблюдается grep
    импортов вне `node:`; внешний процесс в эту красную копию не примешан.
12. **(к2)** (`:134-136`): копия только с внешним `curl` наблюдается grep
    exec/spawn внешних путей; сторонний импорт в эту красную копию не
    примешан.
13. **(л)** (`:137-150`): три заявленных дефекта имеют раздельные наблюдения.
    Кэш периода на старте даёт первый период и/или 402 на втором вызове;
    поиск тарифа/потолка только по model расходится по суммам двух provider;
    IEEE-754 расходится с точной строкой порождённого целого выше 2^53.
14. **(м)** (`:151-156`): потеря канала не объявляется успехом по догадке —
    канал измеряется живым прогоном, а допустимый запасной коррелятор обязан
    быть детерминированной функцией uuid и восстановим из найденной строки;
    прямой маршрут с тем же коррелятором обязан вернуть 1.

Это ровно пятнадцать ветвей: пункты 1 и 13 выше группируют соответственно две
ветви и три под-исхода одной ветви, но ни одной ветви не пропускают. Ни одно
названное обманное предъявление матрицы не требует красноты на входе, где его
дефект ненаблюдаем.

Cold-start также пройден полностью (`contracts/005-avtonomija-proksi.md:157-172`):

- успешный переход после kill измеряет живые healthz и pid; подставной
  слушатель — код 1 с названной причиной;
- красное (3а) не наблюдает гибель ребёнка и ждёт `W_probe+1`; ненулевой код
  погибшего ребёнка и внешний предел `W_probe` делают ровно этот дефект
  наблюдаемым;
- красное (3б) не имеет общего колпака и безгранично ждёт живого молчащего
  ребёнка; внешний `W_probe` делает ровно этот дефект наблюдаемым;
- дополняющее красное раннего возврата различается нижней границей
  `W_healthz`;
- стаб с обоими умолчаниями 8765/5 с различается порождёнными портом и окном;
  положительный исход требует прочитать оба значения;
- `W_healthz=5 с` и `W_probe=15 с` названы раздельно, внешняя граница строго
  больше внутренней.

Ложных красных диагнозов в cold-start не найдено.

## Полный прогон обязательных арбитражных перечней

### `proksi-rotaciya-passthrough-live.md:80-151`

Минимум исполнен дословно.

- **Ротация:** отдельная ветвь (и), собственный secrets-файл через цепочку
  `--config`, один живой процесс с утверждаемым pid, T1/T2 одной роли R,
  строка с R после каждого успешного вызова, финальный 401 без роста журнала
  и красное стартового кэша названы (`contracts/005-avtonomija-proksi.md:125-130`).
- **Passthrough:** отдельные POST и GET имеют собственные порождённые пути и
  собственные красные предъявления; upstream фиксирует метод и путь, а также
  сверх минимума тело, content-type и request-id; ответ имеет нетривиальные
  статус, content-type и не-JSON байты; клиент сверяет ровно требуемую
  арбитражем тройку. POST-only стаб краснеет на GET, пересборка ответа — на
  POST и GET (`contracts/005-avtonomija-proksi.md:89-106`).
- **Live-коррелятор:** схема содержит `request_id`; положительная проба требует
  восстановимый из строки коррелятор, ожидаемую роль и свежий `ts`;
  отрицательный прямой маршрут возвращает 1
  (`contracts/005-avtonomija-proksi.md:36-40,69-76,151-156`).
- **Четыре неповторённых хвоста:** grep охватывает весь репозиторий (`:123-124`);
  известные порождённые usage/тариф дают ненулевой точный usd (`:89-99`);
  полная схема и отсутствие строк unknown/unpriced закреплены (`:36-45,
  87-99,117-124`); workshop вынесен в команду cold-start (`:60-68,157-172`).

### `proksi-cold-start.md:116-151`

Минимум исполнен дословно.

- (3а): порождённый фатально битый конфиг, ненулевой код погибшего ребёнка,
  код лаунчера 1 с названной причиной, полный настенный замер не больше
  `W_probe` и собственное красное отсутствия надзора за гибелью (`:160-163`).
- (3б): ребёнок жив, healthz молчит до конца `W_healthz`, код 1 с названной
  причиной, полный замер между `W_healthz` и `W_probe`, красное без общего
  колпака и дополняющее красное раннего возврата (`:163-167`).
- Два числа раздельны и согласованы: `W_healthz=5 с`, `W_probe=15 с`, внешнее
  строго больше внутреннего (`:171-172`).
- Недоступный upstream не используется как неуспех запуска: прокси остаётся
  здоров, а ошибка закреплена как пер-запросный 502 со строкой (`:64-68,
  117-119`).

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Конкретного состояния дерева, где все
   объявленные наблюдаемые исходы и их красные предъявления сходятся, а
   предмет не сделан, не найдено. [К8-1] закрыта без ослабления предмета:
   красное пересборки сохранено на POST, POST-only краснеет на GET.
2. **«Готово» недоказуемо названной командой?** Не найдено. Selftest,
   основной барьер, live, cold-start, antiplacebo/CI-parity и живая регрессия
   имеют команды с кодом возврата (`contracts/005-avtonomija-proksi.md:78-174,
   186-193`). CI-исключение ограничено selftest и снабжено причиной.
3. **Решение молча оставлено исполнителю?** Неназванного решения, меняющего
   наблюдаемый контракт, не найдено. Оставленные варианты реализации прямо
   ограничены исходами: инъекция времени, способ корреляции при недоступном
   заголовке, устройство лаунчера и фикстур.
4. **Границы исполнения не названы?** Названы. Architect владеет контрактом,
   прокси, барьером, fixture-деревом, metering-конфигом, рольной проводкой и
   `.gitignore`; implementer — package/CI, `workshop` и `.omp/models.yml`
   (`contracts/005-avtonomija-proksi.md:176-184`). Необходимого пути вне зон
   не найдено; механизм границ — `scripts/check_zones.sh`.
5. **Противоречие `AGENTS.md`?** Не найдено. Скриптам не предписана печать
   PASS; пороги имеют область и команды с кодом; пятнадцать ветвей
   пересчитаны полным именным списком, а не той же командой; каждое правило
   имеет механизм либо объявленную границу; после [К8-1] все названные красные
   предъявления говорят правду о наблюдаемом дефекте.

## Находки

Блокирующих находок и советов нет. Оснований для `FAIL` или `ESCALATE` нет;
заморозка разрешена.
