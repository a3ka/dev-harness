FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` на HEAD
`ac87a3d35e12cc00585126a7eb4c8f461ab7749e`, пятая итерация до первой заморозки.
Предмет закоммичен (blob `ca650f9ccc3cf349db62a18eaa605b9ec4784450`);
тегов `frozen/contracts/005/*` нет.

Набор артефактов полный. Предмет назван в
`contracts/005-avtonomija-proksi.md:7-74`, критерий готовности и команды — в
`:76-121,133-140`, два исполнителя и их границы — двумя `ЗОНА`-строками в
`:123-131`. В отдельном клоне на судимом HEAD `bash scripts/check_zones.sh`
вернул 0 по четырём действующим замороженным контрактам; контракт 005 до
заморозки в проверяемый диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта
`01b4dc1`, судимый текст `7dbbd4e`, восемь блокирующих находок. Круг 2:
коммит вердикта `b67dd2f`, судимый текст `a23a6b1`, семь блокирующих находок;
повторённые причины ротации, passthrough и live-коррелятора переданы арбитру.
Их обязательный минимум записан в
`verdicts/arbitration/proksi-rotaciya-passthrough-live.md` (`4c03aa7`). Круг 3:
коммит вердикта `f61e535`, судимый текст `4e6d3b8`, четыре блокирующие
находки; повтор cold-start передан арбитру. Его обязательный минимум записан
в `verdicts/arbitration/proksi-cold-start.md` (`4cb0c7a`). Решённые причины
заново не открываются; ниже по ним предъявлены только буквальные и
предметные несоответствия обязательным перечням.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:87-95` — обязательный минимум
passthrough по-прежнему разложен между разными вызовами вместо двух
составных исходов, предписанных
`verdicts/arbitration/proksi-rotaciya-passthrough-live.md:101-114`. Арбитраж
требует, чтобы КАЖДЫЙ из отдельных GET и POST одновременно имел свой
порождённый путь, сверку принятых upstream метода/пути и сверку нетривиальной
тройки ответа. Ветка (в) сверяет upstream POST/путь, но требует только 200 и
строку журнала, а не нетривиальную тройку; (в2) сверяет upstream GET/путь, но
не тройку; (в3) сверяет тройку для клиентского GET, но не требует сверить
принятые upstream метод и путь именно этого вызова.
ОБХОД: в (в) доставлять POST и путь честно, но JSON-ответ upstream
разбирать и пересобирать перед клиентом; в (в2) честно доставлять GET и путь;
в (в3) клиентский GET превращать для upstream в POST либо менять путь, а
подставной upstream настроить вернуть ту же нетривиальную тройку независимо
от метода и пути. Все исходы `:87-95` и зонтичная команда сходятся, однако
POST-ответ не возвращён байт-в-байт, а passthrough-вызов (в3) не доказал
дословную доставку метода/пути; предмет `:17-20` и обязательный минимум не
сделаны.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:24-33,103-105` — ветвь ротации
не фиксирует два условия обязательного минимума
`verdicts/arbitration/proksi-rotaciya-passthrough-live.md:86-97`: T1 должен
порождаться в СОБСТВЕННОМ файле секретов фикстуры, доступном через цепочку
`--config`, а T2 должен быть приписан той же роли. Контракт, напротив,
называет фиксированный файл дома секретов и оставляет роли как R1/R2 без
равенства. Один процесс, строки обеих успешных попыток, отзыв T1 и красное
против стартового кэша названы, но решение о файле и равенстве роли молча
оставлено исполнителю.
ОБХОД: фикстура делает резервную копию реального
`~/.config/dev-harness/secrets.env`, пишет туда T1 для R1, затем T2 для
другой роли R2, а после прогона восстанавливает файл; прокси всегда игнорирует
любой путь secrets из `--config` и читает только фиксированный HOME. Строки
R1/R2, принятие T1/T2, финальный 401, тождество процесса и все коды сходятся,
но собственный файл фикстуры через конфигурационную цепочку и ротация токена
той же роли, буквально назначенные арбитром, не предъявлены.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:67-74,107-108` — минимум
live-коррелятора исполнен не дословно: арбитраж
`verdicts/arbitration/proksi-rotaciya-passthrough-live.md:121-124` оставляет
обязательными коррелятор, роль И свежий `ts`. Текущий положительный исход
требует только коррелятор и роль, а `:73-74` прямо исключает роль+ts как
отдельный критерий, не закрепляя свежесть `ts` в составной сверке.
ОБХОД: после порождения uuid сама фикстура до живого вызова дописывает в
`calls.jsonl` старую строку с этим коррелятором и ожидаемой ролью, затем
посылает живой вызов маршрутом мимо прокси. Положительная ветвь находит
коррелятор+роль и возвращает 0; прямой маршрут новой строки не рождает и даёт
1. Живой вызов не учтён, но все названные исходы `:107-108` выполнены; проверка
свежего `ts` отличила бы старую строку, как и требует обязательный минимум.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:109-119` — обязательный минимум
cold-start требует ДВА разных ЧИСЛА, оба названных в контракте, и внешнюю
границу строго больше внутреннего
(`verdicts/arbitration/proksi-cold-start.md:137-141`). В контракте число есть
только у `W_healthz` (5 с); `W_probe` — без численного значения, записано лишь
неравенство. Фраза «именованные числа» не создаёт второго числа и оставляет
исполнителю величину внешнего допуска.
ОБХОД: исполнитель выбирает `W_probe = 60 с`, а фикстура проверяет только код
1 и полное время не больше этих 60 с; лаунчер игнорирует `W_healthz = 5 с` и
возвращает отказ молчащему живому ребёнку через 59 с. Условие
`W_probe > W_healthz`, оба кода 1 и внешний замер фикстурой сходятся, но
предметное ожидание по внутреннему пятисекундному окну `:60-62,116-119` не
выполнено. Именно такой недифференцированный длинный таймаут был обходом,
ради которого арбитр потребовал два заранее названных числа.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:109-120` — для под-исходов (3а)
и (3б) названа только зелёная зонтичная команда и замер времени. У каждого
нет собственного красного предъявления, хотя это дословная часть минимума
`verdicts/arbitration/proksi-cold-start.md:121,143-147`. Общая формула
`npm run check:antiplacebo` не говорит, что она валит отдельно лаунчер,
наблюдающий гибель ребёнка без колпака, и лаунчер с колпаком без наблюдения
гибели. Это прямое противоречие `AGENTS.md:162-163` («проверка не готова,
пока не предъявлена КРАСНОЙ»); по исключению роли отдельный `ОБХОД` для
противоречия старшей норме не требуется.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:17-23,87-95` — НОВАЯ причина,
не являющаяся повтором двух арбитражей: предмет обещает OpenAI-совместимый
ретранслятор любых GET/POST-путей, но ни один исход не требует, чтобы тело
POST-запроса дошло до upstream. Ветка (в) сверяет только метод/путь upstream,
его ответ и журнал; остальные passthrough-ветви — GET. Решение, пересылать ли
тело и сохранять ли его байты/content-type, молча оставлено исполнителю.
ОБХОД: `metering_proxy.ts` для любого POST отправляет upstream пустое тело
`{}` независимо от клиентского запроса; подставной upstream ветви (в)
проверяет только метод и путь и всегда возвращает заданные fixture usage.
Все одиннадцать ветвей, selftest, live/cold-start и приёмка могут вернуть 0,
но реальные OpenAI POST-запросы теряют сообщения и параметры, то есть
объявленный ретранслятор не работает.

Проверка четырёх находок круга 3 по существу:

1. Счёт закрыт: `contracts/005-avtonomija-proksi.md:81-83` называет одиннадцать
   и перечисляет ровно одиннадцать ветвей.
2. Непосредственные недостающие слова добавлены: (в) фиксирует upstream
   POST/путь (`:87-90`), (в2) — GET/путь (`:91-92`), (в3) назван GET
   (`:93-95`). Но обязательный состав арбитража «для каждого вызова
   метод+путь+тройка» не закрыт; обход дан в первой находке.
3. Строки успешной ротации с ролями R1/R2 появились (`:103-105`). Однако
   полный минимум ротации про собственный файл через `--config` и ту же роль
   не закрыт; обход дан во второй находке.
4. Негодный мёртвый upstream исключён как причина гибели (`:62-65`), появились
   фатальный invalid JSON, живой молчащий healthz и коды 1 (`:109-117`). Но
   численное значение `W_probe` и два отдельных красных предъявления не
   вошли; минимум cold-start исполнен не полностью.

Полный перечень `proksi-rotaciya-passthrough-live` прогнан повторно:

- **Ротация:** один процесс/тождество pid, T1/T2, обе строки с ролями,
  финальный 401 без роста и красное стартового кэша названы (`:29-33,81-84,
  103-105`); собственный secrets-файл через `--config` и та же роль не
  названы.
- **GET+POST/passthrough:** отдельные методы и порождённые пути появились, но
  метод+путь и нетривиальная тройка не собраны в КАЖДОМ из двух вызовов.
- **Live:** поле `request_id`, восстановимый коррелятор, роль и отрицательный
  прямой маршрут названы (`:34-38,67-74,107-108`); обязательный свежий `ts`
  не назван.
- Четыре неповторённые хвоста арбитража сохранены: grep всего репозитория
  (`:101-102`), известные usage/тариф и ненулевой целый usd (`:87-90`), полная
  схема и отсутствие строк для unknown/unpriced (`:34-41,86,100`), workshop
  вынесен в cold-start (`:58-66,109-119`).

Полный перечень `proksi-cold-start` прогнан повторно:

- (3а) фатальный invalid JSON, гибель на старте, возврат лаунчера 1 и замер
  фикстурой названы (`:112-114`); ненулевой код самого погибшего ребёнка не
  зафиксирован явно.
- (3б) живой молчащий healthz, отказ по `W_healthz` и код 1 названы
  (`:114-117`).
- Недоступный upstream прямо оставлен пер-запросным 502 и не является
  причиной гибели (`:62-65`).
- Внешнее окно названо `W_probe` и объявлено больше внутреннего, но второго
  численного значения нет (`:117-119`), а полное время каждого под-исхода не
  привязано отдельной формулой `≤ W_probe`.
- Собственные красные предъявления (3а)/(3б) отсутствуют, несмотря на
  буквальный минимум арбитража.

Пять обязательных вопросов:

1. **Критерий слабее предмета?** Да. Пересборка POST-ответа, подмена
   upstream-метода в (в3), отбрасывание тела POST, старая live-строка и
   59-секундный cold-start дают конкретные состояния, где команды сходятся,
   а предмет не сделан.
2. **«Готово» недоказуемо названной командой?** Да. Команды не доказывают
   составной passthrough для каждого метода, тело POST, свежесть live-строки и
   численную внешнюю границу; cold-start не имеет двух предписанных красных
   предъявлений.
3. **Решение молча оставлено исполнителю?** Да. Оставлены файл секретов
   ротации, равенство R1/R2, пересылка тела POST, численное `W_probe` и точный
   состав cold-start-антиплацебо.
4. **Границы исполнения не названы?** Названы. Architect владеет контрактом,
   прокси, барьером, fixture-деревом и конфигом; implementer — package/CI,
   `workshop` и `.omp/models.yml` (`:123-131`). Прямо необходимого нового
   файла вне зон не найдено; существующий `scripts/check_skills.sh` в
   приёмке только запускается и менять его контракт не требует.
5. **Противоречие `AGENTS.md`?** Да: отсутствие отдельных красных
   предъявлений (3а)/(3б) противоречит `AGENTS.md:162-163`. Печать `PASS` не
   требуется; счёт одиннадцати теперь непротиворечив; CI-исключение названо с
   причиной.

Новый блокер про тело POST предъявлен впервые, поэтому триггера второго FAIL
по одной новой причине нет. По уже решённым причинам новый арбитраж не
созывается: обязательные минимумы двух имеющихся решений просто не исполнены
дословно.

Вердикт `FAIL`: шесть действительных блокирующих находок. Пять имеют
конкретные обходы; шестая блокирует как прямое противоречие `AGENTS.md` по
предусмотренному ролью исключению. Вопросов воли или денег для `ESCALATE`
нет.
