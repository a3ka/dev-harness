FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` в коммите
`9791d0369c655da99d25e095d440211bcb2591a8`, шестая итерация до первой
заморозки. Предмет закоммичен (blob
`2f0cf6fa4bd8de7924a1cc7e1a5d1dde8c2f73b5`); тегов
`frozen/contracts/005/*` нет. После начала суда основной HEAD сдвинулся
независимым коммитом `6101e7b`, изменившим только `NABLIUDENIA.md`; blob
судимого контракта остался тем же.

Набор артефактов полный. Предмет назван в
`contracts/005-avtonomija-proksi.md:7-76`, критерий готовности и команды — в
`:78-132,144-151`, два исполнителя и границы — двумя `ЗОНА`-строками в
`:134-142`. В отдельном клоне судимого HEAD `bash scripts/check_zones.sh`
вернул 0 по четырём действующим замороженным контрактам; контракт 005 до
заморозки в проверяемый диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта
`01b4dc1`, судимый текст `7dbbd4e`, восемь блокирующих находок. Круг 2:
коммит вердикта `b67dd2f`, судимый текст `a23a6b1`, семь блокирующих находок;
повторённые причины ротации, passthrough и live-коррелятора переданы арбитру
и решены в `verdicts/arbitration/proksi-rotaciya-passthrough-live.md`
(`4c03aa7`). Круг 3: коммит вердикта `f61e535`, судимый текст `4e6d3b8`,
четыре блокирующие находки; повтор cold-start передан арбитру и решён в
`verdicts/arbitration/proksi-cold-start.md` (`4cb0c7a`). Круг 4: коммит
вердикта `546e8e0`, судимый текст `ac87a3d`, шесть блокирующих находок
[К4-1]–[К4-6]. Решённые причины заново не открываются: ниже несоответствия
арбитражам предъявлены как буквальное неисполнение уже обязательного
минимума.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:19-21,90-100` — новая причина:
предмет требует дословно донести до upstream в том числе клиентский
`content-type`, но составной POST лишь ПОРОЖДАЕТ это значение; upstream-счётчик
сверяет метод, путь, тело и `request_id`, не сверяя принятый request
`content-type`. GET-счётчик ещё уже: сверяет только метод и путь. Проверяемый
`content-type` ответа в `:94-100` — другое направление и разрыв не закрывает.
ОБХОД: прокси всегда заменяет клиентский request `content-type` на
`application/json`, сохраняя метод, путь, тело и `x-request-id`; подставной
upstream фиксирует ровно перечисленные в `:92-93` поля, а клиентская сторона
честно сверяет отдельный response `content-type`. Все десять ветвей и все
названные команды могут вернуть 0, но требование предмета о дословной
доставке request `content-type` не сделано.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:83-100` — обязательный минимум
passthrough исполнен не дословно. Арбитраж
`verdicts/arbitration/proksi-rotaciya-passthrough-live.md:101-114` требует не
просто по одному неназванному красному на ветвь, а прямо фиксирует два
обманных предъявления: POST-only ретранслятор и ретранслятор с пересборкой
ответа обязаны падать. Контракт говорит лишь, что у каждой ветви будет
«красный вызов собственной фикстуры», и ни в (в), ни в (в2) не называет эти
два стаба. Тем самым выбор того, что именно красить, снова оставлен автору
реализации, вопреки уже вынесенному решению.
ОБХОД: обе fixture краснеют на вырожденном прокси, который сразу отказывает,
а `check_metering.sh` не сверяет фактически принятый upstream метод и
побайтовый ответ; рабочий прокси при этом превращает GET в POST и пересобирает
ответ. Собственные красные у обеих ветвей существуют, зонтичная команда и
`check:antiplacebo` возвращают 0, однако именно два обязательных обманных
ретранслятора не предъявлены и passthrough предмета не сделан.

БЛОКИРУЕТ `contracts/005-avtonomija-proksi.md:123-130` — собственное красное
(3б) не является красным, предписанным cold-start-арбитражем. Обязательный
перечень `verdicts/arbitration/proksi-cold-start.md:143-147` требует, чтобы
(3б) валила лаунчер БЕЗ общего колпака времени, безгранично ждущий живого
молчащего ребёнка. Итерация 6 вместо него предъявляет противоположный дефект:
лаунчер, возвращающийся РАНЬШЕ `W_healthz`. Это полезная проверка нижней
границы, но она не заменяет обязательную проверку выхода по верхней границе.
ОБХОД: `check_metering.sh --cold-start` проверяет только `time >= W_healthz`
и код отказа, но не утверждает `time <= W_probe`; действующий лаунчер ждёт
живого молчащего ребёнка 60 секунд и затем возвращает 1. Зелёный прогон в
итоге возвращает 0, а единственная красная (3б), возвращающаяся раньше 5
секунд, честно краснеет; `check:antiplacebo` тоже возвращает 0. Лаунчер без
общего колпака ни разу не подставлен, и предметное окно 5 секунд не защищено.

Проверка шести находок круга 4 по существу:

1. [К4-1] Составные POST и GET теперь действительно находятся каждый в одной
   ветви; обе сверяют upstream метод+путь и нетривиальную тройку ответа
   (`:90-100`).
2. [К4-2] Ротация использует собственный файл секретов фикстуры через
   `--config`, не трогает реальный дом и приписывает T1/T2 одной роли R
   (`:107-113`).
3. [К4-3] Положительный live-исход требует коррелятор, ожидаемую роль и свежий
   `ts`, строго позже старта (`:114-116`).
4. [К4-4] Названы два числа: `W_healthz = 5 с`, `W_probe = 15 с`, и второе
   строго больше первого (`:128-130`).
5. [К4-5] Собственные красные (3а)/(3б) текстуально появились (`:123-128`),
   но (3б) проверяет ранний возврат вместо обязательного отсутствия общего
   колпака; находка закрыта по форме, не по полному арбитражному существу.
6. [К4-6] POST теперь сверяет тело байт-в-байт и `request_id` (`:90-96`), но
   добавленный в предмет request `content-type` не вошёл в upstream-сверку;
   обход дан в первой находке.

Полный перечень `proksi-rotaciya-passthrough-live` прогнан:

- ротация: собственный secrets-файл через `--config`, один pid, T1/T2 той же
  роли, обе строки, финальный 401 без роста и красное стартового кэша названы
  (`:107-113`);
- GET+POST: отдельные порождённые пути, принятые метод/путь и нетривиальная
  тройка каждого ответа названы (`:90-100`), но два конкретных красных стаба
  арбитража не названы;
- live: `request_id`, восстановимый коррелятор, роль, свежий `ts` и
  отрицательный прямой маршрут названы (`:36-40,67-76,114-116`);
- четыре неповторённых хвоста сохранены: grep всего репозитория (`:103-105`),
  известные usage/тариф и ненулевой целый usd (`:90-96`), полная схема и
  отсутствие строк unknown/unpriced (`:36-45,89,102`), workshop проверяется
  отдельным cold-start (`:58-66,117-130`).

Полный перечень `proksi-cold-start` прогнан:

- (3а) фатальный invalid JSON, ненулевой код погибшего ребёнка, возврат
  лаунчера 1 с причиной и внешний замер `<= W_probe` названы (`:120-125`);
- (3б) живой молчащий healthz, отказ 1, нижняя и верхняя границы замера
  названы (`:125-130`);
- недоступный upstream прямо оставлен пер-запросным 502 (`:61-65`);
- внутреннее и внешнее окна названы разными числами, внешнее больше
  внутреннего (`:128-130`);
- красное (3а) предъявляет отсутствие наблюдения гибели и превышение
  `W_probe` (`:123-125`); красное (3б) не предъявляет отсутствие общего
  колпака, буквально требуемое арбитражем.

Пять обязательных вопросов:

1. **Критерий слабее предмета?** Да: замена request `content-type` проходит
   upstream-сверку; вырожденные красные проходят буквальную формулу «свой
   красный», не предъявляя два обязательных passthrough-обмана; раннее (3б)
   не защищает верхний предел ожидания.
2. **«Готово» недоказуемо названной командой?** Да: команды не доказывают
   request `content-type` и два обязательных красных passthrough/cold-start.
3. **Решение молча оставлено исполнителю?** Да: сверять ли request
   `content-type`, какими стабами предъявлять passthrough и предъявлять ли
   лаунчер без общего колпака.
4. **Границы исполнения не названы?** Названы: architect владеет контрактом,
   прокси, барьером, fixture-деревом и конфигом; implementer — package/CI,
   `workshop` и `.omp/models.yml` (`:134-142`). Необходимого файла вне зон не
   найдено.
5. **Противоречие `AGENTS.md`?** Да по существу правила 3: неназванный
   «красный вызов» не гарантирует предъявление конкретной обманной заглушки;
   два обязательных стаба passthrough и стаб (3б) зафиксированы арбитрами, но
   контракт их не несёт. Печать PASS не требуется; десять перечисленных
   ветвей независимо пересчитаны как десять; CI-исключение названо с
   причиной.

Повторные причины уже решены двумя действующими арбитражами, поэтому нового
арбитра по ним не созываю: требуется дословно перенести их минимумы. Блокер
request `content-type` предъявлен впервые, триггера второго FAIL по этой
новой причине нет. Вопросов воли или денег для `ESCALATE` нет.

Вердикт `FAIL`: три действительные блокирующие находки, у каждой назван
конкретный обход.
