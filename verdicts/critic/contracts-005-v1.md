FAIL

Судимый предмет: `contracts/005-avtonomija-proksi.md` в коммите
`a2130d8686e51c12ce0f073659cdfb185888a2b7`, седьмая итерация до первой
заморозки. Предмет закоммичен (blob
`a0f3e5fa39c9cf9a01c57f86637ecd421aef85c8`); тегов
`frozen/contracts/005/*` нет.

Набор артефактов полный. Предмет назван в
`contracts/005-avtonomija-proksi.md:7-76`, критерий готовности и команды — в
`:78-140,152-159`, два исполнителя и границы — двумя `ЗОНА`-строками в
`:142-150`. В отдельном клоне судимого HEAD `bash scripts/check_zones.sh`
вернул 0 по четырём действующим замороженным контрактам; контракт 005 до
заморозки в проверяемый диапазон ещё не входит.

История кругов сохранена при перезаписи файла. Круг 1: коммит вердикта
`01b4dc1`, судимый текст `7dbbd4e`, восемь блокирующих находок. Круг 2:
коммит вердикта `b67dd2f`, судимый текст `a23a6b1`, семь блокирующих находок;
повторённые причины ротации, passthrough и live-коррелятора решены в
`verdicts/arbitration/proksi-rotaciya-passthrough-live.md` (`4c03aa7`). Круг
3: коммит вердикта `f61e535`, судимый текст `4e6d3b8`, четыре блокирующие
находки; повтор cold-start решён в
`verdicts/arbitration/proksi-cold-start.md` (`4cb0c7a`). Круг 4: коммит
вердикта `546e8e0`, судимый текст `ac87a3d`, шесть блокирующих находок
[К4-1]–[К4-6]. Круг 5: коммит вердикта `f2cf684`, судимый текст `9791d03`,
три блокирующие находки [К5-1]–[К5-3]. Круг 6 — настоящий вердикт по
`a2130d8`.

Три блокера круга 5 закрыты по существу:

1. [К5-1] В составном POST теперь порождается request `content-type`, отличный
   от `application/json`, а upstream-счётчик сверяет дословно все пять полей:
   метод, путь, тело байт-в-байт, REQUEST content-type и `request_id`
   (`contracts/005-avtonomija-proksi.md:90-96`).
2. [К5-2] Оба обязательных обманных предъявления названы поимённо: стаб
   POST-only, превращающий GET в POST, и стаб, пересобирающий ответ из usage;
   их красный исход закреплён в составных ветвях POST/GET
   (`contracts/005-avtonomija-proksi.md:98-105`).
3. [К5-3] Собственное красное (3б) теперь предъявляет именно лаунчер без
   общего колпака, безгранично ждущий живого молчащего ребёнка; внешняя
   фикстура валит его по `W_probe`, а дополняющее красное раннего возврата
   сохранено (`contracts/005-avtonomija-proksi.md:132-140`).

Полный обязательный перечень арбитража
`proksi-rotaciya-passthrough-live.md` прогнан:

- ротация: собственный secrets-файл через `--config`, один живой pid, T1/T2
  одной роли R, обе строки, финальный 401 без роста журнала и красное
  стартового кэша названы (`contracts/005-avtonomija-proksi.md:113-118`);
- passthrough: отдельные составные POST и GET имеют порождённые пути, сверку
  принятых upstream метода/пути, нетривиальную порождённую тройку ответа и
  клиентскую байтовую сверку; POST-only и пересборка названы красными
  предъявлениями (`contracts/005-avtonomija-proksi.md:90-105`);
- live: схема содержит `request_id`; положительный исход требует
  восстановимый коррелятор, ожидаемую роль и свежий `ts`, отрицательный
  прямой маршрут возвращает 1 (`contracts/005-avtonomija-proksi.md:36-40,
  69-76,119-121`);
- четыре неповторённых хвоста сохранены: grep всего репозитория
  (`:111-112`), известные usage/тариф и ненулевой целый usd (`:90-98`),
  полная схема успешной строки и отсутствие строк unknown/unpriced
  (`:36-45,89-98,110`), отдельный cold-start workshop (`:60-68,122-140`).

Полный обязательный перечень арбитража `proksi-cold-start.md` прогнан:

- (3а) использует фатальный invalid JSON, фиксирует ненулевой код погибшего
  ребёнка, возврат лаунчера 1 с причиной и полное время не больше `W_probe`;
  красное не наблюдает гибель ребёнка и превышает внешнюю границу
  (`contracts/005-avtonomija-proksi.md:122-131`);
- (3б) держит ребёнка живым и healthz молчащим, требует код 1 с причиной,
  полное время между `W_healthz` и `W_probe`; красное без общего колпака
  валится внешней границей (`contracts/005-avtonomija-proksi.md:131-138`);
- мёртвый upstream прямо оставлен пер-запросным 502, а не причиной гибели
  старта (`contracts/005-avtonomija-proksi.md:65-68`);
- два числа названы раздельно: `W_healthz = 5 с`, `W_probe = 15 с`, внешнее
  строго больше внутреннего (`contracts/005-avtonomija-proksi.md:138-140`).

Ниже — новые разрывы предмета и критерия. Они не меняют вывод о том, что оба
арбитражных минимума и три [К5-N] перенесены дословно.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:9-11,78-140 — предмет требует
прокси без внешних зависимостей, только на `node:` builtins, но ни один пункт
критерия не проверяет состав импортов/запускаемых программ и не предъявляет
красное против внешней зависимости. Это также прямое противоречие
`AGENTS.md:164-169`: правило осталось без механизма и красного предъявления.
ОБХОД: положить стороннюю HTTP-библиотеку в `scripts/proxy/vendor/` и
импортировать её из `metering_proxy.ts` либо вызывать из него внешний `curl`;
реализовать на ней все названные HTTP-исходы. Selftest, десять ветвей,
live/cold-start и вся приёмка возвращают 0, но прокси состоит не только из
`node:` builtins и заявленная автономия не сделана.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:17-21,101-105 — требование
дословной доставки клиентского запроса перечисляет пять полей без исключения
для GET, однако составной GET сверяет upstream только метод и путь. В отличие
от POST, он не сверяет тело, request content-type и `x-request-id`. Это не
расширение арбитражного минимума: минимум GET действительно требует лишь
метод/путь и тройку ответа; разрыв возник с более сильным собственным
требованием [К4-6] в разделе «Предмет».
ОБХОД: для POST дословно пересылать все пять полей, а для GET удалять тело,
request content-type и `x-request-id` перед upstream; `request_id` строки
журнала брать непосредственно из клиентского запроса. Ветвь (в) сверяет все
пять и проходит, ветвь (в2) видит правильные GET+путь, полную строку и
нетривиальный ответ, оба обязательных обманных стаба краснеют, все команды
возвращают 0 — но GET-запрос до upstream не дошёл дословно.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:36-43,107 — предмет утверждает,
что ВСЯ схема обязательна и сверяется для строки каждого вызова, включая
неуспешный. Ветвь обрыва upstream сверяет лишь наличие строки, статус ошибки и
нулевые tokens/usd; `role, provider, model, path, latency_ms, request_id` для
этой строки не названы в исходе.
ОБХОД: успешные ветви писать полной схемой, а при обрыве upstream добавлять
JSON-строку только с `status`, `tokens_in`, `tokens_out`, `usd`. Ветка (д)
проходит дословно, нулевая сумма не мешает свёртке бюджета, прочие ветви и
приёмка возвращают 0, но строка неуспешного вызова не имеет обязательной
схемы журнала.

БЛОКИРУЕТ contracts/005-avtonomija-proksi.md:55-59,106 — предмет закрепляет
границу «потолок ДО запроса, списание ПОСЛЕ» и разрешённый записанный
перерасход на один вызов. Критерий проверяет только уже исчерпанный потолок;
вызова, который начинается ниже потолка и пересекает его известной стоимостью,
нет.
ОБХОД: при бюджете ниже потолка послать запрос upstream, после получения
usage вычислить пересечение потолка, заменить ответ провайдера на 402 и не
записывать стоимость. Для составной ветви (в) задать достаточный потолок, а
для (г) — уже исчерпанный; обе и все остальные команды возвращают 0. Но
объявленный единственный перерасход не возвращён клиенту и не записан, то есть
граница «списание ПОСЛЕ» не реализована.

Пять обязательных вопросов:

1. **Критерий слабее предмета?** Да. Сторонняя библиотека, усечённый GET,
   неполная строка ошибки и подавление единственного перерасхода дают четыре
   конкретных состояния, где все команды сходятся, а предмет не сделан.
2. **«Готово» недоказуемо названной командой?** Да. Ни одна команда не
   различает builtins-only от внешней зависимости, не сверяет пять полей
   GET-запроса, полную схему строки обрыва и переход бюджета через потолок.
3. **Решение молча оставлено исполнителю?** Да: допустимый граф зависимостей,
   семантика GET-заголовков/тела, схема ошибочной строки и поведение ровно на
   пересечении потолка не закреплены различающими исходами.
4. **Границы исполнения не названы?** Названы. Architect владеет контрактом,
   прокси, барьером, fixture-деревом, конфигом и игнором; implementer —
   package/CI, `workshop` и `.omp/models.yml`
   (`contracts/005-avtonomija-proksi.md:142-150`). Все файлы, необходимые для
   закрытия названных разрывов, уже попадают в эти зоны; необходимого пути вне
   зон не найдено.
5. **Противоречие `AGENTS.md`?** Да: builtins-only не имеет механизма и
   красного предъявления (`AGENTS.md:164-169`). Печать PASS не требуется;
   десять ветвей независимо пересчитаны как десять; CI-исключение названо с
   причиной; счётное утверждение не доказывается повтором той же команды.

Новые причины выше ранее не были предметом несогласия сторон; Н-38 не требует
арбитража при принятой автором находке. Решённые причины двух действующих
арбитражей заново не открыты: их полный обязательный минимум исполнен.
Вопросов воли, риска или денег для `ESCALATE` нет.

Вердикт `FAIL`: четыре действительные блокирующие находки, у каждой назван
конкретный обход.
