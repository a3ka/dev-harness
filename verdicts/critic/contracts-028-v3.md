accept

## Предмет и граница

Только две добавленные строки `contracts/028-home-sessii-vne-dereva.md:136,140`
на HEAD `9dd59e988e450b948f4b11c5372a13d62e4d7bdb`:
`СПАСЕНО architect: …` и `ЗОНА arbiter: verdicts/arbitration/`.
База — `frozen/contracts/028/2`, блоб
`232292d1a23a700002b68b0fbe7719a2fc16b8d2`; новый блоб —
`5d1c77dc3685d2465ccc09438f117c1eff3170f6`.
Слот v3 означает версию заморозки, а не пересуд всей приёмки.
Предмет, rc-команды и названные исполнители присутствуют в закоммиченном
контракте. Разрешения в телах `bd3853f` и land-merge `9dd59e9` прочитаны:
обе строки `РАЗРЕШИЛ-ВЛАДЕЛЕЦ:` разрешают именно эту двухстрочную дельту.

## Результат узкого суда

Нового ОБХОДА в этих двух строках не найдено. Блокирующих находок и советов нет.

1. `contracts/028-home-sessii-vne-dereva.md:136`: исключение из суда зон
   ограничено двумя существующими полными SHA, а не будущими коммитами
   architect. Автор объявлен строкой :135; причина непуста, разделитель
   `—` на месте, оба SHA находятся в диапазоне первой заморозки..HEAD.
   Проверены дельты самих коммитов: `dad01175` добавляет шесть проб;
   `8662a66` переносит четыре пробы R100 и меняет контракт с наблюдениями.
   Старая зона `fixtures/workshop/` действительно объявлена в v1.
   Снятие ретроактивного отказа не возвращает эту зону для новых коммитов.
2. `contracts/028-home-sessii-vne-dereva.md:140`: объявлен исполнитель arbiter
   и каталог с завершающим `/`, без шаблонов и выхода из дерева. Единый
   читатель `lib_zones.sh` распознаёт строку. Оба пути переименования
   `ea9df8a` принадлежат этому каталогу; старый и новый блобы равны:
   `3221cb0fdc5fa264703ad400284df99ecb865134`. Это проверка содержимого,
   не подмена её именем автора (Н-101).
3. По пяти вопросам гейта, только в пределах дельты: предмет и критерий не
   ослаблены; rc-команды не изменены; нового решения на усмотрение исполнителя
   нет; границы объявлены и читаются существующим механизмом; противоречия
   AGENTS.md не обнаружено. Достаточность прежней приёмки не пересуживалась.

## Исполненное доказательство

Собственный клон `/tmp/dev-harness-verify/critic-028-v5/repo`, HEAD строго
`9dd59e9`. Команда:

    python3 /tmp/dev-harness-verify/critic-028-v5/proverka.py

Код 0. Проверка сравнила именно git-блобы: ровно **2 добавленные строки,
0 удалённых**; после удаления этих двух строк новый блоб побайтово равен
v2. Ничего третьего в тексте контракта не изменено.

Внутри прогона создан только локальный тег `frozen/contracts/028/3` на
судимый HEAD, выполнен живой `bash scripts/check_zones.sh` — **rc 0**.
В выводе названы зона `arbiter → verdicts/arbitration/` и обе записи:

    контракт 028: коммит 8662a661 (architect) — СПАСЕНО, из суда зон выведен
    контракт 028: коммит dad01175 (architect) — СПАСЕНО, из суда зон выведен

Строк FAIL нет; сообщённые до исправления 8 отказов architect×fixtures/workshop
в этом прогоне отсутствуют. Красный исходный прогон принят из задания,
повторно не запускался. Сырой вывод:
`/tmp/dev-harness-verify/critic-028-v5/zones.log`.

Дополнительно `bash /tmp/dev-harness-verify/critic-028-v5/match-zones.sh` —
rc 0: `zones_match_path` разрешает arbiter оба пути `ea9df8a` и отказывает
для `workshop`. Это отдельное доказательство совпадения с зоной: сам
`check_zones.sh` исключает процессные `verdicts/*`, поэтому его зелёность
сама по себе не доказывала бы применение префикса к арбитражному файлу.

    git diff --exit-code frozen/contracts/028/2 9dd59e9 -- scripts/check_zones.sh scripts/lib_zones.sh AGENTS.md

Код 0: механизмы суда зон и норма не менялись. Локальный тег `/3` удалён
в `finally`; отсутствие тега проверено. Постоянной заморозки этот суд не ставил.

## Раскрытия

Н-98 — благословлённый самотрип, канарейка не перегонялась. CI на судимом
HEAD раскрыт в задании как находящийся в полёте; результат CI этим судьёй
не подтверждается. Полная батарея и реализация 028 вне этого двухстрочного
суда. Узкий accept не объявляет миграцию выполненной.
