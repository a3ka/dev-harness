FAIL

Причина: критерий допускает обнуление меры Н-104 одной перемаркировкой, не закрывает неизвестный класс и не доказывает readonly-безопасность переисполнения. Три блокирующие находки с конкретными обходами ниже.

Предмет: `contracts/029-auto-konsultant-forkov.md` целиком, 202 строки, и все девять красных предъявлений на HEAD `df77102738d4df801bd5212fc4134b110896e30c`.
МОДЕЛЬ: openai-codex/gpt-6-astra
Круг: 1. Суд текста контракта и красных предъявлений, НЕ отсутствующей реализации. Арбитраж этим вердиктом не созывается.
Набор артефактов полный: предмет закоммичен, критерий и команды названы, исполнители и ЗОНА-строки присутствуют.

## Блокирующие находки

### 1. Закрытый словарь не защищает от легальной перемаркировки; счётчик наследует эту дыру

БЛОКИРУЕТ contracts/029-auto-konsultant-forkov.md:33 — инв. 2 обещает закрыть возвращение инженерного форка владельцу, но проверяет только самодекларацию класса и токен. Инв. 5 (:56–62) и close-out (:173–178) считают ту же самодекларацию мерой устранения передаточных касаний.
ОБХОД: оставить восемь записей измеренного дня с `МАРШРУТ: батч`, в пяти передаточных заменить класс на `КЛАСС: воля-владельца` и поставить легальное `ПРИЗНАК: норма`. Например, для `razreshil-026027` вопрос остаётся «как оформить уже разрешённые утром строки», но маркируется нормой; для `ascii-vs-mini` инженерная конвенция объявляется нормой. Консультант не вызывается, пять передаточных вопросов по-прежнему идут человеку. Буквальный инв. 2 принимает записи, буквальный инв. 5 печатает `просмотрено 8`, `передаточных 0`: живая мера close-out выполнена без достижения предмета.

`fixtures/check_fork_route/red_marshrut_b_volja_batch.sh:39` предъявляет только НЕЛЕГАЛЬНЫЙ токен `инженерно-очевидно`, отсутствие признака и неверный маршрут. Входа «инженерный вопрос + легальная норма/риск» нет. `red_schet_peredatochnyh.sh` показывает смену маршрутов, но не отличает её от указанной перемаркировки. Ни одна существующая проба не требует независимого основания для такого исключения.

Проверено отдельным вычислением именно формулы контракта, не прогоном отсутствующего барьера: исходные классы дают N=8/M=5; изменение только пяти классов/признаков даёт N=8/M=0; все восемь маршрутов остаются батч. Исходный замер Н-104 (`NABLIUDENIA.md:118–129`) считал касания по трейсу/HANDOFF/IRC, здесь независимой сверки с касаниями нет. Это также конфликт с правилом AGENTS «счётное утверждение проверяется ДРУГОЙ мерой».

Нужно закрыть обход наблюдаемым основанием исключения и красным предъявлением с легальным токеном либо явно отделить когнитивную классификацию от доказанной меры закрытия Н-104. Требовать от машины понимания смысла вопроса не предлагается: AGENTS прямо ограничивает такую претензию. Простого расширения словаря недостаточно.

### 2. Исчерпываемость есть только для заранее корректного значения КЛАСС

БЛОКИРУЕТ contracts/029-auto-konsultant-forkov.md:25 — для присутствующего, но неизвестного/пустого значения `КЛАСС` не задан отказ; default-open (:28) применяется только при полном отсутствии поля. Красные входы а/б/в не предъявляют этот четвёртый случай.
ОБХОД: реализация обрабатывает три известных класса, отсутствующий относит к инженерному, а ветку неизвестного класса пропускает. На всех предъявленных значениях она неотличима от правильной. Запись `ФОРК: fourth`, `КЛАСС: иной`, `ПРИЗНАК: норма`, `МАРШРУТ: батч`, с корректным свежим `ЗАВЕДЁН`, проходит маршрутизатор без обращения к консультанту; `--schet` считает её одной передаточной, то есть N=1/M=1 всё ещё удовлетворяет порогу close-out. Предъявленный критерий не отличает такой реализации от исчерпывающего классификатора.

Это контрмодель критерия, НЕ утверждение о поведении ещё не написанного скрипта. Норма AGENTS о неразобранном объявлении требует поимённого отказа, а не молчаливого пропуска. Нужен такой исход в контракте и исполняемый красный вход.

Для корректной одиночной строки класса три ветви взаимоисключающие. Перечни признаков б/в действительно не пересекаются: четыре и шесть разных токенов. На этом уровне пересечения нет; дефект — незакрытая область входа, не пересечение словарей.

### 3. Разрешённый глагол не делает команду readonly

БЛОКИРУЕТ contracts/029-auto-konsultant-forkov.md:90 — перечислены глаголы и запрещены метасимволы, но не ограничены записывающие аргументы разрешённых команд и происхождение запускаемых `scripts/check_*.sh` / `scripts/verify_*.sh`. Это слабее собственного обещания «барьер не становится исполнителем чужого кода» (:94–95).
ОБХОД: барьер корректно разбирает argv, отвергает `rm` и `;`, но разрешает `git diff --output=written-by-allowed-command.txt`, поскольку `git diff` в списке, а запрещённых метасимволов нет. Консультант предъявляет честные RC=0 и SHA256 пустого stdout+stderr; сверка проходит, хотя команда создаёт/перезаписывает файл. Все входы `red_lozhnyj_rc.sh` остаются прежними: опасные входы там только `rm -rf verdicts` и `git status; rm -rf verdicts`, записывающего аргумента разрешённого глагола нет. Следовательно, переисполнение может быть честным по rc/sha и одновременно нарушать заявленную границу readonly.

Живое доказательство в отдельной игрушке внутри allowlist: индекс содержит `tracked.txt` со строкой `before`, рабочий файл — `after`; команда `git diff --output=written-by-allowed-command.txt` вернула rc=0, stdout+stderr пусты, создала файл 132 байта с патчем `+after`. Ни контракт, ни файлы предмета для эксперимента не менялись. Это свойство допустимого языка команд, а не ревью реализации.

Нужна проверяемая граница аргументов/исполнения и красный вход на побочный эффект допустимого глагола. Аналогично, glob-имя shell-скрипта само по себе не удостоверяет readonly-содержимое скрипта в проверяемом корне.

## Остальные обязательные проверки и советы

**Q2 действительно стал механизмом, а не прежней прозой.** Инв. 8–9 предписывают собственное переисполнение и сравнение двух результатов. В `red_deko_bez_rc.sh` есть отсутствие блока, пустой блок и честные две тройки; в `red_lozhnyj_rc.sh` отдельно ложный sha на грязном дереве, ложный rc и честный контроль на том же дереве. Сверка не ограничена наличием полей. Перечня достаточно для честных команд: в игрушке получены тройки для `git status --porcelain` (rc=0) и `git rev-parse --verify refs/heads/absent` (rc=128), с sha256 stdout+stderr после удаления завершающих LF. Честная команда чтения ВНЕ перечня, например `head -n 1 README.md`, по явно заданному языку должна быть отклонена; это ограничение интерфейса, не обход. Опасный обход — побочный эффект ВНУТРИ списка, находка 3.

**Отсутствие МОДЕЛЬ не забыто.** Инв. 6 (:68) прямо требует отказ при отсутствии строки в любом из двух артефактов. `fixtures/check_fork_route/red_zapret_dvojnoj_roli.sh:57–61` действительно перезаписывает арбитраж через `otvet ... '-'`, который НЕ печатает МОДЕЛЬ, затем требует rc=1 и текст `МОДЕЛЬ`. Это исполняемый вход, не только комментарий. Пропуск модели консультанта отдельно предъявлен в `red_deko_bez_rc.sh`, вход 4. На текущем HEAD до этих поздних ветвей прогоны не доходят: барьеров ещё нет; факт исполнения поздней ветви не заявляю.

СОВЕТ contracts/029-auto-konsultant-forkov.md:68 — зеркальный случай «нет МОДЕЛЬ у консультанта» стоит предъявить также самому `check_fork_route`, поскольку имеющийся зеркальный вход обращается к другому барьеру. Самодекларация МОДЕЛЬ доказывает равенство заявленных строк, не фактический запуск модели; этот остаточный риск полезно назвать cognitive-only. Отдельного блокера за отсутствие уже существующего арбитражного входа нет.

**Зоны и Н-39 проверены.** Программная сверка: architect — 13 точных файлов (контракт, 9 red, 2 каркаса, наблюдения); implementer — 6; orchestrator — 3. Между этими тремя зонами пересечений нет, glob и каталогов нет. Все девять red перечислены в architect и имеют mode 755. Судейские каталоги указаны отдельно и с рабочими зонами не пересекаются. Все девять файлов полностью прочитаны, включая комментарии СТАБ-ПРИВЯЗКИ; связи названы у наблюдаемых входов: opt-in на отсутствии класса, openlist на чужом токене, over-trigger на рутине, nomodel на отсутствии строки, rconly на ложном sha и т. д. Контракт :149–151 отсылает к коду, а не назначает прозой пары стаб↔ветвь. Рецидива Н-39 в форме переноса привязок в контракт не найдено; узость некоторых самих входов разобрана выше.

СОВЕТ contracts/029-auto-konsultant-forkov.md:119 — регенерация харнеса назначена исполнителю, но её выходы `.omp/` на :121 вынесены за его зону. Исключение для владельца/оркестратора названо, однако порядок передачи результатов между исполнителем роли и владельцем реестра лучше уточнить до раздачи; это не пересечение перечисленных файлов и не отдельный обход критерия.

СОВЕТ contracts/029-auto-konsultant-forkov.md:87 — проба ложного rc требует подстроки `rc` и `1`, но не проверяет второе число `0`; честный ненулевой rc также не предъявлен позитивным входом. Усилить различимость диагностики и честного отказа команды можно без изменения предмета.

## Собственноручный прогон девяти предъявлений

Каждая строка ниже — отдельный запуск `bash <путь>` в чистом одноразовом клоне указанного HEAD. Все девять вернули **rc=1**. Это ожидаемое красное ДО реализации, не девять пойманных реализационных дефектов. Во всех случаях внутренний запуск отсутствующего барьера дал **rc=127** и `No such file or directory`.

| Команда (после `bash`) | rc | Первая строка фактического отказа |
|---|---:|---|
| `fixtures/check_fork_route/red_marshrut_a_inzhenernyj.sh` | 1 | `ОТКАЗ: непромаркированный форк адресован владельцу (default-open, инв. 1) — барьер вернул rc 127, ожидался 1. Вывод:` |
| `fixtures/check_fork_route/red_marshrut_b_volja_batch.sh` | 1 | `ОТКАЗ: законная воля владельца в батче — барьер обязан молчать (инв. 2) — барьер вернул rc 127, ожидался 0. Вывод:` |
| `fixtures/check_fork_route/red_marshrut_v_soreview.sh` | 1 | `ОТКАЗ: высокоставочный дизайн без СОревью (инв. 3-I) — барьер вернул rc 127, ожидался 1. Вывод:` |
| `fixtures/check_fork_route/red_flush_batcha.sh` | 1 | `ОТКАЗ: опрос флаша на здоровой очереди — барьер вернул rc 127, ожидался 0. Вывод:` |
| `fixtures/check_fork_route/red_schet_peredatochnyh.sh` | 1 | `ОТКАЗ: счёт на непустом журнале — барьер вернул rc 127, ожидался 0. Вывод:` |
| `fixtures/check_fork_route/red_zapret_dvojnoj_roli.sh` | 1 | `ОТКАЗ: один голос советует и судит свой совет (инв. 6) — барьер вернул rc 127, ожидался 1. Вывод:` |
| `fixtures/check_fork_route/red_degradacija_taimer.sh` | 1 | `ОТКАЗ: деградация — сигнал, а не отказ (инв. 7) — барьер вернул rc 127, ожидался 0. Вывод:` |
| `fixtures/verify_consultant/red_deko_bez_rc.sh` | 1 | `ОТКАЗ: декой утверждает верификацию дерева без единого выполненного rc (инв. 8) — барьер вернул rc 127, ожидался 1. Вывод:` |
| `fixtures/verify_consultant/red_lozhnyj_rc.sh` | 1 | `ОТКАЗ: заявлено чистое дерево при грязном — ложь ловится переисполнением (инв. 9) — барьер вернул rc 127, ожидался 1. Вывод:` |

Для семи маршрутных проб следующая строка: `fixtures/check_fork_route/_forks.sh: line 62: /tmp/dev-harness-verify/critic029/repo/scripts/check_fork_route.sh: No such file or directory`.
Для двух консультантских: `fixtures/verify_consultant/_konsult.sh: line 79: /tmp/dev-harness-verify/critic029/repo/scripts/verify_consultant.sh: No such file or directory`.

Полный `verify_antiplacebo`, CI, генерация харнеса и проектные проверки не запускались: предмет этого круга — контракт до реализации. Утверждения о пропуске контрмоделей предъявленными входами — анализ критерия; зелёный прогон отсутствующей реализации не заявляется.
