accept

Предмет: ТОЛЬКО применение решения `verdicts/arbitration/granitsa-readonly-ispolnenija-029.md`, §3–4, к оставшейся части блокера 3. Проверен закоммиченный HEAD `49755b2` (фикс `c5dd05b`) в собственном клоне. Достаточность самой границы Г1–Г4 не пересматривалась. Блокеры 1/2 и принятая граница argv не переоткрывались.
МОДЕЛЬ: openai-codex/gpt-6-astra
Круг: 3; v3 — версия следующей заморозки. Реализация барьеров не судилась: её ещё нет.

Набор полный: предмет и команды готовности закоммичены; исполнители и ЗОНА-строки присутствуют. Действительных блокирующих находок нет. Есть один СОВЕТ ниже; он заморозку не блокирует.

## Сверка применения Г1–Г4

Сверены текст §3 арбитража и `contracts/029-auto-konsultant-forkov.md:147–184`. Побайтового тождества всего текста НЕТ: добавлены пояснения, разрешённые платформенные ключи и выбранная автором подстрока Г4. Существенные предписания перенесены без ослабления:

| Мера | Место контракта | Результат сверки |
|---|---|---|
| Г1 | :151–155 | Каждый git-вызов в root, включая оба скан-гейта, под `env -i` + `PATH`, `LC_ALL`; `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_CONFIG_SYSTEM=/dev/null`, `GIT_OPTIONAL_LOCKS=0`. Наследуемые `GIT_*` исключены. |
| Г2 | :156–166 | До первого переисполнения, именно `git -C <root> config --list --local`, не чтение `.git/config`. Закрытый перечень ключей, неизвестный ключ и ненулевой rc скана дают отказ rc=1 до исполнения. Подстрока `конфиг вне белого списка` и имя ключа сохранены. |
| Г3 | :167–171 | Барьерное `-c core.hooksPath=<пустой каталог ВНЕ --root, созданный барьером>` сохранено. |
| Г4 | :172–178 | До первого переисполнения, `git -C <root> ls-files -s` под Г1, mode `160000` означает отказ rc=1 без переисполнения, с путём и выбранной подстрокой `субмодуль вне границы`. Трейдофф отказа честному суперпроекту записан. |

`core.ignorecase`, `core.precomposeunicode`, `core.symlinks` — прямо разрешённое арбитром расширение Г2, не отклонение. Отвергнутые альтернативы и остаточные риски сохранены в :179–184. Нового измеренного канала вне Г1–Г4 в этом круге не предъявлено; повторный арбитраж не созывается.

## Три именованных входа

Сверены не только комментарии, но и построение игрушек в `_konsult.sh`, команды, ожидаемые rc, подстроки и проверки маркеров в `fixtures/verify_consultant/red_lozhnyj_rc.sh`:

- Вход 8, :167–186: изменённый tracked-файл, внешний diff-драйвер, ответ `git diff` / rc=0 / sha пустого вывода; ожидаются rc=1, `конфиг вне белого списка`, `diff.external`, отсутствие маркера. Честность чисел и бессилие сверки rc/sha явно названы. Драйвер лежит вне root, а не буквально `./write-diff.sh`: источник запуска тот же локальный ключ `diff.external`, запись остаётся в проверяемом корне, смысл испытания не меняется.
- Вход 9, :188–207: конфиг без чужих ключей, исполняемый `post-index-change`, грязное дерево, оракул с близнеца без хука; ожидаются rc=0 и отсутствие маркера. Это действительно позитивный контроль, не отказ на сам факт наличия хука.
- Вход 10, :209–232: mode `160000`, невинный конфиг корня, fsmonitor и хук в субмодуле; ожидаются rc=1, `субмодуль вне границы`, путь `sub`, отсутствие ОБОИХ маркеров. Эти ассерты соответствуют решению. Есть неточность утверждения о честности тройки — отдельный совет ниже.

### Личный замер живости

Git 2.55.0. Игрушки построены исполнением закоммиченных построителей и секции входов 8–10; в этой отдельной подготовке вызов отсутствующего барьера и его ассерты не выполнялись. Затем лично запущены настоящие Git-команды. Это измерение входов, НЕ заявление об испытании реализации барьера.

```text
Г2: лишние ключи входа 8 = ['diff.external']
Г2: лишние ключи входов 9 и 10 = []
8: git diff, rc=0, stdout+stderr пуст; marker = side-effect\n
   sha=e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855
9: под Г1+Г3 rc=0, stdout='?? untracha.txt\n', маркера нет
9: без подавления rc=0, тот же stdout, маркер хука создан
   sha=6eb2ee7c34c847f83ae81604815f564bcf05d934e9583fc57da96e545d7e68b9
10: ls-files -s до переисполнения показывает mode=160000, путь=sub;
    оба маркера отсутствуют
10: под Г1+Г3 без Г4 rc=0, fsmonitor-маркер создан, маркера хука нет;
    stdout='?? marker-submodul-fsmonitor.txt\n'
10: без подавления rc=0, ОБА маркера созданы;
    stdout='?? marker-submodul-fsmonitor.txt\n?? marker-submodul-krjuk.txt\n'
```

Все три канала живы именно на предъявленных постройках. Г3 сохраняет позитивный контроль входа 9; Г4 имеет наблюдаемый до исполнения признак, а одни Г1+Г3 не устраняют субмодульный fsmonitor.

СОВЕТ fixtures/verify_consultant/red_lozhnyj_rc.sh:219–221 — тройка входа 10 с близнеца не является честной тройкой фактического запуска в злой игрушке: близнец даёт пустой вывод, а запись маркера fsmonitor становится видимой самому `status`. Под Г1+Г3 получен вывод `?? marker-submodul-fsmonitor.txt\n`, а без подавления — обе строки маркеров. Поэтому комментарии :46 и :68–69 о честности тройки и непременно rc=0 у S-nosubmod неточны: реализация без Г4, но со сверкой sha может вернуть rc=1 «расхождение вывода». Начальный ассерт собственного измерителя, требовавший равенства выводов близнеца и злой игрушки, именно здесь упал; отдельный замер установил причину и подтвердил оба маркера. Это не обход критерия: такой отказ НЕ проходит обязательную подстроку Г4 в :224, а созданные маркеры также запрещены :226–231. Исправить описание или сделать оракул действительно честным полезно, но состояния «критерий выполнен, Г4 отсутствует» эта неточность не предъявляет, поэтому по порогу роли она не блокирует accept.

## Личный прогон девяти red на HEAD

Каждая команда запущена лично без подставленного барьера:

| Команда | rc |
|---|---:|
| `bash fixtures/check_fork_route/red_marshrut_a_inzhenernyj.sh` | 1 |
| `bash fixtures/check_fork_route/red_marshrut_b_volja_batch.sh` | 1 |
| `bash fixtures/check_fork_route/red_marshrut_v_soreview.sh` | 1 |
| `bash fixtures/check_fork_route/red_flush_batcha.sh` | 1 |
| `bash fixtures/check_fork_route/red_schet_peredatochnyh.sh` | 1 |
| `bash fixtures/check_fork_route/red_zapret_dvojnoj_roli.sh` | 1 |
| `bash fixtures/check_fork_route/red_degradacija_taimer.sh` | 1 |
| `bash fixtures/verify_consultant/red_deko_bez_rc.sh` | 1 |
| `bash fixtures/verify_consultant/red_lozhnyj_rc.sh` | 1 |

Во всех девяти причина красного — отсутствующий барьер: внутренний rc=127, затем первый ассерт фикстуры возвращает 1. Сообщения указывают `_forks.sh:87` либо `_konsult.sh:135`, `No such file or directory`. Этот прогон не достигает поздних входов 8–10; их живость доказана отдельными замерами выше, а прохождение всей пробы будущим барьером здесь не заявляется.

## Счёт, границы и пять вопросов гейта

Независимая от списка команд инвентаризация через `git ls-tree -r HEAD` дала ровно 7 маршрутных + 2 консультантских red-файла = 9; все mode `100755`. Фикс `c5dd05b` меняет ровно три существующих файла: контракт, `_konsult.sh`, `red_lozhnyj_rc.sh`; новых файлов этот фикс не добавляет. В сравнении всего диапазона `f9ea6fb..49755b2` дополнительно появился отдельный вердикт арбитра — это не десятая фикстура. ЗОНА-строки контракта программно сравнены с `f9ea6fb`: тождественны.

В пределах порученного применения:

1. Критерий не сводится к существованию файлов: названы rc, диагнозы, положительный контроль и отсутствие побочных записей; обхода этим трём новым предъявлениям не найдено.
2. Проверяемые требования имеют исполняемые команды приёмки; личное красное и отдельная живость выше. Готовность отсутствующей реализации не объявляется.
3. Несущие решения не оставлены исполнителю: среда, закрытые ключи, способ скана, подавление хуков, отказ на гитлинке и коды определены.
4. Добавленная работа остаётся в прежних границах architect/implementer; зон и исполнителей не потеряли.
5. Нового противоречия AGENTS.md в проверяемом применении не обнаружено. Счёт проверен отдельной инвентаризацией, результат команд — rc, а не печать PASS. Необязательная неточность описания испытания отмечена советом, не превращена в новый бесконечный круг.

Проектный CI, генерация харнеса, полный/scoped `verify_antiplacebo` и реализация барьеров не запускались и не оценивались: предмет круга — применение арбитража до реализации. Закрытая арбитром граница принята как норма этого разбора.
