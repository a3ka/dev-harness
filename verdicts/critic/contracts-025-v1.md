accept

Круг 4 по файлу, круг 2 по дельте v1→v2. Судимый HEAD `8e3dbaed9cf73954f9c998b4309308ad2a03a37a`; предмет и закрытия двух блокеров предыдущего вердикта `d141dd9` закоммичены. Набор артефактов полный: предмет, rc-критерии, исполнители и `ЗОНА`-строки названы; граница critic — `contracts/025-sreda-cwd-rc.md:366` (`verdicts/critic/`). Блокирующих находок по судимой дельте нет.

Блокер 1 закрыт в `contracts/025-sreda-cwd-rc.md:121-145,268-277,293-307`: A-2 теперь явно отделяет измеренный обязательный ночной deny-набор (`sed -i`, `>`/`>>`, `printf >>`) от tee — зондовой формы стража; И-3 проверяет обе стороны развязки. В одноразовом декой-клоне добавление deny-правила `*tee *` дало `bash fixtures/check_runner_hygiene/red_deni_patte_rny_zapisi.sh .` → rc 1 с именованным диагнозом «зондовая форма И-6 … матчится deny-правилом … tee-противоречие d141dd9». На честном HEAD та же команда → rc 0. Ночные формы не ослаблены: все три остаются обязательными в тексте и отдельно покрываются живым распарсенным `bash.patterns`; текущие три deny-правила прошли И-3.

Блокер 2 закрыт в `contracts/025-sreda-cwd-rc.md:212-249,283-292,316-337`: null-allowlist задан точно как {`${TMPDIR:-/tmp}/dev-harness-verify`, `artifact://`}; `local://`, `mcp://` и прочие внутренние URI при `worktree:null` блокируются Н-85, тогда как в-пинне прежний URI-allowlist сохраняется. На дереве `0ebb4fe` до реализации `7b7f7ae`, но уже с восьмой ветвью красного предъявления, `bash fixtures/check_runner_hygiene/red_granica_nepin_pipe_tee.sh .` → rc 1 именованно: «ветвь “непиннованный-local” — ожидалось block, получено pass». На HEAD команда → rc 0 и перечислила все 8 ветвей, включая block непиннованного `local://`; `red_pin_allowlist.sh` → rc 0, поэтому в-пинненная ветвь `local://` не потеряна.

Дрейф дельты: `git diff cb8c9cf..8e3dbae -- contracts/025-sreda-cwd-rc.md` меняет только заявленную дифференциацию A-2, fail-closed раздел «Дыра B», согласованные уточнения C-1/И-3/И-5/И-6 и демаркацию конформности (78 добавлений, 18 удалений). Критерии v1 не удалены: И-1—И-7 сохранены, И-3 усилен tee-канарейкой, И-5 разделяет pinned/null, И-6 синхронизирован с tee-формой и пиннованным ребёнком.

Исполнимость на чистом одноразовом клоне HEAD — семь `red_*`:

- `red_deni_patte_rny_zapisi.sh .` → rc 0;
- `red_granica_nepin_pipe_tee.sh .` → rc 0;
- `red_marker_exit.sh .` → rc 0;
- `red_pin_allowlist.sh .` → rc 0;
- `red_pipefail_prefiks.sh .` → rc 0;
- `red_strazh_vectora_utechki.sh .` → rc 0;
- `red_zond_dubl_toolcallid.sh .` → rc 0.

`bash scripts/verify_antiplacebo.sh --scope check_runner_hygiene` → rc 0: 1 барьер, 40 фикстур, все 40 предъявлены красным повторным прогоном.

РАЗРЕШИЛ-грамматика соблюдена. В `84761ef` строка стоит в первой колонке и первым после двоеточия несёт путь `contracts/025-sreda-cwd-rc.md`, затем дословно цитирует два слова владельца (Дыра B 2026-09-11 и Г3 2026-09-10). В merge `0ebb4fe` строка также стоит в первой колонке, путь идёт первым после двоеточия, оба слова владельца названы.

Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза контракта; контракт несёт инварианты + rc-команды».

стенограмма: check_no_leak --check → rc=0
