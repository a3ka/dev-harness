FAIL

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:170-187 — пять предъявленных красных не проверяют заявленные ветви поведения: И-1/И-4/И-5 доверяют `--selftest` самого будущего субъекта, И-2 ищет только слова `prefix:`, `pipefail`, `141`, `EXIT`, И-3 — комментарий-маркер и три любых вхождения `deny`. Это противоречит норме `AGENTS.md:121-126,204-210`: кодовая приёмка обязана отличать честное дерево от обманного стаба.
ОБХОД: дерево с `path-guard.ts` и `exit-marker.ts` из `process.exit(0)`, с мёртвой строкой `prefix: "pipefail 141 EXIT"` и тремя комментариями `# deny` удовлетворяет всем пяти red-командам с rc 0, хотя не блокирует ни одной записи, не включает pipefail, не маркирует exit и не пинит worktree. Контрольный эксперимент на ровно таком дереве дал rc 0 для 5/5 файлов.

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:188-203 — И-6 объявлен критерием готовности, но четыре процедуры не имеют ни команды, ни сохраняемого исполняемого зонда с кодом возврата. В частности, И-6.1 уже не повторяет обязательную форму владельца «субагент + forbidden-call»: текст говорит лишь о сессии omp CLI с cwd клона, а И-6.3 разрешает пустой лог дочерней сессии.
ОБХОД: исполнитель проводит И-6.1 в главной CLI-сессии, где extension загружен, получает блок относительного edit, а дочерний task-агент по-прежнему пишет относительным edit в основной checkout; затем выполнение процедуры подтверждается только прозой. Все файловые И-1…И-5 зелёные, И-6.3 принимает пустой дочерний лог, но исходный дочерний вектор утечки остаётся жив.

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:164-168 — двухносительная конструкция заведомо не покрывает edit/write-вектор дочерних task-сессий: extension до них не доходит, а settings-носитель A-2 управляет только bash. Это не внешний край: именно относительный edit назван ночной формой Б2 и обязательным forbidden-call зонда. Детектор 024 после факта и интерим-дисциплина не являются превенцией, составляющей предмет 025.
ОБХОД: реализованы и зелены A-1/A-2/B/C и все пять red-файлов; дочерний неisolated task-агент вызывает `edit` с относительным путём. По документированной механике он наследует cwd родителя, settings deny к edit не применяется, extension не загружен, поэтому запись попадает в основной checkout до детектора 024.

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:148-162 — пин доверяет любой существующей абсолютной строке `WORKTREE=` и критерий не доказывает тождество пина фактическому рабочему дереву сессии; также батарея не требует канонизации `..` и симлинков до проверки принадлежности. Заявленный запрет записи в основной checkout поэтому можно обойти при полностью зелёном перечисленном selftest.
ОБХОД: задание дочерней сессии содержит существующий `WORKTREE=/home/aka/Documents/dev-harness` либо путь внутри допустимого пина проходит через симлинк/`..` в основной checkout. Лексический allowlist считает цель разрешённой; selftest с обычным правильным пином, прямым main-путём и несуществующим пином остаётся зелёным, но запись разрешена в основной checkout.

БЛОКИРУЕТ contracts/025-sreda-cwd-rc.md:117-127 — контракт оставляет точное имя settings-ключа префикса исполнителю, а И-2 проверяет только текст. На судимом билде `omp config list --json` не объявляет `bash.prefix`, `prefix` или `shell.prefix`, а `omp config get` для каждого возвращает rc 1 `Unknown setting`; значит достижимость B-1 не задана и текущая red-проверка принимает заведомо неисполняемую настройку.
ОБХОД: в `.omp/config.yml` добавлена неизвестная/нечитаемая рантаймом строка `prefix: "pipefail 141 EXIT"`; `red_pipefail_prefiks.sh` возвращает 0, но `false | true` остаётся rc 0 в главной и дочерней сессиях.

СОВЕТ contracts/025-sreda-cwd-rc.md:275-282 — подозрение А-113 о no-op расширениях 015 корректно отделено как не-цель: A/B/C на `gate-draft.ts` и `startup-digest.ts` не полагаются. Здесь блокирующего обхода нет.

ПЛОТНО contracts/025-sreda-cwd-rc.md:65-70 — сам выбор settings как второго носителя имеет документированную точку достижимости: `omp://tools/task.md` говорит об isolated settings snapshot с наследованием parent settings, а `omp://bash-tool-runtime.md` подтверждает применение `bash.patterns` и shell prefix на tool-call пути. Дыра не в идее settings-цепи, а в непредъявленной исполняемой грамматике/проверке и в том, что этот носитель закрывает только bash, не edit/write.

ПЛОТНО contracts/025-sreda-cwd-rc.md:100-103 — чтения read/grep/glob сами не являются каналом записи; разрешение чтений соответствует Г1. Различение главной и spawned-сессии для абсолютного edit, однако, фактически держится только pin-extension, поэтому попадает под названный выше дочерний пробел покрытия.

ПРОГОНЫ на закоммиченном 5a849b58368c9e72df63990fa2f69dd8a3893515 (без пайпов):

- `bash fixtures/check_runner_hygiene/red_strazh_vectora_utechki.sh .` → rc 1, именованный `механизм отсутствует`.
- `bash fixtures/check_runner_hygiene/red_pipefail_prefiks.sh .` → rc 1, именованный `ключ prefix: отсутствует`.
- `bash fixtures/check_runner_hygiene/red_deni_patte_rny_zapisi.sh .` → rc 1, именованный `маркер секции не найден`.
- `bash fixtures/check_runner_hygiene/red_marker_exit.sh .` → rc 1, именованный `механизм отсутствует`.
- `bash fixtures/check_runner_hygiene/red_pin_allowlist.sh .` → rc 1, именованный `механизм отсутствует`.
- Те же пять команд против `/tmp/dev-harness-verify/critic025-bypass` с двумя `process.exit(0)` и текстовым config-стабом → rc 0, rc 0, rc 0, rc 0, rc 0.
- `bash scripts/verify_antiplacebo.sh . --scope check_staged` → rc 0; 23/23 фикстуры предъявлены красным повторным прогоном.
- `bash scripts/check_nabludenia.sh` → rc 0.
- `omp config get bash.prefix`, `omp config get prefix`, `omp config get shell.prefix` → каждый rc 1 `Unknown setting`.
