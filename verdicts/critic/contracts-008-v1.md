FAIL

# Вердикт критика — контракт 008, круг 1

Предмет проверки — закоммиченный HEAD `f0c7b7d24c5564157af0c7756128fc21ef28c1a8`:
`contracts/008-deshevye-sudejskie-krugi.md` и предъявленные красными
`scripts/check_check_contract_ready.sh`, `scripts/check_judge_gate.sh`.

## Находки

БЛОКИРУЕТ scripts/check_check_contract_ready.sh:54-60 — зелёный контроль «готов» объявляет
готовым контракт, чья приёмочная команда ссылается на несуществующий файл. Это прямо слабее
критерия `contracts/008-deshevye-sudejskie-krugi.md:57-60`, где объявленная, но отсутствующая
красная проба обязана дать RC≠0 с названием ветви.
ОБХОД: `check_contract_ready.sh` может считать любой non-zero от объявленной команды достаточным
красным, не проверяя существование самой пробы. На фикстуре `toy_barrier_NESUSHESTVUET.sh`
ветвь «готов» и весь барьер станут зелёными, хотя ни одной красной пробы в дереве нет и предмет
«объявленные красные тесты существуют» не сделан.

БЛОКИРУЕТ scripts/check_check_contract_ready.sh:13-14 — мета-барьер явно не проверяет два
обязательных класса предмета: самосогласованность счётных утверждений и пройденные арбитражные
перечни (`contracts/008-deshevye-sudejskie-krugi.md:15-22,57-60`). Приёмочная команда
`contracts/008-deshevye-sudejskie-krugi.md:67` поэтому не доказывает весь объявленный срез 1.
ОБХОД: предмет может реализовать только три наблюдаемые ветви барьера — отказ без зон, отказ
для уже зелёной команды и RC0 для остальных контрактов. Тогда `check_check_contract_ready.sh`
зелён, но контракт с ложным числом и контракт с непройденным арбитражным пунктом оба приняты
RC0; два явно заявленных свойства предмета отсутствуют.

БЛОКИРУЕТ scripts/check_judge_gate.sh:45-55 — фиктивный `check_ci_gate.sh` задаёт только код и
маркер вызова, но не утверждает, что `judge_gate.sh <sha>` передал полученный SHA нижнему гейту,
как требует `contracts/008-deshevye-sudejskie-krugi.md:30-34`.
ОБХОД: `judge_gate.sh` может игнорировать `$1` и вызвать соседний `check_ci_gate.sh` без
аргументов. Обе ветви увидят маркер и ровно заданный fake-код, поэтому станут зелёными; реальный
`check_ci_gate.sh` при таком вызове проверит свой default `HEAD`, а не SHA из задачи судьи.

## Фактические прогоны на чистом клоне HEAD

- `bash scripts/check_check_contract_ready.sh` → RC=1, именованная причина:
  `ОТКАЗ ветвь (зоны): ... причина «зоны» не названа ... ПРЕДМЕТ-ОТСУТСТВУЕТ`.
- `bash scripts/check_judge_gate.sh` → RC=1, именованная причина:
  `ОТКАЗ ветвь (красный): judge_gate не позвал check_ci_gate (нет маркера) ... ПРЕДМЕТ-ОТСУТСТВУЕТ`.
- Отдельно прогнаны `зоны`, `проба`, `готов`, `красный`, `зелёный`: каждая ветвь → RC=1 с
  собственным именем и причиной отсутствующего предмета.

Стабы «всегда RC0», «всегда RC7» и стаб с иным текстом имеющимися проверками действительно
разведены: красные ветви требуют non-zero, зелёные — RC0, а диагностические ветви требуют
маркеры `зон`/`проб` либо `FAKE-check_ci_gate`. Это не закрывает названные выше обходы: они
удовлетворяют именно тем наблюдениям, которые барьеры сейчас делают.
