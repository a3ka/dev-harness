accept

# Вердикт критика — контракт 008, круг 2

Предмет повторной проверки — закоммиченный HEAD
`2734fcc462fd4581a74404d92afab8fe97f85818`. Область круга ограничена тремя обходами из
круга 1; новые находки не искались.

## Три обхода закрыты

1. Обход с несуществующей красной пробой закрыт. `mk_ready` создаёт реальный
   `scripts/toy_barrier.sh`, записывает в него `exit 1` и делает файл исполняемым
   (`scripts/check_check_contract_ready.sh:94-101`). Отдельная фикстура зелёной пробы создаёт
   существующий исполняемый файл с `exit 0` (`scripts/check_check_contract_ready.sh:128-133`),
   а ветвь `(проба)` требует для него RC≠0 и термин `проб`
   (`scripts/check_check_contract_ready.sh:193-202`). Положительный контроль `(готов)` требует
   RC=0 именно на дереве с реальной красной пробой
   (`scripts/check_check_contract_ready.sh:220-228`).

2. Обход без проверки счёта и арбитража закрыт. `mk_wrongcount` строит две фикстуры при
   объявленных пяти (`scripts/check_check_contract_ready.sh:138-157`), `mk_arb_bad` объявляет
   отсутствующий арбитражный файл (`scripts/check_check_contract_ready.sh:160-176`). Ветви
   `(счёт)` и `(арбитраж)` требуют RC≠0 и соответственно термины `счёт` и `арбитраж` в выводе
   (`scripts/check_check_contract_ready.sh:204-218`).

3. Обход с игнорированием SHA закрыт. Fake `check_ci_gate.sh` возвращает RC=0 только при точном
   `$1 = PASS_THIS_SHA_GREEN`, иначе RC=1 (`scripts/check_judge_gate.sh:59-78`). Ветвь
   `(зелёный)` передаёт этот SHA предмету и отдельно требует в выводе fake маркер
   `sha=PASS_THIS_SHA_GREEN`, после чего требует RC=0
   (`scripts/check_judge_gate.sh:91-101`). Вызов нижнего гейта без аргумента или с иным SHA
   больше не удовлетворяет ветвь.

## Фактические прогоны на HEAD

- `bash scripts/check_check_contract_ready.sh готов` → RC=1: ветвь назвала
  `ПРЕДМЕТ-ОТСУТСТВУЕТ` и не приняла положительный контроль за RC=0.
- `bash scripts/check_check_contract_ready.sh счёт` → RC=1: ветвь не получила обязательный
  термин `счёт` от отсутствующего предмета.
- `bash scripts/check_check_contract_ready.sh арбитраж` → RC=1: ветвь не получила обязательный
  термин `арбитраж` от отсутствующего предмета.
- `bash scripts/check_judge_gate.sh зелёный` → RC=1: ветвь не получила маркер вызова fake от
  отсутствующего предмета.

Все четыре запрошенные красные проверки действительно красны против отсутствующего предмета;
наблюдения, которыми удовлетворялись три обхода круга 1, теперь недостаточны для зелёного
мета-барьера.
