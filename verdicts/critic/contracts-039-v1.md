FAIL

# Контракт 039, круг критика 1

Предмет: `contracts/039-check-protected-rename.md` на HEAD
`34d2b748615278473bee32848e05f78b492a001a`; блоб контракта
`b6e38bd3ee7fcb20d97115151e63edfa821fc207`.
Судится контракт до заморозки, не будущая реализация. Набор полный: закоммичены
предмет, rc-критерии, исполнители, ЗОНА-строки и ПРОВОДКА. Пачка предъявлена из
объявленного scratch, её байты совпали со всеми тремя пинами контракта.

Итог: **1 блокирующая находка, 1 совет**. Заморозка запрещена этим вердиктом.

## Б1 — приёмка не различает обязательное появление нового пути

БЛОКИРУЕТ contracts/039-check-protected-rename.md:38 — приёмка на строках 74–82
не удерживает условие «в удаляющем c появился q со статусом A и блобом B».
Проверяемая слабая реализация сохраняет `excuse_for`, область, shallow, грамматики,
отказы и поиск удаляющего c; для переноса проверяет однозначный родительский B и его
наличие в `ls-tree HEAD`, но вообще не проверяет добавление q в c.

ОБХОД: в c1 защищённый `plans/001-p.md` содержит B; в c2 заранее добавлена
`notes/001-p.md` с тем же B; в c3 удалён только `plans/001-p.md`, без ALLOW и без
добавлений. На HEAD копия жива. Слабая реализация называет это переносом и возвращает
0, хотя п.38 требует отказа: удаляющий diff содержит только `D plans/001-p.md`,
никакого A(q) нет. При этом вся предъявленная красная семья, живой свидетель,
разрешённое удаление и обе объявленные зелёные ветви переноса проходят. Отказной
контроль самого дрилла также не отличает этот дефект от правильной реализации.

Это не требование различать mv и копирование: копирование с добавлением q именно в c
по контракту законно. Контрпример отделяет его от копии, существовавшей ДО c.
Если условие строки 38 сохраняется, ему нужна различающая проба; сейчас молчаливое
исключение этого условия из реализации остаётся зелёным по приёмке.

### Исполнимое свидетельство Б1

Эксперимент выполнен исключительно в
`/tmp/dev-harness-verify/critic039-k1/counterexample`, клоне названного HEAD.
Ни контракт, ни реализация в основном репозитории не менялись.

Временная дельта барьера: `/tmp/dev-harness-verify/scratch/critic039-weak.patch`.
Она добавляет только описанную проверку B и ветвь отчёта переноса; проверку A(q,c)
намеренно не содержит. Закреплённые три case-файла скопированы без изменения.
Команды из корня экспериментального клона:

| Команда | Наблюдение |
|---|---|
| `bash scripts/verify_antiplacebo.sh --scope check_protected` | rc 0; 19/19 предъявлены красными с зелёным контролем |
| `bash scripts/check_protected.sh` | rc 0; оба живых пути 036/038 названы перенесёнными |
| `bash scripts/drill_protected_exception.sh` | rc 0 |
| `bash scripts/drill_protected_rename.sh` | rc 0; перенос внутри и наружу принят и назван |
| `bash scripts/verify_antiplacebo.sh --scope drill_protected_rename` | rc 0; настоящий соседний барьер даёт зелёный контроль, отказной — rc 1 с «перенос не распознан» |
| `bash /tmp/dev-harness-verify/scratch/critic039-boundary.sh` | rc 0; внутри rc 0, наружу rc 0, **заранее созданная копия + отдельное удаление также rc 0** |

Последний скрипт строит три независимых игрушечных истории. Для третьей отдельно
печатает `git diff-tree --no-renames --name-status -r HEAD`: единственная перемена —
`D plans/001-p.md`. Затем проверяет фактические rc и ok-строку слабого барьера.
Это наблюдаемая потеря условия строки 38, а не предположение по тексту.

R3б/в на настоящем HEAD ещё отсутствуют. В эксперименте использованы временные
самостоятельные дрилл и case по объявленным двум случаям и действующему прецеденту
`drill_protected_exception`; это **не** заявление о проверке будущих файлов исполнителя.
Копии экспериментальных файлов сохранены в scratch как
`drill_protected_rename.sh` и `case_perenos_ne_priznan.sh`.

Первый экспериментальный scoped-прогон был недостоверен: параллельный живой барьер
создал `tmp/protected.*`, и контроль неизменности дерева вернул rc 1, хотя все 19
случаев предъявили красное. Он НЕ засчитан. После завершения соседнего вызова тот же
scoped-прогон выполнен отдельно: rc 0. Исходные свидетели ниже этого пересечения не имели.

## С1 — буквальность цитаты

СОВЕТ contracts/039-check-protected-rename.md:14 — метка «дословно» не вполне точна
на уровне пунктуации: в `NABLIUDENIA.md:103` после `check:protected` стоит запятая,
здесь тире; внутренние кавычки инварианта также заменены. Существенные слова решения
Н-121-в, инвариант, запрет ослабления и пара «удаление красное / rename зелёный»
перенесены без смысловой подмены. Это не основание FAIL и не запрос новой санкции.

## Проверки предъявленного HEAD и закреплённой пачки

Корень проверки: `/tmp/dev-harness-verify/critic039-k1/repo`, отдельный клон HEAD.
Для scoped-проб только в этот клон скопированы три файла из
`/tmp/dev-harness-verify/scratch/n121-red/`. Барьеры оставлены исходными.

| Свидетель / команда | rc и результат |
|---|---|
| `bash scripts/check_provodka.sh . contracts/039-check-protected-rename.md` | 0, без вывода; grammar 038 приняла guard-канал |
| `bash scripts/check_protected.sh` | 1, ровно два FAIL-пути, перечислены ниже |
| `bash scripts/drill_protected_exception.sh` | 0, разрешение принято и названо |
| `bash scripts/verify_antiplacebo.sh --scope check_protected/case_udalenie_kommitom` | 0, R1; красный барьер rc 1 |
| `bash scripts/verify_antiplacebo.sh --scope check_protected/case_pereimenovanie_s_pravkoj` | 0, R4; красный барьер rc 1 |
| `bash scripts/verify_antiplacebo.sh --scope check_protected/case_pereimenovanie_naruzhu` | 0, R5; красный барьер rc 1 |
| `bash scripts/verify_antiplacebo.sh --scope check_protected/case_perenos_ciklom` | 0, R6; красный барьер rc 1 |
| `bash scripts/verify_antiplacebo.sh --scope check_protected` | 0, 19/19; у каждого зелёный контроль и повторное красное |
| `bash scripts/scope_select.sh . --scope check_protected` | 0, MODE scoped, KEY check_protected |
| `npm run check:contract-frozen` | 0, frozen-блобы совпали, 039 — незамороженный черновик |
| `git diff --exit-code HEAD -- scripts/check_protected.sh scripts/drill_protected_exception.sh scripts/verify_antiplacebo.sh scripts/scope_select.sh scripts/verify_ci_parity.sh contracts/039-check-protected-rename.md .github/workflows/ci.yml package.json` | 0, без вывода; базовые барьеры, контракт и проводка в клоне неизменны |

Два FAIL-пути живого свидетеля:
- `verdicts/arbitration/036-v4-3-zhivoj-vyzov.md`;
- `verdicts/arbitration/038-dostizhimost-i-zony.md`.

Счёт 19 проверен другой мерой: `git ls-files 'fixtures/check_protected/case_*.sh' |
wc -l` дал 17 (rc 0); закрепление заменяет один существующий и добавляет два новых
case-файла. Отдельное перечисление `case_*.sh` после копирования содержит 19 имён.
Счёт двух живых отказов также виден в поимённом выводе, а не только в суммарной строке.

`sha256sum` трёх файлов scratch — rc 0, совпадение с пинами строк 105–107:

```
b5f059f72c9a8c2bb017e61c9e07d81b60d94cb265a41072923b150acfac844b  case_pereimenovanie_naruzhu.sh
15c631a9c63eb9b0325fe0e72eaf2f04e11c8c0a91c253f2edead4e66e3b8d91  case_pereimenovanie_s_pravkoj.sh
af31f0a640930c21f3581e3bccccc3b19a4e6438305ee8dff35a16c7d316b5d4  case_perenos_ciklom.sh
```

## Остальные обязательные вопросы гейта

- **Предмет и доказательство готовности:** Б1 показывает конкретное дерево и слабую
  реализацию, различить которые приёмка не может. Остальные перечисленные исходные
  команды имеют rc; R3б/в честно объявлены неактивными до реализации.
- **Способ против результата:** §§Предмет/Семантика не требуют git-детекции rename.
  Блоб B и HEAD соответствуют шапке `check_protected.sh:4-10`; мера c сохраняет
  `--no-renames -m` из строк 181–185. Б1 касается добавочного условия c, а не способа mv.
- **Неослабление:** строки 45–55 прямо сохраняют порядок ALLOW → перенос → FAIL,
  грамматику ALLOW, область, shallow и env-гигиену; исходная красная семья и зелёное
  исключение исполнимы. Побайтовая неизменность будущей реализации здесь не объявляется
  проверенной: реализации ещё нет, её область предстоит судить отдельно.
- **Решения и границы исполнения:** предметные файлы строк 89–98 покрыты зонами
  114–119; общая новая семья объявлена двум исполнителям явно, с разделением
  спецификации и приземления. Ограничение — действующий `check_zones` и frozen union;
  не предположение об исключительной собственности одного автора. Первое приземление
  architect касается существующей семьи, поэтому не создаёт сиротский новый барьер.
- **Н-113 / достижимость:** старый `check_protected` уже активен в ap4 (`ci.yml:67`);
  новый `drill_protected_rename` назначен ap2, а не ошибочно выдан за существующий ключ.
  Скрипт, case, npm-имя и matrix-key вводятся атомарно исполнителем. Общая классификация
  раннера и сумма-инвариант `verify_ci_parity.sh:1087-1130,1201-1244` не требуют
  отдельного фиксированного перечня новых ключей. Зелёные контроли старой семьи
  прожиты, объявленные случаи новой — достижимы в эксперименте выше. Старая версия R5
  не подменяла предъявленную: до всех scoped-прогонов скопирована закреплённая новая.
- **Норма AGENTS.md:** самостоятельного блокирующего противоречия вне Б1 не выявлено.
  Здесь не замораживается контракт, не выдаётся санкция владельца и не меняется предмет.
  Полные проектные проверки не запускались; соблюдена scoped-приёмка судьи v2.
