accept

# Контракт 039, круг критика 2

Предмет: `contracts/039-check-protected-rename.md` на HEAD
`201fdccd14ae6c1b70d46864c551c49896bb737e`; блоб контракта
`bc3a38f124403d3fc3a96cc20c06464257707591`.

Итог: **0 блокирующих находок, 0 советов**. Б1 круга 1 закрыт текстом И
различающим прогоном; совет С1 о пунктуации также закрыт. Заморозка разрешена.
Это вердикт по контракту до реализации, не приёмка будущего исправления барьера.

## Полнота и закрытие Б1

Набор полный: предмет закоммичен; §Предмет, rc-критерии §Приёмка,
исполнители и ЗОНА-строки присутствуют. Пачка четырёх case-файлов доступна в
`/tmp/dev-harness-verify/scratch/n121-red/`; все байты сверены с пинами
контракта до копирования в одноразовый клон.

`contracts/039-check-protected-rename.md:34–45` сохраняет исходную семантику:
удаляющий c определяется действующей мерой; неоднозначный B в родителях слияния
не даёт переноса; q появляется статусом A именно в c; B остаётся на HEAD.
Строки 74–80 дополнительно требуют условия А и Б **одновременно**. Строка 92
делает R7 обязательной rc-пробой до и после фикса; строка 120 закрепляет её байты:

```
fd930692e76cfeb143e70313f7341fbdb77a7bdedb454a19e7dd4b7cb49fbd50  case_kopija_zaranee.sh
```

### Прямое повторение контрпримера

В отдельной игрушечной истории выполнен закреплённый R7:

1. c1: защищённый `plans/001-p.md` содержит B.
2. c2: заранее добавлен `notes/001-p.md` с тем же B; исходный путь ещё жив.
3. c3: удалён только `plans/001-p.md`, без ALLOW и без добавлений.

Дополнительные независимые наблюдения после прогона:

- `git diff-tree --no-commit-id --no-renames --name-status -r HEAD` → **rc 0**,
  единственная строка `D\tplans/001-p.md`.
- `git log -1 --format=%B` → **rc 0**; тело удаляющего коммита —
  `план убран, без разрешения и без добавлений`, ALLOW-строки нет.
- `git rev-parse HEAD^:plans/001-p.md` и
  `git rev-parse HEAD:notes/001-p.md` → **rc 0 каждый**, оба дают
  `b03624e85d6d3c6a81d47dce977374b164a04815`.

На текущем, неизменённом `scripts/check_protected.sh` первые два состояния
зелёные; после c3 прямой прогон R7 завершился **rc 1** с причиной:

```
защищённый артефакт существовал и на HEAD его нет: plans/001-p.md
```

Это честный красный текущей реализации, а не ложное зелёное и не доказательство
уже реализованного переноса: фикс под контрактом ещё не выполнен.

### Различение слабой реализации из круга 1

В ДРУГОМ одноразовом клоне применена только временная дельта
`/tmp/dev-harness-verify/scratch/critic039-weak.patch`: сохраняются исходные
проверки, ALLOW и поиск c; добавляется перенос по однозначному родительскому B
и наличию B на HEAD, **без проверки A(q,c)**. Прямой повтор R7 на ней даёт
**rc 0** и `перенесён, контент жив на HEAD: plans/001-p.md` — дефект воспроизведён.

Но теперь объявленная приёмка такую реализацию отклоняет:

| Команда на слабой реализации | rc и наблюдение |
|---|---|
| `bash scripts/verify_antiplacebo.sh --scope check_protected/case_kopija_zaranee` | **1**; `барьер остался зелёным на обманном дереве — красное не предъявлено` |
| `bash scripts/verify_antiplacebo.sh --scope check_protected` | **1**; 19 из 20 предъявлены красными, единственное расхождение — R7 |

Таким образом, конкретное состояние дерева и конкретная слабая реализация,
пропущенные приёмкой круга 1, больше не удовлетворяют приёмке v2. Причина отказа
именно в различении А, а не в отсутствии Б: равенство блобов предъявлено отдельно.

Техническая оговорка воспроизводимости: сохранённая дельта к1 имеет неверные
счётчики строк заголовков hunk. Первый `git apply` вернул 128 (`corrupt patch`),
до изменения барьера. После чтения контекста применён `git apply --recount`
(**rc 0**) без изменения содержательной дельты; все результаты слабой реализации
выше сняты ПОСЛЕ успешного применения. Ошибка применения не засчитывается как
красное свидетельство контракта.

## Все живые свидетели и scoped-приёмка судьи v2

Основной корень проверки — `/tmp/dev-harness-verify/critic039k2/repo`, отдельный
клон названного HEAD. В него скопированы только четыре закреплённых case-файла;
барьеры и контракт оставлены исходными. Команды выполнялись последовательно,
без пересечения живого барьера с контролем неизменности дерева раннера.

| Свидетель / команда | rc и результат |
|---|---|
| `bash scripts/check_provodka.sh . contracts/039-check-protected-rename.md` | **0**, без вывода; guard-канал принят |
| `bash scripts/check_protected.sh` | **1**, ровно два FAIL-пути Н-121, перечислены ниже |
| R1: `bash scripts/verify_antiplacebo.sh --scope check_protected/case_udalenie_kommitom` | **0**, зелёный контроль и повторное красное rc 1 |
| R2: `bash scripts/drill_protected_exception.sh` | **0**, разрешение принято и названо |
| R2, дополнительно scoped: `bash scripts/verify_antiplacebo.sh --scope drill_protected_exception` | **0**, зелёный контроль и повторное красное rc 1 «исключение не принято» |
| R4: `bash scripts/verify_antiplacebo.sh --scope check_protected/case_pereimenovanie_s_pravkoj` | **0**, зелёный контроль и повторное красное rc 1 |
| R5: `bash scripts/verify_antiplacebo.sh --scope check_protected/case_pereimenovanie_naruzhu` | **0**, зелёный контроль и повторное красное rc 1 |
| R6: `bash scripts/verify_antiplacebo.sh --scope check_protected/case_perenos_ciklom` | **0**, зелёный контроль и повторное красное rc 1 |
| R7: `bash scripts/verify_antiplacebo.sh --scope check_protected/case_kopija_zaranee` | **0**, зелёный контроль и повторное красное rc 1 |
| `bash scripts/verify_antiplacebo.sh --scope check_protected` | **0**, 20/20 красных с зелёными контролями |
| `npm run check:contract-frozen` | **0**; frozen-блобы совпадают, 039 — незамороженный черновик |
| `git diff --exit-code HEAD -- scripts/check_protected.sh scripts/drill_protected_exception.sh scripts/verify_antiplacebo.sh scripts/scope_select.sh scripts/verify_ci_parity.sh contracts/039-check-protected-rename.md .github/workflows/ci.yml package.json` | **0**, без вывода; перечисленные исходники, контракт и проводка неизменны |

Два FAIL-пути живого свидетеля, без дополнительных отказов:

- `verdicts/arbitration/036-v4-3-zhivoj-vyzov.md`;
- `verdicts/arbitration/038-dostizhimost-i-zony.md`.

Счёт 20 проверен другой мерой: независимый файловый инвентарь непосредственных
`fixtures/check_protected/case_*.sh` содержит 20 имён. Он не получен повторением
самоотчёта раннера. Пины оставшихся трёх файлов совпали без изменения:

```
b5f059f72c9a8c2bb017e61c9e07d81b60d94cb265a41072923b150acfac844b  case_pereimenovanie_naruzhu.sh
15c631a9c63eb9b0325fe0e72eaf2f04e11c8c0a91c253f2edead4e66e3b8d91  case_pereimenovanie_s_pravkoj.sh
af31f0a640930c21f3581e3bccccc3b19a4e6438305ee8dff35a16c7d316b5d4  case_perenos_ciklom.sh
```

R5/R6 на текущем барьере останавливаются на первом переносе, как и объявлено
в колонке «ДО фикса». Это не выдаётся за исполнение их конечных ветвей на будущей
реализации. На слабом экспериментальном барьере обе пробы проходят дальше и
предъявляют красное; единственное расхождение семьи остаётся R7.
R3б/в честно объявлены неактивными до реализации; их будущий код этим вердиктом
не объявляется проверенным. Полный проектный прогон не запускался.

## Полный пересмотр контракта: пять вопросов роли

1. **Критерий против предмета.** `contracts/039-check-protected-rename.md:14–27,
   34–66,74–93`: удаление, перенос и конъюнкция А/Б разделены исполнимыми пробами;
   конкретный обход Б1 больше не проходит. Иного действительного блокирующего
   обхода при полном пересмотре контрактной части не найдено. Предмет не заменён
   разрешением любой копии, поиском сходства git или исключениями двух живых SHA.
2. **Доказуемость.** Строки 84–93 дают rc-команды до и после фикса. Пины 117–120
   задают точные байты красной пачки; совпадение проверено хешированием. Пункт 5
   семантики требует от нового дрилла не только rc 0, но и именованного переноса;
   строки 100–107 назначают проверку настоящего и отказного барьера исполнителю.
   Существование файла само по себе нигде не выдано за готовность механизма.
3. **Решения исполнителя.** Строки 34–58 фиксируют меру c, B, A, HEAD,
   fail-closed при разных родительских блобах, порядок ALLOW → перенос → FAIL,
   строки отчёта и сохранение остальных правил. Выбор алгоритма реализации
   остаётся исполнителю; предметные развилки ему молча не переданы.
4. **Границы.** Все файлы раздачи строк 100–107 покрыты ЗОНА-строками 127–132.
   Architect приземляет четыре закреплённых case-файла первым пост-фриз коммитом;
   implementer атомарно вводит барьерную правку, дрилл, его case и проводку.
   Общая новая семья объявлена обоим явно, с разделением спецификации и
   приземления на строках 134–137. Механизм границ — frozen-зоны и действующий
   `check_zones`, не предположение о единственном владельце общего каталога.
5. **Норма AGENTS.md и проводка.** Самостоятельного блокирующего противоречия
   не выявлено. Семантика существующей защиты не ослаблена текстом; область,
   грамматика ALLOW, shallow и env-гигиена сохраняются строками 47–58 и исходной
   красной семьёй. ПРОВОДКА на строках 139–147 именует действующий guard и
   объясняет отсутствие role-канала инвариантом дерева; её грамматика исполнена
   с rc 0. `check_protected` уже находится в ap4 (`ci.yml:67`), новый ключ
   `drill_protected_rename` явно назначен ap2 вместе с npm-именем. Классификация
   `scope_select.sh:34–56` и динамическая сверка matrix/fixtures в
   `verify_ci_parity.sh:1086–1130,1216–1265` поддерживают такую проводку;
   скрытый фиксированный перечень ключей для этого шага не требуется.

Пунктуация цитаты `contracts/039-check-protected-rename.md:16–19` теперь
соответствует цитируемому фрагменту Н-121 (`NABLIUDENIA.md:103`); прежний С1 снят.
Н-39: стабы к ветвям привязывает architect по коду, НЕ проза контракта.
Здесь заключение о различении Б1 основано на двух реально исполненных вариантах
барьера и неизменной закреплённой пробе, а не на прогнозе из её шапки.

Сырые журналы и независимый инвентарь сохранены в
`/tmp/dev-harness-verify/critic039k2/evidence/` (`results.json`,
`weak-results.json`, `case-inventory.json`, `pins.json`, поимённые `.log`).
Основной репозиторий получает только этот вердикт; контракт и реализация
критиком не правились. Push не выполняется.
