accept

Предмет суда — исполнение РЕШЕНИЯ арбитража
`verdicts/arbitration/contracts-017-sila-povedencheskih-prob.md` (`a9e14eb`) в
закоммиченной пачке HEAD `2ad816fb9fda37546cddcc293399c60128863200`:
текст v3 `f7f2fc2` и дельта проб `1f0bf27`. Набор артефактов полный: предмет,
rc-критерии, названные исполнители, `ЗОНА`-строки и явная строка
`РАБОТА НЕ РАЗДАЁТСЯ:` присутствуют.

Все прогоны выполнены на закреплённом HEAD в одноразовых клонах
`tmp/critic/017v3-*`; `TMPDIR` направлен внутрь соответствующего клона.

## Испытания решения арбитража

1. **p0.i, контрпример судьи (1а) — пойман.** Дифф из
   `tmp/critic/017v2-repro` извлечён `git diff --no-prefix` и применён
   `patch -p0` только к `scripts/check_metering.sh`; пробы не менялись.
   `bash fixtures/verify_antiplacebo/probe_port0.sh` → **rc=1**; p0.i
   поимённо сообщил `спавн с неоднозначным конфигом`, счёт `--config`: **2**,
   и напечатал оба argv-пути. Прежнее зелёное состояние с заранее выбранным
   управляющим портом больше критерий не удовлетворяет.

2. **p0.i, обратный порядок (1б) — пойман, исход решающий.** В отдельном
   клоне заранее выбранный конфиг передан ПЕРВЫМ, port:0-конфиг — ВТОРЫМ;
   после `proxy_up` порт перечитан из управляющего конфига, поэтому ветвь не
   падала раньше проверки манифеста. Та же команда → **rc=1**; p0.i
   поимённо сообщил неоднозначность и счёт **2**, пути напечатаны в обратном
   порядке. Доработка испытания устраняет посторонний ранний отказ и
   непосредственно доказывает требуемую арбитражем независимость
   единственности от порядка аргументов.

3. **p0.i, честный одиночный спавн (1в) — контроль пройден.** В изолированном
   контрольном дереве ветвь спавнила ровно один `--config`, его снимок нёс
   `port:0`, а после `proxy_up` порт перечитывался из конфига. p0.i напечатал
   `ok port0.i` с фактическим портом из `.actual_port`. Вся команда сохранила
   **rc=1** только из-за ожидаемо ещё красных p0.stub/p0.noreport текущего
   дореализационного дерева; именно контролируемая p0.i была зелёной.

4. **И-4, контрпример судьи (2а) — пойман.** Без изменения пробы в
   `proxy_up` добавлен `sleep 2`, в `branch_и` — `sleep 20`.
   `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` →
   **rc=1**: полный барьер **52713 мс** > порога **32400 мс** при независимой
   базе **360 мс**, 15 ветвях из вывода и факторе 6. Самосогласованное зелёное
   состояние v2 устранено.

5. **И-4, честное дерево (2б) — пройдено.** Та же команда на чистом HEAD →
   **rc=0**: полный барьер **6368 мс** ≤ порога **20070 мс**, независимая база
   **223 мс** (медиана 223/223/173), 15 ветвей взяты из финальной строки.

6. **И-4, ловец (2в) — пройдено.** Команда с `--selftest` → **rc=0** по её
   конвенции: честная мера **10109 мс** ≤ **33120 мс**, затем обёртка со
   `sleep 34` измерена **45944 мс** > **33120 мс** и ловец поимённо подтвердил
   красное замедление.

## Живое состояние HEAD

- `bash fixtures/verify_antiplacebo/probe_port0.sh` → **rc=1**:
  p0.stub красна тремя независимыми предъявлениями, p0.i и p0.noreport красны;
  p0.gen/p0.up/p0.conc/p0.src зелёны.
- `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` → **rc=1**:
  ветвь хранения красна, контроль удаления зелёный.
- `bash scripts/check_metering.sh` → **rc=0**, финальная строка:
  `барьер зелёный: 15 ветвей пройдены`.
- `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` → **rc=0**.
- `bash scripts/check_zones.sh` → **rc=0**.
- `npm run check:contract-ready` → **rc=0**.

## Целостность текста v3

- `contracts/017-deflejk-metiringa.md:75-87` синхронизирует И-4 с решением:
  база названа независимой, её измеряемый путь ограничен node + замороженным
  `metering_proxy.ts` + собственными конфигом/ожиданием/гашением пробы, код зоны
  implementer в нём не исполняется; формула использует счёт из вывода и фактор 6.
- `contracts/017-deflejk-metiringa.md:114-117` по-прежнему запрещает морозить
  счёт фикстур/проб/ветвей; новых замороженных счётчиков и wall-порогов дельта
  v3 не добавила.
- `contracts/017-deflejk-metiringa.md:121-130` соответствует наблюдённому
  красному сейчас и зелёному стражу И-4.
- Дельта v3 не добавила запрещённых пар «вход/стаб ↔ ветвь/исход»; привязки
  остались в коде проб. `contracts/017-deflejk-metiringa.md:91-96` не менялись
  в v3: все зоны и строка `РАБОТА НЕ РАЗДАЁТСЯ:` сохранены.

## Находки

Блокирующих находок и советов нет. Обе меры исполнения решения арбитража
различают воспроизведённые обходы от честных контролей; противоречий норме
`AGENTS.md` не обнаружено.

## История круга 1 (FAIL)

FAIL

Предмет суда — закоммиченные на HEAD `e6273371a1df5ee78518881646ed2c9cac818b99`
разделы предмета, приёмки и зон `contracts/017-deflejk-metiringa.md`, а также красные
пробы коммита `47932583b7952fac3c8a0ce9db01b23648e39e9f` (автор `architect`). Набор
артефактов полный: предмет, критерий с rc-командами, исполнители, `ЗОНА`-строки и
явная строка `РАБОТА НЕ РАЗДАЁТСЯ:` присутствуют.

## Проверено фактом

Все прогоны выполнены из отдельного чистого клона HEAD с
`TMPDIR="$PWD/tmp"`.

- `bash scripts/check_metering.sh --port0` → **rc=1**, 39.46 с. Получены ровно
  заявленные поимённые красные исходы: `port0.stub` ×3, `port0.i`,
  `port0.noreport`; зелёные контроли `port0.gen`, `port0.up`, `port0.conc`,
  `port0.src` прошли.
- `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` → **rc=1**,
  1.48 с. Ветвь «хранит» красна: RUN уничтожен при `fails>0`; зелёный контроль
  «убирает» прошёл.
- `bash scripts/check_metering.sh` → **rc=0**, 5.42 с; финальная строка:
  `барьер зелёный: 15 ветвей пройдены`.

Заявленное красное/зелёное состояние пачки совпало с фактом.

## Находки

БЛОКИРУЕТ `contracts/017-deflejk-metiringa.md:49-55,99-107` — контракт сам
утверждает привязки обманных входов к судимым ветвям: немедленно гибнущий `node` и
тень `ss` привязаны к `stub_upstream`, живой стаб без порт-файла — к именованному
исходу той же ветви, а `p0.i` — к телу `branch_и`. Фраза «привязка к входам — по
коду» этого не отменяет: пары вслед за ней всё равно записаны прозой контракта.
Это прямое противоречие `AGENTS.md:121-126` и измеренный рецидив А-62; для
противоречия норме `AGENTS.md` ОБХОД по регламенту критика не требуется. Инварианты
и rc-команды можно оставить, а конкретные пары должны жить только в коде проб
архитектора.

БЛОКИРУЕТ `contracts/017-deflejk-metiringa.md:49-55,94-107` — красная проба
`p0.i`, объявленная доказательством ухода ветви (и) от скана, проверяет лишь слова
в извлечённом теле функции (`scripts/check_metering.sh:2027-2037`): литерал
`free_port` и числовой литерал непосредственно после `"port":`. Она не доказывает
инвариант И-1 и принимает прежний TOCTOU под другим именем.

ОБХОД: оставить выбор свободного порта сканированием, но вынести его в функцию
`scan_port()` вне тела `branch_и` (внутри она может вызывать нынешний `free_port`),
а в `branch_и` записать `proxy_port="$(scan_port)"` и оставить
`"port": ${proxy_port}`. `p0.i` напечатает `ok`: в извлечённом теле нет слова
`free_port` и нет числового JSON-литерала. Текущий полный барьер уже наблюдён
зелёным при живом скане; после честного закрытия остальных срезов все именованные
rc-команды могут быть зелёными, а гонка scan→bind ветви (и) останется.

БЛОКИРУЕТ `contracts/017-deflejk-metiringa.md:66-69,114-120` — И-4 включает
«ветви … не замедлены» и обещает красный исход при регрессе времени больше `2×`,
но названная команда `bash scripts/check_metering.sh` не измеряет длительность и
не превращает превышение в ненулевой rc. В фактическом выводе есть только
логические ветви и финальный счёт; порога времени нет. Следовательно, часть
инварианта недоказуема названной rc-командой.

ОБХОД: на дереве с честно реализованными срезами добавить, например, `sleep 20` в
одну из ветвей обычного `check_metering`. При наблюдённой базе 5.42 с это заведомо
больше `2×`, но `bash scripts/check_metering.sh` всё равно вернёт 0; три scoped-
прогона также останутся далеко внутри объявленного бюджета ~35 минут. Все
приёмочные rc могут быть зелёными при нарушенном И-4.

БЛОКИРУЕТ `contracts/017-deflejk-metiringa.md:76-87` — две `ЗОНА`-строки дают
`architect` и `implementer` один и тот же целый файл `scripts/check_metering.sh`.
Проза ограничивает architect секцией `mode_port0` до заморозки, но не создаёт
машинной границы внутри файла: `check_zones.sh` читает путь целиком. После
заморозки implementer механически авторизован менять и реализацию, и сам красный
барьер. Это тот же класс Н-71 «граница по памяти», а не исполнимая нарезка.

ОБХОД: после заморозки implementer оставляет нынешние сканирующие
`stub_upstream`/`branch_и`, но в своей разрешённой зоне заменяет `mode_port0` на
безусловный успех (либо удаляет его `bad`-ветви), а срез 5 реализует отдельно в
`scripts/verify_antiplacebo.sh`. Тогда `--port0` возвращает 0, проба RUN может
вернуть 0, обычный барьер уже наблюдён с rc=0, а исходный класс Н-45 остаётся.
Зонный барьер такую правку разрешает. Приёмочную пробу нужно отделить в путь, не
выданный implementer, либо дать иной механический способ отличить изменение
предмета от ослабления пробы.

СОВЕТ `contracts/017-deflejk-metiringa.md:145-150` — размер бюджета тиков и его
выражение оставлены исполнителю. Это осознанно названная деталь, но без нижней
меры «покрывает медленный node под контеншеном» разные реализации получат разные
границы класса. За отсутствием отдельного доказанного обхода сверх блокеров выше
это совет, не самостоятельная причина FAIL.

## Остальная сверка

- Срезы 1–5 покрывают Q1–Q6: событие вместо scan/window, двухчастную приёмку,
  А-19, scoped-бюджет как норму заданий, честный комментарий и очередь Н-60.
- Не-цели названы, включая cold-start, вариант В, остальное А-33, Н-60 и CI-
  проводку.
- Замороженного числа ветвей в критерии нет: число берётся из вывода одной мерой
  до/после. Упоминания пяти срезов и трёх независимых исходов `p0.stub` описывают
  структуру предъявленной пачки, а не будущий числовой порог.
- Строки зон соответствуют грамматике пути; `РАБОТА НЕ РАЗДАЁТСЯ:` явно называет
  нераздаваемые пути. Дефект — не синтаксис, а неразводимый общий файл реализации
  и приёмки.

Вердикт `FAIL`: заморозка этой редакции дороже правки контракта и красных проб до
начала реализации.

## История круга 2 (FAIL)

FAIL

Предмет суда — закоммиченный контракт `contracts/017-deflejk-metiringa.md` на
`2d1731c2e302725d46f842e5776e285feb0bd49c` и предъявленная дельта
`e29d212`. Набор артефактов полный: предмет, rc-критерий, названные исполнители,
`ЗОНА`-строки и явная строка `РАБОТА НЕ РАЗДАЁТСЯ:` присутствуют.

Все прогоны и контрольные эксперименты выполнены в одноразовых клонах под
`tmp/critic/017v2b*` с `TMPDIR` внутри клона.

## Проверка четырёх закрытий v1

1. **Проза привязок — закрыто.** Греп по `пример|например|заглуш|стаб|вход|→|исход|ветв|красн|против|подмен|тень|гиб|слом|лов`
   и чтение предмета, инвариантов, зон и §«Красное сейчас» не нашли пары
   «контролируемый вход X → ветвь/исход Y». Строки
   `contracts/017-deflejk-metiringa.md:13,46-50,117-120` называют фактическое
   красное состояние и наблюдаемые инварианты проб, но привязку конкретного
   входа к месту наблюдения оставляют коду проб.

2. **p0.i — заявленные контроли пройдены, но класс закрыт не полностью.** На
   экспериментальной честной реализации `stub_upstream` через `listen(0)` и
   файл-событие плюс ветвь (и) с `port:0` прямая
   `probe_port0.sh` вернула **rc=0**. Реализация с функцией
   `choose_candidate()` поверх прежнего скана вернула **rc=1**: p0.i увидела
   снимок с заранее выбранным портом `12980`. Независимый вариант без
   литерального номера — порт предварительно получен через временный
   `listen(Number(false))`, закрыт и прочитан из файла — вернул **rc=1** со
   снимком порта `34551`.

   Дополнительный контроль нашёл зелёное состояние с заранее выбранным портом:
   первый `--config` при spawn содержит `port:0` и попадает в снимок обёртки,
   второй `--config` содержит предварительно выбранный порт и фактически читается
   штатным парсером прокси, где побеждает последний аргумент. Проба вернула
   **rc=0**; независимо сохранённые значения совпали:
   `dual-config-chosen=46409`, `dual-config-actual=46409`.

3. **И-4 — механизм добавлен, но обещанная сила не достигнута.** На исходном
   дереве `probe_vetvi_ne_zamedleny.sh` вернула **rc=0**: полный барьер
   `10150 ms`, база `753 ms`, порог `45180 ms`. `--selftest` вернул **rc=0** по
   своей конвенции и внутри себя подтвердил контрольное замедление: обёртка
   `sleep 52 s` измерена как `63461 ms > 51600 ms`. Обычная проба с `sleep 20`
   в ветви вернула **rc=1** (`30045 ms > 25380 ms`), а с `sleep 5` — **rc=0**
   (`17311 ms < 49320 ms`); малое замедление ниже явно заданного порога само по
   себе тексту не противоречит.

   Однако база не независима от судимой реализации. В контрольном дереве
   добавлены `sleep 2` в общий `proxy_up` и `sleep 20` в ветвь (и), без правки
   стража. Полный барьер замедлился до `51478 ms`, база выросла до `2678 ms`,
   порог вслед за ней — до `160680 ms`, и страж вернул **rc=0** с сообщением
   «замедления нет». Это кратное замедление, которое текст обещает отмечать.

4. **Машинная граница — прямой слой отделён, но его поведенческая сила остаётся
   предметом находки p0.i.** Прямые приёмочные пробы И-1/И-2/И-4 находятся в
   `fixtures/verify_antiplacebo/`, то есть вне зоны implementer; вспомогательные
   команды полного барьера и дыма закономерно остаются в его зоне. Контрольная
   замена только диспетчера `--port0` и библиотечного выхода в
   `scripts/check_metering.sh` дала диспетчеру **rc=0**, но прямой
   `probe_port0.sh` сохранил **rc=1** на нереализованных срезах. На чистом
   дереве утечка `PROBE017_LIB=1` при прямом запуске
   `bash scripts/check_metering.sh --port0` также дала **rc=1**, а не молчаливый
   успех. При этом зелёный двойной `--config` выше получен изменениями только в
   зоне implementer: отделение файла защищает текст пробы от прямой правки, но
   не делает её наблюдение полного класса достаточным.

## Обязательные прогоны HEAD

- `bash fixtures/verify_antiplacebo/probe_port0.sh` → **rc=1**; p0.stub ×3,
  p0.i и p0.noreport красны, p0.gen/p0.up/p0.conc/p0.src зелёны.
- `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` → **rc=1**;
  ветвь хранения красна, зелёный контроль удаления прошёл.
- `bash scripts/check_metering.sh --port0` → **rc=1** с теми же поимёнными
  исходами прямой пробы.
- `bash scripts/check_metering.sh` → **rc=0**, финальная строка:
  `барьер зелёный: 15 ветвей пройдены`.
- `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` → **rc=0**.
- `bash scripts/check_zones.sh` → **rc=0**.
- `npm run check:contract-ready` → **rc=0**.

Дозаморозочный перенос не ослабил наблюдённые живые поверхности 005/007:
обычный барьер остался зелёным, а новый предметный `--port0` остался красным.
`scripts/check_metering.sh` отсутствует в зоне architect редакции v2; прямые
пробы находятся в её отдельной зоне.

## Находки

БЛОКИРУЕТ `contracts/017-deflejk-metiringa.md:46-50,65-71,94-102,110-120,128-134` —
p0.i наблюдает только конфиг с ожидаемым путём и поэтому не доказывает заявленный
класс «любой заранее выбранный порт». Штатный парсер допускает несколько
`--config` и использует последний; первый конфиг может удовлетворить снимок, а
второй — управлять фактическим bind.

КОНТРПРИМЕР: в зоне implementer ветвь предварительно выбирает порт через
краткоживущий слушатель и файл; `proxy_up` сохраняет этот конфиг вторым аргументом,
а первым передаёт конфиг с `port:0`. Не меняя прямую пробу architect, дерево даёт
`probe_port0.sh` **rc=0**, хотя выбранный до запуска порт и фактический bind равны
`46409`: событие сообщает уже заранее выбранное значение, класс scan→bind не
устранён.

БЛОКИРУЕТ `contracts/017-deflejk-metiringa.md:75-83,94-102,124-134` — страж И-4
строит порог из `proxy_up` той же судимой реализации и затем ещё умножает эту базу
на число ветвей и фактор 4. Реализационное замедление увеличивает собственный
порог быстрее, чем измеряемый полный прогон; `--selftest` проверяет лишь добавку
ровно уже вычисленного порога и этого состояния не различает.

КОНТРПРИМЕР: без изменения пробы добавить `sleep 2` в общий `proxy_up` и
`sleep 20` в ветвь (и). Полный барьер измерен `51478 ms` вместо наблюдённых на
исходном дереве `10150–12430 ms`, но собственная база стала `2678 ms`, порог —
`160680 ms`, и обязательная команда вернула **rc=0** с выводом «замедления нет».
Предмет «ветви не замедлены» и обещание красного при кратном замедлении не
выполнены при зелёном критерии.

АРБИТР: `arbiter`; предмет спора — повторное закрытие силы p0.i и И-4 после
одноимённых блокирующих находок v1: достаточно ли снимка одного `--config` и базы
из судимого `proxy_up` для доказательства заявленных классов.

Вердикт `FAIL`: блокеры прозы и файловой зоны закрыты, однако две поведенческие
пробы допускают наблюдённые зелёные состояния при невыполненном предмете.
