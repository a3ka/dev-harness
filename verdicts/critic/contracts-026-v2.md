accept

# Контракт 026 — критик v2, только дельта зоны

Судимый HEAD: `f859ff4982cef6c76c5aae9397a54f4dd2ea9d12`, свежий клон origin `ssh://git@github.com/a3ka/dev-harness.git` в `tmp/critic026v2-2/repo`. Судится только дельта `frozen/contracts/026/1..HEAD` файла `contracts/026-zhnec-tmp.md`: одна заменённая строка 71, добавляющая `scripts/verify_antiplacebo.sh` в зону implementer исключительно для cleanup `wipe_ro`. Реализация и прежняя приёмка v1 заново не судятся.

Набор артефактов полон: закоммиченные предмет, rc-приёмка, именованные исполнители и ЗОНА-строки. Коммит `f859ff4` содержит `РАЗРЕШИЛ-ВЛАДЕЛЕЦ: contracts/026-zhnec-tmp.md` с причиной v2-респека. Блокирующих находок в дельте нет. Заморозка v2 разрешена; тег этим вердиктом не ставится.

Н-39 дословно: «стабы к ветвям привязывает architect по коду, НЕ проза контракта; контракт несёт инварианты + rc-команды».

## Пять обязательных вопросов

1. **Критерий слабее предмета?** Нет ослабления в дельте (`contracts/026-zhnec-tmp.md:71,89-156`): меняется зона, не инварианты, пробы или требуемые коды. Разрешение ограничено cleanup — восстановлением прав перед удалением; подмена результатов проверок или преждевременное исправление красного входа этой оговорке не соответствует. Обхода приёмки за счёт дельты не установлено.
2. **Готовность недоказуема командами?** Нового пункта готовности строка 71 не вводит; прежние rc-команды сохранены (`contracts/026-zhnec-tmp.md:138-150`). Scoped-регресс `gc_agent_branches` выполнен: rc 0, пять фикстур с зелёным контролем и повторным красным. У `case_zhnets_reapstale.sh` повтор остался rc 1 «не удалось удалить». Это scoped-свидетельство, не полная приёмка реализации или close-out.
3. **Решение молча оставлено исполнителю?** В дельте нет нового молчаливого решения (`contracts/026-zhnec-tmp.md:71`): названы файл, исполнитель и характер изменения — cleanup `wipe_ro`, без изменения приёмочной семантики. A/B-локализация дефекта в раннере дана в задании и записана в коммите; собственного повторного A/B-эксперимента я не заявляю.
4. **Границы исполнения названы?** Да (`contracts/026-zhnec-tmp.md:70-79`): implementer получает один конкретный файл, остальные зоны сохранены. Файловую границу держит `check_zones.sh` по авторству; узость изменения внутри файла задана строкой 71 и проверяется ревью, не самим зонным барьером. Оракул 024 и критерии исключением не затронуты.
5. **Противоречие AGENTS.md?** Нового противоречия норме в дельте не установлено (`contracts/026-zhnec-tmp.md:71`): изменение замороженного текста оформлено v2 с разрешающей строкой владельца и новым вердиктом; критерии не переписаны под код. Scoped-прогон и проверка неизменности приёмочных артефактов выполнены; полный прогон оставлен CI.

СОВЕТ contracts/026-zhnec-tmp.md:81-85,167 — прежние общие фразы всё ещё называют verify_antiplacebo.sh замороженным вне зон. Строка 71 явно задаёт узкое v2-исключение только для cleanup, не общее разрешение менять раннер. При следующей содержательной редакции стоит согласовать упоминания с исключением. Обхода приёмки эта редакционная несогласованность сама по себе не предъявляет и заморозку не блокирует.

## Доказательство и границы

Diff контракта показал ровно одну заменённую строку зоны. `git diff --exit-code frozen/contracts/026/1 HEAD -- scripts/check_no_leak.sh fixtures/gc_agent_branches/ AGENTS.md` завершился rc 0 без вывода. Отдельный diff раннера НЕ пуст: `wipe_ro` и восстановление u+w перед удалением; раннер неизменным не объявляется. Просмотр ограничен основанием зонного исключения и не заменяет суд реализации. `check_no_leak --check` основного дерева не выполнялся. Scratch вердикта расположен в `/tmp/dev-harness-verify/critic026v2-2/`: относительная запись отклонена инструментом Н-85, разрешённый scratch использован для церемонии hash-object → update-index → commit под critic → checkout только вердикта.

СТЕНГРАММА: свежий origin-клон HEAD=f859ff4982cef6c76c5aae9397a54f4dd2ea9d12; git diff frozen/contracts/026/1 HEAD -- contracts/026-zhnec-tmp.md: одна заменённая строка 71; diff --exit-code оракула/фикстур/AGENTS.md: rc=0, вывода нет; bash scripts/verify_antiplacebo.sh --scope gc_agent_branches: rc=0, «барьеров: 1 · фикстур: 5 · предъявлено красным повторным прогоном: 5», case_zhnets_reapstale — повтор rc=1 «не удалось удалить».
