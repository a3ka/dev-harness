accept

# Адверсарий: контракт 004, замер закрытия круга 3

Предмет: `850b29d`; исполнение закрытия: `d74e802`. Это замер закрытия
единственной находки круга 2 из
`verdicts/adversary/milestone-004-zakrytie.md`: ошибка `wc -c` не должна
превращаться в размер `0` и успешный результат. Предмет и проверка не
изменялись. Все обманные деревья и запуски созданы только в
`tmp/adversary004-r3/`.

## Позитивные контроли HEAD

| Команда | Код | Существенный вывод |
|---|---:|---|
| `bash scripts/check_ceilings.sh` | 0 | `персоны: 7 файл(ов)`, `правила: 1 файл(ов)`, `раздел требований: 4 черновик(ов) судится, замороженные — по тегам`, `потолки в порядке` |
| `npm run check:antiplacebo` | 0 | `барьеров: 20 · фикстур: 138 · предъявлено красным повторным прогоном: 138`; в том числе `case_razmer_nechitaem.sh` — код 1, причина `размер нечитаем` |
| `npm run check:ci-parity` | 0 | `workflow-команд: 16 · скриптов в приёмке: 24 · объявленных исключений: 8 · расхождений: 0` |
| `npm run check:zones` | 0 | `замороженных контрактов: 4 · объявленных авторов: 2 · коммитов в диапазонах: 110 · проверено по зонам: 69` |

## Обманные заглушки

Для каждой заглушки взята побайтовая копия настоящего
`check_ceilings.sh`, а в изолированном дереве оставлены полный набор семи
фикстур `fixtures/check_ceilings/`, включая новую
`case_razmer_nechitaem.sh`. Запускался настоящий проверяющий:
`bash scripts/verify_antiplacebo.sh <временное-дерево>`.

| Заглушка | Неверное поведение | Код проверки | Наблюдение |
|---|---|---:|---|
| `size-zero` | В обеих ветвях `roles/` и `.omp/rules/` заменяет отказ `wc -c` на `size=0` | 1 | `case_razmer_nechitaem.sh: барьер остался зелёным на обманном дереве — красное не предъявлено`; `барьеров: 1 · фикстур: 7 · предъявлено красным повторным прогоном: 6`; `расхождений: 1` |
| `whitelist` | Вместо чтения тегов считает замороженным только номер `001` | 1 | `case_dvoinik_zamorozhennosti.sh: нет положительного контроля`; `барьеров: 1 · фикстур: 7 · предъявлено красным повторным прогоном: 6`; `расхождений: 1` |
| `only-net` | Принимает `нет`, но отклоняет любой валидный список `- …` | 1 | `case_razdel_grammatika.sh: повторный прогон покраснел, но не назвал причину «(120 байт): нет раздела»`; `барьеров: 1 · фикстур: 7 · предъявлено красным повторным прогоном: 6`; `расхождений: 1` |
| `constant-refusal` | На каждое нарушение печатает только `FAIL > потолка Незаполненные требования`, без имени и размера | 1 | Все 7 фикстур отвергнуты за неназванную причину; например `case_razmer_nechitaem.sh: повторный прогон покраснел, но не назвал причину «размер нечитаем»`; `барьеров: 1 · фикстур: 7 · предъявлено красным повторным прогоном: 0`; `расхождений: 7` |

Точная регрессия круга 2 воспроизведена против полного набора фикстур:
ошибочный `size=0` действительно оставляет битые ссылки зелёными, но новая
фикстура удерживает это исполнением и заставляет `check:antiplacebo` вернуть
`1`. Обе ветви нечитаемого размера проверены одной фикстурой: битая ссылка
создаётся и в `roles/`, и в `.omp/rules/` после зелёного контроля обычных
файлов.

## Вердикт

accept. Единственная находка круга 2 закрыта: её точная регрессия теперь
отвергается, три прежние заглушки по-прежнему отвергаются, а честные позитивные
контроли зелёные. Новых классов находок в этом замере нет; вопрос капа не
поднят.
