FAIL

Пачка не выдержала единственный круг: три обманных состояния прошли барьеры, хотя должны были дать код 1. Дополнительно HEAD не проходит собственную приёмку `check:charter` (код 2).

## 1. Слепок анти-плацебо не видит символическую ссылку

- Класс заглушки: четвёртая форма записи вне `$WORK` после прежних `contracts/`, `tmp/` и `tmp/antiplacebo/`.
- Построение: в отдельном подставном дереве был оставлен честный `verify_toy`; его фикстура дала зелёный вызов, красный вызов (`.slomano`), затем выполнила `ln -s "$WORK/.slomano" "$REPO/несанкционированная-ссылка"`.
- Прошло: `bash scripts/verify_antiplacebo.sh <подставное-дерево>` вернул 0, предъявил красное игрушки и не назвал изменение дерева; ссылка осталась в корне подставного репозитория.
- Должно было упасть: кодом 1 с `дерево изменилось вне $WORK` и именем ссылки. Контракт 001 обещает слепок всего содержимого репозитория, кроме ровно `.git`, `.zones` и `tmp/antiplacebo/run-$$`.
- Точное место: `scripts/verify_antiplacebo.sh:214-215`. `find … -type f -print0` не перечисляет ни символьные ссылки, ни изменения их цели. Следовательно, `snapshot_of` не измеряет заявленную область.

## 2. Неграмматичное разрешение владельца принимается

- Класс заглушки: правильный путь и причина, но не та грамматика — отсутствует обязательный разделитель после закрывающей кавычки.
- Построение: в подставном репозитории с `ustav/1` изменён `ROADMAP.md`; тело того же коммита:

  ```
  РАЗРЕШИЛ-ВЛАДЕЛЕЦ: "ROADMAP.md"причина без обязательного пробела
  ```

- Прошло: `bash scripts/check_charter.sh <подставное-дерево>` вернул 0 и напечатал `уставной документ изменён с разрешения владельца`.
- Должно было упасть: кодом 1. Объявленная грамматика требует `РАЗРЕШИЛ-ВЛАДЕЛЕЦ: <путь> <непустая причина>`; это не путь, затем пробел, затем причина. Иначе барьер принимает форму, которую сам контракт не объявил.
- Точное место: `scripts/check_charter.sh:108-111`. После выделения текста до `"` выражение `reason="${line#\"$path\"}"` не проверяет пробел-разделитель и принимает любой непустой остаток.
- Контроли: строка с отступом и строка с омоглифом `꞉` дали код 1; обычная строка с пробелом дала 0. Пробита именно граница после закрывающей кавычки.

## 3. `check_zones.sh` теряет объявленного автора с табуляцией и пропускает его выход из зоны

- Класс заглушки: правильный ответ не на тот вопрос — имя автора используется как поле TSV без запрета разделителя.
- Построение: замороженный контракт содержал literal-строку `ЗОНА alice<TAB>evil: scripts/`. Затем создан коммит автора с точно тем же git `user.name` (`alice<TAB>evil`), меняющий файл `README.outside` вне `scripts/`.
- Прошло: `bash scripts/check_zones.sh <подставное-дерево>` вернул 0. Вывод показал искажение `alice → evil<TAB>scripts/`; счётчик был `коммитов в диапазонах: 1 · проверено по зонам: 0`.
- Должно было упасть: кодом 1 с `коммит вне зоны`. Имя в `ЗОНА` объявлено как git `user.name`; такой `user.name` реально принимается git и должен быть либо запрещён грамматикой до сериализации, либо сохранён без потери. Сейчас объявленный исполнитель становится «необъявленным» и не проверяется.
- Точные места: `scripts/check_zones.sh:127-154` принимает имя с табуляцией и пишет его через `printf '%s\t%s\n'`; `scripts/check_zones.sh:194-209` затем берёт `cut -f1`, сравнивает уже усечённое `alice` и молча пропускает настоящий `alice<TAB>evil`.

## Дополнительная проба грамматики зон

`ЗОНА utf8: "scripts/a file.sh"` вернула 0, хотя путь с пробелом и кавычками запрещён. В строке `scripts/check_zones.sh:147-154` неразобранная строка расщепляется на две другие зоны: `"scripts/a` и `file.sh"`; обе проходят `path_prefix_valid`. Пустой список путей дал код 1. Нормальный UTF-8 автор `Жора` дал 0 на правке `scripts/` и код 1 на правке вне зоны: нейтрализация работает для обычного имени.

## Проверенные, но не пробившиеся ветви

- Положительный контроль процедуры: `npm run --silent drill:contract-change` → 0; шесть шагов, `freeze_contract.sh` выдал v1 и v2, оба читателя приняли процедуру.
- Локальный `file://` origin с удалённым `frozen/*` и локальным клоном без тегов: писатель отказал кодом 1 (`missing-remote`) до выдачи версии. `origin`, указывающий на локальный путь самого дерева, законно выдал v1. Последовательность v1→v4 выдала именно v2, v3, v4: константы версии нет.
- Инструмент мимо PATH: при PATH с `dirname`, но без `git`, `check_zones.sh` вернул 2 `NOT_IMPLEMENTED`, а не 0.
- Пустой список путей зон дал код 1. Полный `npm run --silent check:antiplacebo` до проб показал 98/98 красных фикстур и код 0.

## Неготовность HEAD

В основном дереве `npm run --silent check:charter` вернул 2: `NOT_IMPLEMENTED: устав не введён — создайте тег ustav/1 последним коммитом документных правок`. Поэтому названная в плане чистая приёмка не может пройти на HEAD независимо от трёх пробоев выше.
