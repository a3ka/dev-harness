FAIL

БЛОКИРУЕТ — В2 снова выпускает census за пределы корня через символьный сегмент пути.

В toy-репозитории рядом с корнем создан `outside/out.txt`; в корне `a` — symlink на `../outside`. Конформная строка замера:

```text
замер: `ls a/*.txt | wc -l` = 1 census a/*.txt
```

содержит только разрешённые сегменты `a` и `*.txt`, команда буквально содержит glob, а M1, M2_pre и M2_post все равны 1. Фактический запуск настоящего предмета:

```text
symlink-escape: rc=0 expected=1; OK
```

Тем самым сегментная проверка `..` защищает только текст glob, но не его разрешение: Bash раскрывает `a/*.txt` в файл вне candidate-root. Это не file-census дерева-кандидата и обход той же корневой причины, что к2 `../`: граница корня проверена лексически, а не по разрешённому объекту. Нужен fail-closed контроль, что каждый раскрытый путь после разрешения остаётся внутри канонического корня (либо запрет symlink-переходов). На третьем круге повторного корня назван арбитр: **arbiter**.

БЛОКИРУЕТ — M2_pre/M2_post не замечает мутацию, если авторская M1 восстанавливает одинаковый census между снимками.

В toy-репозитории до запуска есть единственный `base.txt`. Конформная строка:

```text
замер: `touch transient.txt; : *.txt; rm transient.txt; printf '1\n'` = 1 census *.txt
```

M1 действительно меняет дерево: между `touch` и `rm` glob имеет два совпадения. Однако к началу M1 M2_pre=1, после её возврата M2_post=1 и M1=1; все конечные числа совпадают с N. Настоящий запуск:

```text
k2-m1-restored-mutation: rc=0 expected=1; OK
```

Следовательно, реализована проверка равенства двух чисел, не проверка заявленного свойства «изменение дерева → отказ»: короткая мутация не наблюдаема и проходит. Это повтор корня к2 M1-мутации; на третьем круге назван арбитр: **arbiter**. Для закрытия необходима реальная изоляция/неизменяемый снимок M1 либо иной механизм, наблюдающий операцию, а не только два конечных размера.

## Закрытые к1/к2 обходы и контроли

Полная дословная/эквивалентная матрица против настоящего `scripts/check_spec_ready.sh`:

```text
k1-command-substitution: rc=1; спек-гейт 036: census-глоб вне грамматики path-glob
k1-regex-author: rc=1; спек-гейт 036: перенос зоны: scripts/foo.sh (implementer): нет коммитов автора, закрепляющих покрытие СПАСЕНО (замороженный frozen/contracts/001/1)
k2-tab-author: rc=1; спек-гейт 036: имя автора вне грамматики (табуляция)
k2-dotdot: rc=1; спек-гейт 036: census-глоб вне грамматики path-glob (выход из корня)
k2-m1-create-remove: rc=1; спек-гейт 036: замер расходится: заявлено 1, дерево даёт 0
k2-m1-background: rc=1; спек-гейт 036: дерево изменено замер-командой
honest-single-file: rc=0; OK
empty-glob: rc=0; OK
only-directory: rc=0; OK
```

Последний результат фиксирует текущую объявленную Bash-семантику: `files=( $glob )` считает совпавший каталог как один элемент; самостоятельным обходом без более узкого определения census это не названо. Пустой glob корректно даёт ноль. Позитивный контроль с одним обычным файлом `probes/a.txt` прошёл, поэтому барьер не вечно-красный.

NUL-канал автора не предъявил обхода. В raw git-config было записано имя `nul\0evil`; `git var GIT_AUTHOR_IDENT` вернул `nul <nul@example.invalid>`, то есть NUL не переживает C-строкое чтение конфигурации и не может попасть в `%an`. Locale-вектор при `LANG=en_US.UTF-8` также именованно отвергнут (`rc=1`, `спек-гейт 036: замер не по грамматике: нет census-глоба`).

## Приёмка и свежесть

- Все четыре обязательные red-прогона завершились rc=0: `red_prichina_predmeta_036.sh`, `red_zamer_036.sh`, `red_perenos_zony_036.sh`, `red_spec_preflight_036.sh`. В частности, их стенограмма предъявляет положительные г1/г3е/г4б/г4е/г5б и различение стабов.
- `npm run check:antiplacebo -- --scope check_check_spec_ready`, затем `freeze_contract` и `check_zones` — rc=0; соответственно 3, 15 и 21 повторно предъявленных красных фикстур.
- `git diff --exit-code frozen/contracts/036/1 HEAD -- contracts/036-*.md` — rc=0.
- Детектор: `bash scripts/check_no_leak.sh --snapshot <clone> && bash scripts/check_no_leak.sh --check <clone>` — rc=0, вывод `основной чекаут чист`.

Итог: accept → reviewer → done/036 невозможен. Два новых зелёных обхода нарушают границу корня и гарантию неизменности M1; оба принадлежат уже повторённым корням к2, поэтому дальнейшая достаточность фикса — предмет арбитра, а не четвёртого узкого круга.
