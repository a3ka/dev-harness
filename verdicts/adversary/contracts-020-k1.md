FAIL — контрпример воспроизведён на настоящем `scripts/verify_ci_parity.sh`: сохранённый legacy-запуск через официальный псевдоним `npm run-script` проходит зелёным.

# Контракт 020, к1 — вердикт adversary

## Контрпример: legacy-запуск скрыт формой `npm run-script`

Вне клона собран конформный двухшардный контрольный корень обычным `_fake_root.sh` + `_shard_tree.sh`. Первый запуск настоящего барьера был зелёным (`workflow-команд: 2`, `matrix-ключей: 4`, `анти-плацебо-запусков: 1`, `расхождений: 0`). Затем в главную job добавлен исполнимый прежний шаг:

```yaml
run: npm run-script check:antiplacebo -- --changed ${{ github.event.before }}
```

Повторный запуск настоящего `bash scripts/verify_ci_parity.sh <подставной-корень>` завершился `rc=0`: `workflow-команд: 3`, `matrix-ключей: 4`, `анти-плацебо-запусков: 1`, `расхождений: 0`.

Это нарушает инвариант 4 и чистый cutover: в CI остаётся запуск анти-плацебо вне matrix с `--changed`. Псевдоним не экзотичен: тот же барьер в проверке правила 6 прямо признаёт `npm run-script` официальным эквивалентом `npm run`. Дефект в отдельном сборщике `ANTI_TSV`: он ищет только литерал `npm run check:antiplacebo`, поэтому alias не попадает в `anti_cmds`; правило 6 покрывает команду, а исключительность её не видит. Данная слабая реализация проходит проверку, следовательно это дефект проверки.

Воспроизведение: `bash /tmp/dev-harness-verify/adv-020-toys/npm_run_script_legacy.sh`.

## Контрольные эксперименты

- Честное дерево клона: `bash scripts/verify_ci_parity.sh` — `rc=0`, 29 workflow-команд, 46 scripts, 17 исключений, 35 matrix-ключей, 1 запуск анти-плацебо, 0 расхождений.
- Семь проб 020 пройдены настоящим раннером scoped: все содержат зелёный контроль и затем ожидаемый красный повтор. Покрыты выпавший ключ, дубль, мёртвый ключ, legacy-шаг в канонической форме, сломанный `--scope`, отсутствие matrix и шаблонное имя команды (правило 6).
- Отсутствующий в PATH `python3` не маскируется успехом: настоящий барьер вернул `rc=2` и `NOT_IMPLEMENTED: нет python3`.
- Пустой fixture-free корень даёт `rc=0` без matrix; это соответствует явно объявленной границе инварианта (нет `case_*.sh` — нечего шардировать), не контрпример.
- Статическая проверка боевого `ci.yml`: пять `ap1`…`ap5`, `concurrency`, `timeout-minutes: 20`, статичное имя `check:antiplacebo`; scoped-шаги остаются отдельными в job `ci`, вне matrix. Контрпример относится только к невидимой альтернативной форме запуска.

## Незыблемость

`git diff --exit-code frozen/contracts/020/1 -- 'contracts/020-*.md' scripts/verify_antiplacebo.sh scripts/scope_select.sh` завершился `rc=0`; замороженный контракт и раннеры вне предмета не менялись.

## Раскрытия

1. Н-98 composer-самотрип детектора на живом дереве: благословение владельца 2026-09-17; миграция после done×4.
2. CI зелёный `f4a62c4`: 6/6 success, шардированный.
