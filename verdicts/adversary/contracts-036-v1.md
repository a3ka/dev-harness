FAIL

БЛОКИРУЕТ — В2 обход независимой меры census: `scripts/check_spec_ready.sh` подставляет контрактный `<glob>` без разбора и экранирования в собственный `bash -c` (`files=( $glob )`). Тем самым поле, которое контракт 036 §Инварианты п.2 объявляет относительным path-glob, является исполняемым shell-кодом. Гейт считает слова, напечатанные подстановкой, а не файлы дерева.
ОБХОД: в чистом toy-репозитории без файлов, относящихся к замеру, закоммичена строка:
`замер: `echo 2; : $(echo${IFS}x;echo${IFS}y)` = 2 census $(echo${IFS}x;echo${IFS}y)`
Токен census не содержит пробелов и дословно входит в команду. Команда печатает 2; собственный `bash -c` гейта выполняет `$(echo${IFS}x;echo${IFS}y)`, получает два слова `x` и `y`, собирает массив из двух элементов и тоже получает 2. Реальный файловый набор пуст. Фактический прогон: `census_command_substitution_probe: rc=0; files_under_root=0; OK`.
Следствие: замер-ложь проходит spec-preflight и freeze, хотя В2 обязан независимо пересчитать файловое множество и отказать. Нужны строгая валидация грамматики path-glob до исполнения и раскрытие без интерпретации фрагмента контракта как shell-кода.

БЛОКИРУЕТ — В3 не различает точного автора и совпадение с regex. Для обязательных коммитов `scripts/check_spec_ready.sh` вызывает `git log --author="$role"`; Git трактует это как регулярное выражение/подстроку. Toy: frozen v1 содержит `ЗОНА implementer: scripts/foo.sh scripts/bar.sh`; единственный коммит по `scripts/foo.sh` сделан автором `implementer-evil`; v2 убирает `scripts/foo.sh` и записывает `СПАСЕНО implementer: <SHA implementer-evil> — reason`. `check_spec_ready` дал rc 0/OK. Затем настоящий `freeze_contract.sh` дал rc 0 и создал `frozen/contracts/001/2`; настоящий `check_zones.sh` также rc 0 (`проверено по зонам: 0`).
ОБХОД: имя `implementer-evil` не равно роли `implementer`, но проходит предфильтр spec-preflight из-за regex-совпадения и может быть записан в СПАСЕНО этой роли. Это нарушает требование В3 проверять коммиты автора роли и расходится с точным сравнением `%an` в живом `check_zones.sh`. Нужна выборка коммитов с точным сравнением `%an == <роль>` (не `git log --author`).

Итог: accept невозможен до устранения как минимум В2 обхода; по контрактному маршруту это FAIL, а не reviewer/done.
