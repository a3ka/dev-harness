FAIL

# Адверсарий: контракт 021, замороженный v2

Судилась реализация на HEAD против `frozen/contracts/021/2` (`4b23d46`). Все репро и контрольные эксперименты исполнены в одноразовом клоне `/tmp/dev-harness-verify/adv021`; основной чекаут до записи этого вердикта не менялся. Блокирует один названный обход меры И-7.

## Блокирующая находка

### Мера И-7 принимает зашитую пару вместо общего вычисления

**Обход.** Слабая реализация читает и проверяет теги по настоящему правилу только при аргументах `001 002`, а для любой другой пары трёхзначных номеров возвращает `0`. На конформной последовательной паре, например `022 023`, она тем самым объявит непараллельные окна параллельными. Это нарушает предмет меры: входом являются два номера контрактов, не фиксированная пара фикстуры.

**Контрольный эксперимент.** В одноразовом клоне создана отдельная слабая реализация: для `001 002` она воспроизводит моментные проверки и диагностические отказы, для любых иных `A B` — `exit 0`. Затем вместо предмета на время эксперимента подставлена только эта реализация.

- `bash fixtures/check_zones/red_mera_parallelnosti_okon.sh` → `rc 0`;
- `bash fixtures/check_zones/probe_slabye_realizacii.sh` → `rc 0` (включая честный контроль и все имеющиеся слабые формы).

Значит, текущие ворота проверяют константу `001/002`, но не инвариантность меры к номерам контрактов. Позитивный контроль настоящей реализации был ранее зелёным на тех же воротах (`red_mera...` → `rc 0`, `probe...` → `rc 0`), поэтому это не вечно-красная проверка.

## Неизменность замороженного

`git diff frozen/contracts/021/2 HEAD --stat -- contracts/ plans/` → `rc 0`, stdout пуст.

## Скопированный scoped-регресс

Все команды ниже исполнены в одноразовом клоне и завершились `rc 0`.

| Проверка | Результат |
|---|---|
| `bash scripts/verify_antiplacebo.sh . --scope check_zones` | 19/19 фикстур; каждая предъявила зелёный контроль и именованный красный повтор |
| `bash scripts/verify_antiplacebo.sh . --scope land_agent` | 9/9 фикстур; каждая предъявила зелёный контроль и именованный красный повтор |
| `bash fixtures/check_zones/probe_slabye_realizacii.sh` | пойманы «всегда-0», «пусто-зелёная», «теряет merge-принесённые», «лексикографическая»; честная форма зелёная |
| `bash fixtures/check_zones/red_mera_parallelnosti_okon.sh` | все четыре живых ворот меры зелёные |
| `bash fixtures/check_zones/_schet_fixtur.sh` | 19 |
| `bash scripts/check_zones.sh .` | зелёный; сводка: 19 замороженных контрактов, 2 объявленных автора, 480 коммитов в диапазонах, 348 проверено по зонам |
| `npm run check:nabludenia` | `OK` |
| `npm run check:measure-probe` | зелёный |
| `npm run check:ci-parity` | 28 workflow-команд, 45 команд приёмки, 17 объявленных исключений, 0 расхождений |

## Контрольные эксперименты

- **И-1—И-5, И-8.** Scoped-прогон выше подтвердил у каждого нового case зелёный контроль, повторный красный прогон и именованную причину. Различающие ассерты исполняются до первой пары по протоколу раннера. И-3 остаётся красным на прямом коммите вне зоны, И-5 — после снятия done-тега; это предъявлено в `case_prjamoj_kommit_objavlennogo_avtora` и `case_done_diapazony_neizmenny`.
- **И-1, слабая линейная форма.** В копии `check_zones` нейтрализовано исключение коммитов чужого `land: wip/<OTHER>/…` merge. `case_chuzhoe_okno_ne_suditsja` упал на первом, зелёном, входе с `FAIL коммит вне зоны … plans/chuzhoe-okno.md` (`rc 1`). Контроль действительно ловит старый линейный хвост.
- **И-4.** `case_draft_priznanie` в scoped-регрессе различил tagged-зелёный и untagged-красный пути. В живой истории `git rev-parse 'id/CONTRACT/021^{commit}'` → `9d23ded5391bfe53d22ca7455ac331228ad0dc6e`; живой `check_zones` зелёный.
- **И-7, живые вызовы.** `red_mera_parallelnosti_okon.sh` непосредственно вызывает предмет на перекрывшейся паре (`rc 0`), последовательной (`rc 1`, `не параллельно`), недостающем done-теге (`rc 1` с `done/contracts/002/1`) и последовательной паре со смешанными offset (`rc 1`). Форма с `%ct` отличена от лексикографической. При отсутствии `git` (`PATH=/nonexistent`) предмет вернул `rc 1` и `ОТКАЗ: нет git — меру исполнить нечем`, не успех.
- **Проводка меры.** Шапка `scripts/measure_parallel_windows.sh` прямо отмечает «НЕ БАРЬЕР»; `.github/workflows/ci.yml:54` исполняет `npm run check:measure-probe`, а `package.json` связывает её с `probe_slabye_realizacii.sh`.
- **Нулевой след `lib_zones`.** После живого `check_zones` в корне клона нет `tmp/lib_zones.*` и `tmp/.lib_zones_active`. Дополнительно land_agent-подобный toy (`case_kommit_mimo_merge`) прошёл с `rc 0`; последующий `git -C <toy>/repo status --porcelain` был пуст. `land_agent.sh:134` вызывает именно `zones_load`, поэтому контроль покрывает чужой корень.

## Советы (не блокируют)

1. Добавить в `red_mera_parallelnosti_okon.sh` и/или `probe_slabye_realizacii.sh` вторую пару номеров, отличную от `001/002`, с последовательным входом. Она должна быть красной для реализации, зашивающей текущую пару, при сохранении зелёного контроля честной формы.
2. Параметризовать номера в контрольной истории либо явно прогонять перестановку/подстановку `A,B`; это механизирует требование инвариантности к значениям из демаркации конформности.
