accept

# Контракт 017 — история и перегон усиленных проб

## Круг 1 — краткая история

Предыдущий вердикт `ea091d1` был `FAIL`: (A) сохранённый `invocations.log`
содержал только метаданные, не причину отказа, и (B) страж И-4 брал заявленный
счёт ветвей из финальной строки вместо факта исполнения. Усиления из `e84923e`
судятся только по этим классам; остальные ранее принятые части не расширялись.

## Перегон на HEAD `185a12a`

Все мутации ниже сделаны только в одноразовых клонах
`tmp/adversary-017.N2CjB6/*`; файлы проб не изменялись. Временные каталоги
создавали сами пробы по их правилам (`TMPDIR` для И-3, `$REPO/tmp` для И-4).

### И-3 — `probe_release_hranit_run.sh`

В каждом варианте изменён только журнал `serve()` в
`scripts/verify_antiplacebo.sh`; RUN сохранялся, а положительный контроль
оставался тем же. Команда
`bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` вернула ровно
`rc=1` во всех вариантах:

| Вариант журнала упавшей ветви | Наблюдаемый исход пробы |
|---|---|
| Только строка метаданных, без вывода барьера (контрпример A) | `FAIL`: нет причины `ОТКАЗ: игрушка сломана` |
| Полная, но другая строка: `ОТКАЗ: чужая ветвь сломана` | `FAIL`: нет точного совпадения причины |
| Обрывок: `ОТКАЗ: игрушка` | `FAIL`: нет точного совпадения причины |
| Причина записана только журналу другой ветви; `case_krasnyj_srazu` содержит метаданные | `FAIL`: причина отсутствует именно в журнале упавшей ветви |

Тем самым проба не принимает непустой файл вместо диагностируемой причины
конкретной упавшей ветви.

### И-4 — `probe_vetvi_ne_zamedleny.sh`

В каждом счётном варианте изменён только финальный отчёт
`scripts/check_metering.sh`; фактическое исполнение оставалось из 15 ветвей.

| Вариант | Команда | rc и наблюдение |
|---|---|---|
| Заявлено `100`, без задержки (контрпример C) | `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` | `1`: `заявленное ветвей 100 ≠ измеренному 15` |
| Заявлено `100`, перед отчётом добавлена задержка 20 с (контрпример B) | та же | `1`: та же именованная сверка, до порога не маскируется |
| Заявлено `1` при исполненных 15 | та же | `1`: `заявленное ветвей 1 ≠ измеренному 15`; отказ относится к ложному отчёту, а не к сжатому порогу времени |
| Повторённый маркер `ok` существующей ветви | та же | `0`: уникальный независимый счёт остался 15, ложной красноты нет |
| Добавлены все те же 15 маркеров повторно в перемешанном порядке после отчёта | та же | `0`: порядок и повторы не меняют уникальный счёт 15, ложной красноты нет |

Следовательно, порог вычисляется из измеренного множества имён, а заявленное
число отдельно связано с ним равенством.

### Позитивный контроль и заморозка

На немодифицированном клоне вся указанная приёмочная поверхность вернула
`rc=0`:

- `bash scripts/check_contract_frozen.sh`;
- `bash fixtures/verify_antiplacebo/probe_port0.sh`;
- `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh`;
- `bash scripts/check_metering.sh` и `bash scripts/check_metering.sh --port0`;
- `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh`;
- `bash scripts/check_zones.sh`;
- `npm run check:contract-ready`.

На честном прогоне И-4 измерены `M=N=15`, независимая база 190 мс,
полный путь 15138 мс и порог 17100 мс. Дополнительная побайтовая сверка
`git diff --exit-code frozen/contracts/017/1 HEAD -- contracts/017-deflejk-metiringa.md`
дала `rc=0` и пустой вывод: frozen-блоб контракта 017 не затронут.

## Итог

`accept`: обе усиленные пробы ловят исходные и дополнительные варианты
соответствующих классов; позитивный контроль зелёный.
