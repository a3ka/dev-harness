FAIL

# Контрольные эксперименты реализации 017

Дата: 2026-08-31. Основа: HEAD `057a1c8`, реализация `205355d`.
Каждая мутация выполнялась только в одноразовом клоне `tmp/adversary/017-*` с
`TMPDIR` внутри соответствующего клона. Пробы из
`fixtures/verify_antiplacebo/` не изменялись.

## Снапшот честного дерева

| Команда | Фактический rc | Наблюдение |
|---|---:|---|
| `bash fixtures/verify_antiplacebo/probe_port0.sh` | 0 | 9 строк `ok` (p0.gen, p0.up, p0.conc, p0.src, три p0.stub, p0.i, p0.noreport); 63.75 с |
| `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` | 0 | сохранение RUN при красном и удаление при зелёном; 1.46 с |
| `bash scripts/check_metering.sh` | 0 | `барьер зелёный: 15 ветвей пройдены`; 6.63 с |
| `bash scripts/check_metering.sh --port0` | 0 | все p0-пробы; 63.60 с |
| `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` | 0 | база 171 мс, полный 6502 мс, порог 15390 мс |
| `bash scripts/check_zones.sh` | 0 | зона 017: `scripts/check_metering.sh` и `scripts/verify_antiplacebo.sh` — implementer; пробы — architect |
| `npm run check:contract-ready` | 0 | `check_check_contract_ready: ветви «all» зелены` |

Позитивный контроль тем самым предъявлен: честное дерево зелёное по всей
указанной поверхности.

Сверка заморозки выполнена командой
`git diff --exit-code frozen/contracts/017/1 HEAD -- contracts/017-deflejk-metiringa.md`:
пустой вывод, rc=0.

## Контрпример A — RUN сохранён, но журнал отказа не содержит вывода барьера

**Состояние дерева.** В клоне `017-control2` изменён только
`scripts/verify_antiplacebo.sh`: в `serve()` после строки заголовка вызова
удалена запись `"$out"` в `$d/invocations.log`; остаётся только строка
`=== вызов …: код …, каталог …, корень …`. В памяти раннера `$out` остаётся,
поэтому его решение о зелёном/красном не меняется, но оставленный на диске
журнал не сохраняет ни сообщение барьера, ни названную причину отказа.

Это слабая реализация среза 5: RUN переживает отказ и файл непуст, но улика
для человека не позволяет диагностировать отказ. При этом проба проверяет
только `-size +0`, а не наличие результата вызова/причины.

| Приёмочная команда в изменённом клоне | rc |
|---|---:|
| `bash fixtures/verify_antiplacebo/probe_port0.sh` | 0 |
| `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` | 0 |
| `bash scripts/check_metering.sh` | 0 |
| `bash scripts/check_metering.sh --port0` | 0 |
| `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` | 0 |
| `bash scripts/check_zones.sh` | 0 |
| `npm run check:contract-ready` | 0 |

Вывод: **прошло; дефект приёмки И-3.** Непустота файла не доказывает
наблюдаемость причины. Нужна проба, которая после намеренно красного
игрушечного запуска читает оставленный `invocations.log` и требует в нём
фактический вывод/подстроку названной причины именно того вызова, а не только
любой байт.

## Контрпример B — число ветвей берётся не из исполнения

**Состояние дерева.** В клоне `017-wrong-count` изменён только
`scripts/check_metering.sh`:

1. финальная строка заменена с `барьер зелёный: 15 ветвей пройдены` на
   `барьер зелёный: 100 ветвей пройдены`;
2. непосредственно перед уборкой успешного `$work` добавлен `sleep 20`.

Список `BRANCHES` и действительно выполненные ветви остались из 15 элементов.
Следовательно, финальная строка сообщает зашитое, не соответствующее
исполнению число. Страж времени читает именно это чужое число из финальной
строки и расширяет допустимый порог. В наблюдённом прогоне он получил базу
564 мс, «100 ветвей», полный путь 29636 мс и порог 338400 мс, хотя счёт
предмета — 15. Это правильное числовое поле не на тот вопрос.

| Приёмочная команда в изменённом клоне | rc |
|---|---:|
| `bash fixtures/verify_antiplacebo/probe_port0.sh` | 0 |
| `bash fixtures/verify_antiplacebo/probe_release_hranit_run.sh` | 0 |
| `bash scripts/check_metering.sh` | 0 |
| `bash scripts/check_metering.sh --port0` | 0 |
| `bash fixtures/verify_antiplacebo/probe_vetvi_ne_zamedleny.sh` | 0 |
| `bash scripts/check_zones.sh` | 0 |
| `npm run check:contract-ready` | 0 |

Вывод: **прошло; дефект приёмки И-4.** Приёмка не связывает число из финальной
строки с реально пройденным списком. Нужна независимая проверка соответствия:
либо счёт обязан быть вычислен из наблюдённого вывода поимённых ветвей, либо
страж сверяет финальное число с фактическим количеством строк успеха/явным
списком `BRANCHES`, не находящимся под контролем измеряемой строки.

## Контрольные эксперименты, которые были пойманы

| Класс | Состояние клона | Команда и исход | Вывод |
|---|---|---|---|
| Окно готовности | `017-window`: цикл healthz в `proxy_up` сокращён с 1200 попыток до одной | default `rc=1` (`proxy_up: healthz не ответил`); прямая p0-проба `rc=0` | Ловится обычным полным барьером; p0 не предъявляет медленный healthz. Это не отдельная находка, так как вся поверхность не зелёная. |
| Зашитый порт | `017-constant`: `proxy_port=8765` вместо `0` | p0-проба `rc=1`, `port0.gen` назвала 8765 | Поймано. |
| Пустой список | `017-empty`: `BRANCHES=()` | default `rc=1`, `ОТКАЗ: объявлено ветвей 0, контракт обещает 15` | Поймано fail-closed. |
| Инструмент вне PATH | `PATH=/nonexistent` для барьера | `rc=2`, `NOT_IMPLEMENTED: нет node` | Отсутствие инструмента не засчитывается за зелёный исход. |

Отдельно проверена объявленная граница standard A: в шапке
`probe_port0.sh:27-30` явно названа гонка перезаписи единственного port:0
конфига между `exec` и чтением прокси и прямо сказано, что ловец не заводится.
Это только регистрация границы; новой пробы или изменения архитектурной зоны
не делалось.

## Вердикт и границы

`FAIL`: обнаружены два состояния дерева из зоны implementer, в которых все
семь приёмочных команд зелёные, но соответственно (A) отказ не наблюдаем по
сохранённому журналу и (B) И-4 использует ложный счёт вместо факта исполнения.

Не повторялись известные закрытые классы: двойной `--config`, алиас
скан-функции, самосогласованная база И-4 и изменение проб через зону
implementer. Полный CI не запускался: по условию он уже зелёный и относится к
CI; выполнены только scoped-команды поверхности.
