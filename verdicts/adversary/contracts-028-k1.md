accept

# Контрольный эксперимент к1 — контракт 028

Судимый предмет: `workshop`, `scripts/models_actual.sh` и шесть предъявлений
`fixtures/check_judge_gate/` в чистом клоне на `f4a62c4`. Замороженный
`contracts/028-home-sessii-vne-dereva.md` сверён с
`frozen/contracts/028/3`: `git diff --exit-code` дал rc 0.

## Положительный контроль

Все шесть предъявлений дали rc 0 в клоне:

1. `red_scratch_vnutri_dereva.sh` — именованный ненулевой отказ до создания и exec.
2. `red_zony_net_v_dereve.sh` — внешний HOME, неизменное дерево и передача
   `HARNESS_SESSION_HOME` настоящему потомку; `models_actual.sh` нашёл трейс.
3. `red_xdg_vnutri_dereva.sh` — прямой in-root XDG и внешний симлинк в дерево
   отвергнуты до exec.
4. `red_regress_028_zony_evil.sh` — обе evil-записи под `.zones/` названы
   детектором; контроль пустого входа зелёный; слабая реализация, глушащая всю
   `.zones/`, слепа ровно на том же входе.
5. `canary_solo_028.sh <клон>` — snapshot, check и повторный check дали rc 0.
6. `red_migratsija_sohrannost.sh` — `mv` сохранил все три файла, первый запуск
   использовал единственный внешний корень и `--resume 02802808` дошёл до exec.

Дополнительно `bash scripts/verify_antiplacebo.sh --scope models_actual` дал rc 0:
зелёный контроль `case_model_razoshlas.sh` существует, повторный прогон красен
кодом 1. `bash scripts/check_judge_gate.sh` дал rc 0 в красной и зелёной ветвях.

## Атаки и слабые реализации

Проверены реальные входы предмета:

- двухзвенная цепочка внешних симлинков, указывающая в корень клона, отвергнута
  как для `XDG_STATE_HOME`, так и для `HARNESS_SCRATCH`;
- `XDG_STATE_HOME=<клон>/../xdg-safe-028` прошёл: потомок получил внешний
  `.../xdg-safe-028/dev-harness-sessions/<hash8>/zones/dev`, то есть `..` не
  даёт ложного in-root отказа;
- нестандартный `HOME` внутри клона без ручек корректно отвергнут;
- запуск `workshop` через `env -i` с внешним `HARNESS_SCRATCH` дошёл до exec;
  подставной `omp` увидел одинаковые `HOME` и `HARNESS_SESSION_HOME`, оба
  байт-точно равны `<scratch>/zones/dev`.

Контрольные слабые реализации строились только вне клона:

- удаление `export HARNESS_SESSION_HOME` поймано
  `red_zony_net_v_dereve.sh` как `peredacha-net`;
- нейтрализация именованного канонического отказа поймана
  `red_xdg_vnutri_dereva.sh` на прямом XDG-входе;
- зашитый `hash8=deadbeef` (зафиксирован в игрушечном репозитории, чтобы
  внутренний `git clone` миграционной пробы действительно получил слабый
  предмет) пойман `red_migratsija_sohrannost.sh` как `home-ne-v-novom-korne`.

Ни одна неверная реализация не прошла соответствующее предъявление. Контрпример
на настоящем предмете не воспроизведён.

## Раскрытия и граница

Н-98 composer-самотрип на живом дереве благословлён владельцем 2026-09-17;
миграция выполняется владельцем только после done×4. Поэтому канарейка здесь
запущена на чистом клоне, а не выдана за результат ещё не выполненной миграции
живого дерева. По переданному состоянию CI `f4a62c4` зелёный: 6/6 success,
шардированный. Вердикт не закрывает отдельный нормативный гейт и не заменяет
церемонию миграции.
