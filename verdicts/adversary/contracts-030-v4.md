# Adversary 030, круг 4 — ACCEPT

Судья: `adversary` (отдельное от автора семейство). Проверен клон `/tmp/dev-harness-verify/adv-030-k4/repo` на `HEAD 0fdd23d08c52b970bc369a4bdbd34c22c67100a4`. Предмет и пробы в исходном checkout не менялись; контрреализации строились только в одноразовых копиях под `/tmp/dev-harness-verify/adv-030-k4/`.

## Вердикт

**ACCEPT.** На честном дереве все предъявленные пробы зелёные, включая два независимых свежеслучайных запуска hub-пробы. Все семь предписанных контрреализаций к1×3 + к2×2 + к3×2 отвергнуты именованно с `rc=1`. Новая попытка подстроиться под статистику генератора не дала ложного `rc=0`: свежий полный прогон отказал (`rc=2`, а не зелёный), при этом живой суд во всех шести проходах именованно наблюдал отсутствие `hub`.

По границе решения арбитра (`proby-030-podognannye-kontrrealizacii.md` @ `910a598`) новое основание круга существует **только** при ложном `rc=0` на свежем полном прогоне собственной случайности пробы. Такого прогона не найдено. Cognitive-only варианты не являются основанием нового круга.

## Позитивный контроль

Выполнены три честные пробы; hub-проба запущена дважды, так что обе независимые выборки случайных окружений сошлись зелёным:

```text
bash fixtures/workshop_inventory/red_inventar_hub.sh  # run 1: rc=0
bash fixtures/workshop_inventory/red_inventar_hub.sh  # run 2: rc=0
bash fixtures/qa_kanal_xd/red_kanal_qa_report_issue.sh # rc=0
bash fixtures/gc_agent_branches/red_gonka_svezhij_spavn_vyzhivaet.sh # rc=0
```

Оба запуска inventory-батареи дали ожидаемые результаты: `w1,w2,w3,k1,k2,v1,v2,v3,v5,b1,cr,shape,a6` — красные; `w4,w5,w6,pos,v4` — зелёные. В частности `v5` больше не пропускается: `rc=1`, ожидалось красное. QA и GC-пробы завершились `rc=0`.

## Семь контрреализаций

Все построены на копии честного дерева, чтобы сохранялась рабочая положительная ветвь и нейтрализовался ровно соответствующий инвариант.

| Контрреализация | Нейтрализованный инвариант | Фактический результат пробы |
|---|---|---|
| К1 / A-030-1 | `hub` добавляется только при прежнем sentinel `ZAI_API_KEY=x`; обычная база без `hub` | inventory `rc=1`: `ОТКАЗ: hub в условном контексте — if-охват hub (A-030-4, статическая сеть)` |
| К1 / A-030-2 | точный `xd://report_issue` у непинна блокируется только в `edit`, при сохранённых write/bash | QA `rc=1`, строка 10: `edit-форма → pass … CLI: ожидалось pass, получено block` |
| К1 / A-030-3 | после честного GC ошибочно сносится только свежая непредсказуемая ветка семейства `wip/999/fresh-*` | GC `rc=1`: `свежая пустая ветка с непредсказуемым именем … снесена — жнец узнаёт КОНСТАНТЫ игрушки` |
| К2 / A-030-4 | `hub` оставлен лишь для старого hex-16 класса секрета | inventory `rc=1`: `ОТКАЗ: условное изъятие hub — класс ${TOOLS/,hub/}` |
| К2 / A-030-5 | CLI решает верно, но factory возвращает `undefined` для `write xd://other` | QA `rc=1`, строка 2: `FACTORY: ожидалось block, получено undefined (нет блока)` |
| К3 / A-030-6 | имя `hub` косвенно собирается как `h$(printf ub)` и убирается вне трёх прежних профилей | inventory `rc=1`; свежие проходы `p1`…`p6` именованно красили F=0 и проектный exec: `операнд ведущей сессии без hub` |
| К3 / A-030-7 | `skill://` ошибочно разрешён в обоих зеркалах `judgeEditWrite` и `judgeBash` | QA `rc=1` на порождённой строке 19: `skill://<случайный хвост> … write → block`, CLI вернул `pass` |

Таким образом, проверка не принимает отказ за успех: ни один из неверных, но селективно работающих стабов не дал `rc=0`. В особенности A-030-6 проверен новой формой исполнения, а не текстовым шаблоном: шесть свежих окружений дали наблюдаемый неверный операнд в обоих обязательных exec-путях.

## Новая попытка обхода формы

Построена дополнительная контрреализация «узнать генератор по статистике»: она косвенно собирает имя `hub`, но убирает его, когда длина `ZAI_API_KEY` находится в опубликованном диапазоне генератора `5..48`. Это не использует перечисление старых классов и не содержит буквального `hub` в условии.

Свежий полный запуск inventory-пробы не прошёл зелёным: `rc=2`. Главный живой суд на каждом из шести свежих проходов наблюдал и именованно отклонил базовый и проектный операнды без `hub`; итоговый `rc=2` возник из-за слома селективной батареи, поэтому не может считаться ложным успехом. Это не контрреализация с ложным `rc=0` и не открывает новый круг по арбитражной границе.

## Заморозка и детектор

```text
git diff --exit-code frozen/contracts/030/1 HEAD -- contracts/030-*.md
rc=0

bash scripts/check_no_leak.sh --snapshot /home/aka/Documents/dev-harness
rc=0

bash scripts/check_no_leak.sh --check /home/aka/Documents/dev-harness
основной чекаут чист
rc=0
```

Клон и скретч-контрреализации сохранены до приземления вердикта оркестратором; ветки, worktree и main не трогались.

## Маршрут

Приземлить этот текст с `author=adversary` в `verdicts/adversary/contracts-030-v4.md`. После landing: `reviewer 030 → done/030` разрешён.
