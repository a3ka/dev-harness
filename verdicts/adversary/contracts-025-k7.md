accept

# Адверсарийский вердикт — контракт 025, круг к7

Судимая база: `f2fa54c3e81ec8f1f92fdbad084dff3e965e8eae`; фикс к7 — `4f5a0e4`. Предмет: `fixtures/check_runner_hygiene/probe025_dochernij_vector.sh`. Прогоны выполнены в отдельном свежем клоне, синтетические улики создавались вне субъекта в `/tmp/dev-harness-verify/` и после прогона удалены.

## B-025-k6-1: reverse-order join закрыт

Новая восьмифазная синтетическая батарея строила полноценные MAIN+CHILD улики и проверяла парсер через `--parse`:

- честный положительный контроль: `rc 0`;
- три попытки одного канонического `yes | head -1` (forward-success, result-first success, result-first success): `rc 0`;
- та же трёхпопытная группа, где третья result-first попытка имеет `isError=true, exitCode=1`: `rc 1`, `ЛОЖНАЯ КРАСНОТА канарейки yes|head MAIN`;
- после этой смешанной reverse-order/positive-группы добавлена result-first пара `toolResult.toolName=read` / `toolCall.name=bash`: `rc 2`, `несогласованность имени инструмента`.

Это не повторяет двухпарные фикстуры к6: в одной улике одновременно есть три+ канонические попытки, обратный порядок и позитивный вентиль. Позднее обогащение после полного join восстанавливает `cmd` result-first вызовов, а сверка имён срабатывает независимо от порядка.

Границы join после перестройки также отказали точно:

- осиротевший result в начале файла перед всеми корректными парами: `rc 2`, `результат без вызова`;
- осиротевший call после полного корректного набора: `rc 2`, `вызов без результата`;
- result-first корректная пара, затем второй call с тем же id: `rc 2`, `два вызова`;
- result-first корректная пара, затем второй result с тем же id: `rc 2`, `два результата`.

## B-025-k6-2: positive_verdict действительно судит все попытки

Трёхпопытный положительный контроль выше остаётся зелёным только когда успешны все три; добавление одной поздней result-first неуспешной попытки даёт именно красное `rc 1`. Следовательно, позитивный вентиль не выбирает первую попытку и не зависит от порядка строк.

## Структурный аудит `[0]`

Полный поиск `\[0\]` по `probe025_dochernij_vector.sh` не выявил рабочей first-wins индексации в семантических вентилях. `positive_verdict()`, `attribute()` и `rc_vector_verdict()` используют агрегацию `any()`/списки по всей канонической группе. Оставшиеся рабочие `[0]` выбирают только образец для уже принятого решения или диагностики (`bad[0]`, `canon[0]`), единственный идентификатор при уже обнаруженной ошибке join, элемент уже проверенного singleton-набора транскриптов либо поля итоговой таблицы; остальные в комментариях описывают прежний дефект. Ни один не определяет исход группы канонических попыток.

## Регрессия

Все восемь существующих `red_zond_*.sh` прошли со своими ожидаемыми исходами, включая reverse mismatch/leak и все positive-ветви. Шесть статических инвариантных фикстур также завершились успешно:

```text
red_strazh_vectora_utechki.sh
red_deni_patte_rny_zapisi.sh
red_granica_nepin_pipe_tee.sh
red_pipefail_prefiks.sh
red_marker_exit.sh
red_pin_allowlist.sh
```

## Вердикт

**accept.** Оба блокера к6 закрыты: result-first пары полноценно joined и сверяются после полного файла/orphan-check, а все позитивные канонические попытки проходят общий аудит. Положительный контроль зелёный; новые слабые варианты дают соответствующие red/refuse, а прежняя батарея не регрессировала.
