# Adversary 034 — круг 1: суд реализации

**Вердикт: ACCEPT.** Суд проведён в чистом отдельном клоне на `71c6a08f74fc034c8fe2801c9c46e0ffb49fa2ff`; после прогонов `git status --short` пуст, HEAD не изменился. Предмет: реализация `a2fee13`, frozen-контракт 034/1.

## Приёмка

1. `bash scripts/verify_antiplacebo.sh --scope check_zones` — **rc 0**, `21/21` фикстур предъявлены красным повторным прогоном.
2. `bash scripts/verify_antiplacebo.sh` — **не дал итогового rc**: harness оборвал внешний полный прогон по лимиту `600 s`, не по отказу предмета. До обрыва все показанные строки были `ok`; он дошёл через семейства `check_approval` … `check_judge_gate`. Это именно таймаут посторонних семейств, а не наблюдённый FAIL 034. Изолированные прогоны: `--scope check_zones` — rc 0, `--scope check_staged` — **rc 0, 28/28**. Полную зелень и CI этим не заявляю; CI-поллинг остаётся оркестратору.
3. `bash fixtures/verify_antiplacebo_probe/red_katalog_ne_flagaetsa.sh` — **rc 0**, все 5 ворот. В частности контрпример к1-A: нелегальный каталог вне диффа + легальная probe-правка вернул rc 1 и именованный `FAIL fixtures/stranaja`; ранний `MODE: none / rc 0` его не стёр. Контрпример к1-B: селектор дал только `MODE: needs-full`, rc 2, без `KEY:`; раннер — `MODE: none`, rc 0, без исполнения `case_porcha`.
4. `bash fixtures/verify_antiplacebo_probe/red_granicy_klassa.sh` — **rc 0**. Без маркера, маркер+`case_*`, а также маркер в барьерном каталоге все отвергнуты.
5. `bash fixtures/check_zones/red_zamer_podstavnoj_schetchik.sh` — **rc 0**. Позитивный контроль: toy с K=3 дал ровно 3. Исполненная подмена 033-к2 — ветвь на приёмочном аргументе `.` с константой `проверено по зонам: 1` — принята старым N>0-гвардом, но известный ответ K=5 её отверг (`1 ≠ 5`). На абсолютном пути та же подмена честна с K=5, поэтому предъявление локализует дефект именно на форме `.`.
6. Регрессия существующей семьи: `bash scripts/verify_antiplacebo.sh --scope check_staged` — **rc 0, 28/28**.

## Исполненные обходы

- **обход = нелегальный каталог без маркера, но только с `red_*.sh`.** В отдельном mini-дереве получен rc 1 и `FAIL fixtures/stranaja: фикстура без барьера`; имя `red_*` не стало неявным разрешением.
- **обход = пустой `.probe-only`.** Получен rc 1 с прежним флагом `фикстура без барьера`; пустота не является причиной-объявлением.
- **обход = подмена содержимого непустого маркера произвольным текстом.** Минимальная честная структурная реализация остаётся зелёной (rc 0): контракт намеренно судит непустоту механически, а содержание — cognitive-only.
- **обход = маркером замаскировать `case_*.sh`.** Получен rc 1, `смешение ролей`.
- **обход = маркером замаскировать каталог с ключом барьера.** Получен rc 1, `противоречие ролей`.
- **обход = пустой массив case-файлов принять как «нарушений нет».** Удаление единственного `case_*.sh` у барьера дало rc 1, `нет фикстур`.
- **обход = инструмент в PATH существует, но падает rc 127, а выборка объявляется успешной.** Подставной `git` с rc 127 при `scope_select --changed` дал fail-closed `MODE: needs-full`, rc 2 и ни одной `KEY:`-строки — не scoped-успех.
- **обход = константный счётчик вместо фактического замера.** Точная исполненная ветвь 033-к2 на `.` дала 1 против построенного K=5 и ловится известным ответом.

Дефект обходом не найден. Позитивные контроли присутствуют: честный mini probe-каталог проходит полный/scoped/changed, честный toy `check_zones` сходится с K=3, а штатные `check_zones` и `check_staged` проходят свои повторные красные предъявления.
