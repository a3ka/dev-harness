accept

# Адверсарий 016 — суд ИСПОЛНЕНИЯ РЕШЕНИЯ 67fb3b1, круг 4

Проверено на HEAD `181cfb5` в `/home/aka/Documents/dev-harness` 2026-08-30.
Предмет — только исполнение решения `67fb3b1`; дерево не менялось, кроме этого
вердикта. Мои одноразовые репро — `/tmp/adversary016r4_repro.sh` и его `mktemp`
скратч (после прогона удалён).

## По пунктам РЕШЕНИЯ

### §(в): двухфазная проба — исполнено

`scripts/check_hooks.sh:104–223` создаёт scratch ровно через `mktemp -d`, без
переданного шаблона; копирует проверяемый hook, а живой `check_staged.sh` и его
библиотеки — из `SELF_DIR`. В одном вызове пробы он:

1. строит чистый staged `scripts/clean_${clean_token}.txt`, где
   `clean_token=$(printf '%04x%04x' "$RANDOM" "$RANDOM")`, и требует `p1_rc=0`;
2. затем строит `scripts/bad_${bad_token}\nname.txt`, где `bad_token` получен
   заново из `$RANDOM`, и требует `p2_rc!=0`, именованную причину и
   `grep -qF "$bad_token"` в выводе.

**Мои пересобранные репро раунда 3:**

```text
$ bash /tmp/adversary016r4_repro.sh .
CAUGHT sniffer phase=1
CAUGHT forged-output phase=2
MEASURE scratch-unique first=/tmp/tmp.SkwXhHrvNh second=/tmp/tmp.us9lXltSbb
BOUNDARY standard-A-discriminator checker_rc=0
```

* `sniffer`: hook возвращает постоянный отказ только при `pwd=/tmp/*`, а вне
  скратча возвращает 0. Барьер стал красным с собственной причиной фазы 1
  `чистый staged отклонён`.
* `forged-output`: hook честно пропускает чистый индекс, читает staged NUL-пути
  и при control-имени печатает константное `fixed-forged`, не содержащее токен.
  Барьер стал красным по фазе 2 с `не упомянут`.
* Два захваченных фактических пути независимых вызовов различны; это замер
  неповторяемости scratch-имени, а не предположение о ней.

Тем самым обе старые проходившие неверные формы ловятся именно назначенными
фазами. Отдельный положительный контроль не вечно-красный: полные пробы ниже
сначала запускают честные минимальные механизмы зелёным.

### §(б)-остаток: атака исполнена; находка за границей РЕШЕНИЯ 67fb3b1

Тот же мой репро строит hook-дискриминатор: лишь когда видит признаки
синтетического корня (`contracts/001-x.md` и `.git`), он честно `exec`-ит
скопированный `check_staged.sh`; вне такого корня возвращает 0. Он прошёл обе
фазы (`checker_rc=0`). Случайный токен не помогает: честный судья в toy читает
и называет конкретный случайный путь, не предсказывая его.

Это ровно объявленный в `67fb3b1` §(б)-остаток — дискриминатор toy-против-
реального со встроенной честной проводкой, строго сложнее обычного честного
`exec`. Поэтому **за границей (РЕШЕНИЕ 67fb3b1)**, не FAIL. Внутри оставшейся
границы неверной реализации, прошедшей двухфазную форму, не найдено.

### §(а) и §(б) шапки-границы — исполнено

Проверены фактическими `grep -F` (rc=0):

* `scripts/check_staged.sh:31–37` содержит `ГРАНИЦА КАНАРЕЙКИ (РЕШЕНИЕ
  67fb3b1, §(а) стандарт-А)`, explicit fail-closed для битого `python3`,
  исключение адверсариального PATH-двойника и ловец — доверенная среда
  прогона.
* `scripts/check_hooks.sh:18–24` содержит `ГРАНИЦА ПОВЕДЕНЧЕСКОЙ ПРОБЫ
  (РЕШЕНИЕ 67fb3b1, §(б)-остаток стандарт-А)`, формулирует
  дискриминатор+честную проводку и ловец — доверенность среды/ревью
  коммиченного hook.

Интерпретатор-двойник в PATH (§(а)) также **за границей (РЕШЕНИЕ 67fb3b1)**;
он не предъявлялся как FAIL-предмет.

### Красные кейсы, Н-39 и положительный контроль — исполнено

`fixtures/check_hooks/case_huk_sniffer_toy.sh` предъявляет свой дефект фазой 1,
а `case_huk_forged_output.sh` — фазой 2. В шапке
`fixtures/check_hooks/probe_check_hooks_krasnyj.sh:7–25` декои привязаны к
наблюдаемым входам кодом пробы, не этим вердиктом. Н-39 дословно: «Контракту
ЗАПРЕЩЕНО утверждать пару „обманный стаб ↔ ветвь“: привязку стаба к входу, где
его дефект наблюдаем, делает тот, у кого перед глазами код» (`AGENTS.md:124–127`).

На живом HEAD обе полные пробы завершились rc=0. Они предъявили честную форму
зелёной до порчи и именованные красные повторные прогоны; в частности:

```text
$ bash fixtures/check_staged/probe_check_staged_krasnyj.sh
ok: стаб честной формы — все пять case: зелёный контроль + красное повтором
ok: стаб канарейка-слеп пойман на входе case_imja_fake_python3_exit_1
ok: судья зелёный на живом дереве (staged пуст, всё в зоне либо автор не объявлен)

$ bash fixtures/check_hooks/probe_check_hooks_krasnyj.sh
ok: стаб честной формы — все семь case: зелёный контроль + красное повтором
ok: стаб ф1-декой пойман на входе case_huk_sniffer_toy
ok: стаб ф2-фикс-декой пойман на входе case_huk_forged_output
ok: механизм установки хука цел на живом дереве
```

## Полный таймлайн прогонов

1. Прочитаны целиком РЕШЕНИЕ `67fb3b1`, реализация `8aa2c6c` и fixture-слой
   `ae2b257`; затем прочитаны шапки/связи проб и Н-39.
2. `bash scripts/verify_antiplacebo.sh . --scope check_staged` → rc=0, 5/5;
   `--scope check_hooks` → rc=0, 7/7; `--scope spawn_agent` → rc=0, 1/1;
   `--scope land_agent` → rc=0, 8/8; `--scope gc_agent_branches` → rc=0, 2/2;
   `--scope check_zones` → rc=0, 13/13; `--scope check_contract_frozen` →
   rc=0, 6/6. Во всех scoped-прогонах есть зелёный контроль и повторный
   именованный красный сценарий.
3. Выполнены обе полные пробы выше → rc=0.
4. Канарейка И-5 выполнена дословной командой из
   `contracts/016-izoljacija-agenta.md:102–110`:

   ```bash
   ! grep -rnE '\bgit\b[^|<>]*[[:space:]](commit|merge)[[:space:]]' scripts/ | grep -vE ':[0-9]+:[[:space:]]*#' | grep -v merge-base | grep -vE 'user\.(name|email)=|GIT_(AUTHOR|COMMITTER)_(NAME|EMAIL)' | grep .
   ```

   Она вернула rc=0 (мера пуста).
5. Выполнен мой `/tmp/adversary016r4_repro.sh .` → rc=0 с результатами,
   приведёнными выше: две обязательные красноты, замер двух разных scratch и
   единственный surviving standard-A discriminator.
6. Проверены обе шапки-границы точными `grep -F` → rc=0.

## Вердикт

**accept.** Исполнение `67fb3b1` закрывает обе предъявленные формы: sniffer
красен фазой 1, forged-output — фазой 2 с непредсказуемым для константной
подделки токеном; положительный контроль зелёный. Единственная прошедшая
атака воспроизводит прямо объявленный остаток стандарт-А и потому не является
FAIL-предметом этого круга.
