accept

# Адверсарий: контракт 024, круг к7 — детектор утечек основного чекаута

Судимый HEAD `main`: `f2fa54c3e81ec8f1f92fdbad084dff3e965e8eae`. Предмет, предыдущий FAIL к6 и арбитраж `bash-env-predel-024.md` прочитаны целиком. Все исполнения были в отдельном свежем клоне `/tmp/dev-harness-024-k7-run`; toy-репозитории и временная контрпримерная оснастка жили только под `/tmp/dev-harness-verify/adv024k7/`.

Стенограмма чистой церемонии в отдельном клоне: `bash scripts/check_no_leak.sh --snapshot /tmp/dev-harness-024-k7-run && bash scripts/check_no_leak.sh --check /tmp/dev-harness-024-k7-run` → rc 0, `основной чекаут чист`.

## Блокер 3 к6: новые вариации S-dotgit-hook-symlink

Построен один драйвер с отдельным честным baseline перед каждой атакой. Позитивный контроль — минимальный живой git-репозиторий без изменения после snapshot — дал rc 0 и `основной чекаут чист`.

Все шесть новых неверных реализаций hook-конфигурации были отвергнуты ровно как утечки: rc 1, `основной чекаут загрязнён` и имя соответствующего пути `.git/hooks/`.

1. `post-commit` — симлинк на внешний исполняемый файл по абсолютной цели.
2. `post-checkout` — симлинк с относительной целью, разрешающейся вне `.git/hooks/`.
3. `pre-merge-commit` — абсолютная цепочка: hook → внешний симлинк → внешний исполняемый файл.
4. `pre-rebase` — относительная цепочка: hook → симлинк внутри hooks → относительная внешняя исполняемая цель.
5. `aux-directory` — симлинк на каталог, помещённый внутри `.git/hooks/`.
6. `pre-commit` уже был симлинком в baseline; его перенацеливание на другой внешний исполняемый файл после snapshot дало новую строку манифеста и rc 1.

Это не повтор к6 `pre-push`: проверены другие имена hook, абсолютные и относительные цели, обе формы цепочек, каталог и изменение уже базированного симлинка. Поведение объясняется кодом `emit_dotgit_manifest_walk`: он больше не пропускает `-L`, а фиксирует `DOTGIT:SYMLINK:<readlink-цель>`; `readlink` резолвится через доверенный абсолютный путь и его отказ fail-closed. Ложного rc 0 не найдено.

## Предел BASH_ENV и демаркация

Пункт (в) в «Остаточных рисках» контракта совпадает с дословным решением арбитра: он называет доверие к коду, исполнившемуся до первой строки bash, подмену любого командного слова (включая абсолютный путь, `builtin` и `command`), ложный rc 0 и отсутствие bash-закрытия; также совпадают ссылка на З1 и граница ответственности вызывающей стороны. Код этому не противоречит: его in-process `unset` снижает известную поверхность, но сам запускает командные слова и не заявляет невозможную защиту от предисполнительской функции. Кап арбитража соблюдён: новые вариации BASH_ENV-перехвата не предъявлялись.

## Регрессия ворот и позитивные контроли

- `bash fixtures/check_judge_gate/red_detektor_utechek.sh` → rc 0: все 18 ворот зелены, включая 15–18 (отказ `git status`, отсутствие `sha256sum`, нечитаемый путь и недостижимый HEAD submodule).
- `bash fixtures/check_judge_gate/probe_slabyh_detektora.sh` → rc 0: честная форма проходит 18 ворот; все 17 слабых форм пойманы по номеру ворота и диагностической причине; плацебо `exit 1` без причины отвергнуто.
- `bash fixtures/check_judge_gate/canary_zhivoj_024.sh /tmp/dev-harness-024-k7-run` → rc 0: snapshot, check и повторный check все rc 0; оба check напечатали `основной чекаут чист`.

Вердикт: accept. Блокер 3 к6 закрыт фактически; новый вне-BASH_ENV обход не найден.