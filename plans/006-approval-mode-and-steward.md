# План 006 — режим подтверждениями, роль steward

1. `.omp/config.yml`: tools.approvalMode: always-ask (проектный слой, не глобальный).
2. `workshop`: убрать TALK_TOOLS/WORK_TOOLS, --work/--talk, требование --plan. Один
вход.
3. Норма (AGENTS.md, roles/*): режим — политика подтверждений, не инвентарь.
4. decisions/: извлечь 7 записанных решений владельца из прозы. Поля:
   дата, вопрос, решение, основание, область, условие пересмотра.
5. roles/steward.md: model @plan, вердикт decisions/proxy/, ответ только со
   ссылкой на прецедент, иначе отказ. Границы: объём/порядок/приоритет.
6. roles/arbiter.md + AGENTS.md: поправка о делегировании воли, дата 2026-08-17.
7. Фикстуры: правка при always-ask без подтверждения не проходит; вердикт
   steward со ссылкой на несуществующую запись → код 1; корпус без основания → 1.
