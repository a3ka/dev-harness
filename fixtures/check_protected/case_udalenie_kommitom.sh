# ПРИЧИНА: существовал и на HEAD его нет: plans/001-p.md
#
# Способ первый, самый простой: файл удалён коммитом. Прежние редакции правила на соседнем
# проекте начинали именно с него — и на нём же и заканчивались, пока не нашёлся второй способ.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"
g "$R" rm -q plans/001-p.md
g "$R" commit -q -m 'план удалён'
"$BARRIER" "$R"
