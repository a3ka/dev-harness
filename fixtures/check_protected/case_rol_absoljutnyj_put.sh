# ПРИЧИНА: роль вне объявленной грамматики
#
# Третья форма: абсолютный путь. Арбитраж отметил отдельно, что сегодняшний выход на таком
# значении был 128 — то есть барьер нарушал СВОЙ ЖЕ объявленный контракт 0/1/2. Отказ обязан
# быть кодом 1 с названным значением, а не аварией git.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"
printf -- '---\nname: abs\nverdict: /etc/verdicts/\n---\nроль с абсолютным путём\n' > "$R/roles/abs.md"
commit_all "$R" 'роль объявила абсолютный каталог вердиктов'
"$BARRIER" "$R"
