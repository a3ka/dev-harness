# ПРИЧИНА: роль вне объявленной грамматики
#
# Четвёртая форма: `../` — выход за корень проверяемого дерева. Отдельная фикстура, а не
# вариант абсолютного пути: это другой способ назвать чужое место, и правило обязано держать
# основание, а не симптом.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"
printf -- '---\nname: up\nverdict: ../verdicts/\n---\nроль наружу\n' > "$R/roles/up.md"
commit_all "$R" 'роль объявила каталог за корнем'
"$BARRIER" "$R"
