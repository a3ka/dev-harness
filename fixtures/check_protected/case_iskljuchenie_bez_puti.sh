# ПРИЧИНА: не назвало путь
#
# Усиление против плана 005 §4, и вот его цена, предъявленная фикстурой: план требовал лишь
# наличия строки `ALLOW-ARTIFACT-DELETE:` в теле коммита. Безымянная строка — индульгенция:
# она снимает защиту со всего дерева НАВСЕГДА, потому что тело коммита уже не переписать.
# Разрешение на один файл и разрешение на все — разные предметы, и в истории их нечем развести.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"
g "$R" rm -q plans/001-p.md
g "$R" commit -q -F - <<'MSG'
План удалён

ALLOW-ARTIFACT-DELETE:
MSG
"$BARRIER" "$R"
