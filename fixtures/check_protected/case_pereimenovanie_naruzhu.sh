# ПРИЧИНА: существовал и на HEAD его нет: plans/001-p.md
#
# Способ второй: файл не удалён, а ПЕРЕИМЕНОВАН за пределы защищённого пути. Diff показывает
# переименование, а не удаление, и правило, написанное про удаление, молчит. Правило от
# результата видит одно: по защищённому пути на HEAD файла нет.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"
mkdir -p "$R/notes"
g "$R" mv plans/001-p.md notes/001-p.md
g "$R" commit -q -m 'план вынесен в заметки'
"$BARRIER" "$R"
