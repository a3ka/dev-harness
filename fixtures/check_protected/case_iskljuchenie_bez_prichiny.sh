# ПРИЧИНА: без причины — разрешение без записанной причины неотличимо от опечатки
#
# Путь назван, причина нет. Такое разрешение нечем отличить от строки, вставленной по
# привычке или по ошибке: через месяц читающий историю не узнает, было ли удаление решением
# или недосмотром. Причина — часть разрешения, а не украшение к нему.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"
g "$R" rm -q verdicts/adversary/v-a.md
g "$R" commit -q -F - <<'MSG'
Вердикт удалён

ALLOW-ARTIFACT-DELETE: verdicts/adversary/v-a.md
MSG
"$BARRIER" "$R"
