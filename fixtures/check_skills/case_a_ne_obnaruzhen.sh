# ПРИЧИНА: не обнаружен
#
# Ветвь (а): скил не обнаружен omp. Захват запуска: подставной omp отвечает на вызов скила —
# если барьер по какой-то причине не вызывает omp (зашитая константа, инструмент мимо PATH),
# проверка пусто-зелёная. Красное: имя скила в запуске не совпадает с каталогом.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_tree "$R"
"$BARRIER" "$R"

# Порча: имя во фронтматтере не совпадает с каталогом — скил не может быть обнаружен
sed -i 's/^name: grilling$/name: nonexistent-skill/' "$R/skills/grilling/SKILL.md"
"$BARRIER" "$R"
