#!/usr/bin/env bash
# СТАБ (Н-39 — привязка кодом, не прозой контракта) семьи case_push_proba
# (контракт 022, И-10; форма входов — РЕШЕНИЕ арбитража f712e6e).
#
# ВХОД-1 «no-op»: саботажная форма pre-push. Связь с кольцом ОБЪЯВЛЕНА
# не-комментарной строкой ниже — текст-фаза 7 check_hooks её видит и проходит;
# суда НЕТ: `exit 0` пропускает и чистый, и красный диапазон.
#
# Дефект наблюдаем РОВНО на этом входе: двухфазная push-проба обязана умереть
# фазой 2 с «красный диапазон прошёл (rc=0); pre-push не судит или fail-open».
# На диагональных входах (stab_push_proba_bez_*) no-op вёл бы себя так же, как
# честная проба не обязана — наблюдаемость не переносится (Н-39: требовать от
# стаба красноты там, где он честен, — требовать лжи в диагнозе).
: 'scripts/check_charter.sh'
exit 0
