# ПРИЧИНА: грамматика требует ГГГГ-ММ-ДД
#
# Дата в записи не украшение: конфликт двух применимых прецедентов решает ИМЕННО
# она (контракт 002, критерий 3). Прежняя редакция проверяла только непустоту, и
# «вчера после обеда» проходило зелёным (находка 2 круга адверсария по
# майлстоуну 002) — сравнить такие записи по времени нечем.
#
# Зелёный контроль: семь записей с датами в формате → 0. Красное: у законной
# записи 001 подменена ТОЛЬКО дата, остальные пять полей, имя, основание и состав
# штатные — барьер обязан назвать запись и поле.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

write_decision "$R/decisions/001-sessiyu-vedet-rol.md" "вчера после обеда" \
  "кто ведёт сессию" "роль с явным --model" "ca29f7e" "вход" "потеря записи"
commit_all "$R" 'в записи 001 дата без грамматики'
"$BARRIER" "$R"
