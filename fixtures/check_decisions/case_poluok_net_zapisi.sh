# ПРИЧИНА: нет записи
#
# Пропуск одной из семи записей — самый частый класс дефекта: номер назначен
# механизмом, а файл забыт. Тогда `check_ids.sh` уже отказывает (нет тега
# `id/ADR/<N>`), а `check_decisions.sh` ловит другой предмет — отсутствие
# прецедента, на который опирается steward. Проверка названа, потому что
# «номер реестра полон» ≠ «реестр решений полон»: первое держит `check_ids`,
# второе — этот барьер.
#
# Зелёный контроль: все семь на месте → 0. Красное: 005 удалён.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

rm -f "$R/decisions/005-kritik-na-top.md"
commit_all "$R" 'запись 005 случайно удалена'
"$BARRIER" "$R"
