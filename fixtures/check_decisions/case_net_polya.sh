# ПРИЧИНА: нет поля
#
# Шесть полей объявлены грамматикой контракта и обязаны присутствовать.
# Пропуск любого — отказ с названным именем поля, потому что молчаливая
# трактовка «нет поля = пустое решение» обесценивает реестр.
#
# Зелёный контроль: все поля на месте → 0. Красное: 005 без поля «область».
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

# Запись 005 без поля «область». Остальные пять на месте, основание валидное —
# барьер должен краснеть именно из-за отсутствующего поля.
{
  printf '# 005 — критик на ВЕРХНЕМ уровне чужого семейства\n\n'
  printf 'дата: 2026-08-17\n'
  printf 'вопрос: каким семейством закрыт критик\n'
  printf 'решение: верхний уровень чужого семейства\n'
  printf 'основание: 93fc601\n'
  printf 'условие пересмотра: смена семейства\n'
} > "$R/decisions/005-kritik-na-top.md"
commit_all "$R" 'в записи 005 удалено поле область'
"$BARRIER" "$R"
