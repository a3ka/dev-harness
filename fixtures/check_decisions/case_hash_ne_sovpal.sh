# ПРИЧИНА: не совпадает с хешем таблицы контракта
#
# Для 005-007 таблица контракта требует совпадения с НАЗВАННЫМ хешом (арбитраж
# 4, вопрос 1). Запись с РАЗРЕШИМЫМ, но другим хешом — остаточный обход,
# закрытый строковым сравнением. Без него основание «любой существующий
# коммит» проходило бы, и реестр терял привязку к источнику.
#
# Зелёный контроль: хеши совпадают → 0. Красное: 006 ссылается на другой
# существующий коммит.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

# Заменяем основание 006 на другой существующий коммит (`8ed63e6` —
# легитимный, но не тот, что в таблице).
{
  printf '# 006 — у критика ПОРОГ\n\n'
  printf 'дата: 2026-08-17\n'
  printf 'вопрос: когда блокировать\n'
  printf 'решение: только с названным обходом\n'
  printf 'основание: 8ed63e6\n'
  printf 'область: роль critic\n'
  printf 'условие пересмотра: слово владельца\n'
} > "$R/decisions/006-porog-kritika.md"
commit_all "$R" 'основание 006 заменено на другой коммит'
"$BARRIER" "$R"
