# ПРИЧИНА: барьер ответил «нечем проверить» (код 2)
#
# Находка адверсария: игрушка сначала отвечала 2/NOT_IMPLEMENTED, потому что фикстура отняла
# у неё инструмент, и лишь потом проверила бы уже внесённое нарушение. Прогон отдавал 2
# вместо 1 — настоящий отказ прятался под «нечем прогнать».
#
# Закрыто отменой самой поблажки: код 2 от барьера — это провал ФИКСТУРЫ, потому что дать
# барьеру инструменты — её работа. «Нечем проверить» остаётся законным только для всего
# прогона целиком (нет каталога `scripts/`, нет барьеров), где мерить действительно нечего.
set -euo pipefail
. "$(dirname "$0")/_fake_root.sh"
fake_root "$WORK"
"$BARRIER" "$WORK"
set_toy "$WORK" kod2
"$BARRIER" "$WORK"
