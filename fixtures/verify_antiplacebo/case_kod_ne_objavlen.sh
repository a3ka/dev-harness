# ПРИЧИНА: которого не объявлял
#
# Находка адверсария: заглушка не проверяла предмет вовсе — на постороннем аргументе она
# печатала «команда не найдена» и выходила 127, а фикстура объявляла этот текст причиной.
# Неудача ЗАПУСКА не является отказом по предмету, и различать их обязан механизм: барьер
# сам перечислил в шапке коды, с которыми имеет право выходить, и код вне перечня — не
# вердикт, а авария.
set -euo pipefail
. "$(dirname "$0")/_fake_root.sh"
fake_root "$WORK"
"$BARRIER" "$WORK"
set_toy "$WORK" kod127
"$BARRIER" "$WORK"
