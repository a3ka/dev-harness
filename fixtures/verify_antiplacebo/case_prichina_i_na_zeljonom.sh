# ПРИЧИНА: печатается и на ЗЕЛЁНОМ прогоне
#
# Находка адверсария: заглушка печатала «e» ВСЕГДА — и краснея, и на зелёном прогоне, — и
# объявляла «e» своей причиной. Проверка это принимала, то есть «названная причина» не
# идентифицировала ни отказ, ни предмет.
#
# Закрыто не порогом на длину строки (порог, взятый из головы, отстанет от следующей
# заглушки), а положительным контролем: причина обязана РАЗЛИЧАТЬ зелёный прогон и красный.
set -euo pipefail
. "$(dirname "$0")/_fake_root.sh"
fake_root "$WORK"
"$BARRIER" "$WORK"
set_toy "$WORK" vsegda
"$BARRIER" "$WORK"
