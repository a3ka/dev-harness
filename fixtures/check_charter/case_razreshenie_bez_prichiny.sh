# ПРИЧИНА: без причины
#
# Разрешение без записанной причины неотличимо от опечатки: через полгода никто не вспомнит, чем
# правка устава была мотивирована, а спросить будет уже не у кого. Тот же порог, что у заморозки:
# «заморозка без записанной причины неотличима от опечатки».
#
# Зелёный контроль: устав не тронут → 0. Красное: путь назван, причина пуста.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

printf 'шаг 7 начат\n' >> "$R/ROADMAP.md"
commit_body "$R" <<'MSG'
правка роадмапа с разрешением без причины

РАЗРЕШИЛ-ВЛАДЕЛЕЦ: ROADMAP.md
MSG
"$BARRIER" "$R"
