# ПРИЧИНА: изменён без разрешения владельца
#
# План становится уставным с ПЕРВОЙ заморозки — не раньше: черновик пишется правками, и гейт,
# краснеющий на черновике, заставил бы морозить недописанное. После заморозки правка требует и
# нового вердикта критика (это держит `check_contract_frozen.sh`), и слова владельца (это держит
# здешний барьер). Два разных предмета, два разных механизма.
#
# Зелёный контроль: заморожённый план не тронут → 0. Красное: правка без строки разрешения.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

printf 'критерий уточнён по ходу\n' >> "$R/plans/001-p.md"
commit_all "$R" 'уточнение критерия в замороженном плане'
"$BARRIER" "$R"
