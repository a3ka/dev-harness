# ПРИЧИНА: Н-4: статус не объявлен
#
# Инвариант 2: заголовок записи без единой бэктик-группы — «статус не объявлен»,
# fail-closed (незакрытое = ОТКРЫТО по шапке файла; запись вовсе без статуса зелёной
# быть не может). Живой прецедент — А-16/А-17 в NABLIUDENIA_ARCHITECT.md (§Материал).
set -euo pipefail
R="$WORK/repo"
mkdir -p "$R"

# Зелёный контроль: статус объявлен (закрытая запись без адреса — не обратная сила)
printf '### Н-4. Боль `ЗАКРЫТО 013`\n' > "$R/NABLIUDENIA.md"
"$BARRIER" "$R"

# Красное: заголовок без бэктиков — статус не объявлен
printf '### Н-4. Боль без статусного маркера\n' > "$R/NABLIUDENIA.md"
"$BARRIER" "$R" || true
