# ПРИЧИНА: причина пуста
#
# Объявление без причины неотличимо от опечатки — тот же порог, что у заморозки и у строки
# разрешения владельца. «Работа не раздаётся» без причины через полгода нечем проверить: раздавали
# по нему или нет, уже не спросить.
#
# Зелёный контроль: отказ с причиной → 0. Красное: та же строка с пустым остатком.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R" 'РАБОТА НЕ РАЗДАЁТСЯ: кодификация действующего соглашения'
"$BARRIER" "$R"

printf '# контракт 002\n\nРАБОТА НЕ РАЗДАЁТСЯ:\n' > "$R/contracts/002-y.md"
printf 'accept\nвердикт\n' > "$R/verdicts/critic/contracts-002-v1.md"
commit_all "$R" 'контракт 002 с отказом без причины'
g "$R" tag -a frozen/contracts/002/1 -m 'заморожен'
"$BARRIER" "$R"
