# ПРИЧИНА: шаг вне разбираемого подмножества: heredoc
#
# Предписано арбитражем прямым текстом: шаг с heredoc без исключения — красное с текстом «вне
# подмножества» и БЕЗ выдуманных «команд» вроде «EOF». Прежде барьер резал тело heredoc на
# строки и объявлял их командами: это было ЛОЖНОЕ КРАСНОЕ с ложным диагнозом, а такому красному
# перестают верить так же, как ложному зелёному. Потом его научили тело пропускать — но тогда
# heredoc стал ещё одним способом спрятать запуск.
#
# Правда третья: механизм heredoc не разбирает и говорит об этом. Выход у автора есть всегда —
# переписать шаг простыми командами либо объявить исключение с причиной.
set -euo pipefail
. "$(dirname "$0")/_fake_root.sh"
fake_root "$WORK"
"$BARRIER" "$WORK"
cat > "$WORK/.github/workflows/ci.yml" <<'YAML'
name: CI
on: [push]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Шаг с heredoc
        run: |
          cat <<'EOF'
          npm run check:existing
          EOF
YAML
"$BARRIER" "$WORK"
