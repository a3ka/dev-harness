# ПРИЧИНА: вердикт вне объявленной грамматики
#
# Неразобранное объявление — отказ с названным местом, а не приём по догадке (правило 7 нормы).
# Мусор в первой строке и отказ судьи — РАЗНЫЕ предметы: первый означает сломанный или чужой
# файл, второй — решение. Один диагноз на оба увёл бы автора искать несуществующий спор.
#
# Зелёный контроль: v1 с `accept` → v1. Красное: v2 с первой строкой «ну ок».
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" contracts/001-x.md "accept прочитан" "$R"

put_verdict "$R" 2 'ну ок'
commit_all "$R" 'вердикт v2 вне грамматики'
"$BARRIER" contracts/001-x.md "заморозка по неразобранному вердикту" "$R"
