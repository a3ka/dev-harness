# ПРИЧИНА: без вердикта критика
#
# Требование владельца дословно: план от архитектора обязан проходить проверку критиком. Здесь оно
# в механической форме — заморозка версии v невозможна без `verdicts/critic/<каталог>-<NNN>-v<v>.md`
# на HEAD. Памяти автора для этого недостаточно: она и была тем механизмом, который семь кругов
# шага 5 не остановил.
#
# Зелёный контроль: вердикт v1 есть → v1. Красное: вердикта v2 нет, а заморозить просят.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" contracts/001-x.md "вердикт v1 лежит на HEAD" "$R"

"$BARRIER" contracts/001-x.md "вторая версия без гейта" "$R"
