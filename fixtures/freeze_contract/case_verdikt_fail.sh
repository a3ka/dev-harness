# ПРИЧИНА: критик заморозку не разрешил
#
# Существование файла вердикта — НЕ гейт. Первая редакция плана 007 требовала лишь наличия файла,
# и тогда автор мог закоммитить отказ критика и заморозить план поверх него: механизм считал бы
# файлы, не читая их. Первая строка вердикта — объявленная грамматика роли, значит она читается.
#
# Зелёный контроль: v1 с `accept` → v1. Красное: v2 с первой строкой `FAIL`.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" contracts/001-x.md "accept прочитан" "$R"

put_verdict "$R" 2 FAIL
commit_all "$R" 'вердикт v2: FAIL'
"$BARRIER" contracts/001-x.md "заморозка поверх отказа" "$R"
