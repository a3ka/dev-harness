#!/usr/bin/env bash
# Р7 (green) — буллет содержит внутренний знак «-» и двоеточие «:» ВНУТРИ
# текста (не только как префикс/метку) → не усечён, полная секция валидна,
# rc 0 (Направление 3, инвариант ii: представление без переиспользования
# байта данных разделителем — построчные массивы, не join+split строкой).
set -uo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
. "$HERE/_toy.sh"

W="$(mktemp -d "${TMPDIR:-/tmp}/tm_case07.XXXXXX")"; trap 'rm -rf "$W"' EXIT
F="$W/c.md"
put_contract "$F" '# kontrakt

## Predmet
p

## Модель угроз

ЗАЩИЩАЕТ:
- разбор-буллета с внутренним знаком - и двоеточием: byte-in-data остаётся данными а не разделителем

НЕ ЗАЩИЩАЕТ:
- враждебное окружение вызывающего процесса за пределами базового PATH-экспорта'
run_barrier "$W" 'c.md'
accept 'case_07'
exit 0
