# Красное предъявление 2/2 семьи verify_consultant (контракт 029, решение владельца Q2):
# ДЕКОЙ ВТОРОГО ПОРЯДКА — консультант, который ИМИТИРУЕТ форму основания. Тройки на месте,
# грамматика безупречна, числа выдуманы. Отличить его от честного можно ровно одним
# способом: барьер ПЕРЕИСПОЛНЯЕТ названные команды сам и сверяет rc и sha со СВОИМ
# результатом (оракул в памяти проверяющего, не на диске проверяемого).
#
# ВТОРОЙ класс обхода, предъявленный критиком в круге 1: РАЗРЕШЁННЫЙ ГЛАГОЛ НЕ ДЕЛАЕТ
# КОМАНДУ READONLY. `git diff --output=<файл>` возвращает честные rc 0 и sha ПУСТОГО
# stdout+stderr — сверка проходит, а файл создан (в замере критика 132 байта). Поэтому
# перечень ОПЦИЙ закрыт так же, как перечень глаголов, а запуск скриптов из ПРОВЕРЯЕМОГО
# дерева убран вовсе: glob-имя `check_*.sh` не удостоверяет содержимое файла.
#
# ВХОД / ПОВЕДЕНИЕ ПОСЛЕ ПРЕДМЕТА (дерево игрушки ГРЯЗНОЕ — untracha.txt untracked):
#   1) заявлен ВЫВОД-SHA256 пустого вывода при грязном `git status --porcelain` →
#      rc 1, отказ называет «расхождение вывода» и команду (инв. 9);
#   2) заявлен RC: 1 там, где команда реально даёт 0 → rc 1 «расхождение rc», отказ
#      печатает ОБА числа — заявленное и полученное (инв. 9);
#   3) команда ВНЕ белого списка (`rm -rf verdicts`) → rc 1 «вне белого списка», и
#      каталог verdicts/ ЦЕЛ: барьер не исполняет произвольное, чтобы «проверить»
#      (инв. 9 — иначе анти-плацебо сам становится исполнителем чужого кода);
#   4) метасимвол в разрешённом префиксе (`git status; rm -rf verdicts`) → rc 1, и
#      каталог verdicts/ ЦЕЛ (ловец белого списка по префиксу вместо разбора);
#   5) ПОЗИТИВНЫЙ КОНТРОЛЬ на ТОМ ЖЕ грязном дереве: честные rc и sha, снятые каркасом
#      в память → rc 0 (грязь дерева сама по себе не преступление; преступление —
#      ЛОЖЬ о ней);
#   6) ЗАПИСЫВАЮЩИЙ АРГУМЕНТ РАЗРЕШЁННОГО ГЛАГОЛА: `git diff --output=<файл>` при
#      РЕАЛЬНОМ непустом diff → rc 1 «опция вне белого списка», и файла в корне НЕТ
#      (инв. 9). Заявленные rc 0 и sha пустого вывода тут ЧЕСТНЫ — сверка чисел этот
#      обход не ловит по построению, ловит только граница аргументов;
#   7) ЗАПУСК СКРИПТА ИЗ ПРОВЕРЯЕМОГО ДЕРЕВА: `bash scripts/check_podlozhnyj.sh`, где
#      скрипт корня пишет файл → rc 1 «вне белого списка», и файла НЕТ (инв. 9:
#      glob-имя не удостоверяет содержимое).
#   8) ЧУЖОЙ КОНФИГ (граница Г2 арбитража): `git diff` БЕЗ опций и метасимволов при
#      `diff.external` проверяемого корня → rc 1 «конфиг вне белого списка» с ИМЕНЕМ
#      КЛЮЧА, и маркера в корне НЕТ. Числа ответа (`RC: 0`, sha ПУСТОГО вывода) честны
#      ПО ПОСТРОЕНИЮ — внешний драйвер молчит, поэтому сверка rc/sha этот обход не
#      ловит вообще; ловит только конфиг-гейт;
#   9) ГОЛЫЙ ХУК (граница Г3): конфиг игрушки ПУСТ (только init-ключи), исполняемый
#      `.git/hooks/post-index-change` пишет маркер; тройка для `git status --porcelain`
#      на грязном дереве ЧЕСТНАЯ → rc 0 И маркера НЕТ. Это ПОЗИТИВНЫЙ контроль,
#      обязанный пройти БЕЗ исполнения хука (замер этой пачки: наивный прогон маркер
#      создаёт, под Г1 и под Г3 — нет). Оракул снимается с БЛИЗНЕЦА-игрушки, чтобы
#      индекс злой игрушки до вызова барьера не трогался;
#  10) СУБМОДУЛЬ (граница Г4): гитлинк при НЕВИННОМ локальном конфиге корня, в конфиге
#      субмодуля `core.fsmonitor=<скрипт-маркер>`, в его hooks — маркер-хук; тройка
#      честная (оракул с близнеца) → rc 1 «субмодуль вне границы» И обоих маркеров НЕТ.
#      Канал недостижим для Г2 (скан корня его не видит) и не закрывается Г3
#      (fsmonitor субмодуля исполняется и под ней — замер арбитража 6/M10).
#
# СТАБ-ПРИВЯЗКИ (Н-39; стаб назван у входа, где его дефект НАБЛЮДАЕМ):
#   S-rconly   — сверяет только RC, вывод не считает: на входе 2 честен, наблюдаем
#                ТОЛЬКО на входе 1 → краснеет ассертом 1;
#   S-shaonly  — сверяет только sha: наблюдаем ТОЛЬКО на входе 2 → ассерт 2;
#   S-trust    — ничего не переисполняет, верит заявленному: наблюдаем на входах 1 и 2;
#   S-exec     — исполняет ЛЮБУЮ названную команду: наблюдаем на входах 3 и 4 —
#                предъявляется не только rc, но и УЦЕЛЕВШИМ каталогом verdicts/;
#   S-prefix   — белый список по префиксу строки без разбора метасимволов: на входе 3
#                честен, наблюдаем ТОЛЬКО на входе 4 → ассерт 4;
#   S-verbonly — перечень ГЛАГОЛОВ без границы аргументов (редакция круга 1): входы 1–5
#                честны, наблюдаем ТОЛЬКО на входе 6 → ассерт 6 (rc и созданный файл);
#   S-globscript — `bash scripts/check_*.sh` оставлен в перечне по glob-имени: на входах
#                1–6 честен, наблюдаем ТОЛЬКО на входе 7 → ассерт 7;
#   S-argvonly — граница argv (редакция круга 2) БЕЗ конфиг-гейта: входы 1–7 честны,
#                наблюдаем ТОЛЬКО на входе 8 → ассерт 8 (rc и СОЗДАННЫЙ маркер);
#   S-nohooks  — Г2 есть, барьерного `-c core.hooksPath=<пустой>` и `GIT_OPTIONAL_LOCKS=0`
#                нет: входы 1–8 честны (там хука нет), наблюдаем ТОЛЬКО на входе 9 →
#                ассерт 9 (маркер хука создан на позитивном контроле);
#   S-nosubmod — Г1–Г3 есть, гитлинк не судится: входы 1–9 честны, наблюдаем ТОЛЬКО на
#                входе 10 → ассерт 10 (rc 0 вместо отказа и маркер fsmonitor субмодуля);
#   S-allred   — «всякий ответ красный»: наблюдаем ТОЛЬКО на входе 5 → ассерт 5.
# На текущем HEAD (барьера нет): rc 127 → ассерт 1.
set -uo pipefail
# shellcheck disable=SC1091
. "$(dirname "$0")/_konsult.sh"

R="$WORK/repo"; igrushka "$R"
printf 'грязь: незакоммиченный файл\n' > "$R/untracha.txt"   # дерево заведомо ГРЯЗНОЕ
FABLE=anthropic/claude-fable-5
SHA_PUSTOGO="$(printf '%s' '' | sha256sum | cut -d' ' -f1)"

# 1) ложный sha: «чисто» при грязном дереве.
D1="$WORK/lozh-sha.md"
otvet_shapka "$D1" tree-status "$FABLE" 'вариант а'
osnovanie "$D1" 'git status --porcelain' 0 "$SHA_PUSTOGO"
zapusk --root "$R" --otvet "$D1"
zhdu_rc 1 'заявлено чистое дерево при грязном — ложь ловится переисполнением (инв. 9)'
zhdu_text 'git status --porcelain' 'отказ обязан называть команду (инв. 9)'
zhdu_text 'вывод' 'отказ обязан называть класс расхождения (инв. 9)'

# 2) ложный rc при верном выводе.
D2="$WORK/lozh-rc.md"
otvet_shapka "$D2" tree-status "$FABLE" 'вариант а'
osnovanie "$D2" 'git status --porcelain' 1 "$(sha_vyvoda "$R" 'git status --porcelain')"
zapusk --root "$R" --otvet "$D2"
zhdu_rc 1 'заявленный rc 1 при реальном 0 (инв. 9)'
zhdu_text 'rc' 'отказ обязан называть класс расхождения (инв. 9)'
zhdu_text '1' 'отказ обязан печатать заявленное число (инв. 9)'
zhdu_text '0' 'отказ обязан печатать и ПОЛУЧЕННОЕ число — обоими числами, а не одним (инв. 9)'

# 3) команда вне белого списка — барьер её НЕ исполняет.
D3="$WORK/vne-spiska.md"
otvet_shapka "$D3" tree-status "$FABLE" 'вариант а'
osnovanie "$D3" 'rm -rf verdicts' 0 "$SHA_PUSTOGO"
zapusk --root "$R" --otvet "$D3"
zhdu_rc 1 'команда вне белого списка readonly-глаголов (инв. 9)'
zhdu_text 'вне белого списка' 'отказ обязан называть класс нарушения (инв. 9)'
[ -d "$R/verdicts" ] || {
  printf 'ОТКАЗ: барьер ИСПОЛНИЛ «rm -rf verdicts» — анти-плацебо стал исполнителем чужого кода (инв. 9)\n' >&2
  exit 1; }

# 4) метасимвол под разрешённым префиксом.
D4="$WORK/metasimvol.md"
otvet_shapka "$D4" tree-status "$FABLE" 'вариант а'
osnovanie "$D4" 'git status; rm -rf verdicts' 0 "$SHA_PUSTOGO"
zapusk --root "$R" --otvet "$D4"
zhdu_rc 1 'белый список обязан судить РАЗОБРАННУЮ команду, не префикс строки (инв. 9)'
[ -d "$R/verdicts" ] || {
  printf 'ОТКАЗ: барьер исполнил хвост после «;» — префиксный белый список дырав (инв. 9)\n' >&2
  exit 1; }

# 5) позитивный контроль на том же грязном дереве.
H="$WORK/chestnyj-grjaznyj.md"
otvet_shapka "$H" tree-status "$FABLE" 'вариант а'
osnovanie_chestnoe "$H" "$R" 'git status --porcelain'
osnovanie_chestnoe "$H" "$R" 'git log -1 --format=%H'
zapusk --root "$R" --otvet "$H"
zhdu_rc 0 'честный отчёт о ГРЯЗНОМ дереве обязан проходить (инв. 9)'

# 6) ЗАПИСЫВАЮЩИЙ АРГУМЕНТ разрешённого глагола. Игрушка своя: tracked-файл изменён, то
#    есть `git diff` даёт НЕПУСТОЙ патч, и исполнение команды создало бы файл в корне.
#    Числа в ответе ЧЕСТНЫ: stdout+stderr пусты, rc 0 — сверка чисел обход не ловит.
R6="$WORK/repo-pishushchij"; igrushka "$R6"
printf 'строка, добавленная после коммита\n' >> "$R6/README.md"
PISHET='zapisano-razreshjonnym-glagolom.txt'
D6="$WORK/pishushchij-argument.md"
otvet_shapka "$D6" tree-status "$FABLE" 'вариант а'
osnovanie "$D6" "git diff --output=$PISHET" 0 "$SHA_PUSTOGO"
zapusk --root "$R6" --otvet "$D6"
zhdu_rc 1 'записывающий аргумент разрешённого глагола (инв. 9)'
zhdu_text 'опция вне белого списка' 'отказ обязан называть класс нарушения — граница аргументов (инв. 9)'
if [ -e "$R6/$PISHET" ]; then
  printf 'ОТКАЗ: барьер исполнил «git diff --output=» — разрешённый глагол ПИСАЛ в дерево (%s байт), readonly-обещание инв. 9 нарушено\n' \
         "$(wc -c < "$R6/$PISHET")" >&2
  exit 1
fi

# 7) ЗАПУСК СКРИПТА ИЗ ПРОВЕРЯЕМОГО ДЕРЕВА: имя по glob `check_*.sh`, содержимое — запись.
R7="$WORK/repo-skript"; igrushka "$R7"
mkdir -p "$R7/scripts"
SLED='pobochnyj-effekt-chuzhogo-skripta.txt'
{
  printf '#!/usr/bin/env bash\n'
  printf 'printf "побочный эффект\\n" > %s\n' "$SLED"
} > "$R7/scripts/check_podlozhnyj.sh"
chmod 755 "$R7/scripts/check_podlozhnyj.sh"
D7="$WORK/chuzhoj-skript.md"
otvet_shapka "$D7" tree-status "$FABLE" 'вариант а'
osnovanie "$D7" 'bash scripts/check_podlozhnyj.sh' 0 "$SHA_PUSTOGO"
zapusk --root "$R7" --otvet "$D7"
zhdu_rc 1 'скрипт из ПРОВЕРЯЕМОГО дерева запускать нечем удостоверить (инв. 9)'
zhdu_text 'вне белого списка' 'отказ обязан называть класс нарушения (инв. 9)'
if [ -e "$R7/$SLED" ]; then
  printf 'ОТКАЗ: барьер запустил scripts/check_podlozhnyj.sh из проверяемого дерева — glob-имя выдано за удостоверение readonly (инв. 9)\n' >&2
  exit 1
fi

# 8) ЧУЖОЙ КОНФИГ проверяемого корня (Г2). Опций нет, метасимволов нет, argv целиком
#    разрешён редакцией круга 2 — и этого достаточно для исполнения чужого скрипта.
#    Фикстура САМА команду не прогоняет: прогон создал бы маркер до вызова барьера, а
#    числа тут честны по построению (внешний драйвер молчит → пустой вывод, rc 0).
R8="$WORK/repo-chuzhoj-konfig"; igrushka "$R8"
printf 'строка, добавленная после коммита\n' >> "$R8/README.md"   # непустой git diff
MARKER_KONFIG='wrote-via-external-diff.txt'
vneshnij_diff "$R8" "$MARKER_KONFIG" "$WORK/vne-kornja-8"
D8="$WORK/chuzhoj-konfig.md"
otvet_shapka "$D8" tree-status "$FABLE" 'вариант а'
osnovanie "$D8" 'git diff' 0 "$SHA_PUSTOGO"
zapusk --root "$R8" --otvet "$D8"
zhdu_rc 1 'конфиг проверяемого корня исполняет чужой код при полностью разрешённом argv (Г2)'
zhdu_text 'конфиг вне белого списка' 'отказ обязан называть класс нарушения (Г2)'
zhdu_text 'diff.external' 'отказ обязан называть КЛЮЧ конфига (Г2)'
if [ -e "$R8/$MARKER_KONFIG" ]; then
  printf 'ОТКАЗ: барьер исполнил diff.external проверяемого корня — чужой код запущен при честных rc/sha (%s байт), граница Г2 не стоит\n' \
         "$(wc -c < "$R8/$MARKER_KONFIG")" >&2
  exit 1
fi

# 9) ГОЛЫЙ ХУК при ПУСТОМ конфиге (Г3) — ПОЗИТИВНЫЙ контроль: честная тройка обязана
#    пройти, но БЕЗ исполнения хука. Оракул снимается с БЛИЗНЕЦА (та же грязь, хука нет):
#    прогон в злой игрушке освежил бы её индекс и сам бы дёрнул хук.
R9="$WORK/repo-golyj-krjuk"; igrushka "$R9"
B9="$WORK/repo-bliznec";    igrushka "$B9"
printf 'грязь: незакоммиченный файл\n' > "$R9/untracha.txt"
printf 'грязь: незакоммиченный файл\n' > "$B9/untracha.txt"
MARKER_KRJUK='marker-golyj-krjuk.txt'
krjuk_marker "$R9" post-index-change "$MARKER_KRJUK"
D9="$WORK/golyj-krjuk.md"
otvet_shapka "$D9" tree-status "$FABLE" 'вариант а'
osnovanie "$D9" 'git status --porcelain' \
          "$(rc_komandy "$B9" 'git status --porcelain')" \
          "$(sha_vyvoda "$B9" 'git status --porcelain')"
zapusk --root "$R9" --otvet "$D9"
zhdu_rc 0 'честная тройка о грязном дереве обязана проходить и при наличии хука (Г3)'
if [ -e "$R9/$MARKER_KRJUK" ]; then
  printf 'ОТКАЗ: барьер дёрнул .git/hooks/post-index-change проверяемого корня — чужой код исполнен при ПУСТОМ конфиге, граница Г3 не стоит\n' >&2
  exit 1
fi

# 10) СУБМОДУЛЬ (Г4): конфиг КОРНЯ невинен (ровно init-ключи), канал живёт в
#     конфиг-пространстве субмодуля — для скана корня недостижим, и Г3 его не закрывает.
#     Оракул снимается с БЛИЗНЕЦА-суперпроекта той же постройки, без злых настроек.
R10="$WORK/repo-submodul";  superproekt "$R10" "$WORK/sub-istochnik-10"
B10="$WORK/repo-submodul-bliznec"; superproekt "$B10" "$WORK/sub-istochnik-10b"
MARKER_FSMON='marker-submodul-fsmonitor.txt'
MARKER_SUBKRJUK='marker-submodul-krjuk.txt'
zlo_submodulja "$R10" "$MARKER_FSMON" "$MARKER_SUBKRJUK" "$WORK/vne-kornja-10"
D10="$WORK/submodul.md"
otvet_shapka "$D10" tree-status "$FABLE" 'вариант а'
osnovanie "$D10" 'git status --porcelain' \
          "$(rc_komandy "$B10" 'git status --porcelain')" \
          "$(sha_vyvoda "$B10" 'git status --porcelain')"
zapusk --root "$R10" --otvet "$D10"
zhdu_rc 1 'гитлинк обязан отвергаться ДО переисполнения — конфиг субмодуля вне досягаемости скана (Г4)'
zhdu_text 'субмодуль вне границы' 'отказ обязан называть класс нарушения (Г4)'
zhdu_text 'sub' 'отказ обязан называть путь гитлинка (Г4)'
for mm in "$MARKER_FSMON" "$MARKER_SUBKRJUK"; do
  if [ -e "$R10/$mm" ]; then
    printf 'ОТКАЗ: барьер переисполнил команду в суперпроекте — сработал %s из конфиг-пространства субмодуля, граница Г4 не стоит\n' "$mm" >&2
    exit 1
  fi
done
