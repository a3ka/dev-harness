# ПРИЧИНА: изменён без новой заморозки
#
# Предмет барьера целиком: то, по чему раздали работу, обязано остаться тем, что лежит в дереве.
# Ровно этот результат стоил семи кругов на шаге 5 — спека и красные критерии менялись во время
# исполнения, и каждая смена обесценивала сданное.
#
# Зелёный контроль: контракт заморожен и не тронут → 0. Красное: одна строка дописана и
# закоммичена без новой заморозки.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
g "$R" tag -a frozen/contracts/001/1 -m 'заморожен'
"$BARRIER" "$R"

printf 'уточнение по ходу\n' >> "$R/contracts/001-x.md"
commit_all "$R" 'мелкая правка замороженного контракта'
"$BARRIER" "$R"
