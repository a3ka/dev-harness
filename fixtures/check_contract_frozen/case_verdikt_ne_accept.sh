# ПРИЧИНА: поверх вердикта, который её не разрешил
#
# Существование файла вердикта — не гейт: читать надо первую строку. Здесь диагноз ОДИН на все
# значения, отличные от `accept`, и это осознанно — разводить `FAIL`, `ESCALATE` и мусор обязан
# ПИСАТЕЛЬ, который решает, ставить ли тег. Читателю тег уже дан, и его предмет — что тег не имел
# права появиться.
#
# Зелёный контроль: вердикт v1 с `accept` → 0. Красное: тот же вердикт переписан на `FAIL`.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
g "$R" tag -a frozen/contracts/001/1 -m 'заморожен'
"$BARRIER" "$R"

put_verdict "$R" 1 FAIL
commit_all "$R" 'вердикт переписан на отказ уже после заморозки'
"$BARRIER" "$R"
