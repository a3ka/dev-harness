# ПРИЧИНА: объявление вне грамматики
#
# Неразобранное объявление — отказ с названным местом, а не молчаливое исключение из области
# (правило 7 нормы). Файл в `contracts/` без номера версией заморозки не обладает вовсе: у него
# нет `NNN`, значит нет и тега, значит он молча выпал бы из проверки — и именно так область и
# сужается незаметно.
#
# Зелёный контроль: только валидные имена → 0. Красное: добавлен `contracts/draft.md`.
set -euo pipefail
. "$(dirname "$0")/_repo.sh"
R="$WORK/repo"
make_repo "$R"
"$BARRIER" "$R"

printf 'черновик без номера\n' > "$R/contracts/draft.md"
commit_all "$R" 'файл вне грамматики в contracts/'
"$BARRIER" "$R"
