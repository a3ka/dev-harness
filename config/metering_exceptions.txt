# Исключения учёта: НАЗНАЧЕННЫЕ провайдеры, которым разрешено идти МИМО прокси учёта.
#
# Формат: `<провайдер> = <непустая причина>`, как у config/ci_parity_exceptions.txt. Читает
# САМ барьер (`scripts/overlay.sh`, блок «учёт расхода»), любая правка проявляется на прогоне
# без перекомпиляции.
#
# Правило, из которого файл существует: провайдер, назначенный в .omp/config.yml, обязан либо
# идти через прокси учёта (.omp/models.yml, baseUrl = METERING_PROXY_URL), либо иметь записанную
# здесь причину. Молчаливая дыра запрещена: расход, который нигде не считается, обнаруживается
# по счетам, а не механизмом.
#
# ──────────────────────────────────────────────────────────────────────────────
# Подписка Coding Plan. Маршрута z.ai в прокси учёта фабрики нет (прокси поднимает ПРОЕКТ под
# свои провайдеры), расход виден в дашборде z.ai: 5-часовой и недельный лимиты кредитов.
# Пересмотр: появится маршрут в прокси — строка удаляется, и учёт становится сквозным.
zai = подписка Coding Plan: маршрута в прокси фабрики нет, расход виден в лимитах кредитов z.ai (5ч/нед), потолок ставит сам провайдер
